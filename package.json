{
  "name": "gmo-onair",
  "version": "0.3.9",
  "private": true,
  "description": "GMO ONAiR - 統合業務管理システム",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "build:render": "cd client && npx vite build && cd ../server && npx tsc && mkdir -p dist/shared/db && cp -r src/shared/db/migrations dist/shared/db/",
    "start:render": "node server/dist/index.js",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run db:seed -w server",
    "db:reset": "npm run db:reset -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
