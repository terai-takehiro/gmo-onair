node_modules/
dist/
*.db
*.sqlite
.DS_Store
*.log
.vite/

# Dev Basic Auth (VPS上でのみ作成)
deploy/dev-htpasswd

# Environment & secrets - NEVER commit these
.env
.mcp.json
.env.local
.env.production
.env.*.local
*.pem
*.key
credentials.json
service-account.json

# TypeScript build info
*.tsbuildinfo

# k6 負荷試験の生成物
scripts/loadtest/results/

# 経理データ取込み検証用 CSV — 機微情報 (社員名・取引金額) を含むため
# git には push しない。Box (folder 381450796408) から都度取得。
docs/accounting-import/samples/
