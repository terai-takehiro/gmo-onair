# client — 案件管理・財務管理・カレンダー・設定（v4 対象）

ONAiR で一番大きいアプリ。**1つの Vite バンドルに4つ（v4 では5つ）の「入口」が入っている**。
ベースパス `/`・ポート 5173。44画面・約36,000行。

## 入口とルート接頭辞

| 入口（利用者から見た名前） | ルート | 権限モジュール | contexts |
| --- | --- | --- | --- |
| 案件管理 | `/sales/*` | `sales` | `contexts/sales`, `contexts/tasks` |
| 財務管理 | `/budget/*` | `budget` | `contexts/finance` |
| カレンダー | `/studio/*` | `studio` / `partner_schedule` | `contexts/production` |
| 設定 | `/settings/*`（**v4 で `/admin/*` から改名**・旧 URL は転送） | `admin` | `contexts/platform` |
| **プロジェクト管理（v4 で新規）** | `/gpm/*` | `gpm` | `contexts/gpm`（新設） |

- ルート定義は `src/App.tsx` の1ファイル。**旧URLからの転送表もここ**（`<Navigate>` 約12本）
- **入口の URL（`/sales` など）はその入口のダッシュボードへ送る。** アプリ切替と
  トップページのタイルがここに来るので、案件一覧に送っていると
  「トップを押してもダッシュボードにならない」ことになる（v4 で直した）
- 左メニューの定義は `src/components/layout/nav.ts` の `CLIENT_NAV`（入口ごとの塊）。
  **案件管理だけ v4 の情報設計に差し替え済み**（下記）
- ページは `contexts/<領域>/pages/` に置く。`contexts/shared/components/` は領域をまたぐ部品

## 案件のライフサイクル（旧「統合プロジェクトライフサイクル」）

- 旧 `opportunities` テーブルは廃止し、**単一 `projects` テーブル**に統合済み
- `stage`: `neta` → `d_hold` → `c_proposal` → `b_verbal` → `a_won` → `s_completed` / `e_lost`
- **`gls_number IS NULL` = ヨミ段階、`IS NOT NULL` = GLS発番済み**
- GLS発番は別エンドポイント: `POST /projects/:id/issue-gls`
- 案件分類は `gls_category`（`A`=スタジオ / `B`=ビジネス）。発番後の A↔B 切替は
  `PATCH /projects/:id/gls-category`（採番し直し＋エピソードコード＋BOX フォルダを追随）
- 費用按分は `project_groups` テーブル

## v4 で作り直した画面（済み）

| 画面 | 置き場所 | 決めたこと |
| --- | --- | --- |
| ① ダッシュボード | `contexts/platform/pages/DashboardPage.tsx` ＋ `pages/salesDashboard/` | 下記 |
| ② 案件作成（**受付を統合**） | `contexts/sales/pages/projectNew/` | 下記 |
| ③ 案件一覧 | `contexts/sales/pages/ProjectListPage.tsx` ＋ `pages/projectList/` | 下記 |
| ④ タスク一覧 | `contexts/tasks/pages/TaskDashboardPage.tsx` ＋ `pages/taskList/` | 下記 |
| ⑥ 案件詳細（ふりかえり以外の7タブ） | `contexts/sales/pages/ProjectDetailPage.tsx` ＋ `pages/projectDetail/` | 下記 |
| ⑤ 見積・請求（全案件） | `contexts/sales/pages/BillingListPage.tsx` ＋ `pages/billing/` | 下記 |
| ⑧ 料金表 | `contexts/sales/pages/PricingListPage.tsx` ＋ `pages/pricing/` | 下記 |
| ⑦ 標準工程テンプレート | `contexts/sales/pages/flow/` | 下記 |
| カレンダー ①〜④ | `contexts/production/pages/{UnifiedCalendarPage,RoomAvailabilityPage,HoldListPage,CalendarSettingsPage}.tsx` ＋ `pages/{holds,rooms,calendarSettings}/` | 下記 |
| 設定 ①② ＋ システムの情報 | `contexts/platform/pages/{SettingsHubPage,SitesPage,SystemInfoPage}.tsx` ＋ `pages/settings/` | 下記 |
| 財務 ③④⑤ 売上・仕入・販管費 | `contexts/finance/pages/{Revenue,Purchase,Sga}ListPage.tsx` ＋ `pages/ledger/` | 下記 |
| 財務 ⑥ 受け取った書類 | `contexts/finance/pages/DocumentsPage.tsx` ＋ `pages/documents/` | 下記 |
| 財務 ⑦ 取り込み | `contexts/finance/pages/ImportPage.tsx` ＋ `pages/import/` | 下記 |

**ダッシュボード（①）で決めたこと**
- **お金の画面をやめて、案件の画面にした。** 旧実装は売上・仕入・粗利・販管費・営業利益の
  KPI と月次推移のグラフで、**中身は財務管理ダッシュボード（`/budget/dashboard`）と同じもの**
  だった。案件を見に来た人が決算の数字を1画面ぶんスクロールしていたので外した。
  案件管理から見たいお金は**「見積の返事待ち」と「今月の売上」の2つだけ**に絞り、
  どちらも押すと元の画面へ行ける
- **「今月の受注」ではなく「今月の売上（確定）」を出す。** モックは受注額だが、
  **DB がステージの変更日を記録していない**ので「今月 受注になった案件」を数えられない
  （`projects.updated_at` は名前を直しただけでも動く）。数えられないものをそれらしく
  出さない。履歴を残すようにしたら差し替える
- **数字は `GET /dashboard/sales-overview` の1本でまとめて取る**
  （`contexts/platform/services/salesOverview.service.ts`）。5本に分けると遅い1本のせいで
  数字が後から差し替わり、読み間違える。ステージ別・今週の現場・動いている案件は
  **既にある物を使う** — 同じ数字を2か所で数えると必ず食い違う
- **「動いている案件」は案件一覧と同じ `GET /projects`** を `sort_by=last_move` で並べ替えただけ。
  そのために `SORT_COLUMN_MAP` に `last_move` を足した（SELECT の別名をそのまま使う。
  式を書き写すと片方だけ直したとき並び順と表示が食い違う）
- **「自分の案件」にしない。** v4 は案件担当者という概念を持たないので、
  誰がやるかは**次のアクションの担当**として1行ごとに出す
- **「止まっている」理由はステージから導く。** モックは「見積を送ったまま連絡がありません」と
  具体的に書くが、それを言うにはステージ変更の履歴が要る。**作り話をしない**
- **受付カードは動く入口だけ出す。** モックは3タイル（メール取込・録音・手で登録）だが、
  **録音はまだ無い**。押しても何も起きないボタンを置くと「壊れている」と受け取られ、
  以後この枠全体が信用されなくなる。無いものは無いと書く
- `salesDashboard/Panel.tsx` は**この画面だけの部品**。`dashboard/SectionCard` は v4 より前の段
  （`rounded-lg` = 8px・見出しの下に区切り線）で、まだ作り直していない画面が使っている

**ダッシュボード（①）— 数字の書き方を揃えた回**
- **単位を数字と別に描く**（ご指示）。「860万円」を1つの文字列にすると**単位まで 22px で
  描かれ**、5つ並べたときに数字の桁が縦にそろわない。**数字だけを大きく、単位は 12px の
  グレー**にして5つとも同じ形にした。**カウントアップも数字にだけ効く**（文字列では効かない）
- **万円に丸めるのは `toMan` 1本。** ここで割り算を書くと、同じ金額が画面によって
  違う値に丸められる
- **ステージ別の札は記号つきの正式名**（`ProjectStageLabels`）。一覧のチップから
  「E 問合せ」を外したので、チップの表から引くと**ネタだけ名前が見つからず生の `neta`**
  が画面に出ていた。ここは内訳の表なので5段すべてを記号つきで並べるのが正しい
- **ボードの列名も記号つき。** 行の中のバッジは和文だけ（均等割り付けのため）だが、
  列の見出しは幅が広く、受注に近い順であることを記号が示す
- **ネタの行き先だけ `?view=seed`。** 一覧の「すべて」にネタを出していないので、
  `?stage=neta` で送ると 0 件の一覧に着く
- **トップの「今日」の並び（今日の予定 → 自分のやること → お待たせ中）は前の回のまま。**
  ご指示の並びと一致しているので触っていない

**トップページ（共通）で決めたこと**
- **上＝アプリの入口 ／ 下＝自分の今日**に絞った（`pages/HomePage.tsx` ＋ `pages/home/`）。
  1,273行 → 177行。旧トップは KPI・営業ダッシュボード・AI活動フィード・直近の案件・
  クイックアクセスまで載っていて、**同じ数字を各アプリのダッシュボードでも見ていた**
  （トップの KPI は財務ダッシュボードと同じ中身）。行き先は `HomePage.tsx` の冒頭に列挙
- **アプリは2段。** 日々の業務（3列の大きいタイル）と**イベントで使うもの**
  （本番の日にだけ開く・4列）。毎日の入口と本番だけの入口を混ぜると探しにくい
- **タイルの数字は「押せば片づくもの」の件数。** そのアプリにある物の総数ではない
  （総数は押す理由にならない）。**数えられないアプリには数字を出さない**
- **同じものを2か所で数えない。** 案件管理・日常業務は受信箱（`/dashboard/inbox`）が
  数えているものを振り分けて使い、財務・カレンダー・機材だけを
  `/dashboard/app-badges`（新設）で数える
- **`/dashboard/app-badges` は `sales` の権限ゲートの外に置く。** `/dashboard` の
  ルーターは全ルートに `sales` を要求するが、**トップは全員が最初に開く画面**で
  経理だけ・機材だけの人も来る。中に置くとその人たちのトップで 403 になる
  （実際に 403 を確認してから外に出した）
- **数えられないときは書かない。** `sales` も `dailyops` も無い人に「0件です」と書くと、
  見えていないだけなのに「無い」と言い切ることになる。その1行ごと出さない
- **プロジェクト管理のタイルは出さない。** モックには載っているが画面がまだ無い
- **中身が1枚も無いなら「今日」の節ごと出さない**（見出しだけ残ると壊れて見える）
- **「AI に任せる」は既存の `TaskIntakeBox` をそのまま置いた。** LLM 解析 ＋ 規則ベースの
  フォールバックを持つ動いている部品なので、枠の作り直しと同時に中身を触らない
- **お待たせ中は受付（②）と同じ `inbox/kinds.ts` を使う。** 見出しの作り方を書き写すと、
  片方だけ直したときトップと受付で表記が食い違う
- **スマホの入口は破線を使わない**（M8）。破線は「まだ中身が無い／ここに落とす」の
  印なので、動いている入口が未完成に見える。淡い青の面と青い文字だけで入口だと伝わる
  （青いバーの中の2枚もこれに合わせてある）
- ~~**スマホでは AI の入口を1行に畳む**（M7・`home/MobileIntake.tsx`）~~ →
  **モックの青いバーに置き換えた**（下記「モックとの差を詰めた回」）。
  畳む考え方（場所は最上部のまま・大きさだけ小さく）と**開いたら閉じない**は
  そのまま引き継いでいる
- **スマホでは挨拶に名前を出さない**（M7）。自分の端末なので誰かは分かっており、
  「おはようございます、○○ さん」は 390px で2行になって 140px 使っていた
- **「自分のタスクを全部ひらく」はスマホで出さない**（M7）。`MyTasksCard` の
  「全部ひらく」と**同じ行き先で二重**だった
- **スマホでは「今日」を先に出す**（M3 / M7）。375px では自分のタスクに着くまで
  **2,273px → 411px** になった（実測）。**モックの並び（アプリ → 今日）とは違う**が、
  **利用者からの差分の指摘に並びは入っていない**（指摘はタイルの形とピル）ので、
  測って選んだこちらを残している。**並べ替えるだけで、消していない**

**トップページ — 投入口を1本化した回（この版・モック `v4-live`）**
- **挨拶は姓だけ**（「おはようございます、寺井さん」）。氏名の空白（半角・全角）で切った先頭を使い、
  **空白が無い人はそのまま**出す（適当な位置で切ると別人の名前になる）。
  取り出しは `home/Greeting.tsx` の `familyName`（`shared/tests/intake.test.ts` で固定）
- **「アプリ」「今日」の見出しと説明文を出さない。** タイルを見ればアプリの並びだと分かり、
  カードを見れば今日のことだと分かる。**「イベントで使うもの」の見出しだけ残す** —
  上のタイルと見た目が違う理由が、見出しが無いと読み取れない。説明文（`note`）は全廃した
  （出していたのは PC だけで、毎日開く画面で毎日同じ説明を読ませていた）
- **スマホの並びを アプリ → 今日 に戻した**（M3 / M7 の逆）。モックが正
- **「今日」の3枚を 今日の予定 → 自分のやること → お待たせ中 に並べ替えた。**
  中身は1行も変えていない
- **スマホの上辺バーはロゴだけにした**（`shell/AppTopbar.tsx`・**トップに限らず共通**）。
  `☰` ／ ロゴ(18px) ／ 余白 ／ ベル ／ 本人。アプリ名のチップとパンくずは 1024px 以上でだけ出す
  （`v4-wide-only`）。**現在地はパンくずと左メニューが示す**
- **投入口を1本化した**（下記「投入口」）。⚠️ **スマホの AI はボトムシート**になった
  （`home/MobileAiBar.tsx`）。M7 の「開いたら閉じない」は**シートでは要らない**（画面を覆うので
  書きかけが隠れない）が、**状態は `MobileAiBar` が持つ** — シートの中で持つと、
  閉じた瞬間に部品ごと消えて**書きかけが消える**
- **動きを5つ足した**（実体は `shared/src/client/tokens-v4.css`・判定は `home/motion.ts`）:
  スクロールで下から14px／スケルトン→実データ／数字のカウントアップ（620ms）／
  タイルの光沢（`v4-gloss`）／シートのせり上がり（24px・`Sheet` の `rise`）。
  ⚠️ **既定は「見える」**。隠すのは JS が `data-reveal="hidden"` を付けたときだけ —
  CSS で先に隠すと、**JS が落ちた日にトップページが白紙**になる（誰も気づけない壊れ方）

**投入口（`contexts/tasks/components/intake/`）で決めたこと**
- **「ひとこと / 議事録」の切替を廃止**して入力欄を1つにした。切替は**押す人に AI の都合を
  選ばせていただけ**で、行き先はどちらもタスクだった
- **行き先は AI が1件ずつ決める**（`dest`: タスク / ネタ案件 / 活動記録 / 議事録）。
  確認画面の**1行目に札**を置き、その横に**どのデータに入るか**を添える
  （「案件（ネタ）」だけでは何が起きるのか分からない）。**人が付け替えられる**
- **案件（ネタ）を作ったら、その案件を開く**（ご判断）。「受付に貼る」ボタンは**置かない** —
  投入口を1本にして AI が行き先を決める形にした以上、**同じ文をどちらの口に入れるかを
  押す人に選ばせる**ことになり、1本化の意味が消える。引き合いを入れた人が次にやるのは
  たいてい中身を足すことなので、そのまま案件詳細へ送る。
  - **`sales` が無い人には遷移しない**（`canOpenProject`）。案件詳細は `sales` を要求するので、
    送ると 403 の画面に着く。`dailyops` だけの人は登録して終わり
  - **2件以上できたときは遷移しない。** どれか1つを勝手に開くと、**残りが登録されたことに
    気づけない**。投入欄の下にリンクを並べて選ばせる
- **判断が付かないものは `task` に倒す。** 一番取り消しやすく、必ず人の目に触れる —
  ネタ案件や活動記録に落とすと、間違っていても気づかれないまま残る
- **`dest` を差分（`ai_corrections`）の先頭項目に入れた。** 行き先まで AI に決めさせた以上、
  「ネタに振ったのを人がタスクに直した」が**いちばん価値のある教師データ**になる。
  ここを外すと**一番よく間違える項目だけが記録に残らない**
- **お客様の find-or-create はネタ案件だけ。** 活動記録では照合だけ（作らない）—
  済んだやり取りの記録から会社を増やすと、綴り違いの重複が台帳に溜まる。画面にもそう書く
- **4つの行き先を1つのトランザクションで書く。** 分けると「タスクは入ったが活動記録は
  入っていない」が起き、**人は登録したと思っているのでもう一度は投げない**（黙って消える）
- **作ったものに `idempotency_key`（`intake:<投入id>:<draft_key>`）と `source_channel='intake'`
  を残す**（migration 126 の既存列・**列を足していない**）。①押し直しの二重登録を DB が拒否し、
  ②あとから「投入口から生まれた案件が受注に至ったか」を**新しい表を作らずに**数えられる
- **添付（PDF・画像）と録音も同じ1回の解析に載せる。** 別の口を作ると、写真に写っている社名と
  本文に書かれた依頼が別々の下書きになり、人がつなぎ直すことになる。
  音声だけ先に Whisper で文字にしてから混ぜる（画像・PDF はモデルがそのまま読める）
- **録音は PC にも置いた**（投入欄の中）。`/sales/record` は残す。32kbps は `RecordDialog` と同じ
  — **既定の 128kbps だと 25 分で Whisper の 25MB 上限に当たる**
- ⚠️ **録音の文字起こしは裏で走らせる**（migration 180）。**リクエストの中で待たない** —
  nginx の `/api/` は `proxy_read_timeout` を書いていないので**既定の 60 秒で切れ**、
  待つ形にすると 3 分程度の録音しか投げられない（打合せには短すぎる）。
  議事録（`project_minutes`）と同じく**行を先に作って 202 を返し**、
  画面は `GET /tasks/intakes/:id` を 3 秒ごとに読みに行く。
  - **上限は Whisper の 25MB だけ**（32kbps で約100分）。時間の上限は置かない
  - **ジョブの表は作らない。** 行に状態を持たせる（`transcribing` / `failed`）。
    落ちた回は**読むときに経過時間で失敗として見せる**（DB は書き換えない —
    書き換えると生き返った処理と競合する）
  - **失敗の理由を行に残す**（`error_message`）。残さないと「押しても何も起きない」になる
  - **待ち受けの状態は `useIntake` が持つ**（シートを閉じても止めない）。
    画面を閉じても裏は進み、あとから投入ログに出ることを**画面に書く**
  - **進捗率を作らない。** Whisper は途中経過を返さないので、出せるのは経過時間だけ
- **録音は「構える → 録る」の2段**（ご判断・`intake/Recorder.tsx`）。`録音する` を押しても
  **まだ録り始めない** — 録音の画面に切り替わり、そこの `録音を開始` を押して初めて録る。
  押し間違いがそのまま録音にならず、**「録音することを相手にお伝えください」を録る前に出せる**
  （録音中に出しても、もう録り始めているので手遅れ）
- **録りながら 20 秒ごとに文字にして直近4行だけ出す**（下読み・ご要望）。
  赤い点と経過時間だけでは、**マイクが別の機器を向いていても同じ見た目**になるため。
  - **録音機を 2 つ動かす。** 本番用は止めずに録り続け（継ぎ目を作らない）、
    下読み用だけ 20 秒ごとに録り直す。1 本で兼ねると**継ぎ目の音が本番からも欠ける**
  - **下読みは使い捨て。** 本文になるのは**止めたあとに録音まるごとを1回で通したもの**。
    20 秒で切ると切れ目で単語が割れるので、議事録の本文にはできない。
    ⚠️ そのぶん **Whisper を2回通す**（費用が気になるなら間隔を延ばすか口を止める。
    `POST /tasks/intake/preview-transcribe` を止めても録音は動く）
  - **下読みは DB にも `ai_outputs` にも残さない。** 混ぜると同じ録音が2回
    教師データに入って無修正採用率がぶれる
  - **失敗しても 200 で返す。** 下読みが1回抜けただけで録音を止めさせない
- **知らない種類の添付は黙って捨てず、理由を付けて断る**（添付したのに読まれていない、が一番困る）
- **サーバーは `kind` を使わない**（受け取っても無視）。commit は `rows` で受け、
  **`tasks` でも受ける** — 名前を変えただけで古い画面から登録できなくなるのは割に合わない
- **画面の「登録できない理由」（`intake/types.ts` の `rowBlockers`）はサーバーの検証と同じ条件。**
  ゆるくすると押したあとに 400 で戻され、**どの行が悪いのか画面に出ない**
- 解析は **ChatGPT API (OpenAI) が主経路**。落ちたら規則ベースに縮退し、
  そのときは**行き先が全部タスクになることを確認画面に書く**（黙って倒すと「決めてくれない」と読まれる）

**AI の利用料を下げるために決めたこと（ご要望・この版）**
**「見えるようにする → 効く順に削る」**の順で入れた。**精度を落とす削り方はしていない**
（読み落として依頼が消えるほうが、数円より高くつく）。
- ⓪**まず測る**（migration 182 の `ai_usage` ＋ `shared/services/ai-usage.service.ts`）。
  どこにいくら掛かっているか分からないまま削ると、**効かない我慢だけが残る**。
  設定 → システムの情報 →「AI の使用量」（`settings/AiUsageCard.tsx` / `GET /admin/ai-usage`）で
  **種類 × モデル**に足して出す。
  - **`ai_outputs` では足りないので表を足した**（新しい表を作る前に確かめた）。
    下読みは「出力」ではないので載らず、**失敗した呼び出し**も出力が無いので載らず、
    文字起こしは**トークンではなく分**で課金される
  - **失敗した回も数える。** 課金されることがあるので、外すと総額が合わない
  - **単価はコードに焼き込まない**（`AI_PRICING_JSON`）。変わるうえ、焼き込むと
    「いつの値段か」が分からないまま金額が独り歩きする。**単価が無ければ金額を出さない** —
    トークンと分数だけでも「どこが重いか」は分かる。
    **単価の分からないモデルは合計に足さない**（0 として混ぜると総額が嘘になる）
  - **1分未満を「0分」と出さない**（下読みは 20 秒の切れ端。丸めると「録音していない」と読める）
- ①**プロンプトの並べ替え**（`intake-ai.service.ts` の `buildUserPrompt`）。
  OpenAI も Anthropic も**入力の「先頭から共通している部分」だけ**を安くするので、
  **先頭に現在の日時を置くと、後ろが全部同じでもキャッシュに当たらない**。
  担当者一覧 → 案件候補 → 過去の傾向 → **日時 → 投入テキスト**の順にした。
  **中身は1文字も変えていない**ので読み取りの精度は変わらない。
  効いているかは使用量の「うち再利用 N%」で見える
- ②**短い投入だけ軽いモデルへ**（段の既定は `shared/services/ai-model.ts`。いまは `gpt-5.6-luna`）。
  **添付が無く 400 文字以内・6行以内**のものだけ。写真・PDF・長文・録音の文字起こしは今までどおり。
  **迷ったら上位モデルに倒す**。⚠️ **軽いモデルで落ちたら上位モデルで1回だけやり直す** —
  モデル名が使えない環境で黙って規則ベースに縮退すると、
  「行き先を決めてくれなくなった」という**気づきにくい劣化**になる。`off` で止められる
- ③**下読みの間隔を段階にした**（`intake/Recorder.tsx`）。目的は「**本当に拾えているか**」の
  確認なので、**最初の1分だけ 20 秒ごと・そのあとは5分ごと**。
  1時間の打合せで **14 回 ≒ 4.7 分ぶん**（ずっと 20 秒ごとだと 180 回 = 60 分ぶん。**12分の1**）。
  **間隔が空くことを画面に書く** — 書かないと「止まった」と読まれる
- ④**写真は送る前に縮める**（`client-v4/downscaleImage.ts`・長辺 1600px / JPEG 0.8）。
  画像の費用は**画素数で決まる**ので、4000×3000 の写真1枚が長い文章より高くつく。
  名刺・ホワイトボードは 1600px で読める。**拡大しない／元より重ければ元を使う／
  縮められなければ元を送る**（ここで失敗して添付が消えるほうが費用より高くつく）
- **やらなかったこと**: 同じ文の再投入を N 分ではじく重複除け。
  「同じ依頼をもう一度入れた」のか「押したのに出なかったので入れ直した」のかを
  文面からは区別できず、**黙って捨てると登録したはずのものが無い**ことになる

**トップページ — モックとの差を詰めた回（前の版）**
モックと実装を1項目ずつ突き合わせた結果を反映した。**モックが正**（`docs/v4-plan.md` の大前提）。
- **アプリタイルの説明文を外した。** モックは**アイコン ＋ 名前の1行だけ**。
  説明を足すとタイルが「読ませる物」になり、毎日押す入口としては重い
  （説明はアプリ切替の一覧と ⑪ 探すにある）
- **ミニアプリの畳みを入れた**（`home/AppTiles.tsx` の `MINI_APPS`）。モックが持たせて
  いるのは**日常業務だけ**（週報・ニュース・情報・書類・内覧会・カード）。
  hover だけだと**指とキーボードで開けない**ので、chevron を**押せるボタン**にした。
  ⚠️ **件数は出さない** — 受信箱が数えている物を別の粒度で数え直すことになり、
  タイルの数字と食い違う
- **⌘K の窓は「入口の権限」を引き継ぐ**（`layout/searchFeatures.ts`）。`nav.ts` の項目は
  多くが `module` を書いていない（左メニューはその入口に入れた人にしか見えないので
  書く必要が無く、権限は `App.tsx` の `<PermissionRoute>` が持っている）。
  そのまま並べたら **`sales` が無い人にも「案件一覧」「料金表」「標準工程テンプレート」が
  出た**（実 DB の 経理だけ / 権限なし で確認）。**何も書いていない項目は入口の権限を要求する。**
  入口をわざと開けてあるのは**設定だけ**（`OPEN_ENTRANCES`）— 案内板・システムの情報・
  パスワード変更は全員が使う。既定は「要求する」側なので、**知らないアプリは出さない側に倒れる**
- **PC の件数は文章**（「お客様を待たせているものが 3件、…」）、**スマホはチップ**。
  モックがそうなっている（375px で文章にすると2行になり読み飛ばされる）。
  どちらも**数字が押せる**（お待たせ → 受付／期限切れ → やること）
- **「最終更新 MM/DD HH:mm」を出す**（モック右上）。出どころは
  **この画面が数字を取ってきた時刻**（react-query の `dataUpdatedAt`）。
  サーバーに「集計した時刻」は無いので、**持っていない時刻を作らない**。
  **スマホには出さない**（挨拶の下に1行まるごと足すことになり、モックのスマホにも無い）
- **スマホのタイルはモックの形にした**（2列・92px の正方形・件数は右上、イベントはピル）。
  PC の横長タイルを縦に積むのをやめた。**アプリの段が 1,676px → 約 500px** になる。
  **並び順（今日 → アプリ）は M3 / M7 のまま**変えていない
- **スマホの「AIに任せる」は青いバー1本**（`home/MobileAiBar.tsx`）。M7 で1行に畳んだ
  `MobileIntake` を**モックの形（`#005bac` の帯・高さ 60px）に置き換えた**もので、
  畳む考え方（場所は最上部のまま・大きさだけ小さく）はそのまま引き継いでいる。
  **押すと開いて中身は今までと同じ**（投入欄・電話を貼る・打合せを録音）。
  ⚠️ **開いたら閉じない**（M7 の理由をそのまま守る）— 書いている途中で畳むと
  入力が消えたように見える。**「撮る」は出さない**（名刺を読む口が無い）
- **スマホの節見出しは字間 .1em の小見出し**（`v4-eyebrow`・12px/800）。
  PC と同じ型スケールだったので、375px では見出しだけで縦を食っていた
- ⚠️ **以下は「合わせない」と決めた当時の記録です。いまは
  [モックが正](../docs/design/v4/_tokens.md)（利用者からの指示）**なので、
  **残すかどうかは相談してから決めること**。トップの挨拶 25px はモック8ファイル中
  1か所だけの外れ値で、寄せると v4 の全画面の見出しが動きます（そこが相談の中身）
- **モックに合わせなかったもの（当時の判断・実測で確認済み）**: 挨拶の h1 は **23px**
  （モックのトップは 25px）、節見出しは **19px/800**（同 18px/700）、
  タイル名は **19px/800**（同 20px/800）。どれも `docs/design/v4/_tokens.md` が
  確定値として決めた型スケール（`text-h1` / `text-h2`）で、**v4 の全画面が同じ段を使っている**
  （スマホでは M6 が `tokens-v4.css` でさらに詰めている）。
  **モック8ファイルを数えると、トップの3つはその画面だけの外れ値**だった:

  | 大きさ | モック全体での出現数 | 何に使われているか |
  | --- | --- | --- |
  | 22px | 59 | 画面見出し（トップ以外のページ） |
  | **23px** | **44** | 画面見出し ＝ `text-h1` |
  | **19px** | **22** | 節見出し ＝ `text-h2` |
  | 20px | 9 | トップのタイル名 |
  | 18px | 6 | トップの節見出し |
  | 25px | **1** | トップの挨拶（**8ファイル中1か所だけ**） |

  1か所のために型スケールを動かすと **v4 の全画面の見出しが動く**ので、
  トークンを正とした。逆に**トークンごと寄せたくなったとき**は
  `shared/tailwind.preset.ts` の `fontSize.h1` / `h2` の2行を変えるだけで全画面に効く
  （`docs/design/v4/_tokens.md` の表も同時に直すこと）
- **制作資料・技術資料を「イベントで使うもの」の段へ移した**（M3）。
  どちらも凍結アプリで、案件の本番の日に開くもの。大きいタイルが 9 → 7 枚になる。
  ⚠️ **消してはいけない** — `visibleApps()` は凍結4アプリを既定で外すので、
  **上辺バーのアプリ切替にも左メニューにも凍結アプリは出ていない**。
  つまり**押して開ける場所はトップのタイルだけ**で、消すと Qシート・技術資料・
  計時LIVE・リアルタイムCG が URL 直打ちでしか開けなくなる（放送が止まる）。
  `DAILY_KEYS`（`HomePage.tsx`）と `EVENT_KEYS`（`home/AppTiles.tsx`）は**対**なので、
  片方だけ直すとタイルが二重に出るかどこにも出なくなる
- **凍結4アプリをトップから消していない**（ご判断）。プラン承認時の決定は
  「トップページのアプリ一覧・アプリ切替からは外す」だったが、モックは
  制作資料・技術資料を並びに残し、計時LIVE・リアルタイムCG を
  **「イベントで使うもの」**という別の列に置いている。4つとも今日使える画面で、
  消すと **URL を覚えている人しか開けなくなる**ため、モックに従って残した。
  → **「検証環境なので消さなくてよい」**とご判断いただいた。
  **本番に出す前（Phase 7）にもう一度だけ確認する**（`docs/v4-plan.md` の項目2）。
  戻すなら `home/AppTiles.tsx` の `EVENT_KEYS` と `HomePage.tsx` の `DAILY_KEYS` の2行

**左メニュー（案件管理）で決めたこと**
- **v4 の情報設計に差し替えた**（モックの `navMenus`）。旧メニューは7つの塊に 20 項目あり、
  **ダッシュボードが「ふりかえり」の中**にあった。さらに「カンバン」と「タスクリスト」は
  **押すと同じ画面**（v4 でカンバンを畳んだのにメニューだけ残っていた）
- **業務**（ダッシュボード・受付・案件一覧）／**全案件**（タスク一覧・見積・請求）／
  **設定**（料金表・標準工程テンプレート）の3つ
- **受付をメニューに出す**（ご判断）。モックの PC サイドバーには無いが、
  毎日開く画面なのでダッシュボードのカード経由だけだと遠回りになる
- **v4 で作り直していない画面は「そのほか（作り直し前）」に畳む**（ご判断）。
  消すと動いている画面に辿り着けず、全部並べると v4 の並びが読めない。
  作り直したものから上の塊へ移す（着手時11画面 → 棚卸しで2つ削除・1つ作り直して
  移動し、残り6画面。下の「棚卸しした回」を参照）
- **タスク一覧は `/sales/tasks/list` を指す。** `/sales/tasks` は `kanban` へ転送されるが、
  `kanban` と `list` は同じ画面

**「そのほか（作り直し前）」9画面を棚卸しした回** — 削除可否を1画面ずつ実装コードで確認した
- **削除できたのは2つだけ**。**確定案件（スタジオ）**（`ConfirmedProjectsPage`）は専用APIを
  持たず `GET /projects` を呼ぶだけで、v4の③案件一覧（`gls_category: 'A'` 固定・検索/絞込/
  並替/カード・ボード表示）が完全に上位互換だったため削除した。旧URL
  `/sales/projects/confirmed/:category` は `/sales/projects` へ転送する。
  **ガントチャート**（左メニュー単独項目）は④タスク一覧（`TaskDashboardPage`）内蔵のガント
  タブと**完全に同一のコンポーネント**を指す重複リンクだったため、メニュー項目だけを消した
  （URL `/sales/tasks/gantt` 自体はタスク一覧のガント表示として現役なので残した）
- **削除ついでに、削除前から壊れていたリンクを直した。** `BusinessProjectView.tsx`
  （GPMの見積・請求タブが呼ぶ）の「確定案件一覧」への戻るボタンが `/sales` の付かない
  旧パスへ `navigate` しており、押すとホームに戻っていた。正しい行き先
  （`/sales/projects` / `/gpm/projects`）に直した
- **残り7画面は削除できない**（うち営業活動記録・按分グループは後の回で作り直した）。
  営業活動記録（全案件横断の一覧・編集・削除・次回アクション横断アラート）／按分グループ
  （複数案件間の費用按分の唯一の編集口）／旧GLS決算取込（決算取込案件の一括属性編集の
  唯一の口）／営業レビュー（ファネル・失注理由・担当者別目標のv4に無い分析）／報告資料
  （月次予算目標・経営会議議事録サマリのv4に無い唯一の入力口）／AI活動履歴
  （`mcp_audit_log` を見る唯一の画面。v4のトップページがAI活動フィードを撤去したとき、
  明示的にここへ行き先を集約した経緯がある）／取引先マスター（v4financeの取引先画面が
  明示的に処理を委譲している唯一の顧客/仕入先/販管費支払先マスタ）。いずれもv4のどこにも
  無い独自機能を持つため、作り直しが必要（**残るは5画面**）

**営業活動記録（`/sales/activity-logs`）を v4 に作り直した回** — `pages/activityLog/`
- **PC表とスマホカードを別々に手書きしていたのを、`Row`/`RowMain`/`RowSlot` 1本にした。**
  列は 活動日(96px) ／ 種別(72px・`TableBadge`) ／ 件名・案件・次回アクション（伸びる）／
  担当(96px・スマホでは落とす)。**次回アクションは列にしていない** — 文字数が案件ごとに
  大きく違ううえ完了・延期のボタンを持つので、固定幅に押し込むと文字が切れる。`RowMain`
  の3行目に置き、PC・スマホ両方で同じ形にした
- **活動の種別バッジは今までどおり生の色**（`kinds.ts` の `color`）。`cat-1`〜`cat-8`
  （見分けの色トークン）は用意されているがどの画面もまだ採用しておらず、8色に対して
  種別が9つあるうえ `check-contrast-tokens.mjs` の対象にも入っていない。ここで最初に
  採用してコントラストや割り当てを決め打ちするより、既存の債務（件数は増やさない）を
  残すほうが安全と判断した
- **次回アクションの完了・延期をこの一覧からもできるようにした**（`useNextActionActions.ts`）。
  `POST /:id/complete-next-action` `/postpone-next-action` はサーバーに前からあったが、
  この一覧からは一度も呼ばれておらず、片づけるには編集ダイアログを開き直すしかなかった。
  顧客360°ビュー（`CustomerDetailPage.tsx`）が先に同じ口を使っており、「完了」＋
  「延期（明日／1週間）」の形をそちらに合わせた（画面によって片づけ方が変わると迷う）
- **削除ボタンを `manager` に絞った。** サーバーは前から `requirePermission('sales', 'manager')`
  を要求しており、旧実装は開ける人なら誰でもボタンを出していたので**押せるのに 403** だった
- **`pcOnlyScreens.ts` の PC専用指定を外し、スマホに開放した。** `Row stackOnMobile` と
  `MobileFilterBar` で縦積みになり、375px で崩れないことを実ブラウザ（Playwright）で確認済み。
  左メニューは「そのほか」から「全案件」（案件台帳の隣）へ移した

**費用を分け合うグループ（旧「按分グループ」・`/sales/project-groups`）を v4 に作り直した回**
- **一覧と詳細を別URLに分けた。** 旧実装は1ファイル1174行で、一覧⇄詳細を画面内
  `useState` の切り替えだけで行っていた — ブラウザの戻るが効かず、詳細をブックマーク・
  共有できず、再読み込みすると一覧に戻っていた。v4の他の詳細画面（`/sales/projects/:id`
  等）と同じく `/sales/project-groups/:id` を新設し、`pages/projectGroup/` に12ファイルへ
  分割した（すべて400行以下）
- **会計用語「按分」は画面に出さない**（`docs/wording.md` の決めごと。会計用語を知らない
  人にも分かるように）。「按分グループ」→「費用を分け合うグループ」、「按分方法」→
  「分け方」、「按分内訳」→「分けた額の内訳」、「均等按分」→「均等に分ける」に置き換えた。
  **DB・API・URL（`project_groups` / `allocations` / `/sales/project-groups`）はそのまま** —
  変わるのは画面に出る文字だけ。コード内の技術コメントは概念名として「按分」を残している
  （`check-ui-tokens.mjs` の `forbidden-wording` はコメント行を対象外にしている）
- **仕入・売上ダイアログが持っていた「分け方」ロジックの重複を1つの hook にまとめた**
  （`useAllocation.ts`）。旧実装は同じ式（均等按分の端数は先頭の案件に寄せる）を2か所に
  コピーしており、片方だけ直すと仕入と売上で端数の付き方が食い違う状態になっていた
- **直す行の初期値は `useState` の遅延初期化子で決める。** 直す行の分け方が均等なら
  「均等に分ける」、そうでなければ「任意の比率」で開く。ダイアログは
  `{open && <PurchaseDialog key={…} />}` で描くので、開くたびに初期化し直され
  消し忘れの前の値が残らない（`RevenueDialog.tsx` と同じ「マウントに任せる」作り）
- **削除ボタンを `manager` に絞った。** サーバーは前から `requirePermission('sales', 'manager')`
  を要求しており、旧実装は開ける人なら誰でもボタンを出していたので**押せるのに 403** だった
- **`ToggleCard` の選択中テキストは `text-white/80`（生の色）のまま残した。** 部品自身が
  選択中の背景を Tailwind のクラスではなく inline style で当てているため、
  `text-primary-foreground` のようなトークンに置き換えると `check-contrast-tokens.mjs` が
  地の色を見つけられず「塗りの無いところに塗りの上の文字色」と誤検知する（実際に踏んだ）。
  部品自身の `description` スロットも同じ理由で `text-white`／`text-white/85` を使っている
- **PC専用のまま。** 任意の比率で複数案件へ金額を割り振る作業は、案件ごとの金額を
  その場で比べながら入力する必要があり、v4で作り直しても変わらずPC向き
  （`pcOnlyScreens.ts` に詳細URLも追加した）

**案件作成（②・受付を統合した回）で決めたこと**
- **画面を1つ減らした。** 旧 `/sales/inbox`（受付）と `/sales/projects/new`（案件登録）は
  **同じ仕事**でした — 届いたものを読んで、足りないところを埋めて、案件にするかどうかを決める。
  2画面に分けていたので、**受付で確かめてから登録画面でもう一度同じ項目を入れる**
  ことになっていました。`/sales/inbox` は `/sales/projects/new` へ転送します
  （**URL は消さない** — ブックマーク・ホームの「お待たせ中」・通知から来る道がある）
- **上にレール、下にフォーム、上辺に3つの決め方。**
  ネタのまま残す ／ 見送りにする ／ **案件にする（与件化）**
- **GLS 番号はここで発番しない**（ご指示）。受注が固まった時点で案件詳細から採ります。
  「案件にする（GLS を発番）」という表記は使いません
- **必須は5つだけ**（お客様・案件名・客入れの有無・案件分類・社内の担当）。
  残りは「進んだら聞く」に畳む。**電話を切る前に入れ終わる**のが目標
- **削った2つ**: 返事の期限（タスクの期限と役割が重なる）／求められているもの
  （書いても次にやることが決まらない）。**列は消していない** — すでに入っている値は
  案件詳細で読めるようにしてある（新しい案件には入らない）
- **改名3つ**: やりたいこと→**案件内容** ／ 規模→**来場人数** ／ 入手経路→**リード経路**
- **無観客のときは来場人数の欄ごと出さない。** 出して 0 を入れさせると、
  「0 名だった」と「聞いていない」が見分けられなくなる。**サーバー側でも落とす**
  （MCP や旧フォームから来ると「無観客なのに 150 名」の行ができる）
- **「聞き方の下書き」は受付から持ち込んだ**（ご判断）。引き合いを案件にする前に
  何を聞けばいいかが分かる唯一の場所で、消すと電話をかけ直す判断材料が無くなる。
  見る先だけ**保存済みの案件 → いま画面に入っている値**に変えたので、
  打ち込むそばから聞くことが減る（`projectNew/ask.ts`）。**AI ではない** —
  入っていない項目から機械的に組み立てる（同じ入力なら必ず同じ結果になる）
- **レールは掴んで滑らせる**（`client-v4/rail.ts`）。**8px 動くまではクリック扱いで、
  10px を超えたら直後のクリックを1回だけ捨てる** — カードを掴んだだけで開く誤爆を止める。
  `preventDefault` で殺さないのは、掴まずに押したときは今までどおり開けるため
- **1件も届いていないときはレールごと出さない。**「自動で届いたもの 0件」の帯が
  毎回出ると、案件作成が「受付の付属品」に見える
- **カードを選び直したら上書きする。** 「人が書いた文字は残す」形にすると、
  1枚目を直したあと2枚目を選んだときに**1枚目の内容が混ざったまま**になり、
  どちらの引き合いを登録しているのか分からなくなる（選択をやめれば空に戻せる）
- **「見送りにする」は選んでいるときだけ出す。** 手で入れている途中は消す対象が無く、
  押せるように見せると「登録せずに閉じる」と誤解される
- **送るところは PC とスマホで1つ**（`useProjectDecisions` ＋ `useNewProjectForm`）。
  写すと、列が増えたとき片方だけ送らなくなり、引き合いへの書き戻しも片方だけ忘れる
  （忘れると未仕分けに残り、**同じ引き合いから案件が2件**できる）
- **確信度（高/中/低）は出さない。** MCP の `create_project` は確信度を渡さないので、
  その値が無い。無い数字をそれらしく出すより**足りない項目を名指し**する
- **AI に返る経路はそのまま**（会社方針「AI を使い捨てにしない」）:
  - `create_project`（MCP）が起票内容の**全文**を `ai_outputs`(kind=`project_draft`) に残す。
    `mcp_audit_log` は `args` を 1000 文字で切り詰める監査用で、教師データにならない
  - `projectService.update` が保存時に**サーバー側で自動比較**し、
    「どの項目を・何から何に」を `ai_corrections` に入れる（`project-ai-feedback.service.ts`）。
    **人には何も入力させない**
  - **2段分類を差分の項目に足した**（migration 182）。旧分類は4種しかなく
    「有観客の収録」と「有観客の配信」が同じ `hybrid_event` に寄るので、
    **種類だけを見ていると人が直した分類の半分が「無修正」に数えられる**
  - **起票から7日以内の更新だけ**を見る。窓を切らないと数ヶ月後の通常の業務更新まで
    「AI の誤り」に数えられ、修正率が意味のない数字になる
  - **直さなかった項目も `none` で残す**（無修正採用率の分母）。ただし**直した回だけ**。
    開くたびに積むと、よく開かれる案件ほど精度が高く見える
  - 見送り（`e_lost`）は `reject` として残す＝**拾いすぎの指標**
  - 成果（受注/失注）は `projects.stage` から導出する。**`ai_outcomes` に行を足さない** —
    既存データで表現できるものに新しいテーブルを作ると、書き忘れた日から数字が嘘になる
  - 還流は `get_ai_feedback_digest`（MCP）が `kind=project_draft` を読めるようにした
- **`pages/inbox/kinds.ts` だけ残してある。** 種類の見せ方を**案件作成のレールと
  ホームの「お待たせ中」の2か所**が使うため。書き写すと、片方だけ直したときに
  **同じ引き合いが画面によって違う名前で出る**

**案件分類の2段化（migration 182）で決めたこと**
- **`project_type` を捨てず、2列を足して併存させた**（ご判断）。
  旧 `project_type` の7種のうち **GLS-B の3種（GMO案件・コンサル・その他）は
  プロジェクト管理側**に回っており、「客入れの有無 × 配信/収録/イベント」の
  どこにも当てはまらない。捨てると**その3つの受け皿を別に決めたうえで**
  標準工程・Excel 入出力・シード・AI の項目名・GLS 分類の判定（`getProjectCategory`）を
  一斉に直すことになる
- **書くのはサーバーの1か所だけ**（`server/.../project-classification.ts`）。
  画面は2段だけを送り、`project_type` はそこから導く。
  **画面に両方送らせると、片方だけ更新された行ができる**（一覧と詳細で分類が食い違う）
- **移行は指示どおり**: ハイブリッド→有観客＋配信 ／ ライブ配信→無観客＋配信 ／
  収録→無観客＋収録 ／ オフライン→有観客＋イベント。GLS-B の3種は **NULL のまま**
- **すでに2段が入っている行は上書きしない。** 旧分類は4種しかないので、
  「有観客の収録（公開収録）」が「有観客の配信」に戻ってしまう（実 DB で確認済み）
- **更新で2段を渡さなければ今の値を保つ。** 直す画面（`ProjectFormPage`）には
  まだ2段の欄が無いので、保つ形にしていないと**保存するたびに分類が消える**
  （タグ・登録16項目と同じ壊れ方）
- **工程テンプレートの「使う案件の種類」も6通りに読み替えた。** 旧い値のまま残すと
  **1つも当たらず「どの案件にも出てこない型」**になり、黙って全部出るので気づけない。
  API は旧 `project_type` でも受ける（旧い画面・MCP を 400 で止めない）
- **グループ会社の判定は `customers.is_gmo_group`**（ご判断）。判定の**正はいまも
  `customers`** — 案件が `customer_id → customers` を直接見ており、`company_id` が
  nullable で **未リンクの取引先があると判定が空振りする**ため。
  ⚠️ **「社名の文字列一致では判定しない」は v4.0.28 で撤回した**（ご指示: GMO と
  ついているものはすべてグループ）。ただし**正は列のまま**で、社名から入るのは
  **移行の1回と、新しく登録するときの初期値だけ**。取引先マスターのチェックで
  外せて、**外した印は戻らない**（migration 192 / `shared/src/utils/gmoGroup.ts`）
- **リード経路に `group` の値を足した**（画面の固定表示だけにしない）。
  値を持たないと、あとから「グループ案件が何件あったか」を数えるときに
  **取引先マスターの現在のフラグ**で数えることになり、案件を取った当時の姿と食い違う
- **リード経路に「内覧会」を足し、「Web」を「WEBフォーム」に改名した**
  （ご要望・migration 183）。ご要望は「内覧会と WEBフォームの2つを足す」だったが、
  **WEBフォームに当たる値はすでにあった**（`web`）。別の値として `webform` を足すと、
  プルダウンに「Web」と「WEBフォーム」が並んで**誰も違いを説明できず**、
  **同じ意味の引き合いが2つの値に分かれて集計できなくなる**ので名前だけ変えた。
  **既存データは1行も書き換えていない**（`web` = 問い合わせフォーム経由で意味は同じ）。
  内覧会は本当に別の入口（来場予約フォーム → 後日そのまま案件）なので値を分けた。
  並びはモックどおり メール／電話／内覧会／紹介／WEBフォーム
- **MCP の `create_project` にも `audience` / `project_category` を足した。**
  無いと AI が起こしたネタは**分類が空のまま**で、人が案件作成で埋め直すことになる
  （そのぶん `ai_corrections` に「AI が間違えた」と記録されてしまう）。
  **`group` は渡せない** — グループ会社かどうかは取引先マスターが決めるため
- **取引先の保存で `is_gmo_group` を渡さなければ今の値を保つ。** 欄を持たない古い画面から
  保存されるだけで印が黙って外れる（リード経路が固定されなくなる）

**グループ内 / グループ外を自動で決めた回（v4.0.28・migration 192・ご指示）**
- **チェックボックスは取引先マスター**（`companies.is_gmo_group`）。列は `customers` と
  **保存のたびに双方向で同期**する — 同じ相手が2つの画面に出るので、片方だけ直ると
  **どちらが本当か画面から分からない**
- ⚠️ **案件の「グループ区分」は人が選ばない。** 見積の単価（定価 / グループ内価格）が
  この値で決まるのに、**選び忘れても画面には何も出ない** — グループ会社の案件に定価が
  並んでも、気づくのは見積を送ったあと。保存のたびに**お客様から導く**
  （`project.service` の `resolveCustomerType`）。直す画面は**読むだけの固定表示**にして、
  違うときの行き先（取引先マスター）を欄の下に書く。**欄ごと消さない** — なぜグループ内
  価格にならないのかを調べる取っかかりが無くなる
- **列は残す。** 毎回 `customers` を見れば列は要らないが、あとから「グループ内の案件が
  何件あったか」を数えるとき**取引先マスターの「いまの」印**で数えることになり、
  案件を取った当時の姿と食い違う（リード経路の `group` と同じ理由）
- **お客様が分からない案件は今の値を保つ**（`customer_id` が空・消えた行）。
  どちらとも言えないものを機械で決めると、決めたことに誰も気づけない
- ⚠️ **移行は「印を増やす」方向にしか動かさない。** 社名から埋めるだけだと、**GMO と
  ついていないグループ会社**（社名を変えた会社・関連会社）の案件が黙って社外に戻る。
  **グループ内の案件を1件でも持つお客様にも印を付けて**から引き直す
- **判定の関数は server と画面の2か所**（server は `shared/` を import しない構成）。
  食い違うと**チェックが入って見えるのに保存すると外れる**ので、
  `shared/tests/gmoGroup.test.ts` が両方を読んで突き合わせる

**見積・請求（⑤）で決めたこと**
- **検収と入金を ONAiR で持つ**（ご判断）。`revenues` には請求日・支払期日・
  請求書の発行済フラグはあったが、**「検収したか」と「いつ入金されたか」が無かった**
  （仕入 `purchases` には `inspection_date` があるのに売上側には無い、という非対称）。
  migration 140 で `inspection_date` / `paid_date` を追加
- **フラグではなく日付**。済んだかを別のフラグでも持つと片方だけ更新されて必ず食い違う
  （日付が入っていれば済み）。`invoice_issued` と `billing_date` が同じ形になりかけている
- **取り消しは訊く。** 黙って戻せると、経理の訂正が誰にも気づかれない
- **旧版（`superseded`）は出さない。** サーバー側で外している。差し替え済みの版が並ぶと
  「返事待ちが何件か」が読めない
- **どちらのタブも常に引く。** 開いている側だけにすると、閉じている側のタブに出る件数が
  0 のままで**数字が嘘になる**（実ブラウザで「見積 0 / 請求 2」と出て気づいた）
- **「見積を作成」ボタンは置かない。** 見積は案件にぶら下がるもので、ここで押しても
  「どの案件の？」を先に訊くことになる。行を押すとその案件の見積タブへ飛ぶ
- 請求は `status='confirmed'` かつ `group_id IS NULL`（按分の親行を二重に数えない）

**標準工程テンプレート（⑦）で決めたこと**（`pages/flow/` ＋ migration 178）
- **モックの 6 段 26 工程をそのまま初期値に入れた。** 担当の職種・実施日からの逆算日数・
  外せるかどうかまで書かれているので、文書で整理し直すより**画面で目にしながら直すほうが早い**
- **種類ごとに工程を削らない。** モックは種類ごとに工程数が違う（リアルイベント 18 /
  ハイブリッド 26 / 生放送 24 / 公開収録 22 / 収録ありイベント 20 / スタジオ収録 14）が、
  **どれを外すのかは書かれていない**。推測で 8 本消すと「この種類には要らない工程」を
  勝手に決めることになり、しかも消えたことに誰も気づけない。
  → **26 本の型を1つ入れ、複製して削ってもらう**（画面にもそう書いてある）
- **案件をつくったときに黙って入れない**（ご判断）。小さい案件でも 26 行並び、
  使わないタスクの山でタスクタブが読めなくなる。タスクタブの帯 → `ApplyFlowDialog` で
  **一覧を見せ、チェックを外してから**入れる。外せない工程だけチェックを固定する
- **二度は入れられない**（`projects.flow_applied_at`）。押し直しで**同じタスクが 2 組**
  できると、どちらを消すか分からなくなる。**守りを外すと 26 → 52 件になることを確かめてある**
- **担当は入れない。** 型が持つのは**職種**で、誰がやるかは案件ごとに決まる
  （v4 は案件担当者という概念を持たない）。適当な人を入れると
  「自分のタスク」に他人の仕事が並ぶ
- **期限が出せない工程も落とさずに入れる。** 実施日が未定の案件では逆算の工程の期限が
  出せないが、落とすと**「入るはずの工程が入っていない」ことに誰も気づけない**。
  期限なしで入れ、**何件が期限なしになるかを押す前に出す**
- **日付はサーバーが案件から読む**（`POST /flow-templates/:id/preview` に `project_id`）。
  画面から日付を送らせると、**下見に出た期限と実際に入る期限が食い違う**
- **受付日は SQL で文字列にする**（`to_char(created_at AT TIME ZONE 'Asia/Tokyo', …)`）。
  `created_at` は TIMESTAMPTZ で pg は `Date` を返すので、`String(...).slice(0,10)` は
  `Wed Aug 08` になり、**受付から数える工程の期限が全部 null になっていた**
  （型チェックには出ない。実 DB に当てて見つけた）。時間帯を書かないと、
  夕方以降に登録した案件の受付日が前日になる
- **入れたあとは 4 つの鍵を落とす。** かんばん `task-columns` / リストとガント
  `project-tasks` / 全案件 `task-dashboard` / 帯を消すための `project`。
  1つ落とし忘れると、その見え方だけ古いまま = 「入れたのに出てこない」
- **型を直せるのは `sales` の manager 以上。** 型を変えると**以後すべての案件**に効くので、
  案件を1件直すのと同じ権限（editor）では緩すぎる。入れるのは editor、読むのは reader
- **職種は選ぶ形にする**（`ROLES` の4つ）。自由入力だと「テクニカル」「技術」「Tech」と
  揺れて、あとで職種で絞り込めない。**外す（決めない）こともできる**
- **日数は符号つきの1つの数。** 「前／後」の選択と数の2つで持つと、
  前を選んだまま -60 と入れて 60 日後になる取り違えが起きる
- **プロジェクト管理（`/gpm/templates`）とは別系統。** 工事・構築の工程
  （発注確定 → 設計 → 調達 → 施工 → 検収）と放送案件の工程は1つも重ならない

**料金表（⑧）で決めたこと**
- **場所ごとの表にした**（大③・migration 172）。`pricing_categories.location_id`。
  分類ごと場所に属する（品目に持たせると1つの分類に用賀と渋谷が混ざる）。
  **既存 78 品目はすべて用賀の料金**なので `loc-yoga` に寄せた。NOT NULL —
  場所の決まっていない分類は、どのタブにも出ないか全部のタブに二重で出るかになる
- **空の場所は「料金は未定」と言い切る。** ふつうの空表示だと「消えた」と読まれる。
  始め方は ①ほかの場所の表を丸ごと写す ②空から作る の2つ。
  **写せるのは空の場所だけ**（2回押すと同じ品目が2つ並び、どちらを選んだかで金額が変わる）
- **分類を別の場所へ移せない。** 移すと、その分類を使った過去の見積が
  「別の場所の表から選んだ」ことになる。要るなら写して消す（何が起きるか目に見える）
- **見積を組むときは場所を出す**（`pages/pricing/LocationPicker.tsx`）。
  案件のスタジオ予約が1つの場所に揃っていればそれを既定にし、**なぜその場所かを1行書く**。
  複数の拠点にまたがる案件では**推測しない**（どちらの料金でも間違いになりうる）。
  `projects` に場所の列は無いので、`studio_bookings` → `studio_booking_rooms` →
  `studio_rooms.location_id` からたどる（`studio_bookings.room_id` は存在しない）
- **`created_by` / `updated_by` を足した（migration 139）。** `pricing.routes.ts` の
  **6つの書き込みすべてが 500 で落ちていた** — ルートはこの2列に書いているのに、
  どちらのテーブルにも列が無かった（001b の定義に入っていない）。つまり料金表は
  **読むことしかできない状態**で、画面に `onError` が無かったため
  「ダイアログが閉じない」以外に何も出ていなかった。SQL から列を消しても直るが、
  **誰が値段を変えたかは残したい**ので列を足すほうを選んだ
- **並び順の数字を入力させない。** 「10 と 20 の間だから 15」は内部の都合の押し付けで、
  同じ数字を2つ入れると並びが不定になる。**↑↓ で2つの `sort_order` を入れ替える**
  （全件に振り直すと、同時に別の人が触っていたとき見ていない分類まで動く）
- **「設定なし」(NULL) と「¥0」を見分けられるようにした。** NULL は「その相手には
  出さない品目」で 0 円とは別。両方 NULL のときは「見積のどこにも出てきません」と警告する
- **消す前に何がいっしょに消えるかを出す。** 分類のゴミ箱は押した瞬間に消えていた
- **権限が無い人には操作ボタンを出さない**（分類 `owner` / 品目 `editor`・消すのは `manager`）。
  出しても押せば 403 になるだけ
- **モックの「変更の記録」パネルは載せていない。** 履歴テーブルが無く、出せる中身がない

**案件一覧（③）で決めたこと**
- **ボードは別画面をやめて「見え方」にした。** 旧 `/sales/pipeline`（`PipelinePage`）は削除し、
  `/sales/projects?view=board` へ転送する。別画面だと絞り込みが引き継げず、
  **別のエンドポイント**（`/dashboard/sales-board`）を叩いていたので
  一覧は確定売上・ボードは想定金額を合計していて数字が合わなかった
- **ステージのチップの件数は「ステージ以外の絞り込みだけ」を掛けて数える**
  （`GET /projects` の `stage_counts`）。全件の内訳を出すと、検索中に押した先が 0 件になる
- **`stage` はカンマ区切りで複数受ける**（「終了」= `s_completed,e_lost`）。
  知らないステージ名は**素通ししない**（絞り込みを指定したのに全件返ると気づけない）
- **バッジの文字は和文だけ**（`STAGE_BADGE_LABEL`）。「A 受注済」のまま入れると
  均等割り付けが効かず、色の塊の幅が行ごとに変わる
- **「D 仮押さえ」は灰ではなく淡い青**（モックとの意図した違い。理由は
  `pages/projectList/stages.ts`）

**案件一覧（③）— 絞り込みと列を作り直した回**
- **ステージのチップから「E 問合せ」を外した。** **ネタ ＝ E 問合せで同じもの**なのに
  チップと見え方タブの両方にあり、同じ案件が2か所から絞り込めて名前も違っていた。
  ネタは3つ目の見え方タブが持つ（列が違うのでそちらのほうが読める）
- **「すべて」は空にせず、出すステージを名指しする**（`ALL_STAGES`）。空にすると
  サーバーは絞り込み無し＝**ネタも終了も全部**返す。「全N件」も同じ集合で数える —
  チップを足し算すると、出していないネタと終了まで数えて**「全 42 件」で 31 行**になる
- **終了（完了・失注）は「終了」チップのときだけ出す**（ご判断で `stage` 判定）。
  実施日の経過では判定しない — 本番後の請求・ふりかえりが残っている案件が
  「すべて」から消える
- **並び順を9つから5つに減らした。** 逆順（実施日が遠い順・作成が古い順・金額が安い順）が
  半分を占めており、**選ぶ理由がある場面が無かった**。いまは おすすめ順 ／
  見積金額が大きい順 ／ 実施日が近い順 ／ 期限が近い順 ／ 最後の動きが古い順
- **「おすすめ順」は `sort_by=recommended` の新しい鍵にした。** 既定（`default`）は
  MCP の `list_projects`・Excel の書き出し・確定案件の一覧からも使われており、
  定義を差し替えると**何も指定していない呼び出し全部の並びが黙って変わる**
- **「期限」は次のタスクの期限**（ご判断）。返事の期限（`reply_due`）はフォームから
  外したので、新しい案件には入らず、時間が経つほど並び順が意味を失う
- **期間は「単位」と「対象」を分けて持つ**（`projectList/period.ts`）。旧実装は
  単位ごとに違う入力欄を出しており、**単位を変えるたびに帯が1行↔2行で動いていた**。
  **既定は半年（いま属する期）** — 旧実装の「今月〜半年先」は期の区切りに合っておらず、
  月をまたぐたびに範囲が滑っていた
- **月末は「翌月の1日の前日」で出す。** `-31` を固定で書くと 2月が 2/31 になり、
  文字列比較なので**2月末の案件が範囲から外れる**（旧実装がそうだった）。
  `shared/tests/projectPeriod.test.ts` で 23 項目を固定してある
- **操作バーは固定2段。** 1段目 40px（検索・単位・対象）／2段目 36px（並び順・AI・用語）。
  **「全件」でも対象の枠を消さない** — `display:none` にすると右側が詰まって位置が動く
- **段の切り替えの罫線はボタン自身に置く。** 枠に置くと中のボタンが 40−1−1＝38px になり、
  寸法表のボタンの段（32/36/40/44/48）から外れる（`verify:ui` で実測して直した）
- **押せない札に `fg-disabled` を使わない。** 白地で 2.61:1 しかなく「薄すぎる文字」で落ちる。
  押せないことは `opacity` が示す
- **金額の列は「見積金額」**（`estimates` の**束ごとの最新版**の合計・税抜）。
  旧版（`superseded`）と失注（`rejected`）を外す。`group_id` を無視して SUM すると
  **版を重ねた案件ほど金額が膨らむ**。まだ見積が無い案件は想定金額を薄字で出す。
  **確定売上は出さない** — 列名が「見積金額」なので、受注済の行だけ中身が売上に変わると
  同じ列で意味が2つになる
- **次のタスクの担当はピル。** 「山田：見積を送る」と文中に混ぜるとタスク名と同じ字面になり、
  誰がやるのか目で拾えない。**担当がいない行にも同じ位置に薄いピル**を置いて列を保つ
  （消すと期限だけが左に寄る）。**頭文字のアバターは出さない** — 同姓が同じ丸になるうえ、
  200px の列で 20px を色の丸に使うとタスク名が読めない
- **`?stage=` を読むようにした。** ダッシュボードのステージ別・KPI から
  `/sales/projects?stage=a_won` で送られてくるのに読んでおらず、**押しても「すべて」のまま**
  だった。ネタは「すべて」に出さないので、ステージ別の**ネタだけは `?view=seed` へ送る**
- **スマホは横スクロールのチップをやめた**（指示書 4-6）。390px では 6 個のうち 3 個しか
  見えず、**残りがあることに気づけない**うえ、払うつもりが押して絞り込みが変わっていた。
  「ステージ｜すべて 6 ▾」の 52px 1行 → **下から出るシート**。期間は 単位 → 対象 の2段シート。
  検索欄の右は「絞り込み」ではなく**並び順**（絞り込みは上の2行が持っている）
- **並びが変わるときは行が滑る**（`client-v4/flip.ts`）。変える**直前に位置を覚え、
  更新後に1回だけ戻す**。新しく現れた行だけ 8px 下からフェードイン（1行 18ms ずらし）—
  元から居た行も一緒にフェードさせると、絞り込むたびに一覧全体が明滅する。
  **`data-row`（`ui/row.tsx` の検査の印）とは別の属性**（`data-flip-key`）にしてある
- **シートを閉じるのと同じコマで測らない**（`beforeChange`）。閉じかけの高さが混ざって行が飛ぶ

**タスク一覧（④）で決めたこと**
- **タスクの状態は4つ**（未着手／進行中／相手待ち／完了）。ただし**DB は2列に分けている**：
  `is_completed`（完了したか・**ここが正**）と `work_state`（完了していないときの止まり方）。
  1列に4値を持たせると「完了したか」が2か所になり、かんばん・ガント・MCP・依頼フロー・
  週報の集計のどれか1つが片方しか更新しない瞬間に食い違う。
  **画面は必ず `taskState()`（`pages/taskList/state.ts`）を通す**こと
- **「相手待ち」を足したのがこの版の中身。** これが無いと未完了タスクが全部同じ重さに見え、
  自分が止めているものが埋もれる。導出できない（`progress` からは出せない）ので列を足した
- **カンバンを畳んだ。** 旧「カンバン」は案件ごとの小さな板を縦に積み**各列5件まで**で、
  案件をまたいで見えていなかった。同じことは案件詳細の板のほうがよくできる
- **ガントは残した**（全案件の山を1枚で見る場所がここしかない）。
  ただし**中身は旧実装のまま**（`DashboardGantt/DashboardGanttView.tsx` 522行）。
  時間軸の描画は別の仕事なので、一覧の作り直しと混ぜていない
- **`invalidateTasks` は `task-dashboard` も落とす。** 案件の中のタスクと全案件の一覧は
  同じタスクを別の鍵で持っており、案件側だけ落としていたので**一覧から直しても一覧が古いまま**
  だった（反証試験で再現を確認済み）
- **チェックボックスは列にしていない。** 7段のいちばん狭い枠が 56px で 18px の四角には広すぎ、
  段を増やすと「その画面だけの幅」ができる。`RowMain` の中に入れてある

**案件詳細（⑥）で決めたこと**
- **「読む画面」と「直す画面」を分けた。** `/sales/projects/:id` は v4 のタブ付き詳細（読む）、
  `/sales/projects/:id/edit` が**いままでの `ProjectFormPage` そのまま**（直す・新規も）。
  **フォームは中身を1行も変えていない** — 枠の入れ替えと中身の作り直しを同じ回でやると
  どちらが原因で壊れたか切り分けられない（前回の刷新が捨てられたのがこれ）
- **タブの URL の鍵は `task` のように単数**。`tasks` にすると
  既存の `/sales/projects/:projectId/tasks` と衝突し、押した瞬間に枠ごと消える
  （React Router は静的な区切りを優先する）
- **7タブとも中身がある**（概要／やり取り／タスク／見積／書類／当日／ふりかえり）。
  ふりかえりはモック自身が「これから作ります」と書いていた分で、
  絵が無いので**いま持っている数字だけ**で組んだ（下記）。**「エピソード（回）」タブは
  無い** — 最新モック（`v4-live-sales.dc.html`）の見えるタブバーに含まれていないため、
  2026-08 に外した（下記「エピソードタブを外した回」）
- 旧 `EpisodeListPage` / `EstimatePage` は**同じものを別 URL で開いていた32行のラッパー**
  だったので削除し、旧 URL は転送にした（違いは `isEstimateMode` の有無だけだった）
- **タスクタブは既存のかんばん／リスト／ガントをそのまま呼んでいる。** 落としたのは
  見出しだけ（枠が出すので二重になる）。旧 `/sales/projects/:id/tasks` は転送にして
  `ProjectTasksPage` は削除した
- **ふりかえりタブを作った**（`projectDetail/ReviewTab.tsx`）。**モックに絵が無い**分
  （モック自身が「これから作ります」と書いている）なので、**いま持っている数字だけ**で
  組んだ — 新しい欄を作って「入力してください」と言うと、入れない欄が増えるだけになる。
  ①お金（想定 → 出した見積 → 確定売上 → 仕入 → 粗利を**引き算の順に**並べる）
  ②進み方（タスクの終わり具合・期限を過ぎたまま）
  ③実施の記録（`event_reports`。**隔週キープと同じ表・同じ口**）。
  - **差の読み方を書かない。** 「見積より N% 高く売れた」は、値引きなのか追加受注なのか
    数字からは分からない。**数字を並べるだけ**にして、読むのは人に任せる
  - **`GET /keep/event-reports/:id` は書いていなくてもお金の実績を返す**ようにした。
    売上・仕入は書いたかどうかと関係なく存在するのに、`found=false` で落としていたので
    **ふりかえりが「売上 —」で始まっていた**（実ブラウザで見つけた）
  - **空欄（null）と 0 を分ける。** 「数えていない」と「0 人だった」は別
- **議事録の持ち帰りを1件ずつタスクにできる**（`POST …/minutes/:id/open-items/:index/task`）。
  - **二度作らない。** 作ると `open_items` のその要素に `task_id` を書き戻す。
    画面のボタンを隠すだけだと、**同時に開いた別の画面が古いままボタンを出す**
  - **担当は入れない。** `owner` は AI が文字起こしから拾った**名前の文字列**で、
    利用者の id ではない。名前で突き合わせると同姓の人や取引先の名前を社内の誰かに割り当てる。
    **説明に書くだけ**にする（どの打合せから来たかも一緒に残す）
  - 期限は `YYYY-MM-DD` の形のときだけ写す（**推測の日付を作らない**）
- **書類タブから BOX にファイルを置ける**（`POST /projects/:id/box-files?scope=`）。
  - **社内と社外は別のカードで、置き場所を選ばせない。** 1つの枠で「どちらに入れますか」と
    訊く形にすると、急いでいるときに取り違えて**原価が外に出る**
  - **読み取りと違って失敗を隠さない。** 中身を出すほうは BOX が落ちていても 200 で返すが
    （案件を止めないため）、**置くほうは理由を付けて失敗を返す** — 上がっていないのに
    上がったように見えるのが一番困る
  - **同じ名前は新しい版として上げる**（BOX の 409 を拾って `uploadFileVersion`）。
    諦めると「上げ直したのに入らない」が起き、しかも理由が出ないと押し直され続ける
  - **1つずつ上げて、上がった分だけ返す**。まとめて失敗にすると「3つ中2つは入っている」ことに
    気づけず、同じものをもう一度上げることになる
  - multer は multipart のファイル名を latin1 で読む。**日本語の名前が化けたまま BOX に載ると
    探せないうえ直せない**ので、`Buffer.from(name, 'latin1').toString('utf8')` に通す
  - 置き場所が無い（フォルダ未作成・BOX 未接続）ときは**枠ごと出さない**
- **書類タブは BOX の中身を1階層ぶん出す**（`GET /projects/:id/box-files?scope=`）。
  **再帰しない** — 深いフォルダを全部たどると呼び出し回数が読めず画面が固まる。
  **失敗しても 200 で返し `reason` を渡す**（`NO_FOLDER` / `NOT_CONFIGURED` /
  `UNAVAILABLE`）。500 にすると BOX が落ちた日に案件詳細が全部開けなくなる
  （`shared/services/box.ts` の「BOX 障害が業務をブロックしない」方針）。
  **ファイルを置く（アップロード）はまだ無い**ので画面に明記している
- **当日タブは凍結アプリへの導線。** `GET /qsheet/documents?project_id=` /
  `GET /techsheet/documents?project_id=` でこの案件の資料を引き、**あるものだけ**並べて
  `/qsheet/editor/:id` を直接開く（凍結＝URL は生きているので今日から使える）。
  資料ごとの進み具合は凍結アプリ側の作りに依存するので出さず、その旨を画面に書いた
- **見積は `estimates` / `estimate_items` の別テーブル**（migration 138）。
  **`revenues` に相乗りさせてはいけない** — `revenues` を読む **41 か所が `status` を
  見ておらず**（トップの当月売上を含む）、見積の行を入れると売上に足される。
  実測してから別テーブルに切り替えた
- **版は1行ずつ**。v1 と v2 は別の行で、`group_id` で束ねる。列に版番号を持たせて
  上書きすると v1 の中身が残らない（「送った見積を見返せる」が要件そのもの）
- **出した版（`sent`）と旧版（`superseded`）は直せない。** 直すなら次の版を作る
  （前の版を丸ごと写して version を +1、前の版は中身を触らず印だけ付ける）
- **合計はサーバーが出す。** 画面が送ってきた合計は保存しない（計算が2つになると必ず食い違う）
- **「請求」タブは作らず、見積タブを「見積・請求」にする**（ご判断）。モックには請求タブが
  無いが、案件を開いている人が請求額を探しに行かずに済むほうを取った
- **「打合せを録音」を入れた**（やり取りタブ）。録音 or 音声ファイル → **Whisper** で文字起こし
  → LLM が決定事項・持ち帰りを下書き → 人が直して確定。
  - **音声は保存しない。** 文字にしたら捨てる（容量と、取引先の声が入るため）。
    残すのは `transcript`（全文・切り詰めない）と議事録だけ
  - **裏で走らせる。** 1時間の録音で数分かかるので、行を先に作って `status` を持たせる
    （リクエストの中で待つと **nginx の 60 秒で切れる**）。ジョブの表は作らない
  - **決定事項には引用を必須にする。** 引用が出せないものは決定にせず持ち帰りへ落とす
    （`normalizeMinutes`）。議事録は取引先との合意の記録なので、AI が話を補うのが最悪
  - **日付は推測させない。** 壊れた形は空にする
  - **差分の before は「AI が出したもの」**（`ai_outputs.payload_snapshot`）。
    直前の行の状態と比べると、**書きかけを保存してから確定した人の修正が全部
    「無修正」になる**（実測して直した）
  - `met_on` は差分に数えない — 人が投げるときに入れる値で、AI の出力ではない
  - 録音は 32kbps で録る。**既定の 128kbps だと 25 分で Whisper の 25MB 上限に当たる**

**案件詳細（⑥）— 案件記録を作り直した回（この版・migration 184/185/186）**
- **「次にやること」は AI が作った1行を出す**（ご指示・migration 190 /
  `next-action-short.service`）。本番の言い切り1文は 40〜60 字あり、帯の枠を
  1行ぶんに広げてもスマホでは収まらない。**規則で切ると必ず途中で切れる**ので、
  **28 字以内の一文を AI に作らせて列に持つ**（`next_action_short`）。
  - **原文（`next_action`）は書き換えない。** 短い一文は表示用の別の値で、
    全文はやり取りタブに並びのまま出る。上書きにすると、要約を間違えた日に
    **やることが1件消えたことに誰も気づけない**
  - **無ければ規則の見出しに落ちる**（`nextActionLine`）。作るのは毎晩 3:10 なので、
    取り込んだ直後の行にはまだ入っていない。**ここで空を返すと、いちばん見たい
    新しい行だけが画面から消える**
  - **`next_action` を直したら要約は捨てる**（サーバー側）。残すと**古いやることが
    帯に出たまま**になる
  - 待ち行列・失敗の印・費用の出し方は**やり取りの整形と同じ形**にしてある
    （`/activity-logs/short-status` `short-run` `short-reset-failed` `:id/short-redo`）
- **メモという入れ物をやめ、やり取りに一本化した。** `projects.notes` と `activity_logs` は
  どちらも「この案件について書き残したこと」で、**書く人がどちらに書けばよいか決められません**
  でした。畳む先をやり取りにしたのは、やり取りが**時系列を持っている**から
  （メモには日付が無く「いつ時点の話か」が分からない）。社内の書き置きは
  `activity_type='memo'` として同じ列に並びます
  - **`notes` の引数は受け取り続ける**（MCP・Excel・案件作成フォーム）。本番のメール取込
    スキルが `create_project` を毎日叩いており、**引数を消すと呼び出しごと落ちます**。
    受け取ったものはメモ1件として書き、**同じ本文なら足しません**（保存し直すたびに
    同じメモが積み上がると、やり取りがメモで埋まる）
  - ⚠️ **`projects.notes` は人のメモだけではなかった。** 決算取込が自動で作った案件の印として
    `[kessan:2026-03]` を先頭に書き込んでおり、**絞り込み・マーカー一覧・実施日の埋め戻しの
    3か所がその文字列を読んでいました**。落とす前に `projects.kessan_marker` の列へ逃がしてある
    （自由文に印を混ぜる形はもともと危うく、人が `[kessan:` で始まるメモを書くと誤判定した）
  - ネタ一覧の要点と案件作成の下書きは `memo_excerpt`（いちばん新しいメモ）で引き直す。
    Excel の「備考」も列を消さずに中身だけ差し替えた（**配ってある様式を変えない** —
    列が1つ減ると、古い様式で取り込んだ人の備考が別の列に入る）
- **やり取りは打ちっぱなしで書ける**（`thread/ComposeBox.tsx`）。件名を訊きません —
  電話を切った直後に3つの欄を出すと、**書くのが面倒になって記録そのものが残りません**。
  形にするのは保存時に AI（`activity-ai.service`）。**原文は `description` に必ず残す**ので、
  整形が的外れなときは戻せますし、プロンプトを直したあと同じ原文でやり直せます
  - **整形に失敗しても記録は残す。** ここで 500 を返すと打った文がまるごと消える。
    整えられなかったことは `format_error` で画面に出す（黙ると AI が効いていないことに気づけない）
  - **AI に HTML を書かせるのはここだけ。** メール取込は「意味の単位」で受けます
    （`rich-content.ts`）が、やり取りは**人が書いた文章そのもの**を段落・箇条書きのまま
    残したいため。そのぶん通す形をきつく絞る — 許可タグ9つ・**属性は1つも通さない**
    （`html-sanitize.ts`。`href` も不可＝リンクは作れないが、URL は素の文字で残る）
  - **削るのは入口（保存時）の1か所だけ。** 画面でも削ると、片方だけ直したときに
    「保存はできるのに表示だけ消える」という追いにくい壊れ方をする
- **ふりかえりは実施記録がメイン**（`ReviewTab.tsx`）。前は お金 → 進み方 → 実施の記録 の
  縦一列で、**書く場所に着くまで数字を3枚スクロール**していた。PC は左に実施記録・
  右 340px に お金 → 進み方。スマホは実施記録が先頭
- **「よかったこと」の自由行を KPT の3枠にした**（`review/KptPanel.tsx`・migration 185）。
  1本の配列に続けたいこと・困ったこと・次に試すことが混ざっており、読む側が毎回
  読み分けていた。**書いた人と日付を1件ずつ残す** — ふりかえりは「その人がその現場で
  見たこと」なので、誰が書いたか分からないと後から確かめられない
  - 追加ボタンは「**寺井 として足す**」。**誰として書くのかを押す前に見せる**
    （姓の取り出しはトップの挨拶と同じ `familyName`。写すと画面によって表記が混ざる）
  - **AI の下書きは `confirmed_at` が入るまで「未確認」。** 人が確かめずに隔週キープの
    資料へ出ると、**AI の推測が実施報告になります**。確かめる／直す／消す のどれをしても
    結果が `ai_corrections` に返る（消したのは「拾いすぎ」の指標なので `reject` で残す）
  - **未確認の下書きが残っているあいだは起こさない。** 押し直しで同じ内容が2組できると、
    どちらを消すか分からなくなる（標準工程テンプレートと同じ守り方）
  - **書いた人は案件の担当**（押した人ではない）。定時実行には押した人が居ないうえ、
    AI が起こした行を押した人の名前で残すと、その人が見ていないことがその人の言葉になる
- **当日の写真は BOX の「社外と共有するフォルダ／08_写真」に貯める**（ご指示）。
  自前ストレージには置かず、**サムネイルも BOX のものを流すだけ**で複製しない。
  社外に入ることを**枠の中に書く**（別の場所の注意書きは読まれない）。
  報告資料の生成はこの回では作らない — **貯めるところまで**
  - `08_写真` は GLS 発番時の自動生成に足したが、**それ以前の案件には入っていない**ので
    初回のアップロードで無ければ作る（`ensureSubfolder`。409 は探し直して使う）
  - **読むだけのときは作らない。** 開いただけで空のフォルダが増えると BOX がゴミで埋まる
  - サムネイルが出せないことがある（変換中・対応外・契約プラン）。そのときは
    **ファイル名だけ**に落とす（絵が出ないのは我慢できるが、枠が壊れると枚数も分からない）
- **スマホのタブは段階で入れ替える**（`tabs.ts` の `MOBILE_TABS_BY_PHASE`）。
  3つ固定にすると、どの段階でも1つは使わないタブが混ざっていた。
  ふだん 概要/やり取り/タスク ／ 本番日 概要/タスク/**当日**（いちばん右）／
  終わった案件 概要/やり取り/ふりかえり。
  - **本番日は「いずれかの日が今日」**。飛び日があるので期間で判定すると中日が本番になり、
    開始日だけを見ると最終日が本番にならない（`shared/tests/projectPhase.test.ts` で固定）
  - **2種類の「開けない」を混ぜない。** どの段階でもスマホに出さないタブ（見積・書類・回）は
    今までどおり**すり替えず**「PC で見る画面です」と出す。別の段階なら出るタブは
    **概要に落とす** — こちらは幅の問題ではないので「PC で見てください」は嘘になる
- **PC のヘッダーを固定2段にした。** タブとステージ帯が1行に並んで横スクロールになり、
  さらに**選択中のステージだけ名前を出していた**ので、押すたびに帯の幅が動いて
  タブの位置がずれていた（押した先が別のタブになる）。
  1段目 42px（ステージ **E〜A 各104px の等幅** ＋ 完了・失注 各60px ＋ 右端に最後の更新）／
  2段目 44px（タブを**均等割り**。横スクロールにしない — スクロールすると
  「まだ右にタブがある」ことに気づけず、押されないタブができる）
- **「打合せを録音」をヘッダーから外した**（指示書 1-5）。トップページの投入口に同じ機能があり、
  **同じことをする入口が2つ**になっていた。案件の中で録るときは、やり取りの書く枠の
  「録音から起こす」を使う
- **AI が使えるかは文字起こしと別の鍵で見る**（`ai_available`）。文字起こしは Whisper＝OpenAI 固定、
  整形は OpenAI / Anthropic のどちらでもよい。まとめて判定すると、
  「文字起こしは使えないが整形はできる」環境で書く枠から AI の案内が消える

**案件管理のリンクを全部当たり直し、v3 の置き土産を落とした回**
- **きっかけ**（ご指摘）: 「一部リンクが遷移しない」。原因は**行き先の無いリンクが
  404 にならないこと**でした — `App.tsx` の最後の `<Route path="*">` が拾って
  **黙ってホームに戻る**ので、押した人には「押しても遷移しない」としか見えず、
  型検査にも lint にも出ません
- **直したリンク5つ**（どれも実ブラウザで再現を確認してから直した）:
  - **概要タブの「すべて見る（N件）」が効かなかった。** `to="?tab=thread"` と
    書いてあったが、**タブは URL の区間で持っている**（`/sales/projects/:id/:tab`）
    ので、アドレスだけ変わって画面は概要のまま。クエリで持っていた頃の書き方の残り
  - **仕入の「按分グループ」がホームに飛んでいた。** `/project-groups`（正しくは
    `/sales/project-groups`）。接頭辞の無い旧 URL がそのまま残っていた
  - **旧 `/sales/projects/:id/episodes` が概要に落ちていた。** 転送先が
    2026-08 に消した `episode` タブのままだった。**回の表と「回を足す」は
    タスクタブにある**ので `task` へ向けた
  - **転送が `?` を落としていた。** `/sales/inbox?inquiry=123` から来ると
    **引き合いの id が消え**、案件を作っても元の情報に書き戻せない
    ＝ 未仕分けに残って翌日また送られ、**同じ引き合いから案件が2件**できる。
    `RedirectKeepQuery` で引き継ぐ（行き先が自分で持つ `?src=pdf` などは行き先が勝つ）
  - **「カレンダーで空きを見る」が今日の月表を出すだけだった。** 送る側
    （`ProjectFormPage` の `state`）は残っていたのに、**v4 で ① 予定を作り直したとき
    受け口が落ちていた**。案件の日へ飛んで日表にし、押さえたい部屋で絞る。
    旧画面は予約ダイアログを開いていたが、ボタンの名前は「**空きを見る**」なので
    見せるところまでにする。**一度きり**（`history.replaceState`）— 消さないと
    再読み込みのたび同じ日に引き戻される
- ⚠️ **`ProjectQuickLinks` の「カレンダー」から `?project_id=` を外した。**
  ① 予定は案件で絞る機能を持っておらず、**誰も読まないまま全件が出る** —
  絞られたと読まれるぶん、無いほうがまし
- **同じ壊れ方が戻らないように `npm run lint` に検査を足した**
  （`scripts/check-links.mjs`）。`App.tsx` のルート表と、画面が持つ行き先
  （`to=` / `href=` / `navigate()` と、左メニュー・設定トップ・⌘K の `to:`）を
  突き合わせる。別バンドル（`/qsheet` など）と API は見ない。
  わざと置く行き先は `ALLOW` に**理由付きで**足す（いまは「制作支援」「素材納品」の
  comingSoon 2件）。**実際に `/project-groups` を戻して落ちることを確かめてある**
- **落とした v3 の置き土産**:
  - `contexts/tasks/components/MyTasksSummarySection.tsx`（204行）… 旧トップページの
    カード。トップを 1,273 → 177 行に作り直したとき呼び手が消え、
    **どこからも読まれないまま** pre-v4 の `SectionCard` を抱えて残っていた
  - `components/ui/scroll-area.tsx` / `separator.tsx` … 参照 0 の1行再エクスポート
  - `server/src/shared/utils/validators.ts` … 参照 0
  - ⚠️ `server/src/shared/db/reset.ts` は**消さない**（`npm run db:reset` の実体）
  - **概要タブの「タグ」節は値があるときだけ出す。** v4 で入力欄を外し、
    **絞り込む口も画面に1つも無い**ので、ほぼ全部の案件で
    「タグ：付いていません。」という永久に埋まらない枠が並んでいた。
    **列と既存の値は消さない** — v3 で付けたタグを読む場所がここしか無い
- **まだ v3 のまま残っているもの（この回では触っていない）**: 左メニューの
  「そのほか（作り直し前）」10画面（顧客・営業活動記録・確定案件・按分グループ・
  旧GLS・ガント・営業レビュー・報告資料・AI活動履歴・取引先マスター）。
  **どれも動いていて、v4 の並びをまだ決めていないだけ**（`docs/v4-plan.md`）。
  作り直しは掃除ではなく画面の作り直しなので、別の回にする
  - ⚠️ **この段落は当時の記録。** 「顧客」はその後に取引先マスターへ一本化され、
    「そのほか」の棚卸し・確定案件とガントの削除・営業活動記録の作り直しを
    別の回でやった（`341行目以降「そのほか（作り直し前）」9画面を棚卸しした回` 参照）。
    いまの残りは6画面
- **検証**: 実ブラウザで案件管理の**34 の URL を開き**（v4 の画面・詳細7タブ・
  旧 URL の転送6本）、本文のリンクを1つずつ押して
  **ホームに戻るもの 0・押せないもの 0・JS エラー 0**。直した5つは前後で実測。
  `check-links` はルート 90 本 × 行き先 95 種で OK

**案件を直す（`/sales/projects/:id/edit`）— 案件作成と同じ中身・同じ見た目にした回**
- **きっかけ**（ご指摘）: 2つの画面の整合が取れていなかった。案件作成で訊く
  **客入れの有無・案件分類・ご担当・継続区分・来場人数・案件内容・リード経路が
  直す画面にひとつも無く**、代わりに**旧1段の「案件種類」**（ハイブリッド／生放送／
  収録／…）だけが残っていた。つまり**作るときに入れたものを、あとから直す場所が
  どこにも無かった**うえ、同じ案件が画面によって違う分類で見えていた。
  言葉も揃っておらず（顧客／お客様・主担当／社内の担当・想定金額／予算）、
  見出しの型（`text-sm/medium` と `text-sub/secondary`）も枠の形も別物だった
- **欄を書き写さず、案件作成の部品をそのまま呼ぶ**（`projectNew/RequiredFields` /
  `projectNew/MoreFields`）。**写すと必ずまた離れる** — 離れても型でも lint でも
  見つからず、画面を開いた人だけが気づく（今回がそれ）。
  値の形（`NewProjectValues`）・必須の判定（`missingOf`）・畳んだ札の数
  （`moreFieldCount`）も1つの定義を両方が読む。**項目は `projectNew/fields.ts` に
  足すだけ**で両方に出る。`projectForm/BasicSection.tsx` と `AmountSection.tsx` は削除
- **出し分けは部品の中に書く**（`mode: 'create' | 'edit'`）。呼ぶ側で欄を組み直すと、
  片方の画面にだけ欄が増えた状態にまた戻る
- **直す画面で出さない4つ**（どれも「置くと壊れる」もの）:
  **ステージ**（`PUT /projects/:id` は `stage` を1文字も見ない＝押せるのに何も
  起きない欄になる。段は履歴・失注理由・GLS 発番の確認を伴うので
  `PATCH /projects/:id/stage`）／**実施日**（「スタジオの日程」が `project_dates` の
  唯一のもと。2か所から全置換すると片方で足した日が消える）／**最初のタスク**・
  **メモ**（直すたびに同じものが1件ずつ増える。タスクタブとやり取りが持つ）
- **直す画面にだけ出す1つ**: **グループ区分**（`customer_type`）。見積の単価
  （定価 / グループ内価格）がこの値で決まり、直せる場所がここ以外に無い
- **「進んだら聞く」は直す画面では開いて出す。** 枠も項目も同じで、初めから
  開いているかどうかだけが違う（直しに来た人が探しているのはこの中の項目）
- **必須の出し方も揃えた。** 赤い箱に「顧客を選択してください」ではなく、
  案件作成と同じ**黄色の帯で項目を名指し**し、保存ボタンを押せなくする
- **リード経路は `update()` が1度も書いていなかった**（server）。作るときだけ入り、
  **あとから直す道がどこにも無かった** — 直す画面に欄が無かったので気づけない。
  `project-ai-feedback.service` の突き合わせ項目には最初から入っていたので、
  人が経路を直しても差分は必ず「無修正」に数えられていた。
  **渡さなければ今の値を保つ／知らない値は NULL に落とす**（CHECK で 500 にしない）
- ⚠️ **`customer_type` も「渡さなければ今の値を保つ」に直した。**
  `normalizeCustomerType` は知らない値を `external` に倒すので、欄を持たない
  呼び出しから保存されるだけで**グループ内の案件が黙って社外に戻り**、
  見積がグループ内価格を選ばなくなる（タグ・登録16項目と同じ壊れ方）
- **2段分類は GLS-B のときだけ訊かない**（`missingOf`）。工事・構築のプロジェクトには
  「客入れの有無」も「配信か収録か」も意味が無く、サーバーも NULL のままにする。
  作る画面は必ず GLS-A なので、効くのは古い GLS-B の行を開いたときだけ
- **旧 `project_type` は保存で送らない。** 読み込んだ値を送り返すと、2段を直しても
  古い種類が一緒に来て**分類と種類がずれた行**ができる（導くのはサーバーの1か所）
- **GLS の操作を見出しにまとめた**（発番 / 別の GLS へ付け替える /
  **プロジェクト管理へ移す**）。最後の1つは「基本情報」の中に埋まっていて、
  番号にまつわる操作が見出しと本文に散っていた
- **検証**: 実ブラウザで**2画面の項目を突き合わせ**（作成14／直す13・差は上の4＋1と
  日程の2つだけ）・値の読み込み・有観客に切り替えると来場人数の欄が現れる・
  必須が欠けると帯が出て保存が押せない・**1280px と 390px で横はみ出し 0px**・
  13px を割る読ませる文 0・JS エラー 0。**実 Postgres** で
  何も変えずに保存しても全項目が保たれること／2段を直すと `project_type` が
  追随し無観客なら来場人数が落ちること／**リード経路が保存されること**（これまで
  できなかった）／欄を持たない呼び出しでリード経路とグループ区分が**保たれる**こと／
  知らない値と空文字が NULL に落ちること／**グループ会社なら `group` で保存される**こと／
  古い GLS-B の行が**開けて保存でき、分類を勝手に埋めない**ことを実測

**案件詳細（⑥）— エピソードタブを外し、売上・請求を作り直した回（2026-08）**
- **きっかけ**: `docs/design/v4/mockups/README.md` が明示する正の案件管理モック
  `v4-live-sales.dc.html` を実測すると、`det.tabs` は概要／やり取り／タスク／見積／
  書類／当日／ふりかえりの**7つだけ**で、エピソード（回）が入っていなかった
  （`docs/v4-mock-deviations.md` の 2026-08-13 追記参照）
- **「エピソード」タブをタブごと外した**（ご判断）。`PROJECT_TABS`（`tabs.ts`）から
  `episode` を削除し、`DetailHeader.tsx` / `ProjectDetailPage.tsx` の `isSeries` に
  よるタブ出し分けも削除した。古い `/sales/projects/:id/episode` URL は
  `ProjectTabKey` に無い値として自然に概要タブへフォールバックする（追加の転送
  コードは書いていない — `tab` の既定値ロジックがそのまま吸収する）
- ⚠️ **タブを外す前に「回を新しく作る手段」を確認した。** `EpisodeScopeToggle`
  もスタジオ予約ダイアログも「既存の回から選ぶ」だけで、**作る口が無かった**。
  レギュラー（GLS-A）案件は今日も回を増やしながら運用しているので、タブが
  持っていた簡易表（回・名前・実施日・タスク進捗・状態）と「回を足す」ボタンを
  **タスクタブの回の絞り込みのすぐ隣**に移設した
  （`contexts/tasks/components/EpisodesPanel.tsx` 新設・ご判断）。
  状態はタスクの完了件数から自動で導く（`todo`/`doing`/`done`）ので
  `episodes.status` 列（誰も書いていない自由文）には頼らない。
  `GET /projects/:id/episodes` に `task_count` / `task_done_count` の
  サブクエリを足した（`parent_task_id IS NULL` = チェックリストの子タスクを
  含めない。一覧・かんばんの完了数え方と同じ決めごと）
- **見積タブの「売上・請求」ペインを `BusinessProjectView` からモックの3カード
  設計へ置き換えた**（ご判断）。新設した `RevenueBillingPane.tsx` は売上／請求
  （`invoice_issued` が立っている行だけ）／仕入（原価）の3枚のカード＋確定売上・
  仕入・粗利の合計行、**読むだけ**（行を押しても編集は開かない）。
  月次ユニット作成・PDF/Excel出力・明細のインライン編集は畳んだ — 既存の財務台帳
  （`/budget/revenues` `/budget/purchases`）と ⑤ 見積・請求（全案件）にすでにある
  ので、同じ `revenues`/`purchases` を2か所で編集できる形にすると片方だけ直された
  行ができる（財務の台帳3画面と同じ理由）。データの出どころ（`GET /revenues?
  project_id=` `GET /purchases?project_id=`・`budget` 権限）はレガシー版と同じ
  ものをそのまま使っており、権限の要件は変えていない
- **呼び手が無くなった `LegacyViewTab.tsx` は削除した。** `BusinessProjectView.tsx`
  自体は残す — **プロジェクト管理（GPM）の見積タブ**
  （`contexts/gpm/pages/projectDetail/BillingTab.tsx`）がいまも直接呼んでいる

**プロジェクト管理（GPM）— 工程の下のタスクを出し、落ちた列で壊れていた4画面を直した回**
- ⚠️ **前提: プロジェクト管理は全画面が 500 で開けませんでした。** migration 184 が
  `projects.notes` を落としたとき、**案件管理側だけが直っていて GPM が取り残されていました**。
  `gpm.service.ts` の SELECT / INSERT / UPDATE が `notes` を触りつづけていたので、
  一覧・詳細・作る・直すの4つとも `column "notes" of relation "projects" does not exist`。
  **型検査にも lint にも出ません**（SQL の中身は見ない）。画面には「サーバー内部エラー」だけ出る
- **メモは案件と同じ関数を読む**（`addMemoActivity` / `MEMO_LATERAL` を `project.service.ts` から
  export して呼ぶ）。写すと、メモの置き場所を次に変えた日にまた片方だけ取り残されます。
  読む側の名前（`notes`）は変えていないので画面はそのまま — **中身が「いちばん新しいメモ」**になった
- **同じ壊れ方を止める試験を足した**（`shared/tests/droppedColumns.test.ts`）。
  migration の `DROP COLUMN` を集めて、サーバーの**SQL 文字列だけ**を見に行く
  （`--` の注釈は落とす。「migration 184 で projects.notes を落とした」という説明文は正しいので、
  素の文字列検索にすると注釈で落ちる）。**`p.notes` を戻すと実際に落ちることを確かめてある**
- ⚠️ **この試験が2つめの穴を見つけました。** 投入口（`task-intake.service`）の
  **ネタ案件を作る道も同じ列を触っていて 500** でした（実 API で再現 → 直したあと登録できることを実測）。
  本文は**同じトランザクションの中で**やり取りのメモ1件にする — 外に出すと
  「案件はできたが本文が消えた」が起き、入れた人は登録したつもりなので入れ直しません
- **工程の名前を押すとタスクが出る**（`projectDetail/OverviewTab.tsx` ＋ `TaskRows.tsx`）。
  着手前は工程の「3 / 7」という**件数だけ**で、**何が残っているのかがこの画面から分からず**
  ⑤ 全プロジェクトのタスクで絞り込み直すことになっていました
  （⑤ の画面には「タスクを足す・直すのはプロジェクト詳細の工程から」と書いてあったのに、
  **詳細にその口が無かった**）。既定は閉じています（7工程 × 4〜5タスクで 30行を超える）
- **開いている工程はタブの部品が持つ。** 行の中に持たせると、並べ替えや保存で
  行が作り直された瞬間に閉じます
- **タスクの期限は `due_at` の 18:00**（ひな形から写すときと同じ形）。`due_date` に入れると、
  同じプロジェクトのタスクが2つの列に分かれて「自分のタスク」の並び
  （`COALESCE(due_at, due_date+18:00)`）が日によって入れ替わります
- **直すときは渡した項目だけを書き換える**（サーバー側）。画面が持っていない項目
  （担当・工程）が空で送られて黙って外れるのを防ぐため。期限だけは
  **鍵が入っていれば `null` でも書き換える** — でないと「期限を消す」ができません
- **工程を消しても配下のタスクは消さない**（`gpm_phase_id` を NULL にするだけ）。
  一緒に消すと「工程の名前を直したかっただけ」の人が**タスクを何十件も消します**。
  外れたタスクは詳細の「工程に付いていないタスク」の束に残るので付け直せます。
  **何件外れるかを確認に出す**
- **並べ替えは隣と入れ替えるだけ**（`PUT /gpm/phases/:id/move`）。並び全体を採番し直すと、
  2人が同時に押したときに片方の並びが丸ごと巻き戻ります。**端では何もしない**
  （400 を返すと、端の工程で押した人にだけ赤い札が出る）
- **他のプロジェクトの工程にタスクを付けられない**（サーバーが `project_id` を突き合わせる）。
  付いてしまうと、そのタスクが別のプロジェクトの工程の下に並び、進み具合の分母も相手側に足されます
- ⚠️ **`useInvalidateGpm` がタスクの鍵を落としていませんでした。** ⑤ でチェックを入れると
  「完了にしました」の札は出るのに**行が変わらない**（鍵が違うだけなので型検査にも lint にも出ない）。
  `['gpm','tasks']` と `['gpm-project-tasks']` を足した
- **一覧に「見積」の列を足した**（モックの `money`）。金額は**案件一覧と同じ式**
  （サーバーの `ESTIMATE_AMOUNT_LATERAL` を export して読む）。
  「金額の列はありません」という**古い but 書きっぱなしの注記**を消した — 見積は migration 173 から入っている
- **シードにプロジェクト管理のデータを入れた**（固定 id `gpm-1` / `gpm-2`）。
  着手前は**データが1件も無く `gpm` の権限も誰にも付いていなかった**ので、検証環境では
  system_admin で開いて「プロジェクトがまだありません」を見るしかありませんでした。
  工程は「完了・進行中・待ち・未着手」を1つずつ入れてあります（1つの状態しか無いと色と並びを確かめられない）
- **`verify:ui` に詳細の4タブを足した。** いちばん操作の多い画面が見た目の検査に1度も載っていませんでした

**プロジェクト管理（GPM）— 議事録と書類を足した回（同じ版）**
- **議事録は表もサービスも作り直していない。** `project_minutes.project_id` は `projects(id)` を
  指し、プロジェクトは GLS-B の案件なので**そのまま載る**。Whisper の投げ方・整形のプロンプト・
  差分の記録（`ai_corrections`）は案件と同じものを呼ぶ。**分けたのは口（URL）だけ** —
  案件側のルートは `sales` を要求するので、`gpm` だけの人は自分のプロジェクトの議事録を開けなかった
- **持ち帰りの行き先だけが違う**（`MinutesCard` の `track`）。案件は**タスク**、
  プロジェクトは**未確認事項**（`gpm_open_items` ＋ migration 161 の `source_minutes_id`）。
  工事・構築で打合せから出る持ち帰りはほとんどが「先方の判断待ち」で、タスクにすると
  「自分がやること」に相手待ちが混ざり、**プロジェクトをまたいで「いま何件止まっているか」を
  数えられない**（それが `gpm_open_items` を作った理由そのもの）
- **誰に訊くかは決めつけない。** `open_items[].owner` は AI が文字起こしから拾った**名前の文字列**で、
  取引先の担当者も社内の人も同じ形で入っている。`to_kind` を機械で決めると必ず取り違えるので、
  **既定を「発注者」にして、拾った名前は `to_name` に文字として残す**だけにした
- **同じ持ち帰りから二度は作れない**（サーバーが `open_items[].ask_id` を書き戻す）。
  画面のボタンを隠すだけだと、同時に開いた別の画面が古いままボタンを出す
- **会社方針の5条件は「同じサービスを呼ぶ」ことで自動的に満たされる**（記録・差分・還流）。
  ⚠️ **穴だった「成果」を塞いだ**: `get_ai_feedback_digest` に
  **持ち帰りが追いかけられた率**（`open_items[].task_id` / `ask_id` の付いた数 ÷ 全体）を足した。
  `ai_outcomes` に行は足さない（**読み取り時に導出**）。
  **追跡率を「AI が正しかった率」と読まないこと** — 人が言い換えて登録すると印が付かないので、
  **拾いすぎの目安**として見る（advice の文もそう書いてある）
- ⚠️ **SQL に jsonb の存在演算子（疑問符）を書くと落ちる。** この製品の DB 層は `?` を
  プレースホルダとして数えるので `syntax error at or near "$1"` になる（実際に踏んだ）。
  `->>` で書く。**再発を止める試験を足した**（`shared/tests/sqlPlaceholder.test.ts`）
- **書類タブから BOX にファイルを置けるようにした**（案件詳細と**同じ部品** `FolderCard` に
  口の前置き `base` を渡すだけ）。サーバー側も**同じ1本**（`project-box-files.service` に
  切り出して案件側もそれを呼ぶ）。写すと、どちらかだけ直した日に**片方が原価を外に出す**
- ⚠️ **案件側の「当日の写真」は持ち込んでいない。** プロジェクトのフォルダ構成に `08_写真` が無く
  （`gpm-box-folder.service`）、工事の写真をどこに貯めるかを決めていない
- **BOX の実際の書き込みは検証環境で確かめられない**（資格情報が無い）。確かめたのは
  **入口の分岐すべて**（scope 無し 400 ／ ファイル無し 400 ／ フォルダ無し NO_FOLDER ／
  案件の id を渡すと 404 ／ 一覧は 200 ＋ 理由）と、**案件側の振る舞いが切り出し前と同じこと**

**議事録の3件（レビューで指摘された穴・PR #103 → #104）**
- ⚠️ **文字起こしは一度も開けていなかった。** 一覧（`listMinutes`）は本文を積まないのに、
  `MinutesCard` は**本文の有無**で「文字起こしを見る」を出すか決めていたので、
  **案件でもプロジェクトでもこの枠は出ていなかった**（詳細の口
  `GET /projects/:pid/minutes/:id` は書いてあるのに**どこからも呼ばれていなかった**）。
  → 一覧が**文字数だけ**返す（`transcript_chars`）。本文は**押されたら**詳細から取る
  （`detailPath` を渡す形にして、案件は `/projects/:pid/minutes/:id`、
  プロジェクトは `/gpm/minutes/:id` を指す）
- ⚠️ **失敗した行にも文字起こしの枠を出す。** 「文字起こしは残っています。下の
  『文字起こしを見る』から取り出せます」と書いてあるのに、失敗の行はその枠を持っていなかった。
  整形で落ちても文字起こしは残るので、取り出せないと**録音し直し**になる
- ⚠️ **`canEdit` で「消す」を出していた。** 消せるのは manager（サーバーがそう止めている）なので、
  editor には**押しても何も起きないボタン**が出ていた（プロジェクトは黙って無視、案件は 403）。
  → **`canDelete` を別の props にして型で強制**。呼ぶ側2か所が漏れなく渡す
- ⚠️ **持ち帰り→未確認事項／タスクは同時押しで2件できた。** 印（`ask_id` / `task_id`）の確認を
  **取引の外**でしていたため、2人が古いタブから押すと**両方が確認を通り、印は後から書いた1つだけ**が残る
  ＝ **参照の無い行が「止まっているもの」として数えられ続け、議事録の画面からは消せない**。
  → 確認を**取引の中に入れ、行を押さえてから読み直す**（`FOR UPDATE`）。
  案件側（`openItemToTask`）は**取引そのものが無かった**ので一緒に直した。
  **割り込みを人工的に作って両方向を実測してある**（直した版は待って `ALREADY_EXISTS`・
  外すと2件できて参照の無い行が残る）

**プロジェクト管理（GPM）の見積で決めたこと（大⑤・migration 173）**
- **`estimates` を案件と共用する。** 別表にすると版・明細・合計の作りが2つになり、
  片方だけ直る形が生まれる。`project_id`（案件）と `gpm_project_id`（プロジェクト）は
  **CHECK で排他**（両方入ると案件管理とプロジェクト管理の両方に出て合計が二重になる）
- **先に「案件がある前提の読み手」を数えた。** `estimates` を読むのは **3か所**で、
  `revenues` の 41/53 のような広がりは無かった。ただし
  **`salesOverview` の「見積の返事待ち」だけが案件の結合を持たず**、
  GPM の見積が案件管理のダッシュボードに足される穴だった（`project_id IS NOT NULL` で塞いだ）。
  `billing.routes` は内部結合で自然に落ちるが、**偶然そうなっている**状態に頼らず
  `gpm_project_id IS NULL` を明示した
- **提出先（自社／依頼元／PM会社）は作るときに必須。** 同じ工事でも社内見積と
  PM 会社への見積は中身も金額も別物なので、空だと「どちらの見積か」が分からなくなる
- **版を作ると行き先と提出先を写す。** 片方だけ写すと CHECK に弾かれるか、
  案件とプロジェクトの両方に出る行ができる
- **「検収待ち」は「受注にしたが検収が済んでいないもの」。** 検収日は売上が持つが、
  **プロジェクトの見積は売上に変換できない**（`revenues.project_id` が NOT NULL）。
  請求前のものも数に入るので、**その但し書きをカードに出している**
- **明細は案件の見積と同じ部品**（`projectDetail/EstimateItems.tsx`）。
  写しを作ると合計と粗利の計算が2か所になり、片方だけ直した日から
  **同じ見積が画面によって違う金額**を出す。保存する口だけ GPM 側
  （`PUT /gpm/estimates/:id/items`）に向けていて、**サーバーはプロジェクトの見積しか
  受け付けない**（`gpm_project_id IS NULL` を 403 で弾く。案件の金額を
  `gpm` だけの人に書かせない）。読むのは `GET /gpm/estimates/:id`（明細つき・
  一覧には明細を積まない）

**カレンダー（①〜④）で決めたこと**
- **カレンダーを1本にした。** 旧メニューは「統合／スタジオ／パートナー／マイ」の
  4本が並び、**どれを開けばよいか分からなかった**（中身はほぼ同じで見えるレイヤーが
  違うだけ）。v4 は **① 予定 = 統合カレンダー1本 + レイヤー**。`/studio/all` は転送
- **パートナーとマイは残す**（「そのほか（作り直し前）」に畳む）。
  **予定を作る導線がそこにしかない** — 消すと作れなくなる
- **① 予定は FullCalendar をやめて自分で描いた**（`pages/calendar/`）。
  モックの月マスは **17px の帯に「3px の色棒 + 時刻 + 題名」**、週・日は
  **重なりを横に割った角丸の札**で、FullCalendar の DOM とは組み立てが違う。
  CSS で寄せると `.fc-*` に依存した規則が何十行も積み上がり、
  **版が上がるたびに黙って崩れる**。**旧スタジオ・パートナー・マイの3画面は
  FullCalendar のまま**（作り直し前の画面なので同じ回で触らない）
- **置き方の計算は `calendar/calendarLayout.ts`。** 間違えると**予定が消える**
  （描画の外に飛ぶ・幅 0 になる）のに、画面を見ても「無い」としか分からない。
  `shared/tests/calendarLayout.test.ts` で 26 項目を固定してある
- **週表と日表は同じ部品**（`TimeGrid`）。違うのは列の数だけ。2つ書くと、
  どちらかだけ直した日から**同じ予定が別の位置に出る**
- **重なりは「同時にいくつあるか」で割る。** 「1件めは左半分・2件めは右半分」に
  すると**3件重なった日に3件めが消える**。塊ごとに列を数えてその数で割る
- **表示（8:00〜22:00）の外から続く予定を捨てない。** 端で切って **↑↓** を
  付けて出す（② 部屋の空きと同じ決め方。捨てると朝から使っている部屋が空きに見える）
- **「いま」の線は時間帯の外なら出さない。** 上端・下端に貼り付くと、
  そこに何かあると読まれる
- **「予定を入れる」を足した。** 着手前の統合カレンダーには**新規作成が1つも無く**、
  「入れるときは各カレンダーへ」と書いてあるだけだった。v4 は ① 予定が1本なので、
  ここから入れられないと**入れる場所が消える**。3つ（部屋・自分・パートナー）は
  持ち物が違うので、まず何を入れるか選んでもらう（1つのフォームにまとめない）
- **祝日は設定 ⑥ の表から読む**（`GET /business-hours/holidays`）。着手前は画面に
  **2025〜2027 が直書き**で、**2028 年になると祝日が1つも出なくなる**状態だった。
  この口だけ**権限を掛けていない** — 祝日は国が決めた公開情報で、`studio` を
  要求するとパートナーの予定しか見ない人のカレンダーで日付が黒いままになる
- **期間に時刻が付いていても通す。** この製品の期間指定は `to=…T23:59` の形が多く、
  日付だけを要求すると呼ぶ側が2通りになる（日表に切り替えたとき 400 を踏んだ）
- **絞り込みは「何が消えるか」を先に書く。** 部屋で絞ると部屋を持たない自分・
  パートナーの予定が、人で絞ると人を持たないスタジオ予約が出なくなる（モックの注記）
- **② 部屋の空きを別画面にした。** ① は「日付ごとに何があるか」の画面で、
  「8/2 の 14 時に WORLD が空いているか」は読めない（全部読んで頭で並べ替えることになる）。
  **部屋 × 時間の1枚**にすると空いている幅がそのまま見える
- **終日の予定は帯にしない**（部屋名の下に出す）。時間の幅を持たないので、
  横一杯の帯にすると**丸1日埋まって見える**
- **表示の外（8:00 より前）から始まる予定を捨てない。** 端で切って必ず出す —
  捨てると朝から使っている部屋が空きに見える。短い予定には最低幅を持たせる
  （線になると空きと見分けが付かない）
- **③ 仮押さえは残り日数順**（日付順ではない）。**過ぎてしまった仮押さえが先頭に来る** —
  押さえたまま忘れられた枠がそれ。7日/14日で色を変える
- **`PUT /studios/bookings/:id` を部分更新に直した。** 元は全置換で、
  `{ status: 'confirmed' }` だけ送ると**件名・日時・部屋が消えていた**
  （`start_time` は NOT NULL なので運が良ければ 500）。仮押さえの一覧から
  状態だけ変えたいので直した。全部送っている既存の呼び出しは今までどおり動く
- **④ 設定はダイアログの中に隠れていたものを出しただけ**（部屋 / 外部カレンダー /
  サイネージ の3タブ）。足す・直すは今までのダイアログをそのまま開く
- **サイネージの URL はログインなしで開ける**（表示機にログインさせられないため）。
  **URL を知っている人はその部屋の予定を全部見られる**ので、画面にそう書いた。
  トークンの作り直しは配ってある URL を全部無効にするので system_admin だけ
- **ICS は黙って止まる**（URL 変更・期限切れ）。予定が増えないだけなので
  見ている人は気づけない。**最後に取れた時刻とエラーを必ず出す**

**設定（①② ＋ システムの情報）で決めたこと**
- **設定トップは「案内板」にした**（`SettingsHubPage`）。旧 `/settings` はアプリの版と
  Excel バックアップが置いてあるだけで、**設定を探しに来た人が欲しいもの
  （拠点・料金表・権限）へは行けなかった**。それらは左メニューの別々の場所にあり、
  **どこにあるか知っている人しか辿り着けなかった**
- **7枚とも押せるようになった。** 着手時は3枚（お金のルール / 休日・営業時間 /
  通知とテンプレート）が画面を持たず、`to: null` ＋ `todo` で「これから作ります」と
  出していた。**`to: null` の仕組みは残してある** — 今後また「並べるが押せない」枚が
  出たときに、押しても何も起きないカードを置かずに済むようにするため
- **「直せるのは誰か」をカードに書く。** 設定は権限の分かれ方が細かく、
  **押してから 403 で気づく**ことが多い場所。権限が無いカードは**カードごと出さない**
- **設定トップとシステムの情報には権限を掛けていない。** 掛けると
  料金表（`sales`）や取引先（`budget`）だけを直す人が来られない。
  旧 `/settings` は `admin` 必須だったので、**パスワードを変えたい人が
  管理者しか来られなかった**。全データバックアップだけ画面の中で `system_admin` に絞る
- **拠点・部屋を設定に置いた理由。** 拠点はカレンダーだけのものではない —
  migration 172 で**料金表が拠点ごと**になったので、拠点を1つ足すと
  「予約できる部屋」と「見積に出る金額」の両方が変わる。
  **部屋の一覧はカレンダーの設定と同じ部品**（`calendarSettings/RoomsTab`）を使う。
  この画面が足しているのは**拠点の段**（部屋数と料金表の品目数）だけ
- **拠点・部屋を直せるのは `system_admin` だけ。** サーバーが
  `requireRole('system_admin')` を掛けている（`studio.routes.ts` の `adminOnly`）。
  **`studio` の manager にボタンを出していたのは間違いで、押すと 403 だった**
  （カレンダーの設定も同じ間違いをしていたので直した）
- **拠点の略称は設定で持つ**（ご要望・migration 189 の `studio_locations.abbreviation`）。
  正式名「GMOサムライスタジオ用賀」は12文字あり、案件詳細の会場のような狭い枠では
  **拠点名だけで埋まって部屋名が消える**。`/settings/sites` の拠点の表が
  **正式名 ↔ 略称の対照表**で、直すのは `StudioRoomsManagerDialog`（拠点も部屋も同じ口）。
  - ⚠️ **コードで正式名を短くしない。**「GMOサムライスタジオ」を機械的に削る細工は、
    拠点が増えた日・名前が変わった日に黙って崩れる。**何を略称と呼ぶかは人が決める**
  - **決めていない拠点には前置きを付けない**（部屋名だけ）。正式名で代用すると枠が壊れる。
    そのため画面には**「未設定」と出す** — 空欄だと短く出せること自体に気づけない
  - **`abbreviation` を渡さない更新は今の値を保つ**（`PUT /studios/locations/:id`）。
    全置換にすると、この欄を持たない呼び出しから拠点名を直すだけで略称が黙って消える。
    消すときは空文字を渡す（**空文字は保存せず NULL**。「決めていない」と区別する）
  - **保存したら `project-studio-bookings` の鍵も落とす。** 会場は予約にぶら下がる
    拠点の略称を読むので、落とさないと**直したのに案件の画面は古いまま**になる
  - **部屋の略称（`studio_rooms.abbreviation`）は前からある**（カレンダーの部屋チェイン用）。
    案件詳細の会場は**部屋名のほうを出す** — 幅があるので「WS」にする必要が無い
- **⑤ お金のルール（`pages/money/`）— 並べるだけにしない。**
  14 項目を表示するだけの画面にすると「設定した気になる」だけで何も変わらない。
  **3つは実際の計算に効かせてある**:
  1. **支払期日** … 締め日＋サイトから逆算（`server/src/shared/services/dueDate.ts`）。
     **取引先ごとの例外が優先**（`customers.closing_day / payment_months / payment_day`。
     **NULL = 決めていない**。0 を既定値に入れないこと — 「0か月後と決めた」と区別できなくなる）
  2. **消費税の端数** … `tax-category.service` の `roundByRule` 1 か所。
     **既定を四捨五入から切り捨てに変えた**（モックの指定）。今後つくる書類の税額が最大 1 円下がる。
     `xpoint-parse.service` が 1.1 / 1.08 を直書きしていたのもここに寄せた
     （放っておくと**取込だけ四捨五入のまま**で、同じ請求が入口によって 1 円ずれる）
  3. **値引きの上限** … 役割ごと（`role_discount_limits`）。超えた見積は
     **保存はできるが `approval_state='pending'` になり、送付済みにできない**。
     承認できるのは**その見積を作った人の役割に決めた承認者だけ**（誰でも承認できたら上限が無意味）
- **期日の計算を画面に写さないこと。** この製品は **server が `shared/` を import しない構成**
  （`rootDir` が `server/src`）。画面にも同じ式を書くと写しが2つになるので、
  **画面は `POST /money-rules/preview` に訊く**（保存前の値も渡せる）。
  計算そのものは `shared/tests/dueDate.test.ts` が server のファイルを直接読んで固定している
- **「上限なし」と 0 は別物。** 空欄を 0 として保存すると**1 円も値引けなくなる**。
  上限を決めていない役割は画面に「決めていません」と出す（「上限なし」と決めたのとは別の見た目）
- **請求書の自動下書きは作らない**（ご判断）。締め処理という新しい業務フローを1つ作ることになり、
  誤って請求書が量産される余地が生まれる。画面には「自動では作りません」と書いてある
- **⑥ 休日・営業時間（`pages/hours/`）— 予約を止めない。**
  着手前は**時間の検査が1行も無く、何時でも予約が作れた**。ここで初めて制限が入るので
  一番ゆるい段から始める（ご判断）:
  - **注意を出して通す。** 保存は止めず、`studio_bookings.out_of_hours` に印を残し、
    `GET /business-hours/out-of-hours/list` であとから拾う
  - **既存の予約は1行も書き換えない**（モックの指定）。列の既定は false のまま。
    遡って印を付けると「昔から時間外だった予約」が大量に出て、見るべきものが埋もれる
  - 休業日を足すときは**保存する前に「重なる予約が N 件あります」を出す**。
    消さない仕様なので、出さないと「休業日にしたのに予約が残っている」に気づけない
  - **割増（＋30％・＋50％）の列は作らない**（ご判断）。何に掛けるかが決まっておらず、
    列にすると入っている数字が正しいと読まれる
- **祝日は計算して表に書き出す**（`server/src/shared/services/holidays.ts` → migration 176 に 89 件）。
  手で入れると毎年 16 件、外の API を見に行くと通信できないときに予約画面が止まる。
  - **初期値は `open`（営業する）。** 放送・制作は祝日こそ稼働するので、
    いきなり「相談のうえ」にすると祝日の予約すべてに注意が出るだけになる。
    **当てても予約の見え方は 1 つも変わらない**
  - **春分・秋分は「予測」。** 政府が前年 2 月に公示するまで確定しない。
    近似式（1980〜2099）で出し、`estimated` の印を画面に出す
- **`closed_days.location_id IS NULL` = 全拠点。** 拠点ごとの行があればそちらが優先。
  こうしないと祝日 16 日 × 拠点 4 = 64 行を毎年作ることになる
- **⑦ 通知とテンプレート（`pages/notify/`）— 社外メールは送らない**（ご判断）。
  モックの 11 本のうち 6 本はお客様に届くメール。差し込みのずれ・きっかけの誤爆・
  古い宛先のどれも**届いてしまうと取り返しがつかない**。文面を貯めて
  **件名と本文を別々にコピーできる**ところまでにした。
  - **「まだ作っていない」ではなく「作らないと決めた」と画面に書く。**
    送信ボタンが無いだけだと「未完成」と読まれる
  - 差し込み語は**埋めずにそのまま**コピーさせる。空にすると「・金額：」のような
    欠けた行になり、値が 0 なのか差し替え漏れなのか読み手に分からない
  - 社外の時刻型2本は送らず、「**送る時期が来ました**」を社内のベルに出す
- **社内通知は上辺バーのベル**（`shared/src/client-v4/NotificationBell.tsx`）。
  **`src/client/` に置かないこと** — 凍結4アプリの CSS が増える。
  シェル側は `notificationSlot` の受け口だけ（新しいクラス名を書かない）
  - 通知を押したときの移動は**素の遷移**（`window.location.href`）。行き先が
    アプリをまたぐ（案件管理のベルに機材の未返却が出る）ので、ルーターでは動けない。
    3アプリそれぞれに「自分の中か外か」を判定させると、間違えたアプリだけ
    押しても動かないという気づきにくい壊れ方をする
- **定時実行（`scheduler.service.ts`）は二重に送らないことが最優先。** 2段で守る:
  1. `scheduled_job_runs(job_key, run_date)` の主キー … その日2回目は行を作れない
  2. `notifications` の一意索引 … 同じ人・同じ対象・同じ日は1行だけ
  片方だけだと、記録を書く前に落ちた回や複数プロセスの回で二重になる。
  **索引を外すと実際に倍になることを確かめてある**
  - 15分ごとに起きて「過ぎていれば流す」形。**時刻ちょうどを狙わない** —
    起きられなかった回（再起動中など）がその日ぶんまるごと飛ぶ
  - 実績の件数は `created = created + ?` で**足し込む**。`= ?` にすると
    画面から流し直したとき「新しく出たのは0件」で上書きされ、その日の実績が消える
  - **SQL の列名は実 DB で確かめてから書くこと。** 型チェックは SQL の中身を見ない。
    `project_tasks.status`（存在しない・正しくは `is_completed`）と
    `due_date` が `date` 型（他は TEXT なので `substr` が落ちる）で実際に踏んだ
- **料金表の場所を URL に持たせた**（`/sales/pricing?location=`）。
  画面の中の状態だけだと、拠点・部屋から送られた側は毎回タブを押し直すことになる
- **メニューの `module` を書いていなかったのを直した。** データビューア・
  DBバックアップ・権限とメンバーは無指定で、**`admin` が無い人にも出ていた**。
  旧 `/settings` 自体が admin 必須だったので気づけなかったが、
  設定トップを開放した以上そのままにはできない
- **権限とメンバー（`pages/members/`）— 役割は「型」にした。**
  モックは **役割5種 × 権限8項目 × 3レベル**で権限は役割にだけ付くが、いまの ONAiR は
  **人ごとに 12 区画 × 5 段**を持ち、その判定が **311 か所**ある。判定を役割ベースに
  置き換えると、取りこぼした 1 か所が「開けない（仕事が止まる）」か
  「見えてはいけないものが見える」になる。そこで **役割は `user_permissions` へ押す型**にした
  （migration 174 ／ `permission-role.service.ts`）。画面は役割が主役、下は今までどおり。
  役割は自分たちで足せる。
  - **`ROLE_MODULES` に凍結4アプリを足さないこと。** 足すと役割を押すだけで
    Qシート・リアルタイムCG などの権限が消える（放送が止まる）。
    守りを外して実際に消えることを確かめてある
  - **表の行は 12 区画で、モックの 8 項目にしない。** モックの 8 のうち 4 つ
    （案件・見積・工程・料金表）は実装では**同じ `sales`**。8 行に分けると
    「見積だけ見るだけにした」つもりで案件も工程も料金表も変わる（`moduleLabels.ts`）
  - **旧「権限修復」ボタンは外した。** 全スタッフに 12 区画すべてを配る作りで、
    押すと経理しか見てはいけない数字が全員に見える。役割ができた今は
    「役割を押し直す」が正しい直し方（API は残してある）
  - 型を直したとき、押してある人への反映は **既定で「しない」**。
    保存時に「この役割の N 名にも反映しますか」を出す

**⑧ 取引先（財務）で決めたこと**
- **仕入先とパートナーを1画面のタブにまとめた**（`pages/CounterpartyPage.tsx`）。
  旧実装は2画面（208行＋224行）で、**中身はほぼ同じ**（名前・担当・電話・メール＋1項目）。
  別画面だと、探すときにどちらにいるか思い出す必要がある。旧 `/budget/partners` は転送
- **請求先（顧客）はここに入れない。** 案件管理の「取引先マスター」で編集する。
  **同じ相手を2か所から直せるようにすると、片方だけ直された相手ができる**。
  画面からはそちらへ送るだけにした
- **フックは1つ。** タブで `endpoint` を切り替えるだけにする（2つ持つと、
  片方だけ検索が残る・件数が古いままになる）
- **送る列は種類で変える。** 仕入先＝`invoice_registration_number`（文字列）、
  パートナー＝`specialties`（配列）。**サーバーが知らない列を送らない**
- **取引額は出していない。** モックは相手ごとの取引額を出すが、
  **期間の取り方（今年度／直近12か月／全期間）が決まっていない**。
  仕入先だけは既存の「仕入先集計」へ送るボタンを置いた

**① 財務ダッシュボードで決めたこと**
- **KPI を8枚並べるのをやめて、引き算の順に並べた**（`financeDashboard/ProfitFlow.tsx`）。
  売上 − 変動原価 = 限界利益 − 固定原価 = 売上総利益 − 販管費 = 営業利益。
  旧実装は同じ大きさのカードが8枚で、**どれとどれを引くとどれになるのかが
  読み取れなかった**（「限界利益」と「売上総利益」が隣にあるのに関係が書いていない）
- **利益（引き算の結果）だけ枠と色を変え、赤字は赤**にする。並べただけでは
  マイナスに気づかない
- **期間の計算と5本のクエリは1行も変えていない。** 金額の集計を、画面の作り直しと
  同じ回で触らない（月／四半期／年／期間指定・案件での絞り込みはそのまま）
- **合計を画面で足し算し直さない。** 内訳は上位だけを出すので、画面側で足すと
  内訳と合計が食い違う。合計は `GET /monthly-summary` が出したものをそのまま出し、
  「内訳は上位だけ・合計とは一致しない」と画面に書いた
- **案件で絞ると販管費が外れることを画面に書く。** 書かないと
  「販管費が 0 になった＝壊れた」と読まれる（販管費は案件に紐づかない）

**② 請求・入金（財務）で決めたこと**
- **月次の締めを一括でやる画面**にした（`pages/ClosingPage.tsx`）。締め月を決めて、
  その月ぶんを選んで「請求書を出す／入金を記録する／検収を記録する」
- **⑤ 見積・請求（案件管理）との住み分け。** あちらは案件をまたいで取りこぼさない
  ための一覧（1件ずつ・期日順）、こちらは締め月ぶんをまとめて処理する画面。
  **書き込む列も検査も同じ**なので、どちらから記録しても食い違わない
- **`sales` か `budget` のどちらかで通す**（`requireAnyPermission` を新設）。
  `billing.routes` は `sales` だけを要求していたので、**経理だけの人は
  月次の締めができなかった**（実際に 403 を確認した）
- **申込書が揃っていない案件は請求書を出せない。** 四角を消すのではなく
  **押せない四角と鍵のアイコン**を出す（消すと「なぜ選べないのか」が分からない）。
  サーバー側でも弾く — 画面で隠すだけだと直接叩けば通る
- **申込書を要求するのは請求書を出すときだけ。** 入金と検収の記録は止めない
- **一部だけ処理したときは飛ばした件数を返す**（`skipped_blocked`）。
  黙って一部だけ入るのがいちばん困る
- **請求書番号は付けていない。** `revenues` に列が無く、採番には
  ①連番の単位 ②取り消したときの扱い ③いま経理が使っている番号との突き合わせ
  を決める必要がある。**勝手に採番すると会計側の番号と二重になる**。画面に明記した
- **売上台帳からのリンク先を `/budget/billing` にした**（同じ財務の中で完結する）

**⑥ 受け取った書類（財務）で決めたこと**
- **日常業務から財務へ移した**（`/daily/finance` → `/budget/documents`）。旧実装は
  `dailyops` 権限だけを要求しており、**経理が開けなかった**（実測で 403）。中身は
  金額・締月・支払期日・GLS番号 で完全に経理の道具なので財務に置き、
  **`dailyops` か `budget` のどちらか**で通す（`requireAnyPermission`）。
  **いま見られる人は見られたまま、経理が見られるようになる**
- **`/daily/finance` は転送だけ残した**（`client-daily/src/App.tsx` の `RedirectToFinanceDocs`）。
  別のバンドルなので `navigate` では飛べず `window.location.replace` を使う
  （`replace` なので「戻る」で転送ページに戻り続けない）
- **日常業務の左メニューとホームのタイルは消していない。** `dailyops` だけの人は
  アプリ切替に財務管理が出ないので、消すと**辿り着く道が無くなる**
- **「処理完了」を本物にした。** 以前は状態が変わるだけで**台帳に何も作られず**、
  同じ請求書を2回入力して突き合わせは記憶頼みだった。いまは「台帳に入れる」で
  仕入か販管費の行を作り、**書類にどの行になったかを記録する**（migration 142 の
  `linked_kind` / `linked_id`）。行き先が2つのテーブルに分かれるので外部キーにはできない
- **承認済みだけ渡せる。** 確認前・却下を入れられると承認のステップが意味を持たない
- **二度渡せない**（409 `ALREADY_LINKED`）。渡し済みをもう一度渡すと台帳に二重に載る
- **金額を勝手に割り戻して保存しない。** 書類の金額は**税込**（`record_finance_doc` の
  説明が「金額 (税込・円)」）で台帳は税抜。書類に税区分が無いので、黙って 1.1 で割ると
  8% や 非課税 の書類で静かにずれる。**目安を初期値に入れ、そう入れたことを画面に書く**
- **取り消しても台帳の行は消さない。** 経理がそのあと直しているかもしれないので、
  勝手に消すほうが危険。書類側の結びつきだけ外して「承認」に戻し、**確認ダイアログで
  「行は残る・いるかどうかは台帳で確かめて手で消す」と明示**する
- **この画面より前に「処理完了」にした書類は `linked_id` が無い。** 黙って隠すと
  二重入力に気づけないので、**結びつきが無いことを行に書く**
- **書き込みは1つのトランザクション。** 分けると台帳に入ったのに書類が未処理のまま残り、
  もう一度渡される

**⑦ 取り込み（財務）で決めたこと**
- **3画面を1画面3タブに畳んだ。** 精算PDF（`/budget/xpoint-import` 980行）・
  総勘定元帳（`/budget/kessan-import`）・二重計上（`/budget/dedup-screening`）は
  やることが全部「外の数字を読む → 中身を確かめる → 台帳に入れる」で同じなのに、
  **手順の見せ方も結果の出し方も確認の訊き方もバラバラ**だった
  （`alert` と `window.confirm` が計4か所）。決算インポートと二重計上は
  **続けて使うもの**なのに別の URL でメニュー経由でしか行き来できなかった
- **旧3 URL は必ず生かす**（毎月使う業務画面でブックマークされている）。
  `?src=pdf` / `?src=gl` / `?src=dedup` へ転送する
- **権限はタブで出し分ける。** 精算PDF は `budget` の editor、総勘定元帳と
  二重計上は `system_admin`（サーバーが `requireRole` で止める）。
  **タブが1つしか無い人にはタブ自体を出さない**（選べないものを選ばせない）
- **ステップの現在地を作り話にしない。** 行の状態（`new`/`parsed`/`registered`）や
  `dryRun` から導く。常に1段目を光らせると飾りになり、2回目からは誰も見ない
- **AI の読み取り信頼度 % を出さない。** モックは行ごとに「読み取り 98%」を出すが、
  `xpoint-parse.service.ts` は正規表現のパーサで**スコアという概念が無い**。
  それらしい数字を置くと、他の画面で守っている決めをここだけ破ることになる
- **「BOX に置く必要はありません」とも書かない。** 解析は Box の extracted_text 一本道で、
  Box が使えない日は直接アップロードも動かない。書くとその日に「壊れた」と言われる
- **金額を組み立てるロジックは1行も変えていない。** 税抜換算（`税込 ÷ (1+税率)`）と
  `billing_key` の作り方はサーバーと噛み合っており、整理のついでに触ると
  **実際の仕入・販管費の金額がずれる**
- **総勘定元帳の「台帳に入れる」は物理削除を伴う。** 対象月のマーカー
  `[kessan:YYYY-MM]` が付いた行を `DELETE FROM` で消してから入れ直す（論理削除ではない）。
  4つのトグルの既定値は旧実装から変えていない
- **二重計上の確認ダイアログから2文を落とさない** —「手で入れた行は残ります」と
  「論理削除なのでバックアップから戻せます」。無いと押す人は何が起きるか分からない

**AI が取り込んだメールの出し方（⑥ と 日常業務⑤ で共通）**
- **`details`（意味の単位の配列）と `body_text`（原文）を持つ**（migration 160）。
  それまでは 差出人・要件・希望日・人数・予算・期限 を**1本の自由文に潰して**
  渡しており、画面は `📝 <そのままのテキスト>` と出すだけだった。
  しかも `finance_docs.content` は**一度も描かれていなかった**
- **AI に HTML を書かせない。** 取引先の文面がそのまま実行される（XSS）うえ、
  書体・色・余白が AI ごとに変わり、後から検索も集計もできない。
  **AI は「何の情報か」を言い、見せ方は `RichContent` が決める**
- 部品は **`shared/src/client-v4/richContent.tsx`**。`shared/src/client/` に置くと
  **凍結4アプリの CSS が増える**（実測4規則・231バイト）ので、
  v4 対象3アプリの Tailwind だけが読む場所に分けてある
- **行に出さず「中身を読む」で開く。** 行に出すと中身の量で行の高さがバラバラになり、
  金額の桁が縦にそろわない
- **古い行は自由文しか持たない**ので `fallback` で今までどおり出す（過去のぶんを消さない）

**財務の台帳3画面（③ 売上 ／ ④ 仕入 ／ ⑤ 販管費）で決めたこと**
- **行の描き方を1つにした**（`pages/ledger/LedgerRows.tsx`）。3画面は列の意味が同じ
  （コード／名前・説明／相手先／金額／税／計上月／状態）なのに、旧実装は
  **売上8列・仕入9列・販管費10列**で金額の桁も揃っていなかった
- **列幅のドラッグを外した。** 3画面に3つの実装があり、他の一覧には無く、保存もされない
  （次に開くと戻る）。揃った幅を部品側で持つほうが桁を読み違えない
- **金額を扱うフォームは中身を変えていない。** ダイアログを別ファイルに出しただけ
  （`ledger/RevenueDialog.tsx` / `ledger/PurchaseDialog.tsx` / 既存の `SgaDialog`）。
  枠の入れ替えと中身の作り直しを同じ回でやらない
- **合計は絞り込み全体**を出す（`total_amount`）。ページごとの合計だと、めくるたびに
  数字が変わって読み間違える。絞り込みチップの件数（`state_counts`）も
  **その軸以外の絞り込みだけ**を掛けて数える（案件一覧の `stage_counts` と同じ考え方）
- **売上の請求・入金の状態はここでは変えられない。** 押すと ⑤ 見積・請求へ移る。
  同じ数字を2か所から書き換えられると、経理の記録が台帳から戻される事故が起きる
- **仕入は変動原価と固定原価をタブで分けた。** サーバーは `fixed_cost` で絞れるのに
  画面から渡しておらず、**案件の原価と月の固定費を足した「合計」**が出ていた
- **販管費は勘定科目で絞れない。** `sga_expenses` に勘定科目の列が無い（モックは
  通信費／地代家賃／旅費交通費で絞る形）。持っている軸（固定費／都度、社員／経理）
  だけを出し、無い理由を画面に書いた
- **直したこと3件（どれも実データで再現を確認済み）**:
  - **売上を案件名で検索できなかった。** `buildRevenueWhere` が請求KEYと備考しか
    見ておらず、画面には「請求KEY・案件名で検索」と書いてあった。仕入も案件名が抜けていた
  - **明細のある売上を直すと明細が消えた。** 編集で開くと `items` を空にしていたので、
    1行足して保存すると**元の明細が全部消える**（サーバーは `items` があると全置換）。
    開くときに `GET /revenues/:id` で明細を読むようにした
  - **`status=all` が「status という文字列が all の売上」を探していた**（＝0件）。
    「確定も見込みも出す」を表す値として扱うようにした。**空文字とは区別する** —
    既定を変えると `status` を渡していない集計（月次サマリ・MCP・Excel）に
    見込みが混ざって金額が変わる

**スマホ（Phase 6）で決めたこと**
- **画面ごとに直していない。** 375px の 29 画面を実ブラウザで測って
  **44px 未満のタップ対象 263 件・13px 未満の文字 728 種**を見つけたが、
  1つずつ直すと必ず取り残しが出るので**部品と型スケールの側**で効かせた
  （`shared/src/client/tokens-v4.css`。中身は `shared/CLAUDE.md`）。
  **結果は 44px 未満 0 件・横はみ出し 0px・JS エラー 0 件**
- **下タブの3つ目を「探す」にした**（`/search`・`contexts/platform/pages/SearchPage.tsx`）。
  v4 の決めごとは ホーム / やること / 検索 だが、**スマホには検索が1つも無かった**
  （上辺バーの `GlobalSearch` は `hidden sm:block`）ので「メニュー」で代用していた。
  **メニューは上辺バーの ☰ からも開ける**ので、下タブの1枠が二重の入口になっていた
- **探すは打ち込む前・打ち込み中・0件を分ける。** 打つ前に「該当なし」と出すと、
  探す前から無いと言うことになる。権限はサーバー（`GET /search`）が種類ごとに見るので
  画面では止めない — 止めると案件だけの人・経理だけの人が検索そのものを使えなくなる
- **カレンダーの回遊ピルの行き先が壊れていたのを直した。** v4 で
  `/studio/calendar` が ① 予定（統合）になり `/studio/all` が転送になったので、
  **「統合」と「スタジオ」が同じ画面に着いていた**（実ブラウザで確認）
- **文中のリンクは `py-[13px] -my-[13px]` で当たり判定だけ広げる。** 行の高さを
  変えずに 44px を確保する（素で高さを足すと段落の行送りが崩れる）
- **トップの「完了にする」四角はスマホだけ 44×44。** 四角そのものは 18px のまま中に置く
  — 押し間違えると取り消しに行くことになるので、指で確実に当たる大きさが要る
- **モックのスマホ 13 枚はすべて作った**（⑩ は ⑬ に統合したので画面は 12 枚）。
  13 枚の中身・決めごと8つ・作る順番は **[docs/design/v4/mobile.md](../docs/design/v4/mobile.md)**。
  **下タブは3本**（モックの中で3本と5本に割れており、ご判断で3本に決めた）
- **幅で選ぶのは「薄い親」で行う。** 中で `if (mobile) return …` と書くと、
  幅が変わった瞬間に**フックの数が変わって React が落ちる**。判定は
  `client-v4/mobile.ts` の `useIsMobile()` 1本（`lg`=1023px と同じ値）
- **⑬ 今日の予約に ⑩ 今日の現場を畳んだ**（`rooms/MobileToday.tsx`）。
  区分の「リハ・本番」はどちらもスタジオ予約で ⑬ と9割同じ。2枚に分けると
  同じ予約を2つの画面が別々に描く。違いは「出庫」だけなので
  **機材の層として重ねた**（`equipment` の権限がある人にだけ出す）
- **⑫ 入金の確認でできるのは記録だけ**（`closing/MobileCollect.tsx`）。
  取り消しは PC から — スマホで戻せると誰がいつ戻したのか分からなくなる
- **③ 案件一覧はカード**（`projectList/ProjectCards.tsx`）。
  **絞り込みと問い合わせは1つのまま、行の描き方だけ差し替えた**。
  ボードは 375px に1列も入らないので出さず、`?view=board` で来たら理由を書く
- **⑥ 案件詳細のタブはスマホで3つ**（`tabs.ts` の `MOBILE_TAB_KEYS`）。
  **開けないタブを黙って概要にすり替えない** — URL を共有された人が
  「見積を見せたのに概要が出た」ことになる
- **⑪ 探すの「最近見たもの」はシェルの中の1か所で積む**
  （`platform/RecentTracker.tsx`）。画面ごとに `pushRecent` を足すと
  書き忘れが起き、しかも**気づけない壊れ方**をする（出ないだけ）。
  **読めてから積む** — URL だけで積むと 404 の行が残る
- **④ 案件をつくるはスマホだけ3段**（`projectNew/MobileNewProject.tsx`）。
  **送るところは PC と共通**（`projectNew/useCreateProject.ts`）— 写すと、
  列が増えたとき片方だけ送らなくなり、引き合いへの書き戻しも片方だけ忘れる
  （忘れると未仕分けに残り、**同じ引き合いから案件が2件**できる）。
  モックの「標準工程テンプレートが決まります」は**書かない** — つくった時点では
  工程は入らない（タスクタブの帯から**一覧を見せて**から入れる形にした）
- **動き（手触り）は `shared/src/client/tokens-v4.css` の末尾**。画面遷移・押した手応え・
  キーボードの現在地・帯・カード。**画面ごとに書かない**（中身は `shared/CLAUDE.md`）。
  `motion` / `animated-number`（framer-motion）は**使わない** — v4 の動きは CSS で足りる

## v4 で作り直す画面

案件管理8画面（ダッシュボード／受付／案件一覧／タスク一覧／見積・請求／案件詳細／標準工程／料金表）、
財務8画面、カレンダー4画面、設定7画面、プロジェクト管理7画面（新規）。
仕様は `docs/design/v4/` に画面ごとに切り出す（モック HTML を直接 grep しない）。

**v4 の設計判断（モックが明示しているもの）**
- **案件担当者という概念を持たない。** 誰が何をするかは**タスク単位**で表す
- 見積は明細をスタジオ／技術・人員／制作・その他の3グループで表示。行ごとに仕入（見込み）を持ち、
  **粗利率が30%を切ると赤くなるが保存は止めない**。値引きは単価を下げず別建て
- 値引き上限の超過は**保存を止めず「承認待ち」**にする（送付だけ止まる）
- **金額の同時編集は 409 で止める**（他の項目は同時編集可）

## 触るときの注意

- **シェルは共通** (`shared/src/client/shell/`)。残っているのは
  `components/layout/AppShell.tsx`・`nav.ts`（4つの入口ぶんのメニュー）・
  `GlobalSearch.tsx`（上辺バーに差し込む検索）だけ。**旧 `Header.tsx` / `Sidebar.tsx` は削除済み**。
  どの入口にいるかは `appOfPath()` が URL から判定する（`BLOCK_APPS` の前方一致は廃止）

- **スマホの絞り込みは1行に畳んでシートで開く**（M6 → M8 で共通部品
  `shared/src/client-v4/mobileFilterBar.tsx` に移した。`projectList/MobileFilterBar.tsx` は
  **この画面固有の2つ**＝「何を既定と見なすか」（`activeFilterCount`）と
  シートの中身だけを持つ）。
  PC の帯をそのまま縦に積むと約 450px になり、**最初の案件に着くまで 470px**
  ＝ 1画面の7割が枠でした（実測）。畳んだあとは **284px**。
  - **props は PC 版と同じ**（`FilterBarProps` を共有）。写しを作ると片方だけ
    絞り込みが増えて、PC とスマホで違う結果が出る
  - **効いている数をボタンに出す。** 畳むと絞り込んでいること自体を忘れる
    （「12件しかないのはなぜ？」）。0 のときは出さない
  - **検索だけは畳まない。** 探すのは絞り込みではなく目的そのもの
- **`hidden: true` を付けた画面はスマホの左メニューと設定トップから消える**（M6・ご判断）。
  データを入れる道具（決算の取込・DB バックアップ・データビューア）と、
  案件の仕事に出てこない設定です。**ルートは消していない**ので、
  共有された URL を開けば今までどおり案内が出ます。
  - 落とす一覧は**左メニューと設定トップで同じ表**（`CLIENT_MOBILE_HIDDEN`）。
    2つに分けると片方だけ直したときに食い違う
  - 実測: 財務の左メニュー 8 → 7 項目（「取り込み」の節ごと消える）、
    設定トップ 10 → 7 枚
- **「外で判断するもの」は PC 専用から外した**（M10・ご判断）。実データを入れた検証環境で
  44 画面を 390px から開き直して測ったところ、**横にはみ出す画面は3枚だけ**で、
  多くはスマホでも普通に開けました（`/sales/customers` は表ですらなくカードだった）。
  基準は「**読む／1タップで進める**ものは開ける・**入力欄が並ぶもの・設定・
  データを入れる道具は PC のまま**」。第1波で開けたのは **v4 で作り直し済みの5枚**:
  `/studio/holds`（`production/pages/holds/HoldCards.tsx` でスマホ用に縦積み。
  PC の行だと「決める」160px に押されて予定名が 150px しか残らず、
  **何の予約か分からないまま「確定にする」を押させる**形だった → 283px）／
  `/budget/vendors`（Excel と「仕入先集計」をスマホから落とす。
  **後者は行き先が PC 専用で、押すと行き止まりになる**）／
  `/gpm/dashboard` `/gpm/projects` `/gpm/tasks`（そのままで崩れない）。
  - **旧のまま（v4 で作り直していない）4枚は据え置き**: 顧客・取引先マスター・
    お客様の詳細・営業活動記録。**理由文は書き直した** —
    元は「この幅では1社ぶんも並びません」だったが実測で嘘だと分かったため
    （本当の理由は「まだ作り直していないので指で押しにくい」）
- **画面を足したら `src/pcOnlyScreens.ts` のどちらかの表に入れること**（M2）。
  `CLIENT_PC_ONLY`（スマホでは開かない）か `CLIENT_MOBILE_OK`（スマホで触る／読む）で、
  **どちらにも入っていないと `npm run lint` が止まります**。
  - スマホでは `AppShell` の `<PcOnlyGate>` が表を見て**案内に差し替えます**
    （`shared/src/client-v4/pcOnly.tsx`）。画面ごとに `useIsMobile()` を書かないこと —
    書き忘れても「出ないだけ」なので誰も報告せず、**数えられなくなります**
  - **並び順が効く**（先に一致したものが勝つ）。`/gpm/projects/new` は
    `/gpm/projects/:id` より前に置く
  - **`/budget/billing` は PC 側に入れない** — ⑫ 入金の確認がスマホ用にある。
    モックの「お金は置かない」が指すのは**台帳の表**で、**片づく1つの仕事**は置く
  - 案件詳細のタブだけは画面の中で判定する（`MOBILE_TAB_KEYS`）。
    URL は同じで中身が変わるので表では書き分けられないため。
    案内は同じ `PcOnlyPanel` を使う

- **1ファイル400行を上限にする。** いま超過しているもの:
  `contexts/sales/pages/ProjectFormPage.tsx` 2,178行 /
  `contexts/production/components/episodes/BusinessProjectView.tsx` 2,042行 /
  `contexts/finance/pages/RevenueListPage.tsx` 1,435行 /
  `contexts/platform/pages/HomePage.tsx` 1,313行
  → v4 で作り直すときに分割する（案件詳細は7タブなので7ファイルが自然）
- `src/components/ui/` は **`motion` / `animated-number` の2本以外すべて1行の再エクスポート**。
  実体は `shared/src/client/ui/`（F3 で `table` / `searchable-select` / `currency-input` /
  `scroll-area` を移した。`dropdown-menu` は参照0件だったので削除）。
  **新しい共通部品は `shared` に置くこと** — ここに実装を足すと他アプリから使えない
- **`motion` / `animated-number` だけ残してある**。`framer-motion` が案件管理にしか入っておらず、
  v4 は hover を色・罫線だけに絞り画面遷移も CSS で行う（`docs/design/v4/_tokens.md`）ため。
  **v4 の画面を作るときは使わない**（Phase 2 で整理する）
- `src/index.css` にアプリ固有 CSS が約200行（FullCalendar の上書き・サイネージ）。
  サイネージの色はトークン外の直書き
