# GMO ONAiR

GMOグローバルスタジオの制作管理プラットフォーム（会社OS）。
複数のブロックアプリを単一のモノレポで束ね、案件・Qシート・機材・インタラクティブ演出・技術資料・ライブ運用を**GLS番号**を中核に連携させる統合業務システムです。

**本番環境**: https://gmo-onair.jp
**検証環境**: https://dev.gmo-onair.jp
**現在のバージョン**: v3.0.3 — **スマホで通知ベルのパネルが左端から欠けて見える崩れを直しました**。通知ベルは上辺の最右端の要素ではなく右にユーザーメニューが続くため、ほぼ画面幅のパネルをベルの親要素に相対配置すると左端が画面外にはみ出し、切り取られていました。「⋯」メニュー・ユーザーメニューと同じく `document.body` に描画し実測位置で固定配置する方式に揃えて解消しました。検証: 全9ワークスペースの型チェック・ビルド通過、`eslint` 0 errors / 77 warnings（着手前と同数）。 <br>過去の版の要約は [`docs/release-notes.md`](docs/release-notes.md)、全文は [`docs/version-history.md`](docs/version-history.md) にあります。

旧 v2.8.128: 表彰CG 投票パターン UX 刷新（オールスター感謝祭風レイアウト・大型 PinP・スライド型カウントダウン・ラスト5秒巨大表示・棒グラフ豪華化・自動 GRAND PRIX 遷移・GRAND PRIX フルスクリーン刷新）+ **余興用 3 択 Standalone Poll** を新設（表彰DB と独立、画像入稿 or 画像なしも可）

---

## 目次

- [プロジェクト概要](#プロジェクト概要)
- [ブロックアプリ構成](#ブロックアプリ構成)
- [技術スタック](#技術スタック)
- [ディレクトリ構造](#ディレクトリ構造)
- [アーキテクチャ](#アーキテクチャ)
- [データモデルの核](#データモデルの核)
- [開発環境セットアップ](#開発環境セットアップ)
- [ビルドとデプロイ](#ビルドとデプロイ)
- [ブランチ運用](#ブランチ運用)
- [主要なドキュメント](#主要なドキュメント)
- [コード健全性 / 既知の課題](#コード健全性--既知の課題)

---

## プロジェクト概要

GMO ONAiR は、単一の React/Express モノレポ上に**6つのブロックアプリ**を並走させ、制作業務の全ライフサイクル（提案 → 受注 → 制作 → 本番運用 → 請求）を1つの業務OSとして提供します。

- **案件管理（client）**: 売上・仕入・損益・ヨミ〜完了までの統合プロジェクト管理
- **Qシート（client-qsheet）**: 進行台本作成・ランダウン・OnAir 同期
- **機材管理（client-equipment）**: 機材台帳・貸出管理
- **技術資料（client-techsheet）**: カメラ・映像・音声技術仕様書
- **ライブ運用（client-live）**: 本番オペ・進行管理
- **リアルタイムCG（client-awards）**: リアルタイム放送CG演出・送出管理

全アプリは **GLS番号**（例: `GLS-A001`）という統一識別子で紐づき、単一の PostgreSQL データベースを DB 名で本番/検証に分離して共有します。

---

## ブロックアプリ構成

| アプリ | ディレクトリ | 公開パス | dev ポート | 概要 |
|---|---|---|---|---|
| v2.9.275 | AI が編集中の案件に追記できるように (追記だけ・既存の文字は消さない・AI の印つき・開いている画面に即出る)。人がその後どうしたかを次の読み取りで記録して傾向に返す。MCP 3ツール追加 (計85種) |
| v2.9.274 | 案件にコメントと知らせる人 (ベルに「案件で名前を呼ばれた」として出る・消えるのは「対応した」を押したときだけ) + 変更の記録 (主要な10項目だけ・改名や退職でも読めるよう名前も残す) |
| v2.9.273 | 案件の「みんなで書くメモ」とチェックリストを同時編集に (メモは文字単位マージ・金額と案件の中身は対象外で誰がどの欄を触っているかを awareness で表示) |
| v2.9.272 | 問い合わせの返信の下書き。AIが作る → 人が直して保存 → コピーして自分のメールで送る → 「送った」を記録。ONAiRは送らないと明記。全文/修正差分/成果を既存の ai_outputs・ai_corrections・ai_outcomes に入れ、生成前に自分の傾向を読んでプロンプトに載せる (使うほど賢くなる) |
| v2.9.271 | 投入から生まれた個人タスクを案件ボードに出さないと決定し、「全体」の画面に「ここに出るのは案件に紐づくタスクだけ」+「自分」への導線を明記 (黙って出さないと壊れて見える) |
| v2.9.270 | 権限の変更履歴 (誰が・いつ・どの行を どう変えたか) を新設。1行=1モジュールの変化・実際に変わった行だけ・消さない・変えた人の名前をそのとき残す・役割テンプレートを当てた保存かどうかも残す。履歴ゼロのときは「記録開始より前」と断って誤解させない |
| v2.9.269 | 見積・請求の原本 (PDF/画像) をブラウザから付けられるように。承認画面で原本を読み取り値より上に置き、PDFは画面内に表示。実体は Box (フォルダはサーバーが自動作成)。写真も通す。PDFを落とすと行そのものを作れ、下読みできた値だけ埋めて人が確認する |
| v2.9.268 | 朝の1通を Slack に送れるように。個人DM（自分の分だけ・出すものが無い人には送らない）とチャンネルへの投稿（1件=1回なので朝と夕方で内容を変えられる）を分け、出す内容を10種から選べる。金額を選ぶと「権限の外に出る」と赤く警告し個人名は出さない。送る前に本文を確認でき、遅れて起きたときは1時間以内なら追いかける |
| v2.9.267 | 全体レビューで `data?.x.y` の実バグ5件を修正し ⌘K の重複行き先を解消。やり残しを実装 — 案件の409競合バナー（`expected_updated_at` で上書きを止める）と在席アバター、見積・請求の二重計上の警告、問い合わせから「ネタ案件にする」（冪等）、音声サポートの配布URLの無効化（410）、行列ゼロで「直近7日で N件」 |
| v2.9.266 | 権限を役割テンプレートで当てられるように（見えるレールのプレビュー付き）。ふりかえりを `/review` の1画面に統合し、今週は件数より「期限内に終えた割合」を先に出す。現場の道具の成果物を残して後から案件に紐づけられるように。スマホは下タブを 今日/案件/タスク/予定/さがす の5つに絞り、案件のフォームを畳み、予定を今日からの並びに (全13フェーズ完了) |
| v2.9.265 | Qシートの稿番号を手動保存で上げないようにし「稿を上げる」を明示操作に。本番を `?role=` の1入口+役割切替に統合し、受け取り側3画面に放送同期の切断表示。案件メンバーへの既定共有・出す列チップ・保存前でも効く「直前の変更」 |
| v2.9.264 | 機材の14メニューを日々/モノ/設定に再編。日々に返却遅延・今日と明日の出し入れ・種別在庫を集約し、台帳5つを種別タブに統合。案件から貸出を始められるように |
| v2.9.263 | 通知をベル(溜まる場所)と朝の1通だけに。トーストを廃止して「消えないお知らせ帯」に置き換え、通知は保存せず既存データから導出 (migration 137 は受け取り方のみ) |
| v2.9.262 | お金をKPI7枚から「損益の流れ」に。3列レビュー表示 (?view=review3col) とMTG印刷を追加し、取込を「取り込む→確認する→登録する」の1本道に統合 |
| v2.9.261 | カレンダー4本を /schedule 1本に統合 (レイヤー切替)。この画面から3種の予定を登録でき、期限が近い仮押さえをその場で本予約にできる |
| v2.9.260 | お客様一覧を「取引の状態」に (累計売上・案件・最終接点・次の一手)。顧客360をその場編集 + 4タブ化し、取引先マスターを請求先タブに統合 |
| v2.9.259 | タスクを `/tasks` 1画面に統合 (スコープ × 表示形式)。日常業務アプリの実装を移設し、同じ期限が2画面に出ていた状態を解消 |
| 案件管理 | `client/` | `/` | 5173 | 案件・売上・仕入・損益、ダッシュボード |
| Qシート | `client-qsheet/` | `/qsheet/` | 5174 | 進行台本・ランダウン・OnAir |
| 機材管理 | `client-equipment/` | `/equipment/` | 5175 | 機材台帳・貸出 |
| 技術資料 | `client-techsheet/` | `/techsheet/` | 5177 | カメラ/映像/音声 技術仕様書 |
| ライブ運用 | `client-live/` | `/live/` | 5178 | 本番オペ・進行管理 |
| リアルタイムCG | `client-awards/` | `/awards/` | 5179 | リアルタイム放送CG演出・送出管理 (内部識別子は `awards`) |

### 外部リンク (別 VPS / 別タブで開く)
- **インタラクティブ**: <https://interactive.gmo-onair.jp/> — EventStamp・リアルタイム演出
- **翻訳**: <https://gmo-translate.jp/> — GMO 翻訳ツール

全アプリは同一ドメインで配信され、nginx のパスベースルーティング + Express の静的配信で分岐します。認証とAPIは共通（`/api/v1/internal/*`）です。

---

## 技術スタック

### フロントエンド
- **React 18** + **Vite 6**
- **TypeScript**
- **TailwindCSS 3** + **shadcn/ui**
- **TanStack Query** (React Query) — サーバー状態管理
- **Zustand** — クライアント状態管理（UI ストア等）
- **React Router v6**
- **Socket.IO Client** — Qシート/リアルタイムCG/計時LIVE でリアルタイム同期
- **Lucide React** — アイコン

### バックエンド
- **Node.js** + **Express** + **TypeScript**
- **PostgreSQL 16** (`pg` ドライバ)
- **Socket.IO** — `/qsheet` / `/awards` ほか各アプリ名前空間
- **Passport.js** / **JWT** — 認証
- **pdfkit** — Qシート PDF 生成

### インフラ
- **Docker** + **Docker Compose**
- **Nginx** — リバースプロキシ + SSL
- **Let's Encrypt** — SSL 証明書
- **CoNoHa VPS** (東京) — 本番/検証を単一 VPS 上で並走

### 開発運用
- **npm workspaces** — モノレポ
- **Dev Containers** — ローカル開発を隔離
- **Gitea/GitHub Webhook** — `main` ブランチへの push で本番自動デプロイ

---

## ディレクトリ構造

```
gmo-onair/
├── client/                  # 案件管理アプリ (React)
├── client-qsheet/           # Qシートアプリ
├── client-equipment/        # 機材管理アプリ
├── client-techsheet/        # 技術資料アプリ
├── client-live/             # ライブ運用アプリ
├── client-awards/           # リアルタイムCG アプリ (内部識別子は `awards`)
├── shared/                  # 全アプリ共通のファクトリ関数・ユーティリティ
│   └── src/client/
│       ├── createApi.ts           # axios ファクトリ
│       ├── createAuthHook.ts      # useAuth フック ファクトリ
│       ├── queryClient.ts         # React Query 共通設定
│       ├── uiStore.ts             # Zustand UI ストア
│       ├── SharedHeader.tsx       # 共通ヘッダー
│       └── AppSwitcher.tsx        # アプリ切替ドロップダウン
├── server/                  # Express + PostgreSQL API
│   └── src/
│       ├── contexts/              # DDD 風コンテキスト分割
│       │   ├── sales/             # 案件・売上・仕入
│       │   ├── finance/           # 予算・損益・販管費
│       │   ├── production/        # 制作進行
│       │   ├── equipment/         # 機材管理
│       │   ├── qsheet/            # Qシート
│       │   ├── techsheet/         # 技術資料
│       │   ├── interactive/       # インタラクティブ演出
│       │   ├── liveops/           # ライブ運用
│       │   ├── asset/             # 資産マスター
│       │   └── platform/          # 認証・共通
│       └── shared/
│           ├── db/migrations/     # 60+ SQL マイグレーション
│           └── services/          # 共通サービス（GLS発番・PDF生成等）
├── nginx/                   # Nginx 設定
├── deploy/                  # VPS デプロイスクリプト
├── scripts/                 # ユーティリティ（deploy-webhook.js 等）
├── docs/                    # 設計ドキュメント
├── docker-compose.yml       # 本番 + 検証の2環境構成
├── Dockerfile
├── CLAUDE.md                # プロジェクトメモリ（開発方針・ポリシー）
└── package.json             # ワークスペース定義
```

---

## アーキテクチャ

### デプロイ構成（CoNoHa VPS）

```
┌──────────────────────── CoNoHa VPS ────────────────────────┐
│                                                             │
│  Nginx (443/80) ─ SSL 終端・パスベースルーティング           │
│     │                                                       │
│     ├─ / ─────────────► app_prod (3000) ──┐                │
│     │    /qsheet/                          │                │
│     │    /equipment/                       │                │
│     │    /techsheet/                       │                │
│     │    /live/                            │                │
│     │    /awards/                          │                │
│     │    /api/v1/internal/*                │                │
│     │    /socket.io/   ← WebSocket         │                │
│     │                                      │                │
│     └─ dev.gmo-onair.jp ──► app_dev (3001) │                │
│                                            ▼                │
│                         ┌─────── PostgreSQL 16 ───────┐    │
│                         │  onair_prod    onair_dev    │    │
│                         └─────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### リアルタイム通信

- **`/qsheet` ネームスペース**: OnAir ↔ ランダウン画面の同期（cue:update, sync, next, prev, jump, play, pause, reset）
- **`/awards` ネームスペース**: リアルタイムCG の cue:set / cue:sync ブロードキャスト
- **計時LIVE / Quiz** ネームスペース: タイマー同期・クイズ進行
- (旧 `/interactive` ネームスペース は v2.9.17 で別 VPS に切り出し)

### 認証

環境変数 `GOOGLE_CLIENT_ID` の有無で自動切替：
- **未設定**: mockAuth（ユーザーカード選択式、検証環境用）
- **設定済**: Google OAuth 2.0 + Passport.js（招待制）

本番環境では加えて Email/Password + SMS 2FA（Twilio）も利用可能。
JWT は HTTP-only Cookie + Authorization Bearer の二重送信方式。

### サブアプリ間の状態共有

各アプリはそれぞれ独自の `storageKey` で JWT を管理：
`qs_user`（qsheet）, `ts_user`（techsheet）, `is_user`（interactive）, `eq_user`（equipment）。
共通ファクトリ (`shared/src/client/`) を各アプリのラッパーファイルで包んでインスタンス化します。

---

## データモデルの核

### GLS番号による統一キー

全アプリは **GLS番号**（例: `GLS-A001`、`GLS-B003`）を中核に紐づきます：
- **A系**: 制作系（`offline_event`, `hybrid_event`, `live_broadcast`, `recording`）
- **B系**: その他売上

採番は `sequences` テーブルで管理され、`server/src/shared/services/sequence.service.ts` の `generateGlsNumber()` が発行。

### 統合プロジェクトライフサイクル

旧来の `opportunities`（ヨミ）+ `projects`（受注後）の2テーブル分離を**単一 `projects` テーブルに統合**：

```
stage フィールド:
  neta → d_hold → c_proposal → b_verbal → a_won → s_completed
                                                 └→ e_lost

gls_number IS NULL = ヨミ段階
gls_number IS NOT NULL = GLS発番済み（ヨミ以降のステージ）
```

GLS発番は `POST /projects/:id/issue-gls` エンドポイントで別途トリガー。

### エピソードと按分グループ

- **`episodes`**: 1プロジェクトに複数のエピソード（例: `GLS-A002-001`）
- **`project_groups`**: 費用按分グループ（売上・仕入の按分配分に使用）
- **タグベースの分類**: プロジェクトには自由なタグを付与可能

### Qシート / インタラクティブ / 技術資料

- **`qsheet_documents`**: JSONB で進行台本全体を保存（sections / rows / blocks 構造）
- **`interactive_*`**: スタンプ定義・チャンネル・統計
- **`techsheet_documents`**: カメラ/映像/音声/通信のタブ式技術仕様

詳細は `server/src/shared/db/migrations/` の 60+ のマイグレーションを参照。

---

## 開発環境セットアップ

### 前提条件

- Node.js 20+
- Docker + Docker Compose（DB 起動用）
- または Dev Containers 対応 IDE（VSCode 推奨）

### ローカル開発（Dev Containers）

`.devcontainer/` で隔離環境を提供。
コンテナ内で以下が完結：

```bash
npm install
npm run dev    # 全アプリ並列起動（concurrently）
```

各アプリが個別ポートで立ち上がります（ポート 5173〜5178）。

### 個別アプリのみ起動

```bash
npm run dev:client       # 案件管理
npm run dev:qsheet       # Qシート
npm run dev:interactive  # インタラクティブ
npm run dev:server       # Express API
```

### 環境変数

`.env.example` をコピーして使用：

```bash
cp .env.example .env
```

重要な変数：
- `DATABASE_URL` — PostgreSQL 接続文字列
- `JWT_SECRET` — 本番では `openssl rand -hex 32` で生成
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` — 本番の OAuth 用
- `TWILIO_*` — SMS 2FA（本番のみ）

### DB 初期化

```bash
npm run db:migrate   # マイグレーション実行
npm run db:seed      # シードデータ投入（検証環境のみ）
npm run db:reset     # 全リセット + re-seed
```

---

## ビルドとデプロイ

### ビルド

```bash
npm run build    # shared → 全クライアント → server の順に TypeScript ビルド
```

各クライアントは Vite で静的ファイルを生成し、Express が静的配信します。

### 本番デプロイ

**トリガー**: `main` ブランチへの push
**フロー**:
1. GitHub Webhook が `scripts/deploy-webhook.js` (VPS 上、ポート 9000) に通知
2. VPS が `git pull origin main && docker compose up -d --build` を実行
3. `app_prod` コンテナが再ビルド・再起動される

**デプロイ時間**: 約2〜3分

### 検証デプロイ

**トリガー**: `dev` ブランチへの push
検証環境 (`dev.gmo-onair.jp` / `app_dev`) に自動反映されます。

### 緊急ロールバック

```bash
# VPS 上で
git checkout <旧コミット>
docker compose up -d --build
```

---

## ブランチ運用

- **`main`** — 本番デプロイの起点（push で webhook 発火）
- **`master`** — `main` のミラーブランチ（`master ← main` の PR で定期同期）
- **`dev`** — 検証環境の起点
- **`feature/*`, `claude/*`** — 機能ブランチ（main または dev から派生）

### リリースフロー

```
feature/xxx → dev → (検証環境で動作確認) → main → (本番自動デプロイ)
                                              ↓
                                            master (ミラー同期)
```

### バージョン管理

- パッチバージョンのみインクリメント（例: v1.1.100 → v1.1.101）
- `CLAUDE.md` + 全ワークスペースの `package.json` を同期更新
- コミットメッセージに `vX.X.X` を明記

---

## 主要なドキュメント

| ファイル | 内容 |
|---|---|
| [`CLAUDE.md`](./CLAUDE.md) | プロジェクトメモリ（開発方針・環境分離ポリシー・セキュリティポリシー） |
| [`docs/DEPLOY_CONOHA.md`](./docs/DEPLOY_CONOHA.md) | CoNoHa VPS への初回デプロイ手順 |
| [`docs/architecture/v1.0-domain-design.md`](./docs/architecture/v1.0-domain-design.md) | ドメイン設計（案件・エピソード・売上・仕入のモデル） |
| [`docs/architecture/v1.0-data-model.md`](./docs/architecture/v1.0-data-model.md) | データモデル詳細 |
| [`docs/architecture/v1.0-roadmap.md`](./docs/architecture/v1.0-roadmap.md) | 開発ロードマップ |
| [`docs/architecture/improvement-proposal.md`](./docs/architecture/improvement-proposal.md) | 改善提案 |
| [`docs/reviews/code-review-2026-04-28.md`](./docs/reviews/code-review-2026-04-28.md) | フルソースコードレビュー報告（2026-04-28） |

---

## コード健全性 / 既知の課題

**最終確認: v2.9.289（2026-07-26）の全体コードレビュー時。数字はすべてその場で実測したもの。**

### 解消済み（前回レビュー 2026-04-28 の High / Medium）

| 項目 | 当時 | 現在 |
|---|---|---|
| Tailwind バージョン不整合 | 宣言 `^3.4.16` に対し lockfile が `4.x` を解決し `npm ls tailwindcss` が `invalid` | **解消**。全ワークスペースで `3.4.19` に dedupe 済み、`npm ls tailwindcss` は正常終了 |
| フロントエンド build 失敗 | ルート `npm run build` が PostCSS/Tailwind エラーで停止 | **解消**。9 ワークスペースすべて通過 |
| ESLint 基盤未整備 | `npm run lint -w client` が flat config 不在で失敗 | **解消**。ルート `npm run lint` が 0 errors / 77 warnings で通る（禁止パターン検査 + eslint） |

### 未解消（隠さず残す）

| 優先度 | 項目 | 現状（実測） | なぜ今やらないか / 対応方針 |
|---|---|---|---|
| **High** | `xlsx` (SheetJS) の脆弱性 2 件 | Prototype Pollution (GHSA-4r6h-8v6p-xvw6) と ReDoS (GHSA-5pgg-2g8v-p4x9)。**npm に修正版が無い**（SheetJS は 0.18.5 以降 npm への公開を止め、修正版 0.19.3 / 0.20.2 は自社 CDN のみ） | v2.9.289 で**境界で止める**形にした（`server/src/shared/utils/xlsx-safe.ts` に集約。中身を見て表計算ファイルかを判定 + prototype 汚染を検知して消して拒否。`XLSX.read` の直接使用は lint で禁止）。**ライブラリ差し替え（exceljs 等）は別の版で**行う — 読み取りは機材台帳 / 決算 GL / アワードのエントリの 3 系統で使われており、日付・数値・結合セル・空セルの扱いが 1 つ変わるだけで「今まで入っていた表が入らない」形の事故になるため、実データで突き合わせられる環境が必要 |
| **Medium** | `react-router` 6.x の Open redirect / SSR hydration | 修正は **7.18.0 以降**（6.x に修正版が無い）。7 クライアントすべてが `^6.28.0` | v6 → v7 は大版更新で全アプリのルーティングに触る。**単独の版として実施する**（他の変更と混ぜると原因の切り分けができない）。当面の影響は `<Link>` / `useNavigate` にユーザー入力の URL を渡す経路に限られる |
| **Medium** | `box-node-sdk` 3.x 経由の `qs` / `uuid` / `@cypress/request` | 修正は `box-node-sdk` **4.13.0**（大版更新） | BOX 連携（バックアップ / フォルダ生成 / ファイル保存 / Sign）の全経路に影響し、検証には BOX の資格情報が必要。**実環境で突き合わせられる版で実施** |
| **Low** | `eslint` 9.x 経由の `minimatch` / `brace-expansion` ReDoS | 修正は `eslint` **10.x**（大版更新） | **開発時のみ**動くもので本番の実行経路に無い。ルールセットが変わると警告の基準値（77件）が動くため、独立した版で更新する |
| **Low** | MCP SDK 経由 `@hono/node-server` のパストラバーサル | **Windows 限定**。本番は Alpine Linux | 実行環境で発現しない。SDK の更新時にあわせて解消 |
| **Medium** | `alert()` / `confirm()` が **129 か所** | ブラウザ標準のダイアログが残っており、新しい通知・確認の部品と見た目も挙動も揃っていない | 次回のレビューで置き換える（v2.9.289 の残タスク） |
| **Medium** | 共通の状態表示部品の適用率 | **118 ページ中 46 ページ**（読み込み中 / エラー / 空 / 権限なしの表示） | 同上 |
| **Medium** | 1000 行を超えるファイルが **13 本** | 最大 `ProjectFormPage.tsx` 2,513 行 / `BusinessProjectView.tsx` 2,045 行 / `EquipmentListPage.tsx` 2,018 行 | 分割は挙動を変えずに行える一方で差分が大きくなるため、**バグ修正と同じ版に混ぜない**（レビュー不能になる）。独立した版で 1 ファイルずつ |

> `npm audit`: **27 件 → 14 件**（v2.9.289 で `npm audit fix` + nodemailer 8→9 + tsx 更新）。critical 0 件。
> 残る 14 件は上表のとおり**すべて 開発用 / 実行環境で発現しない / 大版更新が必要**なものに分類済み。

---

## セキュリティポリシー

- `.env` やクレデンシャルは**絶対に Git にコミットしない**（`.gitignore` 済み）
- APIキー・JWT シークレットをソースにハードコードしない
- 本番DBと検証DBは**完全分離**（相互参照禁止）
- 本番DBへの直接 SQL 操作は**最小限**（管理者リセット等の緊急時のみ）
- コンテナ内のホスト認証情報は一切持ち込まない

詳細は [`CLAUDE.md`](./CLAUDE.md) の「セキュリティポリシー」「環境分離ポリシー」セクションを参照。

---

## 開発上の重要な慣例

### モバイルファースト

全ての画面は **375px幅（iPhone SE相当）で破綻しない**前提で実装。
- Tailwind の `sm:` `md:` `lg:` を適切に使用
- タップ領域は最低 44px（iOS HIG 基準）
- テーブルは `overflow-x-auto` で囲む
- モーダルは `max-h-[90vh] overflow-y-auto`

### DDD 風コンテキスト分割（server）

`server/src/contexts/{context}/{routes,services,repositories}` で業務ドメインを分割。
コンテキストを跨ぐ依存は避け、共通ロジックは `server/src/shared/services/` に集約。

### 共通ライブラリの活用

新しいクライアントアプリを追加する際は `shared/src/client/` のファクトリ関数を使用し、
重複実装を避けること。共通ヘッダー・認証フック・API クライアントは既に集約済み。

---

## バージョン履歴（抜粋）

| バージョン | 内容 |
|---|---|
| **v3.0.3** | **スマホで通知ベルのパネルが左端から欠けて見える崩れを直した**: 通知ベルは上辺の最右端の要素ではなく、右にユーザーメニューが続く。パネル (`w-[min(92vw,420px)]`、ほぼ画面幅) をベルの親要素に `absolute right-0` で相対配置していたため、パネルの右端はベルの右端に揃うがベルは画面の真の右端より内側にあるぶん、パネルの左端が画面外にはみ出し `AppShell` 直下の `overflow-hidden` に切り取られていた。同じ上辺の「⋯」メニュー・ユーザーメニューは既に `createPortal` で `document.body` に描画し実測位置で `fixed` 配置していたため、通知ベルのパネルも同じ方式に揃えた。左端が画面外に出ないよう右余白の上限をクランプする処理も追加。Playwright で再現し、修正前 `left: -32.8px`（切り取られる）→ 修正後 `left: 8px`（画面内）を確認 |
| **v3.0.2** | **配信媒体トグルで「ネットメディア」等が折り返れる崩れが v3.0.1 でも一部残っていたのを直した**: v3.0.1 で列数を `cols={{ base: 2 }}` に固定したが、2列でも「ネットメディア」（7文字）は枠に収まらず「ネットメ」「ディア」に折り返れたまま。折り返し自体をやめ、`ToggleCard` の共通ラベル部品に**枠の実幅に収まる比率へ横方向だけ縮める(長体)処理**を追加。外枠 (overflow-hidden) と中身 (scaleX) を同じ要素にかけると縮める前の幅で先に切り取られ「YouTub」のように末尾が欠けるため、外枠と中身を別要素にする2重構造にした。下限は設けず常に枠にぴったり収まる比率で縮める（極端に狭い枠では文字が小さくなるが、欠けたり折り返したりはしない）。`ToggleButtonGroup`/`ToggleCard` 全使用箇所に共通適用されるため Playwright で複数の枠幅を模した検証を実施 |
| **v3.0.1** | **配信媒体トグルの折り返し崩れを直し、内覧会 来場予約の同行者を個別受付できるようにした**: ①「番組情報」カードの配信媒体トグルが YouTube→「You」「Tube」のように英字ラベルを1文字ずつ折り返して見えていた崩れを修正。`ToggleButtonGroup` の `cols={{ base: 2, sm: 3 }}` は**画面幅**基準で列数が切り替わるが、`lg:grid-cols-2` の案件フォームのカードや `sm:max-w-md` の GLS発番ダイアログのように**画面幅より枠が狭い場所**に置かれていたため、`sm` 幅を超えた瞬間に3列に切り替わり枠が足りなくなっていた。両箇所とも `cols={{ base: 2 }}` に固定して解消。②内覧会 来場予約の同行者を、代表者とは独立に1人ずつ受付できるようにした。`companions` を氏名の配列から `{id, name, checked_in_at, checked_in_by}` の配列に変換するマイグレーションを追加し、氏名で照合して既存の受付状態を引き継ぐ形にした。同行者ごとにカード化 + 個別の受付ボタンを追加、CSV出力・編集ダイアログも新しい形に対応。③検証: 全9ワークスペースの型チェック・ビルド通過、`eslint` 0 errors / 77 warnings（着手前と同数）、禁止パターン違反0（704ファイル） |
| **v3.0.0** | **現場の道具の統合と刷新デザインの土台をひとつに束ねた節目（ユーザー指定のメジャー版）**: ①`SearchableSelect`・担当メンバー見出し・変更の記録カードの崩れを修正。②検証を `scripts/verify-ui.mjs`（`npm run verify:ui`）としてリポジトリに固定し、PC・スマホ並列実行でフル実行を約4.7分に短縮。変更ワークスペースだけ作る `npm run build:changed` も追加。③⌘K を、未入力時はレール順にグループ化しアイコンを付けた「行き先の地図」に変更。検索語にかな読みを追加。④ボタン高さ(32/36/40/44/48px)・バッジ(22/26px)・列幅(56〜240pxの7段)・タップ領域(46〜52px)を部品化し lint ルール3つを追加。⑤「按分」「エピソード」「データがありません」等の禁止語を排除し `forbidden-wording` ルールで検査。⑥検証: 全9ワークスペース ビルド通過、`npm run verify:ui` で PC・スマホ×20ページ×7項目 = **388/388件通過** |
| **v2.9.297** | **現場の道具（6アプリ）を共通の土台に載せ直した — 崩れの原因は「土台が7か所に手で書かれていた」こと**: ①共通シェル `AppShell` の根は `h-full` なのに `html, body, #root { height: 100% }` を書いていたのは**案件管理だけ**で、**6つのブロックアプリでシェルが画面の途中で切れていた**（1440×900 で実測 510px/900px）。型でもテストでも捕まらない壊れ方。②**リアルタイムCG は輪をかけて外れていた** — `tokens.css` を読まず色変数が**21個**（他は207個）、Tailwind の `content` に `'../shared/src/client/**'` が無く**共通部品のクラスが1つも生成されていなかった**。v2.9.290・291 で共通部品を増やしたぶん**足すほど崩れる**状態。③土台を `shared/src/client/base.css` 1本にまとめ、7アプリの `index.css` は import + 固有CSSだけに（−250行）。④**同じずれを lint で止める**（`app-foundation`: 土台未読込／プリセット未継承／content 未設定）。**わざと外して3つとも落ちることを確認**。⑤生パレット **1,800か所**をトークンへ（CG 1,264／Qシート 393／機材 218／日常業務 181／技術資料 42／計時LIVE 25）。以後 `raw-palette` で止まる。⑥**「見分けるための色」`cat-1`〜`cat-8` を新設**（8色とも白文字が AA）。状態の色で代用すると「話者3が赤」と「話者3に異常」が区別できない。⑦**機械置換だけで進めたとき10色の話者色が7色に潰れていた** — 色を置き換えるのではなく**その色が何を意味するか**を見ないと置換で情報が消える。⑧AI専用の紫 `ai` を、AIと無関係な11か所（機材の映像種別・Qシートのマイク・承認済みバッジ）から `cat-7` へ戻した。⑨リアルタイムCG の本番卓5画面を黒からトークンへ（Qシートの OnAir は既に明るい）。**放送の絵が出る枠は黒のまま**、その上のバッジだけ修正（赤地に赤文字が3か所）。⑩**放送と印刷は対象外**（CG本体・プロンプター・計時の表示機・技術資料の印刷・ラック図）。検査に `NOT_A_SCREEN` として明記。⑪文字の透明度を禁止（`text-muted-foreground/60` は白地で #9ea3ab 相当 = 禁止色 #8b929c を下回る）。⑫`PageTitle` 採用 0 → 9ページ。`DashboardHeader` も `PageTitle` を使う形に（`sm:` と `lg:` の2種類が混在していた）。⑬**山吹 (#eb9800) の上の白文字は 2.3:1** で AA 不成立だったので `--warning-foreground` を濃い文字 (7.3:1) にし、明るい下地の `text-warning` は `text-warning-strong` (4.8:1) へ（46ファイル）。⑭検証: PC(1440)・スマホ(375) の20ページ × 各7項目 = **268項目すべて通過**（シェルが画面いっぱい／地の色と書体が7アプリで同一／見出しの大きさが1ページ1つ／#8b929c より薄い文字0件／横はみ出し0px／JSエラー0件）。⑮検証スクリプト側の誤り4件を記録（放送画面は意図して `.dark`／隠れた h1 を数えていた／輝度を線形化していなかった／グラデーションと半透明を白地とみなしていた） |
| **v2.9.296** | **CI が落ちていたのを修正（検証用の一時ファイルを1本コミットしていた）**: ①v2.9.294 で使い捨てスクリプト `server/vs7.mjs` を消し忘れてコミット。`eslint` が3件のエラー（`console` 未定義／未使用 import）を出し **typecheck ジョブが落ちた**。②消し忘れた理由は「作る→実行→`rm`」を1行につないだコマンドが**時間切れでバックグラウンドに回され末尾の `rm` まで届かなかった**こと。次のコミットで `git add -A` が拾った。③**影響**: v2.9.289 のゲート（`deploy-*` は `needs: [build, typecheck]`）が働き、**v2.9.294・295 は dev に配られていなかった**（dev は v2.9.293 のまま）。**壊れたものが配られる前に止まった**ので意図どおり。④中身は無害（検証用の使い捨て DB への接続のみ・秘密情報なし）。⑤**再発防止は「置き場所を決める」**: 検証スクリプトは `pg` 解決のためワークスペース内に置く必要があるので `**/verify-scratch/` を `.gitignore` に追加 |
| **v2.9.295** | **1000行超のファイル分割 6本目・7本目**: ①`EventEditorPage.tsx` **1,294 → 827行**（`types.ts` 80 / `rows.tsx` 190 / `sections.tsx` 222）。これも関数が9つ並んでいた型でそのまま移せた。②`RevenueListPage.tsx` **1,438 → 1,046行**（`types.tsx` 82 / **`RevenueItemsEditor.tsx` 379**）。③**ダイアログ全体は切らなかった** — 売上ダイアログは571行で最大の塊だが**中で使う変数が56個**あり、props に開くと親より読みにくくなる。中の「明細を編集する部分」だけなら**14個**で収まるのでそこで切った。**「大きいから切る」ではなく「境目が細いところで切る」**。④未使用 import の自動削除で `type X, type Y` から1つ消す際に `type type Y` を作って構文エラーにしたが、型チェックが即座に検出。パターンを修正。⑤ブラウザ15項目 + 既存75項目を再実行して全通過。⑥検証スクリプト側のつまずき3件（パスは `/finance?tab=revenue`／明細追加は「行追加」／売上一覧は**今月で絞り込む**ので12月付の検証データが出なかった） |
| **v2.9.294** | **1000行超のファイル分割 4本目・5本目 — 初めて1000行を切った**: ①`RackLayoutPage.tsx` **1,505 → 714行**（`config.ts` 49 / `dialogs.tsx` 156 / `RackDisplay.tsx` 412 / `print.tsx` 205）。**もともと関数が9つに分かれて並んでいた**ので props は決まっており、そのまま移すだけで済んだ — 「1つの巨大な関数」ではなく「小さい関数が1ファイルに積んである」形なら分割はほぼ機械作業。②`SchedulePage.tsx` **1,515 → 1,234行**（`types.ts` 145 / `dialogs.tsx` 148）。**祝日の一覧（2027年まで手書き）がページ本文に埋まっていた**ので切り出した。③未使用 import は型チェックに列挙させて機械処理（`TS6133` に加え **`TS6192`（import 文まるごと未使用）** も出たので両方・計34件）。④ブラウザ11項目 + 既存47項目を再実行して全通過。⑤検証で「予定表は部屋名を既定では出さない（『部屋』パネルの中）」と判明したが**既存の作りで分割の影響ではない**。⑥残り13本。`manual/content.tsx` 1,455 は**画面の使い方の本文そのもの**なので対象から外す |
| **v2.9.293** | **1000行超のファイル分割 3本目 — 2,000行超がゼロに**: `BusinessProjectView.tsx` **2,047 → 1,520行**（型 `types.ts` 59 / `MonthlyBilling.tsx` 181 / `RevenueList.tsx` 347 / `PurchaseList.tsx` 102）。①**切る場所は「読むだけの並び」を選んだ** — この画面の最大の塊はダイアログ2つ（売上明細400行・仕入198行）だが、**中で使う state が25〜40個**あり props に開くと「渡せるが意味が違う」事故が起きやすい。一覧の並びは表示 + 数個のハンドラなので props 7〜18個で収まり、間違えても型で止まる。②**型は親の宣言をそのまま写す** — 推測で書いたら**5件が型エラー**（`monthEpisodes` は `billing_key` でなく `episode_code`／`handleDownloadPdf` は種類（見積書・請求書・検収書）を取る2引数／`handleDeleteMonth` は Promise を返す）。各ファイル冒頭にこの注意を明記。③条件の位置だけ移動（`{monthlyMode && (…)}` → `{monthlyMode && <MonthlyBilling … />}`）。④未使用 import は型チェック（TS6133）に列挙させて機械的に削除。⑤ブラウザ10項目 + 既存54項目を再実行して全通過 |
| **v2.9.292** | **1000行超のファイル分割 1本目・2本目（ロードマップ3つ目）**: 挙動を1つも変えない作業なので**1版1〜2ファイル**に絞る。①`ProjectFormPage.tsx` **2,513 → 1,925行**（型・定数 `types.ts` / `JourneyPanel.tsx` / `ActivityQuickAdd.tsx` / **ダイアログ7種** `dialogs.tsx`）。ダイアログを1ファイルにまとめたのは、どれも「開く・閉じる + 1操作」の小さな部品で単独では開かないため（7ファイルに割ると import が7行増えるだけ）。②`EquipmentListPage.tsx` **2,026 → 1,880行**（印刷まわり一式 — 列の定義・設定ダイアログ・印刷用の表）。印刷は絞り込みや編集と state を共有していないので切れ目がきれい。③**state は親に残した** — スタジオ日程は約30個の state を保存処理が読んでいるので、子に移すと挙動が変わる。props 30個は不健全なので次回に回した。④`handlePrint` のような関数もそのまま親に残して props で渡した（`print-landscape` クラス + 150ms 待ちがあり、書き直すと印刷の向きが変わる）。⑤検証で「やり取りを記録」ボタンが**2つある**ことが判明（案件内で書ける方と営業活動ページへ飛ぶ方）。コメントの意図と食い違うが**既存の挙動なので今回は直していない**。⑥ブラウザ21項目 + 既存51項目を再実行して全通過。⑦残り11本（`BusinessProjectView.tsx` 2,047 ほか） |
| **v2.9.291** | **読み込み中・空・エラー・権限なしを部品に寄せた（ロードマップ2つ目）**: ①`EmptyState` が `shared/dashboard/` と `shared/states/` の**2か所**にあり、dashboard 側の既定タイトルが **「データがありません」**（§2.4 が禁じる言い方）だった。24ファイルすべてが実際には title を渡していることを機械的に確かめてから互換ラッパーを削除（表示は不変）。②ページ全体の読み込み39か所のうち27か所がその場書きのスピナー → ログイン判定中は `<Delayed>` で包むだけ、画面の中身は `<Delayed><SkeletonRows /></Delayed>` で**骨格は出したまま中身だけ骨組み**に。③空の表示185か所を性質で分け、**一覧の代わりに縦の場所を取る32か所**を `EmptyState`（何が無いか + 次にやること）に。④**全部を機械的に置き換えない判断**: 「待たせているものはありません」は良い知らせで空の状態ではない／高さ40の選択リストや検索欄の注記に枠付きパネルを入れると読みにくくなる → 1行のまま文言だけ直した。⑤権限なし5か所を `NoPermissionPanel` に（**必要な権限を名前で出す**）。⑥機材一覧が**サーバーの `debug` JSON を画面に貼っていた**のを console へ（§2.5）。エラーは `ErrorPanel` に寄せ **HTTPコードを画面に出さない**。⑦検査 `empty-by-hand` / `loading-by-hand` を追加。最初のルールは**広すぎてインラインで正しい12か所まで拾った**ので `text-center` を持つブロックに絞り、**わざと違反を書いて止まることを確認**。逃げ道 `ui-tokens-ok` は1か所のみ。⑧共通部品を使うページが **117中44 → 117中63**。ブラウザ16項目 + 既存75項目を再実行して全通過 |
| **v2.9.290** | **`alert()` / `confirm()` を 129 か所やめて部品にした（ロードマップ1つ目）**: ①土台が3つ不足 — 確認ダイアログの部品が無い／通知ラッパーは7アプリ中1つ／`<NoticeBar />` はページごとに8か所なので**大半のページで `notifyError` がどこにも出ていなかった**。②`await confirmAction({title, description, confirmLabel, tone})` は `window.confirm()` と同じ形（true/false）にして**分岐の形を書き換えずに移行**（書き換えると条件反転の事故が出る）。③取り消せない操作は `tone:'danger'` で赤 + **最初から「やめる」にフォーカス**（開いた瞬間の Enter で実行されない）+ 外側を押しても閉じない。④メッセージを title と description に分割（全角「？」と改行で割る）。⑤`alert()` は中身で `notifyError` / `notifySuccess` / `notifyInfo` に振り分け、**変数を渡していた4か所は目で見て修正**（`catch` 内で「失敗しました」を組んでいた）。⑥**置き場所は AppShell ではなくアプリのルート直下** — OnAir・ランダウン・プロンプターは**AppShell を通らない全画面ページ**で、そこに置くと**本番中に「停止してリセット」が黙って何もしない**（ブラウザ検証で発見）。部品側も二重に置いても1つしか出ないようにした。⑦`<ConfirmHost />` が無い画面では `confirmAction` は **false を返す**（置き忘れで勝手に実行しない）。⑧`browser-dialog` ルールを lint に追加。⑨置き換えは機械的に行い、**`await` の付け忘れ85か所は型チェックが全部列挙**。⑩検証中に自分のスクリプトの誤り3件を修正（`cat?.name` の `?` でテンプレートリテラルを割っていた／import の挿入位置／`importance` が `import` に当たる正規表現）。⑪v2.9.289 で入れた `typecheck` スクリプトの**ラベル順とコマンド順の食い違い**（ログの見出しが別アプリを指す）も修正。⑫OnAir の Esc が `if (confirmStop() && id)` のままだと**Promise が truthy で「やめる」でも画面を離れる**問題を型チェック（TS2801）が検出 |
| **v2.9.289** | **コードレビュー1回目: 本番中に出る不具合 + セキュリティ + デプロイ**: ①接続を持つ仕組みが4か所にあり**4つとも同じ2つの誤り**（つながっていなければ捨てて作り直す／片方が無条件に切る）→ `shared/src/client/socketPool.ts` 1本に集約（鍵ごとに1本・使用数を数え0で切る・つながっているかは見ない）。**送信関数が毎回接続を取り出すので、押した操作そのものが受け口を壊す**順序になっていた。②Qシート OnAir: socket effect の依存に `running`/`next`/`prev` が入っていて**受け口を外さずに張り直していた**→ ランダウンの「次へ」1回でキューが複数進む／放送中に「切断されました」が出る。受け口は `id` につき1組・中身は ref で最新を呼ぶ形に。`cue:update` は毎秒10回→変化時+毎秒1回。`cues` を `useMemo` に（時計が100msで動くため**毎秒10回キュー全体を組み直していた**）。③リアルタイムCG: `setCue` を同値なら同じものを返す形に（再接続時の同内容 `cue:sync` で演出が跳ねる）／`useAwardsStore()` の引数なし購読を選択購読に／`off('cue:sync')` が**他画面の受け口まで消していた**のを修正。④**Qシートはローカルで同期が動かない設定**（API が 3001 ではなく 3000・`/socket.io` の中継が無い）＋**機材管理と Qシートが同じ 5174**。`client-daily` の 5180 が許可オリジンに無く Socket.IO が 400 で落ちる状態も修正。⑤脆弱性 27→14（nodemailer 8→9・tsx 更新・`npm audit fix`）。**npm に修正版の無い xlsx** は `safeReadWorkbook()` 1本に集約し境界で止める（中身で表計算ファイル判定・prototype 汚染を検知して消して拒否）。`XLSX.read` の直接使用は lint で禁止。ZIP 一括の展開上限（1000件/300MB）。⑥**型チェックをイメージのビルドから出して CI で並走**（`noEmit` なので成果物は変わらず、実測約50秒が待ち時間から消える）。`npm install`→`npm ci`。⑦OnAir の操作ボタンに名前が無かったので付け、±1分を 36px→44px に |
| **v2.9.288** | **部品と数字のサイズの共通化（6章）: 画面ごとに手で書けなくする**: 実装を数えたら 金額の手書き14／万円に丸める関数4本／大きい数字の `text-*` 直書き2／ページ見出しの重複38か所（3種類）。**丸め方が2種類あったのが実害** — `Math.round(v/10000)` と `(v/10000).toFixed(0)` は負の数で結果が違い、マイナスの出る画面で同じ金額が違う万円になり得た（`manYen()` 1本にし、絶対値で丸めて符号を戻す形にそろえた）。数字のサイズは**4段だけ**（`KpiCard` と同じ値）にして `<StatValue>` から選ぶ。見出しは `<PageTitle>`（`/finance/import` は枠が `sm:`・中身が `lg:` で**同じページに2つの段が混ざっていた**）。**`scripts/check-ui-tokens.mjs` を `npm run lint` の先頭で走らせて手書きを止める**（理由も出す・`ui-tokens-ok` で逃げ道は1つ残す）。対象外は部品の実装・再エクスポート層・放送に出るCG |
| **v2.9.287** | **お試し（25章）: 練習が実績に混ざらない**: 練習用の画面は作らず（別に作ると練習で通った道と本番で通る道が違い意味が無くなる）、案件に印を1つ足して（migration 153）**数える側で外す**（約20か所／10ファイル・条件の文字列は1か所に集約）。印は**練習だけが持つ**向き（付け忘れると「多い方に間違う」ので気づける）。案件を持たない行を落とさないため `NOT EXISTS` で外す。**外に出るものは書く側で止める** — 予約・計時LIVEのタイマー・GLS発番・BOXフォルダ・請求書（画面で押せないだけでなくサーバーでも弾く）。**本物にはできない**／片づけは人が押す（一度では消さない）／**お試しでない案件はこの口から消せない**。「混ざらない」は**数字を返す口を20個ぜんぶ叩いて前後で1バイトも動かないこと**で確認。**既存バグ2件を修正** — 営業成績が `GROUP BY` の不足で500だった／権限が無いページが必要な権限を名前で言わなかった（`PermissionRoute` を通る全画面が同時に直る） |
| **v2.9.286** | **案件の書類（15章）＋検索結果（36章）: そろっていないものが期日順に並ぶ / 見えないものは件数にも出さない**: 1件の案件に要る12種（お客様とやり取りする紙6・現場で作るもの5・貸したもの1）の定義はコードに置き、**そろったかは元データで判定**（人に「できました」を押させない＝押し忘れが「足りない」として残らない）。**期日順に並べ、過ぎたものは上**。期日はサーバーが計算する（申込書=本番日14日前／現場のもの=7日前／仮押さえ=45日前／入金=支払期日／カード=返却予定日）。**まだ要らないものは「足りない」に数えず「あとで」に分ける**（ヨミで請求書が無いのは当たり前で、数えると印の意味が消える）。**数だけでなく「次にやること」を名前と期日で書く**。カードの貸出に案件を紐づけた（migration 152・任意）。**既存の穴を1件修正** — `GET /search` が権限を見ておらず、案件を見る権限が無い人の ⌘K にも案件名・GLS番号・お客様の名前・仕入先の名前が出ていた（開くと403で止まるが名前はもう見えている）。判定は HTTP と同じ `meetsPermissionLevel` に寄せ、**権限が無い種類は0件でも返さず**（「案件 0件」は「無い」と読まれる）、**名前だけ**を出して「見る権限がないので探していません」と書く。**検索結果の画面**を新設（種類ごとの件数・種類で絞れる・何で探せるかを先に書く）。検索のSQLは1か所で、⌘K と検索結果で違うのは件数の上限だけ |
| **v2.9.285** | **リアルタイムCG（20章 24a/24b/24d/20e/20f）: 送出を1画面にする**: 本番中に見る画面を1つにし、以前の4つ（統合送出／字幕スーパー／アンケート・クイズ／ランキングCG）は「準備（本番中は触りません）」に降ろした。**送出の順番をサーバーに移した** — 画面の定数に持つと「次はWINNER BAR」と出ているのに別のものが出る。**TAKE / CLEAR は既存の `/cue` `/oneshot/cue` をそのまま押すので出る絵は1バイトも変わらない**。**次のTAKEで何が出るか**とそのあとの流れを必ず先に出し、**5分を超えて出たままのもの**は名前と分数で知らせてその場で消せる。キー操作は一覧を出すが**既定では効かない**（v2.9.96 で全廃した判断を変えない）。**スマホに TAKE を置かない**。出力URLは `?bg=1` `?audio=1` `?lang=en` を人に組み立てさせず**選ぶだけ**にした（1文字違いは「何も出ない」形で失敗する）。データの入れ方を**貼る / 落とす / AIに整えさせる**の3つにし、**取り込みの処理と列を当てる画面は1本のまま**（貼った文字はサーバーで xlsx にして既存の取り込みへ渡す＝「ファイルなら入るのに貼ると入らない」を作らない）。AI は表の形に直すところまでで**中身は足させず**、方針「AIを使い捨てにしない」の5条件を満たす（全文記録／`diffByKey` で**「部門::名前」を鍵に**突合・二重計上しない／`imported`と`dropped`／傾向を載せた回は版を分ける／既存の `/review` と MCP）。テンプレートは4つ出して**作れるのはアワードだけ**をその場で言う（画面だけの制限にせずサーバーでも400で止める） |
| **v2.9.284** | **運営マニュアル（22章 29a/29b/29c）: 部品12種を作って最後に束ねる**: いただいたPDF（31ページ）を分解した部品12種の定義はコードに置き（元PDFのどのページかも残す）、DBに持つのは各案件の中身と状態だけ。**足りない部品は名前で指摘し白紙のページは作らない**。自動で入る部品は元データがあるかで判定（人に「できました」を押させない）。**渡す相手で入る部品が変わる**（社内12種／お客様10種＝連絡先とテクニカルを自動で外す／アルバイト3種）。**出した瞬間の中身を残す**のであとで部品を直しても配った版は変わらない。配置図は記号が人・物を指し**凡例は置いた記号から勝手にできる**（位置は0〜1000の相対）。**会場図はAIが下書き**し、方針「AIを使い捨てにしない」の5条件をすべて満たす — 出力の全文記録／`diffByKey` で**記号を鍵に**突合（indexだと1つ足すだけで全部が変更になる）・二重計上しない／`issued`と`dropped`を成果に記録／生成前に傾向を読み**載せた回は版を分ける**／既存の`/review`とMCPに乗る |
| **v2.9.283** | **香盤表（21章 28a）: 当日の動きを1枚にする**: いま Excel で作っているもの。案件の日程・予約・Qシート・機材・メンバーから**最初の1枚を自動で組む**（白紙から作らない）。自動で入った枠は出どころを持つので、**組み直しても手で置いた枠は消えない**（冪等）。枠は分で持ち px は画面が掛けるだけなので、**1時間88px** の目盛りを変えても枠がずれない。人のレーンは案件メンバーを指すので**名前は1か所で直せば表も直る**。重なっているところ（同じ人が2か所／本番の枠がQシートの合計尺より短い／撤収が完全退館をまたぐ）は**サーバーが数えて返す**（画面で判定すると印刷とPDFで違う警告が出る）。部屋の重なりは警告しない（同時に複数の作業が走るため）。レーンの出し入れは**消さずに隠す**（社外版で社内レーンを外す）。**枠の長さがそのまま計時LIVEのタイマーの尺**になり、二度押しても増えない・終わりの無い枠は理由を言って弾く。21章はAIを使わないので「AIを使い捨てにしない」5条件は次版（22章の会場図AI下書き）から適用 |
| **v2.9.282** | **案件の「お金」「予定」タブ化（13章 7a / §7.12）＋ 見積を売上に数えていた既存バグ修正**: 往復がいちばん多い2つだけタブに（やり取り・タスク・書類はあとで、現場の道具はリンクのまま）。お金は**4つの数字（想定金額・確定売上・仕入・粗利）を1本の口で返す**ので、案件一覧と違う数字が出る余地がない。旧「財務サマリー」(3値) は想定金額が無く**受注前は粗利が常にマイナスに見えた**ので置き換え、確定売上が無いときは想定金額を元に「粗利（見込み）」を出す。どの数字にも「どこから来たか」を付け、**仮（見込み）の仕入は件数を別に出す**（分からないと未確定の粗利を信じてしまう）。予定は案件側の口から返し（予約一覧は studio 権限で営業は読めない）、種別を日本語にし、**部屋の無い予約はそう分かるように**して、本予約への切り替え期限（本番日の45日前）もサーバーが返す。**既存バグ修正: `getSummary` に status の絞り込みが無く `status=estimate` の見積が「売上（確定）」に混ざっていた** — 想定金額としても出しているので二重計上で、粗利・粗利率が過大（実測 9,820,000 → 5,000,000）。案件詳細・一覧・MCP・隔週キープが同じ数字を読んでいる |
| **v2.9.281** | **隔週キープをつくる（29章 35a/35b/35c）: 型は Ver.2.5 のまま、AIが14ページ・人が2ページ**: 11の型は変えず「誰が埋めるか」だけ変える（作成 6.5時間 → 45分）。ページの構成はコードの定数に持ち、DBに持つのは**人が書いた分**と**その回に足した議題**だけ（migration 148）。数字は資料に写し取らず出すときに読む（写すと元を直しても資料が古いまま）。①②③は毎回、**④以降は議題があるときだけ足す**（番号・時間配分・ページが自動、無い回はページを作らない）。③の6テーマと担当は固定で人は1行だけ書く。①の3行は**空でも進める**（書かないという判断も記録）。チェックリスト11項目を自動照合して**落ちている項目だけ**出し、確定は止めない。**配布はPDFだけ**（横向きA4・日本語フォント埋め込み。PowerPoint出力は作らない）。過去分は消さない |
| **v2.9.280** | **案件の入力を減らす（14章 27a/27b/27c）: 22項目 → 起票は3つ**: 起票は「どこの・なんの話か／お客様／スタジオを使いますか」だけ（日付も金額も入力欄なし）。案件分類は「スタジオを使うか」から決まるので人には聞かない。お客様はその場で作れる。GLS番号は受注後。残りは**進んだ段で1〜2問だけ**聞く（仮押さえ=日付と部屋／見積提案=金額と飛び日／口頭決定=0問／受注=申込書とロゴ／失注=理由と教訓）。**定義はサーバー1か所**で、画面もサーバーも同じものを使うのでずれない（足りないまま進もうとすると400で何が足りないか日本語で返す）。**すでに入っている項目は聞かない**。「なぜここで聞くか」と「自動で起きること」を項目ごとに出す。仮押さえの部屋を予約に紐づけるようにした（従来は部屋を持たない仮押さえができていた）。タグと案件種類(その他)をフォームから削除（教訓・学びは**ふりかえりが集計しているので残した**）。既存バグ修正: **営業が案件フォームの部屋と予約で403**になり黙って空になっていた |
| **v2.9.279** | **合同案件（32章 40a/40b）: 1回のイベントを複数社で開いて参加社数ぶんの請求書を出す**: 既存の「費用を分け合う」（1社が払い費用を案件で配る = 社内の原価配分）とは向きが逆なので**別の器**（migration 147）にし、見分け方「請求書が何枚出るか」を作成時に聞く。会社を選ぶと**その社数ぶんの案件が自動でできる**（作成時は口頭決定、出したときに受注＋GLS発番）。分け方は均等（既定）／割合／金額を直接。**あまりの1円の行き先を画面に出して変えられる**。**合計が総額に合わないうちはサーバーでも止めて1枚も出さない**。番号は1枚ごとに採り、出した売上は31章の入金待ちにそのまま並ぶ。請求先が本社と違う会社にも対応。1社抜けたら取り消しの請求書を1枚出して残りに配り直す（行は消さず履歴に残す）。ブラウザ検証で実バグ3件を修正（**経理が会社を1社も選べなかった**／戻るのタップ領域36px／375pxで277pxはみ出し）|
| **v2.9.278** | **コーディングの遅さを実測して3つ改善**: CLAUDE.md の96.7%（828,591文字中801,027文字）が過去の履歴で、これがコーディング中に毎ターン読み込まれていた → 最新5件を残して `docs/version-history.md` に切り出し（**828KB → 58KB**）。README も同様に `docs/release-notes.md` へ（**551KB → 40KB**）。生成スクリプトが両ファイルを連結して読むので**画面のバージョン履歴は388件のまま**（アーカイブが読めなければ fail closed でビルドを止める / `.dockerignore` 再包含 + Dockerfile COPY も追加）。ルートの `npm run build` を並列化（**1分50秒 → 47秒**、旧挙動は `build:serial`）。検証用DBの土台を `scripts/dev-verify/` に固定（**冷えた状態から4秒**、毎回踏む落とし穴もREADMEに記録）。検証の項目数と 1章=1PR の粒度は意図的に据置 |
| **v2.9.277** | **請求のしごとを新設（31章 31a）: 締めの日にまとめて出す + 入金の確認 + 検収書**。申込書が揃っていない案件は選べない（サーバーでも弾く）。出した請求書は消せず番号は残す。入金は日付で持ち、「最近入金した分」から打ち間違いを直せる。月次運用は先月と金額が違うものだけ印。新テーブルなし（migration 146 は列の追加だけ）。**既存の重大バグ2件も修正** — 経理（budget のみ）が財務の全画面で403 / ベルが sales 権限の無い人に出ない |
| **v2.9.276** | **見積をつくる画面を新設 (30章 37a): 料金表から選ぶ + AIが下書き + 粗利がその場で出る + PDFはBOXへ**。明細をグループ (スタジオ / 技術・人員 / 制作・その他) で並べ、行に「仕入(見込み)」を持たせて受注時にそのまま見込み仕入にする (冪等・二度打ちしない)。値引きで粗利率がその場で動き 30% を切ると赤になるが止めない。確定で想定金額に入りステージが見積提案に進む (進んだ案件は巻き戻さない)。ONAiR はメールを送らないので PDF を BOX に残し、送るのは人。「送った」で次にやること「申込書をもらう」が自動で立つ。新テーブルなし (migration 145 は列の追加だけ — 同じ「見積」に入口を3つ作らないため)。`AppError` の引数取り違えで画面に日本語ではなくコードが出ていた既存バグも修正 |
| **v2.9.258** | **UI/UX刷新 Phase 4b: 案件ワークスペースを「左=進める / 右=事実」に組み直した (7a)**。左=今すべきこと→やり取り→お金 / 右=案件の中身。ヘッダーは案件名を h1 にして GLS番号を注記つきで降格。ジャーニーは日付つき6段 + 現在地の1文 + **次の一手だけを主ボタン**に (従来はプルダウン7択→「変更」の2ステップ)。やり取りは別ページに飛ばさず**その場で記録** (既存 `POST /activity-logs`)。ヘッダーに「未保存の変更N件」+ 明示保存。在席アバターと409バナー・案件内タブは次フェーズ。 |
| **v2.9.257** | **UI/UX刷新 Phase 4a: 案件の一覧とボードを1画面に統合 (8a / 8b)**。「案件一覧」「ヨミ・パイプライン」「確定案件」の3画面を `/projects` の `?view=` / `?filter=` に統合し3ファイルを削除。行は**案件名が主役** (GLS番号は経理で使う番号なので副情報に降格)。進行中で次回アクションが無い案件を橙で明示、金額は「確定した売上／想定」を書き分け。ボードはカードを運ぶと既存 `PATCH /projects/:id/stage` でステージが変わる。サーバーは `GET /projects` に次にやること + `summary` (区分/タブごとの件数と金額) を追加 (後方互換)。 |
| **v2.9.256** | **Phase 3 の仕上げ: 案件管理の二次ナビ (約40メニュー) を撤去し 6レール + ⌘K に一本化**。削除前に「二次ナビの37の行き先がすべて ⌘K + レールで到達できる」ことを機械的に照合 (差分0件)。あわせて**上辺がスマホ375pxで壊れていた実バグ**を修正 — ユーザーメニューが画面外に押し出されていたので、パンくずをスマホで出さない / `⋯` をユーザーメニューに畳む / ロゴを縮める。二次ナビが残る6アプリでもヘッダーが 375px に収まることを実測。現場アプリの二次ナビは Phase 10〜12 まで残す。 |
| **v2.9.255** | **UI/UX刷新 Phase 3: ⌘K コマンドパレット (6a) + §3.3 リダイレクト表 (36本) + 行列ゼロの状態 (6b)**。⌘K を全7アプリに配線 (「やる → ひらく → 見つかった案件・お客様」・↑↓/enter/tab/esc・コマンド表は静的1ファイル約70件を権限フィルタ)。上辺の中央がさがす場所になり移行期のグローバル検索は削除。**新URLを正にして旧URLは消さずリダイレクト** — `/projects` `/tasks` `/customers` `/schedule` `/finance` `/finance/import` `/settings/*` で、表示の切り替えはクエリ。新URLが既存画面を出す層を `routeAdapters.tsx` に分けたので **redirect しても機能が失われず**、Phase 4〜8 は中身の差し替えだけで URL は変わらない。行列ゼロは祝わず「空いた時間でやるなら」を出す。 |
| **v2.9.254** | **UI/UX刷新: 実装をデザイン (3a/4a/5a) と突き合わせて文言・数字書体・上辺・行列の一手を合わせた**。数字書体に Roboto Condensed を戻し、あいさつ/行列/展開/投入欄/右列の見出しと注記をデザインの文言に統一。行の右端に次の一手 (片づける/確認する/返事をする/内容を確認する)、期限超過と問い合わせを2カラム化 (案件の状態 / 次にとれる予定)、問い合わせに「営業でない (除外)」を追加 (既存API)。さがす場所を上辺中央に。**実バグ1件**: KPI の金額が「¥1,453.152万」と小数で出ていた (丸め漏れ) を修正。⌘K・通知ベル・請求書PDFプレビュー・二重計上の警告は Phase 3/9 と新規サーバー実装が必要なため次フェーズ。 |
| **v2.9.253** | **UI/UX刷新 Phase 2: 「今日」の画面 (3a) + 待たせている行列の種別展開 (5a) + 投入の4状態 (4a)**。HomePage を TodayPage に再構成して受信箱ページを統合 (`/sales/inbox` → `/today`)。重複カード3つ (受信箱サマリー/直近の案件/システム管理) を削除。**行列の全種別が開いた場所で終端に到達**するようにした (従来は見積・請求だけ別アプリに飛ばすだけだった)。新規APIは作らず既存の活動一覧・空き照会・ステータス更新を利用。投入に「AIが読んでいる」状態と読み取り失敗の2択を追加。 |
| **v2.9.252** | **書体を Google Fonts の LINE Seed JP に切り替えた** (v2.9.251 の「配信されていない」という判断が誤りだった)。全7アプリの `index.html` に `family=LINE+Seed+JP:wght@400;700;800` を追加し、自己ホスト用 CSS は削除。日本語は unicode-range で 372 サブセットに分割配信されるため自己ホストより軽い。CSP 変更不要。**配信ウェイトは 400/700/800 だけ**なので `font-medium`(500) は 400、`font-semibold`(600) は 700 に丸められる。 |
| **v2.9.251** | **UI/UX刷新 Phase 1: 共通シェル (上辺 + 6項目のレール) を全7アプリに適用 + 4つの状態を共通化 + 入口の一本化**。`AppSwitcher` と各アプリの Header/Sidebar (計14ファイル) を撤去し `shared/src/client/shell/` に集約 (PCは左88pxの縦レール・スマホは下タブ)。空/読み込み中/エラー/権限なしを `shared/src/client/states/` に共通化し、例外メッセージを画面に出さない `humanizeError()` を関門に。トークン刷新 (背景 #f7f8fa・カード角丸14px・AI色・状態の帯)。**旧グローバル検索から案件を開けなかった実バグ**も修正。 |
| **v2.9.250** | **Hotfix: v2.9.249 の Docker ビルドが `scripts/check-collab-parity.mjs` を見つけられず失敗**。server の `prebuild` に追加した検証スクリプトを、検証対象が両方揃う `build-server` ステージで走らせる設計にしたが、**スクリプト本体を置く `scripts/` を同ステージに COPY していなかった** (`.dockerignore` の再包含はビルドコンテキストに含める指定であって COPY の代わりにはならない)。`build-server` に `COPY scripts/check-collab-parity.mjs scripts/` を追加。ステージのファイルセットを厳密に再現した環境で exit 0 / 乖離時 exit 1 の両方を確認。deploy は `needs: build` のため実行されず dev・本番とも無傷。Dockerfile / .dockerignore のみ。 |
| **v2.9.249** | **改革 Phase 5 下地: 案件 (GLS-B) 同時共同編集の土台**: Qシートの Yjs 基盤を**写さず共有**に切り出し (`shared/collab/roomManager` / `socketAuth` / `socket`)、判定は `meetsPermissionLevel` で HTTP と socket が同じ関数を通る。対象は**作業メモとチェックリスト**に限定 (タスクは行で 5 経路が既に SQL で書いており書き手が 2 つになる)。**`yjs:sync` のレースを修正** — connection ハンドラを async にして await していたため接続直後の最初の sync が捨てられていた (Qシートも同じ形だったので両方修正)。自由文は Y.Text、変換層の 2 部一致をビルド時に検証 (ずれたら exit 1)、HTTP 全置換は編集中なら 409。migration 136 (`project_collab`)。サーバーのみ・画面は UI 改修とマージ後。 |
| **v2.9.248** | **改革 Phase 3: AI改善ループが閉じた**: 投入の解析前に修正傾向のダイジェストを読み **advice をプロンプトに載せる**（MCP を通らない UI 経路でもループが回る）。傾向を載せた回は `prompt_version` を `+fb` で分け、版ごとの無修正採用率で改善を数字で言える。集計が `tasks[d1]` のように draft_key で分裂して読めなかったのを**鍵を潰した集計**に是正。誤検知率と期限内完了率を追加。**確定後 24 時間以内の修正も差分として拾う**（それ以降の期限延長は正常な業務更新として数えない） |
| **v2.9.247** | **改革 Phase 2c: `/daily/tasks` 4 タブ + 9 マスボード + 依頼の差し戻し決着**: マイタスク / 依頼 / 投入ログ / チームを 1 ページに集約。**9 マスボードとスコア順リストの両方**を出す (同点のマスは打ち手が真逆なのでスコアだけでは並べられない)。差し戻された依頼は依頼者が「自分でやる / 振り直す / 取り下げる」を決めるまで消えない。チームタブは**件数だけ**でタイトルは返さない。あわせて解析を **OpenAI (ChatGPT) にも対応** (スキーマ・検証はプロバイダ共通) |
| **v2.9.246** | **改革 Phase 2b: 投入口 + 確認画面 + Claude API 解析 + ストック型通知**: トップ最上部の投入欄に「ひとこと/議事録」を投げると AI が誰に・何を・いつまでにを読み取り、同一ページの確認モーダルを出す (別ページに飛ばすと登録を押し忘れて元の課題が再発するため遷移を挟まない)。解析は Claude API を主経路・規則ベースをフォールバックの 2 本立てで、キー未設定/障害/タイムアウトでも縮退して投入は必ず通す。LLM 出力は後段で必ず検証 (存在しない `users.id` は捨てて宛先要確認に倒す/壊れた期限は期限不明に倒す/緊急度は期限からの写像で上書き)。通知はストック型で件数だけ (ポップアップは作らない)。`ai_outputs` に `model`/`prompt_version` を記録して改善効果を測れるようにした |
| **v2.9.245** | **改革 Phase 2a: 個人タスクと依頼の器 + 投入テキストの一次資料化**: `project_tasks.project_id` を NULL 可にして案件に紐づかない個人タスクを表現可能に。`requester_id`/`due_at`(分単位)/`importance`・`urgency`(1〜3)/`delegation_status` を追加し、スコアはカラムにせず式インデックスで並べる。投入テキストを全文保存する `task_intake` を新設し、タスクから元の議事録本文へ遡れるようにした。MCP 10 ツール (計 82 種) |
| **v2.9.222** | **内覧会 来場予約の強化: 同行者の可視化 + ソート + 会社別サマリー + CSV出力**: ①同行者を代表(登録者)と区別しつつ個別の参加者チップとして表示、②回の順序(新しい/古い)+回内ソート(会社名/氏名/参加人数/来場状況)、③会社別サマリー(会社名｜参加人数を人数降順)、④CSV出力(参加者ごと1行・UTF-8 BOM・Excel文字化け防止)。client-dailyのInviewPageのみ・DB/サーバー変更なし。tsc+vite build+eslint通過 |
| **v2.9.220** | **営業ジャーニー刷新 フェーズD完: IA仕上げ**: ①サイドバーをジャーニー順(要対応→お客様→商談→案件→タスク→ふりかえり→マスター)に再編・顧客を「お客様」トップに昇格、②ヨミ・パイプライン(/sales/pipeline)新設=進行中案件をステージ列カンバンで一望(件数/金額/次アクション/ホット)、③ホーム最上部を受信箱サマリーに(今日待たせているものを最優先)。sales-boardにexpected_amount追加のみ・migration不要。フェーズA〜D完了。ローカルPG+Playwright実機検証済 |
| **v2.9.219** | **営業ジャーニー刷新 フェーズC: 案件ワークスペース再構成**: 案件編集ページを逆算思考のジャーニー縦軸に再構成。ジャーニーステッパー(ネタ→…→完了・現在地強調)、「今すべきこと」(次回アクション+完了/延期、進行中で未設定なら⚠️警告=自問自答⑩)、タイムラインを上部に移動(編集頻度の高いものを上へ)。サーバー変更なし・migration不要。ローカルPG+Playwright実機検証済 |
| **v2.9.218** | **営業ジャーニー刷新 フェーズB: 顧客360**: お客様単位で全接点を1画面に集約する顧客360 (/sales/customers/:id) を新設。取引実績サマリー・統合タイムライン(直付け+案件経由・その場でインライン追記可・D3解消)・案件リスト・年次売上バー。顧客一覧の行クリックで遷移 (従来は編集ダイアログのみ・D1解消)。GET /customers/:id/overview 追加、migration 不要。ローカルPG+Playwright実機検証済 |

最新 20 件のみ。それ以前は [`docs/release-notes.md`](docs/release-notes.md) に、各版の全文は [`docs/version-history.md`](docs/version-history.md) にあります。


---

## ライセンス

本プロジェクトは GMOグローバルスタジオ社内で利用するクローズドソースです。
