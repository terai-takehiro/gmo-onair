# TOKEN消費量が多い理由 (2026-08-18 時点の実測)

このリポジトリでコーディング作業をすると、他プロジェクトより TOKEN 消費が
明確に多くなる構造がある。**「セッション開始時に毎ターン読まれるもの」**と
**「作業中に繰り返し読まれるもの」**の2系統に分けて要因を洗い出した。

## 1. 毎ターン強制的に文脈へ載る CLAUDE.md 自体が大きい

- ルート `CLAUDE.md`: **34.5KB**
- モノレポなので `client/`・`shared/` など各ディレクトリにも個別の `CLAUDE.md` があり、
  そのディレクトリを触るたびにさらに追加で読まれる (階層ごとに積み重なる)

CLAUDE.md 自身が「この節は毎ターン文脈に読み込まれるため、小さく保つ」と
明記して過去に一度 707KB→35KB に切り出した実績があるが、それでも **35KB は
全セッションの土台コストとして毎ターン乗る**。会話が長くなるほど、この
「固定費」の割合が効いてくる。

## 2. バージョン履歴が実質無制限に肥大化する構造

- `docs/version-history.md`: **1.32MB**（v4.0.2 以前・427件分）
- CLAUDE.md の「## 現在のバージョン」に残るのは直近3件だけ、というルールで
  応急的に抑えているが、**リリースのたびに1件ずつアーカイブへ移す運用**なので
  `docs/version-history.md` 自体は増える一方で減らない
- リリースノートの1文が非常に長い（1バージョンで数百〜千字規模、絵文字付き
  箇条書きで「①②③…⑨」まで続く）ため、**バージョンを1つ上げるだけで
  CLAUDE.md に数KB単位で文章が増える**
- この履歴を読む・生成する作業（`node scripts/v4-progress.mjs`、
  `release:notes` 等）に触れるたびに、1.3MB のファイルを Read/Grep する
  コストが発生する

## 3. レビュー指摘の「棚卸し」ファイルも成長し続ける

- `docs/reviews/codex-findings-v4.md`: **82KB**
- 運用ルール上「マージすると指摘は画面から消えるので記録する」「直さないと
  決めたものも表から消さない」となっており、**指摘は基本的に削除されず
  蓄積専用**。PR を出す・レビューを受けるたびにこのファイルへの追記が発生し、
  かつ pr-watch スキル使用時にこのファイル全体を読みに行く

## 4. モノレポ規模そのものが大きい

- npm workspaces が **9個**（client / client-daily / client-equipment /
  client-qsheet / client-techsheet / client-live / client-awards / server /
  shared）
- `build:all` は9ワークスペースすべてを個別ビルド、`typecheck:all` も
  7クライアント+server を毎回叩く。1タスクで済む変更でも
  「関連ワークスペースの CLAUDE.md を読む→lint/build/typecheck の出力を
  読む」を7〜9倍のスケールで踏むことになりやすい
- `lint` は9個のチェックスクリプト（changelog / fonts / tokens /
  shared-wiring / file-size / ui-tokens / mobile宣言 / env / links /
  contrast-tokens）+ eslint の直列実行で、失敗時の出力もすべて文脈に乗る

## 5. スキル・エージェント運用そのものがトークンを使う設計

- `pr-watch` スキル（140行）: **PR を出したら確認を待たずに必ず使う**運用が
  CLAUDE.md に明記されている。マージまで CI失敗・レビュー指摘を継続監視する
  ため、1つの PR で複数ターン分のツール呼び出し（CI ログ読み・レビュー本文
  読み・棚卸しファイル更新）が積み重なる
- `ai-feedback-loop` スキル（85行 + references 32KB）: AI機能を触るたびに
  「5条件の充足表」を作る運用があり、`onair-current-state.md`
  （14.8KB）を毎回読みに行く
- 両スキルとも「必ず使う」「確認を待たずに」という強制トリガーが
  CLAUDE.md に書かれているため、**判断コストではなく実行コストとして
  トークンに乗る**（スキップする余地がない）

## 6. 環境分離・安全策の手順が長文で埋め込まれている

- 本番/検証の分離ポリシー、DBバックアップ/復元の CLI 手順、5層の安全策など、
  実行コマンドの具体例が CLAUDE.md に直接書かれている。これらは頻度は低いが
  1回あたりの文脈占有は大きい（コードブロックが多い）

## まとめ: 効いている順（推定）

1. **`docs/version-history.md` の肥大化**（1.3MB・読む/生成するたびに直撃）
2. **モノレポの9ワークスペース構成**（ビルド・lint・typecheckが横に9倍）
3. **CLAUDE.md 本体の34.5KB**（会話の土台として毎ターン乗る固定費）
4. **pr-watch/ai-feedback-loop の「必ず使う」強制スキル運用**
5. **codex-findings 棚卸しファイルの追記専用・非削除運用**（82KB）

## 改善の方向性（提案）

- `docs/version-history.md` を**年度や v4.0系/v3系で分割**し、必要な範囲だけ
  Grep/Read する運用にする（全文を毎回読まない）
- リリースノート1件の文章量に**上限を設ける**（現状「①②③…⑨」まで書く運用が
  肥大化の主因の一つ）
- `codex-findings-v4.md` も同様に**期間で分割**し、古い棚卸しは別ファイルへ
  切り出す
- CLAUDE.md の「絶対原則」「安全策」系の長文コマンド例は、**必要時にだけ
  読むサブドキュメントへ切り出し、CLAUDE.md からはリンクのみにする**
  （すでに docs/ への切り出しは一部実施済みなので、その範囲を広げる）
