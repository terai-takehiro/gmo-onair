# v4 ネイティブUI監査 詳細（2026-08-20・並列7エージェント）

> [docs/v4-native-ui-plan.md](v4-native-ui-plan.md) の監査結果の元データ。生JSONは
> [v4-native-ui-audit-2026-08-20.json](v4-native-ui-audit-2026-08-20.json)。

## 案件管理・タスク（client/src/contexts/sales/, client/src/contexts/tasks/）

| 画面 | URL | 判定 | 規模 | 不足点 |
| --- | --- | --- | --- | --- |
| ① ダッシュボード | `/sales/dashboard` | ✅ ネイティブ級 | 小 | - 数字カードを押すとページ遷移のみで、詳細を見るための下シートやインライン展開は無い<br>- pull-to-refreshやスケルトン以上のネイティブ的インタラクションは無い |
| ② 案件作成（受付を統合） | `/sales/projects/new` | ✅ ネイティブ級 | 小 | - 戻る操作が矢印ボタンのみで、スワイプバックのようなジェスチャーは無い<br>- 録音・添付はこの画面自体ではなくトップのAIバー経由（投入口）で、画面内には撮影/添付の導線が無い |
| ③ 案件一覧 | `/sales/projects` | ✅ ネイティブ級 | 小 | - カードにスワイプアクション（アーカイブ等）は無く、タップして詳細へ遷移するのみ<br>- ページ送りはボタン式（PageNav）で、無限スクロールではない |
| ④ タスク一覧（やること） | `/sales/tasks/:view (list)` | ✅ ネイティブ級 | 小 | - タスクの新規追加ボタンがこの画面には無い（PCのAddTaskDialog相当が無い）<br>- 延期・担当変更はできず「PCで」の案内のみで、片づけられるのは完了操作だけ |
| ⑥ 案件詳細 | `/sales/projects/:id` | 🔧 レスポンシブ止まり | 大 | - タスクタブ（TasksTab.tsx）はKanbanView/TaskListView/GanttViewという旧v3時代の未改修コンポーネントをそのまま呼んでおり、カンバン列・ガント時間軸が横スクロール前提で375pxに全く最適化されていない（ViewToggle.tsxも rounded-md 等の旧トークンで、v4のRow/Sheet系ではない）<br>- OverviewTabはPC/スマホ共通コンポーネントで、専用のモバイルレイアウト（カード積み等）を持たない<br>- 見積・書類・やり取りタブはPcOnlyPanelで止めているが、「それでもこのまま開く」を選ぶとPC向けの中身がそのまま出る |
| ⑤ 見積・請求（全案件） | `/sales/billing` | 🖥️ PC専用（妥当） | 中 | - 現状スマホからは代替導線が無く、外出先で入金確認したい場合は/budget/billing（⑫入金の確認）に頼るしかない（billingとは別の画面） |
| ⑧ 料金表 | `/sales/pricing` | 🖥️ PC専用（妥当） | 中 | - 読むだけの用途（現場で単価を確認したい）でもPC専用に倒しており、閲覧専用モードのようなスマホ代替は無い |
| ⑦ 標準工程テンプレート | `/sales/flow-templates` | 🖥️ PC専用（妥当） | 小 | - 技術的な列崩れではなく運用判断でのPC専用のため、将来モバイル対応する場合はRow系の`stackOnMobile`をそのまま使える見込みがある（作り込みの土台はある） |
| 案件台帳 | `/sales/projects/ledger` | 🖥️ PC専用（妥当） | 中 | (無し) |
| 取引先マスター | `/sales/companies` | 🔧 レスポンシブ止まり | 中 | - 役割絞り込み（顧客/仕入先/販管費支払先等）がFilterChipsの横並びのままで、案件一覧のような下シート化はされていない<br>- 編集はRowクリックで既存のCrudFormDialog（PCと共通のフォームダイアログ）を開くだけで、スマホ専用の入力導線は無い<br>- 行の縦積み後も連絡先・インボイス番号・関連ページ列はhideOnMobileで単純に非表示になるだけ（情報の再構成ではなく列を消しているだけ） |
| 営業活動記録・営業レビュー | `/sales/activity-logs` | 🖥️ PC専用（妥当） | 中 | - 記録タブ単体は以前スマホ対応済みだった実装（ActivityMobileFilters等）が今も部品として残っており、『それでもこのまま開く』を選べば使える——つまり記録タブだけなら技術的にはモバイル対応済みだが、統合の都合で入口ごと閉じている |
| 費用を分け合うグループ（一覧） | `/sales/project-groups` | ❓ PC専用（要再検証） | 小 | - 一覧ページ自体はカードグリッドで既にモバイル向きの構造になっているのに、詳細画面と同じ理由で入口ごとPC専用に倒されている<br>- 『これから作ります』ではなく理由が詳細画面のものを流用しているだけに見える |
| 費用を分け合うグループ（詳細） | `/sales/project-groups/:id` | 🖥️ PC専用（妥当） | 中 | (無し) |
| お客様の詳細（顧客360） | `/sales/customers/:id` | ❓ PC専用（要再検証） | 大 | - v4のRow/Sheet/PageHeader等のトークンを一切使っておらず、案件一覧・料金表のような技術的な横幅理由とは異なり純粋に未着手<br>- M10の棚卸しで『取引先マスター』は開放されたのに、この画面だけ据え置かれたまま残っている |

## 財務管理 (client/src/contexts/finance/)

| 画面 | URL | 判定 | 規模 | 不足点 |
| --- | --- | --- | --- | --- |
| ① ダッシュボード | `/budget/dashboard` | 🖥️ PC専用（妥当） | 大 | - スマホは PcOnlyGate の案内表示止まりで専用モバイル版が存在しない(意図的)<br>- PC自体もカレンダー①②のようなmacOS風の常設サイドパネル等の演出は無く、通常のWebダッシュボードカード構成のまま<br>- KPIの帯(Breakdown/ProfitFlow)は横並びの引き算表示のみで、iOSライクなカード/シート発想への転用検討は見当たらない |
| ② 請求・入金 | `/budget/billing` | ✅ ネイティブ級 | 小 | - PC側(DesktopClosing)はRow一覧+タブ+sticky footerの通常Web構成で、カレンダーPC版のようなmacOSアプリ風の演出(常設サイドパネル等)は持たない<br>- MobileCollectは検索・絞り込みが一切無く、期日近い順の固定並びのみ(意図的簡略化だが探す手段はゼロ)<br>- 取り消しはPCへ誘導する設計のため、モバイル単独では『記録』の片道機能に留まる(意図通りではあるがネイティブアプリとしての完結度は低い) |
| ③ 売上 | `/budget/revenues` | 🖥️ PC専用（妥当） | 大 | - スマホでは開けない(PcOnlyGate案内のみ)。理由はpcOnlyScreens.tsの通り桁比較の表であることに起因し妥当<br>- 編集ダイアログ(RevenueDialog)は通常のモーダルで、下シート化の検討痕跡なし<br>- 1,435行の単一ファイルのまま(v4作り直し未着手扱い)で、モバイル化以前にPC側の分割自体も残課題 |
| ④ 仕入 | `/budget/purchases` | 🖥️ PC専用（妥当） | 大 | - スマホでは開けない(PcOnlyGate案内のみ)。台帳の金額比較用途としては妥当な判断<br>- 変動原価/固定原価のタブ切替もPC表前提で、カード積み等のモバイル向け表現は未検討<br>- PurchaseDialog(編集)は通常モーダルのまま |
| ⑤ 販管費 | `/budget/sga` | 🖥️ PC専用（妥当） | 大 | - スマホでは開けない(PcOnlyGate案内のみ)。妥当だが、勘定科目で絞れない等の機能的な穴もCLAUDE.mdに記載あり(モバイル化以前の課題)<br>- SgaDialogは通常モーダルのまま、下シート化なし<br>- 他2台帳と同じ共通部品のみで、この画面固有のモバイル検討は行われていない |
| ⑥ 取引先 (仕入先・パートナー) | `/budget/vendors` | 🔧 レスポンシブ止まり | 中 | - 一覧はRow+RowSlot hideOnMobileでPC表の列を落とすだけの縮小表示で、カード積み等の専用モバイルパターンではない<br>- 追加・編集はCrudFormDialog(通常のセンターモーダル)のままで、下から出るシートになっていない<br>- スマホでは道具(Excel取込出力・仕入先集計)を意図的に非表示にしているが、その代替導線(例:電話発信ボタン等)は無く『表を狭めて道具を隠しただけ』の域を出ない |
| ⑦ 受け取った書類 | `/budget/documents` | 🖥️ PC専用（妥当） | 大 | - スマホでは開けない(PcOnlyGate案内のみ)。取り消せない一括処理という理由は妥当<br>- HandoffDialog/DocDetailsはPC向けモーダル・詳細パネル構成で、モバイル向け検討の形跡なし<br>- 日常業務アプリ側(/daily/finance)からの転送元でもあるため、日常的に使う職種が実はスマホから触れない点は業務上の穴として残る(UI設計自体は理由通り妥当) |
| ⑧ 取り込み (精算PDF・総勘定元帳・二重計上) | `/budget/import` | 🖥️ PC専用（妥当） | 大 | - スマホでは左メニューからも消え、URL直打ちでもPcOnlyGate案内のみ(意図的)<br>- 3タブとも表+ダイアログ(PdfReviewDialog等)のPC向け構成で、モバイル対応の検討自体が行われていない<br>- 権限でタブ自体を出し分ける設計(system_admin限定タブあり)のため、そもそも日常的にモバイルで触る対象ではない |
| 仕入先集計 | `/budget/reports/vendors` | 🖥️ PC専用（妥当） | 中 | - スマホでは左メニューからも消え、リンク元(取引先画面)もスマホ版ではボタンごと非表示にして行き止まりを回避している<br>- 縦(仕入先)×横(月)のクロス集計表で、モバイル向けの折りたたみ表現は未検討<br>- ファイルの置き場所がfinance配下ではなくproduction配下にあり、機能上はfinanceの一部として使われているため設計上の把握漏れが起きやすい |

## カレンダー（client/src/contexts/production/ 配下の6画面）— PC macOSアプリ風 / スマホ iOSアプリ風 の到達度監査

| 画面 | URL | 判定 | 規模 | 不足点 |
| --- | --- | --- | --- | --- |
| ① 予定（UnifiedCalendarPage.tsx） | `/studio/calendar` | ✅ ネイティブ級 | 中 | - 『予定を入れる』の3導線のうち部屋を押さえる（StudioBookingDialog.tsx）だけが自前実装の下シート（rounded-t-[20px]+slide-in-from-bottom、L355-357）。パートナー予定・自分の予定の入力（PartnerScheduleDialog/PersonalEventDialog）・予約詳細（StudioBookingDetailDialog）・絞り込み（RoomFilterDialog/UserFilterDialog/LayerFilterDialog）は全て shared/src/client/ui/dialog.tsx の中央ポップアップ（fixed top-50%/left-50%）のままで、6ダイアログ中1つしか下シートになっていない<br>- その1つの下シートも共通部品 client-v4/sheet.tsx の <Sheet>（shared/CLAUDE.mdが定めるopt-inの共通シート実装）を使わず、StudioBookingDialog内に個別実装しており、他画面へ展開する土台になっていない<br>- MobileToday内のレイヤー絞り込み（LayerFilterDialog）も同じ中央ポップアップで、iOSらしい下からの操作導線になっていない |
| ② 部屋の空き（RoomAvailabilityPage.tsx） | `/studio/rooms` | 🖥️ PC専用（妥当） | 大 | - モバイルレイアウトが存在しない（useIsMobile分岐なし、MobileToday/MobileMonthGridに相当する専用UIが無い）<br>- PCの表（部屋×時間の帯）をそのまま横スクロールさせる構造で、崩れてはいないが縮小するだけの形にすらなっていない<br>- 画面内に『今日の予約を見る』のような代替導線は無く、instead案内はpcOnlyScreens.ts側（PcOnlyPanel）任せ |
| ③ 仮押さえ（HoldListPage.tsx + holds/HoldCards.tsx） | `/studio/holds` | ✅ ネイティブ級 | 小 | - 『落とす』の確認は共通のconfirmAction（shared/src/client/ui/confirm.tsx）で、中央ポップアップのまま（下シートではない）<br>- スワイプで確定/落とすといったジェスチャー操作は無く、フルwidthの2ボタン押下のみ<br>- 件数の絞り込み(FilterChips)はPC/モバイル共通の横スクロールチップのみで、専用の下シート絞り込みは持たない（件数が少ない画面なので実害は小さい） |
| ④ 設定（部屋・外部カレンダー・サイネージ）（CalendarSettingsPage.tsx） | `/studio/settings` | 🖥️ PC専用（妥当） | 中 | - useIsMobile等のモバイル分岐が無く、タブ切替(FilterChips)を含め全てPC想定のレイアウトのみ<br>- 呼び出している StudioRoomsManagerDialog / IcsFeedsDialog は v4 で作り直しておらず、生の中央ポップアップDialogのまま（コメントで明言済み・意図的に据え置き）<br>- hidden指定が無いため、スマホの左メニューにはこの設定項目が出続ける（開くとPC専用案内になるだけで導線自体は残る） |
| ⏸（旧）スタジオカレンダー（StudioCalendarPage.tsx） | `/studio/studio-calendar` | 🖥️ PC専用（妥当） | 中 | - useIsMobileはあるが、view を強制的に list（EventTable、Row stackOnMobileの表縮小）に固定するだけで、①予定のような専用モバイルUI（月表+当日アジェンダ）は無い＝responsive_only相当のフォールバック<br>- この画面の主目的である香盤ビュー（KoubanView、部屋を縦に並べた表）はモバイルで完全に非表示になり、代替導線も画面内には無い<br>- ①予定への統合可否の判断待ちで塩漬け中のため、単独でのモバイル作り込みは方針上まだ着手されていない |
| ⏸（旧）自分の予定（MyCalendarPage.tsx） | `/studio/my-calendar` | 🖥️ PC専用（妥当） | 中 | - useIsMobileでviewをlistに強制するだけで、専用モバイルレイアウト（下タブ/下シート/カード積み）は無い<br>- この画面の強み（取込元ごとの詳しい色分け：Google/Outlook/ICS/共有）はlist表示のバッジ色だけに畳まれ、モバイルでの一覧性を高める工夫は特に無い<br>- OAuth連携（Google/Outlook）の設定操作自体もPC想定のまま、モバイル向けの導線は用意されていない |

## 設定・共通（client/src/contexts/platform/ 配下）

| 画面 | URL | 判定 | 規模 | 不足点 |
| --- | --- | --- | --- | --- |
| トップページ | `/` | ✅ ネイティブ級 | 小 | - AI入力シートはRadixのオーバーレイタップ/×ボタンでしか閉じられず、iOSのスワイプダウンでの閉じ操作は無い<br>- アプリタイルは静置の2列グリッドで、iOSホーム画面のような長押し並べ替えは無い(モックにも無い機能だが)<br>- ミニアプリ(日常業務6つ)はPCがhover/chevronで畳めるのに対し、モバイルには畳み導線自体が無くタイルを開いてから1手多い |
| ① 案件管理ダッシュボード(実体はplatform配下) | `/sales/dashboard` | ✅ ネイティブ級 | 小 | - 止まっている案件の中身・ステージ別金額・今週の現場はモバイルで完全非表示（『PCのダッシュボードに出ます』の1行案内のみ）で、簡易版やドリルダウン導線が無い<br>- カードはボタン止まりで、pull-to-refreshやスワイプで直接タスクを開く等のiOSらしいジェスチャーは無い<br>- 受付3行(IntakeRow)と数字3行(CountRow)が同じ縦並びパターンで、iOSのセクション区切り(グループ化テーブルビュー)のような視覚的な塊分けが弱い |
| 探す(スマホ下タブ3つ目) | `/search` | 🔧 レスポンシブ止まり | 中 | - PC/モバイル共通の1レイアウト(Row stackOnMobile)のみで、下タブ画面としての専用ナビゲーション演出(キャンセルボタン等)が無い<br>- 『最近見たもの』は読むだけで削除・スワイプ操作が無い<br>- 結果セクション(案件/お客様/仕入先)はカード化されておらず、PCと同じ罫線区切りリストのまま |
| ① 設定トップ(案内板) | `/settings` | 🔧 レスポンシブ止まり | 中 | - カードグリッドをsm:grid-cols-2に畳んでいるだけで、iOS設定アプリのようなセクション区切りリスト行(chevron付き)になっていない<br>- 権限が無いカードを丸ごと非表示にする配慮はあるが、モバイル専用のインタラクション(検索・折りたたみ)が無い |
| ② 拠点・部屋 | `/settings/sites` | ❓ PC専用（要再検証） | 小 | - pcOnlyScreens.tsの理由文が『影響が大きい』であり、画面の狭さ・列数の多さを根拠にしていない(他のPC専用表と理由の質が違う)<br>- 拠点テーブルの各行はRow(stackOnMobile無し)のままで、375pxで略称/部屋数/料金表の3スロット(合計352px)がRowMainを圧迫する可能性が未検証<br>- 同じ画面が呼ぶRoomsTabは別画面(カレンダー設定)側の判断に委ねられており、この画面独自のモバイル対応方針が無い |
| ③ 権限とメンバー | `/settings/users` | ❓ PC専用（要再検証） | 小 | - PC専用の根拠(pcOnlyScreens.tsの『12区画×5段』)が権限モデル単純化前の古い実装を指しており、現行の7区画バッジ表示とは一致しない<br>- メンバー一覧のRowは既にhideOnMobile/stackOnMobileを使用済みで、活動記録一覧のように開放できる可能性があるのに未検証のままPC専用<br>- 役割レール(240px固定)はlg:flex-rowで縦積みに落ちる作りだが、モバイルでの実機検証(Playwright等)の記録が無い |
| ⑤ お金のルール | `/settings/money` | ❓ PC専用（要再検証） | 中 | - pcOnlyScreens.tsの理由が『効果が全社に及ぶ』という重要性の説明で、375pxで読めない/押せないという技術的根拠が書かれていない<br>- Line/Groupコンポーネントは既にsm:ブレークポイントで縦積みに畳む作りが入っており、実質的にモバイル未検証のまま止めている状態に見える<br>- 右カラムの『この設定が効く場所』パネルはlg:w-[240px]固定で、モバイルでどう配置されるか(下に回り込むだけか)が画面内で作り込まれていない |
| ⑥ 休日・営業時間 | `/settings/hours` | ❓ PC専用（要再検証） | 中 | - 休業日・祝日テーブルは既にstackOnMobileを使用済みで、機構としては開放可能な形に見えるが実機検証の記録が無い<br>- 曜日ごとの営業時間行はselect(時刻)2つ+ボタン群が横一列のflex-wrapで、44px未満のタップ領域や折返し時の見え方が未測定<br>- 拠点レール(240px固定)+本文2カラムのlg:flex-row構成で、モバイルでの縦積み後の情報量(祝日89件の折りたたみ含む)が検証されていない |
| ⑦ 通知とテンプレート | `/settings/notify` | ❓ PC専用（要再検証） | 小 | - PC専用の根拠が『落ち着いて触るもの』という運用上の話で、画面の作り自体は既にsm:ブレークポイント対応済み<br>- 操作が『押す→ダイアログでコピー』の1タップ中心で、他の開放済み画面(取引先など)と比べてモバイル向きの性質を持つのに未検証のままPC専用にしている<br>- 定時実行ログ(直近6件)の表示がテーブルではなく素のflex-wrap行なので、モバイルへ出しても崩れにくい可能性が高いが確認されていない |
| システムの情報 | `/settings/system` | 🔧 レスポンシブ止まり | 小 | - ブロックアプリ一覧はshadcnの生Tableをoverflow-x-auto任せで出しており、PCの表をそのまま横スクロールさせている典型例<br>- パスワード変更フォームはmax-w-smのPC都合の幅指定のみで、モバイル向けの1カラム最適化(ラベル位置・タップ領域)が個別に検討されていない<br>- 各Cardは縦積みの汎用カードで、iOSのグループ化リストのような一体感のある見せ方になっていない |
| データビューア | `/settings/data-viewer` | 🖥️ PC専用（妥当） | 小 | (無し) |
| DBバックアップ | `/settings/db-backups` | 🖥️ PC専用（妥当） | 小 | (無し) |

## プロジェクト管理（新規）— client/src/contexts/gpm/

| 画面 | URL | 判定 | 規模 | 不足点 |
| --- | --- | --- | --- | --- |
| ① ダッシュボード | `/gpm/dashboard` | 🔧 レスポンシブ止まり | 小 | - KPI帯（KpiStrip.tsx）はモバイルで単純に5枚が縦積みになるだけで、案件管理トップが決めた「PCは文章・スマホはチップ」のような出し分けが無い<br>- 『動いているプロジェクト』の行は w-24/w-28 の固定幅spanを横並びさせるミニ表で、案件一覧が卒業した『PCの行をそのまま縮めない』方針（ProjectCards.tsx）とは逆の作り<br>- 下タブ切替・ボトムシート・スワイプなどiOSアプリらしい操作が一切無く、単純な縦スクロール1本 |
| ② プロジェクト一覧 | `/gpm/projects` | 🔧 レスポンシブ止まり | 中 | - 行の描画が Row/RowSlot（hideOnMobile）で『いまの工程・進み具合・見積・次にやること』の4列を丸ごと消すだけの縮小テーブルで、案件一覧が意図的にやった『縮めるのではなくカードとして組み直す』（sales/pages/projectList/ProjectCards.tsx のコメント参照）に相当する作業をしていない<br>- モバイルではボードビュー自体を隠す判断は妥当だが、リストの代替として専用カードUIが用意されていない<br>- 並び替え時のFLIPアニメーション・ページ送りなど案件台帳が持つ仕上げが元々未実装（コメントに明記）で、モバイル固有の作り込みはさらに手薄 |
| ⑤ 全プロジェクトのタスク（やること／未確認事項） | `/gpm/tasks` | 🔧 レスポンシブ止まり | 中 | - タスク表は Row stackOnMobile ＋ hideOnMobile で『工程・担当』列を消すだけの縮小表（RowSlot w=128/96 を単純に隠す）<br>- 未確認事項（OpenItemRows.tsx）も同型の作り。案件一覧のような『金額・次の一手を前面に出すカード』への再構成はされていない<br>- タブ切替（タスク／未確認事項）はPCと同じセグメントボタンのみで、専用のモバイルナビゲーションは無い |
| ③ プロジェクト詳細（概要／未確認事項／体制／議事録／見積／請求／書類の7タブ） | `/gpm/projects/:id (/:tab)` | ❓ PC専用（要再検証） | 大 | - 工程表（PhaseRows.tsx）は既にRow/RowSlot（hideOnMobile）で列を間引く実装になっており、『横に伸びる』という理由と実装が一致していない（他の一覧同様375pxでも崩れずに縮む作り）<br>- 体制タブ（MembersTab.tsx）はコメントで明示的に『スマホでは1列に積まれ、段の見出しがそのまま順序になります』と書かれており、実装済みのレスポンシブ挙動があるのにPC専用扱いのまま<br>- 案件管理の『顧客』『取引先マスター』が同種の未実測理由をM10で書き直した前例（実測したら表ですらなくカードだった）と同じパターンに見え、GPM側はまだ実測されていない<br>- 7タブの切替がPCと同じ横スクロールタブのままで、案件詳細（⑥）が持つ段階別モバイルタブ絞り込み（MOBILE_TABS_BY_PHASE）に相当する仕組みが無い |
| ④ プロジェクト新規作成 | `/gpm/projects/new` | 🖥️ PC専用（妥当） | 大 | - 案件を直す（/sales/projects/:id/edit）が『入力欄が40以上あり、電話が入ると書きかけが残る』としてPC専用にした理由と同種で、体制（人を1人ずつ追加し保存後にまとめて登録）を含む多段フォームなのでPC専用の判断自体は妥当<br>- とはいえ実測（375pxでの崩れ・タップ領域）は行われておらず、他の理由文のような具体的な数字の裏付けが無い<br>- 段の切替（overflow-x-auto の横スクロールチップ）や体制入力の3段カードがモバイル幅でどう振る舞うかは未検証 |
| ⑦ 標準工程テンプレート | `/gpm/templates` | ❓ PC専用（要再検証） | 小 | - 理由『型を変えると以後すべてのプロジェクトに効きます』は /sales/flow-templates の理由文をそのまま流用しており、この画面固有の実測（375pxで崩れるか）が書かれていない<br>- 実装（TemplateCard）はflex-wrapのアコーディオンカードで、コード中のコメントに『shrink-0を付けない。4つの数字と2つのボタンを縮まない塊にすると375pxで44pxはみ出す（実測）』とあり、むしろ狭い幅を意識して調整済み＝モバイルでも大崩れしない可能性が高い<br>- 『設定・落ち着いて触る画面はPCのまま』という会社方針自体は妥当だが、実装が既に崩れない形になっている点で『まだ判断を書き直していないだけ』の疑いがある（顧客・取引先マスターがM10で書き直された前例と同型） |

## 日常業務 (client-daily) — v4 UI ネイティブ化監査

| 画面 | URL | 判定 | 規模 | 不足点 |
| --- | --- | --- | --- | --- |
| ウィークリー活動報告 | `/daily/weekly` | 🔧 レスポンシブ止まり | 中 | - 週選択(WeekRail)はPCと同じ<nav>をlg:w-[240px]の縦メニューからoverflow-x-autoの横スクロールに変えているだけで、専用の週ピッカーUIになっていない<br>- トピックスの追加・編集フォームはRowの位置にインライン展開されるだけ(TopicsSection.tsx)で、ボトムシートやモーダルになっていない<br>- 自動集計(StatsSection)の案件行・イベント行はRow+RowSlotで列を縦積みするだけの表縮小で、カード積み等のiOSらしいパターンは無い |
| デイリーニュース報告 | `/daily/news` | 🔧 レスポンシブ止まり | 中 | - 一覧行(NewsRow)はhideOnMobileでメモ・記入者列を落としRowSubに詰め込むだけで、専用のカードレイアウトは無い<br>- 編集フォーム(NewsForm)はRowの位置にそのまま展開されるインラインフォームで、ボトムシート化されていない<br>- 日付ナビはPCと同じ<input type="date">+前後矢印ボタンの組み合わせのままで、モバイル専用の日付選択は無い |
| 内覧会 開催日の一覧 | `/daily/inview` | 🔧 レスポンシブ止まり | 小 | - 検索・絞り込みはMobileFilterBar(ボトムシート)を使っており、iOSアプリらしいパターンを一部実装済み(他画面より効果は小さい追加投資で足りる)<br>- 開催日の行(DayRow)自体はRow/RowSlot(hideOnMobile)で組数・来場予定・受付の列を落とすだけで、カード型の専用レイアウトにはなっていない<br>- 検索結果(SearchHits)はカード(AttendeeCard)で出るが、通常時の日一覧はテーブル行のまま |
| 内覧会 その日の受付 | `/daily/inview/:date` | 🔧 レスポンシブ止まり | 小 | - 回ごとの見出し行(SessionBlock内のRow)はhideOnMobileで対象・組数・来場予定列を落とすだけの表縮小<br>- 受付/取消はボタンタップのみで、スワイプ操作などのネイティブ的操作は無い<br>- 会社別まとめ(CompanySummary)の表示切替はシート内のトグルボタンで、専用の表示コンポーネントではない |
| 入ってきた情報（その他問い合わせ） | `/daily/inquiries` | 🖥️ PC専用（妥当） | 中 | - 現状PC専用のため、モバイルで開くと案内画面(PcOnlyPanel)に差し替わり、緊急の問い合わせを外出先で仕分ける手段が無い(ただし本文どおり列数が多く仕分け作業自体が縮小に向かない)<br>- 仮にモバイル対応するなら、5タブ+多列表をカード積み+ボトムシート絞り込みへ作り直す必要があり、内覧会当日受付と同程度の投資が要る |
| セキュリティカード | `/daily/security-cards` | 🔧 レスポンシブ止まり | 中 | - master-detailがモバイルではflex-colで単純に縦積みになるだけで、カードを選ぶと詳細が同じページ内で下に続くだけ(専用のドリルダウン遷移やシートは無い)<br>- 貸出/返却(LendDialog/ReturnDialog)はshadcn Dialog(中央モーダル、max-h-[90vh] overflow-y-auto)で、他画面(内覧会)で使われているMobileFilterBarのようなボトムシートパターンは採用していない<br>- 検索・絞り込み(状態チップ・範囲チップ・検索欄)はPCと同じ形でヘッダーに並ぶだけで、モバイル専用の畳んだUIは無い |
| タスク・依頼（やること） | `/daily/tasks` | 🔧 レスポンシブ止まり | 大 | - 編集・依頼作成のダイアログはshadcn Dialog(中央モーダル)で、ボトムシート化されていない<br>- 『9マス』ボード表示はモバイルで固定220px×3列をoverflow-x-autoで横スクロールさせる実装(グリッドを崩さないための計測コメント有り)で、PCのグリッドをそのまま横スクロールさせているだけ<br>- 見出し・タブ・カードの文字サイズ/色が他のv4画面が使うtext-h1/text-h2等のトークンではなく生のTailwindクラス(text-xl, text-sm等)のままで、v4の型スケールに追随していない |

## 機材管理（client-equipment/src/）— PC macOS風・スマホ iOS風ネイティブUI方針への到達度

| 画面 | URL | 判定 | 規模 | 不足点 |
| --- | --- | --- | --- | --- |
| ① ダッシュボード | `/equipment/` | 🔧 レスポンシブ止まり | 中 | - リストは Row の stackOnMobile 任せで、PCの行を縦積みにしただけ（専用のカードデザインではない）<br>- KPIタイルはPC・スマホ同じ形の角丸カードで、macOS/iOSアプリらしい差別化されたウィジェット表現が無い<br>- 遷移はすべて通常のページ遷移（下シートで完結する操作が無い） |
| ② 機材台帳（機材／貸出機材／ケーブル／コネクタ） | `/equipment/items` | 🔧 レスポンシブ止まり | 大 | - その場編集（表で直す）がスマホに無い。押しても何も起きないボタン扱いで ItemsToolbar から落としている状態<br>- カスタム列がスマホでは読み取り専用（監査doc item3で明記された既知の未完了）<br>- 登録・編集は EquipmentDialog（shared/dialog.tsx の中央固定モーダル）のままで下シート化されていない。EquipmentDetailPage.tsx にも同じ密グリッドの重複実装がある |
| ③ ラック図 | `/equipment/racks` | 🖥️ PC専用（妥当） | 中 | - 現状は機能寄りのツール画面で、macOSアプリ風の余白・ツールバー的な意匠にはまだ寄せていない（RackList・RackUnitTableとも素朴なテーブル/リスト）<br>- ホバーツールチップに依存する情報提示があり、タッチのみの環境（PCでもタッチ操作の場合）には弱い |
| ④ メンテナンス | `/equipment/maintenance` | 🔧 レスポンシブ止まり | 中 | - PC表の列を hideOnMobile で間引いただけ（業者名・報告日がスマホで非表示になる）で、専用のモバイルレイアウトではない<br>- 記録を足すダイアログが下シートになっておらず、現場での片手操作を想定した作りではない<br>- 状態変更は Select（プルダウン）のままで、iOSアプリ的なアクションシート/スワイプ操作が無い |
| ⑤ 棚卸し | `/equipment/inventory` | ✅ ネイティブ級 | 小 | - 一覧画面（InventoryPage.tsx）自体は棚卸し「回」を選ぶだけの素の Row リストで、詳細に入って初めてネイティブ品質になる（一覧との差が大きい）<br>- 棚卸しを作るダイアログ（新規作成）は中央固定 Dialog のままで下シートではない<br>- 終了・再オープンなどの操作はPC専用のままで、スマホ側はその旨のテキスト案内のみ（設計判断として妥当だが機能的には非対称） |
| ⑥ QRスキャン | `/equipment/scan` | 🔧 レスポンシブ止まり | 小 | - 読み取り履歴が汎用 Row リストの再利用で、カード積み等の専用表現になっていない<br>- PC版とスマホ版で画面構成に差が無く、macOS/iOSそれぞれに最適化した分岐がされていない（同じ1カラムをそのまま両方に出している） |
| ⑦ 貸出・返却 | `/equipment/lendings` | 🔧 レスポンシブ止まり | 中 | - PC表の列を hideOnMobile で間引いただけ（持出日がスマホで非表示）で専用モバイルレイアウトが無い<br>- 貸出・返却のダイアログがどちらも下シート化されておらず、現場での片手操作向けの導線になっていない<br>- 状態の絞り込みはチップ表示で対応済みだが、それ以外はPC同型のまま |
| ⑧ 設定（保管場所／メーカー・色／貸出カテゴリ／貸出の決めごと） | `/equipment/settings` | 🖥️ PC専用（妥当） | 小 | - PC向けのタブ切替（SubTabs）は標準的なshadcn風で、macOSアプリ的な余白やサイドバー的意匠にはまだ寄せていない |
| 探す（スマホ下タブ3つ目） | `/equipment/search` | 🔧 レスポンシブ止まり | 小 | - 結果一覧が汎用 Row/RowMain の流用で、カード積み等の専用表現になっていない<br>- PC・スマホで全く同じ1カラムレイアウトを出しており、PC側をmacOSアプリ風に振った作り込みがされていない |
