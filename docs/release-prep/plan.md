# v4 リリース準備計画

作成: 2026-08-23（同日、techops 移行 Phase 2〜4 完了・表示レイアウト PR1〜3 完了を受けて R0/R1-c を更新）。
根拠: 7領域のマルチエージェント監査（95件）＋批評。生データは [audit-2026-08-23.md](audit-2026-08-23.md)。
この準備で生む文書（チェックリスト・棚卸し表・実施記録）は **この `docs/release-prep/` に新規ファイルで置く**
（新規ファイルは並行作業と衝突しないため。既存文書へはリンク1行を足すだけにする）。

---

## この計画を3分で（だれでも読める版）

**いまの状態**: v4（今回の全面リニューアルの呼び名）の部品は全部そろった。ただし本番のサイトは 8月18日の版（v4.1.7）のままで、
その後にできた **240 個以上の変更がまだ本番に出ていない**。この中には、データの保管庫（データベース）の
作り替えが約 27 個含まれ、**一部は一度実行すると元に戻せない**。

**いちばん大事な結論**: **大掃除より先に、たまった変更を本番に出す。**
掃除を続けるほど「一度に出す量」が増えて、何かあったときに原因を探せなくなる。
先に出してしまえば、掃除は小さく分けて安全に進められる。

**そのあとの掃除は6つ**（順番に意味がある）:

1. **検査の道具を先に足す** — 文書のリンク切れ検査・画面崩れ検査の穴埋め。掃除で壊したらすぐ分かるようにする
2. **ルールの見直し** — 古くなったルール・矛盾したルールを消す（例: 「凍結アプリ」＝見た目を変えない約束をしたアプリはもう1つも無いのに、それを守るルールが 20 か所以上残っている）
3. **文書の大整理** — 役目を終えた文書を書庫（archive）へ。README（このプロジェクトの玄関にあたる説明ファイル）は本文の8割が過去の更新履歴で埋まって長すぎるので、20分の1に減らす
4. **だれでも読める説明書づくり** — 「ONAiR とは何か」「直しはどうやって本番に届くか」など6本を新しく書く
5. **画面の総点検** — 機械検査 → 全画面のスクリーンショット → 実機（iPhone/iPad）の3段階で崩れを拾って潰す
6. **技術的な借金の返済** — 既知のセキュリティ更新・使っていないコードの削除など

**進め方の約束**: いまも複数の作業が同時に走っているので、
**小さく分ける・数時間で終わらせる・混み合う場所（制作技術支援まわり等）は避ける**。

**あなたに決めてほしいことが 7 つ**、すぐ下にあります。

---

## 決めてほしいこと（ユーザー判断・7件）

| # | 決めること | 推奨 |
|---|---|---|
| 1 | 次の版番号: **v4.2.0** か v4.1.8 か | **v4.2.0**（アプリ1つ削除・1つ廃止・制作技術支援の全面刷新・DB再編はパッチ級ではない） |
| 2 | リリース時期: **大掃除の前に出す**か、掃除込みの大リリースにするか | **前に出す**（本計画の前提。R0参照） |
| 3 | モバイル化の残件（案件詳細5タブ・機材台帳の親子入れ子）をこの準備に**含めるか次期送りか** | 次期送り（準備は「崩れの一掃」に絞る。作り込みは通常開発で） |
| 4 | 「古いデータ削除」の意図に**本番DBの業務データ・VPSのuploads/・BOXバックアップの保持期限**を含むか | 含まない（今回はリポジトリ内のみ。含むなら別途監査する） |
| 5 | 重いルールの継続: 全PRの棚卸し（reviews:debt）・ai-feedback-loop 必須・pr-watch マスト | 3つとも**継続**（実測で効果が出ている）。ただし棚卸しは「表の再構成禁止・行追加のみ」を明文化 |
| 6 | npm audit fix（依存の一括セキュリティ更新）を**リリース前に載せるか直後の小リリースか** | **直後の小リリース**（lockfile は並行PRの衝突源。ビッグバンに混ぜると切り分け不能） |
| 7 | リモートに残る**残骸ブランチ7本の削除承認**（R6で棚卸し表を出す。活動中の枝は含めない） | 表を見て承認（`fix/release-tooling-titles` の未マージ2コミットだけ取り込むか判断） |

---

## 全体の段取り

```
R0 たまった変更を本番に出す（最優先・単独で完結）
└→ R1 検査道具 ──→ R2 ルール刷新 ──→ R3 文書大整理 ──→ R4 説明書（guide/）
   └→ R5 画面の総点検（R1 の道具を使う。R2〜R4 と並行可）
   └→ R6 技術負債の返済（R0 の後ならいつでも。独立PR群）
```

- 各パッケージは **1セッション=1パッケージ=1ブランチ** で割り当てられる粒度にしてある
- どの PR も「着手直前に main を取り直す → 数時間以内にマージ」の短命運用（1日超えたら作り直す）

---

## R0 — たまった変更を本番に出す（完了・2026-08-22）

版番号 v4.2.0 で着手（ユーザー判断）。ゲート①②⑦（`_summary.md`・`release:notes --dry`・先取り表記の修正）を
Claude 側で実施し、PR #343（`release: v4.2.0`）・PR #344（マージ後の棚卸し）をCI green確認後マージ。
ゲート③④⑤⑥（本番DBバックアップ・drift SQL確認・go/no-go・スモークテスト・ロールバック手順の用意）は
[v4.2.0-production-checklist.md](v4.2.0-production-checklist.md) にまとめ、ユーザーが実施。

**確認できた事実**（GitHub API で直接確認）:
- Release `v4.2.0` 公開: 2026-08-22T17:24:45Z
- `release` イベントで起動した本番向け Deploy ワークフロー（run 32587649304）: **success**（17:27:43Z 完了）

### R0-fix — v4.2.0 公開直後に見つかった本番バグ（対応中）

ユーザーから「制作技術支援アプリ自体が露出していません」と報告。原因は qsheet→techops 移行
Phase 2（PR #337）で `apps.ts` の `AppKey` を改名した際、`HomePage.tsx` の `DAILY_KEYS` 配列が
旧キー `qsheet` のまま取り残されたため、トップページのタイルからだけ消えていた
（アプリ切替・左メニューは `visibleApps()` 経由のため無事）。`AppTiles.tsx` の `SEPARATE_BUNDLE`
も同じ理由で取り残されており、あわせて修正。

PR #345 で修正・ユーザーがマージ済み（`main` は反映済み・検証環境へ自動デプロイ）。
**本番（v4.2.0）にはまだこのバグが残っている** — 反映には次の小さなパッチリリース
（v4.2.1 推奨）が必要。マージ後の棚卸し記録は PR #346。

⚠️ 公開直後スモークテスト（チェックリスト§5）の実施結果・`docs/reviews/phase3-2-plan.md` への記録は
このセッションから本番へ到達できないため確認できていない。ユーザー側で完了していれば対応不要。

**内容**: v4.1.7 以降の main（250コミット超・changelog 下書き167件=2026-08-23時点で増加中・migration 204〜233）を1回のリリースで本番へ出す。
元に戻せない migration（customers/vendors 削除・23テーブル削除・列削除ほか）を含むため、ゲートを揃えてから出す。

**リリース断面**: 原則は「**進行中フェーズの途中でタグを打たない**」。
2026-08-23 更新: techops 移行は計画の **Phase 1〜4 がすべて完了**し（旧 `/qsheet/*` URL・旧 MCP ツール名は
ブリッジ/二重登録で無期限維持＝互換の取れた状態）、表示レイアウト機能も **PR1〜3 で完成**した。
つまり**いまの main はフェーズ境界にあり、そのまま断面にできる**。
断面確定時の確認は「open PR に機能の途中分割（PR n/m の途中）が無いこと」だけでよい
（docs のみの棚卸し PR は断面に影響しない）。
断面を切ってからマージまでの間、**他セッションのマージ一時停止をユーザーに依頼**する（開けたまま放置すると下書きが増えて必ず競合する）。

**ゲート（全部そろってから公開）**:

| ゲート | やる人 | 中身 |
|---|---|---|
| ① `docs/changelog.d/_summary.md` を書く | Claude | この版の要約1段落（制作技術支援の作り直し・計時統合・アプリ2つの終了・会社リスト一本化完了）。12KB超の自動分割で使われる。棚卸しだけの下書き約35件（ファイル名が reviewdebt / review-debt / 棚卸し記録系のもの）は1文に圧縮 |
| ② `npm run release:notes -- X.Y.Z --dry` | Claude | 全下書き（実行時点の件数）の消化・README マーカー・version-history 追記が通ることを事前確認 |
| ③ 本番DBのバックアップ＋復元確認 | **ユーザー**（VPS） | `backup-db-to-box.mjs` を公開直前に実行し、復元できるところまで確認 |
| ④ drift SQL を本番で実行 | **ユーザー**（VPS） | `npm run db:drift-sql` の生成SQL（SELECTのみ）で、migration 209 が消す列が本番でも全行既定値なことを確認 → phase3-2-plan.md に記録 |
| ⑤ 公開直後スモークテスト表 | Claude が表を用意 → **ユーザー**が実施 | /health・techops 公開URL・計時ミニアプリ・顧客/仕入先CRUD・MCP日次実行・廃止アプリ(/awards)の404。結果は `docs/release-prep/` に記録 |
| ⑥ 戻し方（ロールバック手順書） | Claude | 「旧タグ再デプロイ＋バックアップ復元」の手順と、公開を中止する判断基準（go/no-go）を1枚に |
| ⑦ 版番号の決定 | **ユーザー** | 判断#1。決定後に `docs/v4-plan.md`・`scripts/check-frozen-css.mjs` の「v4.1.8」先取り表記を実番号へ直す |

**リリースノート冒頭に必ず書くこと**: 利用者から見て「無くなるもの」（技術資料アプリ・リアルタイムCG・計時LIVE単独作成）。
既知の制限3件（機材3画面の名前列幅・catalog の1024px帯・数字の等幅）も1行ずつ。

**PR数の目安**: 2（_summary.md＋先取り表記修正、リリースPR本体）。

---

## R1 — 検査の道具を先に足す（掃除で壊さないため）

| PR | 中身 | 衝突リスク |
|---|---|---|
| R1-a | **Markdown リンクチェッカー新設**（md 内の相対リンク実在確認・10行程度）を `npm run lint` に組み込む。文書移動より**必ず先** | 低（新規ファイル） |
| R1-b | **verify:ui のページ穴埋め**: カレンダー4画面・/sales/companies・顧客360・/search・設定小画面群・機材検索など約12画面を PAGES に追加 | 低（単独先行マージ。以後の各PRは自分の行だけ触る） |
| R1-c | **verify:ui の検査強化**: 768px（iPad縦）を3検査だけで追加＋「数pxに潰れる崩れ」検知（clientWidth 閾値）＋ `--shots` モード（375/768/1280 のスクショ一式）。**/live/display/ の「既定表示」もスクショ基準の照合対象に含める** — 現状 verify:ui はこの画面を検査から除外しており（PAGES に無い・FROZEN_PREFIX は除外用）、check:frozen も APPS 空で no-op のため、機械の見張りがゼロになっている。なお 2026-08-23 の表示レイアウト機能（PR1〜3）以降、この画面の約束は「**レイアウト未設定なら従来の固定描画に完全フォールバック（無変更）・レイアウト適用時に変わるのは仕様**」に更新済み（ユーザー確認済み・docs/design/v4/qsheet-v4-coding/13 参照）。守る対象は**未設定時の既定表示**である | 低 |
| R1-d | **migration 番号の重複禁止チェック**（既存の重複3組は許容リストに載せる） | 低 |

**PR数の目安**: 4。すべて R0 と並行可（リリースに含めなくてよい）。

---

## R2 — ルールの刷新（静穏窓の原子的PRを軸に）

方針: **正は1か所**。数（3アプリ/6アプリ等）を文書に書かない。存在しないものを守るルールは消す。

| PR | 中身 |
|---|---|
| R2-a **CLAUDE.md 原子的PR**（静穏窓で30分以内にマージ。準備期間中これ以外で CLAUDE.md を触らない） | ①「環境分離ポリシー」内の**廃止済み dev ブランチ運用を削除**（「本番リリースはユーザーの明示指示のみ」の1文は必ず残す）②「認証」節を実態（AUTH_MODE: mock/password＋SMS 2FA。Google はカレンダー連携のみ）に書き換え ③凍結の意味・凍結解除中の説明段落を v4-plan.md へ移して脚注1行に ④共有ライブラリ節を削除（shared/CLAUDE.md に一本化）⑤コマンド表から check:frozen・dev:frozen を削除 ⑥「Dockerfile が6アプリ全部ビルド」等の数の記述を修正 ⑦「現在のバージョン」1エントリ=タイトル+2〜3文までのルールを追記し現行3件を圧縮 ⑧リリース手順は branching.md への参照に圧縮 |
| R2-b CONTRIBUTING.md | アプリ表を CLAUDE.md への参照に置換・「check:ui-tokens は人が気をつけて」等の実装済み記述を修正・PRタイトルのアプリ名リストを「ディレクトリ名から選ぶ」に |
| R2-c shared/CLAUDE.md 全面棚卸し | 「凍結4アプリ」前提の記述 20か所以上を削除し、全アプリ v4 系の現状で書き直す |
| R2-d client/CLAUDE.md 減量 | 1,872行 → 300行目安。完了フェーズの経緯ログは docs/ へ。存在しない構成を守る「消してはいけない」警告を現状に合わせる |
| R2-e 死んだ仕組みの削除 | `dev:frozen`・`build:render`/`start:render`・ci.yml の `release/**` トリガー・branching.md の release/v3 手順（「畳んだ」1行に）・check-links.mjs の OTHER_BUNDLE から awards/signage を外す。**`check:frozen`（APPS 空の no-op）は R1-c のスクショ基準が /live/display/ を守り始めてから削除**（先に消すと見た目不変を守る機械検査がゼロになる） |
| R2-f 棚卸し台帳の運用ルール明文化（判断#5） | codex-findings-v4.md 冒頭と branching.md に「並行開発中は行追加のみ・表の再構成は他セッション停止の静穏時のみ」を1行ずつ追記（行追加のみの変更） |

**注意**: R2-a と R3 の README 減量は `collect-changelog.mjs` / `generate-version-history.mjs` が
CLAUDE.md・README の**書式に依存している**ため、必ず `npm run release:notes -- X.Y.Z --dry` を gate に入れる。

**PR数の目安**: 6。

---

## R3 — 文書の大整理

方針: **消さずに書庫へ**。`docs/archive/YYYY/` を新設し、規約は docs/README.md に1枚で書く
（「役目を終えた日付付き文書は archive へ・元の場所に1行ポインタ・追記型の台帳（version-history / codex-findings 等）は対象外」）。

| PR | 中身 |
|---|---|
| R3-a **docs/README.md 新設**（目次） | ①だれでも読める説明書（guide/・R4で作る）②現役の技術文書一覧 ③生成物一覧（生成コマンドと入力。**手で直すな**。version-history.md は「生成物ではなく生成の入力」と明記）④archive/ の4区分を1ページで |
| R3-b archive 第1弾（現役参照なしの低リスク分。git mv のみ・内容変更なし・対象が直近2週間変更ゼロなことを確認して切る） | roadmap.md（末尾のカレンダー重複解消の生きている段取りだけ先に現役計画へ移す）・roadmap-legacy.md・investigations/・design-requests/・requirements/・collab-editing-design.md・architecture/ の v1.0 期6本（box-folder-structure.md のみ残す）・reviews/ の終了6本・release-notes.md（512KB。追記が v2.9.276 で止まり参照もゼロのため「追記型の台帳」ではなく死蔵と判断）・旧世代モック3点（v4-mockup-main.dc.html 等 → mockups/README.md の対応表を更新）。security-review-2026-08-18.md は archive でなく docs/reviews/ へ移動（残課題が現役） |
| R3-c **README.md 減量** | 393KB → 20KB以下。旧版履歴（8割を占める）は version-history.md に既にあるので削除。「主要なドキュメント」「コード健全性」の 2026-04-28 時点の節も削除して docs/README.md への参照に。`collect-changelog.mjs` のマーカー行は維持（--dry を gate） |
| R3-d ia.md 分割 | 現行ルール（入口は1つの判定基準）だけ 5KB に書き直し、v2.9 期の経緯は archive へ |
| R3-e 小物 | mcp-server.md の「制作資料」5か所を「制作技術支援」に・DEPLOY_CONOHA.md → docs/ops/vps-setup.md・version-history.md の裸エントリ1件（v2.9.43）を規約に揃える |

**据え置き（触らない）**: docs/design/ 全体（モックは v4 の正・archive しない）・changelog.d（作業場・2層構造の対象外）・
codex-findings-v4.md（最大の衝突源。**表の再構成は他セッション停止の静穏時のみ**）・handoff-2026-08-20 と native-ui-audit（現役参照が残るため、参照元の消化後に archive）。

**PR数の目安**: 5。

---

## R4 — だれでも読める説明書（guide/ 6本・最後にやる）

技術文書の整理（R2/R3）が終わってから書く（先に書くと参照先が動いて二度手間）。
**書き方の規約**: 各1〜3ページ・1文1情報・カタカナ専門語に必ず言い換えを併記・数字は生成物へのリンクで持たない（手で書くとずれる）。
1本ずつ PR にして、**ユーザーの読み合わせ**を挟む。

| ファイル | 中身 |
|---|---|
| guide/what-is-onair.md | ONAiR とは・アプリ一覧・誰が何に使うか |
| guide/how-changes-ship.md | 直しが本番に届くまで（PR → 検証 dev.gmo-onair.jp → Release 公開 → 本番） |
| guide/environments.md | 本番と検証・壊していい場所 |
| guide/words.md | 用語集（GLS番号・案件・凍結/廃止・ブロックアプリ…） |
| guide/v4-now.md | v4 で何をしていて今どこか（数字は v4-progress.md へのリンク） |
| guide/asking.md | 直してほしいときの頼み方 |

**PR数の目安**: 6。

---

## R5 — 画面の総点検（3層で証跡を残す）

対象: v4 対象3アプリの全ルート＋制作技術支援の外枠＋計時運用画面。
**境界**: /live/display/ は見た目不変の例外。制作技術支援の表本体（CueTable/CueRow/cells）・EditorSidebar・本番系画面は
「開ける・JSエラー0・動く」のみ確認し、見た目基準は当てない（作り直しは通常開発の継続案件）。

| 層 | 中身 | やる人 |
|---|---|---|
| 層1 機械 | R1 で強化した verify:ui（穴埋め済みページ×375/768/1280＋潰れ検知）を毎PRの gate に | Claude |
| 層2 スクショ | `--shots` で3幅一式を取得して目視総点検。**画像はリポジトリに入れず BOX へ**（リポジトリの「ゴミ無し」状態を守る） | Claude 取得 → ユーザーと目視 |
| 層3 実機 | dev.gmo-onair.jp を iPhone/iPad の実機で1巡（WebKit・safe-area・タッチ・**ダークモード**・操作フロー・文言・空状態・エラー時挙動 — 機械では見えないUX側をここで） | **ユーザー**（Claude がチェックリスト用意） |

**スコープ**: この点検は「崩れの一掃」に絞る。作り込みの残件（案件詳細5タブのモバイル化・機材台帳の親子入れ子）は判断#3 に従う（推奨: 次期送り）。

**燃やし方**: shared 部品の修正（PcOnlyPanel の磨き＝バックログ最後の土台残件・潰れ検知が見つける共通部品起因）を**先に小PR**で。
画面個別はエリア単位（sales/finance/calendar/daily/equipment）でブランチを分けて並列化。
未実測と分かっている3点（部屋の空きモバイル・通知グルーピング・金額表記 ¥(1,234)）は層3で必ず見る。
要目視3件（PastePlanDialog の生テーブル・SystemInfoPage・SecurityCardsPage の固定幅）は崩れたものだけ直す。
PR#272 由来の到達不能モバイル実装4ファイルには「現状到達不能」コメントを1行足す（削除しない）。

**やらない**: ダイアログ max-h・非レスポンシブ grid の機械掃討（既定値と意図的設計でカバー済みと確認済み。無駄なPRを作らない）。

**PR数の目安**: 部品先行2＋エリア別（崩れが見つかった分だけ）。

---

## R6 — 技術的な借金の返済（独立PR群・R0 の後ならいつでも）

| PR | 中身 | 時期 |
|---|---|---|
| R6-a npm audit fix | 本番依存の既知脆弱性32件中、fix ありの分を lockfile 更新で解消 → typecheck/test/verify:ui を通す | **単独短命PR**。判断#6（推奨: リリース直後の小リリース） |
| R6-b xlsx → exceljs 置換 | 修正版が出ない High 2件。実利用は excel.ts と kessan-import.service.ts の2ファイルのみ | リリース後の独立PR |
| R6-c 未レビューPRの掃き出しレビュー | レビュー0件マージ約10本の差分をまとめて1回レビューし、結果を codex-findings-v4.md に**行追加のみ**で記録（#336 マージ後に着手） | R0 前が理想・後でも可 |
| R6-d 残骸ブランチ棚卸し | リモート7本（活動中の枝を除く実測値・2026-08-23）の表を作りユーザーに提示（判断#7）。承認後に削除 | いつでも |
| R6-e 未使用 export 整理 | knip / ts-prune を入れて shared/ の約30件を機械確定 → 削除 or @deprecated | リリース後 |
| R6-f 音声サポート旧URL停止 | 段階③の実施判断 → LegacyUrlBanner とトークン無し経路の削除 | ユーザー判断後 |
| R6-g CI に npm audit 定期チェック | `--omit=dev --audit-level=high` | R6-a と同時 |

**据え置き**: techops 改名の Phase 2 以降（進行中の別トラック。この準備に混ぜない）・NotoSansJP のサブセット化（PDF文字化けリスクに見合わない）・
awards コードの削除（「コードは保存」ポリシー通り・隔離済みを確認済み）。

---

## 並行開発と衝突しないための決めごと

いまのマージ速度は **15〜30分に1件**（8/22 実測50件）。以下を全パッケージ共通の規約とする。

1. **短命PR**: 着手直前に main を取り直し、数時間以内にマージ。1日超えたら作り直す
2. **混み合う場所に入らない**: `client-techops/**`・`server/src/contexts/{qsheet,liveops}/**`・`shared/src/client/apps.ts`・server の wiring・生成基準ファイル（file-size-baseline 等）は準備作業の掃除対象から**除外**
3. **ホットスポットは原子的PR**: CLAUDE.md（R2-a の1本のみ）・codex-findings-v4.md（行追加のみ・再構成は静穏時）・リリースPR（切ったら即マージ、他セッションのマージ停止を依頼）
4. **文書移動は git mv のみの専用PR**とし、対象が直近2週間変更ゼロなことを確認してから切る
5. **成果物は新規ファイル**（この docs/release-prep/ 配下）。既存文書へはリンク1行だけ
6. **1セッション=1パッケージ=1ブランチ**。ユーザーがパッケージ ID（R1-a 等）で各セッションに割り当てる

---

## やらないことリスト（シンプルさのために落としたもの）

- ダイアログ max-h / 非レスポンシブ grid の機械掃討（実害なしと確認済み）
- 固定幅 `w-[NNNpx]` の全数修正（PC専用画面の意図的固定が大半）
- release-notes.md の version-history.md への統合（書式が違う。archive で足りる）
- NotoSansJP サブセット化・awards コード削除・techops Phase 2 の同梱
- FormDialog 移行の意図的除外6件（判断済み。「広いシート」設計は次期の設計課題として起票のみ）
- 本番DBの業務データ・uploads/・BOX保持期限の整理（判断#4 が「含む」にならない限り）

---

## 分担の原則（毎回同じ）

本番（VPS・本番DB・Release 公開）へは Claude のセッションから到達できない。
本番に触る手順はすべて「**Claude がチェックリストを用意 → ユーザーが実施 → 結果を docs/release-prep/ か docs/reviews/ に記録**」の形で書く。
**Release の公開はユーザーの明示指示があるときだけ** — この不変条件はどの文書整理でも消さない。
