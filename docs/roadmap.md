# 実装ロードマップ (UI/UX 刷新の残り)

デザイン一式 (6部36章・約60画面) と実装を1画面ずつ突き合わせて引いた順番。
**決まった理由も一緒に書く** — 同じ議論を繰り返さないため。

最終更新: 2026-07-26 / 31章 請求と入金 (v2.9.277) まで完了

---

## 突き合わせで見つかった「デザインにあったのに計画表に無かったもの」

| 章 | 内容 | 扱い |
| --- | --- | --- |
| **14章 案件の入力を減らす** | 22項目 → 起票は3つ。残りは進んだ段で聞く | **順番2に入れた** (毎日いちばん触る入力) |
| 25章 お試し (サンドボックス) | 案件を作らずに道具を試す。実績に混ざらない | 順番7 |
| 36章 検索結果の画面 | ⌘K の続き。見えないものは件数にも出さない | 順番6 |
| Qシート「似た台本から引き継ぐ」 | マイク香盤を前回の台本から写す | 順番6 と一緒に |

これ以外の未実装は計画どおりの残り。**進め方の管理自体は概ね機能していた。**

---

## 順番

| # | 章 | 何ができるようになるか | 大きさ |
| --- | --- | --- | --- |
| — | 30 | **見積をつくる** (料金表・AIの下書き・粗利がその場で出る・PDFはBOX) | 済 v2.9.276 |
| — | 31 | **請求と入金** (締めの日にまとめて出す・入金の確認・検収書) | 済 v2.9.277 |
| 1 | 32 | **合同案件** — 1回のイベントを9社に請求。デザイン到着済 (40a / 40b) | 大 |
| 2 | 14・13 | **案件の入力を減らす** (起票15秒) ＋ **案件のお金・予定タブ** | 大 |
| 3 | 29 | **隔週キープをつくる** — 18ページのうち16ページをAIが埋める (6.5時間 → 45分) | 大 |
| 4 | 21・22 | **香盤表・運営マニュアル** — 当日の動きを1枚に (3〜4時間 → 15分) | 大 |
| 5 | 20 | **リアルタイムCG** — 4つに分かれた送出を1画面に。アワードだけ実装 | 大 |
| 6 | 15・36 | **案件の成果物・書類・カード・検索結果** — 何が揃っていないかが期日順に並ぶ | 中 |
| 7 | 25 | **お試し** — 練習が実績に混ざらない | 小〜中 |
| 8 | 6 | **部品と数字のサイズの共通化** — 画面ごとにバラバラに書けなくする | 中 |

### README の順番から変えた2点

1. **14章 (入力を減らす) を2番目に繰り上げ**
   案件の画面 (2,494行) を **13章のタブ化と同じ工事で触る**ため。別々にやると同じ場所を2回開ける
2. **8番 (部品サイズ) は最後のままだが、新しく作る画面は先に新ルールで作る**
   後から作る画面が増えるほど、まとめて直す量が増えるため。30章で `Money` を新設済み

---

## 決めたこと (2026-07-26)

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 14章の「やめる4項目」 | **グループ区分は やめない**。タグ / 教訓・学び / 案件種類(その他) は削除 | グループ区分 = **費用を分け合う請求**。グループ9社合同の株主総会などで現役 |
| 請求 (31章) と 入力を減らす (14章) の順 | **請求が先** | 締めには期限があり「間に合わなかった」が起きる。入力の重さは重いだけで止まらない |
| 隔週キープの出力 | **PDF のみ** (PowerPoint は作らない) | 投影も配布もPDFで足りる。その分を他の章に回す |
| リハーサルと本番の分離 (24c) | **不要**。デザインから削除依頼 | 分ける概念を持たない |

---

## デザインに出した依頼 (2026-07-26)

`docs/design-requests/2026-07-26-design-change-request.md`

1. **【新規】費用を分け合う請求 (グループ合同案件) の画面** ← 本体
   現状は「**1社が払って費用を9案件で分け合う**」形で、業務の「**9社がそれぞれ払う**」を表現できない。
   請求書を参加社数ぶん出す経路が無い
2. 【修正】14章 27a の「やめる」から グループ区分を外す (+ 27c の中身を埋めてほしい / 件数9 と 中身7 の不一致)
3. 【修正】29章 出力は PDF のみ
4. 【削除】24c リハーサルと本番を分ける
5. 【小】「按分」を画面に出さない (⌘K のラベルは実装側で直す)

**依頼1 は 31章と同じ場所の話**なので、デザインが届き次第31章と合わせて作るのが手戻りが少ない。
遅れる場合は31章を先に作って後から足す (請求の画面を2回触ることになる)。

---

## デザイン修正版の受領と検証 (2026-07-26)

依頼5件はすべて反映された。**ただし依頼していない削除が1件**あった。

| 依頼 | 反映 |
| --- | --- |
| 1 合同案件の画面 | **32章として新設** (40a 2つの「分ける」を区別する / 40b 合同案件をつくる) |
| 2 「やめる」からグループ区分を外す | 済 (やめるは3項目・進んだら聞くは7項目に訂正・**27c の1対1の表も埋まった**) |
| 3 隔週キープは PDF のみ | 済 (PowerPoint / PPTX の記述が 0 件) |
| 4 24c リハーサルと本番を分ける | 削除済 |
| 5 「按分」を画面に出さない | 済 (残る1件は「按分という言葉は画面に出しません」という説明文) |

### 巻き添え削除 (デザインに再依頼が必要)

20章から **24e「テンプレートは『アワード』だけ実装する」** と **24f「データを入れる（アワード）」** が
24c と一緒に消えている (画面IDが 24a/24b/24c/24e/24f/24d → 24a/24b/24d、分量が 51,032 → 28,324 文字)。

- **仕様書 §7.7 には要件が残っている**ので、消えたのは画面だけ
- README の順番表にも「リアルタイムCG のテンプレート化（アワード1枚に畳む）とデータ入力」が残っている
- 20章は順番5なので**当面の作業は止まらない**が、着手前に復元を依頼する

### 32章 合同案件で決まったこと (デザイン側の回答)

| 論点 | 決定 |
| --- | --- |
| 入口 | **お金 ＞ 合同案件**。既存の費用分けは 案件 ＞ お金 ＞「他の案件と分け合う」 |
| 2つの見分け方 | **「請求書が何枚出るか」**。1枚なら費用分け、参加社数ぶんなら合同案件。作成時に最初にこの質問を出す |
| 分け方 | 均等 (既定) / 割合 / 金額を直接。**端数の行き先を画面に出す** (既定は幹事・変更可) |
| 1社だけ直したとき | 合計と総額の一致を画面で示し、**合わないうちは出せない**。差額は「残りの社で割り直す」か「幹事に寄せる」 |
| 出したあとに1社抜けたとき | **取り消しの請求書を1枚出して**残りに配り直す。「取り消し済み」と出して履歴に残す |
| 呼び名 | 「合同案件」。「按分」は出さない |

**31章の請求のしごとは `revenues` を読んでいる**ので、合同案件の請求書もそのまま一覧に並ぶ
(= 32章を作るときに31章を作り直さない)。

### 巻き添え削除の復元 (2026-07-26 第2版で確認済み)

デザイン側が **24e / 24f を復元**した。3回目の受領版で確認:

| 確認項目 | 結果 |
| --- | --- |
| 20章の画面ID | `24a / 24b / 24d / 24e / 24f` (24c は削除のまま = 依頼どおり) |
| 24e | 「テンプレートは『アワード』だけ実装する」バッジ表記は `20e` に振り直し |
| 24f | 「データを入れる（アワード）」バッジ表記は `20f` |
| 依頼1〜5 の反映 | 全部そのまま維持 (32章 40a/40b、27a/27c、PDF のみ、24c 削除、按分の非表示) |
| 意図しない差分 | なし |

**20章は着手できる状態**になった (順番は5のまま)。

---

## コーディングが遅い原因と、直したこと (2026-07-26)

実測して 3 つに切り分けた。

| # | 原因 | 実測 | 直したこと | 効果 |
| --- | --- | --- | --- | --- |
| 1 | **CLAUDE.md が毎ターン全文読み込まれるのに、その 96.7% が過去の履歴だった** | 828,591 文字中 801,027 文字が履歴 | 最新5件だけ残し `docs/version-history.md` に切り出し | **828KB → 58KB (93%減)** |
| 2 | ルートの `npm run build` が 8 ワークスペースを**直列**で回していた | 1分50秒 | `shared` の後を `concurrently -m 4` で並列化 (旧挙動は `build:serial` に残置) | **1分50秒 → 47秒** |
| 3 | 「実 Postgres で N 項目」の**検証の土台を毎回ゼロから書き起こしていた** | 毎回 15〜30 分 + 同じ落とし穴の踏み直し | `scripts/dev-verify/` に up/down を用意し、落とし穴も README に固定 | **冷えた状態から 4 秒** |

### 1 が一番効く理由

CLAUDE.md はコーディング中、**ツールを 1 回叩くたびに全文が読み込まれる**。
履歴を全部ここに置くと、1 行直すだけの作業でも毎回 828KB を運ぶことになる。
さらに読み込み量が増えるほど会話の途中で要約が挟まり、**作業の途中経過が失われてやり直しになる**。
遅さの体感の多くはこれだった。

**今後のルール**: 新しい版を足したら、6件目に押し出された版を `docs/version-history.md` へ移す。
CLAUDE.md に残すのは常に最新5件 (このルールは CLAUDE.md 本文にも書いた)。

### 3 で固定した「毎回踏んでいた落とし穴」

`DATABASE_URL` の 1 本だけを読む / `user_permissions.access_level` (`level` ではない) /
`users.role` は 2 値のみ / `JWT_SECRET` は 32 文字以上 / 開発の認証は `x-user-id` ヘッダー /
Playwright の外部フォント失敗は JS エラーに数えない。

### 手をつけなかったもの (意図的)

- **検証の項目数そのものは減らさない**。v2.9.277 では実際に本番影響のバグを 2 件見つけている
  (経理が財務を開けない / ベルが出ない)。ここを削ると速くはなるが、壊れたまま出る。
- **1章 = 1PR の粒度も変えない**。まとめると差し戻しが効かなくなる。

---

## 32章 合同案件 の実装 (v2.9.279)

順番1が完了。デザイン 40a/40b と仕様書 §7.14 のとおり。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 器 | **新しいテーブル2つ** (`joint_events` / `joint_event_companies`) | 既存の `project_groups` は「1社が払い費用を案件で配る」形。向きが逆なので同じ器に入れると行を見て意味が判別できない |
| `project_groups` の行 | **作らない** | 作ると「費用を分け合う案件」の一覧に合同案件が並び、避けたかった「2つの意味が混ざる」を裏側で作る |
| 見分け方 | 作成時に**「請求書が何枚出るか」**を聞く | 1枚 = 既存の費用分け / 参加社数ぶん = 合同案件 |
| 案件の作成 | 会社を選んだ時点で**社数ぶん自動作成** | 手で9個作らせない (デザインの要件) |
| 初期ステージ | `b_verbal` 口頭決定 → 請求書を出したら `a_won` + GLS 発番 | 最初から受注にすると未確定の金額が数字に乗る。GLS を9本先取りしない |
| 割合の持ち方 | **万分率の整数** | 小数だと9等分で誤差が溜まり合計が総額に1円合わない |
| あまりの1円 | **行き先を画面に出す**。既定は幹事、変更可 | 既存の費用分けは黙って先頭に寄せていて誰に付いたか追えなかった |
| 合計が合わないとき | **サーバーでも止めて1枚も出さない** | 画面だけの制限は必ず抜ける |
| 1社抜けたとき | **取り消しの請求書を1枚出して**残りに配り直す。行は消さない | 出した請求書は消せない |
| 権限 | `budget` (お金) | 入口が「お金 ＞ 合同案件」。31章と同じ担当者 |

### 31章との関係

出した請求書は `revenues` なので、**31章の請求のしごとの入金待ちにそのまま並ぶ**
(実測で9枚並ぶことを確認)。32章のために31章を作り直していない。

### ブラウザ検証で見つかった実バグ3件

1. **経理 (budget だけ) は会社を1社も選べなかった** — 顧客の一覧 `/customers` は `sales` 権限。
   合同案件を作るのは経理なので画面が成立していなかった。選ぶのに要る id と名前だけを返す
   `/joint-events/companies` を足した (連絡先や住所は返さない)。
2. スマホで「合同案件へ戻る」のタップ領域が 36px。
3. **375px で 277px 横にはみ出していた** — `min-w-[720px]` の表は `overflow-x-auto` に
   入れても画面ごと横に伸びる (実測)。スマホでは表を出さず**1社 = 1カードの縦積み**にした。

### まだ無いもの

- **合同案件の見積** (30章の見積画面を「全体で1本」として使う導線)。
  いまは総額を数値で入れる形。デザインは「30章の見積画面をそのまま使う」としているので、
  30章側に「この見積は合同案件のもの」という紐づけを足せば繋がる。

### 次

順番2 = **14章・13章「案件の入力を減らす (22項目 → 起票は3つ) ＋ 案件のお金・予定タブ」**。
27c「進んだ段で聞く」の中身はデザイン側で埋まっている。

---

## 14章 案件の入力を減らす の実装 (v2.9.280)

順番2の前半が完了。デザイン 27a/27b/27c と仕様書 §7.1。
（13章のお金・予定タブは README でも別項目 = 順番4なので分けた）

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 起票で聞く数 | **3つ**（案件名 / お客様 / スタジオを使うか） | 電話を切った直後に分かっているのはこれだけ |
| 起票の入口 | `/sales/projects/new` を差し替え | 既存の入口4箇所を触らずに新画面へ着く |
| 22項目のフォーム | **編集のときだけ**使う | 消すと「進んだ段で聞く」以外の直し方が無くなる |
| 案件分類 | **人に聞かない**（スタジオを使うかから決める） | 2度聞くと片方だけ直された案件ができる |
| GLS番号 | 受注後に採る | 先に採ると消えた案件の番号が空く |
| 聞く項目の定義 | **サーバー1か所**（`stage-ask.service`） | 2か所だとずれ、緩いほうを通って空のまま進む |
| 必須の判定 | 画面とサーバーの**両方** | 画面だけの制限は必ず抜ける |
| すでに入っている項目 | **聞かない** | 同じことを2度打たせない |
| ボードで動かしたとき | 足りないときだけ聞く | 押すだけの段で1手増やさない |

### デザインと違う判断をした1点

**教訓・学びは残した。** 27a では「やめる」だが、ふりかえりの失注分析
（`getLostReasonAnalysis`）が実際に `lessons_learned` を集計しているので、
入力を消すとその画面が空になる。「誰も検索・集計に使っていない」という
27a の前提がこの項目には当てはまらなかった。
かわりに**失注にする瞬間に任意で聞く**形にした（27c の趣旨どおり）。

タグと案件種類(その他) は削除した（サーバー側の絞り込みと MCP の一括更新は残置）。

### ブラウザ検証で見つかった既存バグ1件

**営業が案件フォームの部屋と予約を読めなかった。**
`/studios/locations` と `/studios/bookings` は `studio` 権限で、営業は普通これを持たない。
画面はエラーを出さないので「この案件には部屋が登録されていない」と読めてしまう状態だった。
v2.9.277 のベルと同じ形なので同じ直し方（既定は `studio` 必須のまま、この2本の GET だけ通す）。

### まだ無いもの

- 27a の仕分け（いま必要／進んだら聞く／自動で入る／やめる）を**画面に出すこと**。
  定義は `GET /projects/stage-asks/all` で読めるようにしてあるので、出すだけならすぐできる。
- 13章のお金・予定タブ（順番4）。

### 次

順番3 = **29章 隔週キープをつくる（PDFのみ）**。

---

## 29章 隔週キープをつくる の実装 (v2.9.281)

順番3が完了。デザイン 35a/35b/35c と仕様書 §7.3。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 型 (Ver.2.5) | **変えない**。変えるのは「誰が埋めるか」だけ | 会社のフォーマットなので実装側で変えない |
| ページの構成 | **コードの定数**（`KEEP_PAGES` 16枚） | DB に置くと「型が変わったのか、その回だけ違うのか」が区別できない |
| DB に持つもの | 人が書いた分 と その回に足した議題 だけ | それ以外は既にある |
| 数字 | **写し取らない**。出すときに読む | 写すと元を直しても資料が古いまま |
| ④以降 | **議題があるときだけ足す**。無い回はページを作らない | 毎回の型に混ぜると空のページが残る |
| 「そのほか（自由）」 | **名前を人が書くのを必須にした** | 候補の名前がそのまま議題名になると何の話か分からない |
| ③の6テーマ | テーマと担当は**固定**。人は1行だけ | 毎回作り直さない |
| ①の3行 | **空でも進める**（前の値を消さない） | 相談が無い回もある。書かないという判断も記録 |
| チェックリスト | 落ちている項目**だけ**出す | 全部出すと出したことの意味が消える |
| 確定 | 落ちている項目があっても**止めない** | 止めると会議が始められない |
| 出力 | **PDFだけ** | 依頼3で合意。投影は画面のまま |
| 過去分 | 消さない（削除の経路を作らない） | Ver.2.5 の決まり |

### 既存の資産をそのまま使った

`keep-report.service`（月次PL・イベント報告・議事録）は 29章の「材料」なので作り直していない。
ふりかえり ＞ 隔週キープの既存画面は「材料を確定する場所」として残し、そこから資料づくりへ送る。

### まだ無いもの

- 29c「できあがりのページ」を**画面でそのまま投影**する形（いまは PDF に出す）
- ②案件実施報告への写真の差し込み（BOX の写真は既存のイベント報告側で紐づけ済み）

### 次

順番4 = **13章 案件ワークスペースの「お金」「予定」タブ化**。

---

## 13章 お金・予定タブ の実装 (v2.9.282)

順番4が完了。デザイン 13章 7a と仕様書 §7.12。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| どれをタブにするか | **お金と予定だけ** | §7.12 の判断。案件の中で往復がいちばん多い |
| やり取り・タスク・書類 | あとで（今回は触らない） | §7.12 でそう決めている |
| 現場の道具 | リンクのまま | 同上 |
| 数字の集めかた | **1本の口** (`/projects/:id/money`) | 画面から集めると3〜4往復。数字の出しかたも画面ごとに分かれる |
| 受注前の粗利 | **想定金額を元に見込む**（ラベルは「粗利（見込み）」） | 旧3値は想定金額が無く、受注前は粗利が常にマイナスに見えた |
| 仮の仕入 | 件数を**別に出す** | 分からないと未確定の粗利を信じてしまう |
| 操作の可否 | サーバーが返す | 画面側で条件を書くと条件が2か所になる |
| 予定の出どころ | **案件側の口** | 予約一覧は `studio` 権限で、案件を見るのは営業 |
| 仮押さえの期限 | サーバーが計算（本番日の45日前） | 画面で計算すると案件一覧と違う期限が出る |

### 既存バグ1件（影響範囲が広い）

**`getSummary` が見積を売上に数えていた。** `status` の絞り込みが無く、`status='estimate'`
の見積が「売上（確定）」に混ざっていた。見積はまだ提案で、GLS 発番のとき
`migrateEstimates` が `confirmed` に変える。数えていた間は想定金額としても別に出しているので
**二重計上**になり、案件の粗利・粗利率が過大に出ていた（粗利率が100%を超えることもあり得た）。

同じ数字を読んでいるのは：案件詳細 / 案件一覧 / MCP `get_project` / 隔週キープのイベント報告。
按分ぶん（`revenue_allocations`）にも同じ絞り込みを入れた。
実測で 9,820,000 → 5,000,000、確定に変えれば数え、戻せば元に戻ることを確認。

### まだ無いもの

- やり取り・タスク・書類のタブ化（§7.12 で「あとで」と決めている）

### 次

順番5 = **21・22章 香盤表・運営マニュアル（AIが下書き、人が直す）**。

---

## 21章 香盤表 の実装 (v2.9.283)

順番5の前半が完了。デザイン 21章 28a と仕様書 §7.8。
（22章 運営マニュアルは分量が大きいので次の版に分けた）

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 最初の1枚 | **自動で組む**（白紙を見せない） | Excel を画面に移すだけでは入力が二度手間 |
| 組み直し | 自動の枠だけ差し替え、**手直しは残す** | 区別が無いと「組み直したら消えた」が起きる |
| 枠の持ち方 | **分で持つ**（px は画面が掛ける） | px を持つと目盛りを変えて全部ずれる |
| 人のレーン | 案件メンバーを指す（名前は写さない） | 写すと片方だけ直された表ができる |
| 重なりの判定 | **サーバー** | 画面で判定すると印刷とPDFで違う警告が出る |
| 部屋の重なり | **警告しない** | 部屋では同時に複数の作業が走る |
| レーンの出し入れ | **消さずに隠す** | 消すと置いた枠も消える／2枚作ると食い違う |
| 計時LIVE | 枠の長さ = 尺。冪等 | 当日タイマーを作り直さない |
| 終わりの無い枠 | タイマーにしない（理由を言う） | 尺が決まらない |
| AI | **使っていない** | 21章は既存データからの組み立て |

### 会社方針「AIを使い捨てにしない」について

21章は AI を使わないので、5条件は**この版には適用されない**（`ai-feedback-loop` の
適用範囲どおり）。AI が入るのは 22章の「会場図をAIが下書き」なので、
そこで `ai_outputs` / `ai_corrections` / `ai_outcomes` と `getFeedbackDigest` に載せる。

**22章での5条件の当て方（先に決めておく）**

| 条件 | どう満たすか |
| --- | --- |
| 1 出力の記録 | `ai_outputs`（kind=`venue_layout_draft`・target_table=`manual_parts`・図の要素の全文） |
| 2 修正差分 | 保存のたびにサーバーが `diffByKey` で突合（鍵は要素の**記号**。index だと1つ足すだけで以降全部が「変更」になる） |
| 3 成果 | `ai_outcomes`（PDFに出した / 出さなかった・版数） |
| 4 還流 | 生成前に `getFeedbackDigest('venue_layout_draft')` を読んでプロンプトに載せる |
| 5 レビュー | MCP `get_ai_feedback_digest` と `/review`（既存の経路にそのまま乗る） |

### 検証中に直した実装の穴

1. `studio_bookings.start_time` は TEXT なので `to_char` が使えず日で絞れなかった
2. 部分ユニーク索引を `ON CONFLICT` の的にするには同じ条件を書く必要がある（42P10）

### 次

**22章 運営マニュアル**（部品12種を束ねて1冊にする / 会場図をAIが下書き / 渡す相手ごとの版）。

---

## 22章 運営マニュアル の実装 (v2.9.284)

順番5が完了（21章と合わせて2版に分けた）。デザイン 22章 29a/29b/29c と仕様書 §7.8。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 何を作るか | **PDFを作る機能ではなく、部品を作って束ねる機能** | 部品はそれぞれ別のタイミングで揃う |
| 部品12種の定義 | **コードに置く** | DBだと「種類が変わったのか案件ごとか」が区別できない |
| 足りない部品 | **名前で指摘**。白紙のページは作らない | 何が足りないか分からないと動けない |
| 自動で入る部品 | **元データがあるかで判定** | 人に押させると押し忘れで足りないことになる |
| 渡す相手 | 社内12種／お客様10種／アルバイト3種。**社外版から自動で外す** | 人が毎回消すと必ず1回は消し忘れる |
| 出した版 | **出した瞬間の中身を残す** | 現場で古い紙と混ざったときにどちらが何版か分かる |
| 記号と凡例 | 記号は人・物を指し、**凡例は勝手にできる** | 名前を図に写すとスタッフ表を直しても図が古いまま |
| 位置 | 0〜1000 の相対 | 図の大きさが変わってもずれない |
| 会場図 | **AIが下書き、人が実測と動線を直す** | 下書きが無いと3〜4時間かかる |

### 「AIを使い捨てにしない」5条件の充足

| 条件 | どう満たしたか | 判定 |
| --- | --- | --- |
| 1 出力の記録 | `ai_outputs`（kind=`venue_layout_draft`・target=`manual_layouts`・置いた要素の**全文**） | ○ |
| 2 修正の差分 | 保存のたびにサーバーが `diffByKey`。**鍵は「記号:名前」**。落とされた=`reject`／動かされた=`fix`。二重計上しない | ○ |
| 3 成果 | `ai_outcomes` に `issued`（版数つき）と `dropped`（使わなかった） | ○ |
| 4 還流 | 生成前に `getFeedbackDigest` を読んでプロンプトに載せ、**載せた回は版を分ける**（`v1+digest`） | ○ |
| 5 レビュー | MCP `get_ai_feedback_digest` と `/review` の既存経路に乗る | ○ |

**鍵を index にしない理由**（このスキルが挙げる典型的な失敗）: 記号を1つ足すだけで
以降すべてが「変更された」ことになり、修正率が実態とかけ離れる。

**AI が増やした記号は通さない**（置ける6種だけに絞る）。AIキーが無ければ 503 で
「記号を選んで自分で置くこともできます」と言う。

### まだ無いもの

- PDF のファイル出力（いまは「出した版」として中身を残す。29章と同じ pdfkit の経路に載せられる）
- 会場図の平面図を Box から敷くこと（`floor_plan_box_file_id` は用意済み）
- 部品の中身の編集画面（いまは「できた」の印と自由な中身だけ）

---

## 20章 リアルタイムCG の実装 (v2.9.285)

順番6が完了。デザイン 20章 24a / 24b / 24d / 20e / 20f と仕様書 §7.7。

**24a（送出を1画面にする）はユーザーの明示的な指示で範囲に含めた** — 本番中に押すボタンが
変わるので、着手前に「24a を含めるか」を相談し、「2でいきましょう（含めて全部やる）」の判断を得た。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 送出の画面 | **1つにする**。以前の4つは「準備」に降ろす | どれを開いて本番に臨むのかが人によって違っていた |
| ステップの順番 | **サーバーが正**（画面の定数をやめた） | 2か所に書くと「次はWINNER BAR」と出て別のものが出る |
| 送出の経路 | **既存の `/cue` `/oneshot/cue` をそのまま使う** | 出る絵を1バイトも変えない（本番の絵を作り替えない） |
| 次に出るもの | **必ず先に見せる**（そのあとの流れも） | 分からないまま TAKE を押させない |
| 出したまま | **5分で知らせ、その場で消せる** | 字幕スーパーの出したままは本番中に気づけない事故の型 |
| キー操作 | **一覧を画面に出すが既定では効かない** | v2.9.96 で全廃したのは誤操作で本番に出るため。判断を変えない |
| スマホ | **TAKE を置かない**（24d） | 見張ることと消すことだけ |
| 出力URL | **選ぶだけ**にして URL は組み立てさせない（24b） | 1文字違いは「何も出ない」形で失敗し原因が分からない |
| テンプレート | **一覧には出すが作れないものは言う**（20e） | 隠すと「うちの演出は作れないのか」が分からず毎回聞かれる |
| データの入れ方 | **貼る / 落とす / AIに整えさせる の3つ**（20f） | 届き方が毎回ちがう。メール本文の表は受け口が無く手打ちだった |
| 取り込みの処理 | **1本のまま**（貼った文字はサーバーで xlsx にして既存の取り込みへ） | 2本あると「ファイルなら入るのに貼ると入らない」食い違いが出る |
| 列の当て方の画面 | **作り直さない**（既存のダイアログに貼る口を足した） | 貼るとファイルで当て方が違うと本番中に原因が分からない |
| AI の役割 | **表の形に直すところまで**。取り込むかは人が決める | 中身を作らせない（書かれていない人を足させない） |

### 「AIを使い捨てにしない」5条件の充足

| 条件 | どう満たしたか | 判定 |
| --- | --- | --- |
| 1 出力の記録 | `ai_outputs`（kind=`awards_intake_tidy`・target=`awards_events`・整えた表の**全文**） | ○ |
| 2 修正の差分 | 取り込みのたびにサーバーが `diffByKey`。**鍵は「部門::名前」**。落とされた=`reject`／足された=`enrich`／直された=`fix`。二重計上しない | ○ |
| 3 成果 | `ai_outcomes` に `imported`（入った件数つき）と `dropped`（使わなかった） | ○ |
| 4 還流 | 生成前に `getFeedbackDigest('awards_intake_tidy')` を読んでプロンプトに載せ、**載せた回は版を分ける**（`v1+digest`） | ○ |
| 5 レビュー | MCP `get_ai_feedback_digest` と `/review` の既存経路に乗る | ○ |

**鍵を index にしない理由**: 1行足すだけで以降すべてが「変更された」ことになり、
修正率が実態とかけ離れる。

AIキーが無ければ 503 で「列を自分で当てて取り込むこともできます」と言う（技術用語を出さない）。

### まだ無いもの

- 送出画面からの部門の切り替え（いまは準備の画面で選ぶ。本番中に部門を飛ばす操作は
  誤操作の的が増えるので、次の版で「次の部門へ」1つだけにするか検討する）
- アワード以外のテンプレート（20e は「準備中です」と言うところまで）
- 閲覧だけの人の書き込み制限（awards の権限は今も presence だけを見ている。
  ここを変えると awards の全ルートの意味が変わるので、UI の章では触らなかった）

---

## 15章 案件の書類 / 36章 検索結果 の実装 (v2.9.286)

順番7が完了。デザイン 15章（案件の成果物・書類・カード）と 36章（検索結果の画面）。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 12種の定義 | **コードに置く** | DBだと「種類が変わったのか案件ごとか」が区別できない（22章と同じ） |
| そろったかの判定 | **元データを見る**。人に押させない | 押し忘れが「足りない」として残り、押しただけで実物が無い状態も作れる |
| 並び | **期日順**。過ぎたものは上 | 種類順に並べても次に何をすればよいか分からない |
| まだ要らないもの | **「足りない」に数えず「あとで」に分ける** | ヨミで請求書が無いのは当たり前。数えると印の意味が消える |
| 期日 | **サーバーが計算** | 画面で計算すると案件一覧と食い違う（13章と同じ） |
| 期日の決め方 | 申込書=本番日14日前 / 現場のもの=7日前 / 仮押さえ=45日前 / 入金=支払期日 / カード=返却予定日 | 逆算の根拠を画面にも出す |
| カードの案件紐づけ | **列を1つ足す**（migration 152・任意） | 貸出は「誰に」だけで、どの案件かが分からなかった |
| 検索の権限 | **見えない種類は件数にも出さない** | 「案件 0件」は「無い」と読まれるが実際は「見せてもらえない」 |
| 権限で外した種類 | **名前だけ言う**（件数は出さない） | 黙ると「壊れているのか権限が無いのか」が分からない |
| 検索のSQL | **1か所**。⌘K と検索結果で違うのは件数の上限だけ | 2か所に書くと「⌘K には出るのに検索結果に出ない」が起きる |

### 見つけた既存の穴（36章の本体）

`GET /search` が **ログインしているかだけを見て権限を見ていなかった**。
案件を見る権限が無い人の ⌘K にも

- 案件名と GLS 番号
- お客様の名前
- 仕入先の名前

が出ていた。開くと 403 で止まるが、**名前はもう見えている**。
顧客名・案件名は「誰と何をしているか」なので、それ自体が見せてよい情報ではない。
`meetsPermissionLevel`（HTTP の `requirePermission` と同じ関数）で絞る形にした。

### 検証で見つけて直した実装の穴

`security_card_lendings.due_on` は **DATE 型なので pg が Date オブジェクトで返す**。
`String(v).slice(0,10)` では `Wed Sep 02` になり、画面に
**「Wed Sep 02（9164日 過ぎています）」**と出ていた。
`dateStr()` で Date と TEXT の両方を `YYYY-MM-DD` にそろえた。

### まだ無いもの

- 案件をまたいだ「そろっていないもの」の一覧（いまは案件ごと。ふりかえり・今日の画面に足せる）
- 成果物のファイルそのもの（BOX のリンク。いまは「あるか」だけを見ている）
- 検索の並び替え・期間の絞り込み（いまは種類で絞るところまで）

---

## 25章 お試し の実装 (v2.9.287)

順番8が完了。デザイン 25章（お試し・サンドボックス）。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 練習用の画面 | **作らない**。道具は本物と同じものに入る | 別に作ると練習で通った道と本番で通る道が違い、練習の意味が無くなる |
| 印の向き | `is_sandbox` を**練習だけが持つ**（既定 FALSE） | 逆にすると印を付け忘れた本物が数字から消える。この向きなら「多い方に間違う」ので気づける |
| 数字 | **数える側で外す**（列を1つ足すだけ） | 練習用のDB・テーブルにすると道具を2本書くことになる |
| 外に出るもの | **書く側で止める**（予約・タイマー・GLS・BOX・請求書） | 読む側を1か所忘れると黙って混ざるが、書く側なら「出てしまった」とその場で見える |
| 本物への昇格 | **できない**。画面にもそう書く | 練習の金額・日程が実績に入る道を作らない |
| 片づけ | **人が押す**。自動で消さない | 「作りかけの練習が朝には消えていた」が起きる |
| 予約 | お試しでは**作らせない** | カレンダーに出ると他の人には本物と区別が付かない |

### 「混ざらない」をどう確かめたか

**数字を返す口を20個ぜんぶ叩いて記録 → お試しに 9,999,999 円を入れる → もう一度ぜんぶ叩く
→ 1バイトでも違えば漏れ**、という形で機械的に確かめた。
目で40か所を追うより確実で、あとで数える口が増えても同じ手で確かめられる。

外した場所（約20か所 / 10ファイル）:
案件一覧・件数・タブ・CSV / ダッシュボード（KPI・気づき・最近の案件・売上ボード・月次グラフ・
パイプライン・今週の予定・受信箱・ベル）/ 売上・仕入の一覧と CSV と Excel と MCP
（`list-query.ts` の2つのビルダー1か所で全部に効く）/ 月次サマリー（＝月次PL・予実・MCP）/
ヨミのファネル・失注理由・営業成績 / 週次の数字 / 隔週キープの事実 / 朝の1通 /
さがす（⌘K・検索結果）/ お客様一覧と顧客360 / 請求のしごと / タスクの案件選び /
GLSの選択肢 / 見積の「似た案件」/ AIの成果。

**わざと外していない場所**: 請求書番号の連番カウンタ（全行を数えないと番号が衝突する）、
決算取込の重複判定（全行を見ないと重複を見つけられない）、データビューア（生の表を見る道具）。

### 検証で見つけて直した既存の不具合

**営業成績の画面が 500 で開けなかった**。`SELECT p.assigned_to, u.name … GROUP BY p.assigned_to`
は Postgres では通らない（`column "u.name" must appear in the GROUP BY clause`）。
`u.name` を GROUP BY に足した（2か所）。お試しの検証で全部の口を叩いたときに見つかった。

**ルートの並びの罠**: `/projects/sandbox` は1セグメントなので `/:id` より後ろに置くと
id='sandbox' として食われて 404 になる（`/docs/format` のような2セグメントは食われない）。

### まだ無いもの

- Qシート・リアルタイムCG の練習（別アプリ側にも印を渡す必要がある）
- お試しの中で予約を試すこと（カレンダーに出るので今は作らせない）
- 古いお試しの自動片づけ（いまは日数を出して人が押す）

---

## 6章 部品と数字のサイズの共通化 の実装 (v2.9.288)

順番9（最後）が完了。デザイン 6章。**これでデザイン一式の主要な章はすべて実装済み。**

### 実装を数えたら、こうなっていた

| 見つかったもの | 数 |
| --- | --- |
| `¥{n.toLocaleString()}` を手で書いていた箇所 | 14 |
| 万円に丸める関数（別の名前・別の丸め方） | 4本 |
| 大きい数字に `text-*` を直書き | 2 |
| ページ見出しの重複 | 38か所（書き方が3種類） |

**丸め方が2種類あったのが実害**: `Math.round(v/10000)` と `(v/10000).toFixed(0)` は
**負の数で結果が違う**（`Math.round(-0.5) = -0` / `(-0.5).toFixed(0) = "-1"`）。
取り消しの請求書などマイナスが出る画面があるので、画面によって数字が変わり得た。

**見出しの書き方が3種類あった**: `text-xl lg:text-2xl font-bold` / `text-xl font-bold sm:text-2xl` /
`heading-page text-xl lg:text-2xl`。しかも **`/finance/import` は枠の見出しが `sm:`、
中身の見出しが `lg:`** だったので、**同じページの中で2つの段が混ざっていた**
（幅 640〜1024px では枠と中身で見出しの大きさが違って見える）。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 部品の置き場所 | `shared/src/client/ui/numbers.tsx` に集約 | 7アプリから同じものを使う |
| 数字のサイズ | **4段だけ**（lg / md / sm / inline）。`KpiCard` と同じ値 | 片方だけ変えると同じ数字が画面で違う大きさになる |
| 万円の丸め | `manYen()` 1本 | 負の数で結果が変わらないよう「絶対値で丸めて符号を戻す」形にそろえた |
| ページ見出し | `<PageTitle>` | 38か所・3種類の書き方を1つにする |
| 守り方 | **`npm run lint` の先頭で検査を走らせて止める** | 部品を用意しても手で書けるままだと必ず増える |
| 逃げ道 | 行に `ui-tokens-ok` と書けば見逃す | 例外を作れないと、例外が必要な場面で検査そのものを外される |

### 検査で止めること（理由も一緒に出す）

1. `¥` を数字と一緒に手で書く → 桁が揃わない・丸め方が揃わない
2. 万円に丸める式をその場に書く → 負の数で結果が変わる
3. 大きい数字に `text-*` を直書き → 同じ数字が画面で違う大きさになる
4. ページ見出しの指定を直書き → 見出しの大きさがばらつく

**対象から外したもの**: 部品の実装そのもの（`shared/src/client/ui/`）、各アプリの
再エクスポート層（`components/ui/`）、放送に出るCG（`/cg/`・出力ページ。画面設計の対象外）。

### 検証

- `check:ui-tokens` は **437ファイルで違反0**（着手前は29件、網を広げたあと38件）
- Playwright 1440px で16ページ・375px で6ページ、あわせて **72項目**。
  **見出しの大きさが実測で1つにそろっている**（PC 24px 太字 / スマホ 20px 太字）
- 初回の実行で2ページが FAIL になったが**どちらも検証スクリプト側の誤り**
  （存在しないパスを見ていた / vite の HMR の切断を JS エラーに数えていた）。
  直して再確認し、画面側の不具合ではないことを確かめた

### まだ無いもの

- 色と間隔の共通化（今回は数字と見出しだけ。色は DADS トークンに乗っているので次の段）
- 表の列幅の段（`MoneyCell` の `width` は7段あるが、選ばせているだけで検査していない）

### 全体

デザイン一式（6部36章）の実装は **順番1〜9すべて完了**。
残りは各章の「まだ無いもの」に書いた積み残し（PDFのファイル出力・Qシートの練習など）。

---

## v2.9.289 — 全体コードレビュー 1回目（本番中に出る不具合 + セキュリティ + デプロイ）

デザイン一式の実装が一段落したので、依頼された6点（①全体レビュー ②肥大化の整理
③セキュリティ ④デプロイが遅い ⑤リアルタイムCGとQシートの挙動・バグ ⑥新しいUIUXに
追いついていない箇所）に着手した。**1回で全部やらず、効果が大きく「直った」を
実測で示せるものから**進めている。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| どこから直すか | **本番中に出る不具合 → セキュリティ → デプロイ** の順 | ①は放送が止まる形で出る。②③は「見えないまま損をしている」形 |
| 接続の持ち方 | `shared/src/client/socketPool.ts` **1本に集約**（鍵ごとに1本・使用数を数え0で切る・**つながっているかは見ない**） | 同じ作りが4か所にあり**4つとも同じ2つの誤り**を持っていた。4か所に書くとまた1か所だけ直し忘れる |
| 受け口の張り方 | `id` につき**1組だけ**張り、中身は ref で最新の関数を呼ぶ | 依存に状態を入れると再生・一時停止のたびに張り直し、外さないので受け口が積み上がる |
| xlsx | **ライブラリは差し替えず、境界で止める** | npm に修正版が無い。差し替えは取り込み3系統の実データ突き合わせが必要で、別の版でやる |
| xlsx の守り方 | `safeReadWorkbook()` 1本に集約し、**`XLSX.read` の直接使用を lint で禁止** | 1か所でも直接読むと境界を通らない |
| 型チェック | **イメージのビルドから出して CI で並走**。`deploy-*` は `needs: [build, typecheck]` | `noEmit` なので成果物は変わらないのに約50秒が待ち時間に乗っていた。**型エラーでデプロイは止める** |
| 残る脆弱性 | **README「既知の課題」に隠さず書く**（開発用 / 実行環境で発現しない / 大版更新が必要 に分類） | 「14件残っている」だけだと判断できない |

### 見つけた不具合（すべて実測で確認）

| # | 不具合 | どう見えるか | 直し方 |
| --- | --- | --- | --- |
| 1 | 接続を「つながっていなければ捨てて作り直す」形。**送信関数が毎回それを呼ぶ** | 回線が一瞬揺れているときに TAKE / 「次へ」を押すと**受け口ごと接続を捨てる**。出ている絵は動いているのに**画面の表示だけ止まる** | `socketPool` に集約。つながっているかは見ない |
| 2 | 片付けで**無条件に切る**（共有している接続なのに） | **片方の画面を閉じるともう片方の同期が黙って止まる** | 使用数を数え、0 のときだけ切る |
| 3 | Qシート OnAir の socket effect の依存に `running`/`next`/`prev` 等。**受け口を `off` していない** | **ランダウンで「次へ」を1回押すとキューが複数進む**／放送中に「切断されました」が出る | 受け口は1組・ref で最新を呼ぶ・**全部 `off` する** |
| 4 | `cue:update` の依存に経過秒（100ms ごとに変わる） | **毎秒10回**送っていた（受ける側も毎秒10回描き直す） | 変化時は即座に、経過だけは**毎秒1回** |
| 5 | `cues` を毎レンダー組み直し | **毎秒10回キュー全体を組み直して effect を走らせていた** | `useMemo` |
| 6 | `setCue` が常に新しい object を作る | 再接続時の**同内容の同期**で出ている絵が描き直され、**演出が跳ねる** | 同値なら同じものを返す |
| 7 | `useAwardsStore()` を引数なしで購読（＝ストア全体） | 票数（3秒ごと）が変わるたびに関係ない画面まで描き直す | 選択購読に |
| 8 | `off('cue:sync')` （ハンドラ指定なし） | **他の画面が張った受け口まで消す** | ハンドラを指定して外す |
| 9 | Qシートの vite proxy が `3000`（他は `3001`）＋ **`/socket.io` の中継が無い** | **ローカルでリアルタイム同期が一度も動かない**。本番の nginx では通るので**手元で再現できないまま本番だけ壊れる** | ポートを揃え、中継を足した |
| 10 | 機材管理と Qシートが**同じ 5174** | `npm run dev` は7本同時なので必ず片方が落ちる（ドキュメントの表は 5175） | 5175 に |
| 11 | `client-daily` の 5180 が許可オリジンに無い | Socket.IO のハンドシェイクが **400 で落ちる** | ポート一覧から生成する形に |
| 12 | ZIP 一括アップロードに展開の上限が無い | 10MB の ZIP から数GB を作れる = **1リクエストでメモリが尽きる** | 1000件 / 展開後300MB |
| 13 | OnAir の操作ボタンに名前が無い（「停止」以外） | **本番中に押すボタンが読み上げでも検証でも識別できない**。±1分は 36px | 名前を付け、44px に |

### 検証

- ビルド 9 ワークスペース通過 / `eslint` 0 errors・**77 warnings**（着手前 78）/ 禁止パターン違反 0（677ファイル）
- **xlsx の境界で 17項目** — 中身での判定・パーサに届く前に日本語で弾く・**prototype 汚染を検知して消して拒否**・正常なファイルは今まで通り
- **実サーバー + 実 Postgres + ブラウザ2ページで Qシート 17項目** —
  接続1本 / 5回操作しても増えない / **cue:update が5秒で5回**（修正前 **50回**）/
  **「次へ」1回でキューが1つだけ進む** / 名前で押せる6つ / 44px 4つ / JSエラー0件
- **同 リアルタイムCG 14項目** — `/awards` 1本 / **`/quiz` も1本**（修正前 **2本**）/
  TAKE 4回で増えない / **出力ページを閉じても送出側の同期が止まらない** / JSエラー0件
- **修正前の実測も取った**（同じ手順を修正前のコードで走らせた）: 接続 **4本** / cue:update **50回**

### 次（このレビューの残り）

- `alert()` / `confirm()` **129か所**を新しい通知・確認の部品へ
- 共通の状態表示部品の適用（**118ページ中46ページ**）
- **1000行超のファイル13本**の分割（最大 `ProjectFormPage.tsx` 2,513行）。
  **バグ修正と同じ版に混ぜない**（差分が大きくレビュー不能になる）
- `react-router` v7 / `box-node-sdk` 4 / `eslint` 10 への大版更新（**それぞれ独立した版で**）

---

## v2.9.290 — ロードマップ1つ目: 通知と確認を部品に寄せる

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 確認の書き方 | `await confirmAction({...})` が **true / false を返す**（`window.confirm()` と同じ形） | 呼ぶ側は1行の置き換えで済む。**分岐の形を書き換えないので条件を反転させる事故が出ない** |
| 取り消せない操作 | `tone: 'danger'` で赤 + **最初から「やめる」にフォーカス** + 外側を押しても閉じない | 開いた瞬間に Enter を押しても実行されない（勢いで通すのを防ぐ） |
| メッセージ | **title と description に割る** | 「削除しますか？」だけでは一緒に消えるものを伝えられない |
| `<ConfirmHost />` が無い画面 | **`confirmAction` は false を返す**（実行しない） | 逆向き（置き忘れたら実行する）にすると、置き忘れが一番危ない事故になる |
| 置き場所 | **アプリのルート直下**（AppShell の中ではない） | OnAir・ランダウン・プロンプターは**AppShell を通らない全画面ページ** |
| 二重に置いたとき | **1つしか出ない**（部品側で防ぐ） | 置き場所を足しても事故らないようにする |
| `alert()` の振り分け | 中身で `notifyError` / `notifySuccess` / `notifyInfo`。**変数の4か所は目で見る** | `catch` の中で「失敗しました」を組んでいたので機械判定では info になっていた |
| 守り方 | `browser-dialog` ルールを `npm run lint` に追加 | 部品を作るだけでは必ず戻る |
| 進め方 | 置き換えは機械的に。**`await` の付け忘れは型チェックに列挙させる** | 85か所を目で追うより確実（`await` を非 async に置くと TS が必ず落ちる） |

### つまずいたところ（記録として残す）

1. **置き場所を間違えていた。** 最初は共通の AppShell に1組置いた。だが Qシートの
   OnAir・ランダウン・プロンプターは全画面ページで AppShell を通らないので、
   **本番中に「停止してリセット」を押しても確認が出せず、黙って何も起きない**状態だった
   （`confirmAction` が false を返す）。ブラウザ検証で気づいた。**型チェックでは絶対に見つからない**種類の不具合。
2. **自分の置換スクリプトに誤りが3つあった。** どれも型チェックとブラウザ検証で捕まえた:
   メッセージ分割で ASCII の `?` も見ていたため **`cat?.name` のオプショナルチェーンで
   テンプレートリテラルを割って構文を壊した** / import の追加位置が**複数行 import の途中**に入った /
   `import` の有無を見る正規表現が **`importance` という変数に当たった**。
3. **v2.9.289 で自分が入れた不具合を1件見つけた。** ルートの `typecheck` が `concurrently` の
   **ラベル順とコマンド順で食い違っていた**ので、ログの見出しが別アプリのファイルを指していた
   （`[equipment]` の枠で Qシートを走らせていた）。**この食い違いのせいで、最初の自動修正が
   別アプリのファイルを書き換えて壊した** — CI のログを読む人も同じように誤解する。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0（683ファイル）
- **残った `alert(` / `confirm(` は 0 か所**（置き換え 129 = 確認 89 + 通知 40）
- **ブラウザで 61項目** — 確認ダイアログ17／お知らせ帯18（PC・スマホ375px）／OnAir 全画面ページ9／
  リアルタイムCG・Qシートの同期17（v2.9.289 と同じ結果 = 壊していない）
- 通した中身: **ブラウザ標準ダイアログが1件も出ない** / 「やめる」で**本当に消えない** /
  「削除する」で**本当に消える** / Esc で閉じても消えない / **4秒たっても帯が消えない（トーストではない）** /
  44px / 横はみ出し 0px / JSエラー0件

### 次（ロードマップ2つ目）

読み込み中・空・エラーの表示を部品に寄せる。**117ページ中44ページ**しか共通部品
（`SkeletonCard` / `EmptyState` / `ErrorPanel` / `NoPermissionPanel`）を使っておらず、
「読み込み中」の直書きが29か所、「〜がありません」の直書きが85か所ある。

---

## v2.9.291 — ロードマップ2つ目: 読み込み中・空・エラー・権限なしを部品に寄せる

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| `EmptyState` が2つあった | **`states/` に一本化**し、`dashboard/` の互換ラッパーを削除 | dashboard 側の既定タイトルが**「データがありません」**（§2.4 が禁じる言い方）。**全24ファイルが実際には title を渡していた**ことを機械的に確かめてから消したので表示は変わらない |
| 読み込み中 | ログイン判定中は `<Delayed>` で包むだけ／画面の中身は `<Delayed><SkeletonRows /></Delayed>` | ログイン前は**骨格が分からない**ので骨組みを出せない。画面の中身は骨格を出したまま中身だけ骨組みにする |
| 空 | 一覧の代わりに**縦の場所を取るブロック**だけ `EmptyState` に | **全部を置き換えない。** 「待たせているものはありません」は良い知らせで空の状態ではないし、検索欄の注記や高さ40の選択リストに枠付きパネルを入れると読みにくくなる |
| 権限なし | `NoPermissionPanel`（**必要な権限を名前で出す**） | 「アクセス権限がありません」だけでは誰に何を頼めばよいか分からない |
| エラー | `ErrorPanel`（原因1文 + 次の一手1文） | **HTTPコード・debug の JSON を画面に出さない**（console に留める・§2.5） |
| 検査の広さ | `empty-by-hand` は **`text-center` を持つブロックに絞る** | 最初に書いたルールは広すぎて、**インラインで正しい12か所まで拾った**。逃げ道を12個作るのは「ルールが間違っている」合図 |

### 数えたこと

| | 着手前 | 着手後 |
| --- | --- | --- |
| 共通部品を使うページ | 117中 **44** | 117中 **63** |
| `EmptyState` の実装 | **2つ**（既定タイトルが違う） | **1つ** |
| ページ全体の読み込み分岐 | 39か所（うち27か所がその場書きのスピナー） | `Delayed` 36か所 / `SkeletonRows` 22 / `SkeletonCard` 17 |
| 権限なしの自作表示 | 5か所 | 0（`NoPermissionPanel` 14か所） |
| 画面に出る debug の JSON | 1か所（機材一覧） | 0（console へ） |

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0（683ファイル）
- **ブラウザ16項目** — 空（何が無いか / 次にやること / **「データがありません」で終わっていない**）、
  権限なし（**白紙でなく権限名「機材管理」が出る** / **403・Forbidden を画面に出さない**）、
  読み込み（**300ms で返る取得では骨組みを出さない** / **3秒かかる取得では出る**）、
  エラー（何をしようとして失敗したか / 原因が日本語 / 次の一手 / **HTTPコードを出さない** / やり直せる）
- **v2.9.289・290 の検証 75項目も再実行して全通過**（壊していないことの確認）
- **ルールが本当に止めることを確かめた**（わざと違反を書いて2つとも落ちる）

### つまずいたところ（検証スクリプト側。実装の不具合ではない）

1. ページの URL と API のパスが同じ形（`/equipment/cables`）だったため、
   **モックがページ自体に当たって画面が JSON になった**
2. Playwright のグロブ (`**/…**`) が当たらず、正規表現に変えた
3. **5xx は最大2回リトライする設定**（1秒・2秒）なので、待ち時間が足りずエラー表示を見逃していた

### 次（ロードマップ3つ目）

1000行を超えるファイル13本の分割。最大 `ProjectFormPage.tsx` 2,513行 /
`BusinessProjectView.tsx` 2,045行 / `EquipmentListPage.tsx` 2,018行。
**1版1〜2ファイル**に分ける（挙動を変えない作業なので、差分が大きいとレビューできなくなる）。

---

## v2.9.292 — ロードマップ3つ目: 1000行超のファイル分割 (1本目・2本目)

**挙動を1つも変えない作業**なので、1版で1〜2ファイルに絞る。差分が大きいとレビューできない。

### やったこと

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `ProjectFormPage.tsx` | 2,513 | **1,925** | 型と定数 (`types.ts` 78) / ジャーニー (`JourneyPanel.tsx` 135) / やり取りの記録 (`ActivityQuickAdd.tsx` 93) / **ダイアログ7種** (`dialogs.tsx` 520) |
| `EquipmentListPage.tsx` | 2,026 | **1,880** | 列の定義 (`columns.ts` 24) / **印刷まわり一式** (`printing.tsx` 181) |

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 何のために分けるか | **探せるようにするため**（行数を減らすためではない） | 2,513行の1ファイルだと「この定数はどこで使われているか」を追うのにファイル内検索しか手が無く、**変更の影響範囲が読めない** |
| ダイアログ7種 | **1ファイルにまとめる** | どれも「開く・閉じる + 1操作」の小さな部品で、単独では開かない（必ず案件フォームから開く）。7ファイルに割ると import が7行増えるだけ |
| どこで切るか | **state を共有していないところ** | 印刷は列を選ぶ・題を付ける・紙に出すだけで、一覧の絞り込みや編集と state を共有していない = 切れ目がきれい |
| state の置き場所 | **親に残す** | 案件フォームのスタジオ日程は約30個の state を**保存処理が読んでいる**。子に移すと挙動が変わる |
| 関数の扱い | **そのまま親に残して props で渡す** | `handlePrint` は `print-landscape` クラスを付けて 150ms 待つ。書き直すと印刷の向きが変わる |

### 検証で分かったこと（直していない）

「やり取りを記録」というボタンが **2つある**。案件の中で書ける方と、警告バナーから
営業活動ページへ飛ぶ方。後者は `ActivityQuickAdd` のコメント「営業活動ページに飛ばさない —
飛ばすと戻ってこないので」と食い違う。**既存の挙動なので今回は直していない**
（挙動を変えない版なので、直すなら別の版で）。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ21項目** — 案件フォーム（ジャーニーの6段 / 次の一手が1つ /
  **ステージ変更の確認に「見積提案」から「口頭決定」と出る** / 失注ダイアログ /
  **やり取りをその場で書ける = 画面を離れない**）、機材台帳（**印刷設定が開く** / 列 / 用紙方向 / 件数 / 印刷実行）
- **v2.9.290・291 の検証51項目も再実行して全通過**

### 残り (11本)

`BusinessProjectView.tsx` 2,047 / `SchedulePage.tsx` 1,515 / `RackLayoutPage.tsx` 1,505 /
`manual/content.tsx` 1,455 / `RevenueListPage.tsx` 1,438 / `EventEditorPage.tsx` 1,294 /
`ProjectGroupListPage.tsx` 1,177 / `project.service.ts` 1,130 / `TodayPage.tsx` 1,117 /
`EditorPage.tsx` (Qシート) 1,094 / `EquipmentDetailPage.tsx` 1,093 / `CueTable.tsx` 1,078。

今回の2本も **1,880〜1,925行**あるので続きがある
（案件フォームのスタジオ日程、機材台帳の登録・一括編集ダイアログ）。

---

## v2.9.293 — ロードマップ3つ目の続き: 分割 3本目 (2,000行超がゼロに)

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `BusinessProjectView.tsx` | 2,047 | **1,520** | 型 (`types.ts` 59) / 月次請求 (`MonthlyBilling.tsx` 181) / 売上一覧 (`RevenueList.tsx` 347) / 仕入一覧 (`PurchaseList.tsx` 102) |

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| どこを切るか | **「読むだけの並び」を選ぶ** | この画面の最大の塊はダイアログ2つ（売上明細400行・仕入198行）だが、**中で使う state が 25〜40 個**ある。props に開くと「渡せるが意味が違う」事故が起きやすい。一覧の並びは表示 + 数個のハンドラなので props 7〜18 個で収まる |
| props の型 | **親の宣言をそのまま写す** | 推測で書いたら **5件が型エラーで止まった**（後述）。各ファイルの冒頭にこの注意を書いた |
| 条件分岐 | **位置だけ動かす** | 月次請求は元が `{monthlyMode && (…)}`。中身を部品にして `{monthlyMode && <MonthlyBilling … />}` にした（意味は同じ） |
| 未使用の import | **型チェック (TS6133) に列挙させる** | 目で探すより確実。7件を機械的に消し、残り1件だけ手で消した |

### 型を推測して間違えたところ（型チェックが止めてくれた）

- `monthEpisodes` は `billing_key` ではなく **`episode_code`** を持つ
- `handleDownloadPdf` は**引数が2つ**で、種類（見積書 / 請求書 / **検収書**）を取る
- `handleDeleteMonth` は **Promise を返す**（`async`）
- `openNewForMonth` は `(epId, monthTitle, recMonth?)` の**3引数**

**「渡せてしまうが中身が違う」**形なので、型が無ければ気づけない。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ10項目** — 案件名 / **月次管理と「GLS-B900-YYMM の請求単位」の説明** /
  売上の見出し / **登録した明細の 1,200,000 円が出る** / 仕入の見出しと追加ボタン /
  **仕入ダイアログが開く（親に残した部分）** / 横はみ出し 0px / JSエラー0件
- **v2.9.291・292 の検証54項目も再実行して全通過**

### 残り (14本・2,000行超はゼロ)

`ProjectFormPage.tsx` 1,925 / `EquipmentListPage.tsx` 1,880 / `BusinessProjectView.tsx` 1,520 /
`SchedulePage.tsx` 1,515 / `RackLayoutPage.tsx` 1,505 / `manual/content.tsx` 1,455 /
`RevenueListPage.tsx` 1,438 / `EventEditorPage.tsx` 1,294 / `ProjectGroupListPage.tsx` 1,177 /
`project.service.ts` 1,130 / `TodayPage.tsx` 1,117 / `EditorPage.tsx` (Qシート) 1,094 /
`EquipmentDetailPage.tsx` 1,093 / `CueTable.tsx` 1,078。

`manual/content.tsx` は**文章のかたまり**（画面の使い方の本文）なので、分割の意味が薄い。
次は `SchedulePage.tsx` / `RackLayoutPage.tsx` のような**画面**から進める。

---

## v2.9.294 — 分割 4本目・5本目 (初めて1000行を切った)

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `RackLayoutPage.tsx` | 1,505 | **714** | 定数と型 (`config.ts` 49) / ダイアログ2つ (`dialogs.tsx` 156) / ラック図の絵 (`RackDisplay.tsx` 412) / 印刷用 (`print.tsx` 205) |
| `SchedulePage.tsx` | 1,515 | **1,234** | 型・定数・道具 (`types.ts` 145) / カレンダー連携と種別ピッカー (`dialogs.tsx` 148) |

### 分かったこと: 「切りやすいファイル」と「切りにくいファイル」がある

`RackLayoutPage` は **もともと関数が9つに分かれて並んでいた**
(`RackDisplay` / `DefaultCellContent` / `ConfiguredCellContent` / `UnitBadge` /
`ItemTooltip` / `PrintRackArea` / `PrintRackDisplay` / ダイアログ2つ)。
props はすでに決まっているので**そのまま移すだけ**で済み、−791行と最大の削減になった。

つまり **「1つの巨大な関数」なのか「小さい関数が1ファイルに積んである」のか**で
分割の難易度がまったく違う。後者はほぼ機械作業。前者 (ProjectFormPage / EquipmentListPage /
BusinessProjectView) は props を1つずつ決める必要があり、型を間違えると
「渡せるが意味が違う」事故になる。

### 祝日の一覧がページ本文に埋まっていた

`SchedulePage.tsx` には **2027年までの祝日が手で書いてあった**。
「調べて直すもの」がページの本文にあると、直すたびに 1,500 行のファイルを開くことになる。
`schedule/types.ts` に移した。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ11項目** — 予定表（**「部屋」パネルに切り出した部屋とスタジオ名が出る** /
  **カレンダー連携ダイアログが開く** / **種別ピッカーが開く**）、
  ラック図（**前面・背面の切替** / **印刷ボタン**）、横はみ出し 0px、JSエラー0件
- **v2.9.292・293 の検証47項目も再実行して全通過**
- 検証で「予定表は部屋名を既定では出さない（「部屋」パネルの中）」と分かった。
  最初これを知らずに FAIL と判定したが、**既存の作りであって分割の影響ではない**

### 残り (13本)

`ProjectFormPage.tsx` 1,925 / `EquipmentListPage.tsx` 1,880 / `BusinessProjectView.tsx` 1,520 /
`manual/content.tsx` 1,455 / `RevenueListPage.tsx` 1,438 / `EventEditorPage.tsx` 1,294 /
`SchedulePage.tsx` 1,234 / `ProjectGroupListPage.tsx` 1,177 / `project.service.ts` 1,130 /
`TodayPage.tsx` 1,117 / `EditorPage.tsx` (Qシート) 1,094 / `EquipmentDetailPage.tsx` 1,093 /
`CueTable.tsx` 1,078。

**`manual/content.tsx` は対象から外す** — 画面の使い方の本文そのもの (文章のかたまり) なので、
分割しても読みやすくならない。

---

## v2.9.295 — 分割 6本目・7本目

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `EventEditorPage.tsx` | 1,294 | **827** | 型と計算 (`types.ts` 80) / 1行ぶんの編集部品 (`rows.tsx` 190) / 部門・賞のカード (`sections.tsx` 222) |
| `RevenueListPage.tsx` | 1,438 | **1,046** | 型・定数・並べ替えアイコン (`types.tsx` 82) / 売上明細の編集まわり (`RevenueItemsEditor.tsx` 379) |

### 決めたこと: 「大きいから切る」ではなく「境目が細いところで切る」

売上ダイアログは **571行**あってこのファイル最大の塊だが、**中で使う変数が 56 個**ある。
props に開くと、親のコードを読むより props の一覧を読むほうが大変になり、**分割で読みにくくなる**。

一方、その中の「明細を編集する部分」だけなら **14 個**で収まる。そこで切った。

| 切る候補 | 行数 | 中で使う変数 | 判断 |
| --- | --- | --- | --- |
| 売上ダイアログ全体 | 571 | **56** | 切らない |
| 明細の編集だけ | 341 | **14** | **切る** |
| 一覧 (カード + 表) | 156 | 16 | 今回は見送り (行数が小さい) |

### 機械的にやるなら、機械が壊したことも機械が見つけられる形にする

未使用 import の自動削除で `type X, type Y` から 1 つ消すときに **`type type Y`** という
壊れた形を作った。**型チェックが即座に構文エラーで止めた**ので実害はゼロ。
`type ` 付きも見るようにパターンを直した。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ15項目** — 売上一覧（**登録した 880,000 円が出る** / 売上ダイアログ /
  **明細の見出し** / **「行追加」で行が増える**）、リアルタイムCG のイベント編集
  （**部門が出る** / **受賞者の行と所属が出る**）
- **v2.9.292〜294 の検証75項目も再実行して全通過**

### 検証スクリプト側のつまずき (実装の不具合ではない)

1. 売上一覧のパスは `/finance/revenues` ではなく **`/finance?tab=revenue`**
2. 明細を足すボタンの名前は「項目を追加」ではなく **「行追加」**
3. 売上一覧は**今月で絞り込んでいる**ので、12月の日付で入れた検証データは出てこなかった

### 残り (12本)

`ProjectFormPage.tsx` 1,925 / `EquipmentListPage.tsx` 1,880 / `BusinessProjectView.tsx` 1,520 /
`SchedulePage.tsx` 1,234 / `ProjectGroupListPage.tsx` 1,177 / `project.service.ts` 1,130 /
`TodayPage.tsx` 1,117 / `EditorPage.tsx` (Qシート) 1,094 / `EquipmentDetailPage.tsx` 1,093 /
`CueTable.tsx` 1,078 / `RevenueListPage.tsx` 1,046。
(`manual/content.tsx` 1,455 は文章のかたまりなので対象外)
