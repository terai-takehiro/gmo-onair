# スマホ（Phase 6）— モックから切り出した仕様

**出どころ**: `mockups/v4-mockup-main.dc.html` の**端末枠 13 本**（340×700px）と
`mockups/onair-data.js` の `sp*` 31 キー、`mockups/DESIGN_POLICY.md`。

> **モック HTML を毎回 grep しないこと**（675KB あります）。ここを読んでください。
> 足りないものが出たら、**モックを1度読んでここに足す**のが決まりです。

---

## 0. いまどこまで出来ているか（正直に）

Phase 6 は **「PC の画面を指で押せるようにする」**（M0 以前）から始めて、
いま **モックの 13 枚のうち 7 枚ぶん**（⑤⑦⑧⑩⑫⑬・⑩ は ⑬ に統合／⑪ は途中）を作ったところです。

| | 中身 |
| --- | --- |
| **できている** | タップ対象 44px（29画面で 0 件の違反）／本文 13px ／横はみ出し 0px ／下タブ3つ／`primaryAction` が下端に付く／閉じた引き出しがタブ順に残らない／**専用画面 7枚ぶん**（⑤⑦⑧⑫⑬＋⑩ は ⑬ に統合・⑪ は入力と結果まで） |
| **できていない** | **⑨ 機材 ／ ②③⑥ 案件まわり ／ ① トップの件数の1行 ／ ⑪ の「やること・場所・最近見たもの」**。これらはいまも **PC の画面が縦に畳まれたもの**です |

モックの言い方をそのまま引くと「**PCの情報をそのまま縮小して載せない**」。
**作った分はこれを満たしています**が、**残り（⑨・②③⑥・①の件数・⑪の残り）はまだ縮小して載せているだけ**です。

---

## 1. 下タブ — **3本**（ホーム / やること / 探す）

**モックの中で2通りに割れていました。ご判断で3本に決めました。**

| どこ | 何本 | 中身 |
| --- | --- | --- |
| `DESIGN_POLICY.md` ／ `_rules.md` ／ 操作できる試作品 | **3本** | ホーム / やること / 検索 |
| `onair-data.js` の `spTabs`（13枚の静止画が使う） | 5本 | 今日 / 案件 / タスク / 予定 / 検索 |

→ **3本を正とします**（方針の文章に明記があり、実装済みで、親指の届く範囲に収まるため）。
5本版の「今日」「案件」「予定」は**ホームのタイルから入ります**。

- 高さ 56px ＋ `env(safe-area-inset-bottom)`（`shell/MobileTabs.tsx`）
- **メニューは上辺バーの ☰**。下タブに置くと1枠が二重の入口になる

---

## 2. 決めごと 8つ（`spRules` の逐語）

**「やってはいけない例」が各項目に付いています。** そこが要点です。

| 決めごと | やってはいけない例 |
| --- | --- |
| **主操作は下半分に置く** — 押すものは画面の下から 400px 以内。上半分は読むだけ | 右上だけにある確定ボタン |
| **終わらせるのはシートで** — 一覧の行から画面遷移させない。下から出るシートで確定し、終わったら次の1件へ | 行タップ→別画面→戻ると先頭に戻る |
| **1画面に1つのことだけ** — PC の2〜3カラムを縦に積まない。優先度の低い列は畳む | PC の右カラムをそのまま下に流す |
| **セーフエリアを空ける** — 下タブの下に 22px、上は 48px 以上 | 画面の最下端に貼り付けたボタン |
| **入力は端末に任せる** — 金額は数字キーパッド、日付は端末のピッカー、音声はキーボードのマイク。期限は「明日18:00／3日後18:00／日時を選ぶ」を先に出す | 自作の録音UI・自作の日付ホイール |
| **電波が弱い前提で作る** — 現場の記録は端末に溜めて後で送る。送信中も操作を止めない | 1件ごとに通信して失敗で止まる |
| **本番中は画面を消さない** — 役割URLの間は自動ロックを止める。暗い配色で「いま／つぎ」の2枚だけ大きく | 切れているのに緑のまま |
| **スワイプに意味を持たせない** — 横スワイプで完了・削除を割り当てない（手袋と片手では誤爆する） | スワイプでしか出せない操作 |

## 3. スマホに置かないもの（`spNotOnPhone` の逐語）

| 置かない | 理由（モックの言葉） |
| --- | --- |
| お客様 | 列が多く読めない |
| お金 | 表が横に伸びる |
| 設定・権限 | 落ち着いて触るもの |
| ガント | PCで見る（リストへ誘導） |
| 3列レビュー | MTG用の印刷向き |
| Qシートの編集 | 本番の受け取りだけ出す |

> **「お金」を置かないのに「入金の確認」はある**のが要点です。
> 置かないのは**台帳の表**で、**片づく1つの仕事**は置きます。

---

## 4. 13 枚の中身

いまの実装との差を「差」の列に書いています。**⬜ = 作っていない**。

### ① トップ ⬜（**差は小さい**）
`おはようございます` ＋ `お待たせ 3件 ・ 期限切れ 2件` ／
**AIに任せる**（貼る・撮る）／アプリのタイル／イベントで使うもの／
`今日` `すべて →` ／自分のやること(3) ／今日の予定
- **差**: 13枚のうち**いちばん差が小さい**。挨拶・件数・アプリのタイル・
  「AI に任せる」・今日 の並びは**既にモックと同じ**です。足りないのは
  ① 件数の1行が文章になっている（モックは `お待たせ 3件 ・ 期限切れ 2件` の形）
  ② **「貼る」「撮る」の2ボタン**が無い
  → **「貼る」は足しました**（スマホだけに出る「電話・その他を貼る」→ ⑦）。
    **「撮る」は出していません**（名刺を読む口が無い）。
    残りは件数の1行の形だけなので、②③⑥ と一緒に M4 で整えます

### ② 案件管理 ダッシュボード ⬜
`案件受付 4`（メールから取り込む／電話・その他を貼る／打合せを録音する）／
`進行中の案件 24 件` `止まっている 3 件` `自分のタスクがある案件` ／`一覧 →`
- **差**: 受付の3つの入口が縦に並ぶ形になっていない。**録音はまだ無い**

### ③ 案件一覧（カード） ⬜
`リスト / ボード` の切替、`ネタ` チップ、カード（金額つき）
- **差**: PC の行がスマホで縦積みになるだけ（`Row stackOnMobile`）。カードではない

### ④ 受付 ／ 案件の種類をきめる ⬜
`2 / 3` の段表示。**「ここで標準工程テンプレートが決まります。あとから変えられます」**
／種類を選ぶ／`この種類で進む`（下端固定）
- **差**: 案件作成は PC のモーダル1枚。段に分かれていない

### ⑤ 打合せを録音してAIに渡す ✅ **作った**
どの案件か選ぶ（`案件なし` でも録れる・あとから紐づけ）→ `押すと録音がはじまります`
→ `録音中 12:38` `一時停止` `終わってAIに渡す` → 決定事項・タスクの候補（押すと外せる）
→ `この案件に反映する`。**「文字起こし全文は案件のやり取りに入ります。読むのはPCが向いています」**
- **作った** (`sales/pages/MeetingRecordPage.tsx`・`/sales/record`)。
  **録音そのものは作り直していません** — 案件詳細の `RecordDialog` をそのまま開きます
  （32kbps・25MB の上限・「相手に伝えてください」の表示が入っている）。
  送り先も同じ `POST /projects/:id/minutes`
- **モックと違うところ**:
  | モック | どうしたか |
  | --- | --- |
  | **「案件なし」でも録れる／あとから紐づけ** | **案件を先に選ぶ。** `project_minutes.project_id` が NOT NULL で、置き場所と後から紐づける画面が要る（表の変更なので別の回） |
  | **その場で決定事項とタスクの候補が出る** | **出さない。** 文字起こしは裏で走る（1時間で数分）。スマホを開いたまま待たせるのは無理。**モック自身の但し書き**（「読むのはPCが向いています」）に従い、できたら案件のやり取りに出す |
- **文字起こしにつないでいない環境では押せなくする**（`stt_available`）。
  押せると録ったあとに必ず失敗するので、理由を書いて止める

### ⑥ 案件を開く（概要・標準工程） ⬜
件名＋`LIVE` `ハイブリッドイベント` `B 口頭決定` ／`実施日 あと78日` ／
会場・スタジオ `仮押さえ中` ／標準工程 ／個別タスク ／
**「見積・書類・やり取りは PC で」** ／道具（予定を見る・機材・Qシート・BOX）
- **差**: PC の7タブがそのまま出る。**「PCで」の案内が無い**

### ⑦ 受付（貼って送るだけ） ✅ **作った**
`どこから来た話ですか` ／日時・担当 ／`文字起こし・メモを貼る` ／`録音から` `名刺を撮る` ／
**「読み取った内容の細かい修正はPCで行います。ここでは送るところまで」** ／`AIで取り込む`
- **作った** (`sales/pages/InquiryQuickPage.tsx`・`/sales/inbox/new`)。
  出どころ・いつ・誰から・本文の4つだけ。1行目が受付の見出しになる。
  送ると `POST /dailyops/inquiries` で**未仕分け**に入り、PC の受付にそのまま並ぶ
- **モックから外したもの（作り話をしない）**:
  | モック | どうしたか |
  | --- | --- |
  | ボタン「**AIで取り込む**」 | **「受付に送る」にした。** 貼った文章を読み取る AI はまだ無い |
  | **名刺を撮る** | **出していない。** OCR の口が無く、画像を貼る先も `misc_inquiries` に無い |
  | **録音から** | **出していない。** スマホの録音画面（⑤）はこれから |
- **サーバーの権限を広げた**: `POST /dailyops/inquiries` は `dailyops` の editor
  だけを要求しており、**営業の人が自分で聞いた話を入れられなかった**。
  同じ表を読む `GET /inquiries/:id` と `link-project` は大② で既に
  `dailyops` か `sales` を見るようにしてあり、**入れる口だけ狭いまま**だった

### ⑧ やること ✅ **作った**
`今日 3` `期限切れ 2` `依頼 1` ／`自分14 / 案件62 / 全体128` ／
行（件名・案件・期限・重み）／**「押して消し込むだけ。並べ替えや割り当ての変更はPCで」**
- **作った** (`taskList/MobileTaskList.tsx`)。行を押すと**下から出るシート**が開き、
  **画面は移りません**（URL が変わらないことを実測）。並べ替え・担当の変更は出しません
- **モックと変えたところ**: `依頼 1` を出さず **自分 / 全体** にしました。
  「誰かから頼まれた」を表す列が `project_tasks` に無く、担当が自分かどうかしか
  分からないためです（**数えられないものを置かない**）
- **ガントはスマホに出しません**（`spNotOnPhone`）。`/sales/tasks/gantt` を
  スマホで開くと「PC で見る画面」と書いて「やること」へ送ります
  （**黙ってリストにすり替えない**）

### ⑨ 機材の貸出・返却（現場で） ⬜
`QRを読んで…` ／確認できた 412 ・ 見つからない 3 ・ のこり 189 ／
手で入れる・見つからない一覧・中断する
- **差**: 機材アプリ（別バンドル）。**電波が弱い前提の溜め込みが無い**

### ⑩ 日常業務（今日の現場） ✅ **⑬ に畳んだ**
`今日 / 明日 / 今週` ／時刻・区分（出庫・リハ・本番）・場所・注記の縦並び
- **⑬ 今日の予約と1枚にした。** 区分の「リハ・本番」は**どちらもスタジオ予約**で、
  ⑬ と中身が9割同じです。2枚に分けると同じ予約を2つの画面が別々に描くので、
  片方だけ直したときに時刻の丸め方や区分の色が食い違います
- 違いは「**出庫**」だけなので、⑬ に**機材の層**として重ねました
  （出庫予定 `status=planned` ＋ 返却期限 `due_date`。実装は `rooms/MobileToday.tsx`）
- **`equipment` の権限がある人にだけ出します。** 無い人に空の枠を出すと
  「今日は出庫が無い」と読めますが、実際は見えていないだけです
- **出庫の行に時刻を書きません。** `planned_out_date` / `due_date` は日付だけの値で、
  時刻の列に混ぜると「0:00 に出庫」と読めてしまいます
- **`今日 / 明日 / 今週` のタブは前/次の日にしました。** 「今週」は
  7日ぶんの予約を1画面に積むことになり、現場で目的の日に着くまでスクロールが要ります

### ⑪ 探す（⌘K のスマホ版） ✅ **作った**
やること（やり取りを記録／機材QR／予定を入れる／案件をつくる）／
場所（お客様・お金・機材・Qシート・ふりかえり・設定）／最近見たもの
- **差**: **入力と結果は作った**が、**「やること」4つと「場所」6つ、最近見たものが無い**

### ⑫ 入金の確認（財務管理） ✅ **作った**
金額の縦並び・入金の記録
- 実装は `finance/pages/closing/MobileCollect.tsx`（`/budget/billing` の幅で切り替え）
- **できるのは「入金があったことの記録」1つだけ。** モックの
  「スマホに置かないもの」に**お金（表が横に伸びる）**が挙がっているのに ⑫ だけ端末枠が
  あるのは、置かないのが**台帳の表**で、**片づく1つの仕事**は置くという切り分けだからです
- **並びは支払期日の近い順**（過ぎたものが先頭）。入金の確認は遅れているものを拾う作業です
- **取り消しは置きません。** 経理の訂正は記録が残る場所（PC）でやるべきで、
  スマホで戻せると誰がいつ戻したのか分からなくなります
- **入金日は今日で入ります**（別の日は PC から）。そう画面に書いてあります

### ⑬ 今日の予約（カレンダー）＋ ⑩ 今日の現場 ✅ **作った**
時刻の縦並び ／`空きを押さえる`（下端固定）
- 実装は `production/pages/rooms/MobileToday.tsx`（`/studio/calendar` の幅で切り替え）
- **`空きを押さえる` は「部屋の空きを見る」にしました。** 予約を作るのは
  部屋・時間・案件・用途を決める作業で、PC のダイアログは入力欄が十数個あります。
  **押しても何も起きないボタンは置きません**
- **終日を先頭に**、そのあと開始時刻順。終日は時間の幅を持たないので並べられません
- **前の日から続いている予約は「今日の開始時刻」で書きません**（`〜終了時刻` と出す）
- **下限は日付だけで問い合わせます。** 終日の予約は日付だけで保存されるので、
  `T00:00` を付けるとその日の終日がまるごと落ちます（② 部屋の空きと同じ理由）

---

## 5. 作る順番（案）

**下から出るシート（`Sheet`）が無いと ⑧ も ④ も作れない**ので、先に部品を作ります。

| 段 | 中身 | なぜこの順 |
| --- | --- | --- |
| **M0** ✅ | **下から出るシート**（`client-v4/sheet.tsx`）・`useIsMobile`・期限プリセット・期限の見え方（`client-v4/mobile.ts`） | ⑧④⑤⑦ が全部これを使う。無いと「行→別画面→戻ると先頭」になる |
| **M1** ⚠️ | ⑧ やること ✅ ／ ① トップ **一部** | ⑧ で M0 を実証。トップは「貼る」の入口だけ足した（下記） |
| **M2** ✅ | ⑦ 受付（貼って送るだけ）✅ ／ ⑤ 打合せを録音 ✅ | 外で使う2枚。既存の受付と Whisper をそのまま呼んだ |
| **M3** ✅ | ⑬ 今日の予約（**⑩ 今日の現場を畳んだ**）✅ ／ ⑫ 入金の確認 ✅ | 「今日ぶんだけ」。⑩ と ⑬ は中身が9割同じなので1枚にした |
| **M4** | ② ③ ⑥ 案件まわり ／ ⑪ 探すの残り | 数が多いので最後 |
| **M5** | ⑨ 機材（別バンドル・オフライン溜め込み） | 唯一 `client-equipment` 側。設計判断が要る |

**⑨ だけ先に決めることがあります**: 「端末に溜めて後で送る」は
`localStorage` に積んで送信を再試行する仕組みで、**二重登録をどう防ぐか**
（同じ機材を2回読んだとき）を決めてから作ります。

---

## 6. 実装のときに守ること

- **PC 用の画面に `sm:hidden` を足して分岐させない。** 1つのファイルが
  2つの情報設計を持つと、どちらを直しているのか分からなくなります。
  **スマホ専用の画面はルートを分ける**（例 `/m/today`）か、
  画面の中で**別の部品**に切り替えます
- **下端固定は自分で書かない。** `<PageHeader primaryAction>` に渡せば
  シェルが下タブの上に置きます（`shell/primaryAction.ts`）
- **44px・13px は部品側で効いています**（`tokens-v4.css` の `@media (max-width:1023px)`）。
  画面ごとに `min-h-tap` を書き足す必要はありません
- **測ってから出す。** 375px の実測は `scripts/` に残していません
  （毎回書き捨て）。横はみ出し・44px 未満・13px 未満・JS エラーの4つを数えます
