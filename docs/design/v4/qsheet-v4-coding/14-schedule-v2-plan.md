# 14. スケジュール表・第2版計画 — 「まず使える」を最優先に（機能と UI/UX）

> 2026-09-06。利用者の指摘「現状 MCP くらいしかできる機能がなく、根本的な機能性が皆無。
> UI/UX とここに盛り込む機能を考えて。シンプルが基本」への回答。
> 調査は4体の並列サブエージェント（現状の穴の監査／設計意図の棚卸し／機能案／UI・UX案）に分け、
> ここに統合した。**事実の出典は各所に file:line で添えてある。**
>
> 第1版の仕様は [`02-schedule.md`](02-schedule.md)・実装設計は [`impl/04-schedule-impl.md`](impl/04-schedule-impl.md)。
> この文書は**それらを置き換えない**。第1版で作らなかった部分をどう埋めるかだけを書く。

## 2026-09-06 に決まったこと（利用者の判断）

| 決まったこと | どこに効くか |
| --- | --- |
| **1 日 1 枚のまま。複数日のイベントは、一覧を「イベント（案件・番組）ごと」にまとめて見せる** | §3 B4（連日の導線）・§4-2 (a) 一覧のワイヤー・§4-3 表のヘッダーに「同じイベントの他の日」。複製（B5）は「同じイベントの別の日を写す」として残す |
| **案件メンバーには自動で共有する**（第1版の「共有した人だけ」を上書き） | §3 A4・§3-2（可視性の作り）。`access.ts` と `listSchedules` に**案件メンバー判定を足す小さなサーバー作業**が段Aに入る。番組（`qsheet_programs`）はメンバーを持たないので従来どおり明示共有のみ |

## 0. 結論

**「機能が無い」のではなく、サーバーにある機能が画面に配線されていない。**
REST と MCP には 表・列・項目・共有・ひな形・Excel・ブレイクダウンが一通り揃っている
（§1 の表）のに、画面は**作った直後に何もできない**:

```
新しく作る → 列が 0 本 → 「項目を追加」が disabled（SchedulePage.tsx:207）
          → 列を作る UI が無い → ひな形適用か AI 生成を通す以外に手が無い
```

さらに 表の設定（題・日付・拠点・状態・備考）・共有・削除・列の管理 が**画面のどこにも無い**。
共有を設定できないので、**作成者以外は管理者でない限り一生見られない**（`access.ts:38-50`）。
「MCP しかできない」という体感はここから来ている。

したがって第2版は次の順で進める。

| 段 | ねらい | サーバー作業 |
| --- | --- | --- |
| **A まず使える**— **完了（2026-09-06・PR1〜4）** | 袋小路を塞ぐ。列の管理・表の設定・共有・削除・スマホからの追加・ひな形設定への導線 | **案件メンバーの自動共有 1 本だけ**（§3-2）。ほかは既存 REST の配線のみ |
| **B 日々の運用が楽になる**— **完了（2026-09-06・PR5〜7b）** | ドラッグで押し・巻き、確定の見え方、既存台本との結び直し、連日、絞り込み、印刷 | 複製 1 本＋AI 事後差分 1 本（どちらも小） |
| **C 考えてから**— **決定済み（2026-09-06）: 3件とも現状維持・実装しない** | 利用者の判断が要るもの（§6）。3件（担当のuser連携／ひな形の分類軸／表→ひな形の逆流）を確認し、いずれも現状維持で決着 | — |

**画面は 2 枚（一覧・表）のまま増やさない。** 設定・列・共有は表の上に重ねるシートに置く。
**第2版計画（段A〜C）はこれで完了。** 未決のまま残るのは §6 の #2（深夜またぎ表記）・#7（同時編集人数）の
2件のみで、どちらも段A〜Cの計画には含まれていない一般的な今後の検討事項。

## 1. いま何が起きているか（監査結果）

REST prefix は `/techops`（`server/src/contexts/qsheet/index.ts:53-61`）。

| 能力 | REST | MCP | 画面 |
| --- | --- | --- | --- |
| 表の作成 | `POST /schedules`（title・service_date・location_id・project_id・program_id・episode_id・template_id・onair_start_min） | `create_schedule` | △ 題・日付・案件/番組（URL 由来）だけ。拠点・回・ひな形・本番開始は選べない |
| 表の更新（題・日付・拠点・案件・回・表示時間帯・状態・備考） | `PUT /schedules/:id` | `update_schedule` | **❌ 皆無**（`scheduleApi.updateSchedule` の呼び出し元ゼロ） |
| 表の削除 | `DELETE /schedules/:id` | ❌（意図的） | **❌** |
| 共有 | `PUT /schedules/:id/shares` | ❌ | **❌**（一覧に「共有 N 人」の数だけ） |
| 列の追加 | `POST .../columns` | `create_schedule_column` | **❌** ひな形適用・AI 取り込みの副産物としてのみ |
| 列の改名・部屋・色・幅／並べ替え／削除 | `PUT` / `PUT .../reorder` / `DELETE` | 各ツールあり | **❌**（`col.color` はグリッドで描画すらされない） |
| 項目の追加・編集・削除 | ✅ | ✅ | ✅ PC のみ（スマホの `MobileTimeline` に追加の入口が無い） |
| 項目のドラッグ移動・リサイズ | `PUT .../items/bulk` | △ | **❌**（`bulkUpdateItems` 未使用。`ScheduleGrid.tsx:3-4` に明記） |
| 台本を新規作成して結ぶ | `POST .../items/:id/qsheet` | ❌ | ✅ |
| 既存台本へ結び直す・外す／台本から枠を逆引き | `PUT .../items/:id/qsheet` ／ `GET /documents/:docId/schedule-items` | ❌ | **❌**（リンク切れは赤字表示のみ） |
| ひな形の適用（下見→選択） | ✅ | ❌ | ✅ |
| ひな形の管理 | 作成・複製・更新・削除・列・項目（`system_admin`） | ❌ | △ 名前と題しか入れられない。**画面への導線が無い**（URL 直打ちのみ） |
| AI で枠の下書き | `POST /techops/ai/event-plan` | ❌ | ✅ |
| Excel 書き出し／ブレイクダウン | ✅ | ❌ | ✅（ブレイクダウンは 1 行テキスト） |
| 一覧の絞り込み | date_from/to・project・program・location・status・search | `get_day_schedule` | △ 日付以降・案件/番組のみ。`service_date DESC LIMIT 200` 固定 |
| スタジオ予約との結線 | ❌（`studio_rooms` の FK のみ） | 別ツール | ❌ |

画面から**できないこと**の全 14 件と file:line は監査時の記録に残した（本文書 §8）。

## 2. 決めごと（シンプルを守るための線）

1. **画面は増やさない。** 一覧・表の 2 ページ。設定／列／共有は表の上のシート、項目は既存のダイアログ
2. **サーバーは判定しない**（[`01-app-structure.md`](01-app-structure.md) §4-2 を維持）。状態（下書き／確定／保管）は人が付ける札。「遅れている」「変更済み」の自動判定・％バーは作らない
3. **既存 REST を使い切ってから足す。** 段Aのサーバー変更は 案件メンバーの自動共有（§3-2）だけ。段Bの新設は 複製 と AI 事後差分 の 2 本だけ
4. **第1版の「やらない」を維持**（[`02-schedule.md`](02-schedule.md) §11）: 公開URL・PDF 生成・尺の自動書き戻し・ひな形変更の追随ボタン・重なりの DB 禁止・進み具合ラベル
5. **shared に新しい部品を足さない。** 右サイドドロワーは無いので `client-v4/sheet.tsx` の `Sheet`（PC=中央・スマホ=下端）で組む
6. **削るもの（20 人規模に不要）**: リアルタイム同時編集（Yjs）・項目ごとのコメント・添付・承認ワークフロー・変更履歴の閲覧・任意色ピッカー・区分の自由追加・繰り返し予定・通知・スマホのドラッグ・個人別「自分の 1 日」ビュー（§6 の判断まで）・予約との自動突き合わせ

## 3. 機能（段ごと）

### 段A「まず使える」— 既存 REST の配線だけ（サーバー変更は A4 の自動共有のみ）

| # | 機能 | 使う既存 API | なぜ最初か |
| --- | --- | --- | --- |
| A1 | **列の管理**（追加・改名・グループ・部屋を結ぶ・色・左右へ・削除）— **PR1 で実装済（2026-09-06）** | `POST/PUT/DELETE .../columns`・`PUT .../columns/reorder`・**新設 `GET /techops/studio-rooms`（読み取りのみ・§3-4）** | 列 0 本の袋小路を塞ぐ。色は固定パレット 6 色＋`studio_rooms` の既定色 |
| A2 | **空状態の 3 択**（ひな形から／会場を選んで列を作る／AI で下書き）— **PR1 で実装済** | ひな形 preview/apply・`POST columns`・`ai/event-plan` | 作った直後に次の一手が見える |
| A3 | **表の設定シート**（題・日付・拠点・案件/番組・回・表示時間帯・状態・備考）と**削除** — **PR2 で実装済（2026-09-06）** | `PUT /schedules/:id`・`DELETE /schedules/:id` | 題・日付を直せない・`doc_no` が見えない |
| A4 | **共有** — 案件メンバーは**自動で見える**（§3-2）。それ以外の人はユーザー検索→追加／外す — **PR2 で実装済** | `GET/PUT /schedules/:id/shares`＋**`access.ts`・`listSchedules` に案件メンバー判定を足した** | 作成者以外に見えない。2026-09-06 の決定 |
| A5 | **新規作成に拠点・回・ひな形・本番開始時刻**。案件を選んだら題・日付を先埋め — **PR3 で実装済（2026-09-06）** | `POST /schedules` の既存引数・`GET /lookup/:projectId/context` | codex 棚卸し #325「案件から引けるのに手入力」 |
| A6 | **スマホから項目を追加**（下端固定ボタン→既存ボトムシート）— **PR1 で実装済**。**絞り込み中の列を既定にする — PR3 で実装済**（`MobileTimeline` の絞り込み state を `SchedulePage` に持ち上げた） | `POST .../items` | 空状態の案内「下のボタンから足せます」が今は嘘 |
| A7 | **ひな形設定への導線**＋ひな形項目の中身（区分・基準・オフセット・尺・必須）の編集 — **PR3 で実装済** | 既存のひな形 REST（更新・複製・項目更新） | URL 直打ちでしか届かない。既定値 `other/day/0/30` で固定されている |

### 段B「日々の運用が楽になる」

| # | 機能 | 使う API | 備考 |
| --- | --- | --- | --- |
| B1 | **ドラッグ移動＋下端リサイズ（PC のみ・5 分スナップ）**— **PR5 で実装済（2026-09-06）** | `PUT .../items/bulk` | 押し・巻きの主操作。スマホはタップ→シートのまま |
| B2 | **確定・保管の見え方**（§4-4）— **PR4 で実装済（2026-09-06）** | `PUT /schedules/:id`（status） | 画面だけ。ロックしない |
| B3 | **既存台本へ結び直す／外す**・台本側に「この台本の枠」— **PR7a で実装済（2026-09-06）** | `PUT .../items/:id/qsheet`・**新設 `GET /documents/:docId/schedule-items`** | リンク切れを直せるようにする |
| B4 | **イベントごとにまとめる**（一覧は案件・番組の単位で束ね、その中を日付順に並べる。表のヘッダーに「同じイベントの他の日」）— **PR4 で実装済** | `GET /schedules`（`project_name`・`program_name` は既に返る。束ねるのは画面側） | **2026-09-06 の決定**。「1 日 1 枚」は崩さない（§3-3） |
| B5 | **複製（同じイベントの別の日として写す）**— **PR6 で実装済（2026-09-06）** | **新設 `POST /schedules/:id/duplicate`**（1 トランザクション） | 第1版で見送り（impl §11-3）。複数日のイベントで初日の表を翌日に写す。項目の一括作成 API が無いため必要 |
| B6 | **一覧の絞り込み**（拠点・状態・題／案件の検索）— **PR4 で実装済** | `GET /schedules` の既存引数 | 200 件固定で探せない |
| B7 | **印刷 CSS**（`@media print`・A3 横・1 日 1 枚）— **PR6 で実装済（2026-09-06）** | — | PDF 生成は作らない（02 §10-4） |
| B8 | **担当のユーザー名補完**（FK は張らない）— **PR7a で実装済（2026-09-06）** | `GET /techops/share-users` | 社外出演者を自由入力で残す（02 §11-4） |
| B9 | **予約から会場列を入れる（コピー型）**— **PR7b で実装済（2026-09-06）** | 予約一覧・`POST columns` | 下見→選んで→コピー。**自動生成はしない**（§3-1） |
| B10 | **AI 生成条件に拠点・種別**／**確定時の事後差分記録**— **PR7b で実装済（2026-09-06）** | `ai/event-plan`・`settleScheduleEarlyOnConfirm`（**確定時に early 段だけ締める**。§3-10） | §5 |

#### 3-1. スタジオ予約から列を自動で立てない理由

`studio_bookings` は `hold_rank`・`possible_duplicate` を持つ「仮押さえを含む予約」で、その日の進行そのものではない。
自動で列が立つと仮押さえの部屋まで表に出て、消す手間が増える。一方 `studio_booking_rooms.room_id` と
`qsheet_schedule_columns.room_id` は同じ `studio_rooms` を指すので、**コピー型なら FK もずれも出ない**。
ひな形適用と同じ「下見→選択→コピー」の作法（02 §5-1）に揃える。案件の回（`episodes`）からは日付・拠点の**先埋めだけ**。

#### 3-2. 案件メンバーの自動共有（2026-09-06 決定）の作り

**共有テーブルに写さず、判定のときに案件メンバーを見る**（動的判定）。

- `canAccessSchedule`（`server/src/contexts/qsheet/access.ts`）と `listSchedules` の可視条件
  （`schedule.service.ts` の `created_by = $uid OR EXISTS(shares)`）に、次の 2 条件を **OR で足す**:
  1. `EXISTS (SELECT 1 FROM project_members pm WHERE pm.project_id = s.project_id AND pm.user_id = $uid AND pm.deleted_at IS NULL)`
  2. `EXISTS (SELECT 1 FROM projects p WHERE p.id = s.project_id AND p.assigned_to = $uid)`（主担当。`project_members` に自分を入れていない案件のため）
- 写さない理由: メンバーの追加・削除に追随する。マイグレーション不要。`qsheet_schedule_shares` は「案件メンバー以外に見せたい人」（社外の技術・受付など、案件にメンバーとして載せない人）専用になる
- **番組（`qsheet_programs`）はメンバーを持たない**ので、番組の表は従来どおり 作成者＋明示共有＋管理者
- 外部メンバー（`project_members.user_id IS NULL`・`is_external`）はログインできないので影響なし
- **台本（`qsheet_documents`）は変えない。** 台本は「その番組を作る人」、当日進行表は「その日動く全員」に配るという区別（02 §2-4）の通りで、非対称は意図したもの。`canAccessDoc` には触らない
- 画面: 設定シートの「共有」節の先頭に「案件 GLS-xxxx のメンバー N 人には自動で見えます」と事実だけ 1 行出し、その下に明示共有のチップを置く。一覧の「共有 N 人」は明示共有の数のまま（案件メンバーを数えると案件側の変更で数字が動く）
- 逆引き（`GET /documents/:docId/schedule-items`）と AI 提案（`canAccessProposal`）は `canAccessSchedule` 経由なので自動で揃う。MCP の `gate.ts` も同じ関数を通っているか実装時に確認する（codex 棚卸し #395 が未確認のまま）

**PR2 で実装済（2026-09-06）**: `canAccessSchedule` に上記2条件を追加し、`listSchedules` の可視条件にも同じ2条件を OR で足した。
実 Postgres で確認（`scripts/dev-verify`）: 案件メンバー（`project_members`）は作成者でも明示共有でもないのに一覧・詳細を読める（200）。
案件に無関係な `qsheet` editor は 404（存在秘匿）のまま。番組の表は従来どおり（案件メンバー概念が無いので対象外）。
共有の管理（`GET/PUT /schedules/:id/shares`）は今までどおり**作成者・管理者のみ**（`requireShareManager`）——
案件メンバーが増えても、明示共有できる人を増減できるのは作成者・管理者だけ。

⚠️ **実装中に見つけた別のバグも同じ PR で直した**（`schedules.routes.ts` の `PUT /schedules/:id`・
MCP の `update_schedule`）。両方とも `キー: 条件 ? 値 : undefined` という形でオブジェクトを組み立てており、
`updateSchedule`（`schedule.service.ts`）側は `'projectId' in input` で「送られたか」を判定するため、
**値が `undefined` でもキー自体は必ず存在し、常に true と判定されていた**。結果、`location_id`・
`project_id`・`program_id`・`episode_id`・`notes` は**指定しなかっただけで毎回 null に戻る**状態だった。
この経路（`PUT /schedules/:id`）はこの PR まで呼び出し元が無く誰も踏んでいなかった（監査 §8 の穴 #3）。
スプレッド構文（`...(条件 ? { key: 値 } : {})`）に直し、実 DB で「`project_id` を送らない部分更新」が
値を保持することを確認した。

#### 3-3. 「1 日 1 枚・イベントごとにまとめる」（2026-09-06 決定）の作り

- データは変えない。`qsheet_schedules` の 1 行＝1 日のまま。束ねる鍵は `project_id`／`program_id`（どちらも無い表は「案件・番組なし」の束）
- 一覧（§4-2 (a)）は 束＝イベント（GLS 番号・題）、その中の行＝日付順の表。`?project=`・`?program=` で来たときは束が 1 つになるだけで同じ画面
- 表のヘッダーに「同じイベントの他の日」を日付チップで出す（`GET /schedules?project_id=`／`program_id=` を一覧と同じキャッシュで読む）。前日／翌日の矢印は付けない（飛び日のイベントで意味がずれる）
- 複製（B5）は「同じイベントの別の日として写す」に限定する。写すのは 列・項目・表示時間帯・拠点。共有は写さない（自動共有で足りる）

**PR4 で実装済（2026-09-06）**: 一覧の束ねは `pages/schedule/scheduleBundles.ts`（純関数。プロジェクト/プログラム/なしの
3種の鍵でまとめ、束の中は日付昇順・束どうしも一番近い日付の昇順に並べ直す——サーバーの並び
`service_date DESC` とは向きが違うので必ず画面側で並べ直す）。表のヘッダーの日付チップは
`components/schedule/ScheduleSiblingDays.tsx`（新設）。**一覧と同じキャッシュキーは使わなかった**——
一覧側は拠点・状態・検索の絞り込み条件までキーに含む（§3 B6）ため、キーを合わせると
「一覧を絞り込んだ状態で開いた表」のチップが一覧の絞り込みの影響を受けてしまう
（例: 一覧で「確定」だけに絞ってから表を開くと、下書きの日が消えて見える）。
そのため `["schedule-siblings", projectId, programId]` という**専用の鍵**で、絞り込み無しの
全日を別途取得する（キャッシュの再利用より正しさを優先した）。

#### 3-4. PR1 で足した小さな読み取り API と、見出しの「…」

- **`GET /techops/studio-rooms`**（`server/src/contexts/qsheet/routes/schedule-rooms.routes.ts`）: 拠点ごとの部屋（id・名前・種別・色）。
  `/studios/locations` は `requirePermission('sales')` の下にあり、制作技術支援だけを使う技術・運営のアカウントには 403 になるため、
  qsheet の権限で同じ `studio_rooms` を読むだけの口を足した。書き込みは無い
- **見出しの「…」は依存を増やさず自前で作った**（`components/schedule/MoreMenu.tsx`）。このアプリと shared にドロップダウンの部品が無く、
  Radix の dropdown-menu も依存に入っていない。外側クリック・Esc で閉じる・項目 44px
- **列の削除は `confirmAction`**（`window.confirm` をやめた）。`SchedulePage.tsx` にあった「ConfirmHost 未設置」のコメントは古く、
  共通シェルが器を持っている
- 実ブラウザ確認（PC 1280 / 375px・Playwright）: 空状態→会場 2 部屋から列作成→「…」→列を足す（支度・MC）→鉛筆→左へ→項目を置いて
  列を削除（確認文に項目数）まで通し、横はみ出し 0px・JS エラー 0 件・スマホの主ボタンが下端（48px）に出ることを確認した

#### 3-5. PR3 で足したもの（2026-09-06）— サーバー変更なし

**A5・A6・A7 はすべて既存 REST の配線だけで済んだ**（サーバー変更ゼロ）。

- **「案件／番組」の選び方を共通化した**（`components/schedule/ScheduleOwnerFields.tsx`）。
  表の設定シート（A3）と新規作成ダイアログ（A5）の両方が同じ部品を使う。なし／案件／番組の3択トグル＋
  対応する選択（案件なら回も）を1組にまとめてあり、`project_id`/`program_id` を同時に立てない
  という制約（migration 227）を部品の中に閉じ込めた
- **新規作成ダイアログを別コンポーネントに切り出した**（`components/schedule/CreateScheduleDialog.tsx`）。
  題・日付・拠点・案件/番組・回・ひな形・本番開始時刻をすべて持つ。旧実装は生の `Dialog`（PC 中央固定）
  だったが、`FormDialog`（`Sheet`）に載せ替えたので 375px では下端シートになる
- **先埋めは「事実がある分だけ」。** `GET /lookup/:projectId/context` は拠点の id を返さない
  （`venue` は部屋名を並べた文字列で `studio_locations.id` を持たない）ため、**拠点の先埋めはしない**
  ——実在しない対応付けを当てに行くと「事実でないものを先埋めする」ことになり、02 の語彙ルール
  （サーバーは判定しない・事実だけ）に反する。埋めるのは 題（案件名）と 日付（本番日の初日、
  無ければ `event_start`）だけ。**手で打った値は上書きしない**（`titleTouched`/`dateTouched` で判定。
  `useCreateSheetPrefill.tsx` ほど作り込んだ状態機械ではなく、簡易な一度きりの先埋め）
- **一覧の絞り込みから来た新規作成は、これまでどおり案件/番組を選び直させない**
  （`ScheduleOwnerFields` の `locked` 表示。「フィルタで来ている時点で owner は決まっている」という
  `ScheduleListPage.tsx` 冒頭の決めごとを維持）
- **スマホの列フィルタを `SchedulePage.tsx` に持ち上げた**（`MobileTimeline.tsx` は非制御コンポーネントから
  制御コンポーネントに変更）。「項目を追加」は見出し（`PageHeader` の主ボタン）にあり、絞り込みの state は
  以前 `MobileTimeline` の中に閉じていたため、以前は常に先頭の列に追加していた
- **ひな形設定への導線を2か所に足した**: ① `nav.ts` に `scheduleTemplatesSection()`（`system_admin` のみ。
  `aiKnowledgeSection()` と同じ形——編集系 REST が `qsheet` の権限レベルではなく `role` を直接見ているため
  揃えた）② `ApplyTemplateDialog.tsx` の「ひな形を直す」リンク（`system_admin` のみ・`?template=<id>` で
  設定画面のそのひな形を開く）。ページの閲覧そのものはゲートしていない（`AppShell.tsx` の既存方針どおり）
  ——直URLを叩けば誰でも開けるが、編集ボタンはサーバーが 403 で止める
- **ひな形の項目に区分・基準（当日00:00から／本番開始から）・オフセット・尺・必須を追加**
  （`pages/schedule/TemplateItemsSection.tsx`・新設）。追加だけでなく**編集**もできるようにした
  （鉛筆→フォームに読み込み→「更新する」）。これまでは消して作り直すしかなかった
- **ひな形の名前・説明・拠点の変更と、複製ボタンを追加**（`TemplateMetaForm` 関数）
- **列の名前・部屋・色の変更は今回もできない**（サーバーに `PUT .../templates/columns/:id` が無いため）。
  画面には「消して作り直してください」と事実を書くだけにした
- 実ブラウザ確認（PC 1280 / 375px・Playwright）: 新規作成ダイアログで案件を選ぶと題・日付が先埋めされる
  ことを確認、`system_admin`/一般スタッフでナビ表示の出し分けを確認、ひな形の作成→列追加→項目追加
  （区分・基準・尺・必須）→鉛筆で編集→複製まで通し、スマホで列チップ（MC）を選んでから「項目を追加」を
  開くと列欄が MC になっていることを確認。横はみ出し 0px・JS エラー 0 件

#### 3-6. PR4 で足した一覧の絞り込み（B6）— サーバー変更なし

`GET /schedules` は拠点・状態・検索を前から受けていた（`schedule.service.ts`）が、画面が渡していなかった。

- **拠点**: `scheduleApi.listStudioRooms()`（PR1 で新設した読み取り専用 API）の場所だけを選択肢にした
  `<Select>`。Radix の `Select.Item` は `value=""` を許さない（「未選択」の特別扱いと衝突して例外を投げ、
  **一覧そのものが真っ白になる**）ため、`__all__` という印を使って空文字と往復させている
  （`ScheduleListFilters.tsx`。実ブラウザで最初に踏んで直した）
- **状態**: すべて／下書き／確定／保管の単一選択トグル。「すべて」を選んでいるときは**保管を持ち帰ってから
  画面側で除く**（サーバーには `status` を送らない）。「保管」を明示的に選んだときだけ `status=archived` を
  サーバーへ送る。これで §4-4 の「保管は既定の絞り込みから外れる。選べば見える」を実現している
- **検索**: `useDebounced`（`shared/src/client/hooks/useDebounced.ts`。300ms・`SheetListPage.tsx` と同じ形）
  で問い合わせの鍵だけを遅らせ、入力欄自体は遅らせない
- 一覧の `queryKey` に拠点・状態・検索を足したので、既存の `invalidateQueries({queryKey:["schedules","list"]})`
  （項目の追加・削除、列の変更、表の設定保存などから呼ばれる）は**接頭辞一致でそのまま効く**
  （変更不要だった）
- 実ブラウザ確認: 既定表示で保管中の1件が消えること、「保管」を選ぶとその1件だけ出ること、
  題での検索が絞り込むことを確認。横はみ出し 0px・JS エラー 0 件（拠点セレクトの空文字バグを
  実ブラウザで発見・修正した後）

#### 3-7. PR5 で足したドラッグ移動・下端リサイズ（B1）

「マルチエージェントで全て進めて」（2026-09-06 ご指示）で PR5〜7b の4本を並列実装した1本目。

- ポインタ追跡・ピクセル→分の換算は新設 `useScheduleItemDrag.ts` に切り出した。`timeline.minOf`
  （区切りごとに高さが違う非線形の縦軸）を2点評価して差分を分に変換する。5分スナップは
  **画面側だけ**（API は1分単位のまま。02-schedule.md 付録どおり）
- 「動くまで保存しない・Esc で取りやめ」（`useGridDrag.ts` の踏襲。しきい値4px未満はクリック扱い）
- 保存は既存の `PUT .../items/bulk` に1件だけ渡す薄いラップで呼び、`useItemCommitQueue` にそのまま
  乗せた——単発の項目編集と同じ `expected_updated_at` ローテーション・409 の `conflictedIds`
  反映（赤枠）を使い回し、新しい保存経路は作っていない
- ドラッグ中は新設 `onDragStateChange` 経由で `SchedulePage` の既存 `anySheetOpen` にドラッグ中
  フラグを足し、ポーリングを止める
- `SchedulePage.tsx` が400行の上限を超えたため、項目まわりのハンドラ群を新設
  `useScheduleItemActions.ts` に切り出した（ロジックは変えていない。後続 PR6 が同じ目的で
  別の切り出し `scheduleRefetch.ts` を作っていたため、統合マージで後者は削除し前者に一本化した）
- 実装中に見つけて直したバグ2件（実ブラウザで発見）: ① Esc キャンセル直後の素の `click` が
  編集ダイアログを開いてしまう（`moved` かつ cancelled でも click を1回消費するよう修正）。
  ② 横方向だけのドラッグ（列またぎ）が「動いた」判定に入らず保存されない
  （縦方向だけで見ていた判定を `Math.hypot(dx, dy)` に修正）

#### 3-8. PR6 で足した複製・印刷CSS（B5・B7）

- **複製**: 新設 `POST /schedules/:id/duplicate`（1トランザクション）。題・列・項目・表示時間帯・
  拠点・案件/番組/回を写し、**共有は写さない**・**状態は元の状態に関係なく常に `draft` に戻す**・
  台本リンクも写さない（1文書=1枠の制約回避）。既存の `requireAccessible` ヘルパーをそのまま
  再利用できたため、`apply-template` のような別 router ファイルは新設せず `schedules.routes.ts`
  への追記で済ませた
- **印刷CSS**: A3横・1日1枚。共通シェル（`shared/src/client/shell/`）自体には触れず、既存の
  属性セレクタ（`data-shell-topbar`/`aria-label`）だけで隠し、表側のボタンは既存実績パターンの
  Tailwind `print:hidden` で隠した。グリッドのスクロール枠を印刷時に `!important` で
  un-clip する必要があった（shared/CLAUDE.md の「base.css の外で上書きしない」注意は
  base層ルールの話で、これはユーティリティ層のクラス上書きのため対象外と判断）
- 統合検証（本ブランチへの4本マージ後、実ブラウザで再確認）: 印刷プレビューでヘッダー・
  サイドバー・操作ボタンが隠れ、表本体のみ A3 横で出ることを確認。既知の制限（未解決のまま）:
  列数が多い表は幅の縮小ロジックが無いため横に切れる可能性がある・印刷時の sticky
  （列見出し・時刻列）はブラウザ依存

#### 3-9. PR7a で足した台本の結び直し・逆引き・担当補完（B3・B8）

- **B3**: `ScheduleItemDialog.tsx` の「台本への橋」節を新設 `ScheduleItemDocLink.tsx` に切り出し、
  既存の `PUT .../items/:itemId/qsheet`（サーバー変更なし）で別の進行台本への結び直し・
  結びを外すを追加した。逆引き（新設 `GET /documents/:docId/schedule-items`）のクライアント
  wrapper を `scheduleApi.ts` に足し、進行台本一覧の `DocCard.tsx` に「スケジュール表: 9/12
  本番」バッジを追加した（リンクがある時だけ表示）
- **B8**: 新設 `AssigneeAutocomplete.tsx` が `useBufferedValue` を直接呼び、打鍵ごとの内部値で
  `GET /techops/share-users` の候補を絞り込む（FK は張らず文字列を差し込むだけ・自由入力は維持。
  02-schedule.md §11-4 の決定どおり）
- 実装中に見つけて直したバグ2件: ① `schedule-reverse.routes.ts`（逆引き）が案件メンバー自動
  共有（`canAccessSchedule`）と別のSQLを持っており、案件メンバーには表自体は見えるのに逆引き
  一覧にだけ項目が出ない食い違いがあった。② `ScheduleItemDialog.tsx` の draft リセットが `item`
  オブジェクト参照に依存しており、結び直し/外すのたびに親が新しい参照へ差し替える設計と
  衝突して、開いたまま操作すると打ちかけの題・時刻・担当が保存前に消える回帰があった
  （依存を `item?.id` に変更して解決）
- ⚠️ **統合検証で見つけた、この PR とは無関係の既存バグ**（PR5 の担当者が最初に発見・
  base ブランチだけでも再現することを `git stash` で確認済み。今回のマージ後の実ブラウザ
  検証でも独立に再現を確認した）: `ScheduleItemDialog.tsx` を**初めて**開いたときだけ「項目名」欄
  が空欄になる（2回目以降の開き直しは正しく表示される）。原因は Radix Dialog の
  `onOpenAutoFocus`（開いた瞬間に最初の入力欄へ自動でフォーカスする）と、`useBufferedValue`
  の「フォーカス中は外部からの値を取り込まない」設計（IME 保護のための意図的な仕様）が
  競合すること——ダイアログが開いてから `draft.title` が正しい値に更新されるまでの1レンダーの
  間に自動フォーカスが先に発火してしまい、正しい値が来ても `focused=true` のガードで
  取り込まれない（`onBlur` するまで空欄のまま）。**スケジュール表 PR5〜7 のどの変更にも
  起因しない**・`useBufferedValue.ts` は全社共通の IME 保護フックのため迂闊に直すと影響範囲が
  広い。表本体側の別画面でも同じ形（BufferedInput を最初のフォーカス対象に置く）を取っている
  箇所は同種の不具合を持ちうるため、別途まとめて調査・修正することを推奨する

#### 3-10. PR7b で足した予約から列コピー・AI事後差分（B9・B10）

- **B9**: `VenueColumnsDialog.tsx` の姉妹実装として、その日の `studio_bookings` を読む新設
  読み取り専用 API（`GET /techops/schedules/:id/booking-suggestions`。`qsheet` 権限のみ・
  営業情報である金額・備考・使用者メモは返さない）を足し、新設 `BookingColumnsDialog.tsx` で
  予約ごとに部屋を選ばせる。仮押さえも「仮」ラベルで見せ、**自動生成はしない**（§3-1 の作法どおり）。
  既に列がある会場は「すでに列があります」と表示してスキップできる
- **B10 半分A（拠点・種別）**: `EventPlanDialog.tsx` に拠点・種別（既存の
  `projects.project_category` と同じ語彙 `broadcast`/`recording`/`event`）の任意セレクトを足し、
  プロンプトと `segmentKey` の両方に反映した
- **B10 半分B（確定時の事後差分記録）**: 調査の結果、`qsheet_ai_proposals` の `applied_ids`/
  `applied_payload` ＋既存の「締め」機構（`settle.service.ts`）が相関の仕組みそのものだったため
  **新設のテーブル・列は不要だった**。表を「確定」にした瞬間に対象の①枠提案の **early 段だけ**
  を締めて事後差分を記録する `settleScheduleEarlyOnConfirm` を新設し、`PUT /schedules/:id`
  （`status:'fixed'`）から呼ぶ。**final 段の締めは意図的に対象外**——確定は本番当日よりずっと
  前に押されるのが普通で、ここで final まで締めると「当日の押し・巻き」を二度と拾えなくなる
  ため、final は今までどおり本番日翌日の夜間バッチに任せている
- ⚠️ **統合検証で見つけた、この PR とは無関係の既存バグ**: `POST /techops/ai/event-plan` は
  実DB検証で常に500（`could not determine data type of parameter $2`）を返す。原因は
  `proposals.service.ts` の `assertGenerationAllowed` にある
  `WHERE (document_id = ? AND ? IS NOT NULL) OR (schedule_id = ? AND ? IS NOT NULL)` ——
  `documentId`/`scheduleId` の一方が常に `null` で呼ばれるため、`? IS NOT NULL` 単体の `?` の
  型を Postgres が決められない。**新フィールド（拠点・種別）追加前の同一リクエストでも同じ
  500 が出ることを実測確認済み**——このスケジュール表 PR 群の変更が原因ではなく、①②③の
  AI 生成すべてに共通する既存の穴。スケジュール表の「AI で下書き」ダイアログ自体は正しく
  表示・入力できるが、実際に「下書きを作る」を押すと現状 500 になる。別途修正が必要

#### 3-11. 追加のご依頼（2026-09-06）: カードの見た目・ドラッグ中の時刻・横串

グリッドを実際に使ってのご指摘4点。「①左罫のアクセントをやめる ②引き伸ばしている最中に
何時何分になるのかを見せる ③カードの中に何時間何分かを出す ④横串（Excel のセル結合のような
列をまたぐ項目）を足せるように」。

- **①左罫のアクセントをやめた**: カードの `borderLeft`（列の色の 3px 線）を落とし、列の色は
  見出しの丸だけで示す。区分の色（背景）と列の色（線）が1枚のカードに同居していたのをやめた形
- **②ドラッグ中の時刻**: 吹き出し（`bg-foreground`／`text-background` の小さな帯）を
  **カードの外**に出す。移動中は上に `09:45–12:25 ・ 2時間40分`、下端リサイズ中は下に
  `終了 12:30 ・ 3時間30分`。カードの中に置くと**短い項目で切れて読めない**ため、
  動いている辺の隣に置いた。どちらの向きかは `useScheduleItemDrag` が新たに返す `dragKind` で決める
- **③所要をカードに常時表示**: 高さが 34px 以上なら2行目に `開始–終了 ・ 所要`、
  足りなければ題名の右に所要だけを添える
- **④横串**: `qsheet_schedule_items.span_cols`（migration 280）。
  **1 = この列だけ／N = この列から右へ N 列／0 = 全列**。「全列」を数（そのときの列数）で
  焼き込むと**列を1本足した日から横串が途切れる**ので、全列だけは数ではなく意味で持つ。
  読み方は `shared/src/schedule/span.ts`（`spanPlacement` が「右に足りなければ詰める」まで持つ）
  - 描画は列の中ではなく**列の外側に重ねた1枚**（`ScheduleSpanLayer.tsx`）。列は1本ずつ
    独立した `div` なので、その中のカードを右隣まで伸ばすと後から描かれる列に隠れ、
    列ごとのレーン割り当て（`scheduleLanes.ts`）にも巻き込まれる。**横串を列のレーンから
    外した**ので、横串が1本あるだけで他の項目が細くなることはない
  - 横串どうしが時間で重なったときは**縦に割る**（列の中の項目は横に割るが、横串は
    「どの列に掛かっているか」が本体なので、横に細くすると 2 列ぶんの横串が 1 列ぶんの幅で
    描かれ、意味が壊れる）
  - Excel 書き出しは**横方向のセル結合**で同じ形にする。幅の広い項目から先に置く
    （先に置いた側を勝たせる既存の衝突回避のままだと、1 列の項目が場所を取って横串が丸ごと
    消える）。項目一覧シートには「横串」列を足した
  - スマホの縦積みカードは、**どの列で絞り込んでいても横串を出す**（その列にも掛かっているため）

### 段C「考えてから」— **決定済み（2026-09-06）: 3件とも現状維持・実装しない**

担当を `users` に繋ぐ（`assignee_user_id`）／表→ひな形の逆流／ひな形の分類軸（拠点×式典・配信・公開収録）
の3件について、利用者に確認したところ**いずれも「現状維持」**（追加実装しない）という判断だった。
①担当は自由入力のまま（社外出演者を含む柔軟性を優先。「本人の当日の動き」横断表示は見送り）。
②ひな形の分類は拠点のみのまま（用途が違う場合はひな形の名前で区別する運用）。③表からひな形を作る
逆流機能は作らない（ひな形はひな形設定画面からのみ作る）。**コードの変更は無い**——§6 の表に判断を反映した。

## 4. UI/UX

### 4-1. 情報設計（置き場所）

| 単位 | 置き場所 | 理由 |
| --- | --- | --- |
| 一覧 `/techops/schedules` | ページ（既存） | 絞り込みを足すだけ |
| 表 `/techops/schedules/:id` | ページ（既存） | — |
| 項目 | `ScheduleItemDialog`（PC 中央／スマホ下端）既存 | 変えない |
| **表の設定** | 表の上の `Sheet size="md"`。題・日付・拠点・案件/番組・回・表示時間帯・状態・備考・共有・列・削除を 1 枚に | ページを増やすと戻ったとき表の位置を失う |
| **列の管理** | PC: 列見出しの `…` メニュー→`Sheet size="sm"`。並べ替えは見出しの ◀ ▶。スマホ: 設定シート内の「列」節（読む＋改名のみ） | 列はグリッドの文脈で触るもの |
| **共有** | 設定シート内の 1 節（ユーザー検索＋チップ） | 可視性が共有テーブル依存なので設定と同じ場所 |
| ひな形設定 | 既存ページ（PC 専用のまま）。導線は 左メニュー「設定」節（system_admin）＋適用ダイアログ末尾の「ひな形を直す」 | — |

### 4-2. ワイヤー

**(a) 一覧・PC** — イベント（案件・番組）ごとに束ね、その中を日付順に（2026-09-06 決定）
```
スケジュール表                                                   [＋ 新しく作る]
[今日から ▾] [拠点: すべて ▾] [状態: 下書き | 確定 | 保管 ] [🔍 題・案件]
──────────────────────────────────────────────────────────────────────────
GLS-2401 ○○社 授賞式                                    3 日 ・ メンバー 8 人
  09/11(木)  LOUNGE   前日仕込み                 項目 12 ・ 更新 9/6 14:20   [確定]
  09/12(金)  LOUNGE   本番当日                   項目 32 ・ 更新 9/6 14:20   [確定]
  09/13(土)  LOUNGE   撤収                       項目 5  ・ 更新 9/5 09:10   [下書き]
番組: △△ 公開収録                                        1 日
  09/14(日)  SKY      #12                        項目 0  ・ 更新 9/5 09:10   [下書き]
案件・番組なし                                           1 日
  09/20(土)  —        社内イベント               項目 3  ・ 更新 9/1 10:00   [下書き]
```
束の見出しは GLS 番号・案件名（番組は「番組:」）・日数・案件メンバー数（自動共有の対象・§3-2）。
束の並びは「その束の一番近い日付」順。`?project=`／`?program=` で来たときは束が 1 つになるだけ。
375px: `PageHeader primaryAction="新しく作る"`（共通シェルが下端固定）＋ `MobileFilterBar`。束は見出し行＋`Row stackOnMobile` の縦積み。
一覧に出すのは 日付・曜日・拠点・題・案件・状態・項目数・最終更新 の**事実だけ**（impl §5-2 の決め）。

**(b) 表・PC ヘッダー** — 今は 4 ボタン横並び（ひな形を適用／AI で枠を作る／Excel／項目を追加）
```
← 一覧   GLS-2401 ○○社 授賞式   (09/11)(09/12 ●)(09/13)        No.S-0042
         09/12(金) 本番当日   LOUNGE  [確定]
                                              [⚙ 表の設定]  […]  [＋ 項目を追加]
                              … = ひな形を適用 ／ AI で下書き ／ Excel に書き出す ／ 列を足す ／ 別の日として写す
```
2 行目の日付チップが「同じイベントの他の日」（§3-3）。1 日しか無いイベントではチップを出さない。
主=項目を追加、副=表の設定、残りはオーバーフロー。作る系（ひな形・AI）は空状態 (c) から入るのが正規経路なので通常時は畳む。

**(c) 空状態（列 0 本）**
```
┌────────────────────────────────────────────────────────────────┐
│ まだ列がありません。最初の列を決めると項目を置けます。              │
│  [ ひな形から ]     [ 会場を選んで列を作る ]     [ AI で下書き ]    │
│   拠点の標準構成      studio_rooms から複数選択     自由文から枠を提案 │
└────────────────────────────────────────────────────────────────┘
```
`NewEventChooser`（`client/src/contexts/production/pages/calendar/`）と同じ 3 択カード形式を techops 内に写す。
拠点が未設定なら「ひな形から」の下に「拠点を決めると拠点別のひな形だけが出ます」を 1 行。

**(d) 列見出し（PC・hover/focus で出る）**
```
┌ 会場 ─────────────────┐
│ ● LOUNGE            … │   … → 名前を変える／色／部屋を結ぶ（会場のみ）／左へ・右へ／削除する
│ 重なり 2 件            │   左端の ● = col.color（見出しと項目の左罫に使う）
└───────────────────────┘
```

**(e) 375px タイムライン**
```
┌ 09/12 授賞式 本番当日          [確定] ⚙ ┐
│ (すべて 32)(LOUNGE 12)(MC 6)(受付 4) … │ ← 列チップ・横スクロール
│ 09:00 ┃ 搬入・仕込み            LOUNGE  │
│ 2h    ┃ 仕込み ・ テクニカル             │
│ 11:00 ┃ リハーサル              LOUNGE  │
│ …                                       │
│ [          ＋ 項目を追加            ]    │ ← シェルの差し込み口（safe-area 込み）
└ ホーム / やること / 探す ───────────────┘
```
追加は「絞り込み中の列」を既定列にする。列 0 本のときは (c) の 3 択をカード縦積みで出す
（ひな形・AI は実行可、列作成は会場のチェックリスト）。

**(f) 表の設定シート**
```
表の設定                                                    ×
題   [授賞式 本番当日            ]   日付 [2026-09-12]
拠点 [LOUNGE ▾]   案件 [GLS-2401 ○○社 ▾] ／ 番組 [— ▾]   回 [#3 ▾]
表示時間帯 [08:00]〜[26:00]           状態 ( 下書き | 確定 | 保管 )
備考 [                                                        ]
── 共有 ──  案件 GLS-2401 のメンバー 8 人には自動で見えます
            ほかに見せる人: [＋ 人を足す 🔍]  (田中 ×)(佐藤 ×)     作成者: 寺井
── 列 ──    会場: LOUNGE, SKY ／ 支度: MC, 社長 ／ 運営: 受付   ※PC は列見出しから直す
── 削除 ──  [この表を削除する]   ※項目 32 件・共有 5 人の設定も消えます
                                                  [閉じる] [保存]
```

### 4-3. 操作の詳細

- **ドラッグ（段B）**: PC は移動＋下端リサイズだけ。`client/src/contexts/production/pages/calendar/useGridDrag.ts` の決めごとを踏襲（動くまで保存しない・Esc で取りやめ）。5 分スナップは**画面側だけ**、API は 1 分単位（02 付録）。保存は `bulkUpdateItems` に `expected_updated_at` を付ける
- **キーボード**: Esc=閉じる／取りやめ、Enter=保存、選択中の項目に Delete で確認へ
- **409**: 既存の `conflictedIds` をそのまま使う。ドラッグ由来の 409 は元位置に戻し、赤枠＋「最新を読み込む」
- **ポーリング停止**: 項目シート開放中（既存）に加え、ドラッグ中・設定シート開放中も止める。列の変更後は即時 refetch
- **削除の確認**: `confirmAction({ tone: 'danger' })` に一本化。復元 API が無いのでトースト式の undo は選ばない。
  ⚠️ `SchedulePage.tsx:129` の「client-techops は ConfirmHost 未設置」は**古い** — 共通シェル `shared/src/client/shell/AppShell.tsx:218` が `<ConfirmHost />` を持つので `window.confirm` はそのまま置き換えられる

### 4-4. 状態（下書き／確定／保管）の見せ方 — 1 案

状態は**人が付ける札**。ロックしない・サーバーは判定しない。
- **確定**にすると (1) 見出し右にバッジ「確定」 (2) 一覧の同じ位置に同じ幅で出る (3) 編集は従来どおり可能。項目シートの保存ボタン脇に「確定後の変更として保存」と 1 行出すだけ（保存は止めない）
- **保管**は一覧の既定絞り込みから外れる（「保管」を選べば見える）
- 「変更済み」の自動判定は持たない

**PR2 で (1)(3) を実装済**: 見出しのバッジ（下書き＝`secondary`・確定＝`success`・保管＝`outline`）は
状態が変わった瞬間に見える。編集はロックしない（`fieldset` を disabled にするのは**作成者・管理者以外**の
閲覧専用時だけで、状態そのものでは disable しない）。

**PR4 で (2) と「確定後の変更として保存」を実装済（2026-09-06）**: 一覧の各行にも表と同じバッジを出した
（`ScheduleBundleGroup.tsx`）。項目シート（`ScheduleItemDialog.tsx`）の保存ボタン脇に、表が「確定」の
ときだけ「確定後の変更として保存します」と 1 行出す（`scheduleFixed` prop・保存は止めない）。
保管の既定絞り込み除外は §3 B6 の一覧フィルタ（下記）で実装した——「すべて」を選んでいる間だけ
`status === 'archived'` を画面側で除く。「保管」を明示的に選ぶとサーバーの `status` フィルタで
ちょうど保管だけが返る。「変更済み」の自動判定は今回も持たせていない。

### 4-5. スマホの決めごと

44px タップ／主ボタンはシェル下端（`PageHeader primaryAction`。`fixed` を自前で書かない）／表なし縦積み／列チップに件数／
`env(safe-area-inset-bottom)` は `Sheet` が持つ／本文 13px 以上。

**PR2 で見直した点**: 当初案は「設定シートはスマホで題・状態・備考の3欄だけ編集可、列の並べ替えと共有は
PC 案内」だったが、実装してみると `FormDialog`/`Sheet`（下端シート）に乗せるだけで全欄が 375px でも
問題なく操作できた（実 Playwright 確認：横はみ出し 0px）。**制限を設けず、表の設定は PC・スマホとも
同じ全項目編集可にした。** PC 専用として残るのは列見出しからの並べ替え（グリッド自体が PC 限定）だけ。

### 4-6. 部品の対応（shared は変更しない）

| 提案要素 | 使う既存部品 |
| --- | --- |
| 一覧見出し・主ボタン | `shared/src/client/ui/pageHeader.tsx`（`DashboardHeader` から置換） |
| 一覧の絞り込み | `shared/src/client/ui/filterChips.tsx`／`shared/src/client-v4/mobileFilterBar.tsx`／`shared/src/client/ui/select.tsx` |
| 一覧の行 | `shared/src/client/ui/row.tsx`（`Row stackOnMobile`・`RowSlot`・`TableBadge`） |
| 表の設定・列の編集 | `shared/src/client-v4/sheet.tsx`・`shared/src/client-v4/formDialog.tsx`（`formGrid2`） |
| 3 択の空状態 | `shared/src/client/dashboard/EmptyState.tsx`＋`NewEventChooser.tsx` の形を techops 内に写す |
| 会場の複数選択 | `shared/src/client/ui/toggle-button-group.tsx`（`ToggleCard`） |
| 共有相手の検索 | `shared/src/client/ui/searchable-select.tsx` |
| 削除確認 | `shared/src/client/ui/confirm.tsx`（`confirmAction`・器は共通シェルに既設） |
| ドラッグ | `useGridDrag.ts` の方式を `client-techops/src/components/schedule/` に新設 |
| 文字入力 | `components/editor/BufferedInput.tsx`／`schedule/BufferedTextarea.tsx`（IME 必須・02 付録） |
| **新規 shared が要るもの** | 無し |

### 4-7. 文言（事実だけ・形容詞なし）

- ボタン: 新しく作る／項目を追加／表の設定／ひな形を適用／会場を選んで列を作る／AI で下書き／Excel に書き出す／列を足す／名前を変える／部屋を結ぶ／左へ・右へ／削除する／保存／最新を読み込む／PC で直す
- 空状態: 「まだ列がありません。最初の列を決めると項目を置けます。」「この列にはまだ項目がありません。」「この条件に合う表はありません。」
- 確認: 「この列を削除しますか？ 項目 N 件もいっしょに消えます。」「この表を削除しますか？ 項目 N 件・共有 M 人の設定も消えます。」
- 結果: 「保存しました」「列を足しました」「共有を更新しました（N 人）」「別のタブ／端末で更新されています。最新を読み込んでください。」

## 5. AI「枠を作る」とフィードバックループ（会社方針の確認）

すでに閉じている部分: ①生成を `recordAiOutput` で記録（`ai/generation-common.ts`）②取り込み時の人の修正を `sanitizeApplied`
（`ai/apply.core.ts`）が `fix`／`reject` の差分として `ai_corrections` に保存 ③傾向をプロンプトへ戻す
（`event-plan-ai.service.ts` の「前回までの傾向」）④月次レビュー（`monthly-review.service.ts`）。

**穴**: 取り込んだ**後**にグリッド上で直した分（当日の押し・巻きを含む）が差分に残らない＝成果指標が無い。
**最小の追加（段B・B10）**: 表を「確定」にした時点で AI 由来の項目（`applied_ids.items`）と現在値を比べ、
時刻・題の変更を `fix`、削除を `reject` として **1 回だけ** `recordCorrections` する。
生成条件を自由文 1 本から「拠点・種別」を選べる形にするのは、集計側の `type:|loc:` 区分（`ai-feedback.service.ts`）が既に待っている。

## 6. 利用者に決めてもらうこと（段C の着手条件）

[`02-schedule.md`](02-schedule.md) §12 のうちの状況:

| # | 問い | 状態 | 決まると動くもの |
| --- | --- | --- | --- |
| 1 | 1 日 1 枚のままでよいか。連日はどう見せるか | **決定（9/6）: 1 日 1 枚。イベントごとにまとめる** | B4・B5（§3-3） |
| 5 | 案件メンバーに自動で共有するか | **決定（9/6）: 自動で共有する** | A4（§3-2） |
| 2 | 深夜またぎは `25:30` か `翌 1:30` か | 未決 | 表示全般（今は 25:30） |
| 3 | ひな形の分類は「拠点」だけでよいか、「拠点 × 式典／配信／公開収録」か | **決定（9/6）: 拠点のみ・現状維持** | ひな形設定の作り |
| 4 | 担当を `users` に繋ぐか（「この人の当日の動き」横断が要るか） | **決定（9/6）: 繋がない・自由入力のまま** | B8 → C |
| 6 | 表からひな形を作る逆流が要るか | **決定（9/6）: 作らない** | C |
| 7 | 同時に何人で編集するか（5 人以上なら方式を見直す） | 未決 | 楽観ロック方式の継続可否 |

未決は #2・#7 の2件のみ。いずれも段A・段Bの着手を止めない（既定のまま進め、決まったら足す）。
#3・#4・#6（段Cの候補だった3件）は**いずれも「現状維持・実装しない」で決定した**ため、
段Cとしての新規実装は無い（§3「段C」参照）。

## 7. PR の切り方（案）

| PR | 中身 | 大きさ |
| --- | --- | --- |
| 1 | A1 列の管理＋A2 空状態の 3 択（`window.confirm`→`confirmAction` の置換も）— **済（2026-09-06）** | 中 |
| 2 | A3 表の設定シート＋A4 共有（**案件メンバーの自動共有・サーバー小**）＋削除 — **済（2026-09-06）**。実装中に見つけた `PUT /schedules/:id`（REST・MCP 双方）の部分更新バグ（未指定フィールドが毎回 null に戻る）も同時に修正した | 中 |
| 3 | A5 新規作成の拡張＋A6 スマホ追加＋A7 ひな形導線・項目編集 — **済（2026-09-06）**。サーバー変更なし | 中 |
| 4 | B4 イベントごとにまとめる（一覧の束ね＋ヘッダーの日付チップ）＋B6 絞り込み＋B2 状態 — **済（2026-09-06）**。サーバー変更なし | 中 |
| 5 | B1 ドラッグ・リサイズ — **済（2026-09-06）** | 中 |
| 6 | B5 複製（サーバー 1 本）＋B7 印刷 — **済（2026-09-06）** | 小 |
| 7a | B3 台本の結び直し・逆引き＋B8 担当補完 — **済（2026-09-06）** | 中 |
| 7b | B9 予約からコピー＋B10 AI 事後差分（サーバー 1 本） — **済（2026-09-06）** | 中 |

9/6 の決定で B4 が「日々の運用」から「一覧の基本の形」に上がったので、PR4 に前倒しした。
PR5〜7 は「マルチエージェントで全て進めて」（9/6 ご指示）で 4 本を並列実装した
（各自 git worktree・専用ポート・専用検証用 Postgres に分離）。PR7 は当初1本の予定だったが、
ファイル競合を避けるため 7a（台本・担当）／7b（予約コピー・AI）に分割した。実装後は
1本のブランチへ順にマージし、統合後にもう一度まとめて実ブラウザ検証をしている（§3-7〜§3-10）。

各 PR は 375px と PC の実ブラウザ確認（`npm run verify:ui`）を必須にする。
第1版の PR（#401・#402・#403・#407・#409）はレビュー 0 件・実ブラウザ未確認のままマージされている
（[`docs/reviews/codex-findings-v4.md`](../../../reviews/codex-findings-v4.md)）ので、同じことを繰り返さない。

## 8. 付録: 画面からできないこと 14 件（監査の原本・2026-09-06）

1. 列 0 本の表では何もできない — `SchedulePage.tsx:207`、`schedule.service.ts:126-140` は列を作らない
2. 列の追加・改名・色・幅・部屋・削除・並べ替えが一切できない — `scheduleApi.ts:62,66,70,74` の呼び出し元は `applyEventPlan.ts:41` の 1 箇所のみ
3. 表そのものの編集ができない — `scheduleApi.updateSchedule`（`:47`）呼び出し元ゼロ。`status`／`notes`／`doc_no` は表示すらされない
4. 表の削除ができない — `scheduleApi.deleteSchedule`（`:52`）呼び出し元ゼロ
5. 共有相手を設定できない — `setScheduleShares`（`:56`）呼び出し元ゼロ。可視性は `access.ts:38-50`
6. ドラッグ移動・リサイズができない — `ScheduleGrid.tsx:3-4`。`bulkUpdateItems`（`:91`）未使用
7. 既存台本への結び直し・解除ができない — `setDocumentLink`（`:101`）呼び出し元ゼロ。`ScheduleItemDialog.tsx:190` はリンク切れの表示のみ
8. スマホから項目を追加できない — `MobileTimeline.tsx:9-14` に追加系の props が無い。`:60` の案内が嘘
9. ひな形の項目の中身を設定できない — `ScheduleTemplateSettingsPage.tsx:130` は `column_id`＋`title` だけ送る。既定 `other/day/0/30`（`216_qsheet_schedule.sql:137-143`）
10. ひな形設定への導線が無い — `App.tsx:162` にルートはあるが `nav.ts:183,207` は一覧のみ
11. 拠点を設定できないのでひな形の拠点別絞り込みが死んでいる — `SchedulePage.tsx:258` が渡す `location_id` は常に null
12. 台本→枠の逆引き画面が無い — `schedule-reverse.routes.ts:20` に対応するクライアント関数が無い
13. 一覧の絞り込みが日付以降・案件/番組のみ — `ScheduleListPage.tsx:33-37`。`schedule.service.ts:72` は `LIMIT 200`
14. AI 提案の生成条件が自由文 1 本のみ — `EventPlanDialog.tsx:91-96`

データモデルの縛り（設計時に効くもの）: 時刻は 00:00 JST からの分 INTEGER（上限 2880）／列は venue・prep・ops の 3 グループで
`room_id` は venue のみ、`col_group` は reorder 経由でのみ移動／重なりは DB で禁止しない／状態は 3 値でサーバーに意味づけ無し／
共有＝作成者＋明示共有＋qsheet admin、不可視は 404／担当は自由 TEXT／台本への橋は 1:1 で `onair`・`rehearsal`・`recording` のみ／
ひな形は FK を張らない弱リンク・編集は `system_admin`／`studio_bookings` とは結線なし。
