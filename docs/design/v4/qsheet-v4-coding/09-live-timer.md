# 計時・視聴者（計時LIVE の統合）— ミニアプリとしての実装設計

> **2026-08-21 作成。** 計時LIVE（`client-live/`・ベースパス `/live/`）を制作資料に取り込み、
> **Qシート／スケジュール表／収録・配信設定と並ぶミニアプリ**にするための設計です。
>
> **モックアップ**: https://claude.ai/code/artifact/aa1b9c08-ffb4-4cb1-8ce9-c6aba576734a
> （運用画面・表示画面・タイマー管理・スマホの4枚）
>
> ⚠️ **機能は1つも落としません。** 利用者の指示は「現在の計時LIVEの機能はいいが、
> UI/UX がネイティブ風になっていないのと、導線を制作資料の思想に合わせ直したい」です。
> **変えるのは導線と見た目だけ**で、タイマーの意味論・視聴者取得・公開URLは現行のままです。

---

## 0. 決めたこと

1. **セッション一覧（`/live/`）をやめる。** 入口は**案件のミニアプリ一覧**に一本化する。
   いまの「セッション」は案件と二重に存在しており、GLS 紐付けが任意なのもそのため。
2. **`liveops_programs` は残す。** 視聴者取得の設定（YouTube URL・Jstream LPID・Zoom・Teams）を
   持つ実体として意味があるので、**案件から 1:1 で自動的に用意する**形に変える。
3. **公開URL `/live/display/:timerId` は 1 文字も変えない。** 会場モニター・OBS のブラウザソース・
   配布済みのURLがそこで成立している。**無認証のまま**にする。
4. **タイマーの意味論を変えない。** カウントダウンのみ・0 を過ぎたら `+MM:SS`・
   サーバーが正で 100ms ごとに配信・リセットは尺ごと 0 に戻す・±調整は計時中も効く。
5. **画面が無かった2つを出す。** 黄色に変わるしきい値（DB は 60 秒固定）と、
   運用画面がどのタイマーを出すか（いまは常に 1 本目）。
6. 権限区画は **`liveops` のまま**にする（migration 210 で7区画に統合済みのため増やさない）。

---

## 1. 今ある機能（落とさないための棚卸し）

実装（`client-live/src/`・`server/src/contexts/liveops/`）を読んで確認した事実です。
**「そのまま」は挙動を変えない、「作り直す」は見た目だけ変える、の意味です。**

### 1-1. 画面

| 画面 | 今のURL | 扱い |
| --- | --- | --- |
| セッション一覧 | `/live/` | **やめる** → 案件のミニアプリ一覧が代わり |
| ダッシュボード | `/live/program/:programId` | **作り直す** → `/live/`（案件の文脈で開く） |
| タイマー管理 | `/live/program/:programId/timers` | **作り直す** → `/live/timers` |
| 番組設定 | `/live/program/:programId/settings` | **作り直す** → `/live/sources`（名前を変える。§4-2） |
| 設定（APIキー） | `/live/settings` | **そのまま**（全社共通の設定なので案件の外） |
| **表示画面** | `/live/display/:timerId` | **URL・無認証ともそのまま。**中身だけ作り直す |

### 1-2. タイマー（`socket.ts` / `TimerControls.tsx`）

| 機能 | 扱い |
| --- | --- |
| カウントダウンのみ（カウントアップ・ラップ・時刻表示は無い） | そのまま |
| 0 を過ぎたら `+MM:SS` で超過を出す | そのまま |
| フェーズ `idle` / `countdown` / `yellow`（残り60秒）/ `red`（0以下） | そのまま。**しきい値だけ選べるように**（§3-1） |
| 赤のときの点滅・文字のグロー・背後の放射グラデーション | そのまま（モックで再現済み） |
| サーバーが正・100ms ごとに `state` を配信・クライアントは補間しない | **そのまま**（切断時に固まるのが正しい挙動） |
| プリセット 1/3/5/10/15/20/30 分（**計時中は変えられない**） | そのまま |
| カスタム（分 0–99・秒 0–59） | そのまま |
| ±10秒 / ±30秒 / ±1分（**計時中も効く**） | そのまま |
| 開始（計時中・0以下では不可）／停止（一時停止の意味）／リセット | そのまま |
| **リセットは設定した尺ごと 0 に戻す**（プリセットには戻らない） | そのまま。**画面に明記する**（誤解が多い） |
| 計時中のリセットは確認ダイアログ | そのまま |
| 上限 5999 秒（99:59） | そのまま |
| 誰でも操作でき、全員に反映される（多人数操作） | そのまま |
| サーバー再起動後、`timer:join` で DB から復元（停止中の経過も戻る） | そのまま |
| 権限が無い人には操作を出さず「管理者権限が必要です」と出す | そのまま |

### 1-3. 視聴者カウンター（`useViewer.ts` / `proxy.routes.ts`）

| 機能 | 扱い |
| --- | --- |
| YouTube（複数URL・**URLごとにラベル**・内訳表示） | そのまま |
| Jstream（LPID） | そのまま |
| Zoom（ミーティングとウェビナーを**合算**） | そのまま |
| Teams（Graph の通知で push・**サーバー再起動で 0 に戻る**） | そのまま |
| 取得は**この画面を開いている間だけ**（閉じると止まる） | そのまま。**画面に明記する** |
| 取得対象の ON/OFF（OFF は取りに行かない・合計からも外す） | そのまま |
| OFF は取り消し線、未設定は淡色で「未設定」 | そのまま |
| 取得対象の ON/OFF を端末に覚える | そのまま |
| 取得のたびに記録し、**表示画面はその記録を読む** | そのまま。**画面に明記する** |
| 推移のグラフ（合計は破線） | そのまま |
| ログ（100件・色分け・**画面を閉じると消える**） | そのまま |
| 最終更新の時刻 | そのまま |
| APIキー未設定の警告帯 | そのまま |

### 1-4. 設定（`SettingsPage.tsx`）

| 機能 | 扱い |
| --- | --- |
| 4プラットフォームのキー（表示/非表示の切替・末尾4桁マスク） | そのまま |
| **「他ユーザーのキーを共有利用中」の表示** | そのまま（消すと事故る） |
| 接続テスト（成功時は**応答時間**、失敗時は理由） | そのまま |
| 取得方法の折りたたみ手順（4プラットフォーム分） | そのまま |
| 取得間隔（5〜300秒） | そのまま |
| 設定の書き出し・読み込み（JSON・**キーは含まない**） | そのまま |

### 1-5. 表示画面（`TimerDisplayPage.tsx`）— いちばん壊してはいけない画面

| 機能 | 扱い |
| --- | --- |
| **無認証**（`App.tsx` がパスを見て認証を通さない専用ルーターを立てる） | **そのまま。この仕組みごと残す** |
| 3つのレイアウト（タイマーのみ／6:4分割／視聴者のみ） | そのまま |
| 縦画面では上下に積む | そのまま |
| **白ベースで表示**（黒白の反転） | そのまま |
| 歯車は右下・薄い（`text-white/30`）・背景クリックで閉じる | そのまま |
| 7つの切替を**タイマーごと・端末ごと**に覚える（既定は視聴者OFF・タイマーON） | そのまま |
| 下端の進捗バー・「設定 MM:SS」・点滅ドット | そのまま |
| 番組が紐づいていないときの注記 | そのまま |
| `?programId=` で上書きできる | そのまま |
| 視聴者数は**記録を15秒ごとに読む**（自分では取りに行かない） | そのまま |

### 1-6. いま画面が無い機能（＝この統合で出す）

| # | 実装にあるもの | いまの状態 | この設計 |
| --- | --- | --- | --- |
| 1 | `liveops_timers.warning_threshold_sec` | API は受けるが**UI が無く 60 秒固定** | **タイマーごとに選べるように**（30秒/1分/2分/5分） |
| 2 | 運用画面が使うタイマー | **常に `timers[0]`**。2本目以降は選べない | **どれを出すか明示的に選ぶ** |
| 3 | `liveops_timers.viewer_overlay_program_id` | 表示画面は見るが UI が無い | **今回は出さない**（§6-4 で確認） |
| 4 | Singular Live 連携（サーバーにAPIあり） | 画面が無い | **触らない**（別途判断） |

---

## 2. 置き場所と導線

### 2-1. ミニアプリとして並べる

```
案件 60周年 記念式典（GLS-A012）
├── スケジュール表
├── Qシート（香盤表）
├── 収録設定 ／ 配信設定
└── 計時・視聴者          ◀ ここ
```

⚠️ **`shared/src/production/miniapps.ts`（[`01-app-structure.md`](01-app-structure.md)）に
1件足すこと。**

**名前**: 画面上は「**計時・視聴者**」にします。「計時LIVE」はアプリ名でしたが、
制作資料の中のミニアプリになるので、何をするものかで呼びます
（⚠️ `docs/wording.md` に追記が要ります）。

### 2-2. セッションと案件の関係

いまは `liveops_programs`（セッション）が主体で、`project_id` は**任意**でした。
統合後は**案件が主体**になります。

- **案件のミニアプリを初めて開いたとき、その案件の `liveops_programs` を1件自動で作る**
  （名前は案件名から。`project_id` を入れる）
- 既存の行はそのまま使う（`project_id` が入っているものは案件から引ける）
- **`project_id` が無い既存セッション**は、案件に紐づかない資料単体（`doc_no`）として扱うか、
  移行時に人が案件を選ぶ。→ §6-1 で確認

⚠️ **`liveops_programs` を消さない。** 表示画面が `program_id` で視聴者の記録を引いており、
タイマーとも紐づいています。消すと配布済みの表示URLが壊れます。

### 2-3. URL

| いま | これから | 理由 |
| --- | --- | --- |
| `/live/` （セッション一覧） | **`/live/`（案件の文脈で運用画面）** | 一覧をやめるため |
| `/live/program/:programId` | `/live/`＋`?project=` | 案件から入るので programId を URL に出さない |
| `/live/program/:programId/timers` | `/live/timers` | 同上 |
| `/live/program/:programId/settings` | `/live/sources` | 「番組設定」は視聴者の取得元設定なので名前を変える |
| `/live/settings` | **そのまま** | 全社共通のキー設定 |
| **`/live/display/:timerId`** | **そのまま** | **配布済み。1文字も変えない** |

⚠️ 旧URL（`/live/program/:id`）は**リダイレクトを残す**こと。ブックマークされています。

---

## 3. 画面

### 3-1. 運用画面（PC）

左に**タイマー**、右に**視聴者**。モックのとおり。

- タイマーは大きな数字＋フェーズ色＋下端の進捗バー。**「設定 MM:SS」と点滅ドット**を残す
- プリセットは計時中に淡くして、その理由を1行で書く
- **リセットの意味を注記に書く**（「設定した尺ごと 0 に戻ります。プリセットには戻りません」）
- 視聴者は取得対象のチップ（ON=塗り／OFF=取り消し線／未設定=淡色）→ YouTube の内訳 →
  Jstream・Zoom・Teams・合計 → 推移 → ログ
- **「この画面を開いている間だけ取りに行く」「表示画面はその記録を読む」を注記に明記**
  （現行の最大の落とし穴。閉じると本番中の数字が止まる）

### 3-2. 表示画面（公開URL）

**この画面が本番の出力です。** 歯車の7つの切替で3レイアウトが実際に変わります。

- 既定は黒・タイマーのみ。歯車から視聴者を足すと 6:4 分割、タイマーを消すと視聴者のみ
- 赤フェーズ: 文字の点滅・グロー・背後の放射グラデーションの脈動
- **白ベース**で全体反転
- 切替は**この端末にだけ**残す旨を書く

### 3-3. タイマー管理

- タイマーは案件に**何本でも**（本番尺・休憩・リハ）
- タイマーごとに**表示画面のURL**（コピー／開く）
- **黄色に変わるしきい値**をタイマーごとに選ぶ ← 新
- **運用画面に出すタイマー**を選ぶ ← 新
- 「URLの作り直しはできない。外したいときはタイマーごと消す」と明記

### 3-4. スマホ

**会場で手元から操作する**のが主目的。表示は見るだけ。

- 大きな数字 → 開始/停止（幅広）＋リセット → ±調整（46px）→ プリセット → 視聴者（見るだけ）
- 計時中のリセットは**下から出るシートで確認**
- 「表示画面のURLをコピー」を置く（現場で人に渡す）
- 取得の開始/停止と設定変更は PC に案内する

---

## 4. データモデル

**既存テーブルをそのまま使い、足すのは2列だけ。**

```sql
-- 運用画面に出すタイマーを案件ごとに1本決める
ALTER TABLE liveops_programs
  ADD COLUMN main_timer_id TEXT REFERENCES liveops_timers(id) ON DELETE SET NULL;

-- 案件から1:1で引けるようにする（既存は NULL 可のまま）
CREATE UNIQUE INDEX liveops_programs_project_key
  ON liveops_programs (project_id)
  WHERE project_id IS NOT NULL AND deleted_at IS NULL;
```

- `warning_threshold_sec` は**既にある**ので DDL は不要。API と画面を足すだけ
- `liveops_snapshots` / `liveops_settings` / `liveops_teams_subscriptions` は**変更なし**

⚠️ **一意インデックスを張る前に、`project_id` が重複している既存行が無いか確認すること。**
いまは1案件に複数セッションを作れてしまうため、重複していたら移行時に統合が要ります。

---

## 5. UI/UX で変えること（機能ではない）

| 変える | 今 | これから |
| --- | --- | --- |
| シェル | 計時LIVE 独自のサイドバー（オフキャンバス） | 制作資料の**上辺バー＋ミニアプリのセグメント** |
| 背景 | 白 | **グレーのキャンバスに浮く角丸カード** |
| 上辺バー | 不透明 | **半透明＋ブラー** |
| 切替 | ボタン | **セグメンテッドコントロール** |
| 確認 | `window.confirm` | **下から出るシート**（スマホ）／ダイアログ（PC） |
| 一覧の削除 | ホバーで初めて出る | **常に見える**（触れる端末で出ないため） |
| 数字 | — | `tabular-nums` を徹底（桁が揺れない） |

⚠️ **トーストは `NoticeBar` に置き換えない。** 放送中の切断通知を含むため
（`07-onair-roles.md` と同じ判断）。

---

## 6. 利用者に確認すべきこと

| # | 論点 | なぜ聞くか |
| --- | --- | --- |
| 1 | **案件に紐づいていない既存セッションをどうするか** | いまは GLS 紐付けが任意。移行時に案件を選んでもらうか、資料単体として残すか |
| 2 | **1案件に複数セッションがある既存データはあるか** | 1:1 の一意インデックスを張れるかがこれで決まります |
| 3 | 権限区画を `liveops` のままにしてよいか | 制作資料の中に入るので `qsheet` に寄せる案もありますが、区画を増やす/変えるのは影響が大きい |
| 4 | `viewer_overlay_program_id`（別番組の数字を出す）を画面に出すか | 実装にはありますが使われている形跡が無い。出さないと決めてよいか |
| 5 | Singular Live 連携をどうするか | サーバーに API があるが画面が無い。この統合では触らない前提 |
| 6 | **視聴者の取得をサーバー側に移すか** | いまは画面を開いている間だけ。本番中に閉じると表示画面の数字が止まります。直すなら別の設計が要ります（大きい） |
| 7 | 「計時LIVE」→「計時・視聴者」の改称でよいか | `docs/wording.md` に追記します |
