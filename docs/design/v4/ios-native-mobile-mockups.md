# iOS ネイティブ風モバイルモックアップ — 引き継ぎ

**これはコード変更ではありません。** Claude Design のキャンバス（アートボードを
プレビューできるデザイン成果物）へのリンクと、実装に進むときに知っておくと
早い決めごとをまとめたメモです。実際の画面は作っていません — 次にこの方向で
実装するときの起点として使ってください。

## 見るところ

**「1モックアップ＝1キャンバス」にしてあります**（1つのキャンバスに全画面を
詰め込むとスマホの Safari で繰り返しクラッシュしたため。経緯は下の
「なぜ分割したか」を参照）。画面ごとに別々のアーティファクトです。

### スマホの実画面（実装をそのまま見た目だけ作り直したもの）

| 画面 | キャンバス |
| --- | --- |
| トップ | https://claude.ai/code/artifact/78c55f96-6db9-41f1-a670-8db75e36ec2a |
| 案件管理ダッシュボード | https://claude.ai/code/artifact/36dffb49-ed62-467e-8577-876dd1558289 |
| 案件一覧（カード） | https://claude.ai/code/artifact/67a64e7b-4648-4ffd-9136-e1ff4e7b2651 |
| やること | https://claude.ai/code/artifact/c900b2da-6427-4ba5-b42f-dc6ad574bc88 |
| 案件詳細・概要タブ | https://claude.ai/code/artifact/e35777ed-7481-4e49-9d04-cce025277b53 |
| 案件をつくる（1/3段目） | https://claude.ai/code/artifact/742df6a6-ac1b-41a8-ab95-25464f45eaa5 |
| 入金の確認（財務） | https://claude.ai/code/artifact/4fd7d0c8-c174-4234-8f47-76f0849fef21 |
| 探す（下タブ3本目） | https://claude.ai/code/artifact/9a5f2439-c6b5-492f-b64b-a36177254b76 |
| 取引先（財務・仕入先／パートナー） | https://claude.ai/code/artifact/843467b3-472c-4504-abca-b2485d8eb0af |

### 方針転換の初期案（実画面の再現ではない・確認前の叩き台）

| 画面 | キャンバス |
| --- | --- |
| 見積・請求のスマホ版試作（案件管理・BillingMobile） | https://claude.ai/code/artifact/0beca473-6a2d-4f66-b212-5127009aa6ab |
| 売上のスマホ版試作（財務・RevenueMobile） | https://claude.ai/code/artifact/f1bcb17e-d3d1-42a2-be90-804b2fb34f00 |
| 仕入のスマホ版試作（財務・PurchaseMobile） | https://claude.ai/code/artifact/b986c07e-510e-4f2d-8a19-573a6d86ba79 |
| 販管費のスマホ版試作（財務・SgaMobile） | https://claude.ai/code/artifact/f523e42a-ab56-4ad3-9d82-a857d27a75a2 |
| ガントチャートのスマホ版試作 | https://claude.ai/code/artifact/8dc57eda-4aa2-4179-be94-9ac08b24ee41 |
| 案件台帳のスマホ版試作 | https://claude.ai/code/artifact/0d2999cf-a751-4c51-ae61-656c7b3ed864 |
| 案件を直すのスマホ版試作 | https://claude.ai/code/artifact/671a9a1f-931d-4f13-94e2-f58572c76ef9 |
| 標準工程テンプレートのスマホ版試作 | https://claude.ai/code/artifact/dea98f6b-f241-41c6-a3f0-52b60886f9fa |
| 料金表のスマホ版試作 | https://claude.ai/code/artifact/9c6484b2-279a-408e-a852-6e06d384febd |
| 費用を分け合うグループ（一覧）のスマホ版試作 | https://claude.ai/code/artifact/407570e2-507e-4c1e-86d8-a635c06535e6 |
| 費用を分け合うグループ（詳細）のスマホ版試作 | https://claude.ai/code/artifact/a850bd9c-ea5d-4702-bcbf-0c874e729131 |
| 営業活動記録のスマホ版試作 | https://claude.ai/code/artifact/696d7f8e-5c67-42c5-a0b9-208929e5d9d4 |
| お客様の詳細のスマホ版試作（⚠️ v4化した後の投機。PC版とは前提が違う） | https://claude.ai/code/artifact/4980c2c3-55b9-4daa-a6fc-f91feadfdfd2 |

### PC専用画面（デスクトップサイズでそのまま再現）

| 画面 | キャンバス |
| --- | --- |
| 見積・請求（全案件・案件管理） | https://claude.ai/code/artifact/602f83b2-14aa-458f-a8f9-05e1189d6240 |
| ガントチャート | https://claude.ai/code/artifact/04c440ee-2d83-4c4b-bf4e-3606ace92df2 |
| 案件台帳 | https://claude.ai/code/artifact/9fba6ac5-1bb7-4199-a9d6-dd6d0f62ee34 |
| 案件を直す | https://claude.ai/code/artifact/839291ca-939a-4211-9167-6755ba28283b |
| 標準工程テンプレート | https://claude.ai/code/artifact/edbbceba-1d92-4d4e-aff6-ecdb77865c9f |
| 料金表 | https://claude.ai/code/artifact/61c799fb-8c49-405d-b5c7-897fbbf67a4d |
| 費用を分け合うグループ（一覧） | https://claude.ai/code/artifact/e57a892c-9725-48f1-b551-16a6ead5d963 |
| 費用を分け合うグループ（詳細） | https://claude.ai/code/artifact/e0100dd1-d86b-41c1-871a-c82a3e7fa1a6 |
| 営業活動記録・営業レビュー | https://claude.ai/code/artifact/72e10678-d58a-409c-8083-51c680e3fea0 |
| お客様の詳細（⚠️ pre-v4。下記「お客様の詳細だけ特別扱い」参照） | https://claude.ai/code/artifact/9db0a717-9d80-4a9a-89f7-72cc496b79ef |

これで**案件管理の PC専用画面10本（`pcOnlyScreens.ts` の `CLIENT_PC_ONLY` に載っている
案件管理ぶん）すべて**にPCデスクトップ版ができました。

### 共通部品（実画面ではない・組み合わせ見本）

https://claude.ai/code/artifact/c5ac7e90-0679-4cec-ad13-bd781994a528
（NavBar・TabBar・ListRow・Sheet の4部品と、組み合わせ見本1枚）

- プライベート公開です。閲覧には共有が必要な場合があります（アーティファクトの共有メニュー）
- **キャンバス上の付箋（アノテーション）は置いていません**（ご指摘により削除済み・
  画面が隠れて邪魔になるため）。実装との対応関係と簡略化した点は、この文書の
  各表とセクションにまとめてあります
- この HTML はキャンバスエディタを内蔵した「早期プレビュー」で、あとから
  claude.ai/design の機能が追いついても自動更新はされません
- スマホの実画面は NavBar・TabBar の2部品だけをそのキャンバス内に同梱しています
  （`<dc-import>` の都合上、部品を使う画面はその部品もアートボードとして
  持つ必要があるため）。見えているのは基本的にメインの画面1枚だけです

### なぜ分割したか

最初は1キャンバスにまとめていましたが、詰め込む画面が増えるにつれて
スマホの Safari で繰り返しクラッシュするようになりました。

1. まず「スマホ画面」と「PC専用画面」の2キャンバスに分けましたが、
   スマホ画面側だけでも14〜15枚（画面本体＋NavBar・TabBar等の共通部品）に
   なっており、それでもクラッシュの懸念が残る規模でした
2. ユーザーから「ページ多すぎてクラッシュする、1モックアップ1キャンバスで」
   という明示の指示があり、**画面ごとに別々のアーティファクトへ分割**しました
   （本メモはその結果です）

→ **今後さらに画面を足すときも、新しい画面ごとに新しいキャンバスを作ること。**
既存のキャンバスにアートボードを追加しない。

## 何のためのモックアップか

`docs/v4-plan.md` の大前提（モックが正）とは**別の探索**です。既存の v4 モックアップ
（`mockups/`）を置き換えるものではなく、**スマホ画面の見た目・手触りを iOS ネイティブの
作法に寄せると何が変わるか**を、実装済みの案件管理まわりの画面で試したものです。

- ブランド（GMOブルー `#005bac`・LINE Seed JP）と情報設計（`mobile.md` の決めごと —
  下タブ3本・スワイプに意味を持たせない・終わらせるのはシートで、等）は変えていません
- 変えたのは見た目と手触りの層だけです（角丸・余白・ナビゲーションの形・
  リストの罫線の引き方・押した手応え）

すべて実装（各コンポーネントのソース）の文言・並び・判定をそのまま使い、
見た目だけ作り直しています。対応する実装ファイルは下の表のとおりです。

| 画面 | 実装ソース |
| --- | --- |
| トップ | `HomePage.tsx` / `home/Greeting.tsx` / `home/MobileAiBar.tsx` / `home/AppTiles.tsx` |
| 案件管理ダッシュボード | `salesDashboard/MobileSalesDashboard.tsx` |
| 案件一覧（カード） | `projectList/ProjectCards.tsx` / `stages.ts` |
| やること | `taskList/MobileTaskList.tsx` / `taskList/state.ts` |
| 案件詳細・概要タブ | `projectDetail/DetailHeader.tsx` / `OverviewTab.tsx` |
| 案件をつくる（1/3段目） | `projectNew/MobileNewProject.tsx` / `RequiredFields.tsx` / `IntakeRail.tsx` |
| 入金の確認（財務） | `closing/MobileCollect.tsx` |
| 探す（下タブ3本目） | `SearchPage.tsx` / `search/shortcuts.ts` |
| 取引先（財務・仕入先／パートナー） | `CounterpartyPage.tsx` |
| 見積・請求（PC専用・デスクトップ再現） | `BillingListPage.tsx` / `billing/EstimateRows.tsx` / `billing/InvoiceRows.tsx` |
| 見積・請求のスマホ版試作（⚠️叩き台） | `BillingListPage.tsx` / `billing/EstimateRows.tsx`（実画面の再現ではない） |
| 売上のスマホ版試作（⚠️叩き台） | `RevenueListPage.tsx` / `ledger/LedgerRows.tsx`（実画面の再現ではない） |
| 仕入のスマホ版試作（⚠️叩き台） | `PurchaseListPage.tsx` / `ledger/LedgerRows.tsx`（実画面の再現ではない） |
| 販管費のスマホ版試作（⚠️叩き台） | `SgaListPage.tsx` / `ledger/LedgerRows.tsx`（実画面の再現ではない） |
| ガントチャート（PC専用・デスクトップ再現） | `TaskDashboardPage.tsx` / `DashboardGantt/DashboardGanttView.tsx` |
| ガントチャートのスマホ版試作（⚠️叩き台） | 同上（実画面の`MobileGanttView`は未到達のため参考にしていない） |
| 案件台帳（PC専用・デスクトップ再現） | `ProjectLedgerPage.tsx` / `projectLedger/{types,LedgerTable,LedgerCells,IntegrityPanel}.tsx` |
| 案件台帳のスマホ版試作（⚠️叩き台） | 同上（実画面の再現ではない） |
| 案件を直す（PC専用・デスクトップ再現） | `ProjectFormPage.tsx` / `projectNew/{RequiredFields,MoreFields,fields}.ts` |
| 案件を直すのスマホ版試作（⚠️叩き台） | 同上（実画面の再現ではない） |
| 標準工程テンプレート（PC専用・デスクトップ再現） | `flow/FlowTemplatePage.tsx` / `flow/FlowTaskRow.tsx` / migration 178 |
| 標準工程テンプレートのスマホ版試作（⚠️叩き台） | 同上（実画面の再現ではない） |
| 料金表（PC専用・デスクトップ再現） | `PricingListPage.tsx` / `pricing/{CategoryCard,LocationTabs,PricingDialogs}.tsx` |
| 料金表のスマホ版試作（⚠️叩き台） | 同上（実画面の再現ではない） |
| 費用を分け合うグループ 一覧・詳細（PC専用・デスクトップ再現） | `ProjectGroupListPage.tsx` / `ProjectGroupDetailPage.tsx` / `projectGroup/{AllocationEditor,useAllocation}.ts` |
| 費用を分け合うグループ 一覧・詳細のスマホ版試作（⚠️叩き台） | 同上（実画面の再現ではない） |
| 営業活動記録・営業レビュー（PC専用・デスクトップ再現） | `ActivityLogPage.tsx` / `activityLog/{ActivityRows,Filters,UpcomingPanel,kinds}.ts` |
| 営業活動記録のスマホ版試作（⚠️叩き台） | 同上（記録タブのみ。分析3タブは省略。実画面の再現ではない） |
| お客様の詳細（PC専用・pre-v4のまま再現） | `CustomerDetailPage.tsx`（v4未着手・下記「お客様の詳細だけ特別扱い」参照） |
| お客様の詳細のスマホ版試作（⚠️叩き台） | 同上（v4化した後を想定した投機。PC版とは前提が違う） |

案件管理は **PC専用の10画面すべて**にPCデスクトップ版＋スマホ版試作ができました
（見積・請求＋今回の9画面）。**財務の残り3画面（財務ダッシュボード・受け取った書類・
取り込み）・カレンダー・日常業務・機材管理・設定はまだ手つかず**です。
財務8画面のうちスマホで開けるのは「入金の確認」「取引先」の2つだけで、残り6画面
（財務ダッシュボード・売上・仕入・販管費・受け取った書類・取り込み）は実装が明示的に
PC専用と判断しています（`pcOnlyScreens.ts`）。**この PC専用の位置づけ自体について、
下の「方針の転換」を参照。**（売上・仕入・販管費は方針転換を受けたスマホ版試作が
上の表にありますが、実画面はまだ PC専用のままです）

### 番号を付けていない理由

途中まで①②③…と通し番号を付けていましたが、この製品には**画面の番号が2系統**
あります（`client/CLAUDE.md` の案件管理8画面の通し番号と、`docs/design/v4/mobile.md` の
スマホ13端末枠の通し番号）。同じ「④」が「タスク一覧」と「案件をつくる」の**両方**を
指していて衝突したため、途中から番号を捨てて画面名だけにしました。
この文書の表にも②③のような番号が部分的に残っています（見出しの一部として） —
実装と突き合わせるときは番号ではなく画面名かソースファイル名で照合してください。

## 新しく足した共通部品（iOS 風の語彙）

実装は共通部品を `shared/` に集約する方針（`shared/CLAUDE.md`）です。この探索で
「これは複数画面で使い回せそうだ」と分かった単位を、キャンバス上のコンポーネントとして
切り出してあります（「共通部品」キャンバス参照）。実装に落とすときの部品分割の参考にしてください。

- **NavBar** — モバイル上部の帯。`mode="logo"` （☰／ロゴ／ベル／本人。実際の全画面で
  使っているのはこちらだけ）と `mode="title"` （大見出し／通常見出しの2状態＋戻る矢印。
  今回はどの実画面でも使っていない — 現状の実装は「戻る」をナビバーに置かず、
  一覧に戻るボタンを本文側に置く形なので、これは将来 iOS 的なプッシュ遷移を
  取り入れるならの提案として残しています）
- **TabBar** — 下タブ3本（ホーム／やること／検索）＋どれも選択しない状態。
  タブ選択中はアイコン形状を変えず色と太さだけ変える簡略化にしてあります
  （アイコンの実装コストを半分にする意図）
- **ListRow** — 角丸カードの一覧行（アイコン・値・スイッチの3パターン）の見本
- **Sheet** — 「終わらせるのはシートで」の実物（下から出るカード・ドラッグハンドル・
  1件を確定するフッターボタン）。やること画面の1件から確定する例で作っています

いずれも**このキャンバスの中だけの実装**（`.dc.html`）で、React コンポーネントには
なっていません。実装するときは `shared/src/client-v4/` 配下に新規で起こすか、
既存の `shell/` 配下のコンポーネントを見た目だけ差し替えることになります。

## モックの中のデータについて

案件管理の画面をまたいで同じ架空の案件・お客様（「ABCプロダクト株式会社」
「ABCプロダクト 新商品発表会」・GLS-A004）を使い回し、画面間の一貫性を
見やすくしています。**実在のお客様・案件ではありません。**GLS番号は
実装の採番（`sequence.service.ts`：`GLS-A004` のようにハイフンなし・3桁）に
合わせた書式です。

## 方針の転換（2026-08・ご意向）

⚠️ **「PC専用画面はスマホ対応しない」という現状の実装ポリシーを、今後は変えていく意向**
がユーザーから示されました。**これはコード変更ではなく、意向の記録です**（この更新でも
`pcOnlyScreens.ts` を含む実装コードは一切触っていません）。

- 対象は特定のアプリに限らず、**`pcOnlyScreens.ts` の `CLIENT_PC_ONLY` に載っている
  全画面**（案件管理・財務・カレンダー・設定・プロジェクト管理の合計20本前後）
- `pcOnlyScreens.ts` の冒頭コメントは現状のPC専用の理由（「お客様」「お金」
  「設定・権限」「ガント」「3列レビュー」等、`docs/design/v4/mobile.md` の
  `spNotOnPhone` に基づく）を書いています。方針転換後もこの「なぜPC専用だったか」の
  記録自体は消さず、**「以前はPC専用だった理由」として残しつつ、スマホ対応版の設計へ
  進む**形になる見込みです
- **実際に着手するとき**は、画面ごとに「金額の桁揃え」「取り消せない操作」等の
  PC専用だった理由を1つずつどう解決するかを設計する必要があります（例: 台帳の表は
  カード化する・危険な操作は確認ダイアログを厚くする、等）。一律の答えは無いので、
  画面ごとにユーザーに確認しながら進めること

### 初期案：見積・請求のスマホ版（BillingMobile）

方針転換を受けて、「見積・請求（全案件）」（PC専用画面としてデスクトップ版を
作った画面）のスマホ版を試作しました。⚠️ **これは実装済みの画面の再現ではなく、
確認前の叩き台**です（上の表でも実画面の一覧とは別枠にしてあります）。

- PC専用だった理由（`pcOnlyScreens.ts`）は「金額・期日・状態が横に並ぶ表で、
  桁を読み違えると請求を間違えます」。この案は**表をやめてカード化**することで、
  読み違えの原因（横並びの数字列）そのものを無くす方向で解決しています
- 合計金額を単独の帯（大きい太字）で最上部に独立させ、他の数字と並べていません。
  カード1件＝1見積で、状態・版バッジ→案件名→お客様・表題→期限（左）／金額
  （右・大きい太字）→担当・帳票の順に置き、**1つの数字が1箇所にしか出ない**
  構成にしました
- 絞り込みは案件一覧と同じ「1行に畳んでシートで開く」形を踏襲
- 紙面の都合で見積タブのみ。請求タブ（検収／入金を押して記録する形）は未着手
- この方向でよければ、他のPC専用画面（財務の残り6画面・案件管理の残り9画面等）にも
  同じ考え方（表の理由を1つずつ解決する）で広げていくことになります

### 初期案：財務の台帳3画面のスマホ版（RevenueMobile / PurchaseMobile / SgaMobile）

同じ考え方を財務の台帳画面にも広げました。売上・仕入・販管費（③④⑤）は
`ledger/LedgerRows.tsx` を共用する同じ構造の一覧で、PC専用の理由もBillingMobileと
同じ「金額の桁を縦にそろえて読む表なので、畳むと桁が比べられません」です。

- **売上**: 合計を単独の帯で強調し、カード1件＝1売上（状態バッジ・GLS番号→
  案件名→請求先→計上月／金額の順）。状態バッジ（未請求／発行済／入金済）は
  実装どおり押すと「請求・入金」画面へ移るだけで、ここでは状態を変えられません
- **仕入**: 変動原価／固定原価のタブ（実装どおり）＋確定／仮のバッジ。
  取引先モック（`CounterpartyPage.tsx`）で使った架空の仕入先名を使い回し、
  画面をまたいだ一貫性を出しています
- **販管費**: 案件に紐づかないので GLS番号・案件名の列はなし。精算番号
  （無ければ「番号待ち」）→詳細→支払先→計上月／金額の順。状態バッジは
  按分中／固定／都度の3つ（実装の `sgaState()` のとおり）

⚠️ **見つけて直した誤り（解消済み）**: 最初の版では GLS番号を実装と違う書式
（`GLS-A-00119` のようにハイフンを1つ多く・5桁）で書いていました。実装の採番
（`sequence.service.ts`）は `GLS-A004` のように**ハイフンなし・3桁**です。
RevenueMobile / PurchaseMobile を直したあと、**それより前に作っていた案件管理・
財務のスマホ実画面（トップ以外の案件管理6画面・共通部品のSheet）に残っていた
古い書式もまとめて直し、全アートボードで書式をそろえました**（対応: TaskList /
Collect / Search / ProjectList / ProjectDetail / Sheet の6ファイル）。
併せて、販管費モックの支払先に実在企業名（NTTコミュニケーションズ株式会社）を
誤って使っていたのも見つけて架空の名前に差し替えています。

### 案件管理の残り9画面（PC＋スマホ両方・この回で完了）

「案件管理を全て完了させましょう」「PC＋スマホ版の両方」というご指示を受け、残っていた
9画面すべてに **PCデスクトップ版＋スマホ版試作**の2キャンバスずつ（計18キャンバス）を
作りました。それぞれ実装ソースを直接読んで文言・並びを再現したうえで、
PC専用理由（`pcOnlyScreens.ts` の `why`）に対する具体的な解決策をスマホ版で提示しています。

| 画面 | PC専用の理由（実装のexact text） | スマホ版の解決の方向 |
| --- | --- | --- |
| ガントチャート | 横に長い時間軸なので、この幅では1週間ぶんも入りません | 日単位グリッドをやめ、案件カード＋期間比率のミニバーに置き換え |
| 案件台帳 | 列が20あり、まとめて書き換える画面。戻せない操作を指で押すことになる | 読み取り専用カード化＋整合性チップは開放、一括編集だけPC案内に |
| 案件を直す | 入力欄が40以上あり、電話が入ると書きかけが残る | 自動下書き保存（提案）とセクションのアコーディオン化 |
| 標準工程テンプレート | 型を変えると以後すべての案件に効くので、落ち着いて触る画面 | 閲覧（アコーディオンで開いて読む）は開放、追加・削除・並べ替えはPC案内に |
| 料金表 | 相手ごとの単価が横に並ぶ表で、1桁違うと見積の金額が変わる | 1品目＝1カードで定価とグループ内価格を縦積み、編集はあえてPCのまま |
| 費用を分け合うグループ（一覧・詳細） | 複数の案件にまたがる金額の割り当てで、全体を見ながら決める必要がある | 一覧・閲覧はカード化して開放、「分け方」編集だけ縦積み入力＋sticky合計チェック帯（提案） |
| 営業活動記録・営業レビュー | 記録タブに加え、3列を並べて打合せの場で映す分析タブを統合している | 記録タブはカード化して開放、分析3タブはPC案内に |
| お客様の詳細 | v4でまだ作り直していない画面。取引の履歴と担当者を並べて読む形 | 下記「お客様の詳細だけ特別扱い」参照 |

⚠️ **お客様の詳細だけ特別扱い。** この画面は実装がまだ v4 化されていない（`framer-motion`・
生の shadcn `Card`・生の Tailwind パレット色を使う pre-v4 の画面だと実装コードを読んで確認済み）。
そのため**PC版のモックも意図的に v4 トークンではなく、実装と同じ pre-v4 の見た目（角丸・
色・タイポ）で再現しています**（他の8画面のPC版はすべて v4 トークンで統一）。一方
**スマホ版試作は「まだ作り直していない今の画面をそのまま縦積みにする」のではなく、
「v4 化した後にスマホ対応したらどうなるか」を先取りした投機**にしてあります —
PC版とスマホ版で前提（v4化前／後）が違う唯一の画面です。実装するときは
**まずこの画面自体の v4 作り直しが必要**（スマホ対応はそのあと）という順序である点に注意。

その他の設計判断・懸念（各エージェントの自己レビューより抜粋）:
- **ガントチャート**: スマホ版のミニバーは全案件共通の日付軸に対する相対位置。
  案件ごとに実施日レンジが大きく異なる実データでの見え方は未検証
- **案件台帳**: 既定表示9列のうち「次の期限」ではなく「最後の動き」が実装上の既定列
  （このドキュメント冒頭の実装ソース説明とは表記が違う場合、実装のほうを正とする）
- **費用を分け合うグループ**: 詳細PC版は売上明細を1件展開し、その中に「分け方」
  （AllocationEditor）を任意の比率・金額不一致（赤字・保存不可）の状態で具体的に再現
- **営業活動記録**: 実装は現在すでに画面全体がPC専用（記録タブも含む・
  `pcOnlyScreens.ts` で確認済み）。スマホ版試作は「記録タブだけ先に開放したら」という
  投機で、実際の実装ではタブごとの状態切替の作り込みが要る

## 次にやるとしたら

1. ユーザーにこの方向性（方針転換とBillingMobileの初期案、および今回の案件管理9画面）
   で進めてよいか確認する
2. 良ければ、財務・カレンダー・日常業務・機材管理・設定の残りのPC専用画面にも
   同じ調子（PCデスクトップ再現＋PC専用理由に応えるスマホ版試作）で広げる。
   **新しい画面ごとに新しいキャンバスを作ること**（「なぜ分割したか」参照 —
   既存キャンバスにアートボードを足さない）
3. 実装に落とすと決まったら、`pcOnlyScreens.ts` の該当行を外す・画面ごとの
   モバイル対応を実装する、という実コードの作業に進む（本メモの範囲外）。
   お客様の詳細だけは**先にv4作り直しが必要**（上記参照）
4. 良ければ NavBar / TabBar / Sheet を `shared/src/client-v4/` の実コンポーネントとして
   起こし、既存のスマホ画面から差し替えていく（1画面ずつ・`docs/v4-plan.md` の
   「モックが正」原則にならい、差し替えの判断はユーザーに確認しながら進める）
