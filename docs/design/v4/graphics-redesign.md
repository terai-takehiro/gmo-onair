# テロップCG — 機能と UI/UX のゼロベース再設計（2026-09-06）

**状態: 設計・モックアップ（コードは1行も変えていない）。** 実装は別 PR で段ごとに進める（§11）。
モックは Claude Design のキャンバス（クリックできる試作・PC 4枚＋スマホ 2枚＋地図 1枚）:

- **https://claude.ai/code/artifact/bf5e785b-0075-459b-ad21-86410c419fde**（作業ファイルは
  [`mockups/native/telop-cg/`](mockups/native/telop-cg/)。書式は
  [`production-v4-native-mockups.md`](production-v4-native-mockups.md) の ①′〜⑩ と同じ）

> **この文書が置き換えるもの**: [graphics.md](graphics.md)（2026-08-30 の初版設計）のうち
> **利用者に見せる語・画面の数・本番の操作モデル**（§1・§3・§4 の UI 部分）。
> 出力の契約（§7）・部品をパッケージ化する思想（§2 の「部品」）・日本語組版と数値
> （[graphics-design-specs.md](graphics-design-specs.md)）・アワード演出の移植内容
> （[graphics-awards-migration-plan.md](graphics-awards-migration-plan.md)）は**そのまま生きる**。
> 直すのは「器の中身」ではなく「器の見せ方」である。

## 0. 経緯

- 旧「リアルタイムCG」（`client-awards/`）は 2026-09-06 に**「凍結」から「移行中」へ整理**した
  （[v4-plan.md](../../v4-plan.md) の「用語」）。後継はこのミニアプリで、旧アプリには新しい機能を足さない
- 後継として 2026-08-30〜09-01 に作った現行のテロップCG（`client-techops/src/pages/graphics/`・68ファイル
  10,383行＋サーバー 3,523行）は、**送出の器としては動くが、「何をする道具か」が画面から読めず、
  導線も分からない**というご指摘を受けた（2026-09-06）。この文書はその作り直し

## 1. 結論（先に答え）

**テロップCG は「番組で出すテロップを、台本の順に並べておき、本番でボタンひとつで出す」道具。**
それ以外の何かに見えた瞬間が負け。

利用者に見せる語は **4つ** だけにする。

| 語 | 意味 | 旧実装の語 |
| --- | --- | --- |
| **テロップ** | 画面に出す1枚（種類＋文言＋見た目） | ページ |
| **種類** | ネーム／題字／一覧／ティッカー／カウントダウン／スコア／投票／ランキング／速報／サイド | 部品 |
| **出す順** | この番組の送出リスト。コーナーで区切れる。進行台本と同期できる | 送出リスト・呼出番号・sortOrder |
| **見た目** | 番組ごとの配色・書体・ロゴ | テーマ＋テンプレートの一部 |

画面は **PC 4枚・スマホ 2枚・出力 1枚**。いまの 9 ルートから減らす。

```
ハブ（JourneyPage）の「テロップCG」タイル
 └ ① テロップ一覧（準備）      /techops/graphics/:ownerKey            ← ホーム。出す順＋右に編集
     ├ ② 本番モード            /techops/graphics/:ownerKey/live       ← 暗い全画面。OA / NEXT / TAKE / CLEAR
     ├ ③ テロップを作る・直す   ①の右パネル（種類を選ぶ → 入力 → プレビュー）
     ├ ④ 設定（4タブ）         /techops/graphics/:ownerKey/settings   ← 出力URL／見た目／同時に出せないもの／連携
     ├ ⑤ 依頼（スマホ）        /techops/graphics/:ownerKey/request    ← ディレクターが文言だけ書く
     └ ⑥ 一覧（スマホ・閲覧＋緊急CLEAR）  /techops/graphics/:ownerKey  ← 同じURL。見るだけ＋いま出ているもの＋緊急時は消せる（§12-4）
 出力（OBS）                      /techops/graphics/output/:projectId    ← 変更なし
```

本番の動詞は **3つ**（TAKE＝出す／CLEAR＝消す／進める）。旧アプリの実績ある「NEXT → TAKE → CLEAR、
TAKE すると NEXT が自動で次へ」に戻す（§8）。

## 2. いまの何が問題か（実装を読んだ証拠）

「UI/UX が最悪・何をしたいのか分からない・導線が分かりにくい」を、原因のレベルまで分解した。

| # | 症状 | 原因（実装上の事実） |
| --- | --- | --- |
| 1 | 開いた画面が何の道具か読めない | ハブの上段に**操作が7個平積み**（テーマ Select・部品ライブラリ・送出コンソールへ・発注フォーム・名簿から一括生成・旧CGから移行・ページを作る）。主・従・年1回の管理操作が同じボタン形で並び、その下に統計バー・自動退出ルールの編集表・未作画の発注・ページ表・出力URLカードが縦に積まれる。**設定と準備と本番の入口が1画面に混ざっている** |
| 2 | 覚える語が多すぎる | 利用者に見える概念が **17**: CGプロジェクト・ページ・呼出番号・スロット（6種）・部品（10種）・テンプレート・テーマ・校正（3段）・発注（3状態）・名簿・退出ルール・演出SE・外部連携・PGM/PVW・5動詞・オールクリア・続き。プロ用送出機（Viz Trio／Chyron）の語彙をそのまま画面に出した |
| 3 | 導線が分からない | 左メニューにテロップCGのサブ項目が**無い**（`nav.ts` の `GRAPHICS_RE` は ownerKey を拾うだけ）。設定系（演出SE・外部連携）は**ハブ → 部品ライブラリ → テンプレート管理 → 当該**の3階層下に埋まり、`/techops/top` からは5クリック。しかも `GRAPHICS_RE` が `sounds`・`interactive-link` を含まず、その2画面ではサイドバーが「トップ」1項目に崩れる（バグ） |
| 4 | 戻る先がばらばら | 戻りリンクの文言が画面ごとに「テロップCG ハブへ戻る」「テロップCGへ戻る」「ページと送出リスト」の3種類。移行ツールだけヘッダーの作りが違い `navigate(-1)` で戻る |
| 5 | 本番画面が重い | 送出コンソールに PGM／PVW／操作卓（番号呼出＋5動詞）／スコア±／ランキング操作／スロットレーン／ページ一覧の**7区画**。「TAKE できるのは PVW だけ」の規律のため、1枚出すのに「行を PVW へ → TAKE」の2操作。旧アプリは「NEXT → TAKE」で TAKE 連打で回っていた |
| 6 | 死んだ・嘘の UI | 部品ライブラリの「新しい部品をリクエスト」はトーストを出すだけ。カードの「24番組」「11番組」「4 フィールド」はモックの固定文字列で実データと無関係。`ranking` 部品はカタログに存在しない（10種中9枚） |
| 7 | 名前の揺れ | 「題字＋肩書」（カタログ）と「題字」（正式ラベル）、「スコアボード」と「スコア」、「サイド」と「サイドスーパー」が同居 |
| 8 | スマホで文脈を失う | PC 専用 7 画面の逃げ先が全部 `/techops/top`。番組のハブへ戻さない |
| 9 | 発注の一覧が二重 | ハブの「未作画の発注」と発注フォーム画面の「自分が出した発注」が別画面・別クエリで並存 |
| 10 | 出力画面が孤島 | 出力URLカードからしか到達できず、逆に戻る手段も無い（認証なしは正しいが、配り方が設定にまとまっていない） |

根っこは1つ: **「テンプレート → ページ → 送出リスト」「PGM/PVW・5動詞」というプロ送出機の型を、
2〜3人で回す GMO のスタジオにそのまま持ち込んだ**こと。型の知識は正しいが、画面に出す語と操作の数を
現場の規模に合わせて畳んでいなかった。

## 3. 使う人と3つの場面

| 場面 | 誰が | どこで | やること |
| --- | --- | --- | --- |
| **準備**（本番の数日前〜直前） | ディレクター／AD | スマホ・PC | 出したい文言を決める（依頼）。台本に書いたテロップをそのまま出す順にする |
|  | 技術（作画・送出担当） | PC | 種類を選んで文言を入れ、見た目を当て、確認済みにする。OBS に出力URLを貼る |
| **本番** | 送出担当（技術 or AD） | PC・配信卓の横 | NEXT を確かめて TAKE。終わったら CLEAR。スコア・投票・ランキングは行の中で進める |
| **出力** | OBS／スイッチャー | ブラウザソース | 公開URL・1920×1080・透過。人は触らない |

GMO の現場は**専任の CG オペレーターがいない**（技術か AD が兼ねる）。だから本番画面は
「読めば分かる」より「**見れば分かる**」を優先し、覚える操作を3つに絞る。

## 4. 概念モデル

### 4-1. 利用者に見せる4語（§1）と、隠す語

| 隠す語 | どうするか |
| --- | --- |
| CGプロジェクト | 番組を開いた時点で自動に作る。画面に出さない（いまも `resolve` が自動作成している） |
| スロット（フルスクリーン／下部／サイド／ティッカー／時計／速報） | **種類が位置を決める**ので利用者は選ばない。内部の排他制御（1枠1枚・自動で下ろす）はそのまま。本番画面の帯では「いま出ているもの」として、位置名ではなく**サムネイルと文言**で見せる |
| テンプレート（部品の初期値＋公開フィールド） | 利用者の語からは外す。**「複製」と「前の番組から見た目と種類をコピー」**に姿を変える（DB の `graphics_templates` はそのまま流用できる）。ディレクターが文言だけ触れる＝依頼フォームが担う |
| 校正（未完成／未確認／確認済） | **「確認済み」のチェック1つ**にする。「未完成」は必須の文言が空なら自動でバッジが付く（人が3段を手で回さない） |
| 呼出番号 | 出す順の番号（1, 2, 3…）を作成時に一度だけ自動で振る。テンキー呼出はそのまま使える（§8）。**並べ替えても番号は変わらない**（§12-3。口頭で番号を呼ぶ運用と事故なく両立させるため） |
| 部品ライブラリ（カタログ画面） | 廃止。**「種類を選ぶ」ステップ**（③）がその役目。使用番組数などの固定文字列は出さない |
| 発注（テロ原） | 「依頼」と呼ぶ。一覧は1か所（①の「依頼 N件」）に統一 |
| 退出ルール | 設定の「同時に出せないもの」表に畳む。種類ごとの既定を持たせ、普段は触らない |
| PGM／PVW | **OA／NEXT**（旧アプリの用語辞典どおり。TAKE／CLEAR は現場語として残す — [terminology-review §5](../../reviews/2026-09-05-terminology-review.md)） |

### 4-2. 「部品をパッケージ化して組み合わせる」思想はどこへ行くか

graphics.md §2 の思想（部品＝描画＋フィールド＋アニメ＋専用操作＋データの口を1袋に）は**実装の構造として残す**。
変えるのは見せ方だけ:

- 利用者が触るのは「種類」。1種類＝1部品パッケージ。**種類を足す＝部品を開発する**（社内 Web 開発者の仕事）
- 「この番組のテロップシステム」＝**見た目（テーマ）＋使う種類の既定値**。これを「前の番組からコピー」できることが
  「次の番組の立ち上げが速くなる」の実体。部品の組み合わせキャンバスは作らない（`layers` の重ね合わせは
  ランキングなど必要な種類の内部実装に留める）

## 5. 画面と導線

### ① テロップ一覧（準備）＝ホーム `/techops/graphics/:ownerKey`

**この画面を開いた瞬間に「番組名・出す順・次にやること」の3つが読めること**が要件。

- 上段: 番組名（GLS・回・日付）。右に **「本番モード」**（主ボタン・赤系ではなく通常の主ボタン）、
  **「＋ テロップ」**、**「設定」**。それ以外のボタンは置かない
- その下に1行の**状態帯**: 「12件 ／ 確認済み 10 ／ 未確認 2 ／ 依頼 2件」。未確認・依頼は押すと絞り込む。
  右端に**「台本に追従」の切替**（既定 OFF・§9-3・§12-2）を置く。台本から取り込んだ番組だけに出す
- 左（主）: **出す順**。コーナー見出し（オープニング／ゲストトーク／…）で区切り、行＝
  `番号｜種類アイコン｜サムネイル（96×54）｜文言（1行目）＋補足（2行目）｜確認チェック｜⋯`。
  ドラッグで並べ替え（**番号は変わらない**・§12-3）。行を押すと右パネルに開く（**別ページに飛ばない**）
- 右（従・幅 420）: 選んだテロップの**本物のプレビュー**（出力と同じ描画・16:9）＋入力欄（種類のフィールドから自動生成・
  文字数上限つき・英語欄は「英語も出す」を開いたときだけ）＋「確認済み」トグル＋複製・削除
- 取り込みの入口は上段の「＋ テロップ」のメニューにまとめる:
  **台本から取り込む**（§9）／**名簿から作る**（Excel）／**依頼から作る**／空から作る
- 最下段に折りたたみの「OBS に貼る URL」（設定④と同じカードの縮約。ここで済む人が多い）
- スマホで開いたときは ⑥（閲覧）になる。「PC で編集」の案内だけ置き、`/techops/top` へは飛ばさない

### ② 本番モード `/techops/graphics/:ownerKey/live`（PC 専用・暗い配色・全画面）

§8 に詳述。**同じ「出す順」を、操作の形を変えて見せる**だけ。準備画面と別のリストを持たない。

### ③ テロップを作る・直す（①の右パネル）

1. **種類を選ぶ**: 10 種のカード（絵つき・位置のピクトグラム・一言）。ここが旧「部品ライブラリ」
2. **入力**: 種類のフィールド（`pageFields.ts` から自動生成）。プレビューは打つたびに更新し、
   選択中の欄を絵の上でハイライト（Viz Trio 方式・graphics.md §3 のまま）
3. **どこに入れるか**: 「いま選んでいる行の次」が既定。コーナーを選べる
4. 保存 → 行が増え、右パネルはそのまま次の入力に使える

### ④ 設定 `/techops/graphics/:ownerKey/settings`（PC 専用・タブ4つ・1画面）

| タブ | 中身 | 旧実装 |
| --- | --- | --- |
| 出力URL | メイン合成（透過）／確認用（黒背景）／英語版。コピー・QR・「OBS での貼り方」3手順 | `OutputUrlCard` |
| 見た目 | テーマ4種のプレビュー付き選択、ロゴ、**前の番組からコピー** | `ThemePicker`＋`graphics_templates` |
| 同時に出せないもの | 「◯◯が出たら △△ を自動で消す」の表。種類ごとの既定入り | `SlotExitRulesEditor` |
| 連携 | インタラクティブ（別VPS）の接続、演出SE、**旧リアルタイムCGから移す**（system_admin だけに出す） | `InteractiveLinkSettingsPanel`・`RankingSoundsPanel`・`AwardsMigrationPage` |

### ⑤ 依頼（スマホ）`/techops/graphics/:ownerKey/request`

いまの `RequestFormPage` の構造を保ち、欄を3つに絞る: **文言**（必須）／**出すタイミング**（自由記述に加えて
**台本の項目から選ぶ**）／**種類**（任意・分からなければ空）。送ると①の「依頼 N件」に入る。
自分の依頼と状態（一覧に入った／却下）はこの画面の下に出す（一覧の二重化をやめる）。

### ⑥ 一覧（スマホ・閲覧＋緊急CLEAR）

①と同じ URL。出す順と確認状態、本番中は**いま出ているもの**（OA）が上に出る。編集（文言・並べ替え）はしない。
**「いま出ているもの」の各行にだけ「消す」ボタンを置く**（§12-4・確認なしの1タップ）。
スマホから本番の並びを組んだり NEXT を送ったりはできない——できるのは「消す」ことだけ
（本番の運転は PC の②が主。スマホは PC が触れない・見えない場面での最終手段）。

### 左メニュー（`nav.ts`）

テロップCG の文脈にいるときは、サイドバーの「テロップCG」の下に **一覧／本番モード／設定** の3つを出す
（いまは何も出ない）。`GRAPHICS_RE` を `settings|live|request` に合わせ、戻りリンクの文言は
「**テロップ一覧**」の1種類に統一する。**段Aで実装済み**（`ShellNavItem` はインデント付き子項目を
持たないため、テロップCGの中を見ているときだけ「テロップCG」1本を同じ深さの3本に展開する形にした）。

## 6. 機能の棚卸し（残す／畳む／統合／足す）

| 機能 | 判断 | 行き先・理由 | 既存コード |
| --- | --- | --- | --- |
| 出力URL（公開・透過・`?bg`/`?audio`/`?lang`） | **残す** | 運用資産。契約は変えない | `GraphicsOutputPage` |
| 種類 10（描画・フィールド・アニメ） | **残す** | 部品パッケージそのもの | `outputParts*.tsx`・`pageFields.ts` |
| 見た目（テーマ4） | **残す** | 「見た目」タブへ | `ThemePicker`・`telopTheme.ts` |
| 名簿から一括生成（Excel・列判定・dry-run） | **残す** | 「＋ テロップ ＞ 名簿から作る」へ | `RosterImportDialog` |
| 依頼（テロ原・スマホ） | **残す・統合** | 一覧を1か所に | `RequestFormPage`・`RequestQueueSection` |
| 校正3段 | **畳む** | 「確認済み」1つ＋自動の「文言が空」バッジ。DB の `proof_state` は残してよい | `ProofBadge` |
| スロット退出ルール | **畳む** | 設定「同時に出せないもの」。種類ごとの既定を足す | `SlotExitRulesEditor` |
| テンプレート管理画面 | **畳む** | 画面は廃止。「複製」「前の番組からコピー」の実装に転用 | `TemplateManagerPage`・`graphics_templates` |
| 部品ライブラリ画面 | **廃止** | 「種類を選ぶ」ステップへ。固定の数値・ダミーボタンは消す | `PartLibraryPage`・`partLibraryData.ts` |
| 呼出番号（テンキー） | **残す・脇役に** | 本番モードの番号欄。番号は作成時に固定・並べ替えても変わらない（§12-3） | `useConsoleKeyboard` |
| 5動詞（スタンバイ／TAKE／続き／OUT／次へ）＋オールクリア | **3動詞に** | TAKE／CLEAR／進める。NEXT は行を押す。全部消すは CLEAR の長押し or 確認つき | `ConsoleControls` |
| PGM／PVW プレビュー | **残す（語を OA／NEXT に）** | 本物の描画は最終防衛線 | `ConsolePreview` |
| スロットレーン | **残す（見せ方を変える）** | 位置名の表ではなく「いま出ているもの」の帯（サムネ＋文言＋経過＋CLEAR） | `ConsoleSlotLanes` |
| スコア±・投票 出題→締切→開票・ランキング 段階発表・Final Pitch | **残す（置き場を変える）** | 別パネルではなく**OA 中の行の中**に出す | `ScoreQuickAdjust`・`RankingControlPanel`・`useConsoleContinue` |
| 外部インタラクティブ連携・演出SE | **残す** | 設定「連携」タブへ | `InteractiveLinkSettingsPanel`・`RankingSoundsPanel` |
| 旧CG（awards）からの移行ツール | **残す（隠す）** | 設定「連携」タブの中・system_admin だけ。API の GET も manager 以上に締める（棚卸し指摘） | `AwardsMigrationPage` |
| **台本から取り込む・台本に追従** | **足す** | §9。この道具の存在理由を画面に出す唯一の機能 | 新規（`qsheet_documents` の `telop` ブロック） |
| **コーナー見出し** | **足す** | 出す順を台本の構造で読めるように | `graphics_pages.section`（新規列） |
| **インライン編集（右パネル）** | **足す** | ダイアログ往復をやめる | `PageFormDialog` を右パネル化 |
| **スマホ閲覧** | **足す** | 文脈を失わせない。編集はしない | 新規（①の narrow 版） |
| ハブの統計バー・「オンエア中のスロット N件」 | **廃止** | 状態帯（確認済み／未確認／依頼）に置き換え | `GraphicsHubPage` |

## 7. 言葉の対応表（旧 → 新）

| 旧（いまの画面） | 新 | 備考 |
| --- | --- | --- |
| ページ／ページを作る | テロップ／＋ テロップ | |
| 部品／部品ライブラリ | 種類／種類を選ぶ | 「題字＋肩書」→「題字」、「スコアボード」→「スコア」、「サイド」→「サイドスーパー」に統一 |
| スロット | （出さない） | 本番の帯は「いま出ているもの」 |
| 呼出番号 | 番号 | |
| 校正 未完成／未確認／確認済 | 確認 未確認／確認済み | 「文言が空」は自動バッジ |
| 発注（テロ原）／未作画の発注 | 依頼／依頼 N件 | |
| 送出コンソール／送出コンソールへ | 本番モード | |
| PGM（いま出ている絵）／ PVW（次に出す絵） | OA（いま出ている） ／ NEXT（次に出す） | 旧アプリの用語辞典と同じ |
| スタンバイ／TAKE／続き／OUT／次へ／オールクリア | 行を押す（NEXT）／TAKE 出す／進める／CLEAR 消す／— ／全部消す | |
| テーマ | 見た目 | |
| 名簿から一括生成 | 名簿から作る | |
| テンプレート | 複製／前の番組からコピー | |
| 自動退出ルール | 同時に出せないもの | |
| 旧CGから移行 | 旧リアルタイムCGから移す | 設定の中・管理者だけ |
| ページと送出リスト／テロップCG ハブへ戻る／テロップCGへ戻る | テロップ一覧 | 戻りリンクを1種類に |

## 8. 本番モードの運転モデル

旧アプリ（`client-awards`）のコックピットは **NEXT → TAKE → CLEAR** の3語で、TAKE すると NEXT が自動で
次へ進み「TAKE 連打で1本回せた」（旧マニュアルの用語辞典・`CgCockpitPage`）。現行の5動詞よりこちらが
現場に合っていた。戻す。

```
[OA いま出ている（合成・本物の描画）]   [NEXT 次に出す（本物の描画）]  [ TAKE 出す (Space) ]
                                                                        [ CLEAR 消す      ]
──── いま出ているもの ────────────────────────────────────────────────────────────
  ▣ 4 ネーム 高梨 一郎  01:12   [消す]    ▣ 7 サイド 実験映像：東都大学提供  03:40 [消す]
──── 出す順 ─────────────────────────────────────────────────────────────────────
  オープニング
   1 ✓ 題字 …            出した 13:02
   …
  ゲストトーク
   4 ✓ ネーム 高梨 一郎            ● OA
 ▶ 5 △ ネーム 小野 美咲            NEXT   ← 行を押すと NEXT。未確認は△で警告
   6 ✓ 一覧 今日のテーマ
   8 ✓ 投票 Q. 火星の1日は…        [出題 → 締切 → 開票]   ← OA 中だけ行の中に出る
  10 ✓ ランキング TOP5              [次の順位を発表]         ← 同上
```

- **TAKE** = NEXT を OA へ。同じ位置に出ているものは自動で下りる（1枠1枚・退出ルールは内部で効く）。
  TAKE 後、NEXT は**出す順の次の行**へ自動で進む（末尾で止まる）
- **CLEAR** = 最後に出したものを消す（Backspace）。「いま出ているもの」の帯からは1枚ずつ消せる。
  全部消すは上辺の別ボタンで確認つき
- **進める** = OA 中の行の中に、その種類の操作として出る（投票: 出題→締切→開票、ランキング: 次の順位、
  一覧・スコア: 次の項目）。別パネルは作らない
- **行を押す** = NEXT にする。テンキーで番号 → Enter も同じ（口頭「5番出して」の運用）
- **安全装置**（構造で守る・注意力に頼らない）: ①TAKE できるのは NEXT に**見えている**ものだけ
  ②未確認は NEXT に立てた時点で△と理由を出し、TAKE は通す（止めない。止めると本番で詰む）
  ③文言が空のものは NEXT に立てられない ④本番中は並べ替えできない（準備に戻る）
  ⑤文言の急な直しは行の「直す」→小さなシート→「反映」で確定（自由記入の即時反映はしない。
  スコア・票数だけ即時）
- **配色**: `<html class="dark">` の v4 ダークトークン（`tokens-v4.css` の `.dark`。進行・ランダウンと同じ）。
  CG の絵そのものは v4 トークンの対象外（graphics.md §5 のまま）
- **キーボード**: Space=TAKE／Esc=（何もしない・誤爆防止）／↑↓=NEXT 移動／数字+Enter=番号呼出。
  旧アプリは「ショートカット無し」を選んでいた（誤爆事故）。本モックは Space だけを主にし、他は補助

## 9. 進行台本との連携（新規・この道具の存在理由）

進行台本（Qシート）には既に**「テロップ」列**（`shared/src/qsheet/blockTypes.ts` の `telop`・各行の
`entries` に文言とメモ）があり、ディレクターは台本を書く時点でテロップの文言を書いている。
いまのテロップCGはそれを**もう一度手で打ち直させている**。

1. **台本から取り込む**（①の「＋ テロップ」メニュー）: 台本を選ぶ → テロップ列の文言が行になる。
   項目（台本の行）の並びがそのままコーナーと出す順になる。種類は文言から推定（「／」入りはネーム、
   短文は題字…）し、あとから直せる
2. **紐づけを持つ**: 取り込んだ行は台本の項目 ID を覚える（`graphics_pages.qsheet_row_id`）。
   台本側の文言が変わったら①に「台本と違います」バッジを出し、押すと差分を取り込める
3. **本番で追従する**（**既定 OFF・番組ごとに①の状態帯から ON にできる**・§12-2で確定）:
   進行（OnAir）画面で現在行が進むと、本番モードの NEXT がその項目のテロップへ自動で移る。
   TAKE は人が押す（自動送出はしない）。トークが前後する番組では OFF のままにする
4. 依頼（⑤）の「出すタイミング」も台本の項目から選べる

これは graphics.md §2 の「送出リストは台本の項目から自動生成できる（順序の正は台本側）」の最小実装で、
段5に予定されていながら未着手だったもの。

## 10. データへの影響（最小）

| 変更 | 内容 |
| --- | --- |
| `graphics_pages.section TEXT NULL` | コーナー見出し（新規列） |
| `graphics_pages.qsheet_doc_id` / `qsheet_row_id` | 台本の項目への紐づけ（新規列・NULL 可） |
| `call_no` | **現行のまま維持**（作成時に一度だけ自動採番。`sort_order` に連動させない）。手入力での変更は引き続き許す。§12-3 の決定により「並べ替え連動」案は採らない |
| `proof_state` | 列はそのまま。UI は `unproofed`/`proofed` の2値＋必須欄空の自動判定（`draft` は自動でのみ付く） |
| `graphics_templates` | 画面は廃止するが表は「前の番組からコピー」の器として残す |
| その他の表・API・Socket `cg:*`・出力URL | 変更なし |

## 11. 実装の段取り（モック確定後・段ごとに別 PR）

| 段 | 内容 | DB | 目安 |
| --- | --- | --- | --- |
| A | ✅ **実装済み（2026-09-06）** 一覧＋右パネル（①③）・設定1画面（④）・左メニューのサブ項目・戻り先の統一・死んだ UI と固定文字列の削除・`GRAPHICS_RE` の修正 | なし | 現行の 68 ファイルの**再配置**が中心 |
| B | ✅ **実装済み（2026-09-06）** 本番モードの作り直し（②・3動詞・OA/NEXT・いま出ているものの帯・行内の操作） | なし | `GraphicsConsolePage` の再構成 |
| C | ✅ **実装済み（2026-09-06）** コーナー・台本から取り込む・台本と違いますバッジ（§9 1〜2） | `section`・`qsheet_doc_id`・`qsheet_row_id`（migration 280） | 新規 |
| D | 依頼の改修（台本の項目から選ぶ）・スマホ閲覧（⑤⑥） | なし | |
| E | 本番で追従（§9 3）・前の番組からコピー | なし | Socket `/techops` の `cue:*` を読む |
| F | 旧 `/awards` の畳み込み（実行の承認は §12-5 で取得済み。**段A〜E を検証環境で1本通してから**実行する） | — | [migration-plan §4 段6-9](graphics-awards-migration-plan.md) |

段A・B は既存の部品を並べ替える作業で、レンダラー・API はそのまま使う。

### 段A 実装メモ（2026-09-06）

- **新規**: `TelopThumb.tsx`（実描画サムネイル・行と種類カードで共用）・`TelopKindGrid.tsx`
  （旧・部品ライブラリのカードを「種類を選ぶ」ステップに埋め込み）・`TelopEditorPanel.tsx`
  （旧 `PageFormDialog.tsx` をダイアログの外枠なしで①に埋め込む右パネルに作り直し。
  400行基準のため `TelopKindOrTemplateStep.tsx`〈1段目〉・`TelopEditorFormBody.tsx`
  〈2段目の本体〉に分割）・`GraphicsSettingsPage.tsx`（④・1画面4タブ）。
- **全面書き換え**: `GraphicsHubPage.tsx`（①。同じ理由で `TelopAddMenu.tsx`〈＋テロップの
  ドロップダウン〉・`TelopListSection.tsx`〈状態チップ＋表〉に分割）。
- **削除**: `PartLibraryPage.tsx`（独立画面としての部品ライブラリ）。旧URL
  （`/parts`・`/sounds`・`/interactive-link`）は `App.tsx` の `RedirectToGraphicsHub`・
  `RedirectToGraphicsSettings` が①・④「連携」タブへ転送する（`RankingSoundsPanel`・
  `InteractiveLinkSettingsPanel` 自体は削除せず、`embedded` prop で④に埋め込んで再利用）。
- **意図した持ち越し・簡略化**:
  - フィールドの真の自動保存はまだ実装しない。明示的な保存ボタンを維持した
    （1文字ごとの自動保存はレース条件のリスクがあり、段Aのスコープでは踏み込まない）
  - 左メニューの子項目に見た目のインデントは付けていない（`ShellNavItem` がインデント付き
    子項目を持たないため、同じ深さの3項目に展開する形にした。上記「左メニュー」参照）
  - 「テンプレートから作る」は①の主導線からは外したが、機能自体は廃止していない
    （新規作成の1段目に「テンプレートから作る」タブとして残し、④設定「見た目」タブから
    テンプレート管理画面〈上級者向け〉へのリンクも残した）
  - サムネイル（`TelopThumb`）は①の行・②新規作成の種類カードに付けた。本番モード
    （②・送出コンソール）側の見せ方は段Bのスコープ
- **検証**: `tsc -b client-techops`／`npm run lint`（0 errors）／`npm run test`
  （shared Vitest 1996件）に加え、`npm run verify:up` の Postgres に対して実サーバー・
  実 `client-techops` dev server を起動し、Playwright で①一覧・行の右パネル編集・
  新規作成の1段目/2段目・④設定の4タブ・旧URL3本の転送先・375px幅でのPC専用ガード表示を
  実ブラウザで確認し、`npm run build -w client-techops` も通した。**未検証のまま残したこと**:
  実機（iOS/Android）でのタップ操作確認・権限別（reader/editor 等）の403表示確認はなし
  ——PRを出す段になったら棚卸しの対象にする

### 段B 実装メモ（2026-09-06・マルチエージェントで実装）

ユーザーの「マルチエージェントで進めて」のご指示を受け、実装（並列2体）→統合（1体）→
型チェック・lint（1体）→検証（並列2体：設計書との批判的レビュー＋機械チェック）→
指摘の修正（1体）という5段のワークフローで進めた。検証段の批判的レビューが実際に
ブロッカー（安全装置③の抜け）を検出し、直後の修正エージェントが解消している
（下記「レビューで見つかった問題」参照）。

- **書き換え**: `ConsoleControls.tsx`（5動詞→TAKE/CLEARの2動詞。番号バッファ表示は
  「NEXTにする」に改称して継続）・`useConsoleKeyboard.ts`（Esc=何もしない・Enterは
  数字入力時のみ・Backspaceは数字が無ければCLEAR）・`ConsoleSlotLanes.tsx`
  （「いま出ているもの」——常時6枠表示をやめ、いま実際に出ているスロットだけの帯に）・
  `ConsolePageList.tsx`（出す順一覧を全面書き換え。行そのものがNEXT切替の操作対象になり、
  スロット札・3値校正バッジ・専用ボタン列を廃止。段Aと同じ✓/○の2値確認表示に統一）・
  `GraphicsConsolePage.tsx`（TAKE/takeAndNextを1つに統合し「次へ」を撤去、outStandby
  〈OUT〉を削除、オールクリア→全部消すに改称。ボタンラベル改称に合わせ確認ダイアログの
  文言も揃えた）。
- **新規**: `useConsoleTake.ts`（400行の目安のため `GraphicsConsolePage.tsx` から
  TAKE（自動前進込み）・CLEAR・↑↓NEXT移動を切り出し。`useConsoleContinue.ts` と同じ思想）。
- **1行だけの変更**: `ConsolePreview.tsx`（表示ラベル PGM/PVW → OA/NEXT）。
- **変更していない**（そのまま流用）: `useConsoleContinue.ts`・`useConsolePages.ts`・
  `useAutoOutHighlight.ts`・`ScoreQuickAdjust.tsx`・`RankingControlPanel.tsx`・
  `rankingFields.ts`・`voteState.ts`。「進める」（旧「続き」）はこれらの既存ロジックを
  そのまま、置き場所だけ `ConsolePageList.tsx` の OA 中の行の内側（`OaInlineAction`）へ
  移した——サーバー・API・Socket は §10 の方針どおり一切変えていない。
- **TAKEの自動前進は「出す順」（`sortOrder`）を使う。** 旧実装は呼出番号順
  （`callOrder`）で次へ進めていたが、呼出番号は段Aの決定で並べ替えても変わらない固定値
  になったため、画面に見えている出す順と一致するとは限らない。↑↓でのNEXT移動も同様に
  `sortOrder` 順に直した。
- **レビューで見つかった問題（すべて対応済み）**:
  - **［ブロッカー→修正済］** 安全装置③（文言が空のものはNEXTに立てられない）が、
    行クリックだけ実装されていて番号呼出（`commitCall`）と↑↓移動（`moveNext`）では
    未対応だった。両方に `isPageContentEmpty` のチェックを追加した。
  - **［ブロッカー相当→追加修正］** 検証段のレビューが見つけた3経路とは別に、
    **TAKE成功後の自動前進**にも同じ抜けがあった（次の行が空欄だとそのままNEXTにして
    しまう）ことにワークフロー完了後の最終確認で気づき、`useConsoleTake.ts` の `take()`
    で「空欄でない次の行」まで読み飛ばすよう直した（見つからなければ元の行に留まる）。
    4経路すべてで安全装置③が揃った状態で検証を終えている。
  - NEXTプレビュー右上バッジが退役させたはずの3値 `ProofBadge`（未完成／未確認／確認済）
    のままだったのを、段Aと同じ✓/○の2値表示（`NextProofStatus`）に直した。
  - 「全部消す」の確認ダイアログの実行ボタンが旧語彙「オールクリア」のまま残っていたのを
    「全部消す」に揃えた。
- **意図した持ち越し・スコープ外**: コーナー見出し（段C待ち）・「出した hh:mm」表示
  （そのデータを持たない）・行の「直す」→小さなシートでの文言修正（新しいUIパターンが要る
  ため。安全装置⑤にあたるが今回は見送った）。
- **検証**: マルチエージェントのワークフロー内で `tsc -b client-techops`／`npm run lint`
  （0 errors）／`npm run test`（1996件）／`npm run build -w client-techops` を確認済み。
  上記の追加修正（自動前進の安全装置）後、同じ4項目を再実行してすべて通過を確認した。
  さらに `npm run verify:up` の Postgres に実サーバー・実 dev server を起動し、Playwright
  で番号呼出→TAKE（空欄行の自動読み飛ばしを含む）→CLEAR→行クリックでのNEXT切替→
  空欄行が押しても/番号呼出でもNEXTにならないこと→OA中の一覧表に「次の項目」ボタンが
  出て押すとページが進むこと→全部消すの改称確認、を実ブラウザで確認した。
  未検証: vote・score・ranking 種類の行内操作（seed データに存在しないため。実装は
  `useConsoleContinue.ts`・`ScoreQuickAdjust.tsx`・`RankingControlPanel.tsx` の
  既存ロジックをそのまま呼ぶだけで新規ロジックは無いためコードレビューでの確認に留めた）・
  実機でのタップ操作・権限別403表示。

### 段C 実装メモ（2026-09-06・マルチエージェントで実装）

ユーザーの「Cへ」のご指示を受け、段Bと同じ5段のワークフロー（実装・並列3体→統合1体→
型チェック・lint 1体→検証・並列2体→修正0〜1体）で進めた。今回は Finalize 段の修正は
発生しなかった（検証段の指摘が2件とも「note」＝ブロッカーでない既知の制約だったため）。

- **新規（サーバー）**: `migrations/280_graphics_qsheet_import.sql`（`graphics_pages` に
  `section`・`qsheet_doc_id`〈`qsheet_documents` への FK・`ON DELETE SET NULL`〉・
  `qsheet_row_id` を追加）・`services/qsheet-import.service.ts`（`inferPartKey`・
  `previewQsheetImport`・`commitQsheetImport`・`fetchQsheetLiveText` の4関数。
  台本 JSONB の走査〈`collectTelopEntries`〉と、共有されていない台本を弾くアクセス制御
  〈`loadAccessibleQsheetDoc` が `qsheet/access.ts` の `canAccessDoc` を通す〉を持つ）・
  `routes/qsheet-import.routes.ts`（3エンドポイント。他の graphics ルートと同じ
  `requirePermission('qsheet')`、投入だけ `editor` 以上）。
- **新規（クライアント）**: `graphicsQsheetImportApi.ts`（APIラッパー）・
  `QsheetImportDialog.tsx`＋`QsheetImportPickStep.tsx`＋`QsheetImportReviewStep.tsx`
  （台本を選ぶ→候補を確認する→取り込む、の3段ダイアログ。`RosterImportDialog.tsx` と
  同じ外枠）・`telopGrouping.ts`（`groupPagesBySection`。①②共用の純粋関数）。
- **拡張のみ**: `store.ts`/`graphicsApi.ts`（`GraphicsPage`/`GraphicsPageRow` に3列を追加。
  `GraphicsPageInput` には追加せず、通常の作成・編集フォームからは触れないようにした）・
  `TelopAddMenu.tsx`（「台本から取り込む」を4つ目のメニュー項目に追加）・
  `TelopListSection.tsx`（状態チップに「台本と違う」を追加・コーナー見出し行の挿入・
  行に警告バッジ＋取り込み直しボタン）・`GraphicsHubPage.tsx`（`fetchQsheetLiveText` の
  取得・`driftPageIds` の算出・取り込み直しハンドラ・ダイアログの開閉配線）・
  `ConsolePageList.tsx`（コーナー見出しの挿入**のみ**。「台本と違います」バッジは
  意図して追加していない——本番中の一覧を賑やかにしないための①だけの非対称。設計指示どおり）。
- **「台本と違います」の判定方法**: 台本側のいまの文言（`liveText`）と、ページの主フィールド
  （`pageFields.ts` の `PRIMARY_FIELD_KEY`）を突き合わせる。`liveText` が `null`（台本・行・
  テロップ列のいずれかが無くなっている）、対応するページが見つからない、主フィールドが
  無い・文字列でない（一覧・スコア・ランキング等の配列型）のいずれかに該当する行は
  **差分ありと判定しない**——false negative 上等・誤検知を避ける設計判断（下記「検証段の
  指摘」2件目もこの制約について）。
- **統合段で見つけて直した不具合2件**（担当1〜3の実装は個別には正しかったが、繋いだ
  ときに顕在化。いずれも検証DBで実データを流して再現・修正確認済み）:
  1. `fetchQsheetLiveText` のクライアント側ラッパーが `pageId` を `String()` 変換していたが、
     `driftPageIds` の算出は `page.id`（実体は number）とそのまま `===` 比較していたため
     `number === string` で常に false——「台本と違います」が**絶対に出ない**状態だった。
     両辺とも型注釈が `string` のため `tsc` では検出できない類のバグ。`String()` 変換を
     やめて解消。
  2. `buildDefaultFields(partKey, text)` は「部品の先頭入力欄」に文言を入れるが、
     ネーム部品だけ先頭欄（`label`＝役割）と主フィールド（`mainText`＝氏名）がずれている
     ——取り込み例そのものである「山田太郎／ゲスト」がまさに該当し、直さないと
     取り込んだ氏名が主フィールドに入らず「未完成」バッジが付き、かつ①の修正後は
     常に「台本と違います」も誤検出される状態だった。`QsheetImportDialog.tsx` に
     ネームだけ主フィールドへ明示上書きする `buildImportFields` を追加して解消
     （他9部品・`score`/`ranking` 等の配列型主フィールドは無変更）。
- **検証段の指摘（2件とも note＝非ブロッカー。対応不要と判断）**:
  1. 1行に telop 型の列が2つ以上ある台本では、`qsheet_row_id` が行 ID までしか覚えず
     列 ID までは覚えない設計（DB影響を最小にするため。migration 280 のコメント参照）
     のため、`fetchQsheetLiveText` は「最初に見つかった列」を機械的に採用する。取り込み時に
     2列目の候補を選んでいた場合、差分検出が実際の紐づけとずれうる——ただしこれは
     スキーマの意図的な単純化が原因で、この段の実装だけでは直せない。
  2. 確認画面で候補の種類を score/ranking/list（配列型主フィールド）に手動変更して
     取り込むと、台本の文言は `page.name` にしか残らず `fields` 側には反映されない。
     `buildDefaultFields` 自体は段Aからの既存共有ヘルパーで、`TelopEditorPanel.tsx` の
     依頼変換パスなど既存の他経路にも同じ制約が元々あり、段Cが新たに悪化させたものではない
     （`inferPartKey` がこれらの種類を提案することもない）。
- **実装とは別に見つけた、シードデータ側の既存ギャップ（段Cのコードの不具合ではない）**:
  ブラウザ確認のため検証DBの既存シード台本（`seed-subapps.ts`）で試したところ、
  取り込み候補が0件になった。調査の結果、原因はシード生成側にあった——
  ①`row()`ヘルパーが `telop`/`video`/`audio` セルを実際の編集画面が読み書きする
  `{ entries: [{ label, memo }] }` 形ではなく素の文字列のまま入れている
  （`CueRow.tsx` の `getEntry()` で確かめると、この形は実際の編集画面でも空欄として
  読まれる——`v2.8.155` の統一前の形が残ったものと見られる）。②同ヘルパーが行に
  `id` を一切振っていない（`stableIds.ts` の `genId()` 規約を使っていない）ため、
  `qsheet_row_id` の元になる `row.id` が存在しない。段Cの `qsheet-import.service.ts` は
  実際の編集画面・`rundownData.ts` の型定義どおりの形を前提に書いており、この2点は
  シード生成コード側の既存の欠陥。段Cでは修正していない（`seed-subapps.ts` は他の
  qsheet/rundown機能の検証にも使われる共有シードで、影響範囲の精査が要るため）——
  ブラウザ確認は使い捨て検証DB上で該当ドキュメントの `blk_t1` セルと行IDをその場限りで
  修正して行った（コミットはしていない・検証DBごと破棄済み）。段D/Eも同じ台本連携を
  触るため、次にこの経路を検証する際は同じ制約に当たる可能性がある。
- **検証**: ワークフロー内で `tsc -b server`／`tsc -b client-techops`／`npm run lint`
  （0 errors）／`npm run test`（1996件）／`npm run build -w client-techops`／
  `verify:up` でのマイグレーション適用確認、をすべて完了。**さらに呼び出し元（自分）が
  独立して**同じ4項目（tsc×2・lint・test）を再実行して確認し、`verify:up` で
  マイグレーション280を再適用して3列・FK・部分インデックスを `\d graphics_pages` で
  確認し、全ファイルを自分で読んで `canAccessDoc` が3関数すべてから呼ばれていることを
  確認したうえで、実サーバー・実 dev server 起動＋Playwright による実ブラウザ確認まで行った:
  ①の「＋テロップ」→「台本から取り込む」→台本を選ぶ→候補確認（コーナー見出し・種類の
  初期推定を含む）→取り込み（15件）→①一覧にコーナー見出し付きで反映・「台本と違う」
  チップが0のまま、を確認。続けて台本側の文言を書き換えて①を再読み込みし、「台本と違う」
  チップが1に増え行に警告バッジと取り込み直しボタンが出ること、押すと0に戻ることを確認。
  最後に②本番モードでコーナー見出しは出るが「台本と違います」バッジは出ないこと
  （設計どおりの非対称）を確認。**未検証**: 実機でのタップ操作・権限別403表示・
  1行に複数telop列がある台本（上記指摘1件目のケース）。

## 12. 決まったこと（2026-09-06 ユーザー回答）

1. **本番モードの「未確認」の扱い → TAKE 可能（△警告のみ・止めない）。** モックのまま確定。
   ダイアログでの二度確認は本番の邪魔になるため付けない
2. **台本に追従（§9 3）→ 既定 OFF・番組ごとに ON にできる。** 提案どおり確定。
   ON/OFF の切替は①テロップ一覧のヘッダー付近に置く（§5 に追記）
3. **番号の振り直し → 「よくわからない」との回答を受け、こちらで決めた: 番号は固定にする。**
   出す順（並べ替え）は自由に変えられるが、**呼出番号は作成時に一度振ったら変わらない**
   （旧 `call_no` 方式・Viz Trio/Chyron のコールアップ番号と同じ考え方）。
   理由: このアプリは「口頭で番号を呼ぶ」運用（§8「5番出して」）を前提にしており、
   生放送中に並べ替えで番号が変わると、口頭指示と画面の番号がずれて事故る。
   §9・§10・§6 の「番号は自動」という記述はこの決定に合わせて「番号は固定」に直した
4. **スマホの本番中の権限 → 緊急の CLEAR（消す）を許可する。** ⑥一覧（スマホ）の
   「いま出ているもの」の各行に「消す」ボタンを追加した（確認なしの1タップ。
   本番中に急いで消す場面を想定しており、PC の CLEAR と同じ「最後に出したもの」ではなく
   **行ごとに指定して消す**——スマホは PGM/NEXT の全体像を見ていないことが多いため）
5. **旧リアルタイムCGの畳み込み → 畳んでよいと承認を得た。** ただし**段A〜E（このモックの
   実装）がまだ影も形もない**状態で先に `client-awards` の配信を止めると、利用者は
   「醜いと言われた画面」も「使い慣れたアワード送出」もどちらも使えなくなる。
   そのため実行順はもとの提案どおり据え置く: **段A〜E を実装し検証環境で1本通してから畳む**。
   この承認は記録済みなので、段F（畳み込み）の実行時に改めて確認を取り直す必要はない
   （[§13](#13-旧アプリ移行中の扱い)・[段F](#11-実装の段取りモック確定後段ごとに別-pr)参照）

## 13. 旧アプリ（移行中）の扱い

- `client-awards/` は「移行中」。URL・API・配信は生かし、新しい機能は足さない
- アワード固有の演出（段階発表・Final Pitch・Celebration・演出SE・外部投票）は既にこのエンジンの
  `ranking`／`vote` 種類へ移植済み（[migration-plan](graphics-awards-migration-plan.md) 段6-5〜6-7）。
  本再設計はそれらの**置き場と操作**を変えるだけで、演出の実装には触らない
- 過去実績データの移行ツールは設定「連携」タブの管理者項目として残す。実行の判断はユーザー
- **畳み込み（「移行中」→「廃止」）は§12-5でユーザーの承認を取得済み。** 「移行中」という状態は
  期限のない待機ではなく、**段A〜E の実装が終わり検証環境で確認できた時点で畳む、と決まっている**
  終着駅つきの状態——実装が追いつくのを待っているだけで、判断待ちではない
