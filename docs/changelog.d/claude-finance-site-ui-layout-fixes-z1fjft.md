**財務ダッシュボードの「売上・仕入（変動原価）・固定原価・販管費」の内訳行で、ラベルが読めない字数まで切れていたのを直した。** ①⚠️ `lg:grid-cols-4` の1列 282px 程度の幅に、ラベル・金額・件数（固定 128px）を横1列で詰め込んでいたため、金額と件数に押し出されてラベルがほぼ0幅まで潰れ、「売上」が「売」、「仕入（変動原価）」が「仕‥」にしか見えなくなっていた（DOM 上の文字列は残っているため、既存の横はみ出し検査には引っかからなかった）。②ラベル＋金額を1行目、件数（sub）を2行目に分け、件数のために横幅を横取りしない形にした。実ブラウザ（Playwright・実データ投入）で財務ダッシュボード・請求入金・売上/仕入/販管費台帳・取引先・取り込み・受け取った書類・仕入先集計の全画面を PC/スマホ両方で確認し、ラベルの欠けが解消されたことを実測した。検証: `npx tsc -b client` / `npm run lint` / `npm run test` OK。

**財務ダッシュボードの「案件で絞り込む」欄で、長い案件名を選ぶと下の注記と重なって読めなくなっていたのを直した**（利用者からのスクリーンショット指摘）。共通部品 `SearchableSelect`（`shared/`・7アプリで利用）の選択欄ボタンは固定高さ（`h-11`/`h-10`）なのに、選んだ項目のラベルに `truncate` が無く、長い案件名（数十文字の問い合わせ案件名など）が4行ぶんに折り返してボタンの高さからあふれ、すぐ下の要素（「案件で絞り込み中は販管費が…」の注記）と重なっていた。ラベルに `min-w-0 flex-1 truncate` を足して1行で省略する形にした。共通部品の変更のため `npx tsc -b`（全6アプリ）・`npm run build:all` 後の `npm run check:frozen`（凍結アプリの CSS 差分なし・ビルド出力バイト単位で確認）まで実施した。

**財務ダッシュボードの結果カード（限界利益・売上総利益・営業利益）が、スマホで金額を隣のカードの下に隠していたのを直した。** 常に横一列（`flex`）で並べていたため、375px幅ではカード1枚が120px程度しか無く、金額（太字の大きい数字）がカードの右端からあふれ、隣のカードの背景に隠れて末尾の桁が消えて見えていた（`¥11,680,000` が `¥11,680,00` に見える。横スクロールとしては現れないため既存の横はみ出し検査にも引っかからなかった）。`640px` 未満は縦積みにし、演算子（`−`/`=`）も横幅いっぱいの薄い帯にした。この崩れが「財務ダッシュボードがスマホでは『PC専用』の案内しか出ず使えない」という指摘の実体だったため、崩れを直したうえで `client/src/pcOnlyScreens.ts` の `/budget/dashboard` を `CLIENT_PC_ONLY` から `CLIENT_MOBILE_OK` へ移した（375/430/640/768pxで横はみ出し・重なり無しを実測）。

**財務の「取引先」「請求・入金」画面で、`text-fg-disabled`（白地で2.61:1しか無い・読ませる文字には使わない決めごと）を実データの表示に誤用していて薄すぎたのを直した。** 取引先一覧・カードの「今年はなし」、請求・入金の「（番号なし）」は装飾やヒントではなく実際の状態を伝える文字なので `text-muted-foreground` に直した。`node scripts/verify-ui.mjs 財務` の「薄すぎる文字」検査で検出・修正後 246/246 通過を確認。

検証（このファイル全体）: `npm run typecheck:all` / `npm run lint` / `npm run test`（1144件）/ `npm run build:all` / `npm run check:frozen` すべてOK。実ブラウザ（Playwright・実データ投入）で財務管理の全9画面をPC（375/430/640/768/1024/1440px 各所）・スマホで確認した。
