**人が書いた本文に「AI 整形」の札が出て、「整え直す」が嘘の返事をしていたのを直した**（Codex のレビュー棚卸し #93。P2 1件 ＋ この作業中に見つけた1件）。①⚠️ **`body_html` は AI が整えた本文にも、人が書いた本文にも入ります。** 画面はその2つを区別せず `!!s || !!a.body_html` で判定していたので、**AI が一度も触っていない記録に「AI 整形」の札が出て**、**「整え直す」も出ていました**。②⚠️ **押しても何も起きません — どころか「戻しました」と言い切ります。** サーバーの待ち行列は `body_html IS NULL OR ai_formatted` を要求するので（`PENDING_SQL`）、**人が書いた本文の行は永久に対象になりません**。前の版は **HTTP 200 で「整え直しの順番に戻しました（毎晩 3:00 に自動で整えます）」**と返していました（実測。**待ち行列に入ったか = false**）。画面は1ドットも変わらず、「待っています」も出ません（それも `ai_formatted` を見ているため）ので、押した人は**何度でも押します**。**AI の印がある行だけ**を AI 扱いにし、**サーバーでも 400 で止めます**（画面で隠すだけだと古いタブ・直接叩きから通る）。③⚠️ **続けて2回押すと、`before` も `after` も `null` の「不採用」が `ai_corrections` に積まれていました**（実測: 3回押して **3 件**・どれも `before=null / after=null`）。**何も否定していない記録**が無修正採用率の分母だけを増やすので、会社方針の条件2（人の修正を差分として残す）が測れなくなります。**整えた中身が無いときは差分を残しません。**④**費用の目安が出せない理由が、いつも「単価が未設定です」でした**（この作業中に実測して見つけたもの）。`usdPerRow` が `null` になる状況は**3つ**あり、**打ち手がそれぞれ違います**: **①単価そのものが入っていない**（`AI_PRICING_JSON` を入れる）／**②単価はあるが、この仕事の実績がまだ無い**（待つ・直すところは無い）／**③実績はあるが、その仕事が呼んだモデルの単価が無い**（そのモデルの鍵を足す）。②③の人は `.env` を直しに行き、**すでに入っている**のを見て途方に暮れます。⚠️ **この PR のレビューで③を指摘され、直しました**（Codex・P2）。私の初版は「鍵が1つでもあれば単価はある」と見なしていたので、**`whisper-1` しか入っていない環境**や**モデルを乗り換えて古い鍵を落とした環境**で「実績がまだありません」と出ます — 実績はあるので**待っても永久に出ません**。しかもこれは `.env.example` が警告している「鍵を落とすと静かに消える」そのものなので、**いちばん直してほしい設定から人を遠ざけます**。**その仕事が実際に呼んだモデルの単価が入っているかで判定し、モデル名も出します**（どの鍵を足せばよいか分からないと、理由だけ分かっても直せない）。**数え方は `perRowCost` の1か所**にまとめました（2つの仕事が同じ計算を書き写していたので、片方だけ直る形も一緒に潰しました）。⑤**検証**: 実 Postgres ＋ 実サーバー ＋ 実ブラウザで — 人が書いた本文の行に整え直しを叩くと **400 `NOT_AI_FORMATTED`・`ai_corrections` は 99 件のまま・行も不変**（**反証: 守りを外すと HTTP 200 で「順番に戻しました」と返るのに、待ち行列に入ったか = false**）。AI が整えた行は1回目で差分が1件増え、**2回目・3回目では増えません**（**反証: 守りを外すと 100 → 103 件**）。費用の理由は**4通りとも実ブラウザで実測**: 単価なし →「**単価 `AI_PRICING_JSON` が未設定です**」／単価あり・実績なし →「**この仕事の実績がまだ無いためです**」／**単価あり・そのモデルの鍵だけ無い →「gpt-5.6-luna の単価が入っていません」**（`whisper-1` だけ入れた環境。**反証: 前の版は「実績がまだ無い」と出る**）／単価あり・実績あり →「残り全部を整えると約 $0.23（実績の1件あたり $0.018 × 13 件）」（`flex = flex`・JS エラー 0）。`shared/tests/redoFormat.test.ts` 6項目、`aiPricingHistory.test.ts` に4項目追加。⑥**DB 変更なし。`shared/src/` は触っていない**ので凍結4アプリの CSS は不変。
