**PR #280 のマージ後の棚卸しを記録した**（コード変更なし）。CLAUDE.md の必須手順（マージすると指摘は画面から消えるため、書かなければ存在ごと消える）に従い、`docs/reviews/codex-findings-v4.md` の「レビューが0件のままマージされた PR」節に #280 を追記した。**レビュー・レビューコメント・通常コメントすべて0件**のままマージされており（`get_reviews` / `get_review_comments` / `get_comments` の3通りで確認）、棚卸しへ移すべき未返信の指摘は無い。⚠️ ただし「0件」と「指摘なし」は画面上見分けが付かない（#154/#159/#165 の前例）ため、0件だった事実そのものを記録した。⚠️ **この回は `npm run reviews:debt` を実行できていない** — 作業環境の `GITHUB_TOKEN` では GitHub REST API が 401 を返すためで（この環境の GitHub アクセスは MCP 経由）、代わりに MCP の `pull_request_read` で3通り確認した。#280 は `docs/` 配下 23 ファイルの追加のみでコード変更が1行も無い PR だったため、Codex が対象外にした可能性がある点も併記した。
