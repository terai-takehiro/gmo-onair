**「探す」（`/search`）をカードとして組み直し、キャンセルボタンと「最近見たもの」の削除を足した**
（マルチエージェント監査 `docs/v4-native-ui-audit-2026-08-20.md` search-sales の続き）。
これまで PC・スマホが `Row stackOnMobile` 1本の同じ罫線区切りリストを共有しており、
下タブ画面としての専用の演出（キャンセルボタン等）が無い・「最近見たもの」は読むだけで
消せない・結果セクション（案件/お客様/仕入先）がカード化されていない、の3つが指摘されていた。

薄い親 `SearchPage.tsx` が問い合わせ・「最近見たもの」の読み書きを1回だけ持ち、見た目を
`search/SearchPageDesktop.tsx`（今までどおりの罫線区切りリスト）と `search/SearchPageMobile.tsx`
（新設・カード積み `search/SearchCards.tsx`）に丸ごと入れ替える形にした
（`useIsMobile()` は薄い親で1回だけ呼ぶ・機材管理の `search/` と同じやり方）。

- **スマホの検索欄に iOS 風の「キャンセル」を足した。** 打ち始める・欄を選ぶと出て、
  押すと打ちかけの文字とキーボードを消して「やること・場所」の一覧に戻す
  （下タブ自体は消えないので画面遷移はしない）
- **「最近見たもの」に削除を足した**（`shared/src/client-v4/recent.ts` に `removeRecent()` を
  追加。1件だけ端末の記憶から消す）。PC は行の右端にボタン、スマホはカードの外側に
  タップ範囲を分けたボタンを置いた — 同じ範囲を「開く」と兼ねると、消したいだけなのに
  行が開いてしまうため
- **結果セクション（案件/お客様/仕入先）をスマホだけカード化した。** PC の行を縮めたのではなく、
  案件一覧・機材管理「探す」と同じ考え方で組み直した。お客様カードの電話リンク
  （PR #73 の指摘・タップ/キー操作とも行の遷移を止める）はそのまま引き継いでいる
- 検証: `npx tsc -b client` / `npm run lint` / `npm run test`（shared 86ファイル・1144件）OK。
  `reachPhone.test.ts` は PC/スマホでファイルが分かれたことに合わせて参照先を更新した。
  ⚠️ ブラウザ自動化ツールが無く、375px幅での実ブラウザ確認は未実施。
