**PR #202（Phase 3-2b — 仕入先系FKを companies へ張り替え）のCodexレビュー指摘5件（P1×2・P2×3）を棚卸しに記録した。** 1巡目3件はマージ前に同PR内（`28ac86a`）で直し・resolve済み: ①（P1）`vendors.routes.ts` — `budget:editor` が `sales:owner` を持たないと `companies` へ同期されない仕様なのに、一覧・詳細・PUT応答が `companies` から読んでいたため、保存直後の画面にも古い値が出続け実質編集できないように見えていた穴。②（P2）`xpoint.routes.ts` — X-Point登録エンドポイントがレビューUIから渡される `vendor_id` を検証せず INSERT していた穴。③（P2）`import-kessan-dev.mjs` — このFK変更で決算取込のdev専用フォールバックスクリプトが機能しなくなる穴（あわせて同じ穴が Phase 3-2a からあった顧客側も直した）。マージ後に付いた2巡目2件は別PR #207 で直し・resolve済み: ④（P1）1巡目の直しが仕入先タブ自身だけを直し、仕入台帳一覧・Excel入出力・MCP・バックアップ・グループ按分仕入・仕入先別集計・決算取込・全体検索・X-Point取込の重複チェック表示には同じ穴が残っていた。⑤（P2）決算取込・X-Point取込の仕入先再利用ロジックが、ロールを外された・削除済みの会社を再利用してしまう穴。マージすると画面から消える指摘を残す決めごとに従い記録した。**検証**: ドキュメントのみの変更。
