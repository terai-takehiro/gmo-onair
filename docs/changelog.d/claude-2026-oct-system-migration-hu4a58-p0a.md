**2026年10月の事業再編（社名変更・計上会社の2社化・案件番号の改番）P0の土台を実装した**（`docs/reorg-2026-10-plan.md` §13の0a・設計のみだったものの最初の実装）。①計上会社マスター`legal_entities`（GJV/GSS/GMOの3行。GSSは今のPDF発行者定数の値をそのまま初期値に、GJVは発行者情報を空欄で作成・設定画面から入力する運用）と旧⇄新の切替状態`org_transition`（既定`off`）を新設（migration 284）。②取引先「GMOサムライコンテンツスタジオ」の重複2行を本番データで確認した内容どおりに名寄せし、GJVの自社行（GPMの依頼元「自社」・社内取引の相手先用）を追加。③案件・売上・仕入・販管費・見積・受け取った書類・月次予算関連の各表に`entity_code`列を追加し、案件番号の履歴を全部残す`project_numbers`表を新設して既存のGLS番号を流し込んだ（migration 285。**まだ列を足すだけ**——`org_transition.state='off'`のあいだ、採番も帳簿の書き込みも一切変えていない）。④`legal-entity.service.ts`・`org-transition.service.ts`と`GET/PUT /legal-entities`・`GET/PUT /org-transition`のAPI、`migrate.ts`起動時チェックを追加（発行者情報の変更は`system_admin`のみ、状態遷移は§5の許容表どおり検証）。検証: 新規マイグレーション2本（284・285番。実装当時は280・281番だったが、後日`main`側との番号衝突で2回改番し、最終的に284・285番になった。§13参照）を検証DBに適用、`server`の型チェック、`shared`のVitest 2002件全通過（新設6件含む）。財務の2社タブ・案件番号の採番・帳簿への書き込み配線は続くPRで対応する。
