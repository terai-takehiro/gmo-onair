**PR #482（失注案件のやること・未入金通知・日常業務の2画面に溜まるゴミを止めた）のマージ後の棚卸しを記録した**（コード変更なし）。作成からCI green まで約2分6秒、CI green からマージまで約32分8秒（terai-takehiro 本人が手動マージ）で、`get_reviews`・`get_review_comments`・`get_comments` とも終始0件だったことを `docs/reviews/codex-findings-v4.md` に記録した（`npm run reviews:debt` は GitHub 401 のためこの環境からは実行できず、GitHub MCP で直接確認した）。⚠️ **55ファイル・+3877/-621・migration 3本と規模が大きいのにレビューが1件も付いていない**ため、この PR で意図して残した判断7件も書き出した。主なもの: ①**migration 246 が過去の通知を機械で既読にする**（請求書未発行あての `inv_late` だけ・行は消さない）— 人が読んでいない通知を機械が既読にするのは初めてで、PR 本文で方針の確認を求めたまま回答が無い状態でマージされた ②**督促の宛先を `editor` 全員＋`system_admin` 全員から `manager` ＋当事者に狭めた**ため、いままで届いていた人に届かなくなる ③**督促の頻度を毎朝から節目（超過1・7・30日目、以後30日ごと）に変えた**のを運用では確かめていない ④**実装は3エージェントの並行作業**で、統合時に担当の切れ目で見つけた3件の不具合（受信箱だけ条件を写していて件数が食い違う／SQL のテンプレートリテラル内のバッククォートによる構文エラー／共通化で古くなった既存テスト2件）は**誰にも確かめられていない**。あわせて、直さないと決めたのではなく範囲が別で持ち越した2件（機材の返却遅れが借用者本人に届かない・「ストック→見直し→見送り」率を数えていない）を ❌ のまま表に足した。
