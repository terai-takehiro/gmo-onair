**新しいブロックアプリ「Wiki」を設計し、段A（器）まで作った**（ご依頼: 「Notion のような Wiki アプリを、MD ファイルネイティブにして AI 連携をしやすく」）。マニュアル・手順書・社内ルールを Markdown で書いてツリーに並べ、検索でき、AI が読める場所。**本文の正は DB の列 `wiki_pages.body_md`**（Markdown の文字列そのもの）で、`.md` として触りたい場面は REST・MCP・書き出しの3つの口で満たす。設計書は `docs/design/v4/wiki.md`、押せるモック（PC 7・スマホ 2・地図 1）は `docs/design/v4/mockups/native/wiki/`。判断12件（名前は Wiki／本文の正は DB の列／データベースも作る＝行＝ページで表・ボード・カレンダー／同時編集は v1 編集ロック／新しい区画 `wiki`／検索は拡張なしで開始／AI に聞くは heavy／出典なしは答えず「足りないページ」に登録／スマホでも編集／見直し予定は既定なし／画像は小さなものだけ ONAiR）は1件ずつ伺って全件決定済み。
**段A で作ったもの**: migration 303 の13表（`wiki_spaces`／`wiki_pages`／`wiki_page_versions`／`wiki_links`／`wiki_databases`／`wiki_ai_*` ほか）／`shared/src/wiki/`（Markdown の純関数＝見出し・リンクの抜き出し、YAML の読み書き、検索の点数付け。React も DOM も使わない）／サーバーの読み取り口8本（スペース・ツリー・ページ・履歴・閲覧の記録・ホーム）と**閲覧範囲の判定**（スペース単位。読めないページは 403 ではなく **404** で存在ごと隠す。下書きは書いた人・担当・管理者だけ）／新しいワークスペース `client-wiki`（`/wiki`・ポート5181）の4画面（ホーム・スペース・ページ・履歴）。本文は react-markdown＋remark-gfm で描き、**`rehype-raw` は入れない**（本文に `<script>` が混ざっても素通しにしない）。足した拡張は注意書き（`> [!NOTE]` ほか4種）と ONAiR カード（段落1本のリンクのときだけカードにする）の2つだけ。アプリ登録（`apps.ts`）・権限の区画 `wiki`・`serveApp('/wiki')`・Dockerfile のビルド段・検査スクリプト12本の配線も入れた。**編集・データベース・検索・AI・見直しは段B〜F**（画面からはまだ書けない）。
**「期限切れ」という言い方をやめた**（利用者のご指摘: 「Wiki なので期限切れという概念はないと思います」）。ページは見直し予定日を過ぎても中身が無効になるわけではないので、**バッジは「要見直し」**・色も異常の赤ではなく情報の色にし、項目名は「見直し期限」→**「見直し予定」**、添える文は「10日 超過」→「10日前」に直した。設計書・モック・型のコメントも合わせた。
**ナビの列を1本に減らした**（利用者のご指摘: 「サイドタブが増えすぎて、この UI だとすごく窮屈」）。「共通の左メニュー ＋ Wiki のツリー ＋ 本文 ＋ 情報」の4列で、1440px の本文が 608px しか残っていなかった。ツリーを共通の左メニューの中へ移し（予定のミニカレンダーと同じ差し込み）、右の情報パネルを開閉式にして既定を閉じにした。本文は 1440px で **860px**（最大幅）になる。閉じている間は目次と情報を本文の下に続けるので、どちらの状態でも情報に辿り着ける。
**あわせて「この道具の決めごと」のような開発文書の比喩語が画面に漏れる件をルール化した**（同日のご指摘「謎表現が相も変わらず続いています。抹殺を。ちゃんとルール化してください」）。`docs/wording.md` にルール11を足し、`scripts/check-ui-tokens.mjs` の `metaphor-wording`（画面のコード・記録なし＝1件で止まる）と新しい `scripts/check-mock-wording.mjs`（Claude が作るモックの画面文字）を `npm run lint` に足した。画面の実装に残っていた9件も直した。
検証: `npm run typecheck:all`（0 errors）・`npm run lint`（0 errors / 54 warnings＝着手前と同じ）・`npm run test`（94件）・`npx vitest run --root shared`（2,540件。うち Wiki は Markdown の純関数36件と、shared とサーバーの複製が同じ答えを出すことを固定する突き合わせ6件＝`shared/tests/wikiMarkdownParity.test.ts`。突き合わせには同じ見出しの重複・行末の井桁・CRLF・柵の入れ子・title 属性つきリンク・検証データの実物を足した）・`npm run build -w client-wiki`・実ブラウザ（Chromium・検証用 Postgres）で 1440px と 375px の4画面を描画し、横はみ出し 0・JS エラー 0・注意書き／チェックリスト／表／ONAiR カードの描画を確認。権限は区画なし=403・他人の下書き=404 を実測。
