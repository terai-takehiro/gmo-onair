**docs/README.md の「guide/ は準備中」を解消して6本の説明書への目次を載せ、PR #369〜#378 の棚卸しを記録した**（コード変更なし）。R4 の「だれでも読める説明書」6本がユーザーの読み合わせを経て全てマージされたので、docs の目次に「だれでも読める説明書」の節を新設した（エンジニアでない人の入口）。棚卸しは2種を区別して記録: #369〜#372 はレビュー0件のまま CI green でマージ（うち #370・#371 はユーザー判断を受けての実行で、この2本の解消により本番依存の High 脆弱性が0件になった）。#373〜#378（guide/ 6本）は**レビュー0件マージではなく**、R4 の決めごとどおりユーザー自身が読み合わせのうえマージした（修正指示0件）。検証: `npm run lint`（check-changelog / check-md-links 含む）OK。
