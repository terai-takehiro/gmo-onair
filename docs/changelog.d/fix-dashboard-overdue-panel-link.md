**案件管理ダッシュボードの「期限が過ぎたやること」パネルのリンクが、パネルの中身と無関係な画面へ送っていたのを直した**（多エージェント監査 `docs/reviews/2026-08-24-logic-audit.md` の指摘）。このパネルは `GET /dashboard/overdue-actions`（`activity_logs.next_action` の期限超過を数える）を並べているのに、「やること一覧で見る」の行き先は `/sales/tasks/list`（`project_tasks` を GLS-A 案件だけに絞った別集合で、次回アクションは1件も含まない）になっており、押しても並んでいた項目はどこにも出てこなかった。同じダッシュボード内の `TaskHubCard.tsx` で先に直っていたのと同型の不具合。行き先を、次回アクションを実際に一覧できる「営業活動記録」（`/sales/activity-logs?sort=next_action`）へ変えた。あわせて `ActivityLogPage.tsx` の並び順が `?sort=` を初回読み込み時に反映していなかった（URL 引数はこれまで `tab` だけが対象だった）ため、深いリンクが実際に次回アクション期限順（＝超過分が先頭）で開くよう対応した。検証: `npx tsc -b client`・`npm run test`（1461件）・`npm run lint` を確認済み。実ブラウザでの動作確認はこのセッションから行っていない。
