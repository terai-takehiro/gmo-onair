**全画面をmacOS/iOSネイティブ級にする追加計画を起こし、進捗表の古いバグを1つ直した**（コード変更なし）。
ユーザー指示（PC専用を原則廃止し、全画面をPCはmacOSアプリ風・スマホはiOSアプリ風にする）を受けて
`docs/v4-native-ui-plan.md` を新設し、対象範囲の決定（凍結4アプリは対象外・⏸据え置き画面は
基本作り直す・既にv4化済みの56画面も同時に見直す）を記録した。調査の過程で
`docs/v4-progress.md` の生成スクリプト（`scripts/v4-progress.mjs`）が
「意図して据え置き」判定でファイルの実在を確認しない作りだったため、
別の回で削除済みの旧「（旧）パートナー」画面（`/studio/partners`。①予定へリダイレクト
済み）が存在しない画面のまま進捗表に残り続けていたバグを直した（56/62 → 56/61）。
残る旧カレンダー2画面（旧スタジオカレンダー・旧自分の予定）は見た目の負債ではなく
①予定にまだ無い固有機能（香盤ビュー・予約を直す導線／取込元の詳しい色分け・外部連携設定）
を持つことを実装で確認し、「モックから作り直す」ではなく「機能を①予定に吸収してから
退役させる」方針に切り替えたことを計画書に記録した。
