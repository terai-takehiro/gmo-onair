**Phase 3-3（`customers`/`vendors` テーブル削除）に向けた参照箇所の再調査を行った**（コード変更なし）。着手条件②達成後、実コードに対して `customers`/`vendors` への直接参照を機械的に洗い出し直したところ、`docs/reviews/phase3-2-plan.md` の表に無かった新規参照箇所（`platform/routes/data-viewer.routes.ts` の `ALLOWED_TABLES` — system_admin 用の汎用テーブルビューアがテーブル名として `'customers'`/`'vendors'` を直接ハードコードしている）を発見し、テーブル削除PRで同時に除去する対象として追記した。あわせて `sga.routes.ts`・`doc-handoff.service.ts` は直接参照ではなかったため一覧から訂正し、`companies.is_customer`/`is_vendor` 列が既に存在するが `DELETE /customers|vendors/:id` がこれを更新していないという設計課題も記録した。
