**PR #596（#595 の棚卸しと自己修正4件）のレビュー棚卸しを記録した**（決めごと: [docs/branching.md](docs/branching.md) 「マージしたら、その PR のレビューを棚卸しに移す」）。⚠️ **この PR もレビューが1件も付かないままマージされた** — 📝 Code Review は Codex の usage limits で一度も実行されず、🔒 Security Review だけが `23e98e8` で完了（findings なし）。**最後のコミット `6fc0292` はどちらのレビューも受けていない**（`get_reviews`・`get_review_comments` とも0件を API で確認。`npm run reviews:debt` はこの環境のトークンでは401）。表に移す指摘は無いが、自分で見つけて直した4件（束の処理月・支払サイトを後続の書類で埋める／束の削除確認を取引の中で `FOR UPDATE`／その押さえ方が0行だと効かなかった件／詰め物なしの base64 を落としていた件）を [docs/reviews/codex-findings-v4.md](docs/reviews/codex-findings-v4.md) の表に書き出した。

**メール自動仕分けのルーティンを実メールで試験実行し、枠の食い潰しを3件直した**（書き込みは一切していない読み取りのみの試験。詳細は [docs/reviews/2026-09-06-mail-intake-taxonomy.md](docs/reviews/2026-09-06-mail-intake-taxonomy.md) の「6. 試験実行」）。①ルーティンの検索式そのままだと3日窓に201スレッドあり、そのうち `notifications@github.com` が新しい順40件中33件（`dmarc-report@` と合わせて87.5%）を占めていた。GitHub は開発中1日25通以上出すので、**1回30通の枠は毎回 GitHub の通知だけで尽き、その裏の請求書には永遠に届かない**。決定表 #1 は「拾ってから捨てる」ためラベルでは間に合わず、検索式に `-from:` を4つ足して 201→54 に落とした。②「古いものから30通」は `search_threads` が新しい順にしか返さないため実行できず、その日の請求書が後回しになるので「新しいものから30通」に変えた。③検索結果の抜粋は「いちばん古い5通」で切れている印も出ないため、6月に始まって今日返信が来たスレッドを6月の本文で仕分けてしまう（実測2件）。判断は `get_thread` の最新メッセージで行うことを3か所に明記した。あわせて社内メーリス経由の通知（54件中12件）が決定表の「残り」に落ちて `record_inquiry` に大量に入る問題に #1b を足した。⚠️ **検証環境での実取り込みは未実施** — この会話の GMO ONAiR MCP コネクタは本番を指しているため。
