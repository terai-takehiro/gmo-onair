**版の付け方を機械で守るようにして、PR どうしがぶつからないようにした**（ご指摘）。⚠️ **決めごとは前からありました** — `CONTRIBUTING.md` に「バージョンはリリースのときだけ上げます（PR ごとには上げません — 複数のブランチが同時に `package.json` を触ると必ず衝突します）」と**書いてあったのに、守られていませんでした**（守っていなかったのは私です）。**文章で決めるだけでは足りない**ので、機械で止める形にしました。⚠️ **直近2週間で `CLAUDE.md` は 55 コミット・`README.md` は 42・`package.json` は 45 動いており、PR のたびに同じ3か所を書き換えていました。** しかも書き足す場所が「履歴のいちばん上」なので、**2本を並行して出すと必ずぶつかります**（実際に #124 と #125 で 5 回、`main` を取り込み直して直しました）。**ぶつかったのは毎回この3か所だけで、コードは1度も競合していません** — つまり衝突は作業の中身ではなく**版の付け方**が作っていました。番号は「枝を切った時刻」ではなく**マージされた順**で決まるので、着手時に取ると先に入った PR と取り合いになります（#123 が先に入って v4.0.27 を使い、こちらの2本が同じ番号を取っていました）。**作業 PR は `docs/changelog.d/<枝の名前>.md` を1つ置くだけ**にし（新しいファイルなので衝突しません）、番号と履歴への差し込みは**リリースの1本**（`npm run release:notes -- X.Y.Z`）に寄せます。**機械で止めます** — `npm run lint` の `check-changelog.mjs` が、作業 PR で版の3か所を触っていたら落とします（リリースは `RELEASE=1` か `release/` の枝で通ります）。`docs/version-history.md` は追記しかしないので `.gitattributes` の `merge=union` に任せます（**`CLAUDE.md` には使いません** — 文章なので両方残すと混ざります）。**検証**: 版を取った状態で `check-changelog` が落ち（**反証: 戻すと通る**）、`release:notes -- 4.0.30` を**実際に走らせて** CLAUDE.md の先頭に積む・4件目をアーカイブへ移す・README と package.json を上げる・下書きを消す、までを確かめてから戻しました。**DB 変更なし。**
