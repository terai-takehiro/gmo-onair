**テロップCGの画面の言葉を、決めた語に統一した**（同じものが2〜3通りの呼び名で出ていた）。①`docs/design/v4/graphics-redesign.md` §7「言葉の対応表（旧 → 新）」は段A〜Fで主な導線にだけ反映されていて、確認ダイアログ・お知らせ・空状態・入力欄のラベル・サーバーのエラー文には旧語が残っていた——同じ依頼が一覧では「未作画の発注」・フォームでは「テロップの発注（テロ原）」・状態バッジでは「ページ化済み」、同じ1枚が「ページ」と「テロップ」、同じ分類が「部品」と「パーツ」と「種類」で出ており、押す前と押した後で言葉が変わる（「全部消す」を押すと「すべてのスロットをクリアしますか？」が出る）場面もあった。②画面に出る文字列を全部読み直し、対応表どおりに寄せた: 発注／テロ原／未作画 → **依頼**、ページ → **テロップ**、部品／パーツ／部品ライブラリ → **種類**、スロット／出る場所／自動OUT／自動退出ルール → **出る位置／同時に出せないもの／自動で消す**、送出コンソール → **本番モード**、校正 → **確認**（「確認済」も「確認済み」に）、テーマ → **見た目**、演出SE → **効果音（ランキング発表）**、CGプロジェクト → **この番組**。戻りリンクは対応表どおり「テロップ一覧」1種類にし、テンプレート管理の「部品ライブラリへ戻る」（押すと転送でテロップ一覧に着くので行き先と文言が食い違っていた）も直した。③`docs/wording.md` のルール5・8・9・11 に触れるものも同時に直した: 別VPS・`awards_events`・owner ID・`client-awards` のような内部の名前を画面から外し、APIキー → **鍵**（モックの表記）、投入する → 作成する、解析中 → 読み込み中、推奨マッピング → 割り当ての候補、「①をピック」 → 「①だけにする」、枕詞 → 前に出す文言、「続き」 → 「進める」、「直せます」 → 「編集できます」。「見つかりませんでした」「開けませんでした」だけだった空状態も、何が無いのかが分かる文型（`案件・番組が見つかりません` / `テロップCGを開けませんでした`）に揃えた。**「やめる」「絵」「出す順」はモックにある語なのでそのまま残した**（モックが v4 の正 — `docs/v4-plan.md` の「大前提」）。④依頼の状態バッジは「テロップ化済み」で7字になるため固定幅を 88px → 96px に広げ、あわせて取り消せない行に同じ幅の空きを置いた（値の有無でバッジの右端がずれていた・`docs/design/v4/_rules.md` の規律1）。サーバー側も、画面に出る `テロップCGが見つかりません`・`テロップ名になる列が空です` などのメッセージを同じ語に直した。検証: `npx tsc -b client-techops server`・`npm run lint`（0 errors。`ui-tokens` の `forbidden-wording` が下書き段階の「ぜんぶ」を1件捕まえたので直した）・`npm run test`（shared 2,548件＋サーバーのレビュー試験 94件）・実ブラウザ（検証用Postgres・案件と依頼と3枚のテロップを入れて 1440px と 375px でテロップ一覧／本番モード／設定4タブ／依頼／テンプレート管理を開き、横はみ出し0・JSエラー0・バッジの桁揃いを確認）。棚卸しの記録は `docs/design/v4/graphics-redesign.md` §7 に追記した。
