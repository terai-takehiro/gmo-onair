**PC専用の判定が疑わしかった7画面を実測し、開けるものはスマホに開放した**（マルチエージェント監査
`docs/v4-native-ui-audit-2026-08-20.md` の続き）。費用を分け合うグループ一覧・拠点部屋・権限と
メンバー・通知とテンプレート・GPM標準工程テンプレート・お金のルール・休日営業時間の7画面を実
ブラウザ375pxで確認し、`document.scrollWidth` では検知できない崩れ（和文が1文字ごとに縦へ
折り返される・画面外に切れるが横スクロールバーは出ない）を数件見つけて直したうえで、すべて
`CLIENT_MOBILE_OK` へ移した。ついでに見つけた別の実装バグ（お金のルールの値引き上限で、
上限を決めていない役割の編集フォームが常に開いた状態になる。原因は `GET /money-rules` の SQL が
`LEFT JOIN` 先の列から `role_id` を返しており、上限未設定の役割ではその列ごと NULL になっていた
ため）も根本のSQLで直した。検証: `npx tsc -b server client` / `npm run lint` / `npm run test`
（1142件）OK・実データを入れた検証用DBでAPIの応答を確認済み。
