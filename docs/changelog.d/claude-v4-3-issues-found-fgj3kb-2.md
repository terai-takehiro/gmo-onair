**制作技術支援・計時・視聴者のブラウザタブのタイトルが旧アプリ名のままだったのを直した**。制作技術支援（`client-techops/`）は2026-08-22に「Qシート」→「制作資料」→「制作技術支援」と改名済みだったが、`index.html` の `<title>` だけ「GMO ONAiR Qシート」のまま取り残されていた（ユーザー指摘）。同種の残骸を横断調査し、同じく2026-08-22に「計時LIVE」→「計時・視聴者」に改名済みの計時・視聴者（`client-live/`）にも同じ取り残しを見つけて直した。表示画面（`/live/display/:timerId`）の見た目は変えていない。あわせて PR #387（v4.3 実機点検の不具合修正）のマージ後棚卸しを記録した（コード変更なし）。
