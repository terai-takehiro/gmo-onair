**横断検索・全データバックアップの顧客側が `customers` テーブルへの直接参照に依存しないようにした**（`customers`/`vendors` テーブル削除に向けた準備の一部）。横断検索（`GET /search`）の顧客検索が行っていた `customers` 行の生存確認（`EXISTS`）を、`companies.is_customer` だけで足りるよう簡略化し、全データバックアップ（`GET /admin/backup.xlsx`）の「顧客」シートも `customers` テーブルではなく `companies` から読むよう変更した。仕入先側（検索・バックアップとも）は `budget:editor` の単独編集を反映するため `vendors` を正として読み続ける設計のままで対象外とした。検証: `npm run typecheck` / `npm run lint` / `npm run test` OK、`npm run verify:up` の実DBで横断検索・バックアップExcelの顧客データを確認した。
