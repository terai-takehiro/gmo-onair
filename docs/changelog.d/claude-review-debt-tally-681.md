**PR #681のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」）。#681（#679・#680のレビュー棚卸し記録）はCode Reviewが2巡とも数分以内に反応し、1件（P2）の指摘が付いた——**棚卸しの記述そのものが実測と食い違っていた**もので、「#679のSecurity Reviewは以後12コミットぶんのpushに再実行されていない」「その中に`/org-seed`の新設が含まれる」と書いていたが、実際は#679のコミットは全13本・`2c8a34df`は8本目でその後のpushは5本、`/org-seed`と`gpm_members.email`の照会は祖先`b16daaae`で入っており読み込み範囲には含まれていた。レビューの穴を過大に記録するのは、実測と食い違う記述を残さないというこの文書の目的そのものに反するため、棚卸し・changelog下書き・PR本文の3か所を直して返信・スレッド解決まで完了した。Security Reviewは完走しfindings無し（ただし修正コミット`a3fb6156`には再実行されていない）。表に移す未対応の指摘は無いが、**棚卸し自身も間違える**ので数や範囲は文章ではなく実物（`get_commits`の並び）で数える、という学びとあわせて記録した。検証: `node scripts/check-md-links.mjs`。
