**顧客一覧・詳細・360°ビューが `customers` テーブルへの JOIN に依存しないようにした**（`customers`/`vendors` テーブル削除に向けた準備。`customers.routes.ts` のみ対象・`vendors.routes.ts` は未対応）。基本情報（名前・連絡先等）は既に `companies` 側に同期済みの値を読んでいたため、`DELETE /customers/:id` が `companies.is_customer` も更新するようになったこと（前回のPR）を前提に、`customers` への INNER JOIN（`CUSTOMER_JOIN`）を撤去し `companies.is_customer = TRUE` だけで判定するよう変更した。あわせてどこからも参照されていなかった `legacy_customer_id` フィールドも応答から外した。`vendors.routes.ts` 側は `budget:editor` 単独編集時に `companies` へ同期しない設計（PR #183 P2）のため `vendors` の値が `companies` より新しい場合があり、今回は対象外とした（`docs/reviews/phase3-2-plan.md` の一次分析・テーブル削除時の反映で扱う）。検証: `npm run typecheck` / `npm run lint` / `npm run test` OK、`npm run verify:up` の実DBで一覧・詳細・360°ビュー・新規作成・更新・削除・旧ID(legacy customers.id)経由のアクセスをすべて確認した。
