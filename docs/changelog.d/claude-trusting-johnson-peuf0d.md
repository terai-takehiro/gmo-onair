**制作技術支援に新しいミニアプリ「技術資料」を作った**（利用者からのご依頼・2026-09-22）。BOX の完成図書（ビデオパッチ盤外観図・映像系統図）をもとに増設機材の映像プランを組みたい／協力会社（ヌーベルバーグ）のメンバー表 PDF から技術スタッフのリストを作りたい、というご依頼。まず設計書 `docs/design/v4/tech-docs.md` と8枚のモック（画面の文言は `docs/wording.md` のルール5〜11 に合わせて総点検）を作り、そのままサブエージェント並列で段A〜Dを実装した。①一覧と作成（複製）・②映像パッチ（機材を選ぶ → その機材に立ち上がっているパッチ番号だけに候補が絞られる・空きが先・使用中の印・増設機材は手入力・名称は行ごとに編集）・③技術スタッフ（作業日ごと・役職はメンバー表の語と順・技術人員からの候補と手入力）・④書き出し（A4横に現場のパッチ表と同じ矢印表記 `CCU1 out [101A] → 入力ルーター in [136B]`・PDF）・運営マニュアルの差し込みブロック `tech.patch`／`tech.staff`・⑤パッチ盤（VJP100〜1800・A段／B段の絵・パッチ番号ごとの機材と名称の転記・進捗）・⑥技術人員（会社ごとの人・主な役職・参加回数）・スマホは閲覧。データは migration 305（`qsheet_tech_*`・`qsheet_patch_*` の7表＋盤18枚と空のパッチ番号1,536行）。同時編集は Yjs を使わず資料まるごとの編集ロック＋楽観ロック（会場図面と同じ）。パッチ番号と機材の対応は PDF の文字抽出では取れないことを実測で確かめたため、初期データは人が外観図から転記する前提（⑤に「転記の進捗 n／48」）。技術人員の初期登録は個人名を git に残さないよう migration ではなく `scripts/tech-persons-import.mjs`（メンバー表の抽出 JSON／CSV から会社と人を upsert・冪等。手順は `docs/ops/tech-persons-import.md`）で入れる。設計書 §13 の判断待ち9件（接頭辞 `TD`・A段/B段の意味づけ・「CA」の正式名称・1資料に複数の作業日・会社の持ち方・個人情報の扱い・音声パッチ・機材台帳との接続・名前の再利用）は主担当の推奨で実装し、決定が変われば合わせる。検証: `npx tsc -b client-techops`・`npx tsc -p server --noEmit`・`npm run lint`（0 errors）・`npm run test`（shared 2,555 件・server 100 件）・検証用 Postgres で migration 305 の実適用と API の実走（CRUD・409・複製・確定）・実ブラウザ（Playwright）で①〜⑥とロック衝突・スマホ 390px を操作して確認、`npm run verify:ui techops` 573／573 通過（技術資料3画面を追加）。
