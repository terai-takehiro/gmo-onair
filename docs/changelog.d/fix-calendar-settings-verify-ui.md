**カレンダー・設定画面の見た目の崩れ6件と、スマホの潰れ2画面を直した**（PR #353 で verify:ui に追加された検査が見つけた既存の崩れ。R5）。
①カレンダー予定（PC）: 月表の「月の外」の日付が読ませる文字に使わない決まりの薄い色（`#9aa1ab`）で描かれていた11件を `--muted-foreground` に。左メニューのマイカレンダーのチェック（38px）とツールバーの「今日」・前後（30px）・絞り込み帯（28px）をボタンの高さの段（36px/32px）に載せた。
②部屋の空き（PC）: 「今日」・前後ボタン（30px）を 32px に。
③仮押さえ（PC）: 残り日数バッジの幅を `TableBadge` の枠（7段の 72px）に持たせ、「あと12日」のような5字超の行だけ右端がずれていたのを直した。
④設定のデータ画面: 編集・削除ボタン（28px）を 32px に。金額列が「¥7,200,000」の1要素だったのを shared の `<Money>`（¥と数字が別要素）に置き換えた。
⑤スマホの潰れ2件（verify:ui の潰れ検知が報告）: お金のルールの値引き上限の表頭が固定列に押されて「役割」が幅 0px になっていた（表頭はスマホでは隠す。本文は縦積みで読める）。休日・営業時間の曜日の注記が残り幅 20px に潰れて読めなかった（スマホでは1行を独占させ、空のときは行ごと出さない）。
検証: `verify:up`＋実サーバー＋同梱 Chromium で対象16ページ×3幅を実走し、6件の FAIL と潰れ報告2件が消えたことを確認。さらに client アプリ全60ページ×3幅（1,823/1,835 通過）で他ページに新しい崩れが出ていないことも確認した（残る FAIL は検証環境で計時LIVE がビルドされていないことによるものと、機材・貸出設定の既存のバッジずれ＝別途）。verify-ui.mjs の判定式は変えていない。
