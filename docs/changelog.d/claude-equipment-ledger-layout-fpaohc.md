**機材台帳の列が隣に重なって読めなくなっていたのを直した**（ご指摘・実ブラウザで実測）。①⚠️ **「種別」のバッジが隣の「設置場所」の上に乗っていました** — 画面では `設備/その他設備` のすぐ後ろに `ACK1` と出ており、実際の値は `RACK1` で**先頭の1文字がバッジの下に隠れて**いました。原因は**列の幅が 72px しかないのに、その列に出る文字が最長 112px** だったこと。この列に出るのは**種別 ＋ 区分をつないだ文字**（`sectionDisplay()`）で、実測すると **16 通りのうち 10 通りが 72px を超えます**（`ネットワーク設備` 109.6 ／ `設備/その他設備` 104.0 ／ `LED/XR設備` 88.7 ／ `インカム設備` 87.1 ／ `カメラ設備` 76.7。バッジの幅が固定されるのは**和文4字まで**で、それ以上は自然幅になります）。②⚠️ **これは「エラーが出ない壊れ方」でした。** 列の幅の指定は正しく効いており、`RowSlot` は `shrink-0` なので**押し出されずに上に重なるだけ**です。開発ツールにも何も出ず、**気づけるのは画面を見た人だけ**でした。**寸法表の段で 112px が入るのは 128** なので、種別を **72 → 128** に上げました。台帳全体を広げないよう、**備考を 200 → 160 に1段落として**相殺しています（備考は必ず1行で省略される列で、全文は `title` で読めます）。③**同じ壊れ方が「貸出機材」のタブにもありました** — 種別の 72px で `ネットワーク`（実測 87.6）がはみ出し、**カテゴリの列は利用者が設定で作る名前**なので長さに上限がありません。種別を 96・カテゴリを 128 に上げ、**どちらも `overflow-hidden` で隣に重ならないようにしました**（切れて読めないより、重なって**両方**読めないほうが悪い。全文は `title`）。⚠️ **止め具を外さないこと** — 種別が増えた日に、同じ壊れ方を静かにやり直します。④**「横に流し始める幅」の算数が 20px 足りませんでした** — 隙間を**列の数だけ**掛けており（正しくは列の数 − 1）、行の左右の余白（`px-4` × 2 = 32px）を足していませんでした。**これも画面には何も出ません**（痩せるのは伸びる列＝商品名だけで、`200px` を割ってから横に流れ始めていた）。`ledgerMinWidth()` の1か所に集めて `shared/tests/equipmentLedgerWidth.test.ts` で固定しました。⑤**貼り付けた「操作」の列に、下に列があるあいだだけ影を出すようにしました。** `right: 0` の `sticky` は**必ず下の内容に重なります**（横に送りきれば操作は本来の位置に戻り、隠れていた列が出てきます）。罫線1本だけだと**中途半端に切れた列**にしか見えず、これも「崩れている」と読めていました。⚠️ **狭い画面では、いちばん右の「貸出可」は横に送るまで見えません**（実測: 台帳の幅の要求は 1,384px なので、それより狭い枠では必ず重なります）。**これは直っていません** — 消すには既定の列を1つ減らすか、操作の列を横スクロールの外に出す作りに変える必要があり、どちらもご判断が要ります。⑥**検証**（実ブラウザ・同梱の LINE Seed JP）: 枠 1,253px で **種別 16 通り・貸出機材の4行とも、どのバッジも自分の列の外に描かれていない**（当たり判定でも 0 件）、**11 列 × 17 行の左端が 1px 以内でそろっている**、影は**重なっているときだけ出て右端まで送ると消える**。枠 1,450px では横に流れず影も出ません。`npm run test` 970 項目（+6）、`typecheck` exit 0、`lint` 0 errors。⑦**`shared/src/` は触っていません**（足したのは `shared/tests/` と、テストが各アプリの `@/…` を読めるようにする `shared/vitest.config.mts` だけ）ので、凍結4アプリの CSS は不変です。⚠️ **その設定が無いと、値を1つ import した瞬間にテストファイルごと落ちます** — 今まで通っていたのは**たまたま型だけの import だったから**（型は変換で消えるので解決されなくても通る）。**DB 変更なし。**
