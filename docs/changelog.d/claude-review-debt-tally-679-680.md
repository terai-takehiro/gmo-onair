**PR #679・#680のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」）。①#679（`feat(techops): 運営マニュアルに体制図ブロックを足し、用語を一般的な語に統一した`）はCode Reviewが5巡すべて数分以内に反応し、全10件（P1×1・P2×9）の指摘が付いた。10件とも本PR内で修正・返信・スレッド解決まで完了している（ドラッグが黙って死ぬ／ブラウザ外で離すと宙に浮く／アンマウント時に購読が漏れる／**P1** 取り込み中にページを送ると移った先が上書きされる／取り込みが通信中の編集を巻き戻す／同名チームへの取り込みで所属が落ちる／StrictModeの再セットアップで取り込みが全部捨てられる、ほか作成ダイアログの3件）。表に移す未対応の指摘は無い。ただし実際にマージされたコミット（`024858e9`）にはCode Reviewが一度も走っておらず、要約表のCommit列は5巡目の`e1c9a75d`で止まったまま約70分後にマージされた。Security ReviewもPR最初のコミット（`2c8a34df`）に読み込みが固定されたまま完走し、以後12コミットぶんのpush（担当者メールを返す`/org-seed`の新設を含む）には一度も再実行されていない——#651・#677と同型で、「✅ Completed」の表示だけではマージされたコードが読まれたことにならない。②#680（#678のレビュー棚卸し記録）はCode Review・Security Reviewとも完走し、Code Reviewから1件（P2・usage limitsによる不発の本数を6本と書いたが列挙は7本だった）の指摘が付き、修正・返信・解決済み。こちらも修正コミット（`c264defc`）にはどちらのレビューも再実行されないままマージされている。検証: `node scripts/check-md-links.mjs`。
