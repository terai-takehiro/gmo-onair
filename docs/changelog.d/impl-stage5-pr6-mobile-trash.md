制作資料のスマートフォン編集で、行・ロールの削除がゴミ箱を通らず、消すと戻せなかった不具合を直した
（PC と同じくゴミ箱（TrashDrawer）経由にした。行削除は PC と同じく確認ダイアログを出さない）。
通常ロールの尺（今まで CM/VTR だけ）もスマートフォンから編集できるようにした。行のハイライト表示が
`scenario` という固定の列 ID を決め打ちしており、サイドバーで足した台本列ではハイライトが出ない
不具合も直した（scenario 型の列を動的に探す形にした）。あわせてロール操作の上下移動・削除ボタンの
タップ領域を 44px（`min-h-tap`）に広げた。
