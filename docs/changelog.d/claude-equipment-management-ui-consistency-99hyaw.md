**機材管理のUIルールがページごとにバラバラだったのを直した**（UI一貫性調査を実施しマルチエージェントで着手）。①機材台帳・貸出返却等の8画面はv4共通トークン（`PageHeader`・`text-h1`・`rounded-card`等）でほぼ統一されていた一方、**ラック図・機材詳細の2画面だけv4化から取り残されており**、ページ幅・見出しのサイズ/太さ・角丸・パレット色（`amber-*`等の直書き）・空状態やエラー表示がこの2画面だけ独自実装になっていた。②特に機材詳細ページの`CardTitle`は`font-semibold`を指定していたが、**LINE Seed JPは500/600ウェイトを配信しておらず400に落ちるため、太字にしたつもりの見出しが実際には太字で表示されていなかった**（他画面のカード見出しは太さをベイクした型スケールを使っており実害なし）。③両画面を`PageHeader`・`rounded-card`・意味トークン（`warning-surface`等）・`Delayed`/`ErrorPanel`/`EmptyState`/`NotFoundPanel`・`Row`系コンポーネントに揃え、機材詳細の生の`<button>`はshadcn `Button`に、ラック図の周辺ダイアログ・機材台帳のExcelインポート系ダイアログに残っていた`font-semibold`/`font-medium`の直書き（LINE Seed JPで太字が効かないウェイト）も明示的な`font-bold`等に置き換えた。ラック配置図のドラッグ・クリック・印刷ロジックおよび機材詳細のデータ取得・フォーム送信ロジックは変更していない。検証: `client-equipment`の型チェック（`tsc -b`）通過、`node scripts/check-ui-tokens.mjs`で既存の記録件数を超えず（`missing-font-weight`は176→130件に減少）。
