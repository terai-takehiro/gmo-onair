**PR #740 の最後のコミット（`115cc1e`）が、Codex の利用上限でレビューされないままマージされたことを棚卸しに記録した。** それより前の巡で届いた指摘（26スレッド）はマージ前にすべて直して解決済みだが、最後の直し（スペースを「メンバーだけ」に切り替えるときに担当を見直す）だけは誰にも読まれていない。記録しないと画面上は「指摘なし」と見分けが付かないため、`docs/reviews/codex-findings-v4.md` の「レビューが0件のままマージされた PR」に足し、次に Wiki を触る PR でこの差分も読ませるよう書いた。検証: `node scripts/check-md-links.mjs`・`node scripts/check-changelog.mjs`。
