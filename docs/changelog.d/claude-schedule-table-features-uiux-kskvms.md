**制作技術支援＞スケジュール表で、列を画面から作れる・直せるようにし、空の表に「最初の一手」を出した**（9/6 ご依頼「現状 MCP くらいしかできる機能がなく、根本的な機能性が皆無」・第2版計画の PR1）。①⚠️ **新しく作った表は列が 0 本で、列を作る画面が無かったため、ひな形か AI を通す以外に何もできなかった**（「項目を追加」も押せない）。サーバー・MCP には列の追加・改名・並べ替え・削除が前からあり、画面に配線されていないだけだった。列が 0 本のときは「ひな形から／会場を選んで列を作る／AI で下書き」の 3 択を出し、「会場を選んで列を作る」はスタジオの部屋を複数選ぶと 1 部屋 1 列で会場列を作る（部屋の色を列の色に引き継ぎ、拠点が未設定なら表の拠点も埋める）。②列見出しの鉛筆から名前・部屋・色を直し、左へ／右へで並べ替え、削除（項目数を書いた確認つき）ができるようにした。列の色は見出しの丸と項目の左罫に出る。③見出しの 4 ボタン（ひな形・AI・Excel・項目）を「項目を追加」＋「…」（列を足す／ひな形を適用／AI で下書き／Excel）に整理し、`PageHeader` に載せたのでスマホでは「項目を追加」が下端に出る（これまでスマホから項目を足せなかった）。④項目の削除の確認を `window.confirm` から共通の `confirmAction` に替えた。部屋の一覧は `GET /techops/studio-rooms`（読み取りのみ）を新設して読む——カレンダー側の `/studios/locations` は営業権限の下にあり技術・運営のアカウントでは 403 になるため。

**続けて PR2 として、表そのものの編集・削除・共有ができるようにした。** ⑤表の設定シート（題・日付・拠点・案件/番組・回・表示時間帯・状態・備考）と削除を新設した——`PUT/DELETE /schedules/:id` は前からあったが、画面からの呼び出し元が1つも無く、題や日付を直す・状態を「確定」にする・表を削除する、が全部できなかった。見出しに状態バッジ（下書き／確定／保管）も出るようにした。⑥**案件メンバーには自動で共有する**（9/6 ご判断）。共有テーブルに写さず、`canAccessSchedule`・一覧の可視条件で `project_members`・案件の主担当を動的に見る形にしたので、メンバーの増減にそのまま追随する（マイグレーション不要）。番組はメンバー概念が無いので従来どおり明示共有のみ、進行台本の共有は変えていない。設定シートの「共有」節に「案件 GLS-xxxx のメンバー N 人には自動で見えます」と事実だけ出し、それ以外に見せたい人はユーザー検索から足す（`GET/PUT /schedules/:id/shares`。作成者・管理者のみ操作可）。⑦**実装中に見つけた既存バグも同時に直した** — `PUT /schedules/:id`（REST・MCP `update_schedule` 双方）は `キー: 条件 ? 値 : undefined` という組み立て方をしており、`updateSchedule` 側の「送られたか」判定（`'projectId' in input`）が**値が undefined でも常に true になる**ため、拠点・案件・番組・回・備考を**指定しなかっただけで毎回 null に戻す**状態だった。この経路はこの PR まで呼び出し元が無く誰も踏んでいなかった。スプレッド構文に直し、実 DB で「一部だけ更新しても他のフィールドが消えない」ことを確認した。

**続けて PR3 として、新規作成の入力欄・スマホの列絞り込み連携・ひな形設定への導線を整えた（サーバー変更なし）。** ⑧新規作成ダイアログに拠点・案件/番組・回・ひな形・本番開始時刻を足した。案件を選ぶと題（案件名）・日付（本番日の初日）を空欄のときだけ先埋めする——拠点は案件の事実に id が含まれない（部屋名の文字列のみ）ため先埋めしない。一覧の絞り込みから来た新規作成は、これまでどおり案件/番組を選び直させない。「案件／番組」の選び方は表の設定シートと共通の部品（`ScheduleOwnerFields`）にまとめた。⑨スマホの列チップの絞り込みを画面の見出しまで持ち上げ、「項目を追加」が絞り込み中の列を既定にするようにした——以前は絞り込んでいても常に先頭の列に追加していた。⑩「スケジュール表のひな形」への導線を左メニュー（`system_admin` のみ）とひな形適用ダイアログ（「ひな形を直す」リンク）に足した——これまで URL 直打ちでしか開けなかった。⑪ひな形の項目に区分・基準（当日00:00から／本番開始から）・オフセット・尺・必須を追加し、追加だけでなく編集もできるようにした（これまでは消して作り直すしかなかった）。ひな形の名前・説明・拠点の変更と複製ボタンも追加した。列の名前・部屋・色の変更は今回もできない（サーバーに対応する更新 API が無いため。画面には「消して作り直してください」と書いた）。

**続けて PR4 として、一覧を「イベント（案件・番組）ごと」にまとめ、絞り込み・状態の見え方を整えた（サーバー変更なし）。** ⑫9/6 のご判断どおり、一覧を案件・番組の単位で束ね、束の中を日付の近い順に並べるようにした（束の見出しに GLS 番号・案件名・番組名・日数・自動共有の対象人数を表示。データは変えず、束ねは画面側の純関数）。表のヘッダーには「同じイベントの他の日」を日付チップで出す（1 日しか無いイベントでは出さない）。複製（同じイベントの別の日として写す機能）は今回のスコープ外。⑬一覧に拠点・状態・題／案件の検索の絞り込みを追加した（200 件固定で探せなかった穴）。状態は「すべて」を選んでいる間だけ保管を画面側で隠し、「保管」を明示的に選ぶと保管だけが出る。⑭一覧・表の見出しに状態バッジ（下書き／確定／保管）を表示し、表が「確定」のときは項目の保存ボタン脇に「確定後の変更として保存します」と 1 行出す（保存自体は止めない・サーバーは判定しない）。⑮**実ブラウザ確認で見つけたバグをその場で修正した** — 拠点の絞り込み選択欄に「すべて」の選択肢を空文字（`value=""`）で作ったところ、Radix の `Select.Item` が空文字を「未選択」の特別な印として扱う仕様と衝突し、一覧画面全体が真っ白になる不具合があった。専用の印（`__all__`）に置き換えて空文字と往復させる形に直した。

検証: `server`/`client-techops` の型チェック、ESLint、UI トークン検査ほか lint 全項目、`shared` の Vitest 1996 件、検証用 Postgres（`npm run verify:up`）を使った実 API 確認（案件メンバーの自動共有・非メンバーからの 404・共有/削除の権限ゲート・部分更新バグの再現と修正確認）、実ブラウザ（Playwright・PC 1280 と 375px）で列作成・並べ替え・削除・表の設定・共有の追加・削除確認・新規作成の先埋め・ひな形の作成/列追加/項目追加/編集/複製・スマホの列絞り込み連携・一覧の束ね/絞り込み/状態表示・表の兄弟日チップまで通し、横はみ出し 0px・JS エラー 0 件。

**続けて「マルチエージェントで全て進めて」（9/6 ご指示）で、残りの段B（PR5〜7b・4本）をマルチエージェントで並列実装し、1本のブランチへ統合した。** 各PRは専用の git worktree・ポート・検証用Postgresに分離して独立に実装・検証させ、実装後にこのブランチへ順にマージし、統合後にもう一度まとめて実ブラウザ検証をしている。⑯**PR5: 項目のドラッグ移動・下端リサイズ（PCのみ・5分スナップ）**。保存は既存の `PUT .../items/bulk` を1件だけ渡す薄いラップで呼び、単発の項目編集と同じ楽観ロック（`expected_updated_at`・409の赤枠）をそのまま使い回した。実装中に見つけて直したバグ2件——Escキャンセル直後の素のclickが編集ダイアログを開いてしまう不具合と、横方向だけのドラッグ（列またぎ）が保存されない不具合。⑰**PR6: 複製（別の日として写す）と印刷CSS**。新設 `POST /schedules/:id/duplicate` は題・列・項目・表示時間帯・拠点・案件/番組/回を写すが、共有・状態（常にdraftへ戻す）・台本リンクは写さない。印刷はA3横・1日1枚で、共通シェルには触れず属性セレクタとTailwindの`print:hidden`だけで組んだ。⑱**PR7a: 既存台本への結び直し・逆引き・担当のユーザー名補完**。台本の結び直し・外すを項目編集シートに追加し、進行台本一覧側にも「どのスケジュール表に結ばれているか」のバッジを出した。担当欄はFKを張らず自由入力のまま、ユーザー名の候補だけ出す補完にした。実装中に見つけて直したバグ2件——逆引き一覧が案件メンバー自動共有の可視条件と食い違っていた不具合と、台本の結び直し中に開いたままの編集シートの打ちかけ内容が消える回帰。⑲**PR7b: 予約から会場列を入れる（コピー型）とAI下書きの拠点・種別条件**。その日のスタジオ予約から部屋を選んで列を作れるようにした（自動生成はしない・営業情報は返さない）。AI下書きダイアログに拠点・種別の任意項目を追加し、表を「確定」にした瞬間に対象のAI提案の事後差分記録（早期段のみ）を締める仕組みも新設した（新規テーブルは不要で、既存の提案テーブルの列を使い回せた）。⑳**統合後の実ブラウザ検証で、今回のPR群とは無関係の既存バグ2件を見つけた（未修正・後日対応が必要）**——(a) 項目編集シートを*初めて*開いたときだけ「項目名」欄が空欄になる（2回目以降は正しく表示される）。Radix Dialogの自動フォーカスと、IME保護のための`useBufferedValue`の「フォーカス中は外部値を取り込まない」仕様が競合するのが原因で、`useBufferedValue`は全社共通のフックのため今回は直していない。(b) AI下書きの「下書きを作る」を押すと常に500エラーになる（`assertGenerationAllowed`のSQLでPostgresがパラメータ型を決められない）。今回追加した拠点・種別の入力欄より前から存在するバグで、今回の変更が原因ではないことを確認済み。

検証（PR5〜7b分）: `server`/`client-techops` の型チェック、ESLint、UI トークン検査ほか lint 全項目、`shared` の Vitest 1996 件を統合後にまとめて実行し全通過。各PRは専用の検証用Postgres＋実ブラウザ（Playwright・PC/375px）で個別に確認済み。統合後もあらためて実DB・実ブラウザ（PC 1280・スマホ375px）でドラッグ移動・複製ダイアログ・印刷プレビュー・台本結び直し・担当補完・予約から列コピー・AI下書きダイアログの拠点/種別欄を一通り通し、横はみ出し 0px・JS エラー 0 件（フォントCDNへの接続エラーを除く。検証環境がネット遮断のため）。設計は [docs/design/v4/qsheet-v4-coding/14-schedule-v2-plan.md](docs/design/v4/qsheet-v4-coding/14-schedule-v2-plan.md) に記録済み（PR4〜7b分の実装済み注記・§3-7〜§3-10を追記）。段Cは利用者の判断待ちのまま未着手。
