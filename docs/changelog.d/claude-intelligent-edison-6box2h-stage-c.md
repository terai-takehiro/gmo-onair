**Wiki にデータベース（行＝ページ）を足した（段C）。** 段B までは本文を書くだけだったので、①**サーバーの口**（項目とビューの定義 `GET`/`PUT /wiki/databases/:pageId`、行の一覧と追加 `/rows`、書き出し `/rows.csv`）、②**データベースの画面 `/p/:id`（`kind='database'`）**（ビューのタブ＝表／ボード／カレンダー、絞り込みと並べ替え、行の追加、右パネルの「項目」）、③**行ページの「情報」欄**（親のデータベースで決めた項目の値がページ②の右パネルに並ぶ）、④**ツリーの `+` から「データベースを追加」**を足した。設計どおり**行は親ページの子ページ**なので、行はツリーにも出るし、押せばページ②と同じ画面になる。項目の型は9つ（文字・選択・複数選択・日付・担当・チェック・数値・URL・ONAiR リンク）、ビューは3つ（表・ボード・カレンダー）だけで、増やしていない。**絞り込みと並べ替えはサーバーが当てる**ので、表・ボード・カレンダー・CSV が同じ答えを見る。**ビューの設定は保存され、開いた人全員に同じに見える**（個人ごとの絞り込みは残さない）。**値の型の検査は `shared/src/wiki/frontMatter.ts` の `isValidPropValue` が正**で、サーバーは同じ判定の複製（`server/src/contexts/wiki/wiki-props.ts`）を保存の前に必ず通し、**型違いは黙って捨てずに項目名を言って止める**（黙って捨てると「入れたのに空のまま」になり、入力の失敗に誰も気づけない）。複製の一致は `shared/tests/wikiPropsParity.test.ts` が固定している。スマホでは表が**縦のカード**になり、**値の編集と行の追加はできる**（PC に案内するのは並べ替え・項目の定義・ビューの追加だけで、`WIKI_PC_ONLY` には入れない）。

**段C を3人で同時に書いた副作用を、取り込みのときに3つ片づけた。** ①データベースの API が `components/database/databaseApi.ts` と `lib/wikiDatabaseApi.ts` の**2つのファイルに分かれ**、片方がもう片方を再エクスポートしていた — 問い合わせの鍵（`wikiDbKeys`）を2か所に持つと、同じ画面が同じものを2回取りに行く形になるので `lib/wikiDatabaseApi.ts` の1本にまとめ、受け持ちの表を `client-wiki/CLAUDE.md` に書いた。②`DatabaseToolbar.tsx` の4つのボタンに `type` が無く、同じファイルの中に Enter で送信する `<form>`（行の追加）があるため `npm run lint` の `check-form-submit` が止まっていた（この状態だと「CSV で書き出す」を押した瞬間に行の追加まで走りうる）ので `type="button"` を付けた。③ツリーの `+` が「ページを追加」しか開かず、**データベースを作る入口がどこにも無かった**（シートだけ作られて配線されていなかった）ので、`+` を「ページを追加／データベースを追加」の2つを出す小さなメニューにし、見出しの `+` と行の `+` の両方から同じ2つを選べるようにした。絵柄を3つ並べず1つの `+` にまとめたのは、ナビの列を1本に保つのと同じ理由（ツリーの行に小さな操作が増えるほど押し間違える）。

検証: `npm run typecheck:all`（0 errors）・`npm run lint`（0 errors・warning は着手前と同じ）・`npm run test`（94件）・`npx vitest run --root shared`（2,553件）・`npm run build -w client-wiki`。実ブラウザ（Chromium・検証用 Postgres・`x-user-id` を変えて3人）で、**ツリーの `+` からデータベースを作り**、項目を5つ（文字／選択／日付／チェック／ONAiR リンク）足せること、行をタイトルだけで作れ**表のセルをその場で直すと再読み込み後も残る**こと、**ボードでカードを別の列へドラッグすると選択の値が変わり保存される**こと、**カレンダーが日付の項目で月に並び**前後の月へ移れること、**行を押すと行ページに移り「情報」に項目の値が並ぶ**こと、API に型違いの値を直接投げると 400 で**項目名つきの理由が返る**こと（日付に文字／チェックに文字／選択肢に無い値／文字に数値／ONAiR リンクに文字／実在しない相手の6通り）、**読むだけの人には同じ3つのビューが見えて値は直せない**こと、**他人の下書きは 403 ではなく 404**（存在ごと隠す）ことを確認。1440px と 375px で**横はみ出し 0・JS エラー 0**、375px では表が**縦のカード**になり PC 専用の操作だけが案内に変わること、**ナビの列は1本のまま**（画面の中に2本目のナビは無い）ことを実測。
