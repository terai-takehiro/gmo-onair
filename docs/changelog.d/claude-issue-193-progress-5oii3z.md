Phase 3-2a（`docs/reviews/phase3-2-plan.md`）を実施 — 顧客系5つのFK（`projects`/`revenues`/`activity_logs`/`estimates`/`gpm_projects` の `customer_id`）を `customers(id)` から `companies(id)` へ張り替えた。値そのものも書き換え、以後は取引先マスター（`companies`, `is_customer=TRUE`）が正になる。あわせて `customer_id` を読み書きしていた約30ファイル（顧客一覧・案件・売上・活動記録・見積・GPM・Excel入出力・決算取込・内覧会・投入口・検索・バックアップ・MCPツール一式）を追随させ、「お客様」ドロップダウン（`GET /customers`, `GET /gpm/customers`）が返す `id` を `companies.id` に揃えた。`npm run typecheck -w server` は exit 0。DBマイグレーションは1件（`200_customer_fk_to_companies.sql`）— これ以降、本番のロールバックは「Releasesのタグでデプロイを再実行」だけになる（イメージだけ差し替えるロールバックは使えない）。
