Phase 3-2a（`docs/reviews/phase3-2-plan.md`）を実施 — 顧客系5つのFK（`projects`/`revenues`/`activity_logs`/`estimates`/`gpm_projects` の `customer_id`）を `customers(id)` から `companies(id)` へ張り替えた。値そのものも書き換え、以後は取引先マスター（`companies`, `is_customer=TRUE`）が正になる。あわせて `customer_id` を読み書きしていた約30ファイル（顧客一覧・案件・売上・活動記録・見積・GPM・Excel入出力・決算取込・内覧会・投入口・検索・バックアップ・MCPツール一式）を追随させ、「お客様」ドロップダウン（`GET /customers`, `GET /gpm/customers`）が返す `id` を `companies.id` に揃えた。`npm run typecheck -w server` は exit 0。DBマイグレーションは1件（`200_customer_fk_to_companies.sql`）— これ以降、本番のロールバックは「Releasesのタグでデプロイを再実行」だけになる（イメージだけ差し替えるロールバックは使えない）。

Codexレビュー（2巡目・#199 P2 × 4件）を反映: ①旧URL（移行前の `customers.id`）から顧客詳細を開いたまま活動を記録・保存すると FK 違反で失敗する穴を塞いだ（書き込みは正規の `companies.id` を優先し、`PUT`/`DELETE /customers/:id` も旧IDを解決するようにした）。②削除済みの顧客が `list_customers`（MCP）・GPMのドロップダウン・全体検索・Excel入出力・決算取込・投入口・内覧会の8か所に残り続けていた穴を塞いだ（`customers` 側が生きている会社だけに絞る）。③FKが `companies.id` を直接指すようになったことで失われた「顧客ロールの会社か」というDBの保証を、案件・売上・活動記録・見積の書き込みで確かめ直すようにした（`assertCustomerCompanyId`）。④`companies` を正として読むようになった一覧・詳細が、`customers` 側だけ直っていた古い名前・連絡先を出していた穴を塞いだ — `200_customer_fk_to_companies.sql` に、既にリンク済みの `companies` 行を `customers` の現在値で上書きする一括更新を足した。実DB（`verify:up`・stale データを模した再現含む）で4パターンすべて確認済み。
