**白い文字の検査に穴が3つあり、塞いだ**（#159 のレビュー・Codex P2 3件）。①⚠️ **どれも「検査が OK と言うのに画面では読めない」形**でした。#159 で足した検査は**画面の不具合は直していますが、守ってくれる範囲が想定より狭い**状態で `main` に入っていました。②**`.tsx` しか見ていませんでした。** この製品はクラス名を **`.ts` の地図**に置く形が多く（`hoursTypes.ts` / `taskList/state.ts` / `inbox/kinds.ts` など10ファイル以上）、Tailwind の `content` も `**/*.{js,ts,jsx,tsx}` で拾っています。つまり**地図に1行足すだけで画面に出るのに、検査は通っていました**（走査 534 → **741 ファイル**）。③**条件つき・半透明の塗りを「塗り」と数えていました。** `hover:bg-warning` は**押していないときは淡い帯のまま**、`bg-warning/10` は**10% なので下の淡い面が透ける**のに、どちらも素通ししていました。⚠️ **ただし「条件つきだから駄目」ではありません** — `data-[state=checked]:bg-primary` と `data-[state=checked]:text-primary-foreground` は**必ず一緒に効く**ので白が淡い面に載ることはなく、一律で弾いたら**チェックボックス2件が実際に落ちました**。見るのは「条件がついているか」ではなく「**文字色と塗りの条件が揃っているか**」にしました。④**逆に締めすぎて、正しい書き方を止めるところでした**（この PR のレビュー・Codex）。`bg-warning/100` は **`bg-warning` と同じ不透明**なのに、`/` を一律で撥ねていたので**lint が落ちます** — 検査は `npm run lint` に入っているので、**その書き方をした人は何も進められません**。`/100` だけ通し、半透明（`/10` など）は撥ねたままにしました。⑤**検証**: 仕込みのファイルを作って**検査を実際に走らせ、止まるか通るかで見ています**（`shared/tests/contrastTokens.test.ts` 12 → **21項目**）。⚠️ **字面を読む試験をやめました** — 書き方を変えた日に通ってしまうためです。**反証: 直す前は読めない3通りが3つとも「OK」で通り、直したあとは3つとも止まる**（実測）。正しい6通り（本物の塗り／濃い段 `bg-primary-800`／親のタグの塗り／同じ条件／`/100`／グラデーション）は通ります。仕込みは一時ディレクトリに置くため、検査に**引数でディレクトリを渡せる**ようにしました（`client/src` に置くと失敗した回に消し残る）。⑥**画面のコードは1行も触っていません**（`client/src` は不変）。`shared/src/` も触っていないので凍結4アプリの CSS も不変。DB 変更なし。
