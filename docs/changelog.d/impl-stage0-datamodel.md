制作資料 (Qシート) の段0「データモデルの前提修正」を実装した。①立ち位置図のセルが `stageTemplates[]` の配列 index (`templateIndex`) を持っていたため、ひな形を1つ消すだけで後ろの index を指す全行が別の図にずれていた（本番画面のランダウン・印刷プレビューでも起きうる不具合だった）→ ひな形に安定 id を持たせ、セルは id (`templateId`) で参照するよう `resolveStageTemplate` に読み取りを1本化した（旧データは開いたときに自動移行、削除済みは「未選択」として扱う）。②スマホ編集がセルの型を無視して常に `{entries:[...]}` で丸ごと上書きしていたため、色を付けただけで立ち位置図の選択やスライド画像が消えていた → 型ごとに PC と同じ形（非破壊マージ）で書くよう作り直した（LED/XR の Cue・トランジションも PC と同じ選択肢に揃えた）。③台本の合計尺の計算が編集画面（ロール尺優先）と進行・ランダウン（行合計優先）で向きが逆にバラけており、PDF出力だけはフォールバックが無く空欄の台本で総尺0分になっていた → 呼び出し側が優先順位を選べる共通関数 `docTotalSec` に統合し（各画面の表示結果は1秒も変えていない）、PDFにも行合計へのフォールバックを追加した。④セルの参照方式 `blk.<type>#<n>`（同型の列が複数あっても区別できる）を純関数として追加した（画面はまだ呼ばない。Excel/AI 連携が使う）。⑤`Date.now()` 由来のid生成を `genId()` に統一した（ゴミ箱からの行復元・カード編集の新規行など、衝突すると同時編集で行が増殖する箇所）。検証: `npx tsc -b client-qsheet` / `npm run typecheck` / `npm run lint` / `npm run test` / `node scripts/check-collab-parity.mjs` OK。
