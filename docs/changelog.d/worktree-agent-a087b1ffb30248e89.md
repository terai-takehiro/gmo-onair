**案件詳細⑥の残り5タブのうち、当日・見積・書類の3タブをスマホでカード積みに作り直した。** v4ネイティブUI監査の積み残し（`docs/handoff-2026-08-20-v4-native-ui.md`「残っている作業」）。やり取り・ふりかえりの2タブは実装を確認した結果、既にカード積み・カメラ直起動などネイティブ級の作りだったため変更していない。
①**当日タブ**（`DayTab.tsx`）: 制作資料・技術資料の一覧を、`Row`（PC表を縮めただけ・右端72〜96pxの「開く」リンクだけがタップ対象）から、行全体をタップできるiOSの一覧セル（アイコンの丸背景＋タイトル＋更新日＋シェブロン）に作り直した。
②**書類タブ**（`FilesTab.tsx`）: BOXの中身一覧を同様に一覧セル化した。
③**見積タブ**（`EstimateTab.tsx` / 新設`EstimateActions.tsx` / `RevenueBillingPane.tsx`）: 明細（`EstimateItems`）が数量・単価・仕入・日付・金額の6列を持つ入力欄の並びのため、タブ全体はCLAUDE.mdの方針（「入力欄が並ぶものはPCのまま」）どおりPC専用ゲート（`ProjectDetailPage.tsx`の`OffPhoneTab`）を維持したが、「それでもこのまま開く」を選んだ人のために版の一覧・売上/請求/仕入の3カードをカード積みに作り直した。操作ボタン群はPC/スマホで共通の`EstimateActions`に切り出し、書き写しによる食い違いを防いだ。
検証: `npx tsc -b client` / `npm run lint` / `npm run test` OK（実ブラウザでの375px確認はサンドボックス環境の制約でスキップ）。
