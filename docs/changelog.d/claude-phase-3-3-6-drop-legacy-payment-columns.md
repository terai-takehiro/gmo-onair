**支払条件の例外の旧列（`customers`/`vendors` 側）を削除した**（Phase 3-3-6・migration 204）。migration 196 で支払条件の取引先ごとの例外を `companies`（`customer_closing_day`/`customer_payment_months`/`customer_payment_day`/`vendor_payment_months`/`vendor_payment_day`）へ一本化して以降、互換のために残していた `customers.closing_day`/`payment_months`/`payment_day`・`vendors.payment_months`/`payment_day` を `DROP COLUMN` した。これらを読み書きするコードは Phase 3-3-4（PR #225）の時点で1つも無いことを確認済み。⚠️ **このmigrationは不可逆**なため、マージ前に①本番のオンデマンドバックアップの取得・確認②検証一式の実施、の両方を済ませた。検証: ユーザーが本番VPSでオンデマンドバックアップを実行・確認。着手セッションが `npm run verify:fresh`（migration込みのフレッシュDB）でmigrationの適用を確認し、サーバーを起動して `GET /companies` `GET /customers` `GET /vendors` `GET /money-rules` の応答・取引先の新規作成（顧客/仕入先）・`budget:editor` による `PUT /vendors/:id`（`vendors` への直接書き込み経路）が正常動作することを確認した。`npm run typecheck:all` / `npm run lint`（0 errors）/ `npm run test`（1135件）もすべて通過。`docs/reviews/phase3-2-plan.md` のPhase 3-3進捗表を更新した。
