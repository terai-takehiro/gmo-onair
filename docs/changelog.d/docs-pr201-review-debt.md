**PR #201のCodexレビュー指摘（P1・1件）を棚卸しに記録した**。PR #201で足したGPMの顧客ロール検証（`assertCustomerCompanyId`）は「値が渡されたら毎回確かめる」形で既存値との比較が無く、GPM詳細の段変更・直すダイアログはどちらも今のcustomer_idを送り直す作りのため、あとから顧客ロールを外された会社のプロジェクトは無関係な直し（段の変更・名前の編集等）まで400で止まっていた。同じ形の穴が案件・活動記録・売上のupdateにも既にあり、4か所とも同PR内（`9223c33`）で「既存値と実際に変わったときだけ確かめる」形に修正・resolve済み。②マージすると画面から消える指摘を残す決めごとに従い記録した。③**検証**: ドキュメントのみの変更。
