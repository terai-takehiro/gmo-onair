**#544 のレビュー棚卸しを記録した**（決めごと: [docs/branching.md](docs/branching.md) 「マージしたら、その PR のレビューを棚卸しに移す」）。⚠️ **CI が緑になった36秒後にマージされ、`get_reviews`・`get_review_comments`・`get_comments` とも0件**（API で直接確認。`npm run reviews:debt` はこの環境のトークンでは 401）。画面上「レビュー0件」は「読まれて問題なし」と見分けが付かないので、事実として書き出した。レビュー0件のぶんを埋めるため、意図して残した判断・未検証のまま入れたもの4件（実機スマホ未確認・`verify:ui` 未実施／権限403の未確認／明細0件時のスマホカードの見た目／GLS 発番ガード変更の本番既存データでの挙動）を自分で書き出している。この行が自分自身も含む記録であることを明記し、棚卸しの PR についてさらに新しい行を足す連鎖はここで止める（v4.5.17 の決めごと）。
