**PR #685のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」）。#685（#683のレビュー棚卸し記録）はCode Reviewが完走し、棚卸し自身の数え間違い（3巡の内訳合計は全7件のはずが「全6件」と誤記）を1件（P2）指摘された。本文書・`docs/changelog.d/`の両方を直して返信・スレッド解決まで完了した（表に移す未対応の指摘は無い）。Security Reviewも完走しfindingsは無かった。修正コミットにはどちらのレビューも再実行されていない（#651・#677・#679・#681と同型）が、ドキュメントの数値訂正のみで実害は無い。棚卸し文書自身が2回連続で数え間違えた（#681は範囲の過大記録、#685は件数の誤記）ことを記録し、数や範囲は暗算ではなく実物を数えてから書くという学びを重ねた。検証: `node scripts/check-md-links.mjs`・`npm run lint`。
