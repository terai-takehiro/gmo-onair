**PR #684・#685・#686のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」）。#685（#683のレビュー棚卸し記録）はCode Reviewが完走し、棚卸し自身の数え間違い（3巡の内訳合計は全7件のはずが「全6件」と誤記）を1件（P2）指摘された。本文書・`docs/changelog.d/`の両方を直して返信・スレッド解決まで完了した。Security Reviewも完走しfindingsは無かった。修正コミットにはどちらのレビューも再実行されていない（#651・#677・#679・#681と同型）が、ドキュメントの数値訂正のみで実害は無い。棚卸し文書自身が2回連続で数え間違えた（#681は範囲の過大記録、#685は件数の誤記）ことを記録し、数や範囲は暗算ではなく実物を数えてから書くという学びを重ねた。**あわせて、同じ区間にマージされていた#684・#686が棚卸しから漏れていたのを追加した**（自PR#688へのCodexレビューの指摘）。#684（会場図面ミニアプリの新設）はCode Review・Security Reviewとも1巡のみ完走し、全8件（P1×6・P2×2）の指摘（camelCase未変換で編集盤が描画不能・`copy_from`のIDOR・自動保存の失敗握りつぶし・印刷縮尺が反映されない・下敷き画像URLの二重結合・下敷き画像の配置漏れ・マーキー選択の不始動・線分品目のドラッグが戻る）が付いたが、全件を本PR内で修正・返信・スレッド解決した。ただし修正コミットの直後にCodexがusage limitsを返し、P1×6件を含む修正そのものが一度もレビューされないままマージされた（#651/#677/#679/#683と同型）。#686（release: v4.6.14）はCode Review・Security Reviewとも完走し、要約文の記述誤り（会場図面を「実装はまだ」と誤記）1件（P2）を修正・返信・スレッド解決した。表に移す未対応の指摘はいずれも無い。検証: `node scripts/check-md-links.mjs`・`npm run lint`。
