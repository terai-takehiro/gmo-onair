**PR #585（メンテナンスの機材選びに付属品を出し、保管場所と機材台帳の崩れを直した）のレビュー棚卸しを記録した**（決めごと: [docs/branching.md](docs/branching.md) 「マージしたら、その PR のレビューを棚卸しに移す」）。⚠️ **Codexの通常コードレビューは「利用上限（usage limits）」に達したため一度も走らず**（#570・#574・#581と同じ落ち方で、これで4本目）、セキュリティレビューだけが完走して指摘0件だった。`get_reviews`・`get_review_comments`とも0件、CI green から約3分後にユーザー自身がマージ（`npm run reviews:debt` はこの環境のトークンでは401のため GitHub MCP で直接確認）。14ファイル・+247/−46行、マイグレーションなし。**つまり「指摘なし」なのはセキュリティ観点だけで、ふだんのコードレビューは読まれていない**ので、原因不明で0件だった過去の PR とは分けて記録した。意図して残した未検証事項（`npm run verify:ui` 未実行・権限別403の画面確認なし・`shared` の `TableBadge` を触ったのに `npm run check:frozen` でのビルド出力の突き合わせなし・商品名2行化で行高が2通りになるが数千行の実データでのスクロールずれは未測定）は [docs/reviews/codex-findings-v4.md](docs/reviews/codex-findings-v4.md) の一覧に書き出した。
