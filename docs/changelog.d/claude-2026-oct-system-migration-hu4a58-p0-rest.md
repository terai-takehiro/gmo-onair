**2026年10月の事業再編（社名変更・計上会社の2社化・案件番号の改番）P0の残り（entity_code配線・PDF/通知の発行者切替・設定画面「会社と切替」）をマルチエージェントで実装した**（`docs/reorg-2026-10-plan.md` §13）。①案件・売上・仕入・販管費・見積の全ての新規行が`entity_code`（現時点は暫定で全てGSS。P1で計上会社の導出規則に置き換える）を明示するよう約20ファイルのINSERT文を配線し直し、計画になかった書き込み口（受け取った書類→台帳への引き渡し）も新たに発見して配線、配線の完了を機械的に確かめる`entityCodeInserts.test.ts`を追加したうえでmigration 286により`entity_code`をNOT NULL化した。②見積書・請求書・検収書PDFと機材レンタルの見積依頼メールの発行者を、ハードコードされていた社名定数から計上会社マスター（`legal_entities`）の読みに差し替えた（社名変更日`renamed_on`が未設定のあいだは今までと同一の表示になる設計のため、切替前の今は見た目が変わらない）。`notification_templates`の4行（実際にはプログラムから一度も描画されていないことを確認した参照用テキスト）の件名も固定の社名から`{発行会社}`変数に差し替えた（migration 287）。③設定に「会社と切替」画面を新設し、計上会社（コンテンツスタジオ／サムライスタジオ／グループ本体）の発行者情報（住所・登録番号・振込先・ロゴ）の編集と、旧⇄新の切替状態の表示・前進操作（`system_admin`のみ）をできるようにした。並列実装した3本の差分をマージする際にmigration番号の衝突（282が2本）が見つかったため283番へ採番し直した（後日`main`側との番号衝突で2回改番し、最終的に287番になった。§13参照）。検証: `server`/`client`の型チェック、`shared`のVitest 2004件、検証DBを作り直してmigration 001b〜283（264ファイル）を通しで適用、`check-migration-numbers`・`check-mobile-declared`・`check-links`・`check-md-links`・`check-shared-wiring`いずれもgreen。GPMの「自社」表示・マニュアル・案件作成のメール下書きの社名は意図して対象外とした（実際には一度も切り替わらないコストに見合わないため。切替作業と一緒に直す）。
