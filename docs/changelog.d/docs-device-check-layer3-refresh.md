**実機総点検チェックリスト（層3）を、その後マージされたPR分で最新化した**（コード変更なし）。PR #368 作成時点から main に入った変更のうち、チェックリストと食い違う3点を直した: ①音声サポートの旧URL（トークン無し）は #371（段階③）で410になる — 「旧URLの生存」に例外を明記し、失効表示が仕様どおりであることを確認する項目を追加 ②計時・視聴者の `/live/` は既にv4.1段2の時点で案内専用画面（`LiveHomeNoticePage`）に置き換わっており、旧行「運用画面が共通シェルで開く」は実機で該当する画面が無かった — 実際の到達経路（`/techops/top` への案内・`/techops/live-legacy`）に差し替えた ③#365で直した6件のうちチェックリストが拾っていなかった2件（部屋の空きのボタン高さ・設定データビューアのボタン高さと金額表示）を追加した。検証: `docs/release-prep/device-check-layer3.md` に列挙された全URLパスを現在の各アプリ `App.tsx`/`nav.ts` と突き合わせて実在を確認。
