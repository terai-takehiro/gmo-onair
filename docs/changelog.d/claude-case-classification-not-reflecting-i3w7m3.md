**案件台帳に「整合性の確認」を足した**（ご要望）。①**この画面を作った目的のもう1つが「v4 より前のデータが揃っているかを確かめる」ことでした。** ⚠️ **そこがいちばん足りていませんでした** — v4.0.30 で入れた「分類が入っていないものだけ」は**画面側の絞り込み**で、**そのページの 100 件の中だけ**しか見られません。**全体で何件おかしいのかが分からない**ので、整合性の確認には使えません（しかも**「これで全部だ」と読まれます** — 数字はそれらしく出るので、突き合わせるまで誰も気づけません）。②**数えるのも絞るのもサーバーの同じ1つの式**にしました（`GET /projects/integrity` ＋ `GET /projects?issue=<鍵>`・`project-integrity.ts` の `sql`）。**別々に書くと「12 件」と出したのに開くと 9 行**という食い違いが起き、**どちらが本当か画面からは分かりません**。**1回の SQL で全部数えます** — 1件ずつ問い合わせるとチェックが増えるほど画面を開くのが遅くなり、しかも**数え終わった順に数字が入れ替わって読み間違えます**。⚠️ **知らない鍵は当たらない条件（`AND FALSE`）に倒します** — 素通りさせると**絞ったのに全件が返り、整合性が取れていると読まれます**。③**8つのチェック**: 案件分類が入っていない／分類と旧「案件種類」がずれている／GLS-B なのに2段分類が入っている／無観客なのに来場人数が入っている／受注しているのに GLS 番号が無い／受注しているのに実施日が無い／リード経路が入っていない／社内の担当が今いない人になっている。**規則はこの製品が既に決めていることだけ**を書きます（「たぶんこうあるべき」を足すと、**直す必要の無いものを直させる**ことになります）。④**「なぜ困るか」と「直し方」を必ず出します** — おかしいとだけ言われても手が止まります。⚠️ **まとめて直せないもの（GLS の発番・リード経路）はそう書きます** — **押しても直せない場所へ送るのがいちばん困ります**。**0 件のチェックも並べます**（消すと「そのチェックが無い」のか「0 件なのか」が分かりません）が、**薄くして押せなくします**。**規則の文はサーバーが持ちます** — 画面に書き写すと、片方だけ直った日から**画面の説明と、実際に数えているものが食い違います**。⑤⚠️ **ここでは何も直しません**（数えて見せるだけ）— **機械で埋められるものと、人が決めるものが混ざっている**ためです（旧「案件種類」が「その他」の案件は、配信か収録かを機械では決められません）。⑥**旧「案件種類」を導く SQL は同じ対応表から組み立てます**（`projectTypeSqlCase`）。表を SQL に書き写すと、片方だけ直した日から**正しい行を「ずれている」と数えて直させる**ことになります。⑦**画面に出す文に印付けの記号を書きません。** `why` / `how` は素の文字として描かれるので、`**` やバッククォートは**そのまま画面に出ます**（実際に2度やり直したので、試験で止めます）。⑧**検証**: 実 Postgres ＋ 実サーバー ＋ 実ブラウザで — わざと壊した行を入れて **8つとも「数えた件数」と「押して絞ったときの件数・行数」が1件ずつ一致**（2/1/1/1/1/1/17/0）、**知らない鍵は 0 件**（**反証: 素通りさせると全 18 件が返る**）。実ブラウザで**札が8つ・0 件の札は薄くて押せない・横はみ出し 0px・JS エラー 0**、ずれている案件で絞ると **1 行**＋「なぜ困るか」「直し方」が出て、**その1件を直すと札が 0 になって押せなくなり**「このチェックに当たる案件はありません」に変わる。`shared/tests/projectIntegrity.test.ts` 17項目（**反証を3つとも実測**: 画面に出す文に `**` を戻すと落ちる／知らない鍵を素通りに変えると落ちる／数える式だけ書き換えると落ちる）、`npm run test` 665項目、`typecheck` exit 0、`lint` 0 errors / 63 warnings。⑨**DB 変更なし。`shared/src/` も触っていない**ので凍結4アプリの CSS は不変（足したのは `shared/tests/` だけ）。
