**① 予定のPC表示を、承認済みのモック（macOSのカレンダーアプリ風）に合わせて作り直した（PC「予定」）。**
旧ツールバーは見え方チップ・前後・出すもの・絞り込みを1本の帯にまとめていたが、
今日／前後／期間の見出し／月・週・一覧の切替／予定を入れる だけの1段
（`calendar/DesktopToolbar.tsx`）にした。ミニカレンダーと「マイカレンダー」（出すもの）は
共通の左メニューへ常設した — 共通シェルに左メニュー上の差し込み口
（`shared/.../shell/sideMenuSlot.ts`。既存の `primaryAction.ts` と同じ portal の形）を
新設し、`calendar/CalSidebarExtras.tsx` をそこへ描く。旧「日」表示は無くし（切替は
月・週・一覧の3つ）、マス目を押しても画面は切り替わらず「選んだ日」の印だけが付く
形にした（新しい予定の既定日として使う）。「部屋で絞る」「人で絞る」（旧ツールバーの
機能）はモックの1段目には無いが、消さずに2段目の細い帯として残した。「① 予定」の
見出し・件数の説明文は、レイヤーが左メニューに常設されて見えるようになったことで
冗長になったため外し、「カレンダー連携」ボタンも同じ行き先（カレンダーの設定）が
左メニューに常設されたため外した。右の3枚（今日の予定・仮押さえ・色の意味）は
変えていない。
検証: `npx tsc -b client` / `npm run lint` / `npm run test` OK。
