**Wiki の見直しとコメントを作った（段F）。これで設計 `docs/design/v4/wiki.md` §9 の段A〜段F が全部そろった。** 段E までで「書く・探す・訊く」は出来たが、**書いたものが古くなったことに誰も気づかない**状態だった。足したのは3つ。①**見直しの画面**（`/wiki/review`・月1回の場。見直し予定／足りないページ／AI の直され方の3タブ）、②**コメント**（ページ②の右パネルのタブ。現場で気づいたことを残し、担当に届く）、③**毎月1日の通知**（各スペースの担当へ）。あわせて案件詳細と機材台帳から、その案件・その機材の Wiki ページへ行けるようにした。

⚠️ **画面に「期限切れ」とは書かない。** 利用者から「**Wiki なので期限切れという概念はないと思います**」とご指摘があったとおり、ページは予定日を過ぎても中身が無効になるわけではない。画面の言葉は「**見直し予定**」「**予定日**」で、印は「**要見直し**」「まもなく」「担当なし」。色も赤ではなく情報の色にした。「期限」「締切」「延滞」「過ぎています」は1語も使っていない（実ブラウザで3タブ×2幅の本文を走査して0件を確認）。

⚠️ **これで左メニューの「作っていない入口」が0になった。** `ready: false` が残るのはスペース管理だけで、画面のある項目はすべて出ている。

**Wiki の「見直しとコメント」（段F）のサーバー側を作った。** 設計 `docs/design/v4/wiki.md` §6-⑦⑧ の月1回の見直しと、ページへのコメントを受ける口が1本も無く、§7-3 条件5 が「仕組み（通知と『見直した』の記録）は段F で作る」と宣言したまま残っていた。足したのは口が7本（コメント5本・見直し2本）と、毎月1日の通知1本。

**コメント**（`GET`/`POST /wiki/pages/:id/comments`・`POST /wiki/comments/:id/resolve`・`DELETE /wiki/comments/:id`）。表は migration 303 にあるので追加の表は作っていない。⚠️ **読めないページのコメントは 403 ではなく 404**（§8「存在ごと見えない」）——`assertReadablePage` を必ず通す。消せるのは**書いた本人か manager だけ**で、ここだけは 403（本人にはその行が見えているので隠す意味がない）。行は消さず `deleted_at` を入れ、**返信を持つコメントを消しても返信は残して上に上げる**（親ごと消すと返信で確定した結論まで消える）。コメントが付いたら**そのページの担当**に、返信なら元のコメントを書いた人にも通知する（ひな形 `wiki_comment`・`refDate` にコメント id を入れるので2通目以降も届く）。

**§7-3 の「穴」を1本埋めた**（`GET /wiki/comments/outcome`・manager）。「AI の答えが現場で正しかったか」は取れないので、代わりに **AI から生まれたページに付いたコメント**を成果として数える。`wiki_pages.ai_output_id`（下書き）と `wiki_ai_messages.spawned_page_id`（回答から「ページにする」）の両方を辿り、**AI の出力より後に付いたコメントだけ**を数える（取り込みで後から結んだページの古い指摘を混ぜない）。既存の `wiki-ai-outcomes.service.ts` は**書き換えていない**（あちらは AI の出力から数える側・こちらはコメントから数える側。同じ数字を2か所で作ると片方を直した日に食い違う）。

**見直し**（`GET /wiki/review`・`POST /wiki/pages/:id/reviewed`）。一覧は3つの区分（予定日を過ぎた／14日以内／担当が空）で、件数は**上限で切る前に数える**（検索の `counts` で踏んだ穴と同じ）。⚠️ **テンプレートとデータベースの行は外す** — 行は1件ずつが記録なので、担当が空の行が数百並ぶと一覧が使えない。「見直した」は**本文を触らない** — `savePageInternal` を通すと版が1つ増えて最終更新が今日になり、一覧の「最終更新」が「見直した日」の写しになってしまう。予定日だけを直し、誰がいつ見直したかは `wiki_reviews`（migration 307）に残す（§7-3 条件5 の「担当の名前と『見直した』の記録」）。次の予定日は既定で6か月後・`months` か `review_by` を送れば従い、`review_by: null` で外せる。**②足りないページと③AI の直され方は段E の口をそのまま使う**（`/wiki/ai/gaps`・`/wiki/ai/digest`）。

**毎月1日の「今月の見直し」の通知**（`scheduler.service.ts` 03:45・ひな形 `wiki_review_monthly`）。宛先は**各スペースの担当**（`wiki_spaces.owner_user_id`）。⚠️ **見直すものが1件も無いスペースには出さない** — 月初に「0件です」が届くと、そのうち誰もベルを見なくなる（`notification.service.ts` 冒頭の「ゴミ通知」の学び）。二重送信は既存の2段（`scheduled_job_runs` と `notifications` の一意索引・`ref_date` は対象月）に乗せた。

⚠️ **「期限切れ」という語は、サーバーが返す文字列に1つも入れていない**（§10 #10・2026-09-22 の利用者のご指摘）。Wiki のページは予定日を過ぎても中身が無効にならないので、通知の本文も「要見直し ○件／14日以内 ○件／担当が空 ○件」とした。区分の鍵 `overdue` は API の内部の名前で、画面に出すのは「要見直し」。

検証: `npx tsc -b server`（0 errors）・`npx eslint server/src/contexts/wiki server/src/contexts/platform`（0 errors）・`node scripts/check-migration-numbers.mjs`・`npx tsc -b client-wiki`（共有の型を `shared/src/wiki/reviewTypes.ts` へ切り出したので確認）。検証用 Postgres（`onair_verify`）で実測: 読めない棚（`members`）のページのコメントは reader・editor とも **404**・区画 `wiki` を持たない人だけ 403、返信への返信は 400、**本人以外の削除は 403・本人と manager は 200**、親を消しても返信は残る、コメント→担当／返信→元の書き手に通知が1通ずつ、見直しの一覧は管理者 9件（要見直し3・14日以内1・担当が空5）に対し限定アクセスの人は 7件（`members` の2本が消える）、「見直した」で `2026-01-31` → `2027-03-23`（6か月後）に入れ替わり **`rev` と `updated_at` は変わらない**、毎月1日の通知は2通・**同じ月に2回流しても新しく出るのは0通**、ひな形を無効にすると0通。

**Wiki に「見直し」の画面（`/wiki/review`）を作った（段F）。** 設計 `docs/design/v4/wiki.md` §6-⑦ の月1回の場が無く、左メニューの「見直し」は押せない項目のまま残っていた。作ったのは3つのタブ。①**見直し予定** — 予定日を過ぎた・14日以内・担当が空のページを1行ずつ並べ、「編集」で直すか「見直した」で次の見直し予定日を入れ直す（本文は変えない）。②**足りないページ** — AI が出典を出せずに答えられなかった質問を回数の多い順に並べ、「ページを作成」（AI に下書きを書かせるか、題だけの空のページか）と「却下」を置いた。却下しても行は消さない（消すと一度決めた質問がまた先頭に並ぶ）。③**AI の直され方** — 「AI に聞く」「AI で下書きを作成」「AI で整える」の3つについて、無修正率・公開まで進んだ率・評価の内訳と、よく直される点、**次の呼び出しに渡している助言**をそのまま出す（会社方針「AIを使い捨てにしない」の条件4を人にも見せる）。

⚠️ **「期限切れ」と画面に書いていない**（2026-09-22 の利用者からのご指摘「Wiki なので期限切れという概念はないと思います」）。ページは予定日を過ぎても中身が無効になるわけではないので、画面の言葉は**「見直し予定」「予定日」**、印は**「要見直し」**（色も赤ではなく情報の色）にした。モック `Review.dc.html` にあった「期限 9/30」「期限を 12か月伸ばす」の言い回しは、この決定に合わせて置き換えている。「期限」「締切」「延滞」も使っていない。

**ナビの列は1本のまま。** モックのタブは本文の上に横1本で置き、画面の中に2本目のナビの列を作っていない（2026-09-22 のご指摘「サイドタブが増えすぎて窮屈」）。タブは共通の `FilterChips`（件数つき・タップ領域 44px・入り切らないときは横スクロール）で、選んだタブは `?tab=` に写すので URL で共有できる。スペースの絞り込みは見出しの右に1つだけ置き、**画面の中で当てる**（3つの口それぞれに同じ絞り込みを足すと片方だけ古くなる）。

**スマホでは表を横に流さない。** 1024px 未満では列をやめ、1行＝「題 ＋ 場所 ＋ 担当・見直し予定・最終更新の1行 ＋ 操作」に畳む。操作のボタンは 640px 未満で幅いっぱい・高さ 44px 以上。`src/pcOnlyScreens.ts` では `WIKI_MOBILE_OK`（設計 §6-⑧）。

**3つのタブは別々の問い合わせにした**（区画ごとに諦められるように）。足りないページと直され方は区分 `wiki` の manager だけが読めるので（設計 §8）、権限が無い人には**投げずに**その区画の中で「何の権限が要るか」を出す — 1つの 403 で画面全体が白紙にならない。**段F の画面ができたので `components/layout/nav.ts` の「見直し」を `ready: true` にした**（左メニューと、スマホ下タブの「やること」）。残っている `ready: false` は スペース管理 だけ。

**実ブラウザで確かめて3つ直した。** 検証用 Postgres（`npm run verify:up`）に実データを入れ、1280px と 375px で3つのタブを開いた。①「AI の直され方」で**React の key が重複**していた（足りないページの助言は `wiki_answer` と `wiki_draft` の両方に載るので、同じ文が2行出ていた）ので先に重複を落とすようにした。②助言の文には次の呼び出しに載せる Markdown の印（`**…**`）が入っており、画面に `**` がそのまま見えていたので、検索の抜粋と同じ `toPlainText` で印だけ落とした。③375px で**押せる場所が 20px しかなかった**（題だけがリンクだった）ので、題・場所・担当の行をまとめて1つのリンクにし、「見直した」の記録の行と「足りないページを見る」も 44px にした。

検証: `npx tsc -b client-wiki --force`（0 errors）・`npx eslint client-wiki/src/pages/review client-wiki/src/components/review client-wiki/src/components/layout`（0 errors）・`node scripts/check-mobile-declared.mjs`（Wiki 10画面すべて宣言済み）・`node scripts/check-file-size.mjs`（新しい7ファイルはいちばん長いもので 362 行）・`node scripts/check-ui-tokens.mjs`（新規の違反 0）・`node scripts/check-md-links.mjs`・`npm run build -w client-wiki`。実ブラウザ（Playwright・検証用 Postgres）: 3タブ × 2幅で **27/27 通過**（見出し・横はみ出し無し・「期限／期限切れ／締切／延滞／過ぎています」が1件も出ない・JS エラー無し・375px のタップ領域 44px）。押したときの動きも **10/10 通過**（「見直した」→確認ダイアログ→記録の知らせ→その行が一覧から外れ、「見直した」の記録に残る／「ページを作成」→シートに質問と回数とスペースと題→「空のページを作成」で下書きができ、その質問が一覧から外れる）。

**Wiki のページにコメントを作った（段F）。** 設計 `docs/design/v4/wiki.md` §6-② は右パネルを「情報・目次・履歴・コメント」の4タブと決めていたが、コメントだけが無かった。手順書は書いた人より使う人のほうが間違いに気づくので、その場で残す先が無いと「違っていた」が記録に残らない。①右パネルに**コメント**のタブを足した（数字は**解決していない**やり取りの数。既定で隠している解決済みを数に入れると「2件と書いてあるのに1件しかない」に見える）。②右パネルは開閉式で既定は閉じのままなので、閉じている間と右パネルを出せない幅では、目次・情報と同じ並びで**本文の下に**コメントを続ける（§6-⑧「スマホでもコメントは書ける」。画面の中に2本目のナビの列は作っていない）。③**読む人（reader）も書けて、解決にする／戻すも押せる**（§8 の表・サーバーも reader で受けている）。消せるのは書いた本人か manager だけで、判定はサーバーが1件ごとに返す `can_delete` に従う（同じ判定を画面でもう一度書かない）。④返信は**1段だけ**。削除は取り消せないので `confirmAction({ tone: 'danger' })` で1回止める。⑤本文は既存の `WikiMarkdown` で描く（`rehype-raw` を入れない＝HTML は描かない）。⑥1件 5,000字はサーバーと同じ上限で、超えたら切らずに断る。

**案件詳細（書類タブ）と機材台帳（詳細）に Wiki のページを出した。** 設計 §4-3 の「案件詳細の Wiki」「機材台帳の詳細に出る使い方のページ」を、**タブを増やさずに**カード1つずつで置いた（入口を増やしすぎない・`docs/ia.md`）。Wiki の本文に `[名前](/sales/projects/:id)` `[品名](/equipment/items/:id)` と書かれたページを逆から引いて並べる。ONAiR リンクは本文の中の文字で専用の表を持たないため、段D の検索（`GET /wiki/search`）に**リンクのパスそのもの**を語として渡している（語は空白でしか切られないので1語のまま当たる）。**新しい API は足していない。** ⚠️ **関係するページが0件・読み込み中・失敗のときは何も出さない** — 「ありません」の枠を毎回出すと空の区画が1つ増えるだけで、次にやることが分からないため。当たるのは読めるスペースの公開ページだけ（§8）。Wiki は別のベースパス（`/wiki/`）なので素の `<a>` で移る。

**機材の詳細ページは足さずに分けた。** `client-equipment/src/pages/EquipmentDetailPage.tsx` は1ファイル 400 行の検査（`scripts/check-file-size.mjs`）の記録を超えたままのファイルなので、品名から同じ品名の機材を引く処理を `pages/detail/nameSuggest.ts` に切り出し（動きは変えていない）、Wiki のカードを足したうえで **1,032 → 1,030 行**にした。

検証: `npx tsc -b client-wiki`・`npx tsc -b client`・`npx tsc -b client-equipment`（0 errors）・`npx eslint`（新しい5ファイルと触った3ファイルで 0 errors）・`node scripts/check-file-size.mjs`・`node scripts/check-ui-tokens.mjs`（新規の違反 0）。


**Codex レビュー（#735 1巡目・P1×1・P2×4）を全件直した。** ①**P1**「見直した」の記録がスペース単位でしか絞られておらず、**下書きの題がスペースの全員に見えて**いた（`markReviewed` は自分の下書きでも押せる）ので、一覧と同じく公開ページだけに揃えた。②担当が空かどうかを**日付より先に**見るようにした（日付が先だと、担当が空で予定日も過ぎたページが `overdue` に入り、「担当なし」の数と月初の通知が実態より少なく出る）。③スペースの絞り込みを**画面からサーバーへ移した**（`?space=`）—— 上限（見直し 300件・足りないページ 50件）の先にある行は手元に無いので、他のスペースで枠が埋まると「0件」に見えていた。④親を消した返信は根に上げているのに `parent_id` を持ったままだったため、画面は返信欄を出すのにサーバーが 400 で断っていた（`parent_id: null` で返し、親が消えている返信は根として受ける。**生きている返信への返信は今までどおり 400**）。⑤「回答からページにする」は**新しい下書きと元の回答の両方**を結ぶので、`UNION` のままだと同じページが2行出て**ページ数もコメント数も倍**になっていた（1ページ＝1行にし、いちばん古い出力を採る）。検証: 5件すべて検証用 Postgres（`onair_verify`）で再現してから直し、実測で確認（①下書きの題が消える ②`no_owner` になる ③上限2件で `0件 → 2件` ④`400 → 201`・生きている返信への返信は 400 のまま ⑤`ai_pages 2 → 1`・`comments_total 2 → 1`）。
