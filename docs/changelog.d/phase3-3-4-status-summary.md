**Phase 3-3-4（`customers`/`vendors` を直接参照する箇所の書き換え）の状況を整理した**（コード変更なし）。PR #225〜#229 で対応した内容（`companies.routes.ts` の旧支払条件列書き込み停止・削除操作のロールフラグ同期・`customers.routes.ts` の `CUSTOMER_JOIN` 除去・横断検索/バックアップ/決算取込/Excel入出力/MCP/共通バリデーションの顧客側 `EXISTS` チェック除去）を `docs/reviews/phase3-2-plan.md` に一本化して記録し、工程表（ガントチャート）も更新した。顧客側の書き換えはこれで完了。仕入先側（`vendors.routes.ts` 自身等）は `budget:editor` 単独編集を反映する設計のため対称にできず、vendor側の一次分析（本番相当データが必要・上表#1）とテーブル削除時の反映まで意図的に着手しないことを明記した。
