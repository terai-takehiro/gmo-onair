**`customers`/`vendors` テーブル本体を削除した**（Phase 3-3-7〜9・migration 205・会社リスト一本化の最終段階）。Phase 1〜3-3-6 で取引先マスターの正を `companies` に一本化してきたが、最後まで残っていた `customers`/`vendors` テーブル本体と、それに依存するコードをすべて撤去した。migration は `LOCK TABLE ... IN ACCESS EXCLUSIVE MODE` で書き込みを止めた上で、`vendors`/`customers` 側が `companies` より新しい可能性のある列（`budget:editor` 単独編集の名残）を最終突き合わせしてから同一トランザクションで `DROP TABLE` する。あわせて仕入先編集の「壁」（`budget:editor` 単独の編集は `companies` に反映しない仕組み）を撤廃し、`budget:editor` の編集がそのまま `companies` に反映されるようにした（ユーザー承認済みの設計判断・新しい列や承認フローは追加しない最もシンプルな選択）。`customers`/`vendors` への直接参照・書き込み・legacy URL 互換コード（Phase 3-3-1 確認済みで実利用0件）を26ファイルから除去し、`platform/routes/data-viewer.routes.ts` の `ALLOWED_TABLES` からも外した。⚠️ **このmigrationは不可逆**（テーブル自体が消える）なため、マージ前に①本番のオンデマンドバックアップ（PR作成前のリハーサル）②本番のオンデマンドバックアップ（マージ直前）の2回の取得・確認をユーザーに依頼した。検証: `npm run verify:fresh`（migration 205 込みのフレッシュDB）でテーブル削除の適用を確認し、サーバーを起動して `GET /companies` `GET /customers` `GET /vendors` `GET /search` `GET /vendor-summary` の応答・取引先の新規作成/更新/削除・`budget:editor` 単独での仕入先編集が即座に `companies` に反映されること（壁撤廃の確認）・legacy URLアクセスが404になることを確認した。`npm run typecheck:all` / `npm run lint`（0 errors）/ `npm run test` もすべて通過。
