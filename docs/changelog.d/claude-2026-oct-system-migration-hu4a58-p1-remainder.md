**2026年10月の事業再編・P1の残作業（移行センター・通知ジョブ・番号解決の配線・文書更新）をマルチエージェントで実装した**（`docs/reorg-2026-10-plan.md` §4.8・§4.10）。①核（直接実装）: 新設`migration-center.service.ts`の`listRenumberCandidates()`が、発番済みで現行番号がまだ旧方式（`project_numbers.scheme='gls'`）の案件を全部取り、1件ずつ既存の`resolveEntity()`（P1）に掛けて対象を絞る（判定を2か所に持たない。B（プロジェクト管理）は`resolveEntity()`自体がstageを見ないため、完了・失注を明示的に除外——§4.4・§9-Lの決定どおり）。この1関数を対象一覧・進捗（`.length`）・`done`状態への残件0ゲート・通知ジョブの4箇所が使い回す。`org-transition.routes.ts`に`GET /renumber-candidates`（`system_admin`限定——お客様名・請求状況・主担当まで含むため、既存の`GET /`と違い認証済みなら誰でも、にはしない。実装直後の見直しで気づき修正した）を新設し、`PUT /`の`done`への遷移だけ残件0を検査するガードを追加した（`org-transition.service.ts`に置くと`migration-center→entity-resolution→org-transition`の循環importになるためroute層に置いた）。`scheduler.service.ts`に`renumber_needed`ジョブ（毎朝9:15・主担当へベル+メール）を新設し、migration 290で通知ひな形を追加した。このジョブだけ明示的に`sendMailAsync`を呼ぶ（ファイル冒頭の「社外メールは送らない」方針は客先向けの話で、これは社内の担当者向けのため対象外と判断）。§4.8「毎朝、残0になるまで」どおり間引かない。②移行センターUI（並列実装）: 「会社と切替」画面に対象一覧のカード（`RenumberCandidatesPanel.tsx`/`RenumberRow.tsx`/`RenumberDialog.tsx`）を追加。現在の番号・実施日・お客様・グループ内外・導出された計上会社（既存の`ENTITY_BADGE_LABEL`/`ENTITY_BADGE_TONE`を流用）・止める理由（発行済み・入金済みの売上の注記）・自社行になり得る客先の注記（`self_customer_hint`。目立たせるだけで自動では書き換えない）を一覧表示し、行ごとの改番ボタン→プレビュー→理由必須→実行の確認ダイアログを実装した。`TransitionPanel.tsx`の「完了にする」ボタンは残件数が1件以上ある間は無効化し、サーバーの400`RENUMBER_REMAINING`を先回りする。③番号解決の配線（並列実装）: `create_studio_booking`MCPツールに`gls_number`引数を追加し、現行番号と旧番号（`project_numbers`）の両方から完全一致で案件を解決するようにした（P1の既存6箇所と同じ形をそのまま踏襲・共有ヘルパー化はしていない）。`client-techops`のトップ検索（`GET /techops/top-items`）と進行台本の案件紐づけ選択（`GET /lookup/gls-options`）に旧番号の配列`retired_numbers`を追加し、トップ検索はクライアント側`matchesSearch()`が旧番号も対象にするよう拡張、紐づけ選択はRadixの`<Select>`のキーボードタイプアヘッドが前方一致のみで末尾追記が効かないことをソースで確認したうえで、選択肢のラベルに「(旧: GLS-A012)」と出す形にした。両方とも`org_transition.state`・`entity_code`は見ない（旧番号→現在の案件、という解決だけの機能で切替状態に関わらず常に効く）。④文書更新（並列実装）: `docs/mcp-server.md`に`entity_code`引数が付いた8ツール・新設`renumber_project`・新設カテゴリ「社内取引」の`create_intercompany_purchase`・`list_projects`/`get_project`出力の`entity_code`系フィールドを追記し（ツール数159→161種・22→23カテゴリ）、`docs/wording.md`に「計上会社」「コストセンター」「改番」「社内取引」の用語と`org_transition.state`の4状態ラベル対応を追記した。`org_transition.state`が今日も`off`のままであることを踏まえ、「もう変わった」ではなく「仕組みが実装済みとして存在する」という書き方に統一した。文書更新は番号解決の配線と並行して進んだため、当初`create_studio_booking`の`gls_number`は未実装として記載を見送っていたが、実装完了後にこちらで追記して揃えた。副産物として、`scripts/generate-mcp-tools.mjs`の`CATEGORY_LABELS`に`create_intercompany_purchase`（P2 Round 2で新設）のカテゴリ`intercompany`のラベルが未登録だった穴（英語キーがそのまま画面に出る）も発見・修正した。検証: `server`/`client`/`client-techops`の型チェック、`shared`のVitest 2005件、検証DBの作り直し（migration 1〜286通し適用＋seed）、`npm run lint`一式、ESLint（0 errors）、実サーバーを起動してのHTTPレベル確認（対象一覧が`off`時0件・`preparing`/`cutover`でGJV/GSS/GMOへの解決と自社行客先の注記・改番の実行と一覧からの消失・`done`ゲートの400・通知ジョブのベル/メール送信と重複防止・旧番号解決の成功/404を一通り確認。確認後はテストデータを削除し状態を`off`へ戻した）。

