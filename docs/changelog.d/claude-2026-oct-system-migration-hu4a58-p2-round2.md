**2026年10月の事業再編（社名変更・計上会社の2社化・案件番号の改番）P2 Round 2（社内取引 GJV⇄GSS）を実装した**（`docs/reorg-2026-10-plan.md` §4.12）。GJV が受けたグループ外の案件を GSS のスタジオ・人員・機材で作るときの社内取引（GSS→GJV の社内売上と GJV の社内仕入）を、同じ案件に付く会社の違う2行として記録できるようにした（写しの案件は作らない）。①サーバー: `intercompany_links`（migration 289）で売上・仕入を1対1に結び、作成（`POST /intercompany`）・編集（`PUT /intercompany/:id`）・削除（`DELETE /intercompany/:id`）・一覧（`GET /intercompany?project_id=`）・金額の下見（`GET /intercompany/suggest-amount`。最新の見積の原価行の合計）の専用経路を新設した。通常の `PUT/DELETE /revenues|purchases/:id` はリンク済みの行を409 `INTERCOMPANY_LINKED` で止め（`revenues.routes.ts`/`purchases.routes.ts` 双方に同じガードを配線し、設計書 §6 が指摘していた「仕入側だけガードが無い穴」を塞いだ）、片方だけ直せない・消せないようにした。売り手（GSS）側が請求書発行・検収・入金済みなら専用経路からも直せない（409 `INTERCOMPANY_INVOICED`）。MCPツール `create_intercompany_purchase`（confirm 2段階）も追加した。②クライアント: 案件詳細の見積タブ「売上・請求」ペイン（`RevenueBillingPane`/`InvoiceGroupsSection` と同じ並び）に、GJV の案件だけに出す「社内取引」区画（`IntercompanySection.tsx`/`IntercompanyDialog.tsx`）を新設した——設計書は入口を「仕入タブ」と書いているが、実装に専用の仕入タブは無く、売上・仕入（原価）を実際に扱っている場所がここだったため。「サムライスタジオへ社内発注」ボタン（`FormDialog`）で回（必須）・金額（下見の値を初期値に）・計上日／備考（任意）を入力して登録し、一覧の編集・削除も同じ区画から行える。削除・編集が409で失敗したときはサーバーのメッセージ（請求書発行・検収・入金が済んでいる旨）をそのまま帯に出す（`notifyApiError`）。一覧の取得もサーバーが `sales:editor` を要求するため、権限が無い利用者には区画ごと出さない（空を「無い」と誤読させないため）。③粗利の集計: 案件全体の粗利（`project.service.ts` の `getSummary`/`getSummaries`。案件一覧・詳細・keep-report 等が使う既存の実装）から社内取引ペアを除外するようにした——社内売上・社内仕入は同額で計上され粗利の円額そのものには影響しないが、除外しないと `total_revenue`/`total_purchase`（＝粗利率の分母）が水増しされる。P2 Round 1 で新設した会社別の内訳 `getSummaryByEntity` は無変更で正しいことを実装・検証して確認した（社内売上は売り手＝GSS、社内仕入は買い手＝GJVの `entity_code` で別々に計上されるため、`entity_code` の GROUP BY だけで自動的に設計（§4.12）どおりの「会社別」の式になる。当初の設計メモ「Round 2 で除外が要る」は実装して確かめた結果、不要と判明した）。④「社内」の印: 売上・仕入の一覧・詳細、締め処理（請求入金・PC版）に `is_intercompany` の印を追加し（サーバー）、`IntercompanyTag`（`GroupTag` と混同しないよう枠線だけの中立色の札）で表示するようにした（クライアント。売上一覧・仕入一覧・締め処理の一覧）。MCPの `list_revenues`/`list_purchases` にも `intercompany` の印を追加した。締め処理のスマホ版（`MobileCollect.tsx`）はP2 Round 1で entity_code 対応を見送った経緯と同じ理由でP2 Round 2でも対象外のまま。⑤案件詳細の粗利表示: 案件全体の粗利は「ふりかえり」タブ（`ReviewTab.tsx`→`review/ReviewSide.tsx`の「お金」カード）にあることが分かり、そこに「全体｜会社別」のセグメントトグルを追加した（既存の全体表示・`getSummary`の呼び出しは無変更）。「会社別」選択時だけ新設の `GET /projects/:id/summary-by-entity` を呼び GJV/GSS の内訳を出す。社内取引が無い案件（内訳が1件以下）ではトグル自体を出さない。⑥ついでに、`purchases.routes.ts` の `PUT` に配分グループの編集ガード（`PURCHASE_IN_ALLOCATION_GROUP`）を追加した——`revenues.routes.ts` の同種ガードに相当するものが仕入側には無かった既知の穴（設計書 §6 が指摘）。MCPの `renumber_project`（P1で追加）が権限ゲート（`gate.ts`）に未登録だったバグも今回のラウンドの検証中に見つけて修正した（`create_intercompany_purchase` は最初から登録済み）。検証: `server`/`client`の型チェック、`shared`のVitest 2004件、検証DBのフレッシュ再構築でmigration 1〜285を通しで適用、社内取引の作成・編集・削除・ガード・粗利集計（全体/会社別）を確かめる統合スモークテスト、実サーバーを起動してのHTTPレベル確認（作成→409ガード→`is_intercompany`の印→`summary`/`summary-by-entity`の数字→締め画面の印、すべて期待どおり）、`npm run lint`の全チェック（`check-links`/`check-mobile-declared`/`check-file-size`/`check-shared-wiring`/`check-tokens`/`check-ui-tokens`/`check-changelog`/`check-migration-numbers`ほか。新規追加箇所での新たな超過なし。`ProjectDetailPage.tsx`/`App.tsx`の400行超過はP0/P1由来の既知分）、ESLint。
