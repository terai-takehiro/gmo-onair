制作資料のスマートフォン編集（1行をシートで開いて編集する画面）で、素の `<input>`/`<textarea>` を
すべて `BufferedInput`/`BufferedTextarea` に載せ替えた（日本語入力の変換中に文字が二重に入る不具合の
予防）。行ラベル・尺・話し手・セリフ・ラベル・メモ・LED/XR の任意入力欄が対象。あわせてスライド列を
スマートフォンからも編集できるようにした（これまでは枠が出るだけだった。画像の形は PC と同じ
`cell.image`）。LED/XR のシーン・Cue・トランジションの選択欄にタップ領域 44px（`min-h-tap`）を足した。
