**PR #599（2026年10月の事業再編・計上会社の2社化・案件番号の改番の基盤）のレビュー棚卸しを記録した**（決めごと: [docs/branching.md](docs/branching.md) 「マージしたら、その PR のレビューを棚卸しに移す」）。⚠️ **Codex のコードレビューは usage limits で一度も実行されず、🔒 Security Review だけが完了（findings なし）**——`get_review_comments` 0件・`get_comments` は bot 通知2件のみで新規の指摘なし（API で直接確認。`npm run reviews:debt` はこの環境のトークンでは401）。あわせて、**このPR自身の育成中に自分で見つけて直した実害バグ3件**も記録した——①`doc-handoff.service.ts` で自分の entity_code 配線が `main` 側の派生値（`recognitionDate`/`paymentDue`）算出を上書きしていた実装当時のバグ、②新設した改番対象一覧APIが `system_admin` 限定を欠いていた権限漏れ、③マイグレーション番号の改番（`main` との2度の番号衝突）で依存順序が壊れ `column "entity_code" does not exist` になった不具合（7本を相対順序を保ったまま284〜290番へ改番し直して解消）。意図して残した未検証事項は [docs/reviews/codex-findings-v4.md](docs/reviews/codex-findings-v4.md) に書き出した。
