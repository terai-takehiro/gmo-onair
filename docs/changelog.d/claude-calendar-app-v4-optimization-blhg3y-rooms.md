**② 部屋の空きのPC表示を、承認済みのモック（macOSのカレンダーアプリ風）に合わせて作り直した（PC「部屋の空き」）。**
① 予定・PC版と同じく、ミニカレンダーを共通の左メニューへ常設した（`shared/.../shell/sideMenuSlot.ts`
の差し込み口へ `calendar/CalSidebarExtras.tsx` を portal する）。この画面はレイヤーという概念を
持たないため「マイカレンダー」のチェックは出さず、代わりに「レイヤーの絞り込みは持ちません」と
説明文を出す（`CalSidebarExtras` に `emptyNote` を追加）。ツールバーは今日／前後（日単位）／
日付の見出し／拠点の絞り込み だけの macOS 風の1段（`rooms/RoomAvailabilityToolbar.tsx`）にした。
部屋は拠点ごとに見出しでグループ化して表示するようにした（旧実装は部屋名の下に拠点名を小さく
書くだけで、拠点をまたいで部屋を探しにくかった）。**予約の帯の色を「部屋の色」から「種別の色」
（`BOOKING_TYPE_COLORS`）に変えた** — ① 予定・③ 仮押さえはどちらも種別で色分けしており、
同じ予約がこの画面だけ違う色に見える食い違いがあったため。帯の中の文字は種別の色をそのまま
使わず、コントラスト対策で濃くした別の色を使う（`rooms/availability.ts` の `KIND_TEXT_COLORS`）。
併せて、① 予定の左メニュー差し込み（先の回でマージ済み）に、承認済みモックにある「見る」ナビ
直前の区切り線が抜けていたのを見つけて足した。
検証: `npx tsc -b client` / `npm run lint` / `npm run test` OK。
