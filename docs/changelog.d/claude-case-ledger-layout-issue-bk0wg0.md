**案件台帳だけ画面の余白が無く、見出しも表も左メニューに貼り付いていたのを直した**（ご指摘）。
①⚠️ **`/sales/projects/ledger` の余白は上下左右とも 0px でした**（実ブラウザで実測）。
共通シェルの `<main>` は余白を持たない決めで、**画面の余白は各画面の根が持ちます**が、
この画面だけ根が `space-y-4` だけでした。そのため**見出し・整合性の確認・絞り込みの帯・表が
すべて左メニューの罫線に接し**、右端も画面の縁に接していました。
②**`PageHeader` を使う 39 画面のうち、根に余白を持たないのはこの1枚だけ**で、
**案件一覧と行き来すると表の左端が 24px 跳ねて**いました。
③**隣り合う一覧と同じ段に揃えました**（`p-4 lg:px-6 lg:pb-6 lg:pt-5` ＝ PC で 上20/左右下24px。
`lg:p-6` にすると上だけ 4px ずれます）。
④⚠️ **エラーが出ず機能も全部動くので誰も報告しません** —「なんとなく詰まって見える」形の崩れです。
⑤**検証**: 1440px / 1280px で 直す前 `0/0/0/0` → 直したあと `20/24/24/24` になり、
**案件一覧・タスク一覧と完全に一致**（横はみ出しは前後とも 0px・表の列は1つも触っていません）。
⑥**変えたのは1ファイルの className 1つだけ。** DB 変更なし、`shared/` も触っていません。
