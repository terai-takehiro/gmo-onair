**案件台帳だけ画面の余白が無く、見出しも表も左メニューに貼り付いていたのを直した**（ご指摘）。
①⚠️ **`/sales/projects/ledger` の余白は上下左右とも 0px でした**（実ブラウザで実測）。
共通シェルの `<main>` は余白を持たない決め（`shared/src/client/shell/AppShell.tsx`）で、
**画面の余白は各画面の根が持ちます**が、この画面だけ根が `space-y-4` だけで
`p-…` を1つも書いていませんでした。そのため**見出し・整合性の確認・絞り込みの帯・表が
すべて左メニューの罫線に接し**、右端も画面の縁に接していました。
②**`PageHeader` を使う画面 39 枚を数えたところ、根に余白を持たないのはこの1枚だけ**でした
（残りは `p-3`/`p-4` ＋ `lg:p-6` 系）。つまり**案件一覧 ⇄ 案件台帳を行き来すると、
同じ役割の表の左端が 24px 跳ねる**状態でした。
③**隣り合う一覧と同じ段に揃えました** — `p-4 lg:px-6 lg:pb-6 lg:pt-5`
（PC で 上20 / 左右下24px）。案件一覧・タスク一覧と1ピクセルも違わない値です
（`lg:p-6` にすると上だけ 4px ずれます）。
④⚠️ **これはエラーが出ず、機能も全部動くので誰も報告しません** — 「なんとなく詰まって見える」
としか言えない形の崩れです。
⑤**検証**: 実ブラウザ（1440px / 1280px）で、直す前は
`padding 0/0/0/0`・見出しの左端が本文領域の左端と同じ **0px**、
直したあとは `20/24/24/24`・見出しの左端 **24px**・上 **20px** で、
**案件一覧・タスク一覧と完全に一致**することを実測しました。
**横はみ出しは前後とも 0px**（表の列は1つも触っていません）。
`npm run test` / `typecheck` / `lint` は下記のとおり。
⑥**変えたのはこの1ファイルの1行（＋説明）だけです。** DB の変更はなく、
`shared/` も1ファイルも触っていません（凍結4アプリの CSS は不変）。
