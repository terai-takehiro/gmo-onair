**Codex のレビューが利用上限で走らないままマージした Wiki の PR（#730・#733・#735）と #740 に `@codex review` を付け直し、届いた14件（P1 5件・P2 9件）をすべて直した。** 主なもの: ①ONAiR の書き出しを取り込み直すと画像が元の閲覧範囲のまま（または壊れたまま）だった ②足りないページの質問の文が、スペースで絞られずに見直しの画面・MCP・**次に質問した全員の AI のプロンプト**に載っていた（数だけにした） ③manager が他人の質問から「AI で下書きを作成」を押すと必ず 404 だった ④空のスペースを削除する間にページが作られると、消えたスペースの下にページが残った（行の錠で直列化） ⑤同じ質問で同時に「ページを作成」を押すと下書きが2本できた（棚卸しで ❌ にしていたもの）。ほか、Wiki の権限が無い人を担当・メンバーに選べる、101人目以降を選べない、月初の通知が読めない担当にも届く、無修正で公開した AI の下書きが採用率の分母から抜ける、同じ会話の同時の質問で答えを取り違える、など。

検証: 各指摘を検証用 Postgres で**直す前の挙動を再現してから**直した（MCP の権限の1件だけは手元で OAuth を通せず、コードの読み直しと型検査まで）。`npm run typecheck:all`（0 errors）・`npm run test`（server 139件）。一覧は `docs/reviews/codex-findings-v4.md`。
