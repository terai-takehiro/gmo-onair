**Codex のレビューが利用上限で走らないままマージした Wiki の PR（#730・#733・#735）と #740 に `@codex review` を付け直し、届いた14件（P1 5件・P2 9件）をすべて直した。あわせて、#730 のマージ直前に付いたまま返信していなかった4件（zip の件数で山が尽きる・20段より深いページが書き出しから消える・同じ名前のページが書き出しで1枚消える・101人目以降が「担当」の候補に出ない）も直した。さらに、その直しに付いた次の巡の2件（2人が同じスペースを同時に編集すると、古い「全員」で上書きされて限定のスペースが全員に開く／メンバーから外された manager が隠れたスペースの質問を AI の下書きの材料にできる）も直した。その次の巡の3件（zip の件数を偽るとすり抜ける・担当の割り当てとメンバーの削除が同時だと担当が読めなくなる・停止中の人を担当に選べる）と、確認中に見つけた取り込み側の20段の打ち切りも直した。最後に、メンバーから外された直後の manager の保存が通り限定のスペースを開けてしまう競合も直した（錠を取ったあとで読めるかを見直す。メンバーの追加も含む4か所）。続く巡の2件（部分の保存が別の人の変えた閲覧範囲を古い値に戻す・既にある下書きに AI で書く途中に質問が片づくと下書きだけ書き換わる）も直した。ページの作成も、錠を取ったあとで読めるかを見直すようにした（「メンバーだけ」へ切り替わる途中に、読めなくなった人がページを足せた）。足りないページの質問を片づける瞬間にも、見てよい質問かを同じ取引の中で見直すようにした（AI を待つ間にスペースから外された manager が片づけられた）。ページの担当・人の項目も、スペースと同じく Wiki を使える在籍中の人だけから選ぶようにした（人の項目はサーバーでも確かめる。担当・人の項目とも、2人が同時に直しても錠を取ったあとの値と比べる。行の作成・MCP の行の作成では、行と値を1つの取引で入れて確かめる）。ページの保存も、錠を取ったあとで読めるかを見直すようにした（「メンバーだけ」へ切り替わる途中に、外れた人の保存が通った）。既にある下書きに AI で書くときの質問の押さえは、書き換えと1つの取引にした（途中でサーバーが止まっても質問だけ片づいた扱いで残らない）。** 主なもの: ①ONAiR の書き出しを取り込み直すと画像が元の閲覧範囲のまま（または壊れたまま）だった ②足りないページの質問の文が、スペースで絞られずに見直しの画面・MCP・**次に質問した全員の AI のプロンプト**に載っていた（数だけにした） ③manager が他人の質問から「AI で下書きを作成」を押すと必ず 404 だった ④空のスペースを削除する間にページが作られると、消えたスペースの下にページが残った（行の錠で直列化） ⑤同じ質問で同時に「ページを作成」を押すと下書きが2本できた（棚卸しで ❌ にしていたもの）。ほか、Wiki の権限が無い人を担当・メンバーに選べる、101人目以降を選べない、月初の通知が読めない担当にも届く、無修正で公開した AI の下書きが採用率の分母から抜ける、同じ会話の同時の質問で答えを取り違える、など。

検証: 各指摘を検証用 Postgres で**直す前の挙動を再現してから**直した（MCP の権限の1件だけは手元で OAuth を通せず、コードの読み直しと型検査まで）。`npm run typecheck:all`（0 errors）・`npm run lint`（0 errors）・`npm run test`（server 139件）。一覧は `docs/reviews/codex-findings-v4.md`。
