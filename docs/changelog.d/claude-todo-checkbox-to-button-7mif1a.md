**ToDo（タスク）を片づける操作を、チェックボックスから「対応済にする」ボタンに替え、画面の言葉も「対応済／未対応」に揃えた**（9/6 ご依頼）。これまでタスクの完了は行の左に置いた**四角のチェック**（チェックボックス、または同じ見た目の 18px の丸/四角ボタン）で切り替えていたが、四角には文字が無いため「押すと何が起きるか」が読み取れず、スマホでは触れただけで状態が変わっていた。文言を持つ共通部品 `TaskDoneButton`（`shared/src/client-v4/taskDoneButton.tsx`。未対応なら「対応済にする」・対応済なら「未対応に戻す」）を新設し、ToDo 系の全画面をこれに寄せた——案件管理のタスク一覧（PC の行は 7段の 128px で「対応」列を新設・スマホのシート・かんばん・サブタスク・案件詳細のタスクタブ）、プロジェクト管理（GLS-B）のタスク（全プロジェクト一覧・プロジェクト詳細の工程配下・かんばん・スマホのカード）、日常業務の「やること」、ホームの「自分のタスク」カード。あわせて画面の語を**完了→対応済／未完了→未対応**に統一した（絞り込みチップ・件数・状態バッジ・トースト・読み上げラベル・依頼の状態）。DB の列（`is_completed`）と API は変えていない。**案件・プロジェクトのステージの「完了」は別の意味なので触っていない**。決めごとは [docs/wording.md](docs/wording.md) の言い換え表に追記した。検証: `client`/`client-daily`/`client-equipment`/`server` の型チェック、lint 全項目、`shared` の Vitest 1996件、`npm run build:changed` すべて通過。実ブラウザでの PC/スマホ確認は未実施（この環境に Docker が無く検証用 DB を立てられないため）。
