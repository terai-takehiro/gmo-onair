**UIUX操作レポート（2026-08-18・寺井氏）の指摘16件を裏取りし、優先度の高いものから直した。** ①⚠️ 案件詳細の見積金額が概要タブと見積タブで矛盾して見える（見積0件だと概要タブが想定金額へ無言でラベルそのまま表示していた）、②タスクのカンバンがカラム0件だとタスク件数を無視して常に空表示になる、③機材点数がダッシュボードと台帳一覧で別の集計（子機材の扱いが違う）だった、④機材管理・日常業務のヘッダーに検索が最初から無くCtrl+Kも効かない、⑤案件台帳のセル編集中はCtrl+Kが`stopPropagation`で握りつぶされていた、⑥財務ダッシュボード・サイドバーの開発者向け文言がそのまま露出、⑦週報が「未確認」のまま確認する手段が無かった（確定＝公開するまで恒久的に未確認）、をそれぞれ直した。検証: `npm run typecheck` / `npm run lint` / `npm run test` OK。調査の全文と残る指摘（カレンダーの「期限が近い仮押さえ」の文言、通知のグルーピング等）は `docs/reviews/2026-08-19-uiux-operation-report-response.md` を参照。
