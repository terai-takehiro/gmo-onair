**GPM のプロジェクト詳細で、コストセンター（GMO）の案件だけ「請求」タブを「予算と実績」タブに差し替えた**（2026年10月の事業再編・P3のクライアント側。サーバー側の予算対実績API・売上登録の閉鎖は前段「P3 コア」で実装済み）。①普通の会社（GJV/GSS）の案件はこれまでどおり「請求」タブ（`BillingTab.tsx`→`BusinessProjectView`）のまま——`key`（URL）は変えず、ラベルと中身だけをプロジェクトの計上会社（`entity_code`）とその`kind`（`GET /legal-entities`）から出し分ける（新設`isCostCenterProject()`・`platform/pages/reorg/types.ts`。**`gls_category==='B'`では判定しない**——`org_transition.state`が`off`／未改番のあいだは GLS-B 案件でも`entity_code`はまだ'GSS'のままなので、判定基準はサーバー側の`isProjectCostCenter`と揃えた）。判定に使う`entity_code`は`GET /gpm/projects/:id`のプロジェクト詳細に新規追加した（一覧`GET /gpm/projects`はこの判定に使わないため対象外）。②コストセンター案件では新設`BudgetTab.tsx`を表示——予算・実績・残の3枚（`StatValue`で強調）・予算に対する実績の割合バー・月次コストの推移（表・行ごとに簡易バー）・仕入の一覧と登録。仕入部分は`BusinessProjectView.tsx`の切り出し済み部品（`usePurchaseForm`/`PurchaseList`/`PurchaseDialog`）をそのまま呼び、写しは作っていない。売上一覧・売上の登録ボタン・月次請求管理は一切出さない——サーバー側で売上登録自体が 409 で拒否される設計のため、見せると「押しても失敗するだけ」のボタンになる。③このタブはスマホでも開ける——`BillingTab`の月次請求（`BusinessProjectView`をそのまま呼ぶ・作り直していないため PC 専用）と違い最初からレスポンシブに作ったので、`effectiveMobileTabs()`にコストセンターのときだけ`billing`をスマホの帯へ足す引数を追加した（普通の会社の案件はこれまでどおり PC 専用の案内のまま）。

⚠️ **見積の受理（`accepted`にする）操作は現状どこにも無い**——既存の「見積」タブ（`EstimatesTab.tsx`）に送付済み・受注へ進めるボタンが無く、サーバー側にも GPM 用の汎用`PUT /gpm/estimates/:id`（案件管理側の`PUT /projects/:projectId/estimates/:id`・`EstimateActions.tsx`に相当するもの）が未整備なため、コストセンター案件の「予算」は当面 0 のまま——別途の対応が要る（フォローアップ）。

⚠️ **検証中に見つけて直した既存バグ（本タスクのスコープ外・当日の別コミットが原因）**: `gpm.service.ts`の`projectService.create()`（`POST /gpm/projects`）の INSERT 文で、列リスト16個に対し VALUES の式が15個しか無く（`entity_code`追加時にプレースホルダの追加漏れ）、**GPM プロジェクトの新規作成が常に500で失敗していた**（`INSERT has more target columns than expressions`）。実サーバーでの検証中に発見し、欠けていた1つの`?`を足して直した（原因コミット: `8800222d`「全INSERTにentity_codeを明示し、282でNOT NULL化した」・34箇所13ファイルの一括変更の1つ）。**同じ回で変更された残り33箇所も同種の見落としが無いか、別途の棚卸しを推奨する。**

検証: `server`/`client`の型チェック、`shared`の Vitest 2004件、`npm run lint`のうち`check-links`/`check-mobile-declared`/`check-file-size`/`check-shared-wiring`（新規追加箇所での新たな超過なし。`ProjectDetailPage.tsx`/`App.tsx`の400行超過は P0/P1 由来の既知分）、ESLint（変更ファイルのみ）。検証用 Postgres（`npm run verify:up`）に対し実サーバーを起動して HTTP レベルで確認（GMO 案件は`entity_code`が正しく返り`/budget`が200、通常案件は404、仕入登録後の実績・月次コストの反映、受理済み見積を模した SQL 更新後の予算反映）。**実ブラウザ（Playwright・PC 1280px／スマホ375px）でも確認**——GMO 案件は「予算と実績」タブに差し替わり予算・実績・残・バー・月次表・仕入一覧が正しく表示され、スマホでも4番目のタブとして開けて崩れない。通常（GJV/GSS）案件は「請求」タブ・PC 専用の案内とも変更前と同じ表示のまま。コンソール・ページエラーなし。
