**PR #459（棚卸し記録）と PR #460/#461（release: v4.5.2）のマージ後の棚卸しを記録した**（コード変更なし）。#459 はレビュー0件のまま terai-takehiro 本人が手動マージ（作成から約2分43秒後）。v4.5.2 の版上げは最初 `claude/release-prep-lod7ck` 枝で #460 として出したが、CI の `check-changelog.mjs` がリリースPRと判定する条件（`RELEASE=1` か枝名が `release/` で始まること）に外れて `checks` が落ちたため、`release/4.5.2` 枝に出し直し #461 として再オープンした（#460 はクローズ、マージなし）。#461 もレビュー0件のまま手動マージ（CI green から約1分12秒後）。`docs/reviews/codex-findings-v4.md` に各PRの経緯を記録した。
