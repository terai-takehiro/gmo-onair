**スタジオ予約にも「担当者」（複数・任意）を追加した**（9/5 ご指摘「スタジオ予約に担当者機能がついてない」）。パートナースケジュール（#564）に続き、スタジオ予約にも実務の割り当ての補助情報として担当者を持たせたかったが、既存の `studio_booking_rooms.occupant`（部屋ごとの自由入力・社外の方も入るため登録ユーザーへの参照ではない）は別目的で、案件の主担当（`projects.assigned_to`）は予約詳細のどこにも出ていなかった。中間テーブル `studio_booking_assignees`（登録ユーザーのみ・複数・0件も可。`partner_schedule_assignees` と同型）を新設し、予約作成・編集ダイアログに担当者チップを追加、詳細ダイアログにも表示、①〜③カレンダーの「人で絞る」フィルタにも対応させた（サーバーは `/users/by-module/sales` と同じ資格条件で検証し、上限超過は400で拒否、sales権限を失った既存担当者は「対象外・外すのみ可」として表示——いずれもパートナースケジュールのレビューで踏んだ落とし穴をあらかじめ避けている）。担当者チップUIは `AssigneePicker`（`client/src/contexts/production/components/AssigneePicker.tsx`）としてパートナースケジュールとスタジオ予約の両方から共用する1本に統合した（写すと片方だけ直った画面ができるため）。あわせて `StudioBookingDialog.tsx` が1ファイル400行の基準を超えていたため、担当者欄を足すぶんの行数を「控室の利用者・用途」欄の切り出し（`GreenroomOccupants.tsx`）で相殺し、基準（685行）を超えないようにした。検証: 新規マイグレーション（279番）を検証DBに適用、`server`/`client` の型チェック、`npm run lint`、`shared` の Vitest 1996件、検証DBでのAPI実地確認（担当者付き作成・一覧/単体取得・部分更新での維持とクリア・sales権限なしユーザーの拒否・21人超過の拒否・無効な担当者IDでの作成拒否時に孤立行が残らないこと）。実ブラウザでのPC/スマホ確認・`npm run build`・`npm run verify:ui` は未実施。
