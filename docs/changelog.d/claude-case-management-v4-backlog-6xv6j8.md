**案件管理の「そのほか（作り直し前）」9画面を棚卸しし、削除できる2画面を削除、営業活動記録をv4に作り直した。** 確定案件（スタジオ）は専用APIを持たずGET /projectsを呼ぶだけで、v4の案件一覧③が完全に上位互換だったため削除（旧URLは案件一覧へ転送）。ガントチャート単独メニュー項目はタスク一覧④内蔵のガントタブと同一コンポーネントを指す重複リンクだったため削除（URL自体はタスク一覧のガント表示として現役なので維持）。残り7画面は独自機能があり削除不可と判断してバックログに残した。営業活動記録（/sales/activity-logs）はPC表・スマホカードを別々に手書きしていた旧UIを、Row/RowMain/RowSlot・PageHeader・FilterChips仲間・EmptyState/ErrorPanel・Pagination等の共通部品に作り直した。あわせてサーバーに前からあった次回アクションの完了・延期API（顧客360°ビューは使用済み）をこの一覧からも呼べるようにし、開かなくても片づけられるようにした。削除ボタンをmanager権限のみに絞り（サーバーは前からmanager必須で押せるのに403だった）、pcOnlyScreens.tsのPC専用指定も外してスマホでも開けるようにした。検証: npm run lint / typecheck / test（1136件）すべてOK。実ブラウザ（Playwright）でPC・375px両方のレイアウト、編集ダイアログ、次回アクションの完了操作を確認済み。
