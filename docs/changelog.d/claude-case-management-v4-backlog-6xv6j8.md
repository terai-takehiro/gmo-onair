**案件管理の「そのほか（作り直し前）」から、v4に吸収済みで死んでいた2画面を削除した。** 確定案件（スタジオ）（`ConfirmedProjectsPage`）は専用APIを持たず`GET /projects`を呼ぶだけで、v4の③案件一覧（`gls_category: 'A'`固定・検索/絞込/並替/カード・ボード表示）が完全に上位互換だったため削除し、旧URLは`/sales/projects`へ転送するようにした。ガントチャート単独メニュー項目は④タスク一覧（`TaskDashboardPage`）内蔵のガントタブと完全に同一コンポーネントを指す重複リンクだったため、メニュー項目だけを削除した（URL `/sales/tasks/gantt` 自体はタスク一覧のガント表示として現役なので残した）。あわせて、削除前から壊れていた確定案件一覧への戻りリンク（`BusinessProjectView.tsx` が `/sales` 抜きの旧パスへ`navigate`しており、押すとホームに戻っていた）も、正しい行き先（案件一覧・プロジェクト管理一覧）に直した。残り7画面（営業活動記録・按分グループ・旧GLS・営業レビュー・報告資料・AI活動履歴・取引先マスター）はv4のどこにも無い独自機能を持つため削除できず、引き続きv4への作り直しが必要。検証: `npm run lint` / `npm run typecheck` OK。
