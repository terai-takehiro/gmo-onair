**案件管理の「そのほか（作り直し前）」9画面を棚卸しし、削除できる2画面を削除、営業活動記録と按分グループをv4に作り直した。** 確定案件（スタジオ）は専用APIを持たずGET /projectsを呼ぶだけで、v4の案件一覧③が完全に上位互換だったため削除（旧URLは案件一覧へ転送）。ガントチャート単独メニュー項目はタスク一覧④内蔵のガントタブと同一コンポーネントを指す重複リンクだったため削除（URL自体はタスク一覧のガント表示として現役なので維持）。残り5画面は独自機能があり削除不可と判断してバックログに残した。営業活動記録（/sales/activity-logs）はPC表・スマホカードを別々に手書きしていた旧UIを共通部品に作り直し、サーバーに前からあった次回アクションの完了・延期APIをこの一覧からも呼べるようにし、削除ボタンをmanager権限のみに絞り、スマホにも開放した。按分グループ（/sales/project-groups）は一覧と詳細が同じ画面内useStateの切り替えで、詳細をURLで持てずブラウザの戻る・ブックマーク・再読み込みのどれも一覧に戻っていたのを、v4の他の詳細画面と同じくURLで持つ形（/sales/project-groups/:id、1174行の1ファイルを12ファイルに分割）に作り直した。会計用語「按分」は画面に出さない方針（docs/wording.md）に沿って、UI文言をすべて「費用を分け合う」「分け方」「分けた額」に置き換えた（コード内の技術コメントは残す）。仕入・売上ダイアログの按分方式選択ロジックが2箇所にコピーされ均等按分の端数処理が食い違うリスクがあったのをuseAllocationフックに一本化した。検証: npm run lint / typecheck / test（1136件）すべてOK。実ブラウザ（Playwright）でグループ作成・仕入登録（均等/任意比率の両方）・売上の明細行と分け方エディタ・権限表示・営業活動記録のPC/スマホ両レイアウトを確認済み。旧GLS（決算取込・/sales/gls-import）は決算取込自体が終わっているため削除し、案件台帳（/sales/projects/ledger）へ転送するようにした。旧画面の一括編集はPATCH /projects/bulkを叩いており、案件台帳の一括編集も同じエンドポイントを叩くため機能は引き継がれる（ステージだけは案件台帳側の既存方針で対象外）。あわせて、決算取込のINSERTがgls_categoryを書かないため取込直後の案件がNULLのまま残り、案件台帳の既定の絞り込みと整合性チェックがgls_category='A'の完全一致でNULLを拾えず「分類なし」の行が画面から見えなくなる穴を見つけ、GLS番号のprefixから埋め戻す移行（203_kessan_gls_category_backfill.sql、migration 086と同じ判定）を先に追加した。営業レビュー（/sales/review）は1ファイル929行を7ファイルに分割してv4に作り直した。shadcn/TabsをRow/RowSlot/MoneyCell・状態の色トークンに差し替え、実データを繋いで検証したところ実装上のバグを2つ見つけて直した：①目標設定ダイアログは旧画面がfiscal_year/fiscal_monthを送りサーバーがtarget_year/target_monthを読んでいたため保存を押すたびに毎回500で落ちていた、②営業評価タブはu.nameを選びながらGROUP BY p.assigned_to/GROUP BY t.user_idのようにu.nameを含めないSQLで、担当者に1件でも実績があると必ず500になっていた（Postgresは標準SQL違反を通さない）。あわせて目標設定ボタンをsales:editor権限で出し分けた（サーバーは前から要求していたのに押せるのに403だった）。検証: npm run lint / typecheck / test（1136件）に加え、実ブラウザで3タブの表示と目標設定の保存が実際に反映されることを確認済み。報告資料（/sales/keep-report）はv4の要件が未定のため作り直さず削除した（ご指示）。API・MCPツール（list_event_reports/get_monthly_pl/get_meeting_minutes）は削除していない — イベント実施報告のGET/PUT /keep/event-reports/:idは案件詳細⑥ふりかえりタブが今も叩いているため。月次予算目標・経理補正・議事録サマリの入力口はこの画面だけが持っていたため、削除後はこれらを書けるUIがどこにも無くなる（読む側のMCPツールは既存データを読み続けるだけになる）。旧URLは転送先が無いため削除し、pcOnlyScreens.tsからも外した。あわせて、旧画面のファイルを直接読んでいた回帰試験（消えた列highlightsを読んでいないことの検査）を1件削除した。検証: npm run lint / typecheck / test（1135件）OK。AI活動履歴（/sales/ai-activity）も「そもそも要らないのでは」というご指摘を受けて調べ直し削除した。この画面が読むmcp_audit_logは5条件が求める記録用テーブル（ai_outputs/ai_corrections/ai_outcomes）とは別物で、監査用と割り切ると明記されている単なる操作ログであり、AI改善への還流（条件4）はget_ai_feedback_digestが、人間向けレビュー（条件5）はops_reports.kind='ai_review'の別の仕組みが担う計画になっていて、どちらもこのページとは無関係だった。「AIが触ったか」を見る用途も、案件一覧の「AI作成のみ」絞り込みと案件詳細のAiReviewBanner（確認アクション付き）のほうが記録単位で上位互換。v4のトップページのコメントにも行き先として書かれていたが、実際には何も表示せずリンクも張っていなかった（旧トップの名残のコメントのみ）。バックエンドのmcp_audit_log収集・MCPツール群は触っていない。削除したのは画面と専用エンドポイント（GET /dashboard/ai-activity-feed）・共用ヘルパー（client/src/lib/aiFeed.ts）・クエリキーのみ。ついでに、aiFeed.tsのrelativeTimeがshared/format.tsのformatRelativeTimeとほぼ同じ実装で他に使っていたのはProjectFormPage.tsxの1箇所だけだったため、aiFeed.tsごと削除しその1箇所を差し替えて重複を解消した。検証: npm run lint / typecheck / test（1135件）に加え、実ブラウザで旧URLが素通りしないこと・ナビから消えたことを確認済み。
