**ウィークリー活動報告に「隔週キープの数字」（BI）と「資料をつくる」（PowerPoint の半自動生成）を足した。** 隔週キープ資料（`260904_橋口社長隔週キープv2.pptx`）の業績報告の数値部分（着地・着地見込の表／推移グラフ／ヨミ表／案件ページ／稼働カレンダー／案件実施報告／内覧会）を、会議1回ぶんの「定例報告パック」（JSON・`GET /dailyops/keep/pack`・MCP `get_keep_report_pack`）に集約し、週報を確定した時点で凍結する。判定・対目標比・差はサーバーの計算列（赤字目標の行は `100−(目標−実績)÷|目標|×100`）。10月の会社分割は、同じ日に `main` に入った2026年10月の事業再編の **計上会社 `entity_code`**（GJV＝GMOサムライコンテンツスタジオ／GSS＝GMOサムライスタジオ／GMO＝グループ本体。決め方は `entity-resolution.service.ts`・切替前は全行 GSS）をそのまま読む——隔週キープ独自の「事業主体」列は持たない。隔週キープが足す表は migration 291 の1本（`projects.keep_pick`・`companies.samurai_group`・`keep_settings`・`keep_report_packs`／`keep_report_inputs`・`keep_decks`／`keep_deck_versions`／`keep_deck_edits`）。収支は計上会社別＋全体（統合＝3社の行の単純合計・社内取引は相殺しない）で出し、目標は設定「お金のルール」の会社タブで会社ごとの月次予算として入れる（`monthly_budgets.entity_code`。経理の補正値・稼働率の数え方も同じ画面）。稼働率は内覧・仮押さえを含む利用があった営業日 ÷ 営業日（メンテナンスは除く）。「資料をつくる」（PC 専用）は前回の構成から自動で組み、ページの並べ替え・部品のドラッグ／クリック・注記の上書きを保存のたびに版と差分（`keep_decks`／`keep_deck_edits`）で残し、GMO流会議フォーマット Ver.2.5 のヘッダー・フッターで `.pptx` を出す（表・グラフはオブジェクト、書体 Noto Sans JP、`KEEP_REPORT_BOX_FOLDER_ID` があれば Box へ配置）。案件管理ではふりかえりタブに「隔週キープに載せる」を足した（計上会社の欄・列は main のものをそのまま使う）。設計の正は [docs/design/v4/keep-report.md](../design/v4/keep-report.md)、絵は `docs/design/v4/mockups/keep-report/`。検証: 検証DBに main の 282〜290 の上に migration 291 を適用してパックと構成を実行、`shared` の Vitest、`typecheck:all`、`lint`、実ブラウザで BI タブ・お金のルール・案件編集を確認、pptx は XML と構成（27ページ）を確認（この環境に Impress が無く画像化は未実施）。

**続き（同じ PR）: 「変更点は赤字」・Slack の文面・案件台帳の計上会社での絞り込み。** pptx は**前回の資料（凍結したパック）と比べて動いた所を自動で赤にする**（GMO流会議フォーマットの「変更点は赤字」・§6.3）: 数値報告の表は動いた升を赤い太字（合計行は升の地を赤）、ヨミ表は前回の会議日以降に増えた案件を行ごと・動いた案件は案件名を赤、稼働カレンダーは稼働率の数字を赤にし、そのページの左下に「赤字＝前回（M/D）の資料から変わった所」と書く（前回の資料が無い初回は何も付けない）。どの升が動いたかは `shared/src/keepReport/packDiff.ts` と サーバーの写し `keep-pack-diff.ts` が決め、`shared/tests/keepReportPackDiff.test.ts` が両方の一致を固定する。**Slack の定例投稿の下書き**をパックから組む `GET /dailyops/keep/slack-draft`／MCP `get_keep_slack_draft`（見出し・前月 着地の3行と主体別・当月 見込・ヨミ表の上位8件・実施報告・内覧会・未確定の売上・数字の元。千円・絵文字なし）と、「隔週キープの数字」タブの「Slack の文面をコピー」を足した — 決定的な整形で AI ではないので `ai_outputs` には残さず、bot 化するときは投稿の id を版に残す（§10 条件3・docs/mcp-server.md）。**案件台帳を計上会社で絞り込める**ようにした（`GET /projects?entity_code=GJV|GSS|GMO`・知らない値は 400。絞り込みの帯は `projectLedger/LedgerFilterBar.tsx` に切り出し）。検証: 検証DBで売上を1件動かして 9/30 のパックを凍結し pptx を出力、XML で動いた升だけが赤（`D1202A`・太字）で脚注が数値報告・ヨミ表・稼働カレンダーのページにだけ出ることを確認。`GET /keep/slack-draft` と `GET /projects?entity_code=GJV` を curl で確認（権限なしは 403・不正な entity_code は 400）。

**続き（同じ PR）: main の2026年10月事業再編（計上会社 `entity_code`・PR #604）に合流し、独自の「事業主体」を廃止した。** 同じ日に `main` へ入った計上会社モデル（`legal_entities` GJV／GSS／GMO・案件と帳簿の行の `entity_code`・`monthly_budgets` の主キー `(entity_code, year_month)`）とこの枝の「事業主体」（`projects.entity` gss／gscs／gig・migration 282〜285）が同じ概念の二重定義になっていたので、**main の側に寄せた**: 隔週キープの集計は帳簿の行の `entity_code`、ヨミ表・案件ページは `projects.entity_code`、HTTP／MCP の絞り込みは `entity_code=all|GJV|GSS|GMO`（不正は 400）、画面の語は「計上会社」「計上会社別」「全体（統合）」。案件フォームの「事業主体」欄・台帳の列・お客様の区分から主体を導く写し（`project-entity.ts`／`deriveEntity`）は削除し、決め方は main の `entity-resolution.service.ts` だけが持つ（`org_transition.state='off'` のあいだは全行 GSS）。隔週キープの表は **migration 291 の1本**に作り直した（旧 282〜285 は削除。番号は main の 290 の次）。設定「お金のルール」の月次予算は会社タブの会社ぶんを出し「全体（統合）を見る」で3社の合計に切り替える（main 側で `値引きの上限` が2回描かれていた重複も1つにした）。あわせてレビュー（P1〜P3）の直し: pptx の稼働カレンダーが `YYYY-MM-DD` の鍵を読まず予定が消えていた（P1）／`GET /keep/decks/:meeting` が閲覧だけで構成の行を作っていた（無ければ dailyops editor だけ作成・reader は 404、`POST /keep/decks/:meeting` を新設）／「今日」を UTC で取っていた（`jst.ts`）／写真の上書きに任意の Box ファイル id を渡せた（パックが知っている id だけ・部品ごと4枚）／保存の楽観ロック（`UPDATE … WHERE version = ?` で 409）・凍結の二重登録（advisory lock・`ops_report_id` 不在は 400）・予算の `null`＝消す／`undefined`＝保つ・`X-Keep-Warnings` は先頭5件2,000バイトまで（`X-Keep-Warnings-Total` 追加）。検証: 検証DBを作り直して main の 282〜290 の上に 291 を適用、`typecheck:all`・`lint`（0 errors）・`shared` の Vitest 2,217 件、パック／構成（22ページ）／Slack 文面／pptx 出力／権限なし 403／不正な `entity_code` 400 を curl で確認。実ブラウザでの再確認は未実施（合流前に BI タブ・お金のルール・案件編集を確認済み）。
