**ウィークリー活動報告に「隔週キープの数字」と「資料をつくる」を足す設計とモックアップを置いた。** 隔週キープ資料（`260904_橋口社長隔週キープv2.pptx`）の業績報告の数値部分を解析し、①数値報告（着地・着地見込の表）②進捗グラフ③ヨミ表④案件ページ⑤稼働カレンダー⑥案件実施報告⑦内覧会の出どころを ONAiR の実装（migration 129 の `keep-report.service` / `event_reports` / `monthly_budgets`、週報の `ops_reports`）に対応づけた。10月の会社分割（GMOサムライスタジオ／GMOサムライコンテンツスタジオ／GMOインターネットグループ人格）は案件の「事業主体」で扱う。設計の正は [docs/design/v4/keep-report.md](../design/v4/keep-report.md)、絵は `docs/design/v4/mockups/keep-report/`（デザインキャンバスとして公開済み）、JSON の型は `shared/src/keepReport/types.ts`。**画面・サーバーの実装はまだ無い**（モックの確認待ち）。
