**ウィークリー活動報告に「隔週キープの数字」（BI）と「資料をつくる」（PowerPoint の半自動生成）を足した。** 隔週キープ資料（`260904_橋口社長隔週キープv2.pptx`）の業績報告の数値部分（着地・着地見込の表／推移グラフ／ヨミ表／案件ページ／稼働カレンダー／案件実施報告／内覧会）を、会議1回ぶんの「定例報告パック」（JSON・`GET /dailyops/keep/pack`・MCP `get_keep_report_pack`）に集約し、週報を確定した時点で凍結する。判定・対目標比・差はサーバーの計算列（赤字目標の行は `100−(目標−実績)÷|目標|×100`）。10月の会社分割は案件の「事業主体」で扱う（グループ内のお客様→GMOサムライスタジオ／外部→GMOサムライコンテンツスタジオ、自動導出・人が上書き可。migration 282〜285）。収支は主体別＋全体（統合）で出し、目標は設定「お金のルール」で主体ごとの月次予算として入れる（経理の補正値・稼働率の数え方も同じ画面）。稼働率は内覧・仮押さえを含む利用があった営業日 ÷ 営業日（メンテナンスは除く）。「資料をつくる」（PC 専用）は前回の構成から自動で組み、ページの並べ替え・部品のドラッグ／クリック・注記の上書きを保存のたびに版と差分（`keep_decks`／`keep_deck_edits`）で残し、GMO流会議フォーマット Ver.2.5 のヘッダー・フッターで `.pptx` を出す（表・グラフはオブジェクト、書体 Noto Sans JP、`KEEP_REPORT_BOX_FOLDER_ID` があれば Box へ配置）。案件管理では案件の作成・編集に「事業主体」、ふりかえりタブに「隔週キープに載せる」を足した。設計の正は [docs/design/v4/keep-report.md](../design/v4/keep-report.md)、絵は `docs/design/v4/mockups/keep-report/`。検証: 検証DBに migration 282〜285 を適用してパックと構成を実行、`shared` の Vitest、`typecheck:all`、`lint`、実ブラウザで BI タブ・お金のルール・案件編集を確認、pptx は XML と構成（27ページ）を確認（この環境に Impress が無く画像化は未実施）。

**続き（同じ PR）: 「変更点は赤字」・Slack の文面・案件台帳の事業主体。** pptx は**前回の資料（凍結したパック）と比べて動いた所を自動で赤にする**（GMO流会議フォーマットの「変更点は赤字」・§6.3）: 数値報告の表は動いた升を赤い太字（合計行は升の地を赤）、ヨミ表は前回の会議日以降に増えた案件を行ごと・動いた案件は案件名を赤、稼働カレンダーは稼働率の数字を赤にし、そのページの左下に「赤字＝前回（M/D）の資料から変わった所」と書く（前回の資料が無い初回は何も付けない）。どの升が動いたかは `shared/src/keepReport/packDiff.ts` と サーバーの写し `keep-pack-diff.ts` が決め、`shared/tests/keepReportPackDiff.test.ts` が両方の一致を固定する。**Slack の定例投稿の下書き**をパックから組む `GET /dailyops/keep/slack-draft`／MCP `get_keep_slack_draft`（見出し・前月 着地の3行と主体別・当月 見込・ヨミ表の上位8件・実施報告・内覧会・未確定の売上・数字の元。千円・絵文字なし）と、「隔週キープの数字」タブの「Slack の文面をコピー」を足した — 決定的な整形で AI ではないので `ai_outputs` には残さず、bot 化するときは投稿の id を版に残す（§10 条件3・docs/mcp-server.md）。**案件台帳を事業主体で絞り込める**ようにした（`GET /projects?entity=gss|gscs|gig`・知らない値は 400。絞り込みの帯は `projectLedger/LedgerFilterBar.tsx` に切り出し）。検証: 検証DBで売上を1件動かして 9/30 のパックを凍結し pptx を出力、XML で動いた升だけが赤（`D1202A`・太字）で脚注が数値報告・ヨミ表・稼働カレンダーのページにだけ出ることを確認。`GET /keep/slack-draft` と `GET /projects?entity=gscs` を curl で確認（権限なしは 403・不正な entity は 400）。

