**技術資料（#731）のマージ直前に付いたレビュー指摘3件を直した**（Codex レビュー指摘）。①資料の複製で本体を先に確定してから行を写していたため、途中で失敗すると一部の行だけの資料が残っていた → 本体と行の複製を1つのトランザクションにした。②パッチ盤の画面の「盤を追加」がお知らせを出すだけで盤を作らなかった → 盤の名前・ch数・種類・場所・型番を入れて作成するダイアログをつないだ。③閲覧だけの人にも作成・複製・削除が出て、押すと必ず権限エラーになっていた → 編集権限で出し分けた。あわせて #731 のレビュー指摘8件を棚卸しに記録した。検証: `npx tsc -b client-techops`・`npx tsc -p server --noEmit`・`npm run lint`・`npm run test`・検証用 Postgres で複製の途中失敗時に資料が残らないことを確認。
