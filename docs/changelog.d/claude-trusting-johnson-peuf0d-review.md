**技術資料（#731）の後続の直しと、§13 のご判断9件の反映**（Codex レビュー指摘・利用者のご判断 2026-09-23）。①マージ直前に付いたレビュー指摘3件を直した（資料の複製が途中で失敗すると一部の行だけの資料が残る→本体と行の複製を1トランザクションに／パッチ盤の「盤を追加」が盤を作らない→作成ダイアログをつないだ／閲覧だけの人にも作成・複製・削除が出て必ず失敗する→編集権限で出し分け）。②§13 の9件を記録し、推奨と違った「会社は取引先を使う」に合わせて技術人員の会社を取引先 `companies` に寄せた（migration 308 で旧 `qsheet_tech_companies` を取引先へ移して削除。「会社を追加」は取引先に仕入先として追加し、編集・削除は案件管理で行う）。役職の正式名称（CA＝カメラアシスタント ほか）を補足に出すようにした。③技術資料を AI（MCP）の資料一覧 `list_production_docs` に出した（種類に運営マニュアル・会場図面を渡すと進行台本とスケジュール表が返っていた不具合も直した）。④運営マニュアルと会場図面で「編集を要求」がロックの延長で60秒後に消えていた不具合を直した（技術資料と同じ直し方）。⑤画面の文言がルールから外れていた11か所（入れて→入力して・確定を解いて→確定を解除して・技術用語のエラー文 ほか）を直し、モックと画面のコードの文言検査に比喩語「口」「紙」と和語の動詞（作る・足す・直す・消す・入れる ほか）を足した（既存の画面の668件・モックの203件は記録して増えたときだけ止める）。⑥パッチ盤の初期データを入れた（利用者が添付した完成図書のビデオパッチ盤外観図 PDF を画像にして19枚・1,600行を書き写し、migration 309 で機材名＝系統の見出し・名称＝穴ごとの印字として登録。VJP1900 を足した。人が画面で直した値は上書きしない）。あわせて #731 のレビュー指摘8件を棚卸しに記録した。検証: `npx tsc -b client-techops`・`npx tsc -p server --noEmit`・`npm run lint`（0 errors）・`npm run test`・検証用 Postgres で migration 308 の移し替え（重複名・既存一致・削除済み）と空の DB からの通し適用、複製の途中失敗時に資料が残らないこと、ロック延長で要求が残ることを確認。
