**PR #295（#294の棚卸し記録・docsのみ）がレビュー0件のままマージされたことを棚卸しに記録した。** 作成から約5分でCIが緑になった後、terai-takehiro本人が手動マージし、レビューは1件も届かなかった（`get_review_comments` で `totalCount: 0`）。表に移す指摘はない。同PR内で並行実行していた「機材台帳の行をRow部品へ移行」エージェントの成果（実際には既に別PRでRow移行完了済みと判明・進捗表の注記のみ修正）も合流させてマージ済み。コード変更なし。
