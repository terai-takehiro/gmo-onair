**プロジェクト管理（GPM）ダッシュボードの KPI「今週が期限の作業」を押しても、絞り込まれない一覧が開いていたのを直した**（多エージェント監査 `docs/reviews/2026-08-24-logic-audit.md` 指摘）。①何が起きていたか — `KpiStrip.tsx`（PC の帯・スマホのレール `MobileKpiRail.tsx` が共用）はこのカードのリンク先を `/gpm/tasks?tab=next` にしていたが、遷移先の `GpmTaskListPage.tsx` は `tab` クエリを `asks` かどうかしか見ておらず、`next` は結局「タスク」タブの既定表示（未完了の全タスク）に落ちていた。絞り込みチップにも「今週期限」に相当するものが無かった。②なぜ困るか — KPI の件数（例: 3件）と、実際に開いた一覧（未完了の全件、例: 20件）が食い違い、「押しても求めていたものが出ない」画面になっていた。③どう直したか — `GpmTaskListPage.tsx` に「今週期限」チップを新設し（`due_at` が今日から7日以内・未完了のタスク。ダッシュボード側 `GpmDashboardPage.tsx` の `weekEnd` と同じローリング7日の定義）、`?tab=next` で開いたときだけ初期状態でこのチップを選ぶようにした。件数と絞り込みは同じ判定関数（`isWeekDue`）1本から出すため、両者が食い違うことはない。KpiStrip 側のリンクは変更していない（`?tab=next` のまま）。④検証 — `npx tsc -b client`・`npm run test`（1461件）・`npm run lint` を確認済み。実ブラウザでの操作確認はこのセッションから行っていない。
