PR #637（`refactor(reorg): 計上会社の接頭辞コードをGJVからSCSに変更`）がマージされたので、docs/branching.mdの決めごとに従いレビューの棚卸しを行った。マージするとCodexレビューの指摘はGitHubの画面から消えて誰も見なくなるため、Codex Code Reviewが4巡で返した4件の指摘（すべてマージ前に修正・返信・スレッド解決済み）と、マージ直前に別PR（#636）が同じファイルを独自に修正していたことによるマージコンフリクト・意味的な不整合（レビューテストのモックが旧キーGJVのままだった穴）の対応内容を`docs/reviews/codex-findings-v4.md`に記録した。`npm run reviews:debt`はこの環境のトークンで401のため、GitHub MCPの`get_review_comments`/`get_reviews`で直接確認した（4スレッドすべて`is_resolved: true`）。検証: `node scripts/check-md-links.mjs`通過。
