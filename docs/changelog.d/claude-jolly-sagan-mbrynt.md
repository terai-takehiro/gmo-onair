**BOXの案件フォルダ名の頭に実施日を付け、BOXの名前順がそのまま実施日順になるようにした**（利用者からのご依頼）。フォルダ名は `【社内】2026.12.03_GLS-A001_案件名` の形（複数日は `2026.12.03-12.05`・年をまたぐときは `2026.12.31-2027.01.02`・実施日が未定なら `未定_`）。年を省かないのは、月日だけだと年明けの案件が前の年の12月より上に来るため。名前の組み立ては `box-folder.service.ts` の `buildProjectFolderName` 1か所にまとめた（`project.service.ts` と `entity-resolution.service.ts` にあった複製は消した）。案件名・実施日・番号が変わると、その場でフォルダ名を付け直す。対象は案件の保存・発番・区分の変更・番号の付け替え・改番と、カレンダーの予約で実施日が動いたとき。最後に合わせた名前を `projects.box_folder_name`（migration 310）に持ち、既存フォルダの付け直しと取りこぼしは日次ジョブが拾う（1回3分まで。続きは翌朝以降に進む）。付け直すのは名前にその案件の番号が入っているフォルダだけで、プロジェクト管理が作ったフォルダ（番号なし）と親フォルダには触らない。失注・終了の片づけも日付入りの名前を見分けられるようにした。検証: `shared/tests/boxFolderName.test.ts`（並び順・年またぎ・未定・安全弁）。
