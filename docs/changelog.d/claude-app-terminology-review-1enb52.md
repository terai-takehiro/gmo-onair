**アプリ全体の用語・表現をゼロベースで見直し、実装に反映した**（画面文字列 約6,500件の棚卸し＋Salesforce・Slack・Outlook・Googleカレンダーほか8サービスの日本語UI調査。調査結果は [docs/reviews/2026-09-05-terminology-review.md](../reviews/2026-09-05-terminology-review.md)）。①**権限3段の表記が画面上に4系統**あり、`manager` を「管理者」と呼ぶ画面が「システム管理者に依頼してください」と誤誘導していたのを「見るだけ／書ける／管理」の1系統に統一（`shared` の `NoPermissionPanel` が唯一の正）。②**同じものを別名で呼んでいた約60概念**を統一（進行台本／按分グループ／保管場所／製造番号／型名／持ち出し／ステージ／AI作成 など）。③ボタンの動詞を**標準語系（追加・編集・削除・キャンセル）に一本化**し、和語系との並走を解消。④失敗の文を「〈対象〉を〜できませんでした。＋次の一手」に統一し、配信対象5アプリから「〜に失敗しました」を全廃。⑤**画面に漏れていた技術語を除去**（MCP・`AI_PRICING_JSON`・`system_admin`・SSH・cron・論理削除・`deleted_at`・WebSocket・スナップショット・ポーリング・例外メッセージの直接表示）。⑥**噛み砕きすぎでラベルが説明文になっていた約40件**を短縮し、説明は副題・ヒントへ移動（「費用を分け合うグループ」→**按分グループ**、「決める人／進める人／手を動かす人」→**決裁層／推進層／実務層** など）。⑦**誤り・矛盾を修正**（機材ダッシュボードの脚注が同じ画面と矛盾／「3件を『入金を記録する』ますか」という壊れた日本語／`機材名 (型番)` が別項目に付いていた／粗利の定義が同一画面に2つ／計時・機材のマニュアルが削除済み画面や旧画面名を案内）。⑧`docs/wording.md` に**ルール6〜8（開く／残すの判断基準・ラベル12文字・動詞1組）と文型3つ**を追記し、`scripts/check-ui-tokens.mjs` の禁止語を実態に合わせて更新（**「按分」は帳簿の標準語なので禁止語から外し**、MCP・論理削除を追加）。禁止語の違反は **45件 → 0件**（baseline から項目ごと消滅）。検証: `typecheck:all`・`npm run lint`・`shared` の Vitest 1996件 全通過。
