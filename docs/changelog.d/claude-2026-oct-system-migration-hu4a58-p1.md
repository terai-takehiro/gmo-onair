**2026年10月の事業再編（社名変更・計上会社の2社化・案件番号の改番）P1の主要部分（計上会社の導出・新方式の採番・改番・回の自動生成の拡大）をマルチエージェントで実装した**（`docs/reorg-2026-10-plan.md` §4.3・§4.4・§4.8・§6）。①`resolveEntity`（§4.4の導出規則。切替状態が`off`のあいだは必ず旧方式=GLSのままになるため、切替前は挙動が変わらない）・新方式の採番（`GJV-0001`形式）・改番（`renumberProject`。回コード・Qシートの写し・BOXフォルダ名・未請求の請求キーを追随させ、旧番号は履歴に残して検索でも引けるようにする）を新設し、案件の発番（`issueGls`）と受注時の処理（`changeStage`）に配線した。受注時の第1回自動生成も、これまでレギュラー案件だけだったものを、切替状態が進んだあとは単発の案件にも広げた（売上・仕入は必ず回に紐づける、という決めごとの実装）。②改番のHTTPルートとMCPツール（`issue_gls`と同じ確認2段階）を追加し、ついでに`issue_gls`の説明文に残っていた「発番で口頭決定に自動昇格する」という2026-09-02に撤去済みの挙動の記述を削除した。③旧番号（改番後に退役した番号）でも案件を引けるよう、機材設定の連携キー・グラフィックスの案件検索・案件一覧の検索・全体検索・決算取込／X-point取込の照合を拡張し、決算取込の正規表現をGJV-/GSS-/GMO-にも広げた（副産物として、新形式でも起きていたはずの決め打ちスライスのバグを同時に直した）。④案件一覧・詳細・案件台帳に計上会社（コンテンツスタジオ／サムライスタジオ／グループ本体）のバッジ・列を追加した。実装中に見つかった用語の食い違い（設計文書の「グループ本体（コスト）」と、既に出ている設定画面の「グループ本体」）は後者に合わせて設計文書を直した。検証: `server`/`client`の型チェック、`shared`のVitest 2004件、検証DBを作り直してmigration 001b〜283（264ファイル）を通しで適用、実データに近い統合スモークテスト（導出の分岐・改番の履歴と請求済みガード・旧番号検索・回の自動生成のoff/preparing差異と冪等性）を実施し全通過。通知ジョブ・移行センターの画面・財務系MCPツールへのentity_codeフィルタ（本来P2の仕事と判断）は次段に送った。
