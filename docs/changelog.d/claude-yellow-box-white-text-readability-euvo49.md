**黄色い注意書きの文字が白くて読めなかったのを直し、同じ形を機械で止めるようにした**（ご指摘）。①⚠️ **淡い帯（`bg-warning-surface` #fff7ed）に「塗りの上に載せる文字色」（`text-warning-foreground` ＝ **白**）を当てていました。** 実測 **1.07:1** で、**文字はそこにあるのに読めません**。②**同じ形を全アプリで数えたら 6画面7か所ありました** — 案件分類の切替（ご指摘のもの）・GLS の付け替え・通知とテンプレート（一覧と詳細）・休業日の追加・役割の編集。**うち5か所はダイアログの中の注意書き**で、つまり**押す前にいちばん読んでほしい文**が消えていました。③**直し方は既にある書き方に揃えました** — 短い1行は **`text-warning`**（v4 は #c2410e・淡い帯の上で **4.88:1** ＝ AA）、見出し＋本文の長い帯は**本文を `text-secondary-foreground`・記号と見出しだけ `text-warning`**（`ApplyFlowDialog` / `PhotoGrid` と同じ形。`bg-warning-surface` の 41 か所がすでにこの書き方）。④⚠️ **これは型検査にも eslint にも出ません。** どちらも実在するクラス名で、名前は**1文字違い**（`text-warning` / `text-warning-foreground`）です。`verify:ui` の「薄すぎる文字」は実ブラウザで拾えますが、**開いていないダイアログの中は測れません** — だから7か所とも残っていました。⑤**`npm run lint` に `check-contrast-tokens.mjs` を足しました。** `tokens.css` を読んで「明るい配色で白い `-foreground`」を**数え上げてから**（コードに焼き込まない）、その近くに塗り（`bg-<s>` / `bg-<s>-[5-9]00` / `var(--<s>)`）があるかを見ます。**淡い面（`-surface` / `-border`）は塗りと数えません** — ここを緩めると、まさにこの形を素通しします。塗りが遠くにあるもの（変数から来る・グラデーション）は `ALLOW` に**理由付きで**3件（`check-links.mjs` と同じやり方）。⑥⚠️ **書いている途中で `tokens.css` の切り方を1度間違えました** — 素の `indexOf('.dark')` が**注釈の中の `.dark`** に当たり、`--warning-surface` より手前で切れていました（v4.1.1 の版の見出しと同じ踏み方）。**行頭の規則で探す**形に直し、試験にも書いてあります。⑦⚠️ **この PR のレビューで検査の穴を2件指摘され、直しました**（Codex・P2 2件。**どちらも「検査が OK と言うのに画面では読めない」形**）。**①`.tsx` しか見ていませんでした** — この製品はクラス名を **`.ts` の地図**に置く形が多く（`hoursTypes.ts` / `taskList/state.ts` / `inbox/kinds.ts` など10ファイル以上）、Tailwind の `content` も `**/*.{js,ts,jsx,tsx}` で拾っています。つまり**地図に1行足すだけで画面に出るのに、検査は通っていました**（走査は 534 → **741 ファイル**）。**②条件つき・半透明の塗りを「塗り」と数えていました** — `hover:bg-warning` は**押していないときは淡い帯のまま**、`bg-warning/10` は**10% なので下の淡い面が透ける**のに、どちらも素通ししていました（3つとも**実際に素通りすることを確かめてから**直しています）。⚠️ **ただし「条件つきだから駄目」ではありません** — `data-[state=checked]:bg-primary` と `data-[state=checked]:text-primary-foreground` は**必ず一緒に効く**ので白が淡い面に載ることはなく、締めすぎるとチェックボックス2件を誤って止めます。見るのは「条件がついているか」ではなく「**文字色と塗りの条件が揃っているか**」にしました。⑧**検証**: `shared/tests/contrastTokens.test.ts` 19項目。**字面を読む試験ではなく、仕込みのファイルを作って検査を実際に走らせ、止まるか通るかで見ています**（字面だけだと書き方を変えた日に通ってしまう）。仕込みは一時ディレクトリに置きます — そのために検査は**引数でディレクトリを渡せる**ようにしました（`client/src` に置くと失敗した回に消し残る）（白は 1.1:1 未満・直し先は 4.5:1 以上・直した6ファイルに古い書き方が無い・検査が lint から呼ばれている）。**反証: 指摘された1行を元に戻すと検査が exit 1 で止まる**（実測）。`npm run test` / `typecheck` / `lint` / `build:all` ＋ `check:frozen` OK。⑧**DB 変更なし。`shared/src/` も触っていない**ので凍結4アプリの CSS は不変（足したのは `shared/tests/` と `scripts/` だけ）。
