**「だれでも読める説明書」（guide/）の5本目、v4（画面の作り直し）の背景と今の状況を説明する文書を追加した**（R4）。v4が何をどう揃え直しているか・なぜやっているかを非エンジニア向けに説明し、進捗の数字は書かず `docs/v4-progress.md`（自動生成の進み具合ページ）へのリンクに委ねた（数字は生成のたびにずれるため）。エージェントが書いた元ドラフトの相対リンクパス誤り（`../docs/v4-progress.md`）を `../v4-progress.md` に修正した。この文書はユーザーの読み合わせを経てから内容を確定する（R4の決めごと）。
