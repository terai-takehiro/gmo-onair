**PR #676（運営マニュアルミニアプリ）のマージ直後に届いたCodexレビュー指摘4件（P1×2・P2×2）を、マージ済みの`main`から新しいPRで修正した**。①[P1] `reorderPages()`（ページの並べ替え）の冒頭`assertEditable`は事前チェックに過ぎず、その直後・トランザクション実行前に管理者が確定を終えても、並べ替えのUPDATE群は無条件のまま確定済みの冊子に対して実行されていた——印刷は生のsort_orderで並べるため、すでに配布済みのrev.Nのページ順が無音で変わりうる欠陥だった。`addPage()`/`fixManual()`と同じ冊子行ロック（`FOR UPDATE OF m`）を取り合ってから改めてeditableを判定するようにした。②[P1] `acquireManualLock()`（編集ロックの取得/60秒ハートビート兼用）のWHERE句に`status != 'fixed'`が抜けていた欠陥: `fixManual()`は`locked_by`を変えないため、編集画面を開いたままの保持者の60秒ハートビートが確定後もずっと`acquired:true`を返し続け、クライアントは読み取り専用に切り替わらないまま、次の保存が汎用の400エラーで失敗するだけだった（画面を再読み込みするまで「確定された」と気づけない）。`status != 'fixed'`を弾くようにし、クライアント（`useManualEditLock.ts`）にも`onFixed`コールバックを足して、確定検知時は「引き継がれました」と分けて「確定されました」と知らせ、冊子の詳細を再取得するようにした。③[P2] ひな形適用時のsourceId再紐付け（前回のレビュー対応）は「資料が複製先の案件/番組に属しているか」だけを見ており、`sheet.resolver.ts`が課している資料自体のアクセス制御（`canAccessDoc`: 作成者本人/個別共有/管理者）を素通りしていた欠陥——同じ案件/番組であっても、ひな形を適用する本人がその資料の作成者でも共有先でもなければ`resolveAccessibleDoc`は`access_denied`を返す。所有者が一致するというだけでは適用した本人が読めるとは限らないため、`canAccessDoc`も合わせて判定し、通らなければ同じく`sourceId: null`に戻すようにした。④[P2] 編集ロックの60秒ハートビートが、実際の操作（マウス/キー/タッチ）の有無を一切見ずに無条件で`locked_at`を延ばし続けていたため、タブを開いたまま離席しても「10分操作が無ければ空き」というサーバー側のstale判定が実質一度も成立せず、タブを閉じるかmanagerが強制解除するまで他の編集者はずっと待たされる欠陥。実際の操作時刻を追い（`mousedown`/`mousemove`/`keydown`/`touchstart`/`wheel`）、一定時間（サーバーの`LOCK_STALE_MS`と同じ10分）操作が無ければハートビート自体を送らないようにした——`locked_at`が更新されなくなることでサーバー側のstale判定が働き、他の編集者が取り直せるようになる。これらの指摘はPR #676が`main`へマージされた直後（別のPRとして出す前）に届いたため、`main`から取り直したこの枝でまとめて修正した。回帰テスト: `server/tests/manual-page-insert-review.test.mjs`（reorderPages用に2件追加）・`manual-lock-review.test.mjs`（acquireManualLockの確定済みガード用に1件追加）・`manual-template-review.test.mjs`（canAccessDoc再検査用に2件追加）。編集ロックのハートビート抑制・確定通知の出し分けは画面/フックのテストを書かない方針（`shared/CLAUDE.md`）のためコードレビューでの確認に留めた。検証: `npx tsc -b server --force`・`npx tsc -b client-techops --force`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`node scripts/check-md-links.mjs`・`npm run lint`（0 errors・警告54件=既存基準のまま）・`npm run test`（shared 170ファイル/2374件・server review 79/79、全pass）。

**運営マニュアルの画面文言「題」を「タイトル」に、コード内の「GLS番号」表記を「管理番号」に直した**（ご指摘対応）。①冊子の新規作成・詳細画面のラベル/placeholder/aria-label・検索欄・ページ一覧の入力欄・書き出し設定の説明文・保存失敗時のエラー文言など、運営マニュアル画面に残っていた「題」を全て「タイトル」に統一した（`CreateManualDialog.tsx`・`ManualDetailPage.tsx`・`ManualListPage.tsx`・`PageRail.tsx`・`ManualExportSettingsPanel.tsx`・`ManualPrintCover.tsx`・関連コメント・`manual.service.ts`のConflictError文言・設計書`production-manual.md`）。「無題」（未入力時の既定表示。台本・スケジュール表など他アプリ全体で使う既存の言い回し）と「題字」（表紙の大見出しの意匠を指す別概念）は対象外——どちらも「題」＝タイトル欄そのものの指摘とは別物のため変更していない。②2026年10月の事業再編で案件番号がGLS→SCS/GSS/GMOへ改番される（`docs/reorg-2026-10-plan.md`）。DB列・API上の内部名`gls_number`は変えない決めごと（ルート`CLAUDE.md`）だが、今回新設したコード内のコメント・エラー文言・テストの固定値に残っていた「GLS番号」という一般名としての表記は、案件管理側で既に使われている汎用語「管理番号」に統一した（`ManualPrintCover.tsx`のコメント2箇所・テストの固定値`server/tests/manual-project-resolver-review.test.mjs`）。検証: `npx tsc -b server --force`・`npx tsc -b client-techops --force`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`node scripts/check-md-links.mjs`・`npm run lint`（0 errors・警告54件=既存基準のまま）・`npm run test`（shared 170ファイル/2374件・server review 79/79、全pass）。
