**制作技術支援のトップページとハブ画面を、iOS/macOSのモック通りに実装した**。
ユーザーから受けた4つの指示をすべて反映: ①アーカイブは**最後の回の翌日**から
（`GET /qsheet/top-items` を新設し、episodes の日付・qsheet_programs の event_date を
サーバー側で集計。GLS-B系・実施日未定の番組は終了しない扱い） ②「直近の本番・収録」は
**案件管理の日付を参照**（同エンドポイントの `next_date`＝今日以降で最も近い日） ③「続きから」
は閲覧履歴（`localStorage`・端末ごと。ハブ画面を開くたびに記録） ④スマホ下タブに
「トップ」を追加（「設定」を落として3本を維持。収録・配信設定はハブのタイルから開けるため）。
あわせてハブ画面（`JourneyPage.tsx`）も見た目を作り直した — ミニアプリのタイル
（進行台本・スケジュール表・レンタル機材検索）に件数バッジを追加し、日ごとのカードを
折りたためるようにし、「決まった にする」「要注意 にする」の横長ボタンを44×44pxの
アイコンボタンに変えた。**データ・ハンドラ・機能は1つも落としていない**（見た目だけの作り直し）。
検証: `npm run typecheck:all` / `npm run lint` / `npm run test`（1321件）/
`npx vite build -w client-qsheet` すべてOK。新エンドポイントは `npm run verify:fresh` で
実Postgresにシードし、GLS-A001〜A008・B001/B002・GPM案件の実データと、一時的に
挿入した過去/未来の番組データで next_date・last_date の計算を実測して確認した。
