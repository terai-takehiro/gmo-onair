**Phase 3-3-3: `mcp_audit_log` の AI登録判定を `customers.id` から `companies.id` 基準に切り替えた**（`customers`/`vendors` テーブル削除に向けた準備）。MCPの `create_customer` が書き込む監査ログの `created_id` を、これまでの `customers.id` から `companies.id` に変更し（`customers.tools.ts`）、顧客一覧・顧客詳細・顧客360°ビューの AI 登録判定（`is_ai_created`/`ai_requested_by`）の逆引きも `companies.id` 基準に切り替えた（`customers.routes.ts`）。既存の監査ログはバックフィルmigration（202）で同じ形に揃えた。実際の検索・保存条件（SQL）は変えていない。検証: `npm run typecheck` / `npm run lint` / `npm run test` OK、`npm run verify:up` の実DBで移行前後の値の変換と `GET /customers`（一覧・詳細）の AI 登録判定表示を確認した。
