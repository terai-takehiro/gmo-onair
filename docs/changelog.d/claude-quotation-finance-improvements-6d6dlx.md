**見積・財務管理の使いにくさをまとめて直した。** ①見積の明細に列見出し（数量/単価/仕入/日付）を出し、行ごとの備考・日付、見積全体の備考、タイトル編集、料金表から明細を選ぶ導線を足した（DB・サーバーは既に持っていた列を画面から使えるようにし、行ごとの日付だけ新しい列（migration 194）を足した）。②財務ダッシュボードの「案件で絞り込む」に出てこない案件があった問題を直した — `/projects?limit=500` が上限と削除済み除外を持つのに対し内訳（`revenues`/`purchases`）はそれらを持たず食い違っていたため、内訳と同じ集合を返す `/budget/projects-with-activity` を新設して合わせた。③案件で絞り込んだときは販管費・固定原価・売上総利益・営業利益のカードごと出さず「売上−仕入＝限界利益」までにした。④ダッシュボード上部のカードを、結果（限界利益・売上総利益・営業利益）を見出しの3枚にし、材料（売上・仕入・固定原価・販管費）は下の小さい1行ずつに変えて数値の圧迫感を減らした。**検証**: `npx tsc -b client` / `npm run typecheck -w server` いずれも exit 0。
