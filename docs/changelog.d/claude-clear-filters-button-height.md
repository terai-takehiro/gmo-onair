**0件のときの「絞り込みをすべて外す」が PC で寸法の段から外れていたのを直した**
（`verify:ui` の指摘。案件一覧・タスク一覧など `NoSearchResults` を出す画面すべて）。
①**PC で 38.25px**（実測）。スマホは `min-h-tap` が効いて 44px ですが、PC は
`lg:min-h-0` で床が外れ、**中身の高さのまま**になっていました
（13.5px × 1.5 ＝ 20.25 ＋ 上下の余白 16 ＋ 罫線 2）。
②ボタンの段は **32/36/40/44/48** なので、`Button` の既定と同じ **40px** に載せました。
③⚠️ **スマホは 1px も変えていません**（44px のまま）。変えたのは 1024px 以上だけです。
④**検証**: 実ブラウザで 38.25 → **40px**（スマホは 44px のまま）。
`verify:ui` は **111/111 通過**（直す前は案件一覧・タスク一覧が「ボタンの高さが段のみ」で失敗）。
⑤⚠️ **`shared/src/client/` を触りましたが、凍結4アプリの CSS は1バイトも変わりません**
（`build:all` ＋ `check:frozen` で確認済み）。DB 変更なし。
