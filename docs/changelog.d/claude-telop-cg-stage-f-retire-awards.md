**テロップCGの段F（旧リアルタイムCGの畳み込み）を実行し、再設計の実装を完了させた**（`docs/design/v4/graphics-redesign.md` §12-5で取得済みの承認に基づく。段A〜E＝後継アプリの実装をPR #600で検証環境に出し確認済みのため、実行順の条件が揃った）。①`server/src/app.ts`の`serveApp('/awards', …)`、`server/src/routes/index.ts`の`createAwardsRoutes()`/`createQuizRoutes()`、`server/src/index.ts`の`initAwardsSocketIO()`/`initQuizSocketIO()`/`initInteractivePoller()`（起動・シャットダウンの両呼び出し）を外し、`Dockerfile`の`build-client-awards`ステージと本番イメージへの`COPY`を外した——`/awards/*`はサーバーが配信しなくなり、Webサイトのどこからも到達できない。②`contexts/awards/`・`contexts/quiz/`自体のファイルは削除していない（コード保存ポリシー）。テロップCG側の投票外部連携ポーラーは`contexts/graphics/`に完全に独立して実装されているため、この撤去による影響はない。③`shared/src/client/apps.ts`のエントリ（`path`/`permissionModule`/`frozen: true`）は前回の廃止時と同じく触らず、コメントと`description`文言だけ「移行中」→「廃止」に更新した。④意図して変更しなかったもの: 検証/開発環境のダミーデータ投入（`seedAwards()`）——「廃止」は到達可能性の話であり、`awards_events`のダミーデータは移行ツール（`AwardsMigrationPage.tsx`）の動作確認に引き続き有用なため残した。⑤過去の一時的な廃止→凍結の巻き戻し時に更新されないまま残っていた検査スクリプト（`check-frozen-css.mjs`・`verify-ui.mjs`・`check-links.mjs`）の「廃止済み」という記述が、今回の実行で偶然正しい記述に戻った——変更は不要だった。⚠️ **本番の実データ移行（`awards_events`等→新エンジン）の実行自体はスコープ外のまま**——本番へ直接アクセスできないこの環境からは、実データが入っているか自体も判定できない。移行ツールは実装・検証用Postgresでの動作確認済みで、設定＞連携＞過去実績の移行（system_admin限定）からいつでも本番アプリ上で実行できる状態のまま変わっていない。CLAUDE.md・README・`docs/v4-plan.md`「用語」節・`client-awards/CLAUDE.md`・`docs/design/v4/graphics-redesign.md`（§11に段F実装メモを追加）・`docs/design/v4/graphics-awards-migration-plan.md`・`client-techops/CLAUDE.md`のリアルタイムCG関連の記述を「廃止」に揃えた。検証: `npx tsc --noEmit -p server`、`npm run typecheck:all`、`npm run lint`（0 errors）、`shared`のVitest 2112件に加え、`verify:up`のPostgresに実サーバーを起動して`/api/v1/internal/awards/events`が404（APIルート撤去を確認）、`/api/v1/internal/graphics/{projects,awards-migration/events}`は`v-admin`で200（テロップCG・移行ツールとも無傷）、`/awards/`は**文字通りのHTTP 404ではなく**案件管理アプリのSPAシェルへフォールバック（200）後にクライアント側ルーターが`/`へ即座に戻すこと（`check-links.mjs`が想定する「黙ってホームに戻る壊れリンク」どおり）を実測した。**未検証**: `npm run build`（フルビルド）、実ブラウザでの他画面（権限管理・データビューア等）への影響確認。
