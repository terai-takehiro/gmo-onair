**PR #647（制作技術支援の UI 統一）で受けたレビュー指摘を棚卸し表に移した**。マージすると指摘は GitHub の画面から消えるため、`docs/reviews/codex-findings-v4.md` に記録する決めごとに従ったもの。①Codex の指摘6件（すべて P2・すべて修正済み）を表に足した。内訳は「足したばかりの検査の範囲がコメントの説明と食い違っていた」「大きさの列挙では手書きの見出しを拾えなかった」「コメント判定の窓が狭く説明文を違反にした」「`PageHeader.sub` に説明文を入れて 375px で読めなくなった（この PR が入れた劣化）」「幅トークンの列挙では `PageShell` を使う決まりを強制できていなかった」「`PageShell` の検出がコメント・文字列の言及でも通った」で、**6件のうち5件は今回追加した検査スクリプト自体の欠陥**だった。②あわせて、直さないと決めたもの・利用者の判断待ちのもの4件（検査の原理的な限界／`page-shell-missing` を他3アプリへ広げるか／`shared` の入力部品の文字サイズ／`verify:ui` に案件 id の要る画面を並べること）も表の行として残した。③**最後の1コミット `8614ebe` が Codex の利用上限でレビューされないままマージされた**ことを「レビューが0件のままマージされた PR」の節に記録した（この PR では指摘を直したコミットに次の指摘が付くことが6回中6回起きており、その位置が未レビューであるため）。
