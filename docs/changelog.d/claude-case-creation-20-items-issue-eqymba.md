**トップページ「お待たせ中」の「残り○件を見る」が、実際には出てこない件数を言っていたのを直した**（ユーザー指摘）。`inboxAllHrefOf` は権限の優先順位（案件作成→入ってきた情報→受け取った書類）だけで1つの行き先を選び、隠れている件数をそのまま「◯件」と出していたが、隠れている中身は期限超過・見積請求などが混ざっており、押した先の案件作成にはネタ案件・問い合わせしか並ばない（`INTAKE_KINDS`）ため、常に実数より多く言っていた（画面の例: 「案件作成で残り20件を見る」と出るが、押した先には並ばない行がほとんど）。件数を言わない汎用の行き先を選ぶ`inboxAllHrefOf`とは別に、**押した先に実際に並ぶ件数だけを行き先ごとに数える**`inboxRestLinksOf`を新設し、`TaskHubCard.tsx`の「残りを見る」をこちらに差し替えた。期限超過は個別の案件のやり取りにしか行き先が無く束ねて見る画面が無いため、この行き先には出さない（行き先の無い一覧は出さない、という既存の判断と同じ）。**さらにユーザーから2点指摘があり追加で直した**: ①ネタ案件・問い合わせの行き先は案件作成の画面まるごとではなく、その上に載る「自動で届いたもの」レール（`IntakeRail.tsx`）にしか並ばないため、札を「案件作成」から実際の見出しと同じ「自動で届いたもの」に直した。②行き先は状況によって「案件作成」固定ではない——`intake`（`sales` の editor）を持たない `dailyops` だけの人には、問い合わせの行き先は「入ってきた情報」になる（`inboxHrefOf` と同じ優先順位）。この分岐は元から実装していたが、①の文言修正と合わせて再確認した。

**「他の人が処理したものも各個人に残っている」というご指摘を受け、期限超過の次回アクションが片づけられない原因を直した。** 「お待たせ中」の期限超過を押すと案件の「やり取り」タブ（`/sales/projects/:id/thread`）に送っているのに、**そのタブ自体には完了・延期のボタンが無く**、片づける手段は別画面（営業活動記録・お客様詳細）にしか無かった。さらに**新しいやり取りを書いても古い次回アクションは自動では「済み」にならない**（別の行として積み上がる）ため、担当者が電話・メールで実際に対応していても、システム上はその古い次回アクションだけが期限超過のまま残り、「お待たせ中」に表示され続けていた（誰の目にも「処理したのに残っている」ように見える）。既存の完了・延期の口（`useNextActionActions.ts` — 営業活動記録・顧客360°ビューと共通）をやり取りタブ（`ThreadCard.tsx`）にも配線し、その場で片づけられるようにした。次回アクションの表示部分は行数の都合（400行上限）で `NextAction.tsx` に切り出した。

検証: `npm run typecheck`・`npm run test`（1461件）・`npm run lint` を確認済み。実DBでの実機確認・実際のOAuthフローでの権限確認はこのセッションから行っていない。
