#521（レギュラー案件のロジックとモック）のレビュー棚卸しを記録したうえで、そこに書いた P1（実害）を直した。

**棚卸し**: #521 はレビュー0件だったので、意図して残した判断と棚卸し中に見つけた穴を表に書き出した。⚠️ **書いている最中に、書いた当の実害が本番で起きていた** — `GLS-A007「インテリジェンス」` が 04:05:33 に受注済＋レギュラーへ設定され、**その11分後の 04:16:46 に日次ジョブ（`updated_by: system:project_tidy`）が `s_completed` へ落としていた**（本番のレコードで観測）。

**実装**（`docs/design/v4/regular-series.md` §10 の1〜4番目）: ①**日次ジョブが `recurrence` を見ていなかった実害を直した** — 完了の条件を「残っている回が無い」に変え、回0件のレギュラーは絶対に対象にしないようにした。②`episode_number` の採番をアトミック化し（`ON CONFLICT ... RETURNING`）、`(project_id, episode_number)` の重複を防ぐ部分UNIQUE索引を足した（migration 260。既存に重複があってもデプロイは止めない設計）。③受注（`a_won`）にしたレギュラー案件は第1回を1件だけ自動で作るようにした（冪等・まとめて全回は作らない）。④回UIの出し分けを `gls_category==='A'` から `recurrence==='regular'` に切り替えた。

検証: `npm run typecheck` 全ワークスペース緑、`npm run test` 1831件緑、`npm run lint` 緑（eslint 58 warning は全て無関係な既存ファイル）。実 Postgres で `changeStage()` を通しで呼び、GLS発番→第1回自動作成→冪等性→日次ジョブが正しく完了させないことまで統合確認した。
