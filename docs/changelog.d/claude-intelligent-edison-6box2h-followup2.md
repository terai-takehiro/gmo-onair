**Wiki で実際にページを書けるようにした（段B）。** 段A は読むだけだったので、①**サーバーの書き込み口**（ページの作成・保存・公開・移動・削除・複製、画像の受け口、編集ロック4本）、②**編集画面 `/wiki/p/:id/edit`**、③**ツリーからの操作**（`+` で子ページを追加・ドラッグで並べ替えと親の付け替え・「…」から上へ／下へ／別のページの下へ）を足した。編集画面は **素の `<textarea>` ＋ 右にプレビュー**で、Milkdown や Tiptap のような WYSIWYG は入れていない（ご指示「手入力の使いやすさを最優先」。本文の正が Markdown の文字列である以上、往復変換で黙って本文が変わる危険を持ち込まない。スマホの contenteditable ＋ 日本語 IME が壊れやすいことも避けた）。ツールバーは6つ（見出し・箇条書き・チェックリスト・表・画像・リンク）で、**押すとカーソルの位置に記法を差し込む**。それ以外のブロックは「その他のブロック」と `/` から選ぶ。**自動保存は打つのを止めて1.5秒**で、変わった項目だけを送る。日本語の変換中は保存の待ちを進めず、**保存の応答で本文の値を書き戻さない**（書き戻すと変換中の文字が消え、カーソルが文末へ飛ぶ。1.5秒おきに起きるので日本語では実質入力できなくなる）。他の人が先に保存していたら 409 を受けて**黙って上書きし直さず**、打った文字を画面に残したまま読み込み直しを促す。編集ロックは運営マニュアルと同じ規則（開いたら取る・60秒ごとに延ばす・10分手が止まれば空き・manager は引き継げる・読むだけの人は「編集を代わってほしい」と申し出られる）で、**手が止まっているあいだはハートビートを送らない**（送り続けると開いたタブが他の人を永久に締め出す）。

**作っていない機能のボタンを編集画面に出していたのを取り下げた**（段Bの取り込み時に見つけた）。ツールバーとスマホのツールバーに「AI で整える」があり、押すと「準備中です」と出るだけだった。これは `client-wiki/CLAUDE.md` の「作っていない画面への入口は出さない（押せるのに何も出ない項目は、利用者には『壊れている』としか見えない）」に反している。PC のツールバーからは外し、スマホのツールバー5つめは「その他」（ブロックの一覧）に差し替えた。設計書 §6-⑨ のスマホ5つめは「AI で整える」のままなので、**段E で差し替える**ことを設計書と `client-wiki/CLAUDE.md` の両方に書き添えた。

検証: `npm run typecheck:all`（0 errors）・`npm run lint`（0 errors / 54 warnings＝着手前と同じ）・`npm run test`（94件）・`npx vitest run --root shared`（2,548件）・`npm run build -w client-wiki`。実ブラウザ（Chromium・検証用 Postgres・`x-user-id` を変えて複数の利用者）で、編集画面に本文が出て**日本語が打てる**こと、ツールバー6つがカーソルの位置に記法を差し込むこと、打つのを止めて1.5秒で保存されヘッダーに保存時刻が出て**再読み込みしても残る**こと、「公開する」で下書き→公開に変わること、**別の利用者が開くと読み取り専用になり「編集を代わってほしい」が出て、申し出が保持者に伝わる**こと、ツリーの `+` でページが増え**ドラッグの並べ替えが再読み込み後も残る**こと、写真を選ぶと縮小して上げ `![…](…)` が差し込まれ取り出せることを確認。1440px と 375px で**横はみ出し 0・JS エラー 0**、スマホのツールバーは5つ・タップ領域 44px、**ナビの列は1本のまま**（画面の中に2本目のナビも2つ目の `☰` も無い）ことを実測。
