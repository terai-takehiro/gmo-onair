- **PR #595（メール自動仕分け・受領書類のひとつづり）のレビュー棚卸しを記録し、レビューが届かなかったぶんを自分で読み直した**（決めごと: [docs/branching.md](docs/branching.md) 「マージしたら、その PR のレビューを棚卸しに移す」）。Codex が4巡し **19スレッド・P1 12件（うちセキュリティ High 1件）／P2 7件**、すべて直してからマージした。全件を [docs/reviews/codex-findings-v4.md](docs/reviews/codex-findings-v4.md) の表に書き出している（`npm run reviews:debt` はこの環境のトークンでは 401 なので GitHub API から直接引いた）。⚠️ **4巡目のあと Codex は usage limits に達し、直しを載せた最後の2コミット（`5482aec` / `05d00b4`）はレビューを受けていない**ため自分で読み直し、3件直した。①**後から届いた書類の処理月・支払サイトで束の空きを埋める** — 束は最初の書類（多くは見積書）で作られるが、処理月とサイトは請求書に書いてあることのほうが多く、埋めないと束は最後まで期日を出せず「期日なし」として一番後ろに沈んだままだった（4巡目の P1 が半分残っていた）。すでに入っている値は上書きしない（人が直した値かもしれないため）。②**束を消すときの確認を取引の中に移し `FOR UPDATE` で行を押さえた** — 取引の外で数えると、数えたあとに台帳へ渡した書類が消され、仕入・販管費の行だけが宙に浮く（渡す側は取引の中で行を押さえるので、こちらも押さえないと擦れ違える）。③**詰め物なしの base64 を落とさないようにした** — 厳しくしすぎて正しい原本を「読めません」で落とすのは、ゴミを通すのと同じくらい困る。詰め物はサーバーが足し、余り1文字（base64 としてありえない）だけ弾く。base64 の試験は**実際に関数を通す形**にした（詰め物なし・折り返し・URL用は通り、`not base64`・途中で切れた・余り1・日本語は落ちる）。検証: `typecheck:all`、`npm run build`、`shared` の Vitest 2099件、`npm run lint`（0 errors）。
