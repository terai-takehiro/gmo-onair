**旧モックの画面文字に残っていた比喩語・禁止語を画面の語に置き換えた**（`docs/wording.md` ルール11・2026-09-22 のご指摘「謎表現の抹殺」の続き）。
①設計書の比喩語（道具・決めごと・棚・帯・手つき）と禁止語（見るだけ・MCP）が、旧モック11ファイル（テロップCG・運営マニュアル・会場図面・隔週キープ）の画面文字に残っていた。モックは実装の前に作るものなので、残しておくと同じ表現がまた実装に写る。
②ルール11の対応表どおりに置き換えた（道具→アプリ／機能、決めごと→設定／ルール、見るだけ→閲覧のみ／閲覧専用、手つき→操作、の帯→上部／ツールバー、棚→ロッカー、MCP→AI 連携）。実装側で先に直した語（テロップCG設定の「設定」・会場図面の「ツールバー」）と同じ語にそろえ、キャンバスの画面名（`canvas.json`）と各フォルダの README の表も合わせた。
③新しい検査 `scripts/check-mock-wording.mjs`（`npm run lint` の1つ）の記録（`BASELINE`）を `--update` で空にし、これ以降はモックに1件でも出たら止まるようにした。公開済みの4つのキャンバスも同じ URL へ公開し直した。
検証: `node scripts/check-mock-wording.mjs`（50 ファイル・0 件）・`npm run lint`・手元の Chromium（Playwright）で直した `.dc.html` を描画し、`{{ }}` の穴と `<sc-if>`／`<sc-for>` が壊れていないことと、新しい文言が画面に出ることを確認。
