**案件分類が「詳細では登録済み・案件を直すでは未登録」に見えていたのを直した**（ご指摘: 「すでに案件分類を登録していても案件を直すを開くと、未登録状態になる」）。
①⚠️ **直す画面が値を戻していたのではなく、2段分類の列が最初から空でした。** 案件分類は**2つの持ち方が併存**しています（migration 182）— 旧 `project_type`（1段・7種）と `audience` / `project_category`（2段）。決めごとは「**書くのはサーバーの1か所**（`project-classification.ts` が導く）」ですが、**通らない口が残っていました**: **Excel 取込**（`excel.routes.ts` の INSERT / UPDATE）と**検証環境のシード**が旧種類だけを書いていたので、そこから入った案件は2段が空のまま残ります。すると**案件詳細の概要タブは旧種類を「案件分類」として出す**ので登録済みに見えるのに、**案件を直す画面は2段しか見ない**ので「選ぶ」＝未登録に見えます。⚠️ **エラーは出ず、画面を突き合わせた人しか気づけません**（型検査も lint も SQL の中身を見ません）。
②**旧種類が「その他」のときは、概要タブでも案件分類として出さないようにしました**（判断は `projectDetail/classificationText.ts` の1か所に切り出しました）。 `projects.project_type` の**既定値が `other`** なので、**AI が起こしたネタ・決算取込・投入口からできた案件は、何も選んでいなくても「その他」と書かれます** — つまり**この画面だけが「案件分類 ＝ その他」＝ 登録済みのように見えて**いました。決めていないものは空欄（—）にして、直す画面と同じことを言います。⚠️ **GLS-B（GMO案件・コンサル）は今までどおり出します** — 工事・構築のプロジェクトは2段を持たない決めごとなので、**そちらでは旧種類が案件分類そのもの**です。
③**すでに空で入ってしまった案件を埋め戻します**（migration 193）。182 の UPDATE は**その時点の行しか埋めない**ので、**あとから Excel 取込で入った案件は空のまま**でした。182 と同じ対応表・同じ守り方で、**2段が両方とも空の行だけ**・**旧種類が4種のときだけ**埋めます（`other` は機械では配信か収録か決められないので**空のまま**にして、案件台帳の「案件分類が入っていない」に残します）。⚠️ **182 に無かった守りを1つ足しました** — **GLS-B の行には入れません**（発番前の A→B 切替は `project_type` を書き換えないので、**B の行に `recording` が残っている**ことが実際にあります）。⚠️ **来場人数は落としません** — 機械的に消すと「150 名と聞いた」という人が入れた情報が黙って消えるので、食い違いは案件台帳の「無観客なのに来場人数が入っている」が数えて人に見せます。
④**Excel 取込は3列そろえて書くようにしました。** ⚠️ **更新のときは、種別を変えていなければ人が入れた2段をそのまま残します** — 旧種類は4種しかないので毎回導き直すと**「有観客の収録（公開収録）」が「有観客の配信」に化けます**（`hybrid_event` に寄せてあるものを逆に引くと配信になる）。Excel の「案件種別」を書き換えたときだけ導き直します。シードと投入口のネタ案件も**空を明示して書く**形にしました（列ごと書かないと「2段を持たないと決めた」のか「書き忘れた」のかが読んでも分からず、そこが今回の入口でした）。
⑤⚠️ **案件台帳の「その場で直す」ポップアップは、直した案件の鍵を落としていませんでした。** 一括編集（`useLedgerState`）は `['project', id]` を落としていたのに、**その場で直す・貼り付け（`useLedgerGrid`）だけ漏れて**いました。`'projects'`（一覧）は `'project'`（1件）に**当たりません**（react-query は鍵の前方一致で落とすので別の文字列＝別の鍵）。落とさないと `staleTime` の 60 秒のあいだ**案件詳細と直す画面が古い姿を出す** ＝ **台帳で案件分類を入れた直後に直す画面を開くと未登録のまま**に見えます。⚠️ **エラーは出ず 60 秒待てば直るので、誰も報告しません。**
⑥**同じ形が戻らないように検査を2本足しました。** ①`shared/tests/projectClassificationWrites.test.ts` … `projects` に `project_type` を**書く** SQL（INSERT の列の並び / UPDATE の SET）は、**同じ文の中で `audience` と `project_category` も書いていること**を見ます（読むだけの SQL は対象外）。②`shared/tests/classificationText.test.ts` … 概要タブが**何を出して何を出さないか**を7項目で固定します。⚠️ **どちらも型でも lint でも見つからない形**です（SQL の中身は見ませんし、後者はどちらも「文字列を返す関数」に見えます）。**反証: ①は Excel 取込を直す前の書き方に戻すと落ち、②は `other` を出す形に戻すと落ちることを確かめてあります。**
⑦⚠️ **この PR のレビューで P1 を1件いただき、直しました**（Codex・#175）。`seed.ts` の `projSql`（複数の呼び出しが共有する INSERT）の列を 16 → 18 に増やしたとき、**ヨミ段階と失注の呼び出しだけを直し、GLS 発番済みの呼び出しを 16 個のまま残していました** — 新しい DB では**最初の GLS-A 案件で bind パラメータ不一致で落ち**、insert が取引にまとまっていないので**途中まで入った状態で止まり**、2回目以降は「ユーザーが居るのでスキップ」になるため**壊れたシードのまま検証環境が立ち上がります**。⚠️ **型検査も lint も通り、しかも CI はシードを1度も流しません**（`checks` / `build` のどちらにも無い）ので、**手元でシードを流す以外に気づく方法がありませんでした**。反証: 16 個のままだと `exec_bind_message` で落ちて `projects` は 14 行で止まり、直した版は完走して **GLS-A の 21 件すべてに2段分類が入り GLS-B の5件は NULL のまま**（実 Postgres で実測）。
⑧**PR を出したあとの見張りを手順にしました**（`.claude/skills/pr-watch`・ご要望）。CI 失敗の直し方（**「flaky」は診断ではない／テストを消して緑にしない／base が赤いときの言い方**）、レビュー指摘の3つの分岐、⚠️ **レビュー0件を「指摘なし」と読まない**（#154・#159・#165 の3本がその形）、マージ後の棚卸し、**マージ／クローズまで見張りを終わらせない**を1本にまとめ、落ち方ごとの読み方（`references/ci-jobs.md`）と**踏んだ落とし穴の記録**（`references/pitfalls.md` — 上の P1 が1件目）を添えています。⚠️ **CI が見ていない3つ**（凍結アプリの CSS・シード・実ブラウザ）も明記しました。
⑨**検証**: 実 Postgres ＋ 実 API で、埋め戻しの5通り（**人が入れた「有観客・収録」は上書きされない ／ 旧種類から埋まる ／ `other` は空のまま ／ GLS-B には入れない ／ GLS 分類が空の行は A 扱いで埋まる**）を実測。Excel 取込は**実 xlsx を実ルートに投げて** 3通り（**新規は2段が入る ／ 種別を変えていない更新は人の2段を保つ ／ 種別を変えた更新は導き直す**）を実測。`GET /projects/:id`（直す画面が読む口）が埋め戻した案件の2段を返すことも実測。`npm run test` 1,115項目、`typecheck` exit 0、`lint` 0 errors / 63 warnings、`build:all` ＋ `check:frozen` OK。
⑩**`shared/src/` は1ファイルも触っていません**（足したのは `shared/tests/` の2本だけ。`check:frozen` で**凍結4アプリの CSS が基準どおり**であることを実測）。**DB マイグレーションを1件含みます**（193）。
