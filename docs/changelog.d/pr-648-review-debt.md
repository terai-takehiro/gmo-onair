**PR #648（マージ後に届いた指摘3件の修正＋#644 の棚卸し）のレビュー指摘を棚卸しに移した**（[docs/branching.md](../branching.md)「マージしたら、その PR のレビューを棚卸しに移す」）。Codex Code Review は4巡走って2件（**P1**「❌ の項目を文章の段落で書いていたため、残数を数える `grep -c '^| .* | ❌'` が拾わず次の棚卸しで静かに消える状態だった。#624 で一度直した形の繰り返し」／**P2**「その直しで『未対応に戻したら `accepted` に戻す』を常に書いていたため、未返答・辞退・相談の依頼に `is_completed:false` を投げるだけで `accepted` に変わる穴を作っていた」）を返し、**どちらもマージ前に修正・返信・スレッド解決済み**なので表に移す未対応の指摘は無い。実測での前後（`grep` の数 26→27、5状態それぞれの遷移）を記録した。あわせて①🔒 Security Review が最初のコミット（ドキュメントのみ）でだけ回り、**そのあとのサーバー変更には再実行されていない**ことを正直に残し、「次に権限や入力の受け取りを触るときは `@codex security review` を明示的に投げる」と書いた。②**この回も手動依頼のレビューが走っている最中にマージされた**（今回は指摘が無く取りこぼしは起きなかったが #644 と同じ形）ことを記録した。検証: `node scripts/check-md-links.mjs`・`npm run lint` 通過。
