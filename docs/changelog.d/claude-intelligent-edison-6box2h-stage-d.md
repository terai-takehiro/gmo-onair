**Wiki に検索・お気に入り・`.md` の出入口を足した（段D）。** 段C までは「書いて並べる」だけで、**貯めた文章を探す手段が無かった**（設計 §9 は段D を段E の土台と決めている）。①**検索の API** `GET /wiki/search`（語・スペース・タグ・担当・更新日で絞る）、②**検索の画面** `/search`（左に絞り込み・右に当たり。当たった見出しと前後の文を抜き出して**語を太字で示す**）、③**どの画面からでも開く小窓**（`Ctrl`／`⌘`＋`K`。語を入れる前は「最近見たもの」が並ぶ）、④**お気に入り**（`POST`／`DELETE /wiki/pages/:id/favorite`。ホームの区画は段A から在り、押して入れられるようになった）、⑤**`.md` の口**（`GET /wiki/pages/:id.md` は YAML の見出し付きで1枚、`GET /wiki/export?space=` はスペースまるごと zip、`POST /wiki/import` は Obsidian・Notion・ONAiR の書き出しを取り込む）を足した。⑤は設計 §5-2 の**約束3「外から読めて書ける」**の REST 側2本で、MCP（段E）は同じ組み立てを呼ぶ。

**検索は拡張を1本も入れずに始めた**（判断6）。`title`／`headings`／`body_md` に `ILIKE` を当て、**点数付けは Node 側**（`wiki-search-score.ts`）に置いた — 重みを直すたびに migration を書かずに済む。遅くなったら `pg_trgm` の GIN 索引を足す。点数の付け方は `shared/src/wiki/search.ts` と**同じ答えを出す複製**で、一致は `shared/tests/wikiSearchParity.test.ts` が固定している（サーバーは `shared` を import できないため。段C の `isValidPropValue` と同じ作法）。本文ごと持ってくるので候補は**更新の新しい順に300件**で止める。⚠️ **読めないスペースのページも、下書きも検索に出さない**（§8「存在ごと見えない」・§7-5）。出すのは `status='published'` だけ。

**「AI に聞く」の入口は、まだ出していない。** 検索の画面にも小窓にも置いていない — 画面は段E なので、いま出すと**押せるのに何も出ないボタン**になる（§9・段A から続けている決めごと）。

検証: `npm run typecheck:all`（0 errors）・`npm run lint`（0 errors・warning は着手前と同じ）・`npm run test`・`npx vitest run --root shared`・`npm run build -w client-wiki`。実ブラウザ（Chromium・検証用 Postgres・`x-user-id` を変えて3人）で、語を打つと当たりが出て**太字で示される**こと、**語を消した瞬間に案内へ戻る**こと（待つと消したのに前の結果が残って見える）、件数と絞り込みの列が**結果が届いてから**出ること（0件と「まだ届いていない」は別のこと）、`Ctrl`＋`K` がどの画面からでも開くこと、お気に入りが入れ外しできホームに出ること、`/pages/:id.md` が YAML の見出し付きで返ること、スペースの zip を**書き出して取り込み直すと同じ形になる**ことを確認。⚠️ **読めないスペースを持つ人で検索すると、そのスペースのページが1件も出ない**こと、**他人の下書きが検索に出ない**ことを実際に確かめた。1440px と 375px で横はみ出し 0・JS エラー 0、**ナビの列は1本のまま**。

**取り込みで、中身の無いフォルダがページになっていたのを直した。** 画像だけを入れた `files/`（書き出しが必ず作る）がそのまま「files」という空のページになり、書き出し→取り込みを繰り返すたびに1枚ずつ増えていた（検証で見つけた）。`.md` も `.csv` も子も無いフォルダはページにしない。

**実ブラウザで見つけて直したものが3つ。** ①**抜粋に Markdown の印がそのまま出ていた** — `![配置図](/api/v1/internal/wiki/files/wf-ba3a21bb)` の URL だけで1行の半分が埋まり、`> [!CAUTION]`・`| --- |`・`**` も並んでいて、当たった語の前後が読めなかった。抜粋を切り出す前に印だけを落とす `toPlainText` を足した（画像の代替文・リンクの文字・引用の中身は**残す**。下線 `snake_case` も消さない）。これも `shared` とサーバーの2か所にあるので、`shared/tests/wikiSearchParity.test.ts` に突き合わせと「印が残らない・文字が消えない」の試験を足した。②**スマホで同じ一文が2つ縦に並んでいた** — 「公開されているページだけが出ます（下書きは出ません）」が、絞り込みバーの添え書きと下の案内の両方に出ていた（検索したあとは件数の行にも出る）ので、添え書きをやめた。③**`MobileFilterBar` の「検索語を消去」（×）が入力欄の外・左下に出ていた**（375px で実測）— `shared/src/client/tokens-v4.css` の `:root .v4-tap { position: relative }` が Tailwind の `.absolute`（詳細度が低い）に勝ってしまい、絶対配置のつもりの札が普通に流れていた。`:not(.absolute):not(.fixed):not(.sticky)` を付けて、**すでに位置を決めてある要素には当てない**ようにした（`absolute` でも `::after` の基準になるので 44px の当たり判定はそのまま）。⚠️ **これは `shared` なので全アプリに効く**が、`v4-tap` と `absolute` を並べているのはこの1か所だけで、ほかの35か所は今までどおり。

**`npm run verify:ui` の見る先に Wiki の3画面を足し、そこで見つかった2つを直した。** 実測の対象は段A のときの3画面（ホーム・スペース2つ）で止まっていたので、テンプレート・検索・書き出しと取り込みを足した。そうしたら**スペースの画面が落ちた**——ツリーの行が 34px、行の `⋯` が 28px で、寸法表の段（32/36/40/44/48）から外れていた（段A から外れたままで、実測の対象に入っていなかったので誰も気づいていなかった）。行を 36px・`⋯` を 32px にして段に戻し、**なぜ 36px なのか**を `WikiTree.tsx` に書いた。あわせて**潰れ検知が `sr-only` を数えていた**のを直した——Tailwind の `sr-only` は 1px の箱に押し込んで `clip` で隠す作りなので上の条件に必ず当たり、**絵柄のボタンに名前を付けた画面が全部「崩れている」と出て本物の崩れが埋もれて**いた（この検査を「0件必須」に格上げできない理由になっていた）。検査の結果は **Wiki の 182/182 通過（6ページ×3画面幅）・全体で潰れ検知 0件**（直す前は3画面で5件出ていた）。全体の通し（106ページ）は 3,244/3,245 で、残る1件は `計時LIVE 設定` の「シェルが画面いっぱい」——**2つの検査を同時に走らせた時だけ出る描画待ちの取りこぼし**で、その画面だけ単独で回すと 61/61 通過する（この PR は `client-live` を触っていない）。

**Codex のレビュー指摘5件（P1×3・P2×2）を直した。** ①**取り込んだ画像が誰でも取れていた**（P1）——取り込みは画像を `page_id` を空のまま保存し、「あとでページに付ける」と書いてあるのに付けるコードが無かった。`GET /wiki/files/:id` は `page_id` がある画像だけ権限を見るので、**限定のスペースへ取り込んだ画像の URL を知っていれば誰でも中身を取れた**。本文が指している画像をそのページに付け直し、あわせて**ページに付いていない画像は上げた本人だけ**に見えるようにした（編集画面の1枚も同じ穴だった）。②**CSV の行が取り込みの上限をすり抜けていた**（P1）——上限（1,000件）の判定はフォルダとファイルを数えるだけで、**あとで1行＝1ページになる CSV は「1件」**にしかならず、10万行の CSV を1枚入れた小さな zip が素通りして DB に大量の書き込みを流せた。CSV を先に読んで行数を上限に足し、読んだ結果は取り込み本体でも使い回す。③**同じ題の行が3行になり本文が入れ違っていた**（P1）——題を鍵に `.md` を1枚だけ覚えていたため、同じ題の行が2つあると2行目が1行目の本文を写し、使われなかった `.md` が余分な3行目になっていた。題ごとに待ち行列を持ち、1行につき1枚ずつ取り出す。④**検索のスペース絞り込みが上限の後に効いていた**（P2）——サーバーには渡さず画面で当て直していたので、当たりが上限（50件）を超えると**下位のスペースのページがそもそも届かず**、絞り込んでも出ない・件数が 0件 に見えた。サーバーが上限で切る前に絞り、件数は**スペースの絞り込みを外して・上限で切る前に**別途数えて返す（`{ hits, counts }`）。⑤**YAML の配列を引用符ごと無視してコンマで割っていた**（P2）——`tags: ["R&D, Japan"]` が2つのタグになり、書き出しも同じインラインの形なので**往復するたびにタグが増えて**いた。引用符を見て区切るようにし、書き出す側もコンマを含む値は必ず引用する（`shared` とサーバーの両方＋突き合わせ試験）。

検証（検証用 Postgres に実際に叩いた）: 限定のスペースへ画像つき zip を取り込むと `wiki_files.page_id` が付き、**読めない利用者には 404**（v-admin 200 ／ v-sales・v-keiri 404 ／ v-none 403）。`page_id` を空に戻すと**上げた本人だけ 200**。1,001行の CSV は「1000件を超えています」で止まる。同じ題の行が2つある Notion 形式の zip は**2行**になり本文が入れ違わない。`limit=1` でも下位のスペースを選べばそのページが出る。`R&D, Japan` のタグは書き出し→取り込みの往復で1つのまま。

**書き出しで、公開のページが黙って消えることがあったのも直した。** 下書きの親の下に公開の子があると（親を下書きのまま作って子だけ公開した形）、親は書き出しの対象外なので子の `parent_id` がどこにも無い id になり、**置き場が見つからないまま zip に1本も書かれないのに README の件数には入る**（＝消えたことに気づけない）状態だった。親が書き出しに入っていないページは根に置く。検証用 Postgres で下書きの親＋公開の子を作って、zip に `公開の子.md` が入ることを確認した。

**Codex のレビュー2巡目の指摘6件（P1×4・P2×2）も直した。** ①**書き出しが、読めない画像を zip に入れていた**（P1）——本文のリンクは誰でも書けるので、読めるスペースの本文に読めないスペースの画像の URL を貼れば、書き出しで中身を取り出せた。口（`GET /wiki/files/:id`）と同じ判定（ページに付いていれば `canReadPage`、付いていなければ上げた本人だけ）を書き出しにも通し、通らない画像は黙って飛ばす。②**データベースの行の下のページが書き出しから消えていた**（P1）——行はページなので子を持てるのに、行を1枚書いて終わりにしていた。孫から下も書くようにした。③**取り込みの上限が、行の `.md` を二重に数えていた**（P2。①の修正が招いたもの）——Notion は CSV と行の `.md` の両方を出すので、600行の台帳が 1,201件 と数えられ、実際に作るのは 601件 なのに止まっていた。`create` と同じ手順で数える `countPlanned` にした。④**タグの絞り込みがコンマで割れていた**（P2）——`R&D, Japan` のタグを持つページが1件も当たらなかった。サーバーはコンマで切らず、画面は同じ名前の引数を並べて送る。⑤**`true` や数字に見える文字が往復で消えていた**（P1）——引用しないまま書き出すと読み直しで真偽値・数値になり、文字の項目は `sanitizeProps` が型違いとして黙って落としていた。型に化ける文字は必ず引用する。⑥**zip 爆弾を止められなかった**（P1）——multer の 50MB は圧縮後の大きさにしか効かず、解いた中身は上限を見る前にメモリへ載っていた。解いた大きさを数えながら読む `ZipReader`（1つ 20MB・全体 300MB）を足した。

検証（検証用 Postgres）: 限定スペースの画像の URL を全社のページに貼って書き出すと、**v-admin の zip には入り v-sales の zip には入らない**。データベースの行の子が `検査用DB/行A/行Aの子.md` として入る。600行＋600枚の `.md` の zip が **601ページで通る**（直す前は「1000件を超えています」）。`R&D, Japan` のタグで1件当たる。`true`・`123` の文字が引用されて往復で残る。57KB で解くと 60MB の zip は「中身が大きすぎます」で止まる。

**Codex のレビュー3巡目の指摘3件（P1×2・P2×1）も直した。** ①**根に「README」という題のページがあると、その中身が消えていた**（P1）——`README.md` に書いたあと、案内の README を同じ名前で書いて上書きしていた（JSZip は後勝ち）。件数には入るのに中身が無くなるので、取り込み直すと本文が（子が無ければページごと）失われる。`README` を先に押さえ、ページ側は `README (wp-xxxx).md` に逃がす。②**取り込みが、データベースの行の下のページを作っていなかった**（P1。書き出し側を直したことで表に出た穴）——書き出しは `行の名前/` の下に書くのに、取り込みは行を1枚作って終わりだったため、往復すると行の下が丸ごと消えていた。行を作ったあと子孫も作り、数え方（`countPlanned`）も合わせた。③**スペースを選んだときの件数が、上限で切ったあとの数だった**（P2）——当たりが多いスペースを選ぶといつも「50件」と出ていた。サーバーが数えた `counts` を出す。

検証（検証用 Postgres）: 根に「README」という題のページを置いて書き出すと **`README (wp-readme).md` として残り**、案内の `README.md` は別に入る。データベース→行→その子の3階層を書き出して取り込み直すと **`往復DB → 行X → 行Xの子` がそのまま戻る**（直す前は孫が消えた）。実ブラウザで「機材」を選ぶと件数が **6件→4件**（サーバーの `counts` と一致・並ぶ行も4件）。

**Codex のレビュー4巡目の指摘4件（P1×3・P2×1）も直した。** ①**解いた大きさの見張りが、止めると決めたあとも解き続けていた**（P1。2巡目に足した `ZipReader` の穴）——`reject` を呼んだだけでは jszip は流れ続け、`data` のたびに溜め込むので、守ったつもりで守れていなかった。溜めた分を捨て、流れを止め、以後の `data` も無視する。②**データベースの下のデータベースが「行」として書き出されていた**（P1）——項目とビューの定義が1行も書かれず、取り込み直すとただのページに化けていた。フォルダとして書き直す。③**取り込みが、フォルダだけの子を飛ばしていた**（P1）——入れ子のデータベースは `_database.md` を持つフォルダで兄弟の `.md` が無いため、`mdPath` が無いという理由で丸ごと消えていた。④**小窓（`Ctrl`＋`K`）で、打ち替えてすぐ Enter を押すと前の語の1件目が開いた**（P2）——250ms の待ちの中では `q` がまだ動かないので、react-query が「置き換え中」にならない。入力欄の字と投げた語がそろうまでは0件にする。

検証: 120KB で解くと 120MB の zip を読ませると止まり、**heap の増分は 0.1MB**（止めたあとも積んでいれば 120MB 近く出る）。`外側DB > 内側DB > 内側の行` を書き出すと `外側DB/内側DB/_database.md` が入り、取り込み直すと **`kind=database` と項目の定義（各1項目）がそのまま戻る**（`databases: 2`）。実ブラウザで、小窓に「機材」を出したあと「ログイン」に打ち替えて 80ms で Enter を押しても、**前の語の行は残らず・どこにも飛ばない**。

**Codex のレビュー5巡目の指摘4件（P1×4）も直した。** ①**データベースの行の見直し予定が往復で消えていた**（P1）——`.md` の見出しには書いてあるのに、行を作るときだけ `review_by` を渡していなかった。行にもふつうのページと同じく渡す。②**同じ題の Notion のフォルダが2つあると、2ページが3ページになっていた**（P1）——Notion は同じ題を32桁の id で区別するのに、id を落とした題で `.md` を探していたため、**2つのフォルダがどちらも1つめの `.md` を掴み**、余った `.md` が別ページとして増えていた。まず id つきの全体の名前で組ませ、掴んだものは他のフォルダが使えないようにした。③**読めなくなった下書きがお気に入りに残り続けていた**（P1）——入れた時点では読めても、担当が変わる・区分が下がると読めなくなる。ホームはスペースの所属しか見ていなかったので、**開くと 404 になるページの題と担当が並び続けて**いた。`canReadPage` と同じ下書きの決まりを当てる。④**大きすぎて止めた画像のエラーを握りつぶしていた**（P1。4巡目の修正の続き）——申告を偽った画像を何枚も並べるだけで1枚あたり上限いっぱいまで解かせ続けられ、zip 全体の budget をすり抜けられた。`ValidationError` は投げ直し、**失敗した読み出しも解いた分を全体の勘定に入れる**。

検証（検証用 Postgres）: 行に `2027-03-31` を入れて往復すると**そのまま戻る**。同じ題の Notion のフォルダ2つを取り込むと **2ページ**（＋それぞれの子）になり、本文が入れ違わない（直す前は3ページ）。v-admin が書いた下書きをお気に入りに入れた v-sales のホームに**その行が出ず**、開くと 404（v-admin には出る）。申告を偽った 25MB×20枚の zip を読ませると、**20回とも止まり・数えた合計が 300MB に達して**全体の上限で打ち切られる（直す前は 0MB のままいくらでも解けた）。
