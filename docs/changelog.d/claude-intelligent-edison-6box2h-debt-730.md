**マージ済み PR #730（Wiki 段D）のレビュー状況を棚卸しに記録した。** マージすると指摘は GitHub の画面から消えるので、[docs/reviews/codex-findings-v4.md](../reviews/codex-findings-v4.md) に書かなければ存在ごと消える（`docs/branching.md` の決めごと）。#730 は **Code Review が6巡・24件（P1×18・P2×6）**でこの一覧でいちばん指摘が多く、全件を検証用 Postgres と実ブラウザで再現してから直して返信・解決済み（未解決スレッド0件）のため、**表に移す未対応の指摘は無い**。

記録には、次に同じところを触る人のために3つの注意を添えた。①**24件のうち3件は前の巡の修正が招いたもの**で、書き出しと取り込みは対なので片側だけ直すと往復が壊れる。②**権限が抜けていた2件は、どちらも「読めないはずの画像を配る経路」**で、同じ中身を配る経路が2つあると片方だけ塞いでも漏れる。③⚠️ **最後のコミット `a747f17f`（6巡目の修正）は Code Review が完了しないままマージされた** —— 7巡目が 17:56 に走り始めて 18:01 のマージで打ち切られており（Security Review も PR を開いた時点のコミットで完了）、**6巡目の修正に指摘が無かったという意味ではない**。段E・段F で同じファイルを触るときに改めてレビューを通す。

検証: `node scripts/check-md-links.mjs`（146文書・相対リンク889本）・`node scripts/check-changelog.mjs`。文書だけの変更でコードは触っていない。
