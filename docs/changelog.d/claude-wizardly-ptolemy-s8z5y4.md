**決算取込CLI（`import-kessan-dev.mjs`）が本体（`kessan-import.service.ts`）とロジックが長期間分岐し、`--commit` が必ず失敗する状態だったのを直した**（利用者指摘）。CLI は本体とほぼ同じ抽出・投入ロジックを丸ごと複製した構成で、本体側の改修（MoneyForward対応・仕訳帳対応・重複検出・表記ゆれ吸収した名寄せ・entity_code対応・GLS新番号形式対応 等）に追従できておらず、①`projects`へのINSERTが既に削除された`notes`列を指定（`kessan_marker`列に統合済み）②`entity_code`（2026年10月の事業再編でNOT NULL化）が4テーブルとも未指定③GLS番号抽出が旧形式`GLS\d+`のみで新形式`GLS-A004`・改番後の`SCS-/GSS-/GMO-`を拾えない④税区分判定が「不課税」「非課税」を区別せず全部`exempt`にまとめる⑤取引先・顧客の名寄せが完全一致のみで表記ゆれ（全角半角・㈱表記）を吸収せず重複会社を作りうる、の5点が乖離していた。二重管理をやめて再発を防ぐため、CLI側の抽出・投入ロジックは全廃し、Web UIの「決算インポート」からも呼ばれる本体`runKessanImport()`（ビルド済み`server/dist`を読む）を呼ぶだけの薄いラッパーに書き換えた。あわせて本体にはあってCLIには無かった`--skip-duplicates`も使えるようにした。

検証の過程で④「不課税」判定を本体に合わせて確認したところ、本体自体にも独立した不具合があると判明したためあわせて直した。`tax-category.service.ts`等5か所のコメントは「CHECK制約はmigration 156でnontaxを許可した」としていたが、**そのmigrationは実在しなかった**（欠番ではなく一度も作られていない）。revenues/purchases/sga_expensesのCHECK制約は今も`tax10`/`tax8`/`exempt`の3値のみで、`tax_category='nontax'`を書き込む経路は決算取込に限らずすべて即座に制約違反で失敗する状態だった。migration 302 で3テーブルのCHECK制約に`nontax`を追加し、誤ったmigration番号を参照していた5か所のコメントを migration 302 へ訂正した。

検証: 検証用Postgres（`npm run verify:up`）に対し新CLIラッパーで`--scope=all --create-masters --commit`を実行し、①`projects`へのINSERTが列エラーなく成功②`projects`/`revenues`/`purchases`/`sga_expenses`全行に`entity_code='GSS'`が入る③新旧両形式のGLS番号（`GLS-A004`・`SCS-0099`）を含む案件が作成される④同一GLS内の2件の仕入で「不課税」→`nontax`・「非課税」→`exempt`と区別して保存される⑤半角表記で事前登録した顧客に全角表記のCSV行が重複作成せず紐付く、の5点をDB直接確認した。dry-runの重複候補検出・再`--commit`の冪等性（マスタ新規作成0件）・引数エラー（`--file`省略／不正`--scope`／不正`--period`）・prodガードも実行して確認した。`npm run typecheck -w server`・`npx tsc -b client`・`npm run test`（shared + serverレビュー試験 94件）・`node scripts/check-migration-numbers.mjs` いずれも問題なし。
