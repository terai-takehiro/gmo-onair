**終了日が空の完了案件が「98_終了案件」へ永久に移らなかったのを直した**（ユーザー報告「GLS-A003 はすでに終了しています」「009、010、013 も終了していると思います」「プロジェクト案件も終了した案件をアーカイブしてください」）。⚠️ **今回は本番の実データで原因を確定している** — MCP で完了（`s_completed`）の案件9件を読み、`event_end` と突き合わせたところ**4件が空**だった（GLS-A002 / A003 / A009 / A010）。判定が `event_end` しか見ていないため、**完了していても永久に移りません**でした。⚠️ **プロジェクト管理（GLS-B）はもっと外れていた** — GPM は自分の終了日を `gpm_projects.ends_on` に持ち `projects.event_end` はほぼ空なので、実測でも完了4件のうち自動で移れたのは1件だけ（B004 / B009 / B010 が残り B006 だけ移った）。「終わった日」を**①`event_end` ②`event_start` ③`gpm_projects.ends_on`** の順に見る式（`DONE_DATE_SQL`）に変えた。⚠️ **3つとも空なら対象外のまま** — 日付が1つも無い案件を「終わった」と決める材料がこちらに無いため（推測で動かすと、まだ動いている案件のフォルダを片づけてしまう）。あわせて、**1回の押下で進む件数が少なすぎた**のも直した（本番で20秒の予算内に3件しか進まず、条件を満たす GLS-A013 / A018 が残った）。置き場（`98_終了案件`）は**両親に1つずつしかないのに案件ごと・側ごとに `ensureSubfolder` を呼んでいた**ので、1回だけ解決して使い回すようにした。検証: 実 Postgres で①終了日も開催日も空→対象外②終了日は空・開催日が過去→拾える③開催日が未来→対象外④開催日も空・GPMの終了日が過去→拾える⑤そのGPMを消す→対象外⑥終了日を入れる→拾える、の6通りを確認。⚠️ **実 DB がプレースホルダの数え違い（1つなのに2つ渡していた）を捕まえた** — テストとtypecheckは通っていた。`npm run typecheck:all` 緑、`npm run test` 1781件緑（この機能は61→63件）、`npm run lint` 緑。
