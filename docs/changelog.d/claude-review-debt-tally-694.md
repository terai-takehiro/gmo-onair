**PR #694のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」）。#694（会場図面の不具合9件の修正）はCode Reviewがpushのたびに13回中12回反応し、全15件（P1×1・P2×14）の指摘が付いた。P1は削除確認中に自動保存の通信が飛んでいると削除後も再送タイマーが生き残り消えた図面へPUTを送り続けるもの、P2×14は「グループはメンバー2人以上でしか存在せず、メンバー構成が変わるたびに`arrange`を消す」という不変条件を複製・部分再グループ化・個別移動・削除・Ctrl+Gの各経路に一貫させる過程で連鎖的に見つかったもの。全件を本PR内で修正・返信・スレッド解決した。表に移す未対応の指摘は無い。ただし最終コミットにはCode ReviewがCodexのusage limitsで実行されず、Security Reviewも最初のコミットに固定されたまま以後14回のpushには一度も再実行されなかった（既知の同型パターン）。検証: `node scripts/check-md-links.mjs`・`npm run lint`。
