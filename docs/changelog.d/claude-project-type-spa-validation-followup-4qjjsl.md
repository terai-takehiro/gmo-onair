**PR #575（SPA遷移でのフォーム初期化レース）で見つかった型のバグを、複数のサブエージェントで他の編集画面にも同種のものが無いか調査し、見つかった3件を直した**（`client`/`client-daily`/`client-equipment`/`client-techops`/`client-live` の全アプリを対象に調査）。①`client-techops/src/pages/live/LiveDisplayLayoutEditorPage.tsx`（表示レイアウト編集）は「reset完了か」を追跡する `initializedRef` を持ちながら、読み込みガードが `timerQuery.isLoading` だけを見ており、`['timer-layout', timerId]` を共有する「テンプレート」画面とのSPA往復で保存済みレイアウトが一瞬既定配置のまま描画される、PR #575と完全に同型の不具合だった——ガードに `layoutQuery.isLoading`／`initializedRef.current` も含め、通信失敗時も初期化済み扱いにして骨組みのまま固まらないようにした。②`LiveProgramSettingsPage.tsx` は `['program', programId]`（ダッシュボードと共有）の再取得（`refetchOnWindowFocus` 既定・保存後の invalidate）のたびに `useEffect` が無条件でフォームへ再流し込みしており、**編集中に入力が黙って消える**実害があった——`syncedProgramIdRef` でこのprogramId分は最初の1回しか流し込まないようにした。③設定＞権限とメンバー編集（`UserDialog.tsx`/`RoleDialog.tsx`）とスタジオ予約編集（`StudioBookingDialog.tsx`）は、対象を閉じずに別の対象へ切り替えると、内部stateの初期化が `useEffect` 頼みのため**前の対象の値のまま1フレーム描画**されてしまう別種のバグだった——呼び出し元（`MembersPage.tsx`/`FormDialogs.tsx`/`MobileToday.tsx`/`UnifiedCalendarPage.tsx`）で対象IDを `key` にして毎回作り直すようにし、`UserDialog`/`RoleDialog` は初期値も props からの遅延初期化に変え、初回レンダーから前の対象の値が出ないようにした。検証: `client`/`client-techops`/`server` の型チェック、`shared` の Vitest 1996件、`npm run lint` 全通過。
