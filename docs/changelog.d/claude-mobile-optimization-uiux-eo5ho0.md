**スマホ最適化の洗い出しで見つかった実装漏れ6件を直した**（`docs/reviews/2026-08-20-mobile-optimization-audit.md`）。
①プロジェクト一覧（`/gpm/projects`）はスマホでも「ボード」を開けてしまっていた — 240px固定カラムが5列並ぶ
カンバンで、375pxには1列も入らない。案件一覧と同じく`useIsMobile()`でスマホでは切替ボタンを隠し、常にリストへ
落とすようにした。②同じ画面の絞り込み帯（状態・区分・並び順）が畳まれず1行にそのまま並び、375pxでは折り返して
縦に重なっていたので、案件一覧・機材台帳と同じ`MobileFilterBar`（シート化）に載せ替えた。③機材の登録・編集
ダイアログが375pxでも常にフル項目・密グリッドで出ていた（「資産・保証」「親機材」など）。新規登録は畳んで開始・
既存の編集は開いたままにする折りたたみを足した。④共通部品`Pagination`の前へ/次へボタンと、④'検索を消す（×）
ボタン（4箇所に複製されていた）が28〜36pxで44pxタップ規則の対象外だった — どちらも`Button`部品を経由しない
生の`<button>`だったためで、`data-ui="button"`を付けて対象にした。⑤共通部品`CrudFormDialog`だけがv4の決めごと
「終わらせるのはシートで」に未対応で中央固定Dialogのままだった（取引先マスター・仕入先の新規追加・編集が通る）。
`client-v4/sheet.tsx`の`<Sheet>`に載せ替えた（外部から見えるpropsは不変）。

検証: `npm run typecheck` / `npm run lint` / `npm run test`（1141件）OK。

⚠️ 機材台帳「機材」タブのカスタム列・その場編集・親子の入れ子のスマホ対応、および
`EquipmentDetailPage.tsx`側の別実装（重複）は今回のスコープから外した（設計から必要な大きい機能追加のため）。
意図的にPC専用にしている35画面の洗い出しも同ドキュメントに記録し、1件ずつ見直す作業リストにした。
