- **メールの自動仕分けを作り直し、受け入れ側の「受領書類」をひとつづり表示にした**。①1か月分の受信メール615通を実際に読んで種別を8つに整理し、Gmail 側の実測件数と ONAiR 側の登録件数を突き合わせた（[docs/reviews/2026-09-06-mail-intake-taxonomy.md](docs/reviews/2026-09-06-mail-intake-taxonomy.md)）。Kairos3 の資料ダウンロード通知21件のうち17件が未処理・`finance_docs` は開設以来0件・`misc_inquiries` は全期間7件で全部「見送り」と、入口と出口の量が桁で合っていないことが分かった。②仕分けの手順そのものを**リポジトリ管理のスキル**にした（[`.claude/skills/mail-intake/`](.claude/skills/mail-intake/SKILL.md)。決定表・Kairos3 の3フォームの読み方・受領書類の束ね方・入ってきた情報の入れ方）。本番の実行体は VPS の Git 管理外にあるため、直したら写す運用。③受領書類を**見積書 → 発注書 → 請求書 の「ひとつづり」**にした（migration 280・`finance_doc_groups`）。見積の改定・見積だけで終わる取引・請求書しか来ない取引・分割請求をすべて同じ形で扱う。**見積書を一覧から外すのをやめた**（外すと「あの見積どうなった」を引く道が無くなる。台帳へ渡せないのは変わらず、画面が理由を言う）。④**当て先（どの案件か・販管費か）を AI が「仮」で置き、人が決める**形にした。AI は `project_hint`（GLS番号か案件名）を渡すだけで、サーバーが探して確からしさと理由を付ける（**候補が複数なら付けない**）。人が直すと `project_source='human'` になり差分が `ai_corrections` に入る。⑤販管費に**支払サイト（日数）と処理月**を持たせ、支払期日を自動で出せるようにした（「翌月末払い」は日数で表せないので別扱い）。⑥**メール添付の PDF を BOX の「受領書類（メール）」フォルダ**へ保存できるようにした（`record_finance_doc` の `attachments`）。**入らなかったものは理由付きで残す**（黙って握り潰すと原本がどこにも無い状態に気づけない）。⑦画面を「1つの取引 = 1枚のカード」に作り直し、**15秒ごとに取り直す**ようにした。トップページの受信箱からも「確認する」「承認」ができる（**却下は置かない** — スクロール中に押した請求書が誰の目にも触れなくなる）。⑧**メール取込ログ**（`ops_reports.kind='mail_intake'`）を新設し、落とした判断を1日1行残せるようにした。⑨用語を「受け取った書類」→「受領書類」に統一した。
