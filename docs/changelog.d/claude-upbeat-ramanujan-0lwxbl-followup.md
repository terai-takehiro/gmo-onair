**マージ済み PR #727 のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す（必須）」）。#727（営業活動記録の案件別化・次のアクションと本文の編集）は**レビューが1件も付かないままマージ**された（作成 15:07:18 / マージ 15:39 ごろ・約32分。`get_reviews` / `get_review_comments` / `get_comments` の3つとも0件で、`chatgpt-codex-connector` の usage limits コメントも無し。CI は両方緑で完了から約29分後にマージ）。**0件は画面上「読まれて問題なし」と見分けが付かない**ので、その事実を `docs/reviews/codex-findings-v4.md` に残した（`npm run reviews:debt` は今回も 401 で使えず、GitHub MCP で直接確認した）。あわせて、**指摘が0件だと表に1行も残らない**ため、#727 の本文に自分で書いた未着手の宿題5件（①絞り込みチップの件数が案件数であってやること件数ではない ②期限の区分が URL に乗っていない・担当者別の絞り込みが無い ③「あと3日」「今日が期限」を直していない ④適用済み migration 177 の通知テンプレートに「過ぎています」が残る ⑤権限のない利用者での 403・非表示を確かめていない）も同じ場所に書き出した。**件数は文章に書かず表と段落を数える形にした**（文章と実際が食い違った前例があるため）。**記録した直後に、0件マージの原因の候補が分かった**のも同じ回で書き足した: この棚卸しの PR（#729）そのものに `chatgpt-codex-connector` が「Codex usage limits に達した」とコメントし（#727 のマージの3分後）、**コードレビューだけが止まってセキュリティレビューは走る**ことも分かった。0件マージを「見る前に入った／利用上限／原因不明」の3つの形に分ける表を足し、**その PR にコメントが付かなくても、その時間帯に上限に達している可能性がある**（#727 がそれ）ことを明記した。
