**PR #684・#686のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」）。#688（#685の棚卸し記録）へのCodexレビューで「同じ区間に含まれる#684・#686が棚卸しから漏れている」と指摘され、両方を記録した。#684（会場図面ミニアプリの新設）はCode Review・Security Reviewとも1巡のみ完走し、全8件（P1×6・P2×2）の指摘（camelCase未変換で編集盤が描画不能・`copy_from`のIDOR・自動保存の失敗握りつぶし・下敷き画像URLの二重結合・下敷き画像の配置漏れ・マーキー選択の不始動・線分品目のドラッグが戻る）が付いたが、全件を本PR内で修正・返信・スレッド解決した。ただし修正コミットの直後にCodexがusage limitsを返し、P1×6件を含む修正そのものが一度もレビューされないままマージされた（#651/#677/#679/#683と同型）。#686（release: v4.6.14）はCode Review・Security Reviewとも完走し、要約文の記述誤り（会場図面を「実装はまだ」と誤記）1件（P2）を修正・返信・スレッド解決した。表に移す未対応の指摘はどちらも無い。検証: `node scripts/check-md-links.mjs`・`npm run lint`。
