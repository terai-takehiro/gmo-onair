**パートナースケジュール（代休・有給・出張・社外活動・リモート・その他）に「希望日」トグルと「担当者（複数・任意）」を追加した**（9/5 ご依頼）。①有給などの予定日がまだ確定していないことがあるのに、確定/未確定を区別する手段が無かった。`partner_schedules` に `status`（`confirmed`/`tentative`。`studio_bookings.status` と同じ2値・既定 `confirmed`）を追加し、①〜③ カレンダーの既存の「仮押さえ」破線描画パイプラインをそのまま流用してオン/オフのディップスイッチ的トグル（`PartnerScheduleDialog` の「希望日（まだ確定していない）」スイッチ）で表示するようにした。②誰の予定かは持っていたが、その予定に他の誰が関わるか（担当・付き添い等）を複数人・0人でも記入する手段が無かった。中間テーブル `partner_schedule_assignees`（登録ユーザーのみ・`project_members` と同じ複数対応の考え方）を新設し、ダイアログに担当者の複数選択チップを追加、カレンダーの「部屋・担当」欄にも表示されるようにした（`GET/POST/PUT /schedule/partner` が `assignees` を返し、`assignee_user_ids` で更新。渡さなければ現状維持、空配列で明示的に全員外せる）。検証: 新規マイグレーション（277番）を検証DBに適用しスキーマを確認、検証サーバーを起動して作成・一覧・部分更新（ステータスのみ／担当者クリア）・不正な担当者IDの拒否をAPIで実地確認、`server`/`client` の型チェック、`shared` の Vitest 1996件 全通過。
