**営業レビューを営業活動記録に統合し、左メニューの置き場所も直した**（ご指示）。
①「営業レビュー」は v4 化済みだったが、左メニューでは「そのほか（作り直し前）」の
折りたたみに取り残されていた（引っ越しの1行だけが漏れていた）。まず「全案件」へ
移した。②そのうえで、記録の一覧（営業活動記録）と分析3タブ（ファネル・失注分析・
営業評価、旧・営業レビュー）は役割が近い画面だったため、`/sales/activity-logs` の
タブへ1画面に統合した。旧 `/sales/review` は `?tab=funnel` 付きで転送する。
③統合先の分析3タブが「3列を並べて打合せの場で映す」前提の PC 専用画面だったため、
画面全体を PC 専用に戻した（営業活動記録は前の回でスマホに開放していたが、
タブによってスマホ対応が割れると PC専用判定を1か所に集める方針が崩れるため）。
集計式・一覧の絞り込み・次回アクションの完了/延期のロジックは1行も変えていない。
検証: `npx tsc -b client` / `npm run lint`（`check-links.mjs` / `check-mobile-declared.mjs`
含む）OK。
