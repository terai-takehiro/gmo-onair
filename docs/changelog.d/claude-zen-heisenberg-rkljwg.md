**技術資料の映像パッチで、機材・パッチ番号の候補が表の枠で切れて選べなかったのを直した**（利用者指摘「UI が非常に操作しづらい」）。表は横スクロールの器の中にあり、候補の箱をその中に出していたため、器の下端で切れて検索欄しか見えなかった。候補（機材・パッチ番号・行の操作）を画面の最前面に出すようにし、下に入りきらないときは上に開くようにした。あわせて機材の列を 128px から 160px に広げて機材名が切れにくくし、検索欄で Enter を押すと先頭の候補（無ければ入力した名前を増設機材）を選べるようにした。
