**PR #570（スタジオ予約の担当者を ics・MCP・マニュアル・シードにも行き渡らせた回）のレビュー棚卸しを記録した**（決めごと: [docs/branching.md](docs/branching.md) 「マージしたら、その PR のレビューを棚卸しに移す」）。⚠️ **同じご依頼から並行して2つの実装が走り、片方（#567）が先にマージされていた** — どちらも `studio_booking_assignees` を**同じ 279 番**で作っており、後から出したほう（#569）は全面的に重複したためクローズし、**#567 に無かった5点だけ**を #570 として出し直した。マイグレーション番号の衝突（#511/#512）と根は同じで、こちらは一段上の「機能そのものが二重に作られる」形。⚠️ **#570 のレビューは0件だが、今回は「届いていない」ではなく「回せなかった」** — Codex が `You have reached your Codex usage limits for code reviews.` を返しており、**利用上限**という新しい落ち方（セキュリティレビューだけは完走・指摘0）。意図して残した未検証事項（`npm run build` 未実行・権限別403の画面確認なし）は [docs/reviews/codex-findings-v4.md](docs/reviews/codex-findings-v4.md) に書き出した。
