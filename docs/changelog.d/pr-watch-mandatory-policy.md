**PR を出したら見張り（`pr-watch`）を自動で始めることをマストなポリシーにした**。
今までは「見張りますか」とユーザーに確認してから購読していたが、確認の返事を待つ間は
CI 失敗もレビュー指摘も誰も見ていない時間になる。`docs/branching.md`（見張りの手順・
やってはいけないこと）と `CLAUDE.md`（ブランチ運用の要点）に、Claude が PR を出した場合は
確認を待たずにその場で `.claude/skills/pr-watch` を使うことを明記した。
既存の `pr-watch` スキル自体は変更していない（手順は既に十分だった）。
