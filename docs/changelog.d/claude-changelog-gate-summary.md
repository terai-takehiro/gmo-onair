**版の下書きの門に、通り抜けられる穴があったのを塞いだ**（Codex のレビュー #173・P2。
**私が #173 で開けた穴**）。
①⚠️ **`_summary.md` だけを足した PR が門を通り抜けます。** #173 で足した「版の要約」は
`docs/changelog.d/_summary.md` に置く決めですが、**門（`check-changelog.mjs`）は
`README.md` しか外しておらず**、これを「下書きを1つ足した」と数えて **OK を出して**いました。
一方**集める側（`collect-changelog.mjs`）は要約なので集めません**。
⚠️ **つまりその PR は版の履歴に1行も載りません。** しかも 12,000 バイトを超えなければ
要約は使われないまま消されるので、**どこにも残りません**（黙って消える）。
②**判定を1か所にしました**（`scripts/lib/changelog-summary.mjs` の `isNoteFile`）。
門と集める側が**同じ関数**を読みます — 片方だけ直すとまた食い違います。
③**検証**（実測）: `_summary.md` だけを足した状態で門を回すと**直した版は exit 1**・
**外し忘れた版は exit 0** で「この PR の下書き 1 件」と出ます（**反証つき**）。
リリースの3通り（分ける／分けない／手書きの要約）も走らせ直し、
**要約はアーカイブに混ざらず（0件）・`CLAUDE.md` にだけ入り（1件）・使ったら消える**ことを確認。
`shared/tests/changelogSummary.test.ts` は 12 → **17 項目**。
④⚠️ **もう1件（P2）— 要約が全文より「長い」と、画面から全文が消えます。**
書き込む側の上限は**バイト**（`CLAUDE.md` が毎ターン文脈に載る費用はバイトで効く）ですが、
画面が同じ版の2つを見比べるのは**文字数**です。日本語は 1文字 3バイトなので、
**全文が日本語ばかり・要約が英数字ばかり**だと「バイトでは要約が小さいのに、
文字数では要約のほうが長い」を作れます。そうなると画面は**要約を本文に採り、
アーカイブの全文を捨てます**。⚠️ **エラーは出ません**（版は1件出るし、要約も正しい文章です）。
→ **生成側と同じ数え方**（`descriptionLengthOf`）で確かめて止めます。
**反証（実測）**: 英数字 11,100 文字の要約は **11,109 バイト＝上限内**なので
**バイトの検査だけでは通り**ますが、文字数では **要約 11,115 ＞ 全文 11,016** で
**exit 1**・**何も書き換えません**（`CLAUDE.md` も下書きも無傷）。
⑤⚠️ **④の直しに、レビューで続けて3件の指摘が付きました。すべて「画面と同じ数え方」の写し漏れです** —
版の行の前置き（`vX.Y.Z — `）を外していない／本文が空のときに丸ごとを使う `|| body` を落としている／
**見出しのあとの句点を2つ落とす**のを1つしか落としていない。
⚠️ **どれも「要約のほうが長い」と誤判定させ、アーカイブの全文を画面から消します**（エラーは出ません）。
⑥**写すのをやめました。** 数え方は `generate-version-history.mjs` の
`titleAndDescriptionOf` を **export して import** します（**唯一の定義**）。
⚠️ **手で写している限り何度でも起きる**ことが4回で分かったので、構造で止めます。
試験は**生成側の出力と直接突き合わせ**ます（写しが増えたらそこで落ちます）。
**反証（実測）**: 見出しだけ＋句点2つの英数字 6,000 文字の要約は、
**直した版は「要約 6,006 文字 / 全文 5,000 文字」で exit 1・`CLAUDE.md` 無傷**、
**句点を1つしか落とさない実装では exit 0 で `CLAUDE.md` に入ってしまう**。
⑦**アプリのコードは1行も触っていません**。DB 変更なし。
