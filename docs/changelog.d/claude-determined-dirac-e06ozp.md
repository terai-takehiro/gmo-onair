**制作技術支援に新しいミニアプリ「会場図面」（会場図面シミュレーター）の設計とモックアップを起こした**（実装はまだ1行も書いていない）。①GMOサムライスタジオ 用賀の会場図面（26F・27F の平面図 PDF）の上に、任意の四角・丸・人の俯瞰・備品リストの品目（20品目）・昭特のペデスタル TP-90B とクレーン TK-53L（台車 TI-04B）を**実寸で**置き、椅子を行×列で一気に並べ、出来た図を運営マニュアルの紙面に差し込みブロックとして載せる（冊子側の「会場図面を編集」で図面側へ遷移し `?return=` で戻る）ミニアプリを、パワポ派・段取り派・連携派の3案を審査役が統合するマルチエージェントの手順で設計した。利用者に見せる語は5つ（会場図面・階／エリア・品目・並べ方・下敷き）、動詞は 追加／編集／削除／キャンセル／保存 に 置く・並べる・差し込む・書き出す を足しただけ。編集の手つきは運営マニュアルと同じ PowerPoint 流で、品目のつまみは図形にしか出さない（備品・人・カメラは実寸固定）。②縮尺は利用者の仕事にしない。図面 PDF を通り芯（X16→X9・Y16→Y9 が各 44,800mm）で校正したところ **X 63.199 mm/pt・Y 60.577 mm/pt と X/Y の縮尺が 4.3% 違う**（Illustrator で非等方に伸ばされた図。1軸の縮尺では 10m で 43cm ずれる）ことが分かり、階ごとに X/Y 別の縮尺と原点を持ち、下敷き画像だけを X/Y 別に伸縮して品目は等方の mm 空間に描く作りにした。p-1（26F）上端の通り芯ラベル列は縮尺の違う飾り（69.0 mm/pt）で校正に使えないことも実測で確かめ、「線で測る。ラベルの円で測らない」を取り込み手順に残した。実寸検証は 44,800／LED ウォール 8,530／MEETING ROOM 3,775／ELV13 1,850×2,010 の4点を ±60mm で通す関所にする。③昭特の製品ページは実行環境から開けず、検索で確定した値（TP-90B 積載 60kg・ストローク 945mm／TK-53 最高レンズ軸高 3,099mm・53L 3,850mm）だけを確定とし、ベース径・アーム長・台車の縦横などは「寸法は推定」の印つきで仮置きした（確定の方法と期限は「決めていただきたいこと」）。④データは `qsheet_venue_floors`・`qsheet_venue_areas`・`qsheet_venue_catalog_items`・`qsheet_venue_layouts` の4本、図面1件＝1行（品目は JSONB・階の絶対 mm）、資料番号は `VL-YYYYMM-0001`、編集ロックと `updated_at` の突き合わせは運営マニュアルの写し。運営マニュアルの盤は A4 固定が7か所にあるため別の盤を書き、純粋関数（リサイズ・回転・整列・すいつき）だけ流用する。⑤設計書は `docs/design/v4/venue-layout.md`、モックは `docs/design/v4/mockups/native/venue-layout/`（地図 1・PC 5〈一覧・編集・並べる・仕上がり・冊子に差し込んだ紙面〉・スマホ 1。押せる試作・下敷きは実際の図面を校正済みの縮尺で同梱）。名前と資料番号の接頭辞・実寸固定・グループ・複数案の持ち方・スマホは閲覧のみ・昭特の寸法の確定方法・線画だけを載せる・図面の確定と版、の8件は同日にご判断をいただいた（昭特の寸法だけ推奨と違い「推定のまま進める」）。あわせて「図面に点在する黒い長方形は消す（トラス脚は残す）」というご指摘から、下敷きのトラス脚の横の黒い矩形は取り込み時に消し込み、トラス脚は白抜きの柱記号で残し、建築図面の寸法線と数字も下敷きから消し込むことを決めた（設計書 §4-7）。クレーンの機種は TK-53L。検証: `node scripts/check-md-links.mjs`・`npm run lint`。

**上記の設計を実装した（会場図面ミニアプリ本体・マルチエージェントで並行実装）**。①器: migration 298（`qsheet_venue_floors`・`qsheet_venue_areas`・`qsheet_venue_catalog_items`・`qsheet_venue_layouts` の4本）と 299（GMOサムライスタジオ用賀 26F/27F のシード。階2・エリア22・カタログ36＝昭特ペデスタル/クレーン含む）を実 Postgres（`npm run verify:up`）で検証して追加。`shared/src/venue/{types,geometry,arrange}.ts` に型・2軸縮尺変換・中心⇔左上座標のアダプタ（運営マニュアルの `manualCanvasGeometry.ts` をそのまま使うための橋渡し）・一気に並べる7プリセットの純粋関数と Vitest 38件を追加。②サーバー: `/techops/venue-layouts` 系・`/techops/venue-floors`・`/techops/venue-catalog` の API 14本（一覧・CRUD・複製・確定/版・編集ロック）、`canAccessVenueLayout` などの可視性判定、運営マニュアルへの差し込み resolver（`venue.layout`/`venue.items`。下敷き画像・外部URLを含まない自己完結の線画データ）を追加し、`shared`/`server` 双方の `miniapps.ts`・`manualBlocks.ts` に `venue` エントリを追加（`check-collab-parity.mjs` 済み）。③クライアント: 一覧・作成ダイアログ・編集盤（PC＝実寸のmm空間SVG盤で置く/並べる/整列/すいつき/undo50手、スマホ＝閲覧専用）・仕上がり書き出し（PNG/PDF・出す前の検査3つ）・運営マニュアル側の差し込み対応（`InsertPanel`/`LinkedBlockContent`/`LinkedBlockInspector`に`venue`分岐、`?return=`で図面⇔冊子を行き来）を実装し、`App.tsx`・左メニュー・ハブのタイル・`pcOnlyScreens.ts` に配線した。検証: `npx tsc -b client-techops`・`npx tsc -b server`・`npm run lint`（0 errors）・`npm run test`（shared 173ファイル・2457件／server 94件、すべて成功）。段F（会場・階の管理画面＝PDF取り込みと縮尺合わせのUI）は設計どおり今回のスコープ外。
