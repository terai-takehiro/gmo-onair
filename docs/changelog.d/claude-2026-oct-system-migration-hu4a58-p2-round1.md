**2026年10月の事業再編（社名変更・計上会社の2社化・案件番号の改番）P2 Round 1（財務の多社対応）をマルチエージェントで実装した**（`docs/reorg-2026-10-plan.md` §4.5・§4.6・§4.12・§6）。社内取引（`intercompany_links`）は Round 2 として今回は見送り、スコープを明示的に2ラウンドへ分割した。①`money_rules`を`id='default'`固定の1行から会社ごとの複数行（`id=entity_code`）に変更し（migration 284）、GJV/GMOはGSSの設定値の写しで初期化した。`monthly_budgets`/`monthly_actual_overrides`のPKも`(entity_code, year_month)`の複合キーに拡張した。②請求書番号を発行者ごとの系列`INV-{entity_code}-{年}-NNNN`に分離した（GSSも新系列`INV-GSS-2026-0001`から。旧`INV-2026-…`系列は凍結し変更しない）。③お金のルール・月次予算/実績補正・損益（`getMonthlyPl`）をentity_code引数対応にした。税の丸め方は今の会社を読んだときだけプロセス全体のグローバルへ反映する形にとどめ、複数社の本格対応はRound 2以降とした。④財務の一覧・Excel・MCP・営業見通し（パイプライン）・締め処理（請求入金）にentity_codeの絞り込みを追加した（省略時は今までどおり全社合算）。⑤案件サマリーの会社別内訳`getSummaryByEntity`を新設した（既存の全体粗利`getSummary`/`getSummaries`は変更なし。別関数・別ルート`/:id/summary-by-entity`として追加。社内取引の相殺はRound 2の課題として残る）。⑥財務ダッシュボード・請求入金（PC版）・売上・仕入・販管費の5画面に会社タブを追加した。⑦お金のルール設定画面を会社タブ化した（値引き上限・フェーズ確度は全社共通ポリシーのため対象外のまま）。副産物として、P1の`renumber_project` MCPツールがgate.tsの権限ゲートに未登録で`npm run build:changed`のprebuildを止めていたバグも発見・修正した。検証: `server`/`client`の型チェック、`shared`のVitest 2004件、検証DBを作り直してのmigration 1〜284通し適用、各機能の実データに近いスモークテスト、実サーバーを起動してのHTTPレベルでのentity_code絞り込み確認（不正な値は400・省略時は全社合算になること）。
