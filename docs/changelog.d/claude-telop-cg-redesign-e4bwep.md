**制作技術支援＞テロップCG の機能と UI/UX をゼロベースで再設計し、旧リアルタイムCGを「凍結」から「移行中」へ整理した**（2026-09-06・設計とモックのみ。アプリのコードは変えていない）。①旧「リアルタイムCG」（`client-awards/`）は v4 で据え置く「凍結」ではなく、制作技術支援のミニアプリ「テロップCG」へ機能を移している途中の**移行元**なので、[docs/v4-plan.md](docs/v4-plan.md) の用語に「移行中」（URL・API・配信は生かす／一覧・メニューには出さない／新しい機能は後継側に作る）を新設し、`CLAUDE.md` の一覧表・`README.md`（「廃止・配信停止」と古い記述だった）・`client-awards/CLAUDE.md`・PR テンプレート・`apps.ts` のコメントと説明文をそろえた（`frozen: true` は「一覧に出さない印」として据え置き）。②2026-08-30〜09-01 に作った現行のテロップCG（68ファイル・10,383行）は、ハブの上段に操作が7個平積み・利用者に見える概念が17・左メニューにサブ項目が無く設定が3階層下・戻り先の文言が3種類・本番画面が7区画で1枚出すのに2操作、と「何をする道具か」が画面から読めなかった。[docs/design/v4/graphics-redesign.md](docs/design/v4/graphics-redesign.md) に、利用者に見せる語を**テロップ／種類／出す順／見た目の4つ**に絞り、画面を **PC 4枚（一覧＋右パネル・本番モード・設定4タブ）＋スマホ 2枚（依頼・一覧）＋出力**にし、本番の動詞を旧アプリの実績ある NEXT→TAKE→CLEAR に戻して **TAKE／CLEAR／進める の3つ**にする再設計を書いた（機能の残す／畳む／統合／足すの表・旧→新の言葉の対応・進行台本の「テロップ」列から出す順を取り込む連携・DB への影響は列2本の追加だけ・段A〜F の段取り・決めていただきたいこと5件）。③クリックできるモックを Claude Design のキャンバスで作った（`docs/design/v4/mockups/native/telop-cg/`・7枚。一覧の絞り込みと確認スイッチ・本番モードの TAKE／CLEAR／締切／開票・設定のタブ・依頼の送信が動く）。検証: 7枚とも実ブラウザ（headless Chromium）で描画を確認。`node scripts/check-changelog.mjs`・`check-md-links.mjs`・`check-links.mjs` を通した。

**続き（同日）: 「決めていただきたいこと」5件にユーザー回答があり、設計とモックへ反映した。** ①本番モードの未確認は TAKE 可（△警告のみ・確定）。②台本に追従は既定 OFF・①テロップ一覧の状態帯から番組ごとに ON にできる（トグルをモックへ追加）。③出す順の番号は、並べ替えの都度振り直す案を検討していたが「よくわからない」との回答を受け、**番号は作成時に固定・並べ替えても変わらない**とこちらで決定した（口頭で番号を呼ぶ運用と、並べ替えで番号がずれる事故を両立できないため）。④スマホからの緊急 CLEAR を許可し、⑥一覧（スマホ）の「いま出ているもの」各行に確認なし1タップの「消す」ボタンを追加した。⑤旧リアルタイムCGの畳み込みは承認を得たが、後継（段A〜E）がまだ実装されていないため、**実行は段A〜E を検証環境で確認したあと**という元の順序どおりとし、この承認は記録済みとして段F実行時に再確認不要にした。詳細は [graphics-redesign.md §12](docs/design/v4/graphics-redesign.md)。検証: 7枚とも実ブラウザで再確認。

**続き（同日）: 再設計の段A（①一覧＋右パネル・④設定1画面・左メニューのサブ項目）を実装した。** 「実装へ」のご指示を受けて着手。①テロップ一覧はヘッダーを 設定／＋テロップ／本番モード の3ボタンに絞り、行を押すと右パネル（`TelopEditorPanel`）でそのまま直せるようにした（旧ダイアログの往復を廃止）。行に実描画サムネイル（`TelopThumb`）を付け、`@dnd-kit` でドラッグ並べ替えができるようにした——**並べ替えても呼出番号（callNo）は変わらない**（§12-3の決定どおり `sortOrder` だけを動かす）。新規作成は「種類を選ぶ（`TelopKindGrid`。旧・部品ライブラリのカードを流用）→ 文言を入れる」の2段にした。④設定は出力URL／見た目／同時に出せないもの／連携の1画面4タブ（`GraphicsSettingsPage`）にまとめ、旧・部品ライブラリの独立画面（`PartLibraryPage.tsx`）は削除、演出SE管理・外部インタラクティブ連携設定の独立画面は「連携」タブに埋め込んだ（`embedded` prop で中身をそのまま再利用・データ取得やAPIは変えていない）。旧URL（`/parts`・`/sounds`・`/interactive-link`）は①・④への転送に置き換えた。左メニューは、テロップCGの中を見ているときだけ「テロップCG」1本を「テロップ一覧／本番モード／テロップCG設定」の3本に展開するようにした（`ShellNavItem` がインデント付き子項目を持たないため、同じ深さで展開する形にした）。意図した持ち越し: フィールドの自動保存はまだ（明示的な保存ボタンを維持）・「テンプレートから作る」機能は①の主導線から外したが④「見た目」タブから引き続き使える。詳細・ファイル一覧は [graphics-redesign.md §11「段A 実装メモ」](docs/design/v4/graphics-redesign.md)。検証: `server`/`client-techops` の型チェック、`npm run lint`（0 errors）、`shared` の Vitest 1996件、`client-techops` の本番ビルド、全通過。加えて `npm run verify:up` の Postgres に実サーバー・実 `client-techops` dev server を起動し、Playwright で①一覧・行の右パネル編集・新規作成の2段・④設定の4タブ・旧URL3本の転送先・375px幅でのPC専用ガード表示を実ブラウザで確認した。未検証: 実機でのタップ操作・権限別403表示（PRを出す段の棚卸し対象）。

**続き（同日）: 再設計の段B（②本番モードの作り直し）をマルチエージェントのワークフローで実装した。**「マルチエージェントで進めて」のご指示を受け、実装（並列2体）→統合（1体）→型チェック・lint（1体）→検証（並列2体：設計書との批判的レビュー＋機械チェック）→修正（1体）の5段構成で進めた。①本番モードの動詞を旧アプリ実績の TAKE→CLEAR の2つに戻し、TAKE すると出す順（`sortOrder`）の次の行へ NEXT が自動で進むようにした（旧5動詞のうち「スタンバイ・次へ」は行クリック＋TAKEの自動前進へ、「続き」はOA中の行の中（`ConsolePageList.tsx` の `OaInlineAction`）へ、「OUT」は「いま出ているもの」帯の「消す」ボタンへ吸収・統合した）。②PGM/PVWの表記をOA/NEXTに、オールクリアを「全部消す」に改称した。③「いま出ているもの」帯（`ConsoleSlotLanes.tsx`）は常時6枠表示をやめ、いま実際に出ているスロットだけを表示するようにした。④出す順一覧（`ConsolePageList.tsx`）は行そのものがNEXT切替の操作対象になり、スロット札・3値校正バッジを廃止して段Aと同じ✓/○の2値表示に統一、OA中の行には種類ごとの「進める」操作（投票=出題→締切→開票、ランキング=次の順位またはFinal Pitchパネル、一覧=次の項目、スコア=±クイック調整）を埋め込んだ——いずれも既存の `useConsoleContinue.ts`・`ScoreQuickAdjust.tsx`・`RankingControlPanel.tsx` をそのまま呼ぶだけで、サーバー・API・Socketは一切変えていない。検証段の批判的レビューが実際に安全装置③（文言が空のものはNEXTに立てられない）の抜け——番号呼出・↑↓移動の2経路が未対応だった——を検出し、修正エージェントが解消した。さらにワークフロー完了後の最終確認で、レビューが見つけた3経路とは別に**TAKE成功後の自動前進**にも同じ抜けがある（次の行が空欄だとそのままNEXTにしてしまう）ことに気づき、`useConsoleTake.ts` の `take()` に「空欄でない次の行まで読み飛ばす」処理を追加した。意図した持ち越し: コーナー見出し（段C待ち）・「出した hh:mm」表示（データが無い）・行の「直す」→シートでの文言修正（新しいUIパターンが要るため見送り）。詳細・レビュー指摘の一覧は [graphics-redesign.md §11「段B 実装メモ」](docs/design/v4/graphics-redesign.md)。検証: `tsc -b client-techops`／`npm run lint`（0 errors）／`npm run test`（1996件）／`npm run build -w client-techops` を全通過（追加修正後も再確認）。加えて `npm run verify:up` の Postgres に実サーバー・実 dev server を起動し、Playwright で番号呼出→TAKE（空欄行の自動読み飛ばしを含む）→CLEAR→行クリックでのNEXT切替→空欄行がNEXTにならないこと→OA中の一覧表の「次の項目」ボタン→全部消すの改称、を実ブラウザで確認した。未検証: vote・score・ranking種類の行内操作（検証用の seed データに存在しないためコードレビューのみ）・実機タップ操作・権限別403表示。

**続き（同日）: 再設計の段C（コーナー・台本から取り込む・台本と違いますバッジ）をマルチエージェントのワークフローで実装した。**「Cへ」のご指示を受け、段Bと同じ実装（並列3体）→統合（1体）→型チェック・lint（1体）→検証（並列2体）→修正（0〜1体・今回は指摘が2件とも非ブロッカーだったため未実行）の5段構成で進めた。①進行台本（Qシート）のテロップ列の文言を、①一覧の「＋テロップ」メニュー「台本から取り込む」から一括でテロップ化できるようにした（台本を選ぶ→テロップ列の文言を候補一覧で確認・種類を直せる→選んだ分だけ`graphics_pages`へ一括作成。文言から種類を推定`inferPartKey`し、台本の行の並びをそのままコーナー・出す順にする）。②取り込んだページは台本の行ID（`qsheet_row_id`）を覚え、台本側の文言が変わると①に「台本と違う」の状態チップと行の「台本と違います」バッジが出るようにした（押すと台本のいまの文言に取り込み直せる。誤検知より見逃しを選ぶ設計——台本・行・列のいずれかが無くなった場合や主フィールドが配列型の部品は差分ありと判定しない）。③台本の`sections[].label`をコーナー見出しとして①一覧・②本番モードの両方の一覧に挿入できるようにした（②は本番中の一覧を賑やかにしないため見出しのみでバッジは付けない意図的な非対称）。DBは`graphics_pages`に`section`・`qsheet_doc_id`（`qsheet_documents`へのFK）・`qsheet_row_id`の3列を追加するmigration 280のみ。台本へのアクセス制御は`qsheet`コンテキストの既存`canAccessDoc`を新設した3つのサービス関数すべてから通し、共有されていない他人の台本の中身が読めないようにした。統合段で「`pageId`の型不一致で台本と違いますバッジが絶対に出ない」「ネーム部品だけ主フィールドの位置がずれて取り込んだ氏名が反映されない」の2件の不具合を検証DBの実データで再現・修正した。検証: `server`/`client-techops`の型チェック、`npm run lint`（0 errors）、`shared`のVitest 1996件、マイグレーション適用確認をワークフロー内で完了させたのに加え、呼び出し元（自分）が独立して同じ項目を再実行し、全ファイルを精読して`canAccessDoc`の呼び出し漏れが無いことを確認したうえで、実サーバー・実dev server起動＋Playwrightで台本選択→候補確認→取り込み→①一覧への反映（コーナー見出し込み）→台本編集後の差分検出→取り込み直し→②本番モードでのコーナー見出し表示（バッジ無し）まで実ブラウザで確認した。副産物として、検証に使った既存シード台本（`seed-subapps.ts`）自体にテロップ/映像/音声セルの形が実編集画面と異なる・行にIDが無いという段Cとは無関係の既存ギャップを発見した（ブラウザ確認は検証DB限定でその場修正して実施・コミットはしていない。段D/Eの検証でも同じ制約に当たりうるため設計書に記録した）。未検証: 実機タップ操作・権限別403表示・1行に複数のテロップ列がある台本。

**続き（同日）: 再設計の段D（依頼フォームの3欄化・台本から選ぶ・スマホ閲覧＋緊急CLEAR）をマルチエージェントのワークフローで実装した。**「残りの工程をマルチエージェントで実装しPR」のご指示を受け、段Cと同じ実装（並列2体）→統合（1体）→型チェック・lint（1体）→検証（並列2体）→修正（0〜1体・今回は指摘1件がコメントの文言だけの note だったため未実行）の5段構成で進めた。①⑤依頼（テロ原）フォームを「文言・出すタイミング・種類」の3欄に絞り、「用途・補足」欄をフォームから削除した（過去分のデータ・表示は後方互換で残置）。「出すタイミング」の横に「台本から選ぶ」ボタンを新設し、台本の各行を「シナリオ列→テロップ列→コーナー名→機械的な文字列」の順で必ず何か表示できるラベルに組み立てて選べるようにした（`client-techops/src/pages/rundown/rundownData.ts`が既に持つ`flattenCues`/`extractCellText`を再利用し、新しいサーバーエンドポイントは作っていない）。②⑥一覧（スマホ・閲覧＋緊急CLEAR）を実装した。①と同じURL（`/techops/graphics/:ownerKey`）を`pcOnlyScreens.ts`のPC専用リストからスマホ対応リストへ移動し、狭い画面のときだけ読み取り専用の別ビューを描画するようにした（PC向けの中身は`hidden sm:block`で包んだだけで一切変更していない）。「いま出ているもの」の各行に確認なし1タップの「消す」ボタンを置いた（§12-4の決定どおり、確認ダイアログを経由しない）。DB変更・新規サーバーエンドポインとも無し（既存API・既存データ取得の組み合わせのみ）。検証: `tsc -b client-techops`／`npm run lint`（0 errors）／`npm run test`（1996件）／`npm run build`をワークフロー内・呼び出し元（自分）の両方で確認し、検証段が見つけたコメントの文言の古さ（削除済みの「用途」欄への言及が2箇所）を自分で修正した。加えて自分でJSXの見た目を整えようとしたところ400行制限に抵触することに気づき、修正前に戻した（lintを通してから直す習慣の効用）。`npm run verify:up`のPostgresに実サーバー・実dev serverを起動し、Playwrightで⑤フォームの3欄化・台本から選ぶ→項目一覧→「出したいタイミング」欄への反映、⑥（375px幅）の読み取り専用表示・横スクロール無し・「いま出ているもの」の確認なしCLEAR、①のPC表示（1400px幅）が変わっていないこと、をすべて実ブラウザで確認した。未検証: 実機タップ操作・権限別403表示。

**続き（同日）: 再設計の段E（台本に追従・前の番組からコピー）をマルチエージェントのワークフローで実装した。これで再設計の実装段（段A〜E）が完了した。** 段Dと同じ実装（並列3体）→統合→型チェック・lint→検証（並列2体）→修正の5段構成で進めた。①「台本に追従」（既定OFF・番組ごとにON）を実装した。`graphics_projects`に`follow_script`列（migration 281）を追加し、①テロップ一覧の状態帯（台本から取り込んだ番組だけに表示）から切り替えられるようにした。ONの番組では、進行（OnAir）画面で現在の行が進むと②本番モードのNEXTが対応するテロップへ自動で移る——**TAKEは常に人が押す**（自動送出はしない）。既存の`RundownPage.tsx`が持つ「台本の進行にだけ追従する」実装をそのまま模写し、Socket `/techops`の`cue:sync`をリッスンするだけの読み取り専用フックにした（`cue:*`のemitは一切しない）。②「前の番組からコピー」を④設定「見た目」タブに実装した。別のCGプロジェクトのテンプレート一式を、新しい行としてこのプロジェクトへ複製できる（コピー元は変更しない・自分自身の指定は400で弾く）。検証段の批判的レビューが実際にブロッカーを検出した——「台本に追従」トグルの設置場所が設計書の指定（①状態帯の右端）と食い違い、上段のボタン行（「それ以外のボタンは置かない」と明記されている場所）に誤って置かれていた。担当のファイル割り当てに、正しい設置場所を持つファイルが含まれていなかったことが原因で、修正エージェントが正しい場所へ置き直して解消した。検証: `server`/`client-techops`の型チェック、`npm run lint`（0 errors）、`shared`のVitest 1996件、`verify:up`でのmigration 281適用確認をワークフロー内で完了させたのに加え、呼び出し元（自分）が独立して同じ項目を再実行し、`useScriptFollow.ts`を精読して`.emit()`呼び出しが1つも無いこと・`isPageContentEmpty`のガードが効いていることを確認した。自動追従は安全上とくに重要な機能のため、ローカルのマルチポートdev環境の制約（vite dev serverが`/socket.io`をプロキシしないため②本番モード自身のSocket接続すら繋がらない）を検証用に一時的に解消したうえで、実socket.io-clientから`cue:update`を実際にemitし、②のNEXTが対応するテロップへ実際に移ること・OAには変化が無い（TAKEが自動発火しない）ことを実ブラウザで確認した（検証用の変更はコミットしていない）。「台本に追従」トグルの表示位置・持続化、「前の番組からコピー」の一覧→確認→複製→DBへの反映も実ブラウザで確認した。未検証: 実機タップ操作・権限別403表示の画面確認（APIレベルでは正常系・異常系とも確認済み）。
