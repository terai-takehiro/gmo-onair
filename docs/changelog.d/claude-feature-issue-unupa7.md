**上辺バーの本人メニュー（利用マニュアル・バージョン履歴・MCP コネクタ・ユーザー切替・ログアウト）が押しても反応しなかったのを直した**。`AppTopbar.tsx` の本人メニューは `createPortal` で `document.body` 直下に描いているが、外側クリック判定（`useDismiss`）が見ていたのはボタン側の `ref` だけで、ポータル先のメニュー本体には `ref` が付いていなかった。そのため項目を押すと `mousedown` の時点で「メニューの外」と誤判定されてメニューが閉じ、続く `click` が発火する前に要素が DOM から消えて各ボタンの `onClick`（`onOpenManual`/`onOpenVersionHistory`/`onOpenMcpInfo`/`onSwitchUser`/`onLogout`）が一度も実行されない状態だった。ポータル先の `<div role="menu">` にも `ref` を渡し、外側クリック判定の対象に含めて修正した（旧 `SharedHeader.tsx` は元から同じ形で正しく実装されていた）。検証: `npx tsc -b client` / `npm run test`（1452件）/ `npm run lint`（新規のエラー・警告なし）。
