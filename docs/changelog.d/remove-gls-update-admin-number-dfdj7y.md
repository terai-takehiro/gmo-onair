**画面に出ていた「GLS番号」という表記を「管理番号」に統一した**。案件管理・財務管理・制作技術支援・機材管理・日常業務の各アプリで、検索欄のプレースホルダー・案件カード/一覧の列名・ダイアログの案内文・エラーメッセージ・アプリ内マニュアルなど画面に出る文言をすべて「管理番号」へ書き換えた。変えたのはラベルの言い方だけで、実際の番号表記（`GLS-A001` 等の書式）・DBの `gls_number`/`gls_category` 列・`issueGls` 等の内部識別子・「GLS発番」「GLS案件」といった発番操作の名前は変えていない（実際の番号が引き続き `GLS-` で始まるため）。`docs/wording.md` の方針（GLS番号は経理・請求で使う社外語なので残す）もあわせて上書きした。検証: `npx tsc -b client server`・`node scripts/check-ui-tokens.mjs`（新規の禁止語違反なし）。
