**PR #598（メール仕分けの枠の食い潰し・本番/検証 MCP の同名問題）のレビュー棚卸しを記録した**（決めごと: [docs/branching.md](docs/branching.md) 「マージしたら、その PR のレビューを棚卸しに移す」）。⚠️ **📝 Code Review は Codex の usage limits で一度も実行されず**（「You have reached your Codex usage limits for code reviews」と明示的に返答）、🔒 Security Review だけが `25b857d` で完了（findings なし）。`get_reviews`・`get_review_comments` とも0件を API で確認（`npm run reviews:debt` はこの環境のトークンでは401）。**#590・#595・#596 に続いて4本連続で Code Review が届いていない** — 通っているのは Security 観点だけなので「指摘なし」ではない。表に移す指摘は無いが、この PR で自分の実測から見つけて直した5件を [docs/reviews/codex-findings-v4.md](docs/reviews/codex-findings-v4.md) の表に書き出した。

**検証 MCP の繋ぎ方を具体化した**。「`MCP_API_KEY_DEV` は VPS の `.env` にある」とだけ書いていたが、**`.env.example` ではコメントアウトされており `docker-compose.yml` は `${MCP_API_KEY_DEV:-}` と未設定なら空で渡す**ため、実際には設定されていないことがあり、その場合 `?key=` は 401 になる（`mcpAuth` は静的キーが無くても OAuth に誘導するので 503 ではない）。①**OAuth を推奨に格上げ**した（キー不要・`MCP_API_KEY` の設定有無に関わらず常に有効・書き込みの actor が共用 `mcp-claude` ではなく本人になる）。②キーが入っているかを**値を出さずに**確かめる手順と、無いときに作って `app_dev` だけ入れ直す手順を書いた。③⚠️ `BOX_MAIL_INTAKE_FOLDER_ID_DEV` を設定しないと本番と同じ BOX フォルダに落ちるため、**検証で取り込んだ試験用の請求書 PDF が本番のフォルダに混ざる**（DB は分かれていても BOX は分かれない）ことを明記した。あわせて `docker-compose.yml` の「migration 280」というコメント2か所を、番号衝突で改番した実際の番号（281）に直した。
