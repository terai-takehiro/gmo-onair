**PR #686のレビュー状況を棚卸しに記録した**（`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」）。#686（`release: v4.6.14`）はCode Reviewが初回コミットに1巡反応し、1件（P2）の指摘が付いた——集約したv4.6.14の要約が「会場図面（会場図面シミュレーター）は設計とモックアップまで（実装はまだ）」と書いていたが、実際は同じ版に含まれる#684内で実装（migration 298/299・サーバーAPI・クライアント編集盤・レビュー指摘8件の修正）まで完了しており、`docs/version-history.md`の全文とも矛盾していた（`docs/changelog.d/`の下書きが「設計のみ」時点のまま更新されず、後から実装まで進んだことが要約に反映されなかったのが原因）。要約を実装済みである旨に修正し、返信・スレッド解決まで完了した（表に移す未対応の指摘は無い）。Security Reviewは完走しfindings無し。ただし修正コミットにはどちらのレビューも再実行されないままマージされた。検証: `node scripts/check-md-links.mjs`。
