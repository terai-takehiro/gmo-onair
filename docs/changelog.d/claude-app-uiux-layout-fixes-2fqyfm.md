**プロジェクト管理以外の全アプリのレイアウト崩れを実測で洗い出して直した**（ユーザー報告「PC、スマホ双方で現状レイアウト崩れを起こしているものも多数あり」）。先進業務ツール（Linear・Stripe・Airtable・Fantastical ほか）のUIパターンをディープリサーチしたうえで、**全89画面を 375/768/1280px の実ブラウザで撮って7エリア並列で監査**し、見つかった崩れだけを直した（監査は憶測を排し、実測値かスクリーンショットの裏付けがあるものだけを対象にした）。

主な修正:
- **スマホの案件詳細で、その案件が今どのステージか初期表示で読めなかった**（帯が左端＝E 問合せから描かれ、A 受注済・完了は x=608 と視野の外。スクロールできる印も無い）。マウント時に現在のステージを視野の中央へ寄せ、帯をレール（続きがある側だけ端が溶ける）にした。あわせてステージ8ボタンが 32px でタップ基準 44px を満たしていなかったのをスマホだけ 44px に広げた（PC は 32px のまま）
- **設定のデータビューアで見出しと操作ボタンの説明が文字化けしていた**（`操作` がそのまま表示。JSX のテキストと属性は `\u` エスケープを解釈しないため）
- 日常業務のやること・投入ログ・タスクダイアログ・ニュースで、44px 未満のタップ領域と寸法の段（36/40/44px）に無い 38px ボタンを揃えた
- 権限チップに内部ID `qsheet` が、通知の定時実行一覧に内部ジョブキーがそのまま出ていたのを日本語にした。DBバックアップ画面に本番VPSの接続先が直書きされていた（クライアントのJSは誰でも取得できる）のを docs 参照に置き換えた
- **検証環境の拠点・部屋がすべて二重に出ていた**のを直した。migration が固定IDで入れた既定の拠点・部屋を seed が同名・別IDで入れ直しており、部屋の空きのタブ・予約ダイアログ・料金表すべてに影響し「料金は未設定」の偽警告まで出していた。名前で既定の行を引いて使う形にした（8拠点28部屋 → 4拠点14部屋）
- **公式のUI検査が制作技術支援の本番4画面を1度も測れていなかった**のを直した。`verify-ui.mjs` が参照する `verify-onair` 文書が検証DBに無く、ずっと「文書が無いとき」の表示だけを測っていた。シードの文書にこのIDを固定した。あわせてカレンダー系の検証が毎回予約の手投入を必要としていたので、今日を含む予約（同室で重なる2件・10分・22時〜翌2時の日またぎ）をシードに入れた
- **財務の3画面で、いちばん読みたい値が列幅の配分ミスで潰れていた**のを直した。売上の編集ダイアログは単価が 76px に潰れて `1,500,000` が切れ（→122.7px）、請求・入金は GLS 番号が全行 `GLS-A00…` で**話数違いの21行を番号で1件も区別できず**（→21行すべてフル表示）、取引先は主キーの社名が 1280px でも 148px（→252px）だった
- **カレンダー②部屋の空きで、重なった予約が同一レーンに重ね描きされ下の予約が消えていた**（週表・香盤には入れた列分けがこの画面だけ漏れていた）。段で分けて全件読めるようにし、**表示窓 8:00–22:00 の外の予約が無表示のまま消えていた**（深夜帯が埋まった部屋が終日空きに見える）のも「↓22時後 N件」チップで出すようにした。帯を押すと予約の中身が読める（従来はスマホから確かめる手段が無かった）
- **機材の2画面が PC の中間幅（1024〜1350px）で商品名列 0px に潰れ、行の右端が切れていた**（1024px では 276px 分が横スクロールの手段なく不可視）。lg から左メニュー 248px が出るのに閾値計算がそれを見ていなかったのが原因で、2xl まで遅らせる段を足した。台帳の種別バッジが和文5字以上で右端4通りにずれていたのも、列単位の固定幅を渡せる口を `TableBadge` に足して1本に揃えた
- **制作技術支援の公開音声URL（ログイン不要の決まり）が、未ログインだとログイン画面へ強制送還されていた** — QRを配った視聴者が到達できない状態だった。401 での転送に公開パスの除外を足した。あわせてエディタのツールバーが 1280px で溢れて ON AIR・プロンプターの起動ボタンが視界外だったのを「…」に畳み、プロンプターが 375px で操作が +171px 画面外へ出ていたのと、存在しない台本で無限スピナーになり脱出できなかったのも直した
- CSP が `data:` のフォントを拒否し、**LINE Seed JP のサブセット20面が全アプリで読み込み失敗して代替書体で描かれていた**のを直した
- 実ブラウザの全画面検査（`npm run verify:ui`・89画面 × 375/768/1280）を **2712/2722 → 2724/2724** にした
