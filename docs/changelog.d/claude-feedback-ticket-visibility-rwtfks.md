**日常業務に「フィードバックチケット」を新設した**。GMO ONAiR 自体（どれかのブロックアプリ）
への要望・不具合報告を記録・追跡する仕組みが無く、口頭や Slack で流れて対応漏れが起きていた。
新しいミニアプリ（`/daily/feedback-tickets`）を足し、**起票は全ユーザー**（`dailyops:reader`
＝日常業務を開ける人なら誰でも）が題名・内容・**対象アプリ→対象の画面・機能の2段プルダウン**
（例：「案件管理・財務管理・カレンダー・設定」→「財務管理：受け取った書類」）・種別
（不具合/要望/その他）を添えて起こせるようにし、**対応状況**（未対応/対応中/対応済み/却下・
`dailyops:editor` が更新）を一覧で絞り込んで追える（`feedback_tickets` テーブル・migration 277。
`target_page` の候補は各アプリの左メニューの項目名に合わせてある）。ホームの「届いたもの」
にもタイルを足し、対応が要る件数（未対応＋対応中）を表示する。
検証: 新規マイグレーション（277番。`target_app`/`target_page` の組み合わせ検証を含む）を
検証DBに適用し、実際にサーバーを起動してAPI越しに正常系・異常系（アプリと画面の不一致・
未指定）を確認。`server`/`client-daily` の型チェック、`shared` の Vitest 1996件・全 lint
（`npm run lint` 主要スクリプト）通過。実ブラウザでの PC/スマホ確認は未実施。
