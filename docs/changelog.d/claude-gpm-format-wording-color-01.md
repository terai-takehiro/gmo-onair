**プロジェクト管理（GPM）の言葉づかいと色を、案件管理のフォーマットに揃えた**（先行PR #574 のモック `docs/design/v4/mockups/gpm-format-alignment.html` の22項目のうち「言葉づかいの統一」「色」の2表だけを対象にした1本目。見た目のレイアウトは変えていない）。①`gpm_open_items` の呼び名が「未解決事項／未確認／持ち帰り」の3通りに割れていた（一覧の列見出し・ダッシュボードKPI・詳細のタブ名・持ち帰りダイアログ・タスク一覧・議事録タブの登録先など）ので、すべて**「持ち帰り」**に統一した。②見積の `status='sent'` が一覧・GPM詳細タブで「提出済」・「送付済」に割れていたので**「提出済」**（案件管理の見積タブと同じ表記）に揃えた。③タスク一覧の見出しが左メニュー「タスクと持ち帰り」と食い違って「全プロジェクトのタスク」になっていたので**メニューと一致**させた（工程テンプレート側はメニュー・見出しとも既に「工程テンプレート」で一致していたため変更なし）。④表示形式トグルの `aria-label` が「表示形式を切り替える」「表示形式」の2通りだったので、既存の多数派（案件管理・GPMの一覧/タスク一覧と同じ）に揃えて**「表示形式を切り替える」**に統一した。⑤概要タブ末尾の「議事録はプロジェクト管理側のデータがまだ無いので出していません」という注記は、議事録タブが実装済みのため利用者に見える嘘になっていたので**削除**した。⑥動詞「作る／直す／足す／消す」が同じダイアログ内のボタン・タイトル・トースト文と混在していた箇所（プロジェクト作成・工程テンプレート作成・BOXフォルダ作成・体制メンバー追加・持ち帰り/タスク/工程の削除確認など）を**追加/編集/削除/キャンセル/保存**（wordingルール8）に揃えた。見積タブの「直すなら次の版をつくってください」は、共有部品 `EstimateItems.tsx` にある案件管理と同じ確立した言い回しなのでそのまま残した。⑦ガント・工程表のSVG色（今日線 `#f87171`・マイルストーン `#f59e0b`/`#b45309`・依存の矢印 `#64748b`）が旧世代の直書きのままだったので、`GpmGanttView`/`GpmGanttBar`/`GpmGanttExtras`/`GpmTimelineView` の4ファイルで1か所にまとめた `ganttColors.ts`（`--destructive`/`--warning`/`--muted-foreground` トークンの値を写した定数）から読むように直した。⑧セグメント（表示形式トグル・ガントのズーム）の角丸が `rounded-control` と `rounded-control-md` に割れていたので `rounded-control` に統一した（アイコン単体ボタンは既存どおり `rounded-control-md` のまま）。検証: `npm run typecheck`（client/server）、`npm run lint`、`shared` の Vitest 1996件、全通過。
