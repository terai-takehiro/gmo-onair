**DBスキーマのドリフト監査（1回目）の結果をまとめ、重要な前提の誤りを記録した**（コード変更なし）。前セッションが見つけたテーブル・FK差分（23テーブル・FK35件）を、全migrationファイル・全ブランチの git 履歴・アプリコード・`docs/roadmap.md`/`docs/ia.md`と突き合わせた。⚠️ **その結果、これらは「安全な実験ゴミ」ではなく、v3.2.0ロールバック（画面だけ旧版に戻し、DBは戻さなかった。migration `160_ai_structured_content.sql` の冒頭コメントに一次証拠あり）で取り残された、一度は本番公開されていた旧機能（Slack連携・隔週キープ・香盤表・運営マニュアル・案件コメント・通知設定・権限変更履歴等）の残骸で、本番DBにも同じ形で残っている可能性が高いと判明した。** 「migrationにもコードにも無い＝消してよい」という前提は成り立たないため、`docs/reviews/db-drift-audit.md` を新設して発見内容と対応表を記録し、既存の `joint_events` 系DROP判断（devの0行確認のみに基づく）も本番側の再確認が必要であることを明記した。あわせて `misc_inquiries.promoted_project_id`/`security_card_lendings.project_id` という新規の未追跡列+FKも発見した。次にやること（行数確認・機能ごとの作り直す/しない判断）は同ドキュメントに整理した。
