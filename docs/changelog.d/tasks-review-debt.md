**PR #644（タスク・依頼の再設計）のレビュー指摘を棚卸しに移した**（[docs/branching.md](../branching.md)「マージしたら、その PR のレビューを棚卸しに移す」）。マージすると Codex の指摘は GitHub の画面から消えるため、[docs/reviews/codex-findings-v4.md](../reviews/codex-findings-v4.md) に記録した。①1巡目（`823f80d`）で返った4件（**P1**「`LIMIT 200` に対して完了かどうかを見ずに並べていたため、優先度の高い完了済みが低優先の未完了を押し出し、動いている仕事が画面から黙って消えていた」／**P2**「完了フィルタ＋ボードで9マスすべて0件」「メモ履歴が375pxで本文1px」「チームの自分の行から自分あての個人タスクが作れた」）は**すべてマージ前に修正・返信・スレッド解決済み**なので、表に移す未対応の指摘は無い。実測での前後（0/5→5/5 件・本文 1px→325px ほか）を記録に残した。②⚠️ **その修正を載せた `cb9cf54` に対する2巡目のレビューは「Running」のままマージされ、マージの3分後に完了してさらに3件（P1×1・P2×2）を返した**（07:35:55 開始 → 07:39:08 マージ → 07:42:16 完了）。マージ済みコードの不具合なので**この PR で直した**: **(P1)** `listMyDelegations` は独自の ORDER BY を持っており、`ORDER_BY_PRIORITY` に入れた「未完了が先」が**この問い合わせには届いていなかった**（同じ直しを入れる場所が2か所あった）。完了ぶんまで取ると優先度の高い完了済みが「承諾済み」「差し戻し」を上限200から押し出し、一覧・件数・「あなたの番」から動いている依頼が消える（実測 **0/5 → 5/5 件**）。**(P2)** 依頼を「未対応に戻す」と `delegation_status='done'` が残るため完了の段に居座り「対応済にする」も出ない（＝一度戻すと二度と片づけられない）→ サーバーは `accepted` へ戻し、画面側も「完了は `is_completed` だけで決める」に直した。**(P2)** 9マスボードは選んだマスを `useState` の初期値でだけ決めており、絞り込みを変えると「完了 1」なのに「このマスにタスクはありません」が出たままになる → 絞り込みが変わったら作り直すようにした。あわせて「レビューが0件のままマージされた PR」の節にも**「Running のままマージは指摘なしではない・走っている間はマージを待つ」**として記録した。③**直していない既存の穴を ❌ のまま残した** — `dailyops` が閲覧の人にも依頼は届くのに承諾・辞退の操作は権限で出ない（受け取れるのに返せない）。何が変われば直すかも併記した。④CI が一時的に runner を確保できず両ジョブが2〜3秒で死んだこと（別ブランチでも同じ・attempt 3 で自然に復旧）も、次に同じ落ち方を見たときのために記録した。⑤Codexレビュー指摘（#648・P1）を受けてさらに直した — **❌ の項目を文章の段落で書いていたため、残数を数える `grep -c '^| .* | ❌'`（この文書の「数え方」）が拾わず、次の棚卸しで静かに消える状態だった**（#624 で一度直した形の繰り返し）。`| PR | 重み | どこ | 何が起きるか | 状態 |` の表に正式な行として書き直し、残数が 26 → 27 に増えることを確認した。あわせて `pr-watch` スキルの `references/pitfalls.md` に2件足した（「Codex が走っている間にマージすると指摘がマージ済みコードに向く」「直さない／まだ直していないは段落ではなく表の行で残す」）。⑥さらに自分の直しへの指摘（#648・P2）も直した — 「未対応に戻したら `accepted` に戻す」を `is_completed` が渡ってきたら**常に**書いていたため、**未返答・辞退・相談の依頼に `is_completed:false` を投げるだけで `accepted` に変わって**しまった（画面は送らないが API は MCP からも叩ける）。受け手の 承諾／相談／辞退 を飛ばせるうえ、差し戻しが依頼者の「あなたの番」から消える。**完了だった行を戻すときだけ**に絞った（実測: requested / declined / consulting / accepted はいずれも状態を保ち、done→false のときだけ accepted、accepted→true で done）。検証: `npm run typecheck:all`・`npm run lint`・`npm run test`（164ファイル）・`npm run build:all`・`node scripts/check-md-links.mjs` 通過。
