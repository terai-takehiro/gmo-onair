**PR #644（タスク・依頼の再設計）のレビュー指摘を棚卸しに移した**（[docs/branching.md](../branching.md)「マージしたら、その PR のレビューを棚卸しに移す」）。マージすると Codex の指摘は GitHub の画面から消えるため、[docs/reviews/codex-findings-v4.md](../reviews/codex-findings-v4.md) に記録した。①1巡目（`823f80d`）で返った4件（**P1**「`LIMIT 200` に対して完了かどうかを見ずに並べていたため、優先度の高い完了済みが低優先の未完了を押し出し、動いている仕事が画面から黙って消えていた」／**P2**「完了フィルタ＋ボードで9マスすべて0件」「メモ履歴が375pxで本文1px」「チームの自分の行から自分あての個人タスクが作れた」）は**すべてマージ前に修正・返信・スレッド解決済み**なので、表に移す未対応の指摘は無い。実測での前後（0/5→5/5 件・本文 1px→325px ほか）を記録に残した。②⚠️ **その修正を載せた `cb9cf54` に対する2巡目のレビューは「Running」のままマージされた** — 画面上は「4件とも解決済み」に見えるが**直したコードそのものはレビューを受けていない**ので、「レビューが0件のままマージされた PR」の節にも同じ危うさとして記録した。③**直していない既存の穴を ❌ のまま残した** — `dailyops` が閲覧の人にも依頼は届くのに承諾・辞退の操作は権限で出ない（受け取れるのに返せない）。何が変われば直すかも併記した。④CI が一時的に runner を確保できず両ジョブが2〜3秒で死んだこと（別ブランチでも同じ・attempt 3 で自然に復旧）も、次に同じ落ち方を見たときのために記録した。検証: `node scripts/check-md-links.mjs`・`npm run lint` 通過。
