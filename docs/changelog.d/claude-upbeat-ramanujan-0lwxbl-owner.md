**営業活動記録で「案件の担当者」で絞れるようにし、「期限なし」を「期限未設定」に揃えた**（利用者のご指示「それぞれ着手を」）。①これまでの絞り込みは活動を記録した人（記録者）だけで、「自分が担当する案件」を並べられなかったので、**案件の担当者**の欄を記録者とは別に足した（URL は `?owner=`。案件別・時系列の両方に効き、案件に紐づかない記録は担当者で絞ると出ない）。②全アプリに残っていた「期限なし」16か所（タスク・プロジェクト管理・標準工程・スマホの期限表示ほか）を「期限未設定」に置き換え、`scripts/check-ui-tokens.mjs` の `overdue-wording` で止めるようにした。あわせて、マージ時点で Codex の利用上限によりコード レビューが0件だった #727・#734 にレビューを付け直し、#734 の記録を `docs/reviews/codex-findings-v4.md` に足した。あわせて、付け直したレビューで届いた指摘（すべて P2）を直した: ①AI の整形を一度直してから全部元へ戻すと、前の修正の記録が残り「直した」と「無修正」の両方に数えられていた ②やり取りの編集が保存に失敗しても編集欄が閉じ、入れた値が消えていた ③時系列で別の人に絞っても「次のアクション予定」の帯は自分の分のままだった（帯も記録者・担当者の絞り込みに従うようにした）。検証: `npm run typecheck:all`・`npm run lint`・`npm run test`・`npm run build:changed`・`npm run verify:ui`。
