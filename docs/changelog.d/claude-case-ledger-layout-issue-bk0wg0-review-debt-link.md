**版の下書きに書いたリンクが3か所のうち2か所で壊れていたのを直し、あわせて
「レビューは来ていた。来る前にマージしていた」ことが分かったので棚卸しに書いた**
（Codex のレビュー #169・P2。**私が #169 で入れた穴**）。
①⚠️ **版の下書きは3つの場所を移動します** — `docs/changelog.d/` に置き、
`release:notes` がルートの `CLAUDE.md` / `README.md` へ写し、次のリリースで
`docs/version-history.md` へ移します。そこに `(reviews/codex-findings-v4.md)` と
**相対リンク**を書いたので、**`docs/changelog.d/reviews/…`**（無い）と
**ルートの `reviews/…`**（無い）を指していました。正しく解決するのは**アーカイブに
入ってからだけ**で、⚠️ **下書きを読む人にも、次のリリースノートを読む人にも踏めません**。
②⚠️ **どの相対リンクを書いても必ずどこかで壊れます。** ルートの `CLAUDE.md` が使っている
`(docs/reviews/…)` も、アーカイブへ移った瞬間に `docs/docs/…` になります。
`docs/version-history.md` が**リンクにせず素のパス**で書いているのはこのためで、それに揃えました。
③⚠️ **`npm run lint` では捕まりません** — `check-links.mjs` が見るのは `App.tsx` の
ルート表だけで、`.md` の中は1つも見ていません。**気づけたのは Codex のレビューだけ**でした。
④**その Codex のレビューは約1分で付いていました。** 直前の版で「レビューが1件も
付かないままマージした PR が4本目」と書きましたが、**Codex そのものは動いています** —
4本は「来ない」のではなく**「来る前にマージしている」**ほうだった可能性が高い、ということです。
実際 **#169 はレビューが付いた 33 秒後にマージ**されました（#159 の「14秒前」と同じ形）。
⚠️ **数え方を「レビューが 0 件だったか」から「レビューを見てからマージしたか」へ
変えたほうがよさそう**ですが、**まだ4本しか手元にないので決め打ちせず**、記録だけ残します。
⑤⚠️ **その1件は直らないまま `main` に入りました** — **この文書が warn している
「マージすると指摘が画面から消える」形そのもの**です（143 件が埋もれたのと同じ）。
表の1行目に足したうえで、この PR で直しています。
⑥**アプリのコードは1行も触っていません**（`docs/` の3ファイルだけ）。DB 変更なし。
