**版の下書きに書いたリンクが3か所のうち2か所で壊れていたのを直した**
（Codex のレビュー #169・P2。**私が #169 で入れた穴**）。
①下書きは `docs/changelog.d/` → ルートの `CLAUDE.md`/`README.md` → `docs/version-history.md` と
**移動する**のに相対リンクを書いたので、**下書きを読む人にも次のリリースノートを読む人にも
踏めません**でした。⚠️ **どの相対リンクを書いても必ずどこかで壊れます**（ルートの `CLAUDE.md` が
使っている形も、アーカイブへ移ると `docs/docs/…` になる）。アーカイブと同じ**素のパス表記**に揃えました。
②⚠️ **`npm run lint` では捕まりません** — `check-links.mjs` は `App.tsx` のルート表しか見ず、
`.md` の中は1つも見ていません。**気づけたのは Codex のレビューだけ**でした。
③⚠️ **その指摘は、直らないまま `main` に入りました**（#169 はレビューの **33 秒後**にマージ）。
④**#169 にはレビューが約1分で付いていました。** 直前に「レビューが付かないまま
マージした PR が4本目」と書きましたが、**Codex は動いています** — 4本は「来ない」のではなく
**「来る前にマージしている」**ほうだった可能性が高い、ということです。棚卸しに記録しました。
