**`main` の CI が赤くなっていたのを直した**（マイグレーションの番号がぶつかっていた）。テロップCG の回（#511）が先に 250〜258 を使っており、あとから入った財務ダッシュボードの回（#512）の `250_finance_recognition_date_index.sql` と**同じ 250 番**になっていた。`check-migration-numbers` が落ちるので、**そのままだと以後どの PR も CI が通りません**（実際に #514 が落ちた）。財務側を **259 に改名**した（あとから足した側を動かす、という検査の指示どおり）。⚠️ **既に 250 として流れた環境で 259 として流れ直しても何も起きません** — 中身は `CREATE INDEX IF NOT EXISTS` だけなので。実 Postgres で「250 の適用記録が残ったまま 259 を流す」を再現し、索引が二重にならないことを確認済み。
