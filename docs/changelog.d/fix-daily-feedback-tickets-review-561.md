**フィードバックチケット（#561）に付いた Codex レビュー指摘6件を直した**。マージ後に届いた
レビューで、①一覧の問い合わせに上限が無く溜まる一方だと重くなる②一覧行がキーボードで開けない
③対応コメントを空にして保存しても直前の値に戻ってしまう④絞り込みチップの件数が上限つきの
一覧から数えていて実態とずれうる⑤対応コメントだけ直しても「対応済みにした時刻」が保存のたび
今に書き換わる⑥題名・内容に文字列以外を送ると500になる、の6件が見つかった。一覧に上限
（既定50・最大200・「入ってきた情報」と同じ考え方）を付けて検索もサーバー側（ILIKE）に寄せ、
絞り込みチップの件数は上限の影響を受けない `GET /feedback-tickets/counts`（対象アプリ別の
内訳を追加）から読むようにし、行のキーボード操作・対応コメントの明示的なクリア・
対応済みにした時刻の保持・非文字列入力の400化を直した。
検証: 検証DBで6件すべてを個別に再現・修正後に解消したことを確認、`server`/`client-daily`
の型チェック、`shared` の Vitest 1996件、lint 通過。
