**ドキュメント全体を現時点の事実に合わせて体系的に書き直した**。版上げのたびに `README.md` が版の全文と「旧 v…」を4件抱えて入口として読めなくなり、`CLAUDE.md`（毎ターン文脈に載る）の6割が版の履歴で、`docs/ops/vps-setup.md` は単一アプリ時代の手順のまま、検証環境の認証を「ユーザーカード選択式」と書いた文書が残るなど、文書ごとに古さの度合いが違っていた。①`README.md` は入口の文書として書き直し、版は `**現在のバージョン**: vX.Y.Z — **見出し**` の1行だけにした（`npm run release:notes` もこの形だけを書くよう `scripts/collect-changelog.mjs` を直し、全文は `CLAUDE.md` の最新3件と `docs/version-history.md` に置く）。②`CLAUDE.md` は「全体に効く決めごとだけ」に整理し、直近3件の版は見出し＋2〜3文の要約にして全文を `docs/version-history.md` へ移した（画面の「バージョン履歴」は同じ版なら長いほうを出すので表示は変わらない）。③`CONTRIBUTING.md`・`docs/README.md`・`docs/branching.md`・`docs/deploy-pipeline.md`・`docs/ops/*`・`docs/guide/*`・`docs/changelog.d/README.md`・`docs/ia.md`・PR テンプレートを現行の仕組みに合わせて書き直し、経緯の語りは `docs/archive/2026/` へ移した。④各アプリの `CLAUDE.md`（client・client-daily・client-equipment・client-techops・client-live・client-awards・shared）を「いま効くルール」だけに絞り、外した経緯は `docs/reviews/*-build-log.md` に記録として残した。⑤計画・設計・レビューの各文書には状態欄（状態・最終確認・位置づけ）を付け、役目を終えた文書（引き継ぎ書・リリース前の作業場・当時のスナップショット）は `docs/archive/2026/` へ移し、`docs/reviews/README.md`・`docs/guide/README.md` の索引を足した。検証: `node scripts/check-md-links.mjs`・`npm run check:version`・`node scripts/generate-version-history.mjs`（513件・v4.6.10 の本文がアーカイブの全文で出ること）・`node scripts/collect-changelog.mjs 4.6.11 --dry`・`npm run lint`・`npm test`。
