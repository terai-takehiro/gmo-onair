**Wiki の段A（器）の残りを作った**（PR #719 は設計・モックと表・共通処理までで先にマージされたので、その続き）。①**サーバーの読み取り口8本**（スペース一覧・スペース1件・ツリー・ページ1件・履歴一覧・版1件・閲覧の記録・ホーム）と**閲覧範囲の判定**。閲覧範囲はスペース単位で、読めないページは 403 ではなく **404** を返して存在ごと隠す（403 だと「そこに何かある」ことが伝わってしまう）。下書きは書いた人・担当・区画の管理者だけが読め、ツリーには「公開＋自分の下書き」しか出さない。②**新しいワークスペース `client-wiki`**（`/wiki`・ポート5181）の4画面（ホーム・スペース・ページ・履歴）。本文は react-markdown＋remark-gfm で描き、**`rehype-raw` は入れない**（本文に `<script>` や `onerror` が混ざっても素通しにしない。AI が書いた本文も人の本文も同じここを通る）。足した拡張は注意書き（`> [!NOTE]` ほか4種）・ONAiR カード（段落1本のリンクのときだけカードにする）・折りたたみ（`<details>`）の3つだけ。③アプリ登録（`shared/src/client/apps.ts`）・権限の区画 `wiki`・`serveApp('/wiki')`・Dockerfile のビルド段と画像の保存先・検査スクリプト12本の配線。**編集・データベース・検索・AI・見直しは段B〜F**（画面からはまだ書けない）。
**ページに「期限切れ」という言い方をやめた**（利用者のご指摘「Wiki なので期限切れという概念はないと思います」）。Wiki のページは見直し予定日を過ぎても中身が無効になるわけではないのに、赤いバッジで「期限切れ」と出すと「もう使えない」と読まれてしまう。バッジは**「要見直し」**・色も異常の赤ではなく情報の色にし、項目名は「見直し期限」→**「見直し予定」**、添える文は「10日 超過」→「10日前」に直した。見直しの仕組み自体は判断#10 のとおり残している（予定日は任意・既定なし。入れたページだけ見直しに出る）。
**ナビの列を1本に減らし、本文の幅を 608px → 860px にした**（利用者のご指摘「サイドタブが増えすぎて、この UI だとすごく窮屈」）。「共通の左メニュー 248px ＋ Wiki のツリー 264px ＋ 本文 ＋ 情報 320px」の4列で、1440px の本文に 608px しか残っていなかった。しかも共通メニューの「スペース」一覧とツリーの見出しで**同じものを2回出していた**。ツリーを共通の左メニューの中へ移し（`sideMenuSlot` への差し込み。予定のミニカレンダーと同じ先例）、右の情報パネルを開閉式にして既定を閉じにした。閉じている間は目次と情報を本文の下に続けるので、どちらの状態でも情報に辿り着ける。実測は 1440px で 閉じ 860px（最大幅）／開き 792px、1280px で 860px／632px。**モックが問題を隠していた**（`Page.dc.html` は共通の左メニューを描かず3列に見えていた）ので、モックにも共通メニューを描いてその中にツリーを入れた。
**折りたたみ（`<details><summary>`）が描けていなかったのを直した**。`rehype-raw` を入れない以上、生の HTML は描かれずに落ちるため、設計 §4-2 の表にある「折りたたみ」だけが平らな文字として出ていた。注意書きと同じ作法（元の文字列から形を見つけて自前の部品で描く）にした。⚠️ **閉じ（`</details>`）が無い開きは切り分けない** — 書きかけの本文を全部畳むと、保存はできているのに画面から消えて見える（`shared/tests/wikiFoldSplit.test.ts` の8件で固定）。あわせて `shared/src/wiki/` と `server/src/contexts/wiki/wiki-markdown.ts` の意図的な複製が同じ答えを出すことを固定する突き合わせ試験（`wikiMarkdownParity.test.ts`・6件）も置いた。
検証: `npm run typecheck:all`（0 errors）・`npm run lint`（0 errors / 54 warnings＝着手前と同じ）・`npm run test`（94件）・`npx vitest run --root shared`（2,548件）・`npm run build -w client-wiki`・実ブラウザ（Chromium・検証用 Postgres）で 1440px / 1280px / 375px の4画面を描画し、横はみ出し 0・JS エラー 0・注意書き／チェックリスト／表／ONAiR カード／折りたたみの描画と、情報パネルの開閉が再読み込み後も残ることを確認。権限は区画なし=403・他人の下書き=404・無い id=404 を実測。
