**PR #737（技術資料の後続の直し）のレビュー指摘を棚卸しに記録した**（`docs/reviews/codex-findings-v4.md`）。Codex の指摘6件（すべて P2）は PR の中で直して解決済みで、未対応は無い。Codex の利用上限のため `813ab0b` 以降のコミットがレビューされていないことと、次に目を通すファイルを書き残した。#731 の表で「後続の PR」としていた3件の状態を #737 に書き換えた。
