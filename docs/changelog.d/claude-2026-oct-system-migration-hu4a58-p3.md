**2026年10月の事業再編 P3（GMOコストセンター・旧GLS-B）をマルチエージェントで実装した**（`docs/reorg-2026-10-plan.md` §4.7・§6）。①GMOに解決した案件（`legal_entities.kind='cost_center'`）は売上を持たないため、`POST`/`PUT /revenues`を409`NO_REVENUE_ENTITY`で止めた（判定はサーバー新設`isProjectCostCenter()`。⚠️`gls_category='B'`では判定しない——`org_transition.state`が`off`／未改番のあいだはB案件の`entity_code`もまだ'GSS'のままなので、この判定は今日はどの案件にも通らず既存の挙動を変えない）。②新設`gpm-budget.service.ts`で「予算（受理済み`status='accepted'`の見積の最新版合計）対 実績（確定した仕入合計）対 残」と月次コスト推移を集計し、`GET /gpm/cost-dashboard`（全コストセンター案件の一覧＋月次推移）と`GET /gpm/projects/:id/budget`（案件単体）を新設した。③営業見通し（パイプライン）はコストセンター案件を常に除外するようにした（GMO自身は売上を持たないため、entityCode指定の有無に関わらず除外——社内取引の除外条件とは異なる扱い）。④GPMのプロジェクト詳細は、コストセンター案件だけ「請求」タブを新設`BudgetTab.tsx`（予算・実績・残・進捗バー・月次推移・仕入一覧、スマホでも開ける作り）に差し替えた（普通の会社の案件は従来どおり）。⑤新設`CostDashboardPage.tsx`（KPIカード・月次推移・案件別内訳の3部品）をGPM配下`/gpm/cost-dashboard`に追加し、左メニュー・スマホ対応表にも登録した。⚠️見積の受理（`accepted`にする）操作が既存のGPM見積タブに無いため、予算は当面0のまま——別途フォローアップが要る。⚠️検証中に見つけて直した既存バグ（本タスクのスコープ外・同日の別コミット`8800222d`「全INSERTにentity_codeを明示し、282でNOT NULL化した」が原因）: `gpm.service.ts`の`POST /gpm/projects`のINSERT文で列16個に対しVALUESの式が15個しかなく、GPMプロジェクトの新規作成が常に500で失敗していた。実サーバーでの検証中に発見し欠けていた1つの`?`を足して直したうえで、同じコミットが変更した残り33箇所（13ファイル）を全数手作業で列数とプレースホルダ数を突き合わせて棚卸しし、他に見落としが無いことを確認した。検証: `server`/`client`の型チェック、`shared`のVitest 2004件、検証DBの作り直し（migration 1〜285通し適用＋seed）、`npm run lint`一式（`App.tsx`はコストダッシュボードのルート追加で401→409行になったため`check-file-size.mjs --update`で基準を更新——ルート定義を1ファイルに集約する設計のため新画面のたびに増える性質の行数増であり、`ProjectDetailPage.tsx`と合わせて分割は別課題として残す）、ESLint(0 errors)、実サーバーでのHTTPレベル確認とPlaywrightでの実ブラウザ確認（PC/スマホ）。
