**顧客・仕入先の登録を「取引先マスター（`companies`）」に一本化した（Phase 1）。** 案件管理の顧客一覧・財務の仕入先タブ・Excel取込・決算取込・内覧会予約・投入口（AIに任せる）・MCP・シードの合計10か所が、取引先マスター（`companies`）を経由せずに `customers`/`vendors` へ直接書き込んでいた。取引先マスターは元々「顧客・仕入先を束ねる唯一の正のマスター」として作られていた（migration 061）のに、この10か所だけが素通りしていたため、取引先マスターに対応行の無い「孤立した顧客・仕入先」を作り続けていた。共通の登録処理（`server/src/shared/services/company-directory.service.ts`）を新設して全10か所を差し替え、既存の全FK（`projects.customer_id`等）は`customers`/`vendors`の id を指したまま変えていない。既存の孤立データは migration 195 で取引先マスターへ埋め戻した。顧客一覧・財務の仕入先タブから社名や連絡先を直したときも、取引先マスター側の同じ会社の行に写るようにした（今までは GMOグループの印だけしか写していなかった）。次の段（各ページを役割で絞った共通の会社一覧UIに統合する・FKを`companies.id`へ直接張り替える）は別PRで進める。**検証**: `npm run test`（shared）1,134項目、`npm run typecheck -w server` exit 0、`npm run lint` 0 errors / 63 warnings（変更前と同数）。検証用Postgresで新規シード後、`customers`/`vendors`ともに孤立0件・全件が`companies`に紐づくことを実測。
