**制作技術支援に新しいミニアプリ「運営マニュアル」の設計とモックアップを起こした**（実装はまだ1行も書いていない）。①制作技術支援には当日の香盤（スケジュール表）・尺と担当（進行台本）・デッキ（収録設定）・配信先（配信設定）・借りる機材（レンタル機材検索）・体制と連絡先（案件管理）が**もう全部入っている**のに、現場に配る A4横の運営マニュアルは毎回 PowerPoint で一から打ち直していた。元の資料を直しても冊子は直らないため、当日、手元の紙と画面が食い違う。冊子の見た目（表紙・柱・章立て）も人ごとにバラバラで前の案件から持ってこられず、配信のストリームキーや会議のパスコードを気づかずに紙へ写してしまう経路もあった。②各ミニアプリの情報を「差し込みブロック」としてA4横のページに置き、1冊に組んで PDF で配るミニアプリを設計した。利用者に見せる語は4つ（冊子・ページ・ブロック・ひな形）、動詞は3つ（差し込む・置く・書き出す）。画面は PC 4枚＋スマホ 1枚で、編集の手つきは PowerPoint に合わせる（つかんで動かす・8方向のつまみ・矢印キー 1mm・Alt 複製・Ctrl+Z・すいつき・整列）。文字の意匠は Web の自由度をそのまま使い、袋文字（`-webkit-text-stroke`）・影・グラデーション文字（`background-clip:text`）・縦書き・長体まで出せる。差し込んだブロックは生きていて、元が変わると「元が変わりました」で知らせ、**確定すると全ブロックがその時の中身のまま凍る**（配った紙と画面が食い違わない）。配信の鍵・合言葉・会議のパスコードは差し込んでも既定で伏せ字のままにし、サーバー側（`resolve`）が平文を返さない。PDF はサーバーに Chromium を入れず、進行台本の印刷（`client-techops/src/components/editor/previewExport.ts`）と同じブラウザ印刷（`@page size: 297mm 210mm`）で出す。③データは表3本（`qsheet_manuals`・`qsheet_manual_pages`・`qsheet_manual_templates`）で、ページ1枚＝1行・ブロックは JSONB。進行台本の `qsheet_documents.data`（Yjs の所有物・サーバーは書かない）とは別物で、こちらはサーバーが書き `updated_at` の突き合わせで取り合いを見つける。作る順は段A〜E（段D の PDF まで通して初めて使える）。④設計書は `docs/design/v4/production-manual.md`、モックは `docs/design/v4/mockups/native/production-manual/`（PC 4枚・スマホ 1枚・A4横の実寸 1枚・考え方の地図 1枚。押せる試作）。名前・「ブロック」という呼び名（進行台本の列の別名と重なる）・確定で凍らせる運用・秘密の伏せ字・同時編集の要否など7件は設計書 §10 に「決めていただきたいこと」として残し、回答を待ってから着手する。検証: `node scripts/check-md-links.mjs`・`npm run lint`。

**運営マニュアルの書体・ウェイト・送りの決めごとを足し、モックの取りこぼしを直した**（設計 §6-5）。①ご指示に沿って「書体は LINE Seed JP と Noto Sans JP が基本／ウェイトは全部使えるように／送りはプロポーショナルが基本で、デザイン上そうしたいところは等幅」を設計に落とした。ウェイトは**その書体が実際に持っている段だけを出す**（押しても変わらない欄を作らない・ブラウザが合成した偽の太字を紙に出さない）とし、Google Fonts の CSS を実際に取って段を確かめた——LINE Seed JP は 100/400/700/800 の4段（200・300・500・600・900 は存在しない）、Noto Sans JP は 100〜900 の9段。書体を変えたときはいちばん近い段へ自動で寄せる。欧文専用の書体（Roboto Condensed 等）は和欧混植で行がガタつくため選択肢に出さず、詰めたいときは長体（`transform: scaleX()`）で行う。送りは既定が `palt`（`base.css` の `body` と同じ値）で、時刻・番号・金額など数字が縦に並ぶところだけ `palt 0` ＋ `tnum 1`（`tokens-v4.css` の `.font-number` と同じ値）。**同梱の LINE Seed JP はいま 400/700/800 の3段しか入れていない**ため（`scripts/vendor-fonts.mjs` の `CSS_URL`）、段A の前に `wght@100;400;700;800` へ直して入れ直すことと、`client-techops/index.html` の Noto Sans JP に `100;200` を足すことを作る順に書き込んだ。②あわせてモック8枚を機械的に見直し、見つけた不具合を直した——差し込みカタログで16項目中14項目が右パネルに別の項目（体制・連絡先）を出していた（「WEB会議」では伏せ字の警告そのものが出なかった）、ページのサムネを押しても何も変わらなかった、ひな形のブロック（柱）が自由ブロックの枝に流れていた、スマホでページをめくると別のページの題が出た、`<button>` の中に `<div>` があった、表紙のサムネ・差し込みカタログ・考え方の地図・A4横の実寸ページで中身が枠からはみ出していた、冊子番号とページ番号と時刻が画面ごとに食い違っていた、英語が1か所紛れていた。検証: `node scripts/check-md-links.mjs`・`node scripts/check-changelog.mjs`・モック8枚の構文とキャンバス定義の機械検査。

**運営マニュアルを「紙に刷る前提」で組み直し、長体と『1行に収める』を足した**（設計 §6-5-1・§6-6）。①ご指示「印刷が基本であることを意識し、インクを大量に使用するようなデザインは回避したい」に応え、紙面の設計規律を決めた——地は白でベタ塗りの面を置かない／面にグラデーションを使わない（文字の色に使うのは可・塗る面積が文字の形のぶんだけ）／色は罫・小さなバッジ・文字にだけ使う／バッジは塗りつぶさず枠線で組む／表の見出しは灰色の帯ではなく上下の罫／大事な行は塗らずに左の太罫で示す（白黒でも分かる）／紙面に影を置かない／写真は原則載せず会場図は線画で／白黒で読めること／罫は 0.25mm 以上。塗り面積の目安は1枚あたり 10% 前後。モックもこれに合わせて直し、表紙が A4横の 44% を濃いグラデーションで埋めていたのを白地＋細い罫に、表の見出しの灰色の帯を上下の罫に、大事な行のベタ塗りを左の太罫に、QR の黒地を白地に変えた。⚠️ これは**紙面（ページの中身）だけ**の決めごとで、画面そのもの（一覧・編集画面の UI）は v4 の共通トークンのままにしてある。書き出しの画面には「インクの目安」（塗り面積・40% を超えたら知らせる）と「色を使わない（白黒で刷る）」を置いた。②ご指示「長体が使えるようにして欲しい（1行に収めたい時があるので）」に応え、長体（70〜130%・10%刻み・`transform: scaleX()`）に加えて**「1行に収める」**を足した。入り切らないぶんだけ自動で詰め、下限 70%、それでも入らなければ詰めるのをやめて省略記号にする。⚠️ 自動の長体は**1回だけ測ってブロックに `style.scaleX` として書き込み、描くときは測らない**作りにすることを設計に明記した——`docs/design/v4/mockups/DESIGN_POLICY.md` の「測って書き戻す処理を入れない」は、v4 のモックで同じ長体フィットを入れて再測定と再描画が連鎖し画面が固まった実績に基づくため。検証: `node scripts/check-md-links.mjs`・`node scripts/check-changelog.mjs`・モック8枚の構文とキャンバス定義の機械検査。

**運営マニュアルの「決めていただきたいこと」8件に全件の回答をいただき、設計とモックに反映した**（設計 §10）。①名前と語＝「ブロック」（進行台本の「列（ブロック）」と重なるため、台本側は別PRで「列」に統一する）。②資料番号の接頭辞＝`OM`（`OM-0001`。SB・SD と並ぶ・あとから変えない）。③確定したあと直すときは**版を上げる**——manager が「確定を解く」→ 直す →「確定する」で `rev` が +1 になり、柱に `OM-0007 rev.2 ・ 2026/08/22 14:00 時点 ・ 5 / 12` と出る。現場の紙が古いかどうかを版だけで見分けられる（`qsheet_manuals` に `rev` 列）。④秘密（配信の鍵・合言葉・パスコード）を紙に出す解除は**誰でもできる**（既定は伏せ字のまま）。止めない範囲の安全策を3つ置いた——出すときに確認を1回／鍵を1件でも出している冊子は表紙と柱に「取扱注意 ── 配信の鍵あり」が自動で付く（消せない）／誰がいつ出したかをブロックの `link.reveal` に残す。サーバー（`resolve`）は `reveal` が入っている項目だけ平文を返す。⑤同時編集は**冊子まるごとの編集ロック**——誰かが編集中は他の人は読むだけ、10分手が止まると自動で解ける、manager は強制的に引き継げる、読むだけの人は「編集を代わってほしい」と申し出られる（`locked_by`/`locked_at`/`lock_requested_by`/`lock_requested_at` と API 4本）。ロックがあっても `updated_at` の突き合わせは最後の砦として残す。⑥冊子は**案件メンバー全員**に自動で見える（スケジュール表 第2版と同じ）。⑦v1 に **AI は入れない**。⑧紙面のベタ面・グラデーション・写真は**置けないようにはしない**——§6-6 は「既定のひな形と差し込みブロックの作り方」に位置づけを変え、利用者が置くのは止めず「インクの目安」（塗り面積・40% で知らせる・止めはしない）で伝えるだけにした。目安は描いた絵を測るのではなくブロックの値から計算する（§6-5-1 と同じ理由）。モックにも編集ロックの表示・交代の申し出の帯・版（rev）・鍵を出すときの説明を入れた。検証: `node scripts/check-md-links.mjs`・`node scripts/check-changelog.mjs`・モック8枚の構文とキャンバス定義の機械検査。

**運営マニュアルの段A（器）を実装した**（設計 `docs/design/v4/production-manual.md` §9・マルチエージェントで実装）。①サーバー: `qsheet_manuals`/`qsheet_manual_pages` の一覧・詳細・CRUD・ページの追加/削除/並べ替えを `server/src/contexts/qsheet/{access.ts, services/manual.service.ts, routes/manuals.routes.ts, routes/manual-pages.routes.ts}` に実装。アクセス制御はスケジュール表の `canAccessSchedule` と同じ形（作成者・案件メンバー・system_admin のみ可視・404で存在秘匿。番組所有の冊子は作成者/管理者のみ）。冊子は作成時に必ず資料番号（`OM-xxxxxx-xxxx`）を発番し、空ページ1枚と一緒に1トランザクションで作る。最後の1ページは削除できない。②クライアント: `/techops/manuals`（一覧・?project=/?program=絞り込み・冊子を作る）と `/techops/manuals/:id`（題のインライン編集・ページの追加/削除/上下並べ替え/章名・題の編集。紙面のブロック編集はまだ無く「紙面はまだありません」のプレースホルダを表示）を `client-techops/src/pages/opsmanual/` に実装し、左メニュー・ジャーニーのタイル（`MiniAppTiles.tsx`）・`pcOnlyScreens.ts`（一覧はスマホ可・詳細はPC専用）に配線した。③設計書 §5-3 が指定していた配置先 `client-techops/src/pages/opsmanual/`（`shared/src/client/manual/` の使い方説明モーダルとの名前衝突を避けるため）に、レビューで一度 `pages/manual/` 配下に置かれていた実装を移設した。④文字入力欄（冊子の題・ページの章名/題）はレビュー指摘を受けて `BufferedInput` に統一（IME変換中の二重入力を防ぐ・値が変わっていなければ再送しない）。段Aの範囲外（紙面のブロック編集・確定/版・編集ロック・秘密の伏せ字解除・PDF書き出し・ひな形・AI）は今回実装していない。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops`・`node scripts/check-collab-parity.mjs`・`node scripts/check-migration-numbers.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors・ベースラインと同じ54件の既存警告のみ）・`npm run test`（13/13 pass）。

**運営マニュアルの段B（自由ブロックと紙面の操作）を実装した**（設計 `docs/design/v4/production-manual.md` §9・マルチエージェントで実装）。①紙面（`ManualCanvas.tsx`・A4横・mm単位）に文字・図形（矩形/角丸/円/線/矢印/吹き出し）・画像・表・QRの5種の自由ブロックを置けるようにした。つかんで動かす・8方向のつまみで伸縮（Shiftで縦横比を保持）・回転つまみ（15°刻み・Shiftで自由角度）・矢印キーで1mm移動（Shiftで0.2mm）・Ctrl+Dで複製・Delete/Backspaceで削除・Ctrl+Z/Ctrl+Shift+Zで元に戻す/やり直す（ページ単位で直近50手）に加え、他ブロックの端・紙面中心・版面余白へのすいつき（`manualCanvasGeometry.ts`）を実装した。②各ブロックの中身は種類ごとの専用部品（`blocks/*.tsx`）が編集し、右パネル（`BlockInspector.tsx`）で大きさ・太さ・色・そろえ・図形の種類/線を変えられる。文字・表の入力欄は `BufferedTextarea`/`BufferedInput`（IME保護）。画像は既存の `/techops/upload-image` をそのまま使う。③サーバー（`manual.service.ts`・`manual-pages.routes.ts`）は `updatePage()` にページ単位の楽観ロックと `blocks`（JSONB）の保存を追加した。クライアントは打つ/動かすのを止めて1.5秒で自動保存する（`useManualPageAutosave.ts`）。差し込みブロック・確定/版・編集ロック・秘密の伏せ字解除・PDF書き出し・ひな形・AI は今回実装していない（段C以降）。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors・ベースラインと同じ54件の既存警告のみ）・`npm run test`（shared 165ファイル/2269件・server 13/13 pass）。

**段Bのレビュー指摘8件（重複を除き実質6件）を確認し、正当なものを直した。** ①[blocking] `ManualCanvas` を `key` 無しでレンダーしていたため、ページを切り替えても同一インスタンスが再利用され、`useManualHistory` の `pastRef`/`futureRef`（ページ単位のはずの undo 履歴）が前のページの内容を持ち越していた。切替後に Ctrl+Z を押すと、いま選んでいるページの `onCommit` に**別ページの旧ブロック配列**が渡り、1.5秒のデバウンス経由でサーバーへ無警告のまま上書き保存されるデータ破壊だった。`ManualDetailPage.tsx` に `<ManualCanvas key={currentPage.id} ...>` を足し、切替のたびに再マウントして履歴をリセットするようにした（指摘1・6は同一内容の重複）。②[should-fix] `BlockToolbar`（＋追加）と `BlockInspector`（書式変更・複製・削除）が `ManualCanvas` 内部の `useManualHistory.commit` を経由せず undo に乗らない非対称があった（指摘2・7も重複）。`ManualCanvas` を `forwardRef` にして `commit` を公開し（`ManualCanvasHandle`）、`ManualDetailPage` からツールバー・右パネルの `onCommit` をこの `commit` 経由に配線し直した。③[should-fix] 8方向リサイズで Shift を押しても縦横比が保たれない（設計 §6-2 の明記要件）。`applyResize()` に `aspectLock` 引数を足し、角のつまみは大きく動いた側の軸に、辺のつまみは掴んだ軸の拡縮率をもう一方の軸にも連動させてアンカー中心で伸縮するようにした。④[should-fix] 複数選択（囲む・Shift で足す・整列・等間隔・重なり）とスペース+ドラッグのパン・Ctrl+ホイールのズームが未実装で、他の段外し機能と違い「意図的に見送った」注記も無かった。設計 §9 の段バケット表が「整列」を段Bのスコープと明記しているため実装した——複数選択は `useManualMarqueeSelect.ts`、整列・等間隔・重なりは `manualCanvasGeometry.ts` の純粋関数（`alignBlocks`/`distributeBlocks`/`reorderZ`）＋浮動ツールバー `ManualSelectionToolbar.tsx`、パンは `useManualPan.ts`、ズームは `ManualCanvas` の内部状態＋Ctrl/⌘+ホイール。1ファイル400行を守るためこれらを個別のフック/部品に切り出した。⑤[should-fix] 紙面の座標計算にテストが1件も無かった。`shared/tests/opsmanualCanvasGeometry.test.ts` を新設し30件のユニットテストで固定した（Shift 縦横比保持・整列6方向・等間隔・重なり・すいつき・回転角度変換を含む）。⑥[should-fix] ページの章名/題編集（楽観ロック未対応）と紙面の自動保存（`expected_updated_at` 必須）が同じ行の `updated_at` を取り合い、同一ユーザーの操作なのに409衝突と誤判定され保存が消える指摘は、章名/題と紙面で楽観ロックの対象を分ける設計判断（別カラムでの版管理、またはリトライ）が要り、今回の段B（紙面の汎用メカニクス）の範囲を超えるため見送った。ユーザーに確認のうえ対応する。⑦[nit] `manual.service.ts`・`manual-pages.routes.ts`・`shared/src/opsmanual/types.ts` 冒頭のコメントが「ブロック編集は段B以降・未実装」のまま段Bの内容と矛盾していたのを、実装済みである旨に更新した。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops`・`node scripts/check-file-size.mjs`・`npm run lint`（0 errors）・`npm run test`（shared 165ファイル / 2299件 pass）。

**運営マニュアルの段C（差し込みブロック）を実装した**（設計 `docs/design/v4/production-manual.md` §4-3・§5-4・§7-2・マルチエージェントで実装）。①4人が並行して作った部品（差し込み元12種の resolver・レジストリ・差し込みカタログ `InsertPanel.tsx`・中身描画 `linked/*.tsx`・右パネル `LinkedBlockInspector.tsx`）を配線し、動く1つの機能にした。サーバーに `resolveLinkedBlock`/`listAvailableLinkedBlocks`/`listLinkSourcesFor`（`manual-resolve.service.ts`）と3本のルート（`GET .../resolve`・`.../link-catalog`・`.../link-sources`）を新設し、`manuals.routes.ts` と同じ `canAccessManual` のゲートを通す。`resolve` は同じ (差し込み元・sourceId・伏せ字解除の範囲) を2回解決しないよう重複排除したうえで並行に解決し、1件の失敗（例外）で他のブロックの結果まで失わないよう個別に捕まえる。②段Bの型変更（`ManualBlock` が `kind:'free'|'linked'` の判別可能な union になった影響）で壊れていた4ファイル（`ManualBlockContent.tsx`・`ManualCanvas.tsx`・`ManualBlockView.tsx`・`BlockInspector.tsx`）を、`kind` で先に分岐してから従来のロジックに入る形に直した。`ManualCanvas` の中身編集コールバックは `kind:'free'` のブロックにだけ書き込むようガードした。③結合時に見つけて直した不具合3件——(a) `sheet.micAssignment`（マイク割り）の中身描画が resolver の実際の戻り値の形（`{ assignments: [...] }`）ではなく他の2種と同じ `rows`/`sections` を探していたため、実データがあっても常に「マイク割りの情報がありません」と出ていた。(b) 差し込みの右パネル（`LinkedBlockInspector.tsx`）のセクション名入力欄が素の `<input onChange>` で、日本語の IME 変換中の文字が二重に入る `client-techops/CLAUDE.md` の既知の不具合パターンのままだったため `BufferedInput` に直した。(c) 借りる機材の「受渡し」欄・スケジュールの「時間」欄が期間を1つの文字列に手で組んでいて `check-ui-tokens` の `date-range-by-hand` を新たに増やしていたため、日付の期間（借りる機材）は `Grid` のセルに `ReactNode` を許すよう広げたうえで `<DateRange>` を使い、時刻の範囲（スケジュール）は `ApplyTemplateDialog.tsx` と同じ理由（日付ではなく時刻なので対象外）で `ui-tokens-ok` を付けた。④秘密の伏せ字（配信のストリームキー・WEB会議のパスコード）は `streaming.resolver.ts` が `link.reveal?.fields` に対応する項目名があるときだけ平文を返す設計どおりであることをコードで確認し、右パネルの解除チェックは共通シェルの `<ConfirmHost />`（`client-techops` も `AppShell` 経由で持っている）で確認を1回はさむ。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops --force`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors・既存警告 54 件のみ）・`npm run test`（shared 165ファイル/2299件・server review 17/17 pass）。

**運営マニュアルの段D（出す）を実装した**（設計 `docs/design/v4/production-manual.md` §6⑤・§6-6・§8・マルチエージェントで実装）。①3人が並行して作った部品（書き出し設定パネル・出す前の検査・仕上がり画面・印刷ドキュメント本体/表紙/柱/目次・PDF書き出し・インクの目安）を配線し、`/techops/manuals/:id/preview`（PC専用・`ManualPreviewPage.tsx`）として動く1つの機能にした。編集画面（`ManualDetailPage.tsx`）の見出し脇に「仕上がり」ボタンを足し、ここから遷移する。②紙面のプレビュー・PDF印刷のどちらも段Bで完成している `renderManualBlockContent` を `selected:false` で呼ぶだけで、新しい静止画/HTML変換は書いていない（`ManualPrintDocument.tsx` が1 `ManualPage` = 1物理ページで並べ直すだけ）。PDF書き出し（`manualPrintExport.ts`）は進行台本の印刷（`previewExport.ts`）と同じ手口——`window.open`→`document.write`で`<head>`を組む→編集画面が読み込み済みのCSSを複製→`ReactDOM.createRoot`で`ManualPrintDocument`を描く→少し待って`print()`——を踏襲した。③表紙・目次・柱（ランニングヘッダー）は自動生成（人が編集しない）。柱は資料番号・状態（下書き/rev.N。段Eの`fixed`に切り替わる分岐だけ書いてあり今回は到達しない）・時点・ページ番号・「取扱注意 ── 配信の鍵あり」（冊子のどこかに伏せ字を解除した差し込みブロックが1つでもあれば表紙にも柱にも消せない形で出す・§7-2）を持ち、目次は`page.chapter`が入ったページを自動で拾う。④書き出し前に4種の検査（紙からはみ出す・中身が空・元の資料が消えた・差し込み元が変わったまま）を**止めずに**見せる（`manualPreExportChecks.ts`。純粋関数・DOM非測定）。「差し込み元が変わったまま」は`link.frozen`が常にnullの今は0件のままが正しい挙動（段Eで効き始める）。⑤インクの目安（`shared/src/opsmanual/inkEstimate.ts`）はブロックの`w×h×塗り係数`から紙面に対する塗り面積を見積もる純粋関数（画像0.5・QR0.3・塗りつぶし図形/表/差し込みは背景色があれば1・文字は背景が透明/白以外のときだけ・線/矢印は常に0）。40%を超えたら知らせるが止めない。⑥書き出し設定（範囲・表紙・目次・ページ番号・時点表示・白黒）の型`ManualExportSettings`/`ManualExportRange`と範囲の絞り込み`selectPagesInRange`は`ManualPrintDocument.tsx`を唯一の正とし、書き出し設定パネル側はそれを re-export するだけにして、プレビュー・検査・インクの目安と実際に刷る本体が常に同じページの並びを見るようにした（結合前から3担当者間でフィールド名・所有元の合意ができていたため、統合時に型の不一致は無かった）。結合検査で見つけた問題は無し。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops --force`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors・既存警告54件のみ）・`npm run test`（shared 167ファイル/2338件・server review 17/17 pass）。

**段Dのレビュー指摘10件（design 5件・correctness 5件）を確認し、正当なものを直した。** ①[blocking] `block.style`（`BlockInspector.tsx`が`patchStyle({"font-size": …})`のようにケバブケースのキーで書き込む）を、`ManualBlockView.tsx`・`TextBlockContent.tsx`・新設した`ManualPrintDocument.tsx`のいずれも`{...block.style as CSSProperties}`とそのままReactの`style`propへ展開していたため、Reactがキャメルケース以外のキーを認識せず**文字の大きさ・太さ・背景色が編集画面でも印刷結果でも一切反映されていなかった**（色だけはハイフンが無いため偶然効いていた）。`manualBlockCssStyle()`（新設）で`font-size→fontSize`等へ変換する層を1か所に作り、3ファイルとも差し替えた（`border-color`/`border-width`は図形ブロックがSVGの`stroke`用に生の値を直接読む専用値のため意図的に変換しない——外枠divのCSS borderにも出すと図形の輪郭と重複してしまう）。②[should-fix] 「紙からはみ出すブロック」の検査が一覧の文字だけで、設計書が明記する「はみ出た分を赤い斜線で示す」視覚化が無かった指摘に対し、画面内の仕上がりプレビューだけに赤い斜線のオーバーレイを追加した（**実際に刷る紙・PDFには出さない**——配る紙そのものに斜線が印刷される事故を避けるため、`highlightOverflow`という別propで明確に分離した）。③[should-fix] 柱の「時点」表示が年を含まず設計書の例（`2026/08/22 14:00 時点`）と食い違っていたのを、年を含む形式に直した。④[nit] 書き出す物理ページが0件（表紙・目次OFF＋範囲指定で該当0件）でも「PDFで書き出す」ボタンが押せてしまう不整合、⑤[nit] 新設した`selectPagesInRange`等の純粋関数にテストが無かった点、⑥[nit] 書き出し範囲で除外したページの`resolved`（差し込みの解決結果。解除済みの秘密を含みうる）が絞り込まれずにReactツリーへ渡っていた点、をそれぞれ直した。「柱9mmと紙面ブロックの衝突が検査に出ない」「一覧の札に取扱注意バッジが無い」の2件は、今回のタスクの範囲（4種の検査・表紙/柱への表示に限定）を独断で広げないと判断し、記録として残すに留めた。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors）・`npm run test`（shared 169ファイル/2359件・server review 17/17 pass）。

**段Cのレビュー指摘6件（design 4件・security 2件）を確認し、正当なものを直した。** ①[should-fix] `LinkedBlockInspector.tsx` は秘密を解除した本人（`link.reveal.by`）をクライアントが自己申告する形になっており、`PUT /techops/manuals/:id/pages/:pageId` がその値を検証せず保存していたため、devtools でリクエスト本文を書き換えれば別人になりすませ、§7-2 が求める監査証跡（誰がいつ出したか）が意味を持たなくなる指摘だった。`manual-pages.routes.ts` に `enforceRevealAuthorship()` を足し、保存直前に `kind:'linked'` ブロックの `link.reveal.by` を必ず `req.user.id` へ強制上書きするようにした（ブロックの他の中身は従来どおり検証しない設計を維持し、監査証跡に関わるこの1フィールドだけ例外的に扱う）。②[should-fix] 12種のうち唯一「秘密」を扱う `streaming.resolver.ts`（伏せ字解除の分岐）に自動テストが1件も無かった指摘に対し、`server/tests/manual-streaming-resolver-review.test.mjs` を新設し、reveal無指定では暗号化列や平文パスコードを一切含まないこと・reveal指定時だけ復号済み平文を返すこと・配信設定が無いownerではnullを返すことを4テストで固定した（既存13件+新規4件で17/17）。③[nit] 段Bのレビューと同型の「頭注が実装済み内容と矛盾」が`ManualDetailPage.tsx`・`ManualCanvas.tsx`の2ファイルで新たに発生していたのを、実装済みの範囲に合わせて書き直した。④[nit] `manual-resolve.service.ts`の実在チェックSQLが`equipment.resolver.ts`と違いステータス文字列を直書きしていたのを`EQUIPMENT_LENDING_STATUS.LENT`のimportに揃えた——あわせて自分の検証で見つけた、この値をSQL文字列へテンプレートリテラルで直接埋め込んでいた箇所（値は定数で実害はないが、常にパラメータ化する規律に反する）も`$2`バインドに直した。⑤[nit] `InsertPanel.tsx`の「資料がありません」案内が番組紐づけの冊子でも「案件」表記のままだったのを`isProgram`で出し分けた。指摘のうち`sheet.resolver.ts`のアクセス制御（sourceIdが冊子と別案件/番組の資料を指すときに弾くか）は個別に再確認し、設計どおり正しく実装済みであることを確認した（修正不要）。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors）・`npm run test`（shared 165ファイル/2299件・server review 17/17 pass）。

**運営マニュアルの段E（v1最後の段——編集ロック・確定/版・ひな形・スマホの閲覧）を実装した**（設計 `docs/design/v4/production-manual.md` §6-2-1・§6⑤・§6①・§6⑥・マルチエージェントで実装）。①5人が並行して作った部品（冊子まるごとの編集ロックAPI5本・確定/解除、組織共通ひな形と前回の冊子からの複製、スマホの閲覧専用画面）を配線し、動く1つの機能にした。API のパス・レスポンス形・エラー型（新設の`LockError`。既存の`ConflictError`と同じ409系統だがコードを`LOCKED`に分け画面が「他の人が編集中」と「保存の衝突」を出し分けられるようにした）はサーバー（`manual-lock.routes.ts`・`manual-templates.routes.ts`）とクライアント（`manualApi.ts`）の間でズレが無いことを確認した。②編集ロックは「取る＝60秒ハートビート兼用」「未取得/自分/10分stale なら取れる」「manager は強制解除できる」の設計どおり`assertEditable()`（ロック＋`status==='fixed'`の両方を1関数で検査）を`updateManual`/`addPage`/`updatePage`/`deletePage`/`reorderPages`全部の入口に配線し、`checkOptimisticLock`（updated_atの楽観ロック）は最後の砦として別に残したままであることをコードで確認した。確定（`fixManual`）は全ページの全`kind:'linked'`ブロックを`resolveLinkedBlock`でそのとき解決し`link.frozen`に書き込んでから`status='fixed'`・`rev+1`を1トランザクションで書き、解除（`unfixManual`）は`status='draft'`に戻すだけで`rev`/`frozen`を変えないことを確認した。ひな形からの複製・前回の冊子からの複製はどちらも`kind:'linked'`ブロックの`link.frozen`/`link.reveal`を新しい冊子ではnullに戻す（`reissueBlock()`。固定するテストを`server/tests/manual-template-review.test.mjs`に追加）ことを確認した。スマホの閲覧（`ManualDetailRouter.tsx`→`useIsMobile()`で編集画面と閲覧専用画面を丸ごと入れ替え）は`shared/CLAUDE.md`の「早期returnしない・薄い親で入れ替える」を守っており、`pcOnlyScreens.ts`も`/techops/manuals/:id`をスマホ可・`/techops/manuals/:id/preview`はPC専用のまま、と設計どおりに宣言されていることを確認した。③結合時に見つけて直した不具合1件——サーバー側の強制引き継ぎAPI（`POST …/lock/takeover`・manager限定）と、それを呼ぶ`manualApi.ts`のラッパー（`takeoverManualLock()`）はどちらも実装済みなのに、**呼び出し元のUIがどこにも無く**、設計書§6-2-1が明記する「manager は待たずに取り上げられる」がmanagerの目には触れられない状態だった。`useManualEditLock.ts`に`takeover`/`takingOver`を足し（取得後は通常のハートビート経路に乗せ直すだけで済む——`POST …/lock/takeover`の直後はサーバー側もう自分が保持者なので、続く`POST …/lock`は必ず`acquired:true`を返す）、`ManualLockBanner.tsx`の「他の人が編集中」の帯に、manager にだけ「強制的に引き継ぐ」ボタンを追加した。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops --force`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors・既存警告54件のみ）・`npm run test`（shared 169ファイル/2359件・server review 23/23 pass）。

**段Eのレビュー指摘6件（design 3件・correctness 3件。実質4件）を確認し、正当なものを直した。** ①[blocking] `deleteManual()`（冊子まるごとの削除）だけ`assertEditable`を呼んでおらず、他人が編集ロックを持っている最中でも、確定（配布）済みの冊子でも、editor権限さえあれば誰でも即座に削除できてしまっていた（`updateManual`等5関数には正しく入っていたのに、同じファイルの削除だけ抜けていた）。`assertEditable`を通すよう直し、クライアント側の「冊子を削除」ボタンも`editable`を見ずに常時押せていたのを`disabled={!editable}`で塞いだ。②[blocking] `fixManual()`（確定）が、解決処理を始める前に読んだ**古い**ページのblocksスナップショットをそのまま書き戻していたため、manager が確定するちょうどそのとき editor がautosaveで保存した最新の編集を、エラーも出さずに上書き消去する事故があった（resolveLinkedBlockの解決に数百ms〜数秒かかる間に他の保存が挟まりうる）。書き込み直前にもう一度各ページのupdated_atを読み直し、スナップショット時点と食い違えば`ConflictError`でトランザクション全体をロールバックするようにした（SQL側の等号比較ではなくJS側のミリ秒比較にした理由——pgがマイクロ秒精度を切り捨てる影響で常に不一致になりうるため——もコードコメントに残した）。③[should-fix] `acquireManualLock()`が「SELECTで読む→JS判定→UPDATE」の非アトミックな2段構えで、ロックが空/staleのタイミングで2人がほぼ同時に取ろうとすると両方に`acquired:true`が返りうる競合状態があった。条件をWHERE句に入れた1文の`UPDATE...RETURNING`（`security-card.service.ts`の`returnCard()`と同じ形）に直し、行ロックそのものに排他を保証させた。あわせて`isConflict()`が新設の`LockError`（code:'LOCKED'）と既存の`ConflictError`（code:'CONFLICT'）をHTTPステータス409だけで区別できず、原因が違うのに同じ「ほかの人が先に保存していました」を出していたのも`isLockError()`を新設して直した。④[nit] `ManualPrintDocument.tsx`の頭注が「段Eはまだ無くstatusは常にdraft」のまま実装済み内容と矛盾していたのを直した（段B・段Cのレビューで繰り返し指摘された同種の見落とし）。回帰テスト`server/tests/manual-lock-review.test.mjs`（9件）を新設。検証: `npx tsc --noEmit -p server/tsconfig.json`・`npx tsc -b client-techops`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`npm run lint`（0 errors）・`npm run test`（shared 169ファイル/2359件・server review 32/32 pass）。以上でv1の全機能（段A〜E）が揃った。

**PR #676（運営マニュアルミニアプリ・段A〜E）へのCodexレビュー指摘20件（Code Review 19件・Security Review 1件。P1×15・P2×5）を修正した**。①[P1] `POST /manuals`がproject_id/program_idを無検査で受けていた——指定した本人がcreated_byになるため`canAccessManual`（作成者本人/案件メンバー/管理者）をそのまま通ってしまい、editor権限があれば他案件になりすまして冊子を作成でき、`resolve`（差し込み・段C）経由でその案件のスケジュール表・収録/配信設定（配信の鍵・WEB会議のパスコード）・レンタル機材・機材貸出まで読めてしまう欠陥だった（`buildPagesForNewManual`のcopyFromManualIdの「同じ案件/番組だけ複製可」ガードもこの穴で無力化されていた）。新設した`canAssignManualProject()`（`access.ts`）で、project_idを指定するときは呼び出し本人がその案件のメンバー（project_members/assigned_to）か管理者であることを`POST /manuals`で先に検査するようにした（存在秘匿のため404）。program_idは対象外——`qsheet_programs`は行単位の権限を持たない設計（`programs.routes.ts`）で、qsheetのreader/editorなら誰でも全件に到達できるため、追加の制限を課す理由が無い。②[P1] `fixManual()`のconflict再検査（内部レビューで一度直したはずのもの）が、実は「SELECTで読む→JSで比較→UPDATE」の2段構えのままで、同じトランザクション内でもSELECTは行ロックを取らない（READ COMMITTED）ため、その2文の間に他人のautosaveが割り込んでも検出できずに上書きしてしまう欠陥が残っていた。比較をUPDATE自身のWHERE句に畳み込んだ1文の条件付きUPDATE（RETURNING id。`acquireManualLock()`と同じ考え方）に直し、`date_trunc('milliseconds', ...)`で両辺をミリ秒に揃えてpgのマイクロ秒切り捨てによる誤検出も避けた。③[P1] `useManualPageAutosave.ts`が、コミット時点の`page.updated_at`（props）をexpected_updated_atとして固定していたため、1回目のPUTがデバウンス中に応答してupdated_atが進んでも、すでに積んである2回目のPUTはその値を知らないまま古い値で送られ、サーバー側の楽観ロックに偽の衝突と判定されて（`isConflict`→`invalidate()`）まだサーバーに届いていない編集内容を無音で失う欠陥があった。送信の直前に読む`lastKnownUpdatedAtRef`（保存成功のたびに更新）に切り替え、さらに送信中に新しい編集が来た場合は同時に送らず直列化する（進行中の保存の完了を待ってから続けて送る）ようにした。④[P2] 冊子のservice_date（本番/開催の予定日）を設定する経路が段Cまで無く、常にnullのままで表紙の日付表示・スケジュール表resolverの日付一致がどちらも不発だった。作成（`POST /manuals`）と更新（`PATCH /manuals/:id`）の両方でservice_dateを受けられるようにし、新規作成ダイアログと冊子詳細画面（`ManualServiceDateField.tsx`。1ファイル400行のラチェットのため別部品に切り出した）に入力欄を足した。⑤[P1・Security Review] `sheet.resolver.ts`（`sheet.rundown`/`sheet.excerpt`/`sheet.micAssignment`）が「sourceIdで引いたqsheet_documentsのproject_id/program_idが冊子と一致するか」だけをゲートにしており、資料自体のアクセス制御（`canAccessDoc`: 作成者/個別共有/`system_admin`。案件メンバー自動可視は持たない）を経由しない設計だった（意図的な設計判断としてコメントに書いていたが、Security Reviewから「案件メンバーなら誰でも他人の非共有台本をsourceId指定だけで全文読める」と指摘された。`canAccessManual`（冊子）は案件メンバー全員に自動で見えるため、案件メンバーというだけで資料側のより狭い権限を踏み越えられてしまっていた）。`resolveAccessibleDoc`に`canAccessDoc`の追加ゲートを入れ（`fetchDocOwner`が`created_by`も返すように変更）、`link-sources`の候補一覧（`listSheetSources`）も同じ条件（作成者/個別共有/管理者）で絞るようにした——一覧に本人が読めない資料のタイトルを出すこと自体が存在の手掛かりになるため。`resolveLinkedBlock`/`fixManual`を含む全呼び出し元で呼び出し本人（`AccessUser`）を渡すよう配線し直した（`fixManual`は`userId`ではなく`user`を取るシグネチャに変更——確定操作を行うmanager自身がその台本にアクセスできなければ、その`sheet.excerpt`等は`access_denied`のまま凍る、意図した挙動）。回帰テストは新設`server/tests/manual-create-access-review.test.mjs`（3件）・`server/tests/manual-sheet-resolver-access-review.test.mjs`（5件）と既存`manual-lock-review.test.mjs`のfixManual用モックの更新（新しいUPDATE...RETURNING形・`user`引数へ追随）。この修正の push（7486585）自体にもさらに5件のCode Review指摘（P1×4・P2×1）が届いた。⑥[P1] `manual-template.service.ts`の「同じ案件/番組の冊子だけ複製可」ガードが`sameProgram`一致だけで通っており、`canAccessManual`が番組紐づけの冊子を作成者本人/管理者にしか見せない設計と矛盾していた（番組が一致するIDを知っているだけで他人の冊子の全ページを複製できてしまう）。`buildPagesForNewManual`に`canAccessManual`の追加ゲートを入れ、`createManual`/`POST /manuals`から呼び出し本人を配線した。⑦[P2] `deletePage()`の「最後の1ページは削除できない」不変条件が「SELECT COUNT→JS判定→DELETE」の3段のままで、同じ冊子の別ページをほぼ同時に消す2つのリクエストが両方count=2を見て両方DELETEでき0ページの冊子ができる欠陥があった。冊子行を`FOR UPDATE`でロックしてから数える1トランザクションに直した。⑧[P1] ページの題/章名の保存（`updatePageMutation`）や並べ替え（`reorderMutation`）もそのページのupdated_atを進めるが、自動保存フック（`useManualPageAutosave`）の revision 追跡はそれを知らず、次の紙面編集が古いrevisionで送られ偽の衝突になっていた。フックから`syncPageRevision`を公開し、両ミューテーションの成功時に呼ぶようにした。⑨[P1] ⑧の対応と合わせて見つかった、前回push（8dda7be）で入れたrevision追跡が単一のrefのままだった欠陥——ページA→Bへ切り替えた直後にAへの保存応答が遅れて届くと、BのrevisionをAの値で上書きしてしまう。ページIDをキーにしたMapに直した。⑩[P1] 確定済み（fixed/archived）の冊子でも印刷/PDF/スマホ閲覧（`ManualPrintDocument.tsx`。3画面が共有）がライブの`/resolve`結果を使っており、確定後に元データ（スケジュール表・収録設定等）が変わるたびに`rev.N`のはずのPDFが黙って変わってしまい「確定すると版が固定される」という保証が崩れていた。status !== 'draft'のときは各linkedブロックの`link.frozen`を使うようにした。回帰テスト: `server/tests/manual-template-review.test.mjs`（既存4件を更新+新設1件）・`server/tests/manual-page-delete-review.test.mjs`（新設・3件）。`useManualPageAutosave.ts`自体は画面のテストを書かない方針（`shared/CLAUDE.md`）のためコードレビューでの確認に留めた。この2巡目push（6ae5e90）自体にもさらに5件のCode Review指摘（P1×3・P2×2）が届いた。⑪[P1] `updatePage()`/`updateManual()`の楽観ロックが「読む→JSで比較→UPDATE」のままだった——冊子の編集ロックは利用者単位（同じ人の2つのタブは両方ロックを通る）なので、同じ人の2つのタブがほぼ同時に保存すると両方が同じupdated_atを読んで両方チェックを通過し、後着の無条件UPDATEが先着を無音上書きできた。比較をUPDATE自身のWHERE句にも畳み込んだ（`updateManual`は指摘対象ではなかったが同じ形の欠陥のため合わせて直した）。⑫[P1] `fixManual()`のページ側ガードはページのupdated_atしか見ないため、ロック保持者がresolve中に「ページに触れない」変更（service_date等）を保存するとすり抜け、古いservice_dateで解決した中身のまま確定してしまう欠陥。冊子行そのものにも同じCASガードを掛けた。⑬[P2] `releaseManualLock()`が「JSで比較→UPDATE」のままで、比較の直後にmanagerがtakeoverでロックを奪うと、この無条件UPDATEがその新しいロックまで消してしまう欠陥。条件をUPDATE自身のWHERE句（locked_by=?）に畳み込んだ。⑭[P2] `enforceRevealAuthorship()`が「revealがある全ブロックへ無条件でby=いま保存している本人」を書いており、editor Aが秘密を解除した直後にeditor Bが別のブロックを動かしただけでも監査証跡（誰が解除したか）がBに書き換わる欠陥。既存ページの状態と突き合わせ、reveal.fieldsが前回と同じなら既存のreveal（by・atとも）をそのまま保つようにした（純関数として`manual-reveal-authorship.service.ts`に切り出し）。⑮[P1] `handleBlocksSaved`が"detail"キャッシュしか更新しておらず、差し込みブロックの追加・秘密の解除設定の変更が"resolve"クエリ（別キャッシュ・staleTime 30秒）に反映されず「読み込み中…」のまま進まなかった欠陥。保存成功時に"resolve"もinvalidateするようにした。回帰テスト: `manual-page-update-atomic-review.test.mjs`（新設・2件）・`manual-lock-review.test.mjs`（releaseManualLock用に2件追加）・`manual-reveal-authorship-review.test.mjs`（新設・4件）。この3巡目push（37e4b09）自体にもさらに5件のCode Review指摘（P1×4・P2×1）が届いた。⑯[P1] 画像ブロックの閲覧が壊れていた欠陥: `/api/v1/internal/qsheet/images/...`の配信ルートがアップロードと同じeditor権限を要求しており、運営マニュアルの導入で新たに生まれた「readerが埋め込み画像を見る」経路（案件メンバー全員に自動で見えるためreaderでも開ける）で画像だけ壊れて見えた。配信（`/images`）だけreader（既定）に緩め、アップロード（`/upload-image`）はeditorのまま維持した。⑰[P1] 下書きの書き出しが差し込み内容の到着を待たない欠陥: `/resolve`が届く前に書き出しボタンを押すと、別ウィンドウに渡す`printedResolved`がその瞬間の空オブジェクトのまま固定され、あとから届いても反映されず「読み込み中…」のままのPDFが出来ていた。差し込みブロックが1つでもある下書きはresolveQueryが届くまで書き出しボタンをdisabledにした（確定済みはlink.frozenを使うため対象外）。⑱[P2] 差し込む資料が0件のとき壊れたブロックが置かれる欠陥:  進行台本の3種（sourceId必須）でlink-sourcesが0件を返すと「1件も無いのでそのまま置く」という既存の分岐がそのまま適用され、sourceId:nullの直せない壊れたブロックが紙面に残っていた（前巡でlisttSheetSourcesにACLフィルタを入れたことで、案件には資料があるが本人が読めるものが1つも無いケースが現実的になった）。0件は「置かずに知らせる」よう3分岐にした。⑲[P1] アンマウント時の最終保存が直列化を無視する欠陥: 編集画面を離れるとき、保存が進行中でも待たずに未送信分を即座に送っており、進行中の保存と同時に飛んでどちらかが無音で失われうる状態だった。進行中の保存のPromiseを保持し、それが終わってから最後の1回を送るようにした。⑳[P1] セクション絞り込みが一切効いていなかった欠陥: `sheet.excerpt`の「セクション」設定（`options.sectionTitle`）を描画側が分割代入すらしておらず、常に台本全文がそのまま紙面・PDFへ出ていた——著者が意図的に外した節まで配ってしまっていた。指定されたラベルを含むセクションだけに絞ってから渡すようにした。回帰テスト: `shared/tests/opsmanualSheetLinkedContentFilter.test.ts`（新設・5件）。検証: `npx tsc -b server`・`npx tsc -b client-techops`・`node scripts/check-collab-parity.mjs`・`node scripts/check-file-size.mjs`・`node scripts/check-mobile-declared.mjs`・`node scripts/check-md-links.mjs`・`npm run lint`（0 errors・警告54件=既存基準のまま）・`npm run test`（shared 170ファイル/2364件・server review 52/52、全pass）。
