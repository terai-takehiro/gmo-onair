**nodemailer を v9 へ更新し、残っていた最後の High 脆弱性を解消した**（R6・ユーザー判断を受けての実行）。v8.0.5 に修正版の無い High（`raw` オプションが `disableFileAccess`/`disableUrlAccess` を回避しファイル読み取り/SSRFを許す）があったが、`server/src/shared/auth/email.ts` の実利用は `createTransport`/`sendMail`（`raw` は未使用）のみで、API はそのまま v9 でも動く。実際に最小の SMTP スタブを立てて送信を通し、日本語件名・本文を含め正常に送れることを確認した。これで npm audit（`--omit=dev --audit-level=high`）の本番依存 High は 0 件になった（xlsx は R6-b で既に解消済み）。検証: `npm run typecheck:all` / `npm run test`（1452件）/ `npm run lint` / 実SMTP送信の動作確認。
