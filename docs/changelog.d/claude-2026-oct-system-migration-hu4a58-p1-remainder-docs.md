**2026年10月の事業再編（P4「文書と用語の更新」）のうち、すでに実装済みの仕組みの説明を`docs/mcp-server.md`・`docs/wording.md`に追記した**。P0〜P3（計上会社マスター・改番・社内取引など）は実装済みだったが、MCPツール一覧と用語の決めごとにはその説明が無く、実装と文書がずれていた（`org_transition.state`は今日時点`off`で社名変更・改番はまだ実施されていないため、追記は「もう変わった」ではなく「この仕組みが実装済みとして存在する」という説明にとどめた）。①`docs/mcp-server.md`: `list_revenues`/`list_purchases`/`list_sga`/`get_monthly_summary`（省略時は全社合算）と`get_monthly_budget`/`upsert_monthly_budget`/`upsert_monthly_actual_override`/`get_monthly_pl`（省略時は今の会社1社。既定が逆なので注記）の`entity_code`引数、新設`renumber_project`（案件管理・confirm 2段階・sales manager権限）、新設カテゴリ「社内取引」の`create_intercompany_purchase`（confirm 2段階・sales editor権限）を追記し、ツール数を159→161種・22→23カテゴリに更新。`list_projects`/`get_project`出力の`entity_code`/`entity_source`/`entity_note`と、`gls_number`のフィールド名は維持したまま値が新方式(GJV-/GSS-/GMO-)にもなり得ることも明記した。②`docs/wording.md`: 「計上会社」「コストセンター」「改番」「社内取引」を用語集の新節に追記し、`org_transition.state`の4状態（off/preparing/cutover/done）を画面表示ラベル（未着手/準備中/切替中/完了）と対で整理。「移行センター」は今日時点で画面の名前ではなく設計上の呼び名であり、対象一覧を算出するAPI（`migration-center.service.ts`・`GET /org-transition/renumber-candidates`）は実装済みだが画面側の導線はまだ無いことを明記した。各項目に設計書（`docs/reorg-2026-10-plan.md`）の§番号と、`state`が`off`のあいだ休眠中であることを添えた。③`create_project`の`entity_code`引数・`create_studio_booking`の`gls_number`引数は実装を確認したが、この時点でまだ実装されていなかったため追記していない（実装され次第、別途追記が必要）。④`guide/words.md`は実体が`docs/guide/words.md`にあり、一般利用者向けの簡潔な用語集（GLS番号・案件/プロジェクト・ブロックアプリ等）と確認したが、今日時点で一般利用者の目に触れる変化が無い（`state='off'`）ため追記しなかった。検証: `node scripts/check-md-links.mjs` green（121文書・相対リンク435本）。
