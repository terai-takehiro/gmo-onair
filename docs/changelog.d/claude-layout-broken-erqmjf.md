**プロジェクト管理の見積タブだけレイアウトが崩れていたのを直した**（ご指摘）。
①⚠️ **合計金額の「¥」だけが行の左端に取り残され、数字が画面の右端まで飛んでいました。**
`<Money>` の**既定は列で使う形**（枠いっぱいに `¥` を左端・数字を右端へ引き離して、
縦に並べたとき金額の右端をそろえる）なので、**文中に置くと `¥` と数字が引き離されます**。
実測で**この1つの金額が 1041px** を占めていました（正しくは 67px）。
`Money` の説明にも「縦に1つしか無い金額に当てると円記号だけが遠くに離れて見える
（利用者からのご指摘。案件詳細の「見積金額」がこれだった）」と書いてある形で、
**同じ間違いを2度目に踏んでいた**ことになります。`inline` を渡して直しました。
②⚠️ **タブの余白がありませんでした。** プロジェクト詳細は**枠が余白を持たず、
中身の側が `p-4 lg:px-6 lg:pb-6 lg:pt-5` を敷く**決めで、7タブのうち**この1つだけが
書き忘れ**でした。そのため見出しも一覧も**左メニューに貼り付いて**いました
（実測: 行の左端が他のタブは 273px、ここだけ 248px ＝ 余白 0）。
③**一覧が白い枠に入っていませんでした。** 他のタブ（概要・未確認事項）と
⑤ 全プロジェクトの一覧はすべて `rounded-card border bg-card` に入れていますが、
ここだけ素の `flex flex-col` で、**表頭も行も背景と同じ灰色の上に浮いて**おり、
どこからどこまでが表なのか読み取れませんでした。
④**「提出先」が空の行に、ダッシュ1文字だけの灰色の帯が出ていました。**
`TableBadge` は**和文2字以上のときだけ 62px に固定**するので、`—` は固定幅に乗らず
`w-full` が効いて**枠いっぱいの帯**になっていました。`RowSlot` は中身が無いときに
自分で `—` を出す（枠の幅は保つ）ので、**値があるときだけ札を出す**形にしました。
⑤**「金額」の列見出しだけ左に寄っていました。** 値は右端でそろっているのに
見出しが左端にあり、どの列の見出しなのか読み取れなかったので右寄せにしました。
あわせて列の中の金額を**既定の形**（`¥` は枠の左端）に戻しています —
`justify-end` だと `¥` が数字にくっついて動くので、桁数の違う行が並んだときに
円記号の位置がばらつきます。
⑥**検証**: 実 Postgres ＋ 実サーバー ＋ 実ブラウザ（1440px）で、
提出先あり（自社／依頼元／PM会社）・提出先なし・旧版・9桁の金額（¥123,456,789）を
混ぜた4行を出して **5つの列の左右端が4行とも同じ座標**（289/373/1119/1203/1343）、
**横はみ出し 0px・JS エラー 0**。プロジェクト詳細の**7タブすべてを測り直して**、
見積タブが他の一覧タブと**同じ左端（273px）・同じ枠（白・角丸 14px・罫 1px）**に
なったことを確認しました。行を押すと明細（案件と同じ部品）が今までどおり開きます。
⑦**`shared/` は触っていない**ので凍結4アプリの CSS は不変。**DB 変更なし・
サーバーのコードも触っていません**（画面 1 ファイルだけ）。
