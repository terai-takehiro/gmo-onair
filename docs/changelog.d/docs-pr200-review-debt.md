**PR #200（棚卸し記録）自体に付いたCodexレビュー3件を反映した**。①1巡目5件の棚卸し漏れ、②changelog下書きの記述矛盾は文書のみの修正。③⚠️ **実コードの穴が2件見つかった** — `200_customer_fk_to_companies.sql` 冒頭のコメントに「Releasesのタグで再実行すればDBも含めて丸ごと戻る」という誤った手順が残っていた（`docs/deploy-pipeline.md` 本文はPR #199で訂正済みだったが、migrationのコメントは直っていなかった）ため訂正した。プロジェクト管理（GPM）の `gpm.service.ts` の create/update が別サービスのため、PR #199で足した顧客ロール検証（`assertCustomerCompanyId`）を素通りしており、仕入先のみ・ロール無しの会社をGPMプロジェクトの依頼元にできてしまう穴があったため検証を追加した。④**検証**: `npm run typecheck -w server` exit 0、`npm run lint` 0 errors/63 warnings、`npm run test` 1136件通過。実DB（`npm run verify:up`）でGPMの検証（仕入先のみの会社を拒否・customer_id省略時の自社構築は今までどおり通る）を確認した。
