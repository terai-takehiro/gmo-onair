**PR #333（計時・視聴者 表示レイアウト機能PR1・スキーマ＋API）のマージ後の棚卸しを記録した**（コード変更なし）。`get_reviews`／`get_comments`がいずれも0件のまま、CI（`checks`/`build`）が完走する前にマージされたことを [docs/reviews/codex-findings-v4.md](../reviews/codex-findings-v4.md) に記録した。実装の妥当性はワークフロー内の独立レビューエージェントと実DB・実サーバーでの検証で別途確認済み。あわせて、このPRのマージ直後に別セッションの `client-qsheet` → `client-techops` ディレクトリ改名（PR #334）がmainへ入ったため、以後のPR2・PR3（表示画面対応・エディタUI）はこの改名後のパスを前提に実装する旨も記録した。
