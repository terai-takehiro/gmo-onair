**仕入の検索と仕入先での並べ替えが、押した瞬間に必ずエラーになっていたのを直した**（調査中に見つけた本番の不具合・実測で確定）。⚠️ **仕入台帳の検索欄・仕入先での並べ替え・Excel 書き出し・MCP の `list_purchases` が、検索語を入れると全部 500 になっていました。** 原因は SQL の別名で、`vendors` 表を `companies` に寄せた回に各クエリの JOIN は `companies vco` へ直ったのに、**条件を組み立てる `list-query.ts` だけ `v.name` のまま取り残されて**いた（`missing FROM-clause entry for table "v"`）。⚠️ **型検査も lint も SQL の中身を見ない**ので、誰も気づけないまま生きていました。しかも**検索語を入れたときだけ落ちる**ので、画面を開くだけでは再現しません。あわせて、件数・合計・絞り込みチップを数えるクエリにも `companies vco` の JOIN が無かったのを足した（行を引くクエリにしか無かった）。⚠️ **同じ壊れ方を二度としないよう、「ビルダーが出す別名を使う側の FROM が定義しているか」を検査するテストを足した** — 直す前のコードに戻すと実際に落ちることを確認済み。検証: 実 Postgres で検索（日本語・英字）・仕入先での並べ替え・検索＋案件・検索＋状態・Excel 書き出し・CSV 書き出しがすべて 200 になり、仕入先名で1件ヒットし、当たらない語で0件になることを確認。
