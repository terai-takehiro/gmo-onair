**PR #384（chore: shared/ の未使用 export を整理・R6-e）のマージ後の棚卸しを記録した**（コード変更なし）。`docs/branching.md`「マージしたら、その PR のレビューを棚卸しに移す」の決めごとどおり、terai-takehiro 本人が手動マージし（レビュー0件・通常のマージコミット）、CI の `build` ジョブが Docker Buildx のブートストラップで `moby/buildkit` イメージの pull に失敗し 502 Bad Gateway で落ちていたが diff とは無関係な純粋インフラ障害だったこと、`chartDefaults`/`chartColors` の組は検証バッチの判定が割れたため今回は見送った（別途判断）ことを `docs/reviews/codex-findings-v4.md` に記録した。
