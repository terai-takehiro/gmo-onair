**TOKEN消費量が増大している要因を実測し、`token-consumption-analysis.md` にまとめた**。①コーディング中のTOKEN消費量が多いという指摘があったが、原因が特定されていなかった。②`docs/version-history.md`（1.32MB・`cat`/`sed`/`git show`等を含め本文を返すツール呼び出しがあった時だけ文脈に乗る）・9ワークスペース構成のモノレポ（`typecheck:all`は7クライアント+server、`build:all`は9ワークスペースを個別ビルド。コマンド自体は1回だが失敗時の出力を読ませると規模分のログが乗る）・`CLAUDE.md` 本体（34.5KB、うち「## 現在のバージョン」節だけで約29%）・`pr-watch`/`ai-feedback-loop` の「必ず使う」強制スキル運用・`docs/reviews/codex-findings-v4.md`（82KB・追記専用）を実測し、効いている順に整理した。③改善の方向性として、バージョン履歴の分割・リリースノート1件の文量に上限を設けること・CLAUDE.mdの長文コマンド例のドキュメント切り出しを提案した。④**検証**: ドキュメント追加のみでコード変更なし。
