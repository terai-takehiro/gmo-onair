**PR が多い版でリリースノートが作れなくなっていたのを直した**（`main` で再現・実測）。
①⚠️ **`npm run release:notes` が exit 1 で止まっていました**（「この版の行が大きすぎます:
19KB（目安 12KB）」）。`CLAUDE.md` の「## 現在のバージョン」は**毎ターン文脈に載る**ので
上限があるのは正しいのですが、**超えたときにやるべき分割（要約は `CLAUDE.md`・全文は
アーカイブ）をやる仕組みがどこにも無く**、案内文が出るだけでした。
⚠️ **踏むのはリリースを出そうとした人だけ**で、しかも**どう書けば画面が正しく読むか**
（見出しは要約側・本文はアーカイブ側・同じ版なら長いほうを採る）は生成側の
コードを読まないと分かりません。②**その分割を道具がやるようにしました。**
全文は `docs/version-history.md` へ、要約は `CLAUDE.md` と `README.md` へ入れます。
**中身は1文字も失われません**（画面の履歴は両方を読んで長いほうを本文にします）。
③⚠️ **要約の中身までは機械に作れません。** できるのは「何が入っているか」を並べることで、
「その版が何だったか」を1文で言うのは人の仕事です。**`docs/changelog.d/_summary.md` を
置けばそれをそのまま使い**、無ければ**収録した見出しを並べたもの**を入れて**警告を出します**。
使い終わったら消します（残すと**次の版が前の版の要約を名乗ります**）。
④**1本ずつの長さも数えて出します**（目安 3,000 バイト）。⚠️ **止めません** —
リリースの日に人の文章を削らせないためで、目的は「毎回上限に当たる」状態を見えるようにすることです。
⑤**検証**: 実際に3通り走らせました — **分ける**（全文 20KB → 要約が `CLAUDE.md`・
全文がアーカイブ・生成された履歴は**1件・見出しは要約のもの・本文 9,023 文字・`isCurrent` も保つ**・
`check:version` 一致）／**分けない**（小さいときは今までどおり。アーカイブに二重に入らない）／
**手書きの要約**（そのまま使い、**全文には混ざらず**、使ったら消える）。
`shared/tests/changelogSummary.test.ts` に 12 項目（**反証: 太字を貪欲に取ると見出しが段落まるごとになる**）。
⑥**アプリのコードは1行も触っていません**（リリースの道具と文書だけ）。DB 変更なし。
