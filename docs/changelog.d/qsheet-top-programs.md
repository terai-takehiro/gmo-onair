**「制作技術支援トップ」を作り直した — まず番組・イベントを選び、そこからミニアプリへ分岐する形にした。** 直前の実装（ミニアプリのタイルをいきなりトップに並べる案）は「押してもどの番組の？が定まらない」ため訂正した（ご指摘）。`/qsheet/top`（`ProductionTopPage.tsx`）が新しいアプリのトップで、①案件管理の番組・イベント（GLS案件・`/lookup/gls-options` から検索）か、②案件管理に無い「ここだけの番組」（マニュアル・新設 `qsheet_programs` テーブル）のどちらかを選ぶ。選んだ先は共通のハブ画面（`JourneyPage.tsx`・`/qsheet/projects/:id` または新設 `/qsheet/programs/:id`）で、そこで初めて進行台本・スケジュール表・収録設定・配信設定のミニアプリタイルが並ぶ。

- **DB（migration 227）**: `qsheet_programs`（id・name・event_date・notes）を新設。進行台本・スケジュール表・収録設定・配信設定の4テーブルすべてに `program_id` を追加し、`project_id` と対称に持てるようにした。収録設定・配信設定は `num_nonnulls(project_id, doc_no, program_id) = 1` の CHECK で「どれか1つだけ」を強制。⚠️ 一意キーも `COALESCE(project_id, doc_no, program_id)` に広げた — 広げないと番組だけの行は鍵が常に NULL になり、同じ番組・同じ日の重複行を防げなくなることを実 DB で確認して見つけた。`production_journey_marks.scope_type` にも `'program'` を追加。
- **サーバー**: 番組の CRUD（`programs.routes.ts` 新設）。owner の解決（`device-settings-owner.ts` の `Owner` 型）に `kind: 'program'` を追加し、収録設定・配信設定・その Excel 出力・ジャーニーがすべて番組でも動くようにした。進行台本・スケジュール表の一覧・作成・更新も `program_id` を受け付ける。番組単位のジャーニー（`getJourneyForProgram`）を追加 — 案件単位との違いは「`episodes` を持たない」の1点だけ。
- **クライアント**: `ProductionTopPage.tsx` を番組・案件のピッカーに作り直し、`JourneyPage.tsx` に `scope: "program"` とミニアプリのタイル行（`MiniAppTiles`）を追加。`SheetListPage.tsx`/`ScheduleListPage.tsx` に `?program=` フィルタを追加（既存の `?project=` と対称）。旧 `TopPage.tsx`（`/qsheet/home`）は主導線から外れ「進行台本だけの案件選択」に位置づけを改めた（URL は生かしたまま）。
- **検証**: `npm run typecheck` / `npm run lint` / `npm run test`（1321件）/ `npm run build` OK。`npm run verify:up` で実 Postgres に migration 227 を適用し、番組の作成→進行台本・スケジュール表・収録設定・配信設定の作成→ジャーニー取得→ピン留めまでを実 API で一通り確認。3-way CHECK・広げた一意キー（重複拒否）の両方を実際に違反させて検証済み。

**案件詳細の当日タブ（`client/`）を制作技術支援のハブと連携させた**（ご指示）。`DayTab.tsx` の先頭に「制作技術支援を開く」カード（`HubCard`）を追加し、この案件の `id` をそのまま owner キーとして `/qsheet/projects/:id`（`JourneyPage.tsx` のハブ画面）へ直接着地させる。新しい API は不要——`resolveOwner` が `projects.id` をそのまま引くため。既存の進行台本一覧（`DocList`）は「進行台本（Qシート）」に見出しを改めて残した（ハブとは別に、この案件の台本だけを素早く開ける入口として）。検証: `npm run typecheck` / `npm run lint`（`check-links.mjs` 含む） / `npm run test` / `npm run build` OK。
