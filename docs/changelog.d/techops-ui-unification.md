**制作技術支援（Qシート）の各ページでバラバラだった UI の作りを統一した**。「各ページのデザイン思想が統一されていない。フォントの太さやサイズ、ページ幅など」というご指摘を受けて全131ファイルを実測したところ、①ページ直下の枠（本文幅と左右余白）が8通り、②画面の名前 `<h1>` の書き方が11通り・14px〜24px（自前の上辺バーを持つ4画面では14pxまで小さくなっていた）、③文字サイズが生 Tailwind 535 か所 対 v4 の型スケール 430 か所、④`font-medium`/`font-semibold` が161か所（LINE Seed JP は 400/700/800 しか無いため黙って落ちる）、⑤角丸が生の段と役割名で混在、と割れていた。原因は凍結解除を段階的に進める過程で「v4化を通ったブロック」と「client-live から移植しただけ／旧Qシートのままのブロック」が同居したこと。①画面の外枠を1か所で決める `<PageShell>`（幅は `full` / `narrow` の2段だけ・余白は案件管理と同じ `p-3 lg:p-6`）を共通部品に新設し、v4 の規律に「5. ページの外枠」を足した。②その上でトップ・案件/番組のハブ・進行台本一覧・スケジュール・収録設定・配信設定・計時・視聴者・テロップCG・レンタル・設定書き出し・AIナレッジの各画面を `<PageShell>`／`<PageHeader>` に寄せ、生 `text-*` を 535→229、`font-medium`/`font-semibold` を 161→89 に減らした（残りは表本体・本番4画面など「触らないと決めた範囲」）。③ページ側の `env(safe-area-inset-bottom)` 直書きが `p-*` の下余白に勝って 0px になっていた5画面の誤用も是正した。④あわせて、狭い画面でミニアプリ切替の帯が横スクロールせず375pxで右端の項目に届かなくなっていた不具合（根が `shrink-0` と `overflow-x-auto` を同時に持っていた）を収録設定・配信設定で直し、スマホのテロップ一覧で左余白が二重に効いて32pxになっていたのも直した。⑤同じ崩れ方が再発しないよう `npm run lint` に検査を3本（ページ幅の中間段・`<h1>` の手書き・safe-area の誤用）足した。棚卸しと残作業は docs/reviews/techops-ui-unification-plan.md。
