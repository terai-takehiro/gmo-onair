**#735（Wiki 段F）のレビュー指摘を棚卸しに記録した。** マージすると指摘は GitHub の画面から消えるので、書かなければ存在ごと消える（`docs/branching.md`）。Code Review 3巡・10件（P1×2・P2×8）は全件を検証用 Postgres と実ブラウザで再現してから直し、未解決スレッドは0件。⚠️ ただし**最終コミット `8af634ed` は Codex の利用上限でレビューが1度も走っていない**ので、「レビュー0件」は「指摘なし」ではない旨と、そこに **P1 の権限修正（コメント通知の宛先を `canReadPage` で絞る）が含まれる**ことを明記した。代わりにその差分を自分で敵対的に読み直し、**同じ質問で2人が同時に「空のページを作成」を押すと下書きが2本できる**（`UPDATE wiki_ai_gaps … WHERE id = ?` が状態を見ないので両方の取引が当たる）のを見つけたので、❌ 未対応として表に残した（段E からの挙動で #735 が持ち込んだものではない。直し方と、AI の下書き側にある同じ穴も書き添えた）。あわせて、10件のうち2件が**前の巡の修正が招いたもの**だったことから「1回で済ませると決めたら、失敗したときに戻せるところまで見る」、権限の抜け2件がどちらも「読めないはずの中身が別の経路で見える」形だったことから「Wiki で人に何かを見せる口を足す人は `canReadPage` を通したか必ず確かめる（通知・書き出し・集計の本文も見せる口）」を学びとして残した。
