**案件フェーズに「実施済（財務処理中）」を追加し、財務処理前に完了フォルダへ入らないようにした**。
①⚠️ **受注後は日次ジョブが `event_end` の翌朝に無条件で「完了」へ繰り上げ、
同じジョブが直後に BOX の `98_終了案件` フォルダへ移していた** — 請求・入金など
財務処理が済んでいなくても翌朝には完了フォルダに入ってしまい、財務側が
「完了フォルダにあるのにまだ処理していない」と分かりづらい状態だった
（`project-health.ts` の `completeElapsedWonProjects()`）。
②新ステージ `r_delivered`（R 実施済）を「A 受注済」と「S 完了」の間に追加。
受注確定の扱い（`WON_STAGES`）には含めつつ、BOX の `98_終了案件` への移動は
従来どおり `stage = 's_completed'` の完全一致でしか発火しない状態を維持し、
日次ジョブの繰り上げ先を `s_completed` から `r_delivered` に変更した
（`s_completed` への昇格は財務が案件詳細から手動で行う運用にした）。
③一覧のバッジ・チップ・案件詳細のステージ変更ボタン・GPM（プロジェクト管理）の
同種のUI・ファネル分析・ダッシュボードの各種「進行中案件」集計・営業活動記録の
次回アクション自動クローズ・DB の CHECK 制約（migration 271）を一通り追随させた。
「進行中」「行を薄くする終了扱い」からは意図的に除外し、独立した警告色のバッジで見せる。
検証: `npm run typecheck`（client/client-daily/client-equipment/server）・
`npm run test`（shared、1964件）全通過。
④仕上げのレビューで見つけた見落とし2件を追加で直した:
AIフィードバック集計（`ai-feedback.service.ts` の `get_ai_feedback_digest`）の
受注/進行中の集計SQLに `r_delivered` が入っておらず、実施済（財務処理中）の
案件だけ「進行中」に数えられていたのを他箇所（`WON_STAGES`）と揃えて修正。
検証環境のシードデータ（`seed.ts` の GLS-A001）を `s_completed` から
`r_delivered` に変更し、新ステージのバッジ・フォルダ挙動を実ブラウザで
見分けられるサンプルを用意した（GLS-A008 は `s_completed` のまま残し、
BOX の `98_終了案件` フォルダ移動の確認用に維持）。
