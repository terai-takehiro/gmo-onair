# Codex のレビュー指摘（v4 の全 PR・#38〜#105）

> **これは棚卸しの結果です。** v4 の PR 68 本すべてのレビューコメントを読み、
> `chatgpt-codex-connector` の指摘を1件ずつ書き出しました（2026-08-15 時点）。
>
> **合計 143 件・うち P1 が 48 件。返信または修正が確認できたのは 9 件だけ**でした。
> 残りは**指摘が付いたままマージ**されています（マージすると指摘は画面から消えるので、
> 誰も見なくなります。これがこの文書を作った理由です）。

## 読み方

- **P1 / P2 は Codex が付けた重みです。** P1 は「壊れている・データが壊れる・
  権限が抜けている」、P2 は「使えない・読めない・数が合わない」がおおよその目安
- **「状態」は 2026-08-15 に確かめたもの**です。⭕️ = 直っている（別の PR で直った分を含む）／
  ❌ = コードにまだ残っている／❓ = 未確認（読んだだけで、コードは見ていない）
- ⚠️ **❌ には「直さないと決めたもの」も入ります**（理由と、何が変われば直すかを書いてあります）。
  **決めたからといって表から消さないこと** — 消すと**次に読んだ人には無かったことになり**、
  同じところをもう一度掘るか、知らないまま「桁がそろう」と書いてしまいます
  （[#141](https://github.com/terai-takehiro/gmo-onair/pull/141) の指摘がまさにこれでした）
- **❓ を「残っている」と読まないこと。** 後の PR で偶然直ったものが実際にあります
  （`?tab=thread` のリンク・`/gpm/estimates/summary` のルート順・持ち帰りの二重登録）

## いま分かっている優先順位

**1. 時刻（JST）** — ✅ **片づきました**（`server/src/shared/utils/jst.ts`）。
コンテナに `TZ` が無いのに、日本時間のつもりで `Date` のローカル取得を使っていた3か所。
**夜間ジョブが昼に走り、朝9時前のダッシュボードが前日を出して**いました。
⚠️ `TZ=Asia/Tokyo` を置く直し方はしていません — **Alpine は tzdata を持たないので
渡しても黙って UTC のまま**になります（設定したのに効かない、いちばん気づけない形）。
`Intl` は ICU 内蔵の時間帯データを使うので、そちらで明示的に変換しています。

**2. 権限（見えてはいけないものが見える側）** — ✅ **片づきました**（#106）。
`dailyops` だけの人に案件80件が返る／その人が案件・台帳に書ける／BOX のサムネが
持ち主を確かめない／端末を共有すると前の人の案件名が出る、の4件。

**2b. 「押せるのに 403」側** — ✅ **片づきました**（#110）。
`sales` だけの人の請求書 PDF ／ reader に出るタスクの四角 ／ `sales` の一覧へ送るリンク ／
**`dailyops` だけの人のトップページで受信箱が 403 になり、数えていないのに
「お客様を待たせているものはありません」と出ていた**もの。
`HoldListPage` と カレンダー設定は**すでに直っていた**ことを確認しました。

**3. お金** — ✅ **片づきました**（#111 / #112）。二重計上3件（見積→売上の同時変換・
請求番号の採番・書類の引き渡し）と取りこぼし1件（グループ請求が一覧に1行も出ない）は #111。
どれも**確かめてから書くの間に他人が入れる**形だったので、**取引の中で行を押さえて**から書きます。
案件詳細の読み取り側2件（分け合う請求を満額で足す／100件を超える売上・仕入が合計から落ちる）は #112 で、
**合計はサーバーが配分で出す**形にしました（画面が行を足さない）。

**3b. 出した見積を書き換えない** — ✅ **片づきました**（#112）。
「出した版・旧版は直せない」という決めごとが**画面の側にしかなく**、サーバーは
**中身の書き換えも明細の入れ替えも削除も受け付けて**いました（古いタブ・MCP・直接叩きで通る）。
あわせて**承認待ちの見積書が社外フォルダに入る**（置いた時点で送ったのと同じ）のと、
**見積段階の売上に入金・検収を記録できる**のも塞いでいます。

**4. 見積の承認** — ✅ **片づきました**（#107 / #108 / #109）。
決めごと2つ（`can_estimate = false` が読まれていない／限度を**送る人**の役割で見ていた＝
**上司が部下の上限超えを承認せずに送れた**）を効くようにし、**承認する画面**も作りました。
`approval_state = 'pending'` になった見積を承認する導線がクライアントに1つも無く、
**pending になった見積は誰も送れませんでした**（サーバーの `/approve` は正しく動いていた）。
案件と GPM の見積タブに同じ帯と「承認する」を出し（**承認できる人にだけ出す** —
判定はサーバーが `can_approve` で渡す）、⑤ 見積・請求に「承認待ち」の絞り込みを足しています。
⚠️ **GPM には承認の口そのものがありませんでした**ので、あわせて新設しました。

**5. 確定した記録が後から変わる** — ✅ **片づきました**（#113）。
**確定した週報**に後から行が足せた（ニュースの「週報へ送る」・MCP・直接叩き。画面だけが止めていた）／
**AI の再実行が確定した本文を書き換えた**／**終わった棚卸し**をスマホから書き換えられた／
**確かめていない AI の K/P/T が隔週キープの資料に載った**／
**受注にした瞬間に放送種別・媒体が消えた**（`changeStage` が `{}` で発番を呼ぶため）。
⚠️ **塞ぐだけにしないこと** — 確定した週報は戻す口がどこにも無かったので、
**「確定を解く」**をあわせて作りました（無いと「もう1行入れたい」で行き止まりになる）。
⚠️ **`published` だけで判断しないこと** — デイリーニュースは*閲覧型*で
`published` を「確定の操作が要らない種類」の印に使っており、種類で分けずに塞ぐと
**ニュースが1行も書けなくなります**。

**5b. 断り文が画面に届いていなかった** — ✅ **片づきました**（#113・**この作業中に見つけたもの**）。
`AppError` は `(状態, コード, 文)` の順ですが、**日常業務の 6 ファイル・35 か所が逆**でした。
`error.message` にコードが入るので、画面（`notifyApiError`）に出ていたのは
**`VALIDATION_ERROR` の6文字**だけ。**理由も直し方も一度も表示されていません**でした。
型はどちらも `string` なので、**型検査にも lint にも出ません**
（`shared/tests/recordIntegrity.test.ts` が server 全体を数えて止めます）。

**6. AI の指標と費用が信じられない** — ✅ **片づきました**（#114）。
会社方針「AI を使い捨てにしない」の**条件2（修正の差分）と条件3（成果）**が
実際には壊れていました。**直さずに承認した回が1件も記録されず**（受入率は永久に 0%）、
**AI が自分で直した分は人の修正として**数えられ（直すほど修正率が上がる）、
**投入口から作った議事録の直しが投入口の指標に混ざって**いました。
費用側も、**落ちた呼び出し**（軽いモデルのやり直し・議事録の失敗）が使用量に残らず、
**録りながらの下読みは 20 秒のはずが5分まるごと**を送っていました
（＝打合せの音声を丸ごと2回文字にしていた。画面には「費用を抑えるため」と出ている）。
⚠️ **「押せなくする」だけでは足りない** — 写真の縮小が終わる前に押せる問題は、
ボタンを `disabled` にしても**選んだのと同じ瞬間の押下は素通り**します
（実ブラウザで**添付 0 件で送られる**ことを実測）。**送る側が待つ**こと。

**7. プロジェクト管理が「開けるのに使えない」** — ✅ **片づきました**（#115）。
`gpm` だけの人は**依頼元を1件も選べず**（`/customers` は `sales` を要求する）
プロジェクトを作れませんでした。請求タブは**開いた瞬間に 403 で真っ白**、
しかも開けた人にも**月次請求が1つも出ません**でした（`isEstimateMode` を渡していたため）。
受注にしても **GLS-B が採られず、段の履歴も残りません**でした
（採る口は案件側にしかなく `sales` を要求するので、**採る手段が無い**）。

⚠️ **測り方の訂正（#115 で判明）。** 実ブラウザの検査で使っていた開発サーバーの
起動の仕方が**間違っていて、Tailwind が走っていませんでした**（`npx vite <dir>` を
リポジトリの根から実行すると PostCSS の設定が読まれない）。つまり
**v4.0.16〜18 の「横はみ出し 0px」は CSS が当たっていない画面で測ったもの**でした。
`npm run dev -w <アプリ>` で測り直した結果、**どの画面も横はみ出し 0px のまま**で
結論は変わりませんでしたが、当時それを言える根拠はありませんでした。
検査の script には**「`.flex` が本当に flex になっているか」を毎回確かめる行**を入れてあります。

**8. 設定したのに効かない支払期日 ／ 同じ引き合いから2件** — ✅ **片づきました**（#116）。
設定 ⑤ の「支払日が休業日のとき（前の営業日へ／次の営業日へ／寄せない）」は
**どこからも読まれておらず**、日曜のままの期日が入っていました。
⚠️ **さらに `computeDueDate` を呼んでいる場所が1つもありませんでした** —
下見の画面が使うだけで、**保存される行の支払期日はずっと空**です
（つまり締め日も支払日も、設定しても台帳には入っていませんでした）。
案件の二重登録は、**同じ瞬間に2回押すと2回とも通る**形でした
（`disabled` は描き直し1回ぶん遅れる）。**DB の一意索引で止め、
先に出来たほうを返します**（エラーにすると、出来ているのにもう一度作られる）。

**これで ❓ の P1 は残っていません。**（この時点で残っていたのは 69 件・すべて P2）

**9. 黙って切れる・古い結果が出る** — ✅ **片づきました**（#122）。
**切れた側には何も出ません。** 見積・請求の一覧は 300 件で切っているのに
**画面が並んだ行を数え、並んだ行を足して**いたので、**388 件 47,850,000 円 のところ
「300 件 4,136,000 円」**と出ていました（月末に突き合わせるまで気づけません）。
BOX は1回に 100 件しか返さないのに1回しか呼んでおらず、**101 個目からのファイルと
写真は一度も画面に出ていません**でした（置いた人には「上げたのに無い」と見えるので、
**同じファイルがもう一度上がります**）。スマホの案件一覧には**前へ／次へが1つも無く**、
21 件目以降を開けませんでした。打合せの録音は**手元の 50 件の中だけ**を絞っていたので、
51 件目の案件は名前を打っても出てきません。⌘K の窓は、**打ち替えた直後に
古い言葉の結果が並んだまま**で、そこで Enter を押すと関係ないものが開きます
（番号を振る守りは「遅れて届いた結果で上書きしない」ためのもので、
**打ち替えた瞬間に前の結果を消しはしません**）。
❓ のうち **3件はすでに直っていました**（SearchPage の2件と、
打合せの録音の「文字起こしが使えない環境でも録音を始められる」）。
読んだだけの行なので、**1件ずつコードで当たり直して**確かめています。

**10. 数字が嘘になる・記録が別のものに付く** — ✅ **片づきました**（#123）。
**それらしい数字が出る**ので誰も報告しません。やり取りの件数は**どの案件も「20件」**
（引いているのが 20 件まで・実データは 46 件）、「次にやること」にぶら下がる **①②③ は
概要から丸ごと消え**（数も出ないので**やることは1つ**に見える）、**`-10万円` の
マイナスは印として食われ**（符号が逆の金額が出る）、**鉤括弧で始まる文があとの行を
全部飲み込み**、**別の拠点の同じ名前の部屋が1つ消え**、AI の成果は
**`SUM(DISTINCT 金額)`** で**同じ金額の案件を1件に潰して**いました（50万円が3件で 50万円）。
短くできなかった印は、**引いてから失敗するまでに直された本文**に付き、
その本文は**二度と短くされません**でした。

⚠️ **#87（変換後も想定金額が古いまま）は、想定金額を上書きしないことにしました。**
`expected_amount` は**起票のときの見込み**で、AI の成果はそれと実績を比べるための値です。
受注のたびに上書きすると**比べる相手そのものが消えます**。代わりに**数えるほう**を直し、
**確定売上があればそれで数え、見込みで数えた件数を添える**形にしました。

**12. 壊れているのに「大丈夫」と出る** — ✅ **片づきました**（#129）。
設定画面の BOX は**空でなければ「つながっています」**（`BOX_CONFIG_JSON` は1行の JSON なので
貼り付けで欠けるのが普通に起きる）、書体の同梱の検査は **woff2 が1つ以上あるか**しか見ておらず
（断片1つが壊れると**その文字だけ**代替書体になる）、`shared` の参照の検査は
引用符が `"` だと**「alias が無い」という嘘の理由**を出していました。

**13. 保存が黙って失敗する・帯が2回目に動かない** — ✅ **片づきました**（#131）。
**いちばん大きいアプリ（案件管理）だけ、共通の「最後の受け皿」が効いていません**でした
（独自の `QueryClient` を作っていたため）。書き込み 296 か所に対して `onError` は 54 か所
しか無いので、残りは **400 / 403 / 500 が返っても画面に何も出ません**。
お知らせの帯は鍵が**中身**だったので、**同じ操作を続けたとき**
（「記録しました」→「記録しました」）に動きが再生されず、
**2回目が出たのか分からない**（そしてもう一度押される）状態でした。

**14. 止まったものが、止まったまま誰にも見えなくなる** — ✅ **片づきました**（#133）。
**ふりかえりの下書き**は「昨日終わった案件」を **20 件だけ**見ていたので、
同じ日に 21 件以上終わる週（大きなイベントの前後では普通に起きます）では
**21 件目から一生作られません** — 仕事は日に1回で、翌日はまた「その日の昨日」を
見るため、**取りこぼしは二度と拾われません**（実測: 25 件のとき 1晩目 20 件・
前の版の2晩目は **0 件**）。**録音の投入**は、サーバーが再起動すると
`transcribing` のまま残ります。

⚠️ **指摘は「一覧だけ直っていない」でしたが、実際は詳細も直っていませんでした。**
復帰の判定は `new Date(String(row.created_at).replace(' ', 'T'))` で日付を組み立て直して
おり、**pg は `timestamp` を `Date` で返す**ので
`Sat Aug 15 2026 10:53:12 GMT+0000 (…)` → `SatTAug …` → **必ず `Invalid Date`**。
`Number.isNaN` で早期に返していたので、**一度も失敗として見せたことがありません**でした。
止まった行は `pending` でも `failed` でもないので、**「確認待ち」の催促にも
「失敗」の一覧にも出ません** — 投げた本人には**録音が消えたようにしか見えません**。
しかも日常業務の投入ログは状態の日本語を3つしか持たず、**英語のまま**
（`transcribing`）出ていました。

**15. GLS-B（工事・構築）に放送の分類が付く** — ✅ **片づきました**（#134）。
`project-classification.ts` は「GLS-B は2段分類を持たない・**NULL のままにする**」と
決めていますが、**書く側で守っていません**でした。直す画面は GLS-B でも
**欄を出したまま**で、押せば保存されます。サーバーは2段から `project_type` を導くので、
**工事のプロジェクトが `hybrid_event`（ハイブリッド）になり**、標準工程の型・Excel・
集計が放送の案件として扱います。**押した人には何も出ません**（選べたのだから正しいと思う）。
**画面で欄を消すだけにせず、サーバーでも落とします** — MCP・古いタブ・直接叩きから通るため。
実測: コンサルの案件（GLS-B002）に2段を送っても `B / consulting / — / —` のまま
（**反証: 守りを外すと `B / hybrid_event / with_audience / broadcast`**）。

**16. AI の単価を書き替えると、過去の費用も一緒に変わる** — ✅ **片づきました**（#134）。
`ai_usage` に残しているのは**トークン数と録音の秒数だけ**で、金額は**読むときに**
いまの `AI_PRICING_JSON` を掛けて出しています。`.env.example` は「値は公開ページで
確認して入れること」と言うだけだったので、**手順どおりにやると先月の金額も変わります**。
さらにモデルを乗り換えて**古い鍵を落とすと、そのぶんが合計から静かに消えます**
（総額が下がるので「安くなった」と読めてしまう）。**「差し替えるのではなく足す」**を
`.env.example` に書き、画面には**いまの単価で出したこと**と、**単価の無いモデルを名前で**
出します。実測: 4 モデルのうちいちばん量の多い `gpt-5.4-old` の鍵を落とすと合計 $3.08 に
1セントも入らず、画面は「単価を入れていない 1 個のモデルは合計に含めていません
（gpt-5.4-old）」（**反証: 前の版は名前を出さない**）。

**⭕️ v4 の棚卸しは、これで全件に当たり終わりました**（143 件＝**元の #38〜#105 のぶん**）。
直したもの・すでに直っていたもの・再現しなかったものを、**どれがどれか分かる形**で
表に残してあります。**「再現しなかった」も測った結果を書いてあります** —
書いておかないと、次に読んだ人が同じところをもう一度掘ります。

⚠️ **これは「Codex の指摘が全部片づいた」という意味ではありません。**
**潰す作業そのものに付いた指摘**（#106 以降）は別勘定で、下の
[この棚卸しのあとに付いた指摘](#この棚卸しのあとに付いた指摘106-以降)にあります。
**そちらは #146 / #147 / #148 で片づきました。** 残っているのは
**直さないと決めた1件だけ**です（下の表を数えてください）。

**残件の数え方。** 件数は文章に書きません — **枝ごとに違う数になり、
毎回この1行がぶつかる**ためです（版の3か所を触らない決めごとと同じ理由。
[docs/changelog.d/README.md](../changelog.d/README.md)）。表を数えてください:

```bash
grep -c '^| .* | ❓' docs/reviews/codex-findings-v4.md   # 未確認（読んだだけ）
grep -c '^| .* | ❌' docs/reviews/codex-findings-v4.md   # コードに残っている
```

⚠️ **前の版は `grep -c '| ❓ |$'` と書いており、必ず 0 を返していました** —
状態の升目には `❓ **この作業中に見つけた**…` のように後ろに文が付くので、
**行末が `|` で終わりません**。**残件があっても「0 件」と出る**数え方で、
この文書が追いかけている「検査が嘘の OK を出す」形そのものでした。

## ⚠️ 実測で分かったこと — 数字は等幅になっていない（#45 を調べていて見つけた）

`font-number` は `font-feature-settings: 'palt' 0, 'tnum' 1` ＋
`font-variant-numeric: tabular-nums` を当てており、`shared/CLAUDE.md` は
**「金額を縦に並べたとき桁が一直線に」**なると書いています。**実測すると効いていません。**

数字の subset を先に読み込ませて（`document.fonts.load(..., '0123456789')` /
`check` が true）16px で測った幅:

| 指定 | `1111111111` | `8888888888` |
| --- | --- | --- |
| 何も指定しない | 77.56px | 114.20px |
| 本文と同じ `palt 1` | 77.56px | 114.20px |
| `tabular-nums` だけ | 77.56px | 114.20px |
| **`font-number` と同じ `palt 0` + `tnum 1`** | **77.56px** | **114.20px** |

**4通りとも同じ**です。比較用に `monospace` で測ると 99.33px / 99.33px なので、
**測り方は正しく等幅を検出できます**。つまり **LINE Seed JP の数字は
プロポーショナル**で、`tnum` も `palt` も**数字には効いていません**。

- **#45 の前提（`palt` が桁をずらす）は成り立ちません** — `palt` は数字を1ピクセルも動かしません
- **ただし `tnum` も効いていない**ので、`font-number` が約束している
  「桁が一直線」は**成り立っていません**

⚠️ **見た目への影響は限定的です。** `MoneyCell` は幅が固定で
`justify-between` が数字を**右端に寄せる**ので、**金額の列の右端は揃います**
（縦に読むときに効くのはこちら）。揃わないのは**桁ごとの位置**だけです。

**ご判断: 記述を実態に合わせる**（コードは変えない）。`shared/CLAUDE.md` と
`docs/design/v4/_tokens.md` の「桁が一直線」を**「金額の列の右端がそろう」**に直しました。
**`palt 0` / `tnum 1` の指定は残します** — いまの書体では効きませんが、
**書体を替えた日に効く**ためです（外すと、替えた瞬間に何が起きるか分からなくなる）。
書体を替える案（別書体を当てる／等幅数字の版を同梱する）は**全画面の数字が動く**ので採りません。

## この棚卸しのあとに付いた指摘（#106 以降）

**潰す作業そのものにも指摘が付きます。** 同じ表に足していきます
（マージすると GitHub の画面からは消えるため）。

⚠️ **この表は #134 で更新が止まっていました**（2026-08-15 に棚卸し）。
「143 件すべてに当たり終わった」のは**元の #38〜#105 のぶん**で、
**それを潰す PR に付いた指摘は別勘定**です。当時ここに載っていたのは **2 件だけ**で、
残りを足してあります。以後 #145〜#149 に付いたぶんも同じ表に足しました。

⚠️ **件数は文章に書きません。** いちど書いて表と食い違わせたためです
（#145 で「20 件のうち 18 件」と書いており、実際は 26 件のうち 24 件でした。
#145 のレビューで指摘されて数え直しています）。
**数えるのは下の「数え方」で** — 文章の数字は古くなりますが、表は古くなりません。

**未解決だったものは #146〜#150 で片づきました。**
⚠️ **この `#106`〜`#170` の回に残っているのは、すべて「直さないと決めたもの」**です
（機材台帳の名前列が狭いまま／数字が等幅にならない。書体を替えないと直らず、
替えると全画面の数字が動く）。⚠️ **件数はここに書きません** — 前の版では「残っているのは
1件だけ」と書いており、実際には表に複数行残っていました。下の「数え方」の grep で
数えてください。
決めたものも**表から消しません** — 消すと次に読んだ人には無かったことになります。
**この段落は `#106`〜`#170` の回だけの集計です。** `#199` 以降の回にも一時
❌（未対応）の行が付くことがあります（直った時点で ⭕️ に更新します。下記）。
⚠️ **`npm run reviews:debt` では数えられません** — 直近20本のマージ済みPRの
**未解決の GitHub スレッド**だけを見る道具で、表の ❌ セルを数える道具ではありません
（スレッドが resolve されても・対象PRが20本の外に出ても、表の ❌ はそのまま残ります）。
**この下の「数え方」の `grep` コマンドだけ**を使ってください。文章の数字はここも
含めて古くなるので、実際の件数は必ず表を数えて確かめること。

`#202`/`#205` にも一時 ❌ が3行ありましたが（レビュー指摘・PR #205 4巡目で
発見・マージ済みコードに残っていた本物の未解決）、直しを push した PR #207 が
2026-08-18 にマージされたのを確認し、3行とも ⭕️ に更新済みです
（監査として、マージを確認する前に ⭕️ を先取りしない）。

⚠️ **「マージされた＝直っている」ではありません。** `#106`〜`#170` の回に足した
指摘はすべて `is_resolved: false` のままマージされています。GitHub の画面から
消えるだけなので、**表に足さないと存在ごと消えます**（この文書を作った理由そのもの）。

⚠️ **`#199` 以降は、resolve 済みでも足します — 個別に「〜は例外」と列挙しません。**
理由は2つ: ①**「消えるのは未解決の指摘だけではない」**（`npm run reviews:debt` が
拾うのは未解決分だけで、resolve 済みの指摘もマージすれば画面から同じく消える）ので、
resolve 済みかどうかは記録する・しないの判断に関係ない。②**列挙すると、その列挙を
足す PR 自身に付いた指摘が次の列挙漏れになり、追いかけっこになります**
（実際に起きた形: `#200`→`#201`→`#203`→`#204` と、棚卸しへ記録するたびに
「この PR の指摘も列挙に足せ」という指摘が繰り返し付きました）。
resolve 済みかどうかを知りたいときは、その行の「状態」欄
（`⭕️ 同PR内` は多くの場合そのPR内でresolve済み）を見てください。

**状態の確かめ方**: `#106`〜`#170` の回の ⭕️ と ❌ は 2026-08-15 に**コードを読んで**
付けています。`#199` 以降の行は、**「状態」欄が指す PR（`⭕️ 同PR内` ならその行の PR、
`⭕️ #NNN` なら `#NNN`）がマージされた日**に、多くは実DB（`npm run verify:up`）で
個別に確認したものとして扱います。⚠️ **1列目の PR 番号では日付を決めないこと** —
指摘が付いた PR と実際に直した PR が別（`⭕️ #NNN` の行）だと、1列目の日付では
まだ直っていない時点を指してしまいます（実際に #204 のレビューで指摘されました）。
都度その「状態」欄が指す回の日付で確かめるので、ここに一覧は持ちません。
**`❌ 未対応` の行は「状態」欄が直した PR を持たない**ので、代わりに**その行を
この表に足した PR（＝棚卸しの記録PR）がマージされた日**を確認日とします。
⚠️ **1列目の PR の日付ではありません** — 1列目の PR はレビューが**マージ後**に
届くことがあり（実際に #202 は 07:37 マージ・レビューは 07:39 と2分後でした）、
1列目の日付だと「まだレビューが付いていない時点」を指してしまいます。行を足した
PR（多くは「状態」欄や本文の脚注に書いてあります。無ければ `git blame`／コミット
履歴でその行を足したコミットを辿ってください）のマージ日なら、少なくとも
**その時点までにこの指摘の存在を把握していた**ことは保証できます。
`is_outdated: true`（＝同じ PR の後の commit が当該行を動かした）の指摘は、
**本当に直っているかを1件ずつ当たり直し**ました — 4 件とも直っていました。

⚠️ **#154 には Codex のレビューが1件も付いていません（2026-08-16 時点）。**
これは**「0 件でした」ではなく「まだ見ていません」**です — レビュー・レビューコメント・
ふつうのコメントのいずれも 0 件で、#152・#153 が2分で付いたのとは様子が違います。
**この2つは画面上どちらも「指摘なし」に見えます**ので、ここに書いておきます
（この文書の目的そのものです）。v4.1.0 のリリースはこの状態で出しました。

⚠️ **#159 は Codex のレビューが付く 14 秒前にマージされました**（マージ 08:51:02 /
レビュー 08:51:16・2026-08-16）。**「指摘が無かった」のではなく「見る前に入った」**ので、
`main` には**穴のあるほうの検査**が入り、続きの #160 で塞いでいます。
`npm run reviews:debt` はマージ後に走らせる決めごとですが、**この速さだと
レビューがまだ無い**ことがあります。**画面上は「指摘なし」と見分けが付きません。**

⚠️ **#160 の行は「直した PR」が自分自身**（`⭕️ #160`）です。マージ前に同じ PR の中で
直したためで、**直っているから書かない、にはしません** — この表は
「何を指摘されたか」の記録でもあり、**同じ検査に3件続けて付いた**という事実は
1件ずつ消すと見えなくなります（緩すぎ2件 → **締めすぎ1件**。
門を1本足すと、その門自体が次のレビュー対象になります）。

⚠️ **#165（この3件を直した PR）には Codex のレビューが1件も付きませんでした。**
**1時間28分開いていました**（作成 11:33 / マージ 13:02・2026-08-16）ので、
**#159 の「14秒で入った」とは違い、待つ時間はありました** — レビュー・レビューコメント・
ふつうのコメントのいずれも 0 件のままです。CI は2本とも緑でした。
⚠️ **これで「レビューが無いままマージした PR」は #154・#159・#165 の3本目**です。
**画面上はどれも「指摘なし」と見分けが付きません。** v4.1.2 のリリースはこの状態で出します。

⚠️ **#167（案件台帳の画面余白）にも Codex のレビューが1件も付きませんでした。**
**25分06秒開いていました**（作成 04:29:11 / マージ 04:54:17・2026-08-17）。
レビュー・レビューコメント・ふつうのコメントのいずれも 0 件で、CI は2本とも緑でした。
⚠️ **これで4本目**です（#154・#159・#165・#167）。**#159 だけは「見る前に入った」
と説明が付きます**（14秒）が、**#165 は1時間28分・#167 は25分開いていました** —
この2本は待つ時間があったのに 0 件です（#154 の開いていた時間は記録していません）。
何が起きているのかは分かっていません — **分かっているのは「画面上は指摘なしと
見分けが付かない」ことだけ**なので、ここに数え続けます。⚠️ **`npm run reviews:debt` はこの環境から 401 で届かないまま**
（2026-08-15 の記述と同じ症状）なので、**#167 も API を直接読んで確かめました**。
つまり**この4本目は、決めごとの手順どおりに走らせただけでは見つかりません**。

**#167 から表に移した指摘は 0 件です**（本当に 0 件でした）。
⚠️ **「0 件だから書かない」にはしません** — 上のとおり、**「指摘が無かった」と
「まだ誰も見ていない」を見分けるにはここに書くしかない**ためです。

⚠️ **上の「4本目」を書いた PR（#169）には、Codex のレビューが約1分で付きました。**
つまり **Codex そのものは動いています** — 数えていた4本は「レビューが来ない」のではなく
**「来る前にマージされている」ほうだった**可能性が高い、ということです。
実際 #169 は**レビューが付いた 33 秒後にマージ**されました（レビュー 05:30:36 /
マージ 05:31:09・2026-08-17）。#159 の「14秒前にマージ」と**同じ形**です。
⚠️ **数え方を「レビューが 0 件だったか」から「レビューを見てからマージしたか」に
変えたほうがよさそう**ですが、**まだ4本しか手元にないので決め打ちしません**。ここに書いて残します。

⚠️ **その #169 のレビュー1件は、直らないまま `main` に入りました**（下の表の1行目）。
**この文書が warn している「マージすると指摘が画面から消える」形そのもの**です
（143 件が埋もれたのと同じ）。**33 秒では、付いたことに気づく余地がありません。**

⚠️ **#161 と #164 の3件は、リリース前の点検で見つけました**（v4.1.2 の準備）。
**マージ済みで、GitHub の画面からは消えています** — `npm run reviews:debt` は
この環境から GitHub に届かず（401）、**API を直接読んで拾いました**。
うち **#164 の P1 は「直した機能が、利用者から見ると直っていない」**もので、
`main` に入ったまま**誰も気づけない**形でした（下記）。

⚠️ **#161 の指摘を直すとき、同じ壊れ方が他に2件見つかりました**（下の「同じ幅の壊れ方」）。
**指摘されたのは貸出の塊1件だけ**ですが、算数を全部の行に当て直したところ
**備品の台帳は 640px でも 768px でも横にはみ出して**いました（指摘された行より重い）。
**測って見つけたものなので、指摘の行と分けて書きます。**

⚠️ **#231 には Codex のレビューが1件も付きませんでした（2026-08-18・15:28マージ）が、
今回は理由が分かっています** — `chatgpt-codex-connector` 自身が「Codex usage limits に
達した」とだけコメントし、レビュー本体は一度も投稿されませんでした。**#154・#159・#165・
#167 の4本と違い、初めて「見る前に入った」でも「マージが速すぎた」でもない、
Codex 側の利用上限という原因がコメントで明示されたケース**です。**画面上は他の
0件マージと見分けが付かない**（コメント欄を開かないと usage limit の通知は見えない）
ので、ここに書いておきます。CI は緑でした。#231 から表に移す指摘はありません
（レビュー本体が無いため）。

⚠️ **#234 も同じ形でした（2026-08-19・07:38マージ）** — `chatgpt-codex-connector` が
「Codex usage limits に達した」とだけコメントし、レビュー本体は投稿されませんでした。
CI（`checks`/`build`）は完了から約6分後にマージされており緑でした。#234 から
表に移す指摘はありません（レビュー本体が無いため）。**#231 と合わせて2件目**で、
利用上限による0件マージが単発ではなく繰り返し起きていることが分かる。

⚠️ **#235（この#234の記録を足したPRそのもの）も同じ形でした（2026-08-19・07:57マージ）。**
利用上限が解けていないため、#234 と同じ「usage limits」コメントだけが1分足らずで
付いた。**#231・#234・#235 と3件連続**で、いま現在アカウントの Codex 利用上限が
切れていることが分かる（一時的な現象ではなく、今日はこの状態が続く見込み）。
⚠️ **この棚卸し連鎖はここで打ち切る。** 上限が続く限り、この PR 自身の記録を足す
新しい PR を作っても同じことが起き際限が無い（#204・#206 の連鎖と同じ理由）。
以後、利用上限を理由とした0件マージは**個別の行を都度足さず**、次にこの表を
触る機会にまとめて追記する。実害のあるコードの指摘が別途届いた場合はもちろん
個別に記録する。

⚠️ **#239（2026-08-19・12:20マージ、この節の続き）も0件マージだった**が、
**#231・#234・#235 と違う点がある** — `chatgpt-codex-connector` の
「usage limits」コメントすら付いていない（`get_comments` / `get_reviews` とも0件）。
作成からマージまで約3分と短く、**#159（14秒）と同じ「見る前に入った」形の可能性が
拭えない**（当日の利用上限がまだ続いているのかは、コメントが無いため確認できない）。
上の決めごとどおり個別の行は足さず、ここにまとめて追記する。CI（`checks`/`build`）は
実行中の時点で作成しており、マージ完了は webhook で確認済み。表に移す指摘はない
（レビュー自体が届いていないため）。

⚠️ **#241（2026-08-19・12:40マージ、この節の続き）も0件マージだった。**
`get_comments` / `get_reviews` とも0件で、#239 と同じく usage limits の
コメントすら付いていない。作成から push（マージ競合の解消）を挟んでマージまで
約6分。CI（`checks`/`build`）は push 直後に走り始めた時点（`build` が
`in_progress`）でマージされており、**CI 完了を待たずに入った**（#159・#239 と
同じ「見る前に入った」形）。`npm run reviews:debt` は今回も 401 で使えなかったため
GitHub MCP で直接確認した。表に移す指摘はない（レビュー自体が届いていないため）。

⚠️ **#244（GLS発番と仕入・売上入力の絞り込みの矛盾を直したPR）も0件マージだった**
（作成 13:21:06 / マージ 14:10:03・2026-08-19）。**約49分開いており、#165（1時間28分）・
#167（25分）と同様に待つ時間は十分あった**（#159・#239・#241 の「見る前に入った」形とは
違う）。`get_comments` / `get_reviews` とも0件で、`chatgpt-codex-connector` の
usage limits コメントも付いていない — 原因不明のまま0件だった #165・#167 に近い形。
CI（`checks`/`build`）は完了（13:23頃）から約47分後にマージされており両方緑だった。
`npm run reviews:debt` は今回も 401 で使えなかったため GitHub MCP で直接確認した。
表に移す指摘はない（レビュー自体が届いていないため）。

⚠️ **#262（スマホ最適化の洗い出し対応PR）も0件マージだった**（作成 07:47:10 /
マージ 08:28:50・2026-08-20）。**約41分開いており、#244（49分）と同様に待つ時間は
十分あった**（#159・#239・#241 の「見る前に入った」形とは違う）。`get_comments` /
`get_review_comments` / `get_reviews` とも0件で、`chatgpt-codex-connector` の
usage limits コメントも付いていない。CI は一度 `checks` が `check-changelog.mjs`
で失敗し（`docs/changelog.d/` への下書きを置き忘れていた）、直しを push して
両方緑になってからマージされている。`npm run reviews:debt` は今回も 401 で
使えなかったため GitHub MCP で直接確認した。表に移す指摘はない（レビュー自体が
届いていないため）。

| PR | 重み | どこ | 何が起きるか | 状態 |
| --- | --- | --- | --- | --- |
| #607 | **P1** | `keep-pack-pl.service.ts` の `listUnconfirmed` | 数値報告の注記に**見込へ足していない金額**が混ざっていた — 本番があるのに売上の行が無い案件の最新見積を `unconfirmed` に入れていたため、pptx・Slack の注記が「見込に含めた」と書き、表の数字と合わなかった | ⭕️ 同PR内（e3dce2c。`unconfirmed`＝見込に確度加味で入れた概算の売上／`unregistered`＝売上の行が無い案件（表には入れていない）に分けて書き分け） |
| #607 | P2 | `client-daily` `deckState.ts` の `saveNow` | 自動保存が飛んでいる間に出力・会議日切替・次の debounce が来ると、古い version の写しを取ってから前の保存を待ち、409 で止まっていた | ⭕️ 同PR内（e3dce2c。保存を直列にし、先行の保存を待った**あと**に写しを取る） |
| #607 | P2 | `keep-pack-store.service.ts` の `freezePack` | 60 秒以内の凍結し直しを時刻だけで束ねていたため、数字を直してすぐ確定し直した版が捨てられ、読む人・出力・Slack が古い数字のまま残った | ⭕️ 同PR内（e3dce2c。中身が同じときだけ束ねる `samePackContent`（`keep-pack-identity.ts`）・テスト 6 件） |
| #607 | **P1** | `client-daily` `PagePropertiesPanel.tsx` | 数値報告ページの計上会社セレクタが `options.entity` しか変えず、プレビューと pptx は `binding` で表を引くので**前の表（全体）のまま**出ていた | ⭕️ 同PR内（dc75b02。`plBinding(mode, entity)` で binding と options を必ず一緒に更新） |
| #607 | P2 | 同 | 計上会社別ページで着地↔見込を切り替えると binding が `forecast.all`（1社の表）に化けた | ⭕️ 同PR内（dc75b02。計上会社別は `forecast` / `landing` そのもの） |
| #607 | P2 | `keep-deck-compose.ts` / shared `buildStandardDeck.ts` の `carryOver` | 部品の x/y/w/h を直しても「数字を更新」や次回への引き継ぎでテンプレの位置に戻っていた（人の直しとして記録されるのに消える） | ⭕️ 同PR内（dc75b02。同じ id の部品の位置と大きさを写す。parity テスト＋テスト追加） |
| #607 | P2 | `client-daily` `WeeklyTabs.tsx` | 週報のタブ切替で `?meeting=` が落ち、凍結した過去の数字を見ていたのに別の会議日の構成を開く／作っていた | ⭕️ 同PR内（dc75b02。数字・資料のタブは会議日を持ち回る） |
| #607 | **P1** | `keep-pack-pl.service.ts` の `listUnconfirmed` | 注記の会社を案件の**今の** `entity_code` で束ねていたため、会社をまたいで移した案件や会社の違う概算売上があると、その会社の表に足していない金額を「含めた」と書いた | ⭕️ 同PR内（dc75b02。見込に足す行と同じ**売上行の** `entity_code` で束ね、全体は案件ごとに足す） |
| #607 | **P1** | `keep-pack-pl.service.ts` の `weightedAdditions` | グループ請求（按分）の確度加味が代表案件（`allocations[0]`）のステージを行全体に掛けていた — 按分先のステージが違うと見込の売上・変動費が大きくずれる | ⭕️ 同PR内（a3103be。`keep-pack-sql.ts` の `perProjectRowsSql` で按分の額を各案件へ割り、それぞれのステージで掛ける。シードの 4 月で手計算と一致） |
| #607 | P2 | 同 `listUnconfirmed` | 「売上が未登録」の判定が月を見ておらず、前の月に確定売上がある案件はその月の分が無くても `unregistered` から漏れた（按分先も見ていなかった） | ⭕️ 同PR内（a3103be。その月の確定売上（直接・按分先）だけで判定） |
| #607 | P2 | `keep-deck.service.ts` の `markExported` | 出力した pptx が**どの版から出たか**追えなかった（出力後に自動保存が走ると version だけ進み、Box のファイルが出ていない版のものに見える） | ⭕️ 同PR内（a3103be。`keep_decks.exported_version` に出力した版を記録・ビルダーに「版N を出力済み／出力後に直しあり」） |
| #607 | **P1** | `keep-pack-calendar.service.ts` の `buildTrend` | 推移（売上の積み上げ）がグループ請求の行を代表案件のお客様区分に丸ごと付けていた（見込側は按分していたのに推移だけ按分していない） | ⭕️ 同PR内（1b8b584。推移も `perProjectRowsSql` を通す。検証DBで 2026-03 の外部売上が SQL の直接集計と一致） |
| #607 | P2 | `client-daily` `CompareDialog.tsx` | 「前回の資料と見比べる」が普通の読み口を使い、editor が開くと議事録から導いただけの前回の会議日に**版1を作ってしまっていた** | ⭕️ 同PR内（1b8b584。`?create=0` の読むだけの口 `useDeckReadOnly`・無ければ「まだ作られていません」） |
| #607 | P2 | `keep-deck.routes.ts` の export | 出力に使った凍結パックを記録しておらず、構成を作った後に凍結し直すと `deck.pack_id` とずれ、Box のファイルの数字を再現できなかった | ⭕️ 同PR内（1b8b584。`keep_decks.exported_pack_id` に描画に使ったパックの id を記録） |
| #607 | P2 | `client-daily` `InviewCard.tsx` / `keep-pack-inputs.service.ts` | 満足度が 4.0 満点の表示なのに画面も API も 5 まで受け、「5.0 / 4.0」が資料に出せた | ⭕️ 同PR内（1b8b584。API は 0〜4 以外を 400・画面も `max=4`） |
| #607 | P2 | `keep-pack-reports.service.ts` の内覧会の分類 | 分類を `LIMIT 12` で切り捨て、pptx の「合計」が参加者の合計と合わなかった | ⭕️ 同PR内（1b8b584。全分類を取り、はみ出た分は「その他（N分類）」1 行に足す） |
| #607 | **P1** | `keep-pptx.service.ts` の `renderPart` | 表の部品をタブ区切りで上書きすると、プレビューは表なのに pptx は `addText` で**生の TSV を文として**出していた（「表は表で出す・PowerPoint で直せる」の約束が崩れる） | ⭕️ 同PR内（2aa5781。読み方を `shared/src/keepReport/tsv.ts` に置き（server は `keep-tsv.ts` の写し・parity テスト）、出力も表として出す。人が置いた表の `options.rows` も同じ） |
| #607 | P2 | `client-daily` `PagePropertiesPanel.tsx` / `keep-pptx.service.ts` | 部品の「文字の大きさ」がプレビューにしか効かず、pptx は既定の大きさのままだった | ⭕️ 同PR内（2aa5781。文・箇条書きの出力に渡す（キャンバスの px × 0.75 = pt）。効かない部品では欄を出さない） |
| #607 | P2 | `keep-pack-pages.service.ts` の `buildProjectPages` | 確定売上の**合計 > 0** で実績と見ていたため、値引き調整で合計が 0 や負になった実績が最新見積に戻り、資料と Slack に見込の額が実績として出た | ⭕️ 同PR内（2aa5781。`getSummaries` の `revenue_count`（行数）で判定・合計 0 以下は粗利率を出さない） |
| #607 | P2 | `client-daily` `KeepSection.tsx` | 「資料をつくる」ボタンが `?meeting=` を落とし、過去の会議日を見ていても別の会議日の構成を開く／作った（タブ側の直しはこのボタンには効いていなかった） | ⭕️ 同PR内（2aa5781。タブと同じく会議日を持って渡る） |
| #607 | P2 | `client` 取引先マスター（`company/types.ts`・`CompanyFormFields.tsx`） | `companies.samurai_group`（サムライ関連の印）を画面から付ける手段が無く、backfill に当たらなかったお客様は外部のヨミ表に固定されていた | ⭕️ 同PR内（2aa5781。登録・編集フォームに「サムライ関連のお客様」のチェック。POST/PUT の payload に載る） |
| #607 | **P1** | `project.service.ts` の `getSummaries` | 新設の `revenue_count` が按分の側で status を見ず、**概算（`status='estimate'`）のグループ請求の按分まで「実績がある」と数えた** → 案件ページ・実施報告が概算の按分額を実績として出す | ⭕️ 同PR内（2cf31d5。行数だけ `COUNT(*) FILTER (WHERE status = 'confirmed')`。按分の**合計**は台帳と同じく変えていない → 次の ❓ 行） |
| #607 | **P1** | `keep-pack-store.service.ts` の `getPackForMeeting` | 週報の確定が全体／全区分しか凍結せず、計上会社・お客様で絞ると凍結版を外れて**「いまの数字」に落ち**、確定した週報と違う数字が出た（Slack／MCP の絞り込み付きの読みも同じ） | ⭕️ 同PR内（2cf31d5。`freezeMeeting` で 12 通り（計上会社 4 × お客様区分 3）を全部凍結・`meeting_frozen` を返し、古い凍結で版が無い絞り込みは画面が「いまの数字」と書く） |
| #607 | — | `project.service.ts` の `TOTAL_REVENUE_SQL` / `getSummaries` の按分の**合計** | **自分で見つけた（Codex の指摘ではない・6 巡目の対応中）**: 台帳の「確定売上」の合計が、按分（`revenue_allocations`）の側では status を見ていない。グループ請求は概算（`status='estimate'`）でも作れる（`project-groups.routes.ts`）ので、**概算のグループ請求の按分額が案件の確定売上・粗利に混ざる**余地がある。#607 では行数（`revenue_count`）だけを confirmed に絞り、合計は「数え方を 2 つにしない」ため台帳のままにした | ❓ 未確認（実データに概算のグループ請求があるか・按分の合計に status を掛けると台帳・案件詳細・キープ資料の数字がどう動くかを見てから決める。直すなら `TOTAL_REVENUE_SQL` と `getSummaries` を同じ回で） |
| #605 | **P1** | `server/src/routes/index.ts`（`createAwardsRoutes()`の撤去） | 旧`awards_entries.photo_url`が`/api/v1/internal/awards/images/...`という絶対パスを値としてそのまま持ち、テロップCGの過去実績移行ツールがこの文字列を書き換えずに`RankingEntry.photoUrl`へコピーするため、`createAwardsRoutes()`をまるごと外すと既に移行済み・今後移行するランキングの顔写真が全て欠ける | ⭕️ 同PR内（`ff47636`。`images.routes.ts`を読み取り専用の`imageServingRouter`と書き込み系に分離し前者だけ`createAwardsImageRoutes()`経由で残した。`verify:up`の実サーバーで実在ファイルが実際のバイト列で返ることを確認） |
| #605 | P2 | `.github/workflows/preview.yml:158` | `client-awards`の配信撤去後もプレビューデプロイのたびに廃止済みの`/awards/`をリンクとして出し続け、押すとホームへ戻るだけの死んだ検証先を提示していた | ⭕️ 同PR内（`ff47636`。該当行を削除） |
| #605 | P2 | `client/src/components/layout/searchFeatures.ts` | APPS走査が`hidden`は見るが`frozen`を見ておらず、⌘K検索に廃止済みの`awards`が死んだリンクとして残っていた | ⭕️ 同PR内（`875f082`。`visibleApps()`の既定と同じく`frozen`も除外する1行を追加。`npx tsc -b client`・`npm run lint`で確認——⌘K検索の実ブラウザ確認はしていない） |
| #606 | P2 | `codex-findings-v4.md`（#605のエントリ） | #605の3件の指摘（上の3行）が集計表に無かった／「Code Review未実行の連鎖」という表現が4巡レビュー済みの#595を挟んでおり不正確だった／「いずれも実サーバーで実測」が過大だった、の3件。#606（本PR）はCI green後まもなくマージされ、直す前に指摘が画面から消えた | ⭕️ #608（3件とも#605のエントリを訂正する形で反映。以降この記録自身の記述精度を巡る指摘は、実害のあるコードバグでない限り新しいPRを割らずまとめて扱う——`#206`と同じ扱い） |
| #521 | **P1** | `project-health.ts` の `completeElapsedWonProjects` | **レギュラー案件が、2回目の収録を迎える前に自動で完了へ落ちる** — 判定が `stage='a_won' AND event_end < 今日` だけで `recurrence` も `episodes` も見ない。レギュラーの `event_end` には第1回の日付が入るため、**受注した瞬間に「もう終わった案件」になる**。⚠️ **本番で観測済み**: `GLS-A007「インテリジェンス」` が 2026-09-01 **04:05:33 に受注済＋レギュラーへ設定 → 04:16:46 に `system:project_tidy` が `s_completed` へ**（11分後）。落ちたあとは v4.5.14 の後始末が BOX を `98_終了案件` へ移す（この案件は 2026-08-31 にも一度移され、`box_cleanup_note` に「internal: 戻した / external: 戻した」と人が戻した跡が残っている） | ❌ 未着手（**直し方は決まっている** — [regular-series.md](../design/v4/regular-series.md) §5。①日次ジョブの対象から `recurrence='regular'` を外す ②完了の条件を「残っている回が0本」にする ③**回が0件のレギュラーは絶対に完了させない**。⚠️ **逆に `event_end` を空にして逃げると `NULLIF(...) IS NOT NULL` に弾かれ、永久に `a_won` に残る**ので回避策にならない） |
| #521 | P2 | `TasksTab.tsx:156` / `project.service.ts:666`・`:1323` | **`recurrence='regular'` を選んでも、画面もサーバーも何ひとつ変わらない** — 回UIの出し分けは `gls_category === 'A'` で代用されており、サーバー全体で `'regular'` を分岐に使う箇所は0。**単発のスタジオ案件には回が出て、レギュラーのビジネス案件には出ない**。利用者から見ると「選べるのに効かない項目」 | ❌ 未着手（1行の付け替えで済むが、**先に P1 を塞がないと「regular にした案件が翌朝消える」ほうが目立つ**。順番は §10） |
| #521 | P2 | `docs/design/v4/mockups/README.md` の世代方針 | **「正のモックに絵が無い」を理由に機能を落とす判断が、また起きうる** — レギュラーがこうなった原因そのもの（2026-08 に `v4-live-sales.dc.html` の7タブに無いからタブごと削除）。今回はロジックを独立文書にして逃がしたが、**正のモック側との整合は取っていない**ので、次に誰かが正のモックだけを見れば同じ判断に至る | ❌ 未着手（**直さないと決めたのではない**。案: 正のモックに「レギュラーは別紙」と1行入れるか、`README.md` の世代表に「絵が無い＝仕様が無い、ではない」を明記する） |
| #521 | P3 | `regular-series.md` §2（収録日をテーブルにしない判断） | **収録日そのものに固有の情報を持たせたくなったとき、置き場が無い** — 「2本撮り」の注記・その日だけのスタジオ変更などは `episodes.recording_date` の GROUP BY では表現できない。⚠️ 意図してテーブルを作らない判断（同じ事実に2つ目の置き場を作らないため）だが、**穴であることに変わりはない** | ❌ 未着手（当面は `studio_bookings` 側に寄せる想定。**実際に足りなくなったときに、実物を見てから決める**） |
| #521 | P3 | `regular-series.md` §4（回の状態をタスクから導出する判断） | **タスクが1件も無い回は、収録が終わっていても永久に「未着手」のまま** — 状態を `episodes.status` に書かず完了率から導くので、工程テンプレートが当たっていない回は動かない。⚠️ `episodes.status` は自由文で誰も書いておらず、2つ目の真実を作らないための判断だが、**レギュラー用の標準工程テンプレート（§10 の 8番）が無いうちは、ほぼ全部の回がこの状態**になる | ❌ 未着手（テンプレートを作るまでの間、状態列は「—」に見えるのが正直。**「タスクが無いので状態が決まらない」と画面に出す**のが最小実装 — #495 の学びと同じ形） |
| #521 | P3 | `regular-series.md` §9（定例イベントを対象外にした判断） | **件数では多数派の定例イベント（カンファレンス10件・決算説明会10件超・株主総会9社×毎年）に、受け皿が何も無い** — 毎回が独立した GLS と請求先を持つのでレギュラーにはしない、という判断は正しいが、**その結果いまも人が案件名の末尾（`2025_3Q` / `(26.03.17)` / `#1-2`）で系列を見分けている**。命名規則は人によってバラバラ | ❌ 未着手（別の機能。「前回から複製」が最小実装で、金額が毎回同一の系列が多い＝効果は大きい。⚠️ **レギュラーと混ぜて設計しないこと**） |
| #209 | P2 | `CLAUDE.md` | v4.1.4のエントリを要約に置き換えたが、`docs/version-history.md`にv4.1.4の全文アーカイブが無かった。`scripts/generate-version-history.mjs` はCLAUDE.md＋アーカイブの両方から画面のバージョン履歴を組み立てるため、このまま要約だけ残すと除いた実装詳細・制限事項が生成物から完全に消える | ⭕️ 同PR内（`cfbd27d` で要約に置き換えた直後の指摘。後続の `527acc6` でv4.1.4の全文を`docs/version-history.md`へ追加して埋め合わせた） |
| #206 | P2 | `codex-findings-v4.md` | #204 で足した「resolve済みでも記録する」一般則を実践して #202 の5件を記録したところ、複数巡にわたって指摘が続けて付いた（要約が古い回のことだと分からない／FKの実際の挙動と説明が食い違う／`❌` 行の確認日が未定義→定義しても実際には「マージ後に届いたレビュー」を説明できず矛盾／`npm run reviews:debt` を表のカウント手段として誤案内／`is_customer`/`is_vendor` の述語名を取り違え／数え方の grep コマンドが拾えない `⚠️` 始まりの部分修正行（状態欄を ❌ 始まりの行へ分割して解消）／**この #204・#206 自身が受けた指摘が表に無い**／main 統合時のコンフリクト解消で複数の訂正・行が失われる）。**行として書いた内容自体を Codex が読んで裏を取り、正確さを崩す表現を見つけ続けた**形で、1件ずつ表に起こすと際限がないため、ここに1行へまとめて記録する（⚠️ **正確な巡数・件数はここに書かない** — この行自体も度々更新されており、書くたびに古くなるため。この節の「数え方」に従い、必要なら PR #206 のレビュー履歴を直接数えること） | ⭕️ 同PR内（各指摘は本文・該当行の記述を都度訂正。**ここでこの棚卸し連鎖PR自体の記録は打ち切り**——以降このドキュメントの記述精度を巡る新しい指摘が付いても、実害のあるコードバグ以外は都度PRを割らずまとめて扱う。⚠️ #206 が記録した #202 の未対応2行（仕入先検索・並び替え／SGA新規仕入先の検証すり抜け）は、この後 PR #207 が修正して ⭕️ に更新済み——下の #202 の行を参照） |
| #205 | P2 | `vendors.routes.ts` | 財務の仕入先タブの一覧が、表示（`VENDOR_FIELDS`）は `vendors` から読むよう直した（#202 同PR内 `28ac86a`）のに、検索条件・並び替えは `co.name`/`co.vendor_type` のままだった。`budget:editor`（`sales:owner`無し）が付けた新しい名前で検索すると0件になり、古い名前でなら検索できてしまっていた。⚠️ **この行自体、最初は誤って `#202` の欄に記録していた**（レビュー指摘・PR #205 4巡目 — `scripts/review-debt.mjs` の `recordedPrs()` は表の1列目の PR番号だけを見るため、見つかった PR（#205）ではなく原因のコードがあった PR（#202）の欄に書くと、`#205` 自身は「記録済み」と認識されない） | ⭕️ #207（`e72e6a2`。検索条件・並び替えも `vendors` から読む形に修正。#207 は2026-08-18にマージ済み） |
| #205 | P1 | `codex-findings-v4.md` | 上の3行（`purchases.routes.ts`・`kessan-import.service.ts`・`vendors.routes.ts`）の修正が PR #207 に push 済みなのに、未マージのまま状態欄を `⭕️` と書きかけていた。「消えるのは未解決の指摘だけではない」ため resolve 済みかどうかは関係なく記録する決めごとと同様に、**「push 済み」と「マージ済み」も同じ理由で区別する必要がある**（マージ前のコードにはまだ穴が残っているため） | ⭕️ 同PR内（`a673b25`。該当3行の状態を「❌ 修正は push 済みだが未マージ」に訂正。#207 マージ後に改めて ⭕️ へ更新する） |
| #205 | P2 | `codex-findings-v4.md` | リベース前の下書きに残っていた「resolve済みの例外を PR 番号で列挙する」形の注記（`#199の5件・#201の1件・#202の5件…`）が、#204 で撤廃されたのと同じ注記のまま復活しかけていた | ⭕️ 同PR内（`7968ac0`・main への rebase で #204 後の非列挙形の注記を採用し、列挙を復活させなかった） |
| #205 | P2 | `codex-findings-v4.md` | 「未解決だったものは #146〜#150 で片づき、残っているのは1件だけ」という要約が `#106`〜`#170` の回限定の集計であることを明示しておらず、`#202`/`#205` に新しく足した3行の未マージ ❌ を読者が見落とす形になっていた | ⭕️ 同PR内（`973cca2`。要約が `#106`〜`#170` 限定であることを明記し、`#202`/`#205` に残る3行の未マージ ❌ を見落とさないよう注記を追加した） |
| #204 | P2 | `codex-findings-v4.md` | #203 の指摘を1件足したが、resolve済みの例外を PR 番号で列挙する注記（「#199 の5件・#201 の1件だけは例外」）を直さなかったため、#203（同PR内でresolve済み）が列挙から漏れ、注記と表の内容が再び食い違っていた。⚠️ **`#200`→`#201`→`#203`→`#204` と4本連続で同じ形の指摘が付いた** — 列挙という設計そのものが、列挙を足す PR 自身の指摘を際限なく生む構造だった | ⭕️ 同PR内。**列挙をやめ、`#199` 以降は resolve 済みかどうかに関わらず記録する一般則に書き換えた**（個別の PR 番号を持たない形にしたので、以後この形の指摘は起きない） |
| #203 | P2 | `codex-findings-v4.md` | #201 の指摘を1件足したが、上の「resolve済みの例外は #199 の5件だけ」という注記を直さなかったため、#201（同PR内でresolve済み）が例外の一覧から漏れ、注記と表の内容が再び食い違っていた | ⭕️ 同PR内（`81e4885`。例外の注記に #201 を追加し、検証日の注記も揃えた。**その後 #204 で、列挙という設計自体をやめる形に置き換えている**） |
| #202 | **P1** | `vendors.routes.ts` | Phase 3-2b で財務の仕入先タブの一覧・詳細・PUT応答を `companies` から読むよう変更したが、`budget:editor` が `sales:owner` を持たないと下の PUT は `companies` へ同期しない（`sales:owner` の壁を守るための意図的な仕様・PR #183 P2）。そのため保存した直後の画面にも古い値が出続け、`budget:editor` にとって仕入先の編集が実質効かなくなっていた | ⭕️ 同PR内（`28ac86a`。基本情報（名前・連絡先等）は書き込み先である `vendors` から読み、`companies` 固有の列（ロール・支払条件の例外）だけ `companies` から読む形に修正。実DBで budget:editor（sales:owner無し）が保存した直後に一覧・詳細・PUT応答すべてへ新しい値が反映され、かつ `companies` は書き換わらない（権限の壁は健在）こと、system_admin の保存では `companies` も同期されることを確認済み） |
| #202 | P2 | `xpoint.routes.ts` | Phase 3-2b で `purchases.vendor_id` / `sga_expenses.vendor_id` が `companies.id` を直接指すようになったことで「仕入先ロールの会社か」という DB の保証が失われたが、`POST /xpoint/files/:id/register` はレビューUIから直接渡される `purchase.vendor_id` / `sga.vendor_id` を検証せず INSERT していた（他の書き込み経路は `assertVendorCompanyId` で確かめていた） | ⭕️ 同PR内（`28ac86a`。仕入・販管費どちらの登録にも `assertVendorCompanyId` を追加） |
| #202 | P2 | `xpoint.routes.ts` | 上の行の検証は `if (s.vendor_id)` の中だけで、SGA側で `new_vendor` を指定して新規作成・再利用した会社（`createdVendorId` 経由）はその外なので検証をすり抜けていた（2巡目レビュー） | ⭕️ #207（`3ad44fc`。検証条件を `if (s.vendor_id)` から実際に使う値 `if (sgaVendorId)` に変更し、`createdVendorId` 経由の値も必ず検証されるようにした。あわせて `new_vendor` の再利用クエリ自体も `vendors.name` だけで引く形から `companies.is_vendor = TRUE` かつ生きている `vendors` 行を要求する形に修正した。#207 は2026-08-18にマージ済み） |
| #202 | P2 | `201_vendor_fk_to_companies.sql` | この FK 変更で、`company-directory.service.ts` を経由しない dev専用の決算取込フォールバックスクリプト（`server/scripts/import-kessan-dev.mjs`）の `findVendor`/`ensureVendor` が壊れる。生SQLのまま `vendors.id` を返し続けており、既存の仕入先は FK違反に、`--create-masters` で新規作成しても `company_id` の無い孤立行のまま FK違反になる | ⭕️ 同PR内（`28ac86a`。`companies` にも紐づけてから `company_id` を返すよう修正。同じ穴が Phase 3-2a の時点から `findCustomer`/`ensureCustomer` にも残っていたため、あわせて直した。検証DBに対して新しいINSERT文を単体実行し、FK違反なく `purchases.vendor_id` に `companies.id` が入ることを確認） |
| #202 | **P1** | `purchases.routes.ts` | 1巡目の直し（`28ac86a`）は財務の仕入先タブ自身の応答だけを `vendors` から読むよう直したが、仕入台帳一覧・財務Excel入出力・MCP・バックアップ出力・グループ按分仕入・仕入先別集計・決算取込の重複チェック表示・全体検索・X-Point取込の重複チェック表示は依然として `companies` の名前を読んでおり、`budget:editor`（`sales:owner`無し）が保存した直後もそれらだけ古い社名が出続けていた（2巡目レビュー） | ⭕️ #207（`3ad44fc`。全箇所を `vendors` から読む形に修正のうえ、3巡目・4巡目レビューで見つかった LEFT JOIN の抜け穴（仕入先削除で名前が消える）も `COALESCE(vendors.name, companies.name)` で塞いだ。#207 は2026-08-18にマージ済み） |
| #202 | P2 | `kessan-import.service.ts` | 決算取込の `ensureVendor` が、仕入先ロールを外された・削除済みの会社でも `vendors` 行が生きていれば `company_id` を返してしまう（同ファイルの `findCustomer` は `companies.is_customer = TRUE` かつ生きている `customers` 行を要求しているのに、`ensureVendor` は `vendors.name` だけで引いていた・2巡目レビュー） | ⭕️ #207（`3ad44fc`。`findCustomer` の仕入先版として、`companies.is_vendor = TRUE` かつ生きている `vendors` 行を要求する形に修正。#207 は2026-08-18にマージ済み） |
| #201 | **P1** | `gpm.service.ts` | #201 が足した GPM の顧客ロール検証（下の #200 の行）は「`customer_id` が渡されたら毎回確かめる」形で、既存値との比較が無かった。GPM 詳細の段変更・直すダイアログはどちらも**今の customer_id を送り直す**ので、あとから顧客ロールを外された会社のプロジェクトは、customer_id を変えていない**無関係な直し**（段を進める・名前を直す等）まで 400 で止まってしまっていた。同じ形の穴が案件（`project.service.ts`）・活動記録（`activity-log.service.ts`）・売上（`revenues.routes.ts`）の update にも既にあった | ⭕️ 同PR内（`9223c33`。4か所とも「既存値と実際に変わったときだけ確かめる」形に直した。実DBで①生存顧客で作成→②ロール除去後もcustomer_id不変の直しは通る→③別のロール無し会社への変更は拒否、を確認済み） |
| #200 | **P1** | `200_customer_fk_to_companies.sql` | この行（下の #199 の行）を「⭕️ 同PR内」と書いたが、`docs/deploy-pipeline.md` 本文は訂正しても migration 200 冒頭のコメントには「Releasesのタグで再実行すればDBも含めて丸ごと戻る」という誤った手順がそのまま残っていた。本番のロールバック時にこのコメントだけを見た運用者が、直したはずの危険な手順をそのまま実行できる状態だった | ⭕️ #201（コメントを訂正し `docs/ops/db-backup-restore.md` を指すよう統一） |
| #200 | P2 | `gpm.service.ts` | 「顧客ロールを検証するようにした」という行（下の #199 の行）は案件・売上・活動記録・見積の書き込みだけを指し、プロジェクト管理（GPM）の `gpm.service.ts` の create/update（`POST`/`PUT /gpm/projects`）は別サービスで `assertCustomerCompanyId` を素通りしていた。仕入先のみ・ロール無し・削除済みの会社をGPMプロジェクトの依頼元にできる穴が残っていた | ⭕️ #201（GPM の customer_id 書き込みにも同じ検証を追加。`customer_id` 省略時に寄せる `SELF_CUSTOMER_ID` は内部の固定行なので検証しない） |
| #200 | P2 | `codex-findings-v4.md` | 棚卸し表に #199 の1巡目レビュー5件（旧FKの外し忘れ・削除済み顧客の残留・ロールバック手順の誤記・顧客ロールを外すと過去売上が0に見える・旧URLでのoverview 404）が抜けていた。2巡目の4件だけを記録しており、マージ前にresolve済みだった1巡目分がこのまま埋もれるところだった | ⭕️ 同PR内（`ebf10bc`。1巡目5件を表に追加） |
| #200 | P2 | `docs-pr199-review-debt.md` | changelogの下書きが「4件を棚卸しに記録した」と書いていたが、棚卸し表自身の注記は「ここに足した指摘はすべて is_resolved: false のままマージされています」と言い切っており、resolve済みの#199の指摘を足すとこの2つの記述が両立しない | ⭕️ 同PR内（`ebf10bc`。#199の5件は resolve済みの例外だと注記を追加し、changelogの下書きも巻き取った） |
| #200 | P2 | `codex-findings-v4.md` | 「⭕️/❌ は2026-08-15にコードを読んで付けている」という検証日の注記が、2026-08-18に追加した#199の9行にもそのまま掛かって見え、実際の検証日と食い違っていた（監査としての来歴が破綻する） | ⭕️ #201（#199の9件は例外として2026-08-18に確認したことを注記に追加） |
| #199 | **P1** | `200_customer_fk_to_companies.sql` | データが入っている実DBでは、旧FK（customers(id) 参照）が値の書き換え前に生きたままだと、`customer_id` を customers.id と一致しない companies.id に書き換えた瞬間に違反し migration 200 全体がロールバックする。空の検証DBでは customers.id と companies.id が作成順で偶然一致することがあり、それが隠れていた | ⭕️ 同PR内（`ea1262c`。値を書き換える前に旧FKを DROP → UPDATE → 新FK追加の順に直した） |
| #199 | P2 | `customers.routes.ts` | `DELETE /customers/:id` は customers 側だけを論理削除し companies.is_customer は触らないため、一覧・詳細・検索・ドロップダウンが companies だけで判定していると削除済みの顧客が残り続ける | ⭕️ 同PR内（`ea1262c`。customers への JOIN を LEFT→INNER に変更） |
| #199 | **P1** | `docs/deploy-pipeline.md` | 本番の production セクションは選択したコミットの checkout・イメージの pull・`app_prod` の再作成しかせず、DBの復元やmigrationの巻き戻しをしない。`_migrations` は forward-only なので、旧リリースを再実行してもFKの張り替え後の値はそのまま残り、記載どおりに読むとDBも含めて戻ると誤解する | ⚠️ #199（`ea1262c`）で `docs/deploy-pipeline.md` 本文は訂正したが、**`200_customer_fk_to_companies.sql` 冒頭のコメントに同じ誤った手順（「Releasesのタグで再実行・DBも含めて丸ごと戻す」）が残っていた**（レビュー指摘・PR #200 P1）。⭕️ #200（コメントを訂正し `docs/ops/db-backup-restore.md` を指すよう統一） |
| #199 | P2 | `companies.routes.ts` | 確定売上のある会社の顧客ロールを外すと `is_customer` は false になるが、紐づく顧客・過去の売上行はそのまま残る。会社サマリーが `is_customer` で絞っていたため、ロールを外した瞬間に過去の確定売上がゼロと表示されていた | ⭕️ 同PR内（`ea1262c`。`is_customer` での絞りを外し、常に companies.id で revenues を見るようにした） |
| #199 | P2 | `customers.routes.ts` | 移行前の customers.id（旧URL・端末の「最近見た」履歴・共有リンク）で顧客詳細を開くと、overview は companies.id 専用になっていたため顧客が存在するのに「見つかりません」画面になっていた | ⭕️ 同PR内（`ea1262c`。companies.id で見つからなければ customers.id として解釈し直すフォールバック `findCustomerRow` を追加） |
| #199 | P2 | `customers.routes.ts` / `CustomerDetailPage.tsx` | 移行前の customers.id（旧URL・端末の「最近見た」履歴・共有リンク）で顧客詳細を開いたまま活動を記録・保存すると、書き込みが旧IDのままで `activity_logs.customer_id` 等のFK（companies.id 参照）に反し失敗する。1巡目で足した正規ID解決は GET だけで、書き込みには使われていなかった | ⭕️ 同PR内（書き込みは正規の companies.id を優先し、`PUT`/`DELETE /customers/:id` も customers.id を解決するようにした） |
| #199 | P2 | `mcp/tools/customers.tools.ts` ほか7か所 | `DELETE /customers/:id` は customers 側だけを論理削除するため、companies だけを見る他の読み口（MCP の list_customers/get_customer・重複ガード、GPMドロップダウン、全体検索、Excel入出力2本、決算取込、投入口、内覧会予約）には削除済みの顧客が残り続け、その id を create_project 等に渡せた。1巡目の修正は customers.routes.ts の1か所だけだった | ⭕️ 同PR内（生きている customers 行があることを要求する条件を8か所に追加） |
| #199 | P2 | `200_customer_fk_to_companies.sql` | FKが companies(id) を直接指すようになったことで「顧客ロールの会社か」というDBの保証が失われ、仕入先のみ・販管費支払先のみ・ロール無しの company_id でも案件・売上・活動記録・見積の書き込みが通ってしまう | ⚠️ #199 で `assertCustomerCompanyId` を新設し案件・売上・活動記録・見積の書き込みに追加したが、**プロジェクト管理（GPM）の `gpm.service.ts` の create/update は別サービスで、検証を素通りしていた**（レビュー指摘・PR #200 P2）。⭕️ #200（GPM の customer_id 書き込みにも同じ検証を追加） |
| #199 | P2 | `customers.routes.ts` | migration 061〜195 の間に customers 側だけ名前・連絡先を直した行は companies が古いスナップショットのままで、一覧・詳細・PDF・Excel・検索が companies を正として読むようになったことで古い値が出てしまう | ⭕️ 同PR内（migration 200 に、既にリンク済みの companies 行を customers の現在値で上書きする一括更新を追加） |
| #197 | P2 | `codex-findings-v4.md` | #194の指摘の状態を「⭕️ #195」と書いていたが、#195時点のツリーはまだ不正確な記述のままで、実際に直したのは#197だった（トレーサビリティが誤誘導になる） | ⭕️ 同PR内（参照先PRを修正） |
| #197 | P2 | `claude-token-consumption-analysis-ebzhpw.md` | version-history.mdが文脈に乗る条件を「エージェントがRead/Grepで返した時だけ」と書いていたが、`cat`/`sed`/`git show`等本文を返す呼び出し全般が同様に文脈化するため範囲が狭すぎた | ⭕️ 同PR内（記述を修正） |
| #194 | P2 | `token-consumption-analysis.md` | `docs/version-history.md` を「読む/生成するたびに直撃」と書いていたが、`v4-progress.mjs` はこのファイルを一切参照しない。`release:notes` は内部で読むが本文を文脈に返さないため乗らない。文脈に乗るのは `cat`/`sed`/`git show` 等を含め本文を返すツール呼び出しがあった時だけ | ⭕️ #197（記述を修正。#195時点はまだ不正確だった） |
| #194 | P2 | `token-consumption-analysis.md` | 「バージョンを1つ上げるだけでCLAUDE.mdに数KB単位で文章が増える」と書いていたが、`collect-changelog.mjs` は3件超で4件目を同時にアーカイブへ退避するため累積肥大しない | ⭕️ 同PR内（記述を修正） |
| #194 | P2 | `token-consumption-analysis.md` | `build:all`/`typecheck:all`/`lint` を「9ワークスペース分9倍」と表現していたが、`typecheck:all`は7クライアント+server、`lint`はチェックスクリプト+eslint1回のコマンド実行であり実態と異なる | ⭕️ 同PR内（記述を修正） |
| #192 | P2 | `198_payment_terms_compat_repair.sql` | 198は「旧列をcompaniesから埋め直す」→「companiesの範囲外月数を正規化」の順で、companiesに範囲外の値が残っていた環境ではその壊れた値が正規化前にcustomers/vendorsへコピーされ、旧列にだけ残り続けてロールバック後も壊れた期日を作り続ける | ⭕️ #192（migration 199 でcustomers/vendorsの旧列を直接正規化） |
| #192 | P2 | `companies.routes.ts` | 支払条件の検証が `Number(false)===0` のような真偽値を数値として受理し、検査は通るが実際のINSERTでPostgresが型エラー（意図しない500）を返す | ⭕️ #192（`toValidInt()` が number/string 型以外を拒否。あわせて検査を通した正規化済みの値を書き込みに使うよう修正） |
| #190 | **P1** | `196_company_payment_terms.sql` | `runMigrations()` は `_migrations` に記録済みのファイル名を二度と実行しない仕組みなので、PR #190 で196の内容を書き換えても**196を実行したことがある環境（`main`へのマージで自動デプロイされる検証環境を含む）には一切届かない**。旧列を消さない改修・月数の範囲チェックがそこだけ欠けたままになる | ⭕️ 同PR内（migration 198 を新設し、`IF NOT EXISTS`/条件つきUPDATEで両方の環境に安全に届く形にした） |
| #190 | P2 | `companies.routes.ts` | 支払条件の検証が `Number(v)` だけを見ており、空文字列（`Number('')===0`）や小数（`1.5`）が範囲チェックを通過したあとINTEGER列へのINSERTでPostgresが例外を返し、意図した400ではなく素の500になっていた | ⭕️ 同PR内（`toValidInt()` で整数判定を追加） |
| #190 | P2 | `196_company_payment_terms.sql` | 月数チェックを足す前に、migration 175時点で範囲チェックの無かった既存の範囲外値（customers/vendors→companiesへ移った値）を正規化していなかった。新規デプロイで既存の範囲外legacy値があると、チェック追加そのものがトランザクション内で失敗し移行が止まる | ⭕️ 同PR内（migration 198 でチェックを足す前に範囲外値をNULLへ正規化） |
| #190 | P2 | `196_company_payment_terms.sql` | POST/PUTで支払条件をcompaniesにだけ書いており、旧イメージへロールバックしている間は旧コードがcustomers/vendorsの旧列を読むため、新規作成した顧客の例外が消え、更新した顧客は古い値に戻って見える | ⭕️ 同PR内（companies.routes.ts のPOST/PUTでcustomers/vendorsの旧列にも同じ値を書くようにした） |
| #188 | **P1** | `196_company_payment_terms.sql` | `customers`/`vendors` の旧支払条件列を削除すると、`docs/deploy-pipeline.md` の「その場でイメージだけ差し替える（急ぎ）」ロールバック（DBはそのまま）で1つ前のイメージに戻した瞬間 `undefined_column` になり、売上作成・支払期日の下見が全滅する | ⭕️ #190（旧列を削除せず、互換のためのリリースを1回挟んでから別migrationで消す方針にした） |
| #188 | **P1** | `money-rules.service` | 会社（`companies`）を削除しても紐づく顧客・仕入先（`customers`/`vendors`）は連動削除されず使われ続けるのに、`companies.deleted_at IS NULL` で絞っていたため会社を消した瞬間に支払例外が消え、既定ルールへ静かに戻ってしまう | ⭕️ #190（`companies` を見るクエリから `deleted_at IS NULL` を外した） |
| #188 | P2 | `companies.routes.ts` | `customer_payment_months`/`vendor_payment_months` に範囲検査が無く、負数や極端に大きい値が `dueDateOf` に渡って壊れた期日を作れた | ⭕️ #190（DBのCHECK制約とPOST/PUTの検査を追加） |
| #188 | P2 | `196_company_payment_terms.sql` | バックフィルが論理削除済みの `customers`/`vendors` 行を対象にしていて、削除済みの古い例外値が生きている会社の値に紛れ込む余地があった | ⭕️ #190（`deleted_at IS NULL` をバックフィルのWHEREに追加） |
| #183 | **P1** | `company-directory.service` | `syncCompanyFromCustomer`/`syncCompanyFromVendor` が、顧客と仕入先を兼ねる会社の**兄弟テーブルを更新していなかった**。1社が両方の役割を持つとき、片方の画面で直すと片方だけ最新になり、次にどちらかを保存したときに古い値で上書きする | ⭕️ #184（この PR は #183 マージ後に別枝で修正済み。書き込み可能CTEで1文にまとめた） |
| #183 | P2 | `company-directory.service` | `createCustomerRecord`/`createVendorRecord` の `companies`→`customers`/`vendors` の2段INSERTが、既定の実行者（HTTPルート・MCP・内覧会・xpoint）では別々の自動コミットだった。片方だけ失敗すると孤立行が残り、押し直すと孤立 `companies` 行が増える | ⭕️ #186（`exec` を渡さない呼び出しは内部で `withTransaction` にまとめた） |
| #183 | P2 | `vendors.routes.ts` | 財務の仕入先PUTが、取引先マスターの共有項目（社名・連絡先）を `companies.routes.ts` が要求する `sales:owner` を経ずに `budget:editor` だけで書き換えられていた（権限の壁のすり抜け） | ⭕️ #186（`sales:owner` を持つ人のときだけ `companies` へ同期するようにした） |
| #181 | P2 | `UserPermissionsDialog` | **自分の権限を保存後リロードなしで反映する」という直し自体が、実際に到達できる経路では効いていなかった**。#181 は「権限の例外」ダイアログの `onSuccess` に `isSelf` 分岐で `refreshPermissions()` を足したが、`UserPermissionsDialog` は対象が `system_admin` だと保存ボタンごと隠す（`MembersPage` を開けるのは `system_admin` だけなので、自分自身を選ぶと必ずこの分岐に入る）。つまり自分の役割・`role` を実際に変える唯一の到達可能な経路は `UserDialog`（役割の付け替え・システム上の区別）で、そちらには反映の仕組みが無かった。あわせて `AuthContext.refreshPermissions` は `permissions` だけを読み直し `currentUser.role` を読み直していなかった点も指摘どおり不足していた（`hasPermission` は先に `role === 'system_admin'` を見るため） | ⭕️ #181（この PR 内で修正済み。`refreshPermissions` を `/auth/me` から `role` も含めて読み直す形に直し、実際に到達可能な `UserDialog` の保存成功時に呼ぶようにした。`UserPermissionsDialog` の `isSelf` 分岐は将来 `isAdmin` 判定が緩んだ場合の保険として残し、到達不能である旨をコメントに明記） |
| #177 | P2 | `GpmProjectListPage` | プロジェクト管理の一覧に足した「おすすめ順」が**サーバーの既定順のまま**で、案件台帳の `sort_by=recommended`（止まっている案件を先に→期限が近い順）と同じ並びになっていなかった | ⭕️ #177（この PR 内で修正済み。`updated_at` を案件の `last_activity_at` の代用にして同じ考え方で並べ直した） |
| #170 | **P1** | `docs/changelog.d/` | ⚠️ **リリースノートが作れなくなる**。`collect-changelog.mjs` は**版1件ぶんの行**に 12,000 バイトの上限を持つ（`CLAUDE.md` の「現在のバージョン」は**毎ターン文脈に載る**ため）。#170 で下書きを1本足したところ、他の下書きと合わせて **13KB** になり `npm run release:notes` が **exit 1**（実測: 足す前は exit 0・足すと exit 1）。⚠️ **この門を踏むのはリリースを出そうとした人**で、下書きを書いた人には見えない。⚠️ **原因は私の下書きが長すぎたこと** — 決めごとは「**載せたい文を1つ置く**」なのに 20 行以上書いていた。自分の3本を 6,722 → 4,110 バイトに詰めた（**他の PR の下書きは触っていない**）。⚠️ **余りは約 1,100 バイトしかない** — 次に下書きが1本増えるとまた止まる（そのときは要約＋アーカイブに分ける。`collect-changelog.mjs` が手順を出す） | ⭕️ #170 |
| #170 | P2 | `docs/reviews/codex-findings-v4.md` | **表の「状態」に `#PLACEHOLDER` を残したまま出した**。行を足す時点では自分の PR 番号が分からないので仮で置き、PR を作ってから入れる手順にしていたが、**入れる前にレビューが走った**。⚠️ **そのままマージされると、直したと書いてあるのに追える先が無い**（この表の目的そのものを失う）。**入れてから push した**（`dd8cd5a`。レビューが見た `5f04942` の次のコミット） | ⭕️ #170 |
| #169 | P2 | `docs/changelog.d/…-review-debt.md` | **版の下書きに書いたリンクの相対パスが、3つの置き場所のうち2つで壊れる**。下書きは `docs/changelog.d/` → ルートの `CLAUDE.md`/`README.md`（`release:notes`）→ `docs/version-history.md`（アーカイブ）と**移動する**のに、`(reviews/codex-findings-v4.md)` と書いたので **`docs/changelog.d/reviews/…`** と **ルートの `reviews/…`** を指す（どちらも無い）。正しく解決するのはアーカイブに入ってからだけ。⚠️ **どの相対リンクを書いても必ずどこかで壊れる** — ルートの `CLAUDE.md` が使っている `(docs/reviews/…)` も、アーカイブに移った瞬間に `docs/docs/…` になる。`docs/version-history.md` が**リンクにせず素のパス**で書いているのはこのため。⚠️ **`check-links.mjs` は `App.tsx` のルートしか見ないので `npm run lint` では捕まらない** | ⭕️ #170 |
| #164 | **P1** | `StudioBookingDialog` / `useProjectActions` / `UnifiedCalendarPage` / `StudioCalendarPage` / `HoldListPage` / `useProjectForm` | **予約を書き換えても案件側を読み直していない**。#164 はサーバーで「予約 → 案件の実施日」を引き直すようにしたのに、画面が落とすのは予約の鍵2つだけ。共通の `QueryClient` は `staleTime: 60_000` ＋ `refetchOnWindowFocus: false` なので、**予約を動かして案件詳細・案件一覧に戻ると最大60秒は古い実施日が出る** — ⚠️ **#164 が直そうとした症状そのものが残っていた**。⚠️ **エラーは出ず、60秒待てば直るので誰も報告しない**（実測: 直す前は6か所中5か所が予約の鍵だけ。6か所目の `useProjectForm` は**検査を書いて初めて見つかった**） | ⭕️ #165 |
| #164 | P2 | `project-event-dates.service` | **同じ案件を2人が同時に触ると、古い期間で上書きされる**。集計と書き込みが別の口で、囲いも掴みも無い。⚠️ **消えたことは画面のどこにも出ない** — カレンダーには予約が見えているのに実施日だけが古く、どちらが本当かは予約を1件ずつ開くまで分からない（実測・実 Postgres: 掴んでいるあいだに 8/28 の本番を足すと、**直す前は 8/25 で上書きして 8/28 が消える**／直した版は 8/28 まで伸びる） | ⭕️ #165 |
| #161 | P2 | `equipmentList/RentalGroupRow` | **640〜767px で機材名が消える**。`hideOnMobile` が列を出し始める幅（640px）と `stackOnMobile` が折り返しをやめる幅が同じなので、**列が増えるのと1行に詰めるのが同時に起きる**。固定列 504px ＋ 隙間 ＋ 余白で、伸びる列（機材名）に残るのは **8px**（実測）。⚠️ **375px と 1280px では正常**なので、その2つで確かめる限り一度も見えない | ⭕️ #165 |

### 同じ幅の壊れ方（#161 を直すときに測って見つけたもの・指摘ではない）

| PR | 重み | どこ | 何が起きるか | 状態 |
| --- | --- | --- | --- | --- |
| （#165 で計測） | P2 | `catalog/CatalogRows` | **640px で −156px・768px でも −28px** ＝ **行が横にはみ出す**（指摘された貸出の塊より重い）。固定列 648px ＋ 隙間 84 ＋ 余白 32 = 764px | ⭕️ #165 |
| （#165 で計測） | P2 | `settings/RentalRulesTab` | 640px で商品名に残るのは **4px**。⚠️ 表頭は `hidden sm:flex` で全列を出すので、**見出しだけ出て中身の無い列**にもなっていた | ⭕️ #165 |
| （#165 で計測） | P2 | `MaintenancePage` / `settings/LocationsTab` / `LendingListPage` | 640px で名前に **136 / 164 / 200px**。**壊れてはいない**（`truncate` が効いて何の行かは読める）が、台帳が決めた下限（`NAME_MIN_PX = 200`）には届かない。⚠️ **直していません** — 壊れている3つと同じ回で触ると、直した範囲が測れなくなる。`shared/tests/rowNameWidth.test.ts` の下限を 128px にしてあるので、**これ以上狭くなれば止まる** | ❌ 直さないと決めた（狭いだけで読める） |
| （#165 で計測） | P2 | `catalog/CatalogRows` | 1024px ちょうど＋左メニュー 248px だと、全列を出した状態で**なお −36px はみ出す**。⚠️ **これは今日も同じ**（`HIDE_UNTIL_WIDE` は狭い側だけを変えるので **1024px 以上の見え方は1ピクセルも変えていない**）。1280px では 220px 残るので、実害はこの幅の帯だけ | ❌ 直さないと決めた（今日と同じ・別の回で幅の段から見直す） |

| PR | 重み | どこ | 何が起きるか | 状態 |
| --- | --- | --- | --- | --- |
| #160 | P2 | `check-contrast-tokens.mjs` | ⚠️ **締めすぎて、正しい書き方を止めていた**。`bg-warning/100` は `bg-warning` と**同じ不透明**なのに、#159 の指摘②を直すとき `/` を一律で撥ねたので「塗りではない」と判定する。この検査は `npm run lint` に入っているので、**その書き方をした人は lint が通らず何も進められない**。⚠️ **緩すぎを直した回に、逆へ振りすぎて生まれた** — 同じ検査への指摘は**これで3件目**（緩すぎ2・締めすぎ1）。実際に一度チェックボックス2件を誤って止めている | ⭕️ #160 |
| #159 | P2 | `check-contrast-tokens.mjs` | **足した検査が `.tsx` しか見ていない**。この製品はクラス名を **`.ts` の地図**に置く形が多く（`hoursTypes.ts` / `taskList/state.ts` / `inbox/kinds.ts` など10ファイル以上）、Tailwind の `content` も `**/*.{js,ts,jsx,tsx}` で拾う。⚠️ **地図に1行足すだけで画面に出るのに、検査は「OK」と言う**（実測: 仕込みの `.ts` が素通り。走査 534 → 741 ファイル） | ⭕️ #160 |
| #159 | P2 | `check-contrast-tokens.mjs` | **条件つき・半透明の塗りを「塗り」と数える**。`hover:bg-warning` は**押していないときは淡い帯のまま**、`bg-warning/10` は**10% で下の淡い面が透ける**のに、どちらも塗り扱いで素通りする（実測: 2つとも「OK」）。⚠️ **締めすぎも駄目** — `data-[state=checked]:` の塗りと文字色は必ず一緒に効くので、条件つきを一律で弾くとチェックボックス2件を誤って止める（実際に落ちた）。見るのは**文字色と塗りの条件が揃っているか** | ⭕️ #160 |
| #153 | **P1** | `billing.routes` | **まとめての守りが「あとの姿」を見ていない**。いまの `invoice_issued` だけを引くので、`{invoice_issued:false, paid_date:…}` を**発行済みの行に**送ると飛ばされず、**取り消しと入金日を同時に書く** — ⚠️ **止めようとしていた「未請求なのに入金済み」がそのままできる**（実測: 200 が返り `invoice_issued=f` ＋ 入金日） | ⭕️ #154 |
| #153 | P2 | `InvoiceRows` | **押せない理由が hover にしか無い**。`disabled` はキーボードで辿り着けず、指で触る端末に hover は無い。**その人たちに見えるのは薄くなった「入金前」だけ**で、理由はどこにも出ない | ⭕️ #154 |
| #152 | **P1** | `InvoiceRows` / `billing.routes` | **未請求の行から入金を記録できる**。「未請求」チップを足したことで `invoice_issued = false` の売上が ⑤ の表に並ぶのに、行の「入金前」は全行で押せ、`PATCH /billing/invoices/:id` も発行済みを検査しない。⚠️ **この PR が ⑫ スマホで塞いだ穴を、PC 側で開け直していた** — できるのは「請求していないのに入金済み」という**どの絞り込みにも出てこない行** | ⭕️ #153 |
| #150 | P2 | `review-debt.mjs` | **数える道具自身が「記録あり」と嘘をつく**。`isRecorded` が本文のどこかの番号に当たるので、表の「どの PR で直したか」の欄（`⭕️ #150`）を拾い、**#150 自身の指摘を記録済みと出す** — 出た人は書かなくてよいと読む | ⭕️ #151 |
| #150 | P2 | `review-debt.mjs` | レビューを **1ページ（50 件）しか引いておらず**、51 件目から先を黙って落とす。落ちたぶんに指摘があると**この道具が「0 件でした」と言う** | ⭕️ #151 |
| #149 | P2 | `useLedgerLookups` | 「やり直す」が**引けている側まで引き直す**。そこで一時的な失敗を踏むと `isSuccess` → `isError` に変わり、**巻き添えでその列も使えなくなる**（列を分けた意味が、やり直す押下そのもので消える） | ⭕️ #150 |
| #147 | **P1** | `check-changelog.mjs` | **リリース PR が必須チェックを通らない**。切り離した頭では枝の名前が `HEAD` になるため。⚠️ **門を直したことがリリースを塞いだ** | ⭕️ #149 |
| #146 | P2 | `ProjectLedgerPage` | 候補が引けないと `isSuccess` が永久に false で、**名前の2列が二度と直せない**（待てと書いてあるのに待っても直らない） | ⭕️ #149 |
| #145 | P2 | `codex-findings-v4.md` | **書いた数が表と合っていない**（20/18 と書いたが実際は 26/24） | ⭕️ #149 |
| #145 | P2 | `codex-findings-v4.md` | 4つの束に分けたとき**1件がどの束にも入っていない**（合計は偶然合っていた） | ⭕️ #148 |
| — | P2 | `tokens-v4.css` の `.font-number` | ⚠️ **数字が等幅になっていない**（[実測](#️-実測で分かったこと--数字は等幅になっていない45-を調べていて見つけた)）。`font-number` は `tnum 1` を当てているが、**LINE Seed JP の数字はプロポーショナル**で `tnum` も `palt` も効かない。**桁ごとの位置はそろわない**（金額の列の右端はそろう） | ❌ **直さないと決めた**（下記）。指定は**残す** — 書体を替えた日に効くため |
| #141 | P2 | `codex-findings-v4.md` | **`tnum` が効いていない**ことが分かったのに、残す行を作らずに元の行を ⭕️ にした。残件はこの表を数えて出すので、**次の棚卸しからこの欠陥が消える** | ⭕️ #148（**上に行を作った**） |
| #140 | P2 | `SearchPage` | 電話番号にキーボード／スイッチで Enter を押すと、`stopPropagation` は**押下の側にしか無い**ので `ClickRow` が拾い、**PC 専用の行き止まりへ飛ぶ**（この PR が避けようとしたもの） | ⭕️ #148 |
| #138 | **P1** | `pricing.routes` | 並べ替えで、`me` と `neighbor` を**取引の外で読んでいる**。隣り合う行への同時の「上へ」「下へ」で**両方が古い位置を書き**、`sort_order` の重複がまた出来る（#52 で直したはずのもの） | ⭕️ #148（**束の親を1行押さえる** — `me`/`neighbor` を順に押さえると向かい合わせで行き詰まる） |
| #137 | P2 | `intake-ai.service` | 残り時間が足りずに諦める道が、軽いモデルの `recordAiUsage` を**飛び越す**。外側は上位モデルの名前で記録するので、**呼んでいないモデルのせい**になる | ⭕️ #137（この PR 内で修正済み。`recordAiUsage` は残り時間の判定の**前**にある） |
| #136 | P2 | `activity-format.service` | 単価の有無を **JSON に鍵が1つでもあるか**で見ており、**その仕事のモデルの単価が無いとき**に「実績がまだ無い」と出る | ⭕️ #136（この PR 内で修正済み。`perRowCost` が `no_model_price` とモデル名を返す） |
| #135 | **P1** | `useLedgerGrid` | 選んだ升目を**行・列の番号**で持っており、ページ送り・並べ替え・絞り込み・列の入れ替えで**中身がずれても消えない**。次の Ctrl+V が**別の案件・別の項目**に書く | ⭕️ #146 |
| #135 | **P1** | `ProjectLedgerPage` | 整合性の確認を押しても `glsCategory: 'A'` が残る。**「GLS-B なのに2段分類が入っている」は必ず 0 行**（件数は出ているのに表が空） | ⭕️ #146 |
| #135 | P2 | `useLedgerGrid` | 表からはみ出した貼り付けを**黙って捨てる**（`if (!row) return`）。20 行貼っても残りの数行しか入らず、確認の窓にも出ない | ⭕️ #146 |
| #135 | P2 | `ProjectLedgerPage` | 候補の `/users?limit=200` `/customers?limit=500` は `extractPagination` が **100 に丸める**（`pagination.ts:12`）。101 人目以降は**名前で貼れず、その場で直せない**。しかも**引き終わる前に**升目編集が開く | ⭕️ #146 |
| #135 | P2 | `editable.ts` | 日付が**月 1–12・日 1–31 しか見ていない**ので `2026-02-31` が通り、TEXT の日付列にそのまま入る | ⭕️ #146 |
| #134 | P2 | `project.service` | GLS-A → B に変えると、`classificationOf` が**旧種類から2段を組み立て直す**ので B の行に放送の分類が残る | ⭕️ #134（この PR 内で修正済み。`resolveClassification` が `glsCategory === 'B'` を先に返す） |
| #133 | **P1** | `scheduler.service` | 材料の無い古い案件が毎晩いちばん先に選ばれ、**その晩の枠を食い潰す** | ⭕️ 該当せず（**上限は AI を呼んだ回数で数えている** — 材料が無い回は `aiCalled: false` で `passed` に入り枠を使わない。走査は 200 行、AI は 20 回。⚠️ 実測はしていない） |
| #133 | P2 | `task-intake.service` | 実効状態の絞り込みが `LIMIT` の**あと**なので、新しい行 50 件で失敗が隠れる | ⭕️ #133（この PR 内で修正済み。`STUCK_SQL` が `WHERE` に入っている） |
| #130 | P2 | `box.ts` | `boxAppSettings` と `enterpriseID` の**有無しか見ない**。`appAuth` が欠けていても SDK は組み立つので、設定画面は**「つながっています」**のまま最初の呼び出しで落ちる | ⭕️ #147 |
| #130 | P2 | `vendor-fonts.mjs` | 200 バイトを超えて切れた woff2 は `wOF2` の印を持つので **`--check` が通る**（普通の途中切断がこれ） | ⭕️ #147（**頭に書いてある大きさと突き合わせる**） |
| #129 | P2 | `CustomerListPage` | 新規追加を2回続けると自動判定の印が戻らない | ⭕️ #129（この PR 内で修正済み・`d480e9d`） |
| #129 | P2 | `MoreFields` | 候補 200 件の外のお客様が**「グループ外」**と出る | ⭕️ #129（この PR 内で修正済み・`d480e9d`） |
| #128 | P2 | `check-env-passthrough` | `ONE_SIDED` の除外が**名前だけ**で、書いてある側に居るかを見ない。`SMTP_HOST` を本番から**消しても**、検証に**移しても** CI が通る | ⭕️ #147 |
| #127 | P2 | `project.service` | 台帳の売上・仕入が `group_id IS NULL` だけを足す。**分け合う請求の案件は少なく出るか `—`**（`getSummary` は配分を足しているのに、台帳は足していない） | ⭕️ #148（`getSummary` と**同じ式を1つ**にした） |
| #127 | P2 | `ProjectLedgerPage` | まとめて直すお客様の候補が **100 件で頭打ち**（`limit=500` は丸められる）。101 件目以降は選べない | ⭕️ #146 |
| #127 | P2 | `LedgerTable` | `sales: reader` にも**「直す」の鉛筆が全行に出る**。ルートも reader を通すので、**書いてから 403** になる | ⭕️ #148 |
| #127 | P2 | `project.service` | 旧 GLS 取込の道で `no_audience` を埋め戻すとき、**来場人数を落としていない**（2段の道と `create` / `update` は落としている） | ⭕️ #148 |
| #126 | P2 | `check-changelog.mjs` | `ci.yml` の `checkout` に `fetch-depth: 0` が無いので `origin/main` が見えず、**どの PR でも黙って飛ばす**。版の3か所を触っても門は緑のまま | ⭕️ #147（`fetch-depth: 0` ＋ **CI では飛ばさない**） |
| #126 | P2 | `check-changelog.mjs` | 下書きを**リポジトリ全体で数えている**ので、その PR が1つも足していなくても通る（版に載らない PR ができる） | ⭕️ #147 |
| #125 | P2 | `billing.routes` | **「入金前」の意味が画面によって違う** — ⑤ 見積・請求のチップは「入金日が空」だけ（**請求書を出していないものも入る**）、月次の締めと ⑫ スマホは `invoice_issued` を要求。同じ言葉で違う集合を指すので、件数が食い違って見える | ⭕️ **式を1か所に集めた**（`shared/services/billing-state.ts` を3つの口が読む）。名前も「入金待ち」にそろえ、**別名 `unpaid_issued` は消した**（1つの集合に2つの名前があること自体が、片方だけ直されて割れる元）。狭めたぶんは**「未請求」チップ**で拾える。**期日超過も出したものに限る**。実測 5件→2件・3件→2件（財務の台帳と一致） |
| #134 | P2 | `ActivityFormatCard` | 費用の目安が出せない理由が**いつも「単価が未設定」** — 単価は入っていて実績がまだ無いだけのとき、`.env` を直しに行って**すでに入っている**のを見ることになる | ⭕️ #135 **この作業中に見つけた** |
| #106 | P2 | `projects.routes` | 持ち主の確認を写真1枚ごとにやると、100 枚で BOX を 200 回叩く（絞られて 404） | ⭕️ #106 |
| #107 | **P1** | `estimate.service` | **承認できる人**の判定だけ `updated_by` のままで、**上司が下書きを直すと本来の承認者が 403** になる | ⭕️ #108 |

### 数え方

⚠️ **文章に書いた数を信じないこと。** 表を数えてください（実際に1度食い違いました）。

```bash
# この節（#106 以降）の指摘の数
# ⚠️ 表頭（`| PR |`）と区切り（`| --- |`）を除くこと。除かないと2多く出ます
sed -n '/^## この棚卸しのあとに付いた指摘/,/^### 数え方/p' docs/reviews/codex-findings-v4.md \
  | grep '^| ' | grep -vc -e '^| PR ' -e '^| --- '

grep -c '^| .* | ❌' docs/reviews/codex-findings-v4.md    # まだ残っているもの
grep -c '^| .* | ❓' docs/reviews/codex-findings-v4.md    # 未確認（読んだだけ）
```

**片づけた順**（直すときの単位）:

1. **案件台帳の升目編集** — #146（#135 の 5 件・P1 2 件を含む＋#127 の候補 100 件）。
   **取り消せない一括更新の入口**なのでいちばん先に潰しました
2. **検査が「大丈夫」と出す** — #147（#126 ×2・#128・#130 ×2）。門そのものが素通りする側で、
   #129 で潰した「壊れているのに大丈夫と出る」の**続き**です
3. **単発と、数え漏らしていた1件** — #148（#138 の並べ替えの取引・#140 のキーボード操作・
   #127 の権限と来場人数・#141 の文書 ＋ **#127 の按分**）。
   ⚠️ **最後の1件は、まとまりに分けたときに数え漏らしていました**
   （4つの束に配ったつもりで、1つだけどの束にも入っていなかった）。
   **束に分けたら、合計が元の数と合うか数え直すこと。**

---

## レビューが0件のままマージされた PR

⚠️ **「指摘なし」ではなく「Codexがまだ読んでいない」の可能性がある。** `pr-watch` スキルの
決めごとどおり、マージ時点でレビューが1件も付いていなかった PR をここに記録する
（画面上は「指摘なし」と見分けが付かないため）。

- **#251**（`feat(sales): 取引先マスターをv4に作り直し、権限バグ2件を修正`）— 作成から
  約1分でCI green・自動マージ。レビュー0件のままマージされた
- **#252**（`fix(sales): 取引先名を2行折り返しに変更・タブレット幅の崩れを修正`）— 作成から
  約27分でCI green・自動マージ。レビュー0件のままマージされた
- **#256**（`feat(sales): 案件台帳に旧GLS絞り込みと削除アイコンを足し、案件削除ボタンも追加した`）—
  作成から約3分でCI green・マージ。レビュー0件のままマージされた
- **#258**（`fix(client): トップの「わたしのタスク」全部ひらくを直し、お待たせ中と統合した`）—
  作成 06:22:10 / CI（`checks`/`build`）両方緑 06:24:29 / マージ 06:35:09（2026-08-20・
  **CI完了から約11分後** — CI完了を待たずに入った #159・#239・#241 とは違い、待つ時間はあった）。
  `get_comments` / `get_reviews` とも0件で、`chatgpt-codex-connector` の usage limits コメントも
  付いていない（原因不明のまま0件だった #165・#167・#244 に近い形）。PRが開いていたのは約13分と
  短く、レビューが付く前に人自身がマージした可能性が高い。表に移す指摘はない（レビュー自体が
  届いていないため）。⚠️ **この記録は当時の作業ブランチに置き去りになっており、残骸ブランチの
  棚卸し（PR #351・`docs/release-prep/stale-branches.md`）で発見して回収した**。
- **#259**（`fix(client): アプリをまたぐリンクの検査漏れとタスク一覧の注記を直した`）—
  作成から約5分でCI green・マージ。レビュー0件のままマージされた
- **#263**（`refactor(shared,client,server): 権限モデルを単純化した`）— 作成から
  約43分でCI green・マージ（途中で`main`とのコンフリクトを解消するpushを1回挟んだ）。
  レビュー0件のままマージされた
- **#264**（`feat(client): カレンダーのモバイル「予定」をiPhone風の月表＋アジェンダに作り直した`）—
  作成から約3分でCI green・マージ。レビュー0件のままマージされた
- **#266**（`feat(client): カレンダーのPC「予定」をmacOS風のツールバー＋左メニュー常設に作り直した`）—
  作成から約7分でCI green・マージ（途中で`main`とのコンフリクト解消のpushを1回挟んだ）。
  レビュー0件のままマージされた。⚠️ **コンフリクト解消時に自分で見つけた実害バグ2件は
  そのpushのコミット・PR本文・コメントに記録済み**（PR #263 の権限モジュールremap漏れ —
  `rooms/MobileToday.tsx` と `components/layout/nav.ts` の3項目。詳細は下の一覧の #266 行）
- **#267**（`feat(client): カレンダーのPC「部屋の空き」をmacOS風のツールバー＋種別色に作り直した`）—
  作成から約4分でCI green・マージ。レビュー0件のままマージされた
- **#270**（`feat(equipment): 機材台帳「機材」タブのスマホカードに拠点グルーピング・複数選択などを足した`）—
  作成 12:01:28 / CI green 12:10:18 / マージ 12:24:20（2026-08-20）。**CI green から約14分待っており、
  #159・#239・#241 の「見る前に入った」形ではない**（#244・#262 に近い、待つ時間はあったが原因不明の0件）。
  途中で`main`（PR #269 の権限モデル単純化）とのコンフリクト解消のpushを1回挟んでいる。
  `npm run reviews:debt` は今回も 401 で使えなかったため GitHub MCP で直接確認した
  （`get_review_comments` 0件）。表に移す指摘はない（レビュー自体が届いていないため）
- **#272**（`feat(sales,finance): PC専用13画面をスマホ対応した`）— 作成 13:51:31 / CI green
  13:53:56 / マージ 13:55:06（2026-08-20）。**CI green から約1分でマージ**（#251・#256 に近い
  「見る前に入った」形）。`get_review_comments` 0件。表に移す指摘はない（レビュー自体が
  届いていないため）。⚠️ **13画面ぶんの大きめのPRで、実ブラウザ確認は13画面中2画面
  （見積・請求／ガントチャート）のみ**（PR本文に明記済み）。残り11画面
  （財務台帳3画面・案件台帳・案件を直す・標準工程テンプレート・料金表・
  費用を分け合うグループ・営業活動記録・お客様の詳細）は型検査・lint・ユニットテスト・
  実装エージェントによるコードレビューのみでの確認。**レビューが付く前にマージされたため、
  残り11画面の実ブラウザ確認は今後のセッションで別途行うことを推奨**
  → ⚠️ **この推奨は#274で一部が意味を失った**（下記）。13画面のうち見積・請求／案件台帳／
  案件を直す／標準工程テンプレート／料金表／費用を分け合うグループ詳細／営業活動記録／
  財務台帳3画面（計11画面）は#274で**PC専用へ差し戻された**ため、これらの実ブラウザ確認は
  不要になった。残る2画面（お客様の詳細・費用を分け合うグループ一覧）は#274側の別実装
  （後者は同一判断・前者は#274の全面刷新版）に置き換わっているため、確認するなら#274側の
  実装を対象にすること
- **#274**（`feat(client,client-daily,client-equipment,shared): 全画面をPC=macOS/スマホ=iOSネイティブ級に作り直した`）—
  作成 2026-08-21 01:59:23 / マージ 02:05:56（terai-takehiro 本人が手動マージ、約6分後）。
  `get_reviews` 0件・`get_comments` 0件。表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **72コミット・229ファイル・+13,951/-7,860 行の非常に大きいPR**（1 PR = 1画面の原則を
  明示的に破っている。理由はPR本文に記載）。マルチエージェント（のべ約65体）による
  11バッチの作業を1本のブランチに積んだもので、typecheck/lint/test(1144件)/7アプリ全ビルド/
  `check:frozen`はすべて確認済みだが、**実ブラウザでのPC/スマホ確認は一部の画面のみ**
  （PR本文の「検証したこと」チェックリストに未確認箇所を明記）。**`main`に並行マージ
  されていたPR #272との重複を解消する`git merge`を含む**（ユーザー判断で#274側の実装を
  採用・詳細は [docs/handoff-2026-08-20-v4-native-ui.md](../handoff-2026-08-20-v4-native-ui.md)）。
  **レビューが付く前にマージされたため、実ブラウザ未確認の画面（PC専用画面を除く大半の
  スマホ表示）は今後のセッションで別途確認することを推奨**
- **#276**（`feat(client,client-daily,client-equipment,server,shared): v4ネイティブUI化の残り4項目に着手し、機材台帳の日付バグを直した`）—
  作成 2026-08-21 03:03:31 / マージ 03:11:45（terai-takehiro 本人が手動マージ、約8分14秒後）。
  `npm run reviews:debt` は今回も 401 で使えなかったため GitHub MCP で直接確認した
  （`get_reviews` 0件・`get_review_comments` 0件）。表に移す指摘はない（レビュー自体が
  届いていないため）。8コミット・29ファイル・+1,247/-334 行。`docs/handoff-2026-08-20-v4-native-ui.md`
  （#274の引き継ぎ書）の「残っている作業」4項目（操作感の演出・案件詳細⑥残り5タブ・
  機材台帳の親子入れ子・凍結アプリCSSズレの原因調査）＋機材台帳の日付バグ修正
  （`purchased_at`を空文字で保存すると500になる不具合）を1本にまとめたPR。
  typecheck/lint/test(1144件)/build/check:frozenは各マージ段階ごとに再検証済み。
  実DB（`verify:up`）+実ブラウザ(Playwright, 375px)での確認は機材台帳の子機材編集・
  案件詳細⑥の当日/書類/見積/やり取り/ふりかえりタブ・購入日バグの再現/修正確認まで
  実施済みだが、**操作感の演出（pull-to-refresh・スワイプ）はサンドボックスにブラウザ
  自動化が無いバッチがあり実ブラウザ確認は未実施**（PR本文・コミットメッセージに明記）。
  **レビューが付く前にマージされたため、操作感の演出の実ブラウザ確認は今後のセッションで
  別途行うことを推奨**
- **#280**（`docs(qsheet): 制作資料 v4化のモックアップとコーディング設計を出した`）—
  作成から約1時間54分でマージ。**レビュー・レビューコメント・通常コメントすべて0件**のまま
  マージされた（`get_reviews` / `get_review_comments` / `get_comments` の3通りで確認）。
  CI は `checks` / `build` とも緑。⚠️ **`docs/` 配下 23 ファイルの追加のみでコード変更が
  1行も無い PR** だったため、Codex が対象外にした可能性がある（他の0件PRはコード変更を
  含むものが多く、性質が違う）。⚠️ **`npm run reviews:debt` はこの回だけ実行できていない** —
  作業環境の `GITHUB_TOKEN` では REST API が 401 を返すため（この環境の GitHub アクセスは
  MCP 経由）。代わりに MCP の `pull_request_read` で3通り確認して0件を確定させた。
  **スクリプトが動く環境で回し直すと、この PR は「表に載っている」ので警告は出ない**
- **#281**（`docs(qsheet): 配信設定にWEB会議の情報を足し、計時の未決3件を利用者の判断で閉じた`）—
  作成 09:54:46 / マージ 09:57:10（2026-08-21）。**terai-takehiro 本人が作成から約2分24秒で
  手動マージ**。**レビューが1件も付く前にマージ**された（`get_reviews` で0件を確認。
  #159・#239・#241 と同じ「見る前に入った」形）。CI は `build` が成功、`checks` は
  マージ時点でまだ `in_progress` — **CI 完了も待たずに入った**。⚠️ **`npm run reviews:debt`
  はこの回も 401**（作業環境の `GITHUB_TOKEN` は REST 不可・GitHub アクセスは MCP 経由）。
  `pull_request_read` の `get_reviews` で0件を確認して代替とした。**`docs/` 配下10ファイル
  （設計書8本の更新＋モック2本の新規）のみでコード変更は0行。** #280 と同じ「コードを
  含まない docs 専用 PR」の性質のため、レビューが対象外だった可能性がある
- **#283**（`feat(awards): リアルタイムCGを廃止(配信停止・コードは保存)`）—
  作成 16:48:13 / マージ 16:55:28（2026-08-21）。**terai-takehiro 本人が作成から約7分15秒で
  手動マージ**。**レビュー・レビューコメント・通常コメントすべて0件**のままマージされた
  （`get_reviews` / `get_review_comments` / `get_comments` の3通りで確認）。
  ただし #281 と違い、**CI（`checks` / `build` とも）はマージ前に完了・成功済み**
  （両ジョブとも 16:50 台に完了、マージは 16:55）。⚠️ **`npm run reviews:debt` は
  この回も 401**（作業環境の `GITHUB_TOKEN` は REST 不可・GitHub アクセスは MCP 経由）。
  `pull_request_read` で代替確認した。サーバーの配信停止・Dockerfile・npm scripts・
  ドキュメント更新のみでコード側の業務ロジック変更は無い PR
- **#284**（`docs(reviews): PR #283 のマージ後の棚卸しを記録した`）— #283の直後、この棚卸し
  自体を記録するために出した PR。作成 16:58:17 / マージ 17:04:46（2026-08-21）。**これも
  レビュー0件のまま terai-takehiro 本人が手動マージ**（`get_reviews` で確認）。CIは
  途中で `checks` が `check-changelog.mjs`（このPRが `docs/changelog.d/` に下書きを
  足していなかった）で一度落ち、下書きを追加して再度緑にしてからのマージ。
  ⚠️ **これ以上の入れ子（#284自身の棚卸しを記録する新しいPR）は作らない** —
  棚卸し用のドキュメントPRについてまで際限なく棚卸しPRを重ねると収束しないため、
  この1行への追記で留めた
- **#286**（`fix(client): 財務管理のカード重なり・スマホ非対応・薄い文字を直した`）—
  作成 2026-08-21 18:19:28 / マージ 2026-08-22 01:43:09（**terai-takehiro 本人が手動マージ、
  約7時間24分後**）。CIは作成から約2分で `checks` / `build` とも緑（`mergeable_state: clean`）
  になっていたが、**レビューが1件も付かないまま長時間open**していた — #159・#239・#241
  （見る前に入った）とは逆に、**待っても届かなかった**ケース。`pr-watch` の決めごとに従い
  1時間おきに `get_review_comments` で計7回チェックインしたが、マージまで一貫して0件
  （最終確認: `get_reviews` 0件・`get_review_comments` 0件）。`npm run reviews:debt` は
  この回も 401（作業環境の `GITHUB_TOKEN` は REST 不可・GitHub アクセスは MCP 経由）のため
  `pull_request_read` で代替確認した。表に移す指摘はない（レビュー自体が届いていないため）。
  財務管理（`client/`）と共通部品（`shared/src/client/ui/searchable-select.tsx`）の
  UI崩れ修正PR。typecheck:all/lint/test(1144件)/build:all/check:frozenは確認済み、
  実ブラウザ（Playwright・実データ投入）でPC(375〜1440px複数幅)・スマホも確認済み
  （PR本文に詳細）
- **#288**（`feat(qsheet): 制作資料 v4 を作り直した（段0〜10・凍結解除）`）— 作成
  2026-08-22 01:52:13 / マージ 02:01:22（terai-takehiro 本人が手動マージ、約9分9秒後）。
  CI（`checks` / `build` とも）はマージ前に完了・成功済み（両ジョブとも02:00台に完了、
  マージはその約31秒後）。`get_review_comments` で `totalCount: 0`（未解決スレッド0件）
  を確認。表に移す指摘はない（レビュー自体が届いていないため）。⚠️ **`npm run reviews:debt`
  は今回、401ではなく403で失敗した** — `NODE_USE_ENV_PROXY=1`を付けてプロキシ経由で
  `api.github.com/graphql`に直接届いたところ、GitHub側から
  `"This GraphQL query is not enabled for this session — only the pinned set of
  PR-review operations is served"`という応答が返った。つまりこの作業環境は
  GraphQLを丸ごと禁止しているのではなく、**MCP経由の定型クエリだけを許可し、
  スクリプトの自由な GraphQL クエリは組織ポリシーで拒否している**（プロキシ越しの認証自体は
  通っていた）。これまでの回の「401」は未検証だが同根の可能性がある。`pull_request_read`
  （MCP・許可された定型クエリの一部）で代替確認した。⚠️ **段0〜10・67コミット・280ファイル・
  +33,424/-1,869行の非常に大きいPR**（複数段にまたがる作業を1本のブランチに積んだ実装で、
  PR本文に理由を明記）。typecheck/lint/test(1321件)/build:all/check:frozenは各段の
  マージ・main統合のたびに再検証済みだが、**実ブラウザでのPC(1440px)/スマホ(375px)確認・
  `verify:ui`・403（権限）確認はいずれも未実施**（このセッションが実ブラウザ確認まで
  行えないため。PR本文の「検証したこと」チェックリストに明記済み）。**レビューが付く前に
  マージされたため、実ブラウザ未確認の3項目は今後のセッションで別途行うことを推奨**
- **#290**（`feat(qsheet,live): 段2・段3・段9の未実装分を埋め、計時LIVEの凍結を解いた`）—
  作成 2026-08-22 03:06:07 / マージ 03:09:05（terai-takehiro 本人が手動マージ、約3分後）。
  CI（`checks` / `build` とも）は03:08台に成功、`mergeable_state: clean` を確認した直後に
  マージされた。`get_review_comments` で `totalCount: 0`（未解決スレッド0件）を確認。
  表に移す指摘はない（レビュー自体が届いていないため）。⚠️ **このPRの由来**: #288で
  「段0〜10は全部実装した」としていたが、利用者から「計時LIVEは制作資料にマージされた
  と思っていた」との指摘を受けて全設計書と git log を突き合わせる監査を行った結果、
  段2 PR2の一部（旧URL予告帯）・段3 PR A一部（tailwind v4Preset・verify-ui.mjs）・
  段3 PR D/F（ジャーニー画面まるごと）・段3 PR G（SheetListPage分離）・段9 PR5〜7
  （しきい値UI・表示画面v4化・凍結解除）が未実装のまま残っていたことが判明し、
  6体のエージェント並行実装＋自分の実装（段9 PR7）で埋めた。**計時LIVEの凍結解除は
  `apps.ts` の `frozen` フラグを落とすところに意図的に絞り**、v4共通シェルへの完全な
  載せ替え・ミニアプリレジストリ登録・改名（「計時LIVE」→「計時・視聴者」）は
  制作資料自身の共通シェル載せ替えとあわせて別段に見送った（理由は
  `docs/design/v4/qsheet-v4-coding/impl/09-live-timer-impl.md` §12-11に記録）。
  typecheck/lint/test(1321件)/build:all/check:frozen/verify:upは確認済みだが、
  **実ブラウザでのPC/スマホ確認・`verify:ui`・403確認はいずれも未実施**
  （段3 PR D/F/Gの担当エージェントは個別にPlaywrightで375px/1440pxを確認済みだが、
  全体としての再実行はしていない。PR本文に明記済み）。`npm run reviews:debt` は
  この回も403で失敗（#288と同じ理由・GraphQLの自由なクエリがセッションポリシーで
  拒否される）ため `pull_request_read` で代替確認した。
- **#292**（`docs(design): 制作資料v4の設計書10本を実装完了後の実態に合わせて更新した`）—
  作成 2026-08-22 03:35:03 / マージ 03:36:04（terai-takehiro 本人が手動マージ、約1分後）。
  `get_review_comments` で `totalCount: 0`（未解決スレッド0件）を確認。表に移す指摘はない
  （レビュー自体が届いていないため）。`npm run reviews:debt` はこの回も403で失敗
  （#288/#290と同じ理由）のため `pull_request_read` で代替確認した。設計書（README＋
  00〜07の8本・impl/4本）冒頭の「コードは書いていません」に実装完了の追記を足し、
  `03-excel.md` §11-3を新設して旧CSV取込の削除条件を3条件（①新経路の本番公開・
  ②通常リリース1回の観察期間・③旧CSV実利用がほぼ0件の確認）に具体化した。
  コード変更なし（docsのみ）。#289/#291と同様、この棚卸し記録自体の入れ子PRは作らない。
- **#294**（`fix(scripts): v4-progress.mjsが「探す」「QRスキャン」を⬜と誤判定していたのを直した`）—
  作成 2026-08-22 03:45:19 / マージ 03:50:31（terai-takehiro 本人が手動マージ、約5分後）。
  CI（`checks`/`build`）は03:46台に成功。`get_review_comments`で`totalCount: 0`を確認。
  表に移す指摘はない。`npm run reviews:debt`はこの回も403で失敗のため`pull_request_read`
  で代替確認した。2026-08-20のv4ネイティブUI監査で「探す」「QRスキャン」が薄い親＋
  Desktop/Mobile分割の構成に作り直し済みだったのに、進捗表の生成スクリプトが
  分割前の親ファイルを判定対象にしたままだったため未着手と誤表示されていたのを発見・修正
  （54/59→56/59）。利用者から「v4全体の完成に向けたコーディングにマルチエージェントで
  着手を」との指示を受けて現状監査した際に見つけた。コード変更なし（生成スクリプトと
  生成物のみ）。
- **#295**（`docs(reviews): PR #294 のマージ後の棚卸しを記録した`）— 作成 2026-08-22 03:52:17 /
  マージ 03:57:39（terai-takehiro 本人が手動マージ、約5分後）。CI（`checks`/`build`）は
  03:55台に成功。`get_review_comments`で`totalCount: 0`を確認。表に移す指摘はない。
  `npm run reviews:debt`はこの回も403で失敗のため`pull_request_read`で代替確認した。
  同PR内で並行実行していた「機材台帳の行をRow部品へ移行」エージェントの成果
  （実際には既に別PRでRow移行完了済みと判明・進捗表の注記のみ修正）も合流させて
  マージ済み。コード変更なし（docsのみ）。#289/#291/#293と同様、この棚卸し記録自体の
  入れ子PRは作らない。
- **#296**（`feat: v4全体の完成 — 制作資料・計時LIVEの共通シェル統合 + 操作感の演出gaps + 制作技術支援への改名・格上げ`）—
  作成 2026-08-22 03:58:52 / マージ 04:58:36（terai-takehiro 本人が手動マージ、約1時間後）。
  CI（`checks`/`build`）は最終コミット（3df5a2e）で04:53台に成功。`get_review_comments`で
  `totalCount: 0`を確認。表に移す指摘はない。マルチエージェントで制作資料・計時LIVEの
  共通シェル統合・操作感の演出gaps（PR #294/#295の棚卸し記録を含む）に着手した後、
  同ブランチに追加コミットで「制作資料」→「制作技術支援」への改名・大アプリ格上げ
  （`apps.ts`の並び順をプロジェクト管理と財務管理の間へ・`DAILY_KEYS`/`EVENT_KEYS`の
  付け替え・表示文字列の統一）も実施した。34→45ファイル・11→12コミットに拡大したため
  マージ前にPRタイトル・本文を更新済み。
- **#297**（`feat(qsheet,sales): 制作技術支援トップを番組・案件のピッカーに作り直し、案件詳細の当日タブと連携`）—
  作成 2026-08-22 05:00:02 / マージ 05:42:02（terai-takehiro 本人が手動マージ、約42分後）。
  CI（`checks`/`build`）は最終コミット（9ed1515）で05:41台に成功。`get_review_comments`で
  `totalCount: 0`を確認。表に移す指摘はない。当初は#296のマージ後棚卸し記録（docsのみ）
  として作成したPRだが、その後利用者から「制作技術支援のトップページが無い」「Qシートは
  トップではない、まず番組・案件を選んでからミニアプリへ分岐する形にしたい」とのご指摘・
  ご指示を受け、同ブランチに大きな機能追加を2回に分けて積んだ:
  ①`qsheet_programs`（番組＝マニュアル・案件管理外）の新設を含むDB・サーバー・
  クライアントの一式（制作技術支援トップを番組・案件ピッカーに作り直し、選んだ先の
  ハブ画面にミニアプリのタイルを出す）、②案件詳細の当日タブから同じハブへ直接
  連携するカードの追加。2→39ファイル・1→3コミットに拡大したためマージ前にPR
  タイトル・本文を更新済み。`npm run verify:up` で実Postgresに新migrationを適用し、
  3-way CHECK・広げた一意キー（重複拒否）の両方を実際に違反させて検証した
  （棚卸しに残す理由: migration・API・画面をまたぐ大きな変更のため、レビュー0件を
  特に明示しておく）。
- **#300**（`docs(qsheet): 制作技術支援のトップとハブを iOS/macOS の作法で作り直したモックを追加`）—
  作成 2026-08-22 07:39:58 / CI green 07:41:51（`checks`。`build` は 07:40:32）/
  マージ 07:43:15（terai-takehiro 本人が手動マージ。作成から約3分17秒後・**CI green から
  約1分24秒後**）。`npm run reviews:debt` は今回も 401 で使えなかったため GitHub MCP で
  直接確認した（`get_reviews` 0件・`get_comments` 0件・`get_review_comments`
  `totalCount: 0`）。表に移す指摘はない（レビュー自体が届いていないため）。
  **CI green から1分半でのマージ**なので、#251・#256・#272 と同じ「見る前に入った」形。
  3コミット・7ファイル・+1,882行、**`docs/` のみでコード変更なし**（デザインキャンバスの
  作業ファイル `docs/design/v4/mockups/native/qsheet-top/*.dc.html` が行数の大半）。
  ⚠️ **このPRは `npm run typecheck` / `lint` / `build` / `verify:ui` を1つも回していません**
  （作業したサンドボックスに `node_modules` が無く実行できなかった。PR本文の「検証したこと」に
  明記済み）。`docs/` しか触っていないため実害は無いはずだが、**「CIが緑」と「検査を回した」が
  一致しない例**として残す。⚠️ **モックの中身（アーカイブの判定をどこで切るか・
  「直近の本番・収録」に要るサーバー側の追加・「続きから」の閲覧履歴・スマホ下タブに
  「トップ」が無いこと）は PR 本文に「決めていただきたいこと」として4点挙げたまま、
  レビューが付かずにマージされている。実装に進むときは PR #300 の本文か
  [production-v4-native-mockups.md](../design/v4/production-v4-native-mockups.md) の
  「①′」節を読み直すこと**
- **#302**（`docs(reviews): PR #300 のマージ後の棚卸しを記録した`）— #300 の直後、この棚卸し
  そのものの PR。作成 2026-08-22 07:54:09 / CI green 07:56:08（`checks`。`build` は 07:55:21）/
  マージ 07:56:16（terai-takehiro 本人が手動マージ。**CI green から約8秒後**）。
  `get_reviews` 0件・`get_comments` 0件・`get_review_comments` `totalCount: 0`。
  表に移す指摘はない。2コミット・3ファイル・+55行、`docs/` と `.claude/skills/` のみ。
  ⚠️ **この行で連鎖を止めます。** 棚卸しの PR にもレビューは付かないので、
  「#302 の棚卸し」「その棚卸しの棚卸し」…と PR が無限に増えます
  （#283→#284・#294→#295 も同じ形でした）。**docs だけの棚卸し PR は、この行のように
  直前の PR の記録に相乗りさせて、そこで終わりにしてください** — 単独の PR を
  もう1本立てても、新しく分かることは何もありません
  （**#303** が「#302 の棚卸し」を実際に運んだ PR。同じ理由で単独の行は持たせていません）
- **#305**（`feat(qsheet): トップページとハブ画面をモック通りに実装`）— 制作技術支援の
  トップ・ハブ画面の実装本体（初のコード変更PR。#300/#302/#303はdocsのみ）。作成
  2026-08-22 08:46:19 / CI green 08:48:23（`checks`。`build` は 08:47:47）/
  マージ 08:50:03（terai-takehiro 本人が手動マージ。**CI green から約1分40秒後**）。
  `get_reviews` 0件・`get_comments` 0件・`get_review_comments` `totalCount: 0`。
  表に移す指摘はない。2コミット・17ファイル・+1,009/-324行。`npm run typecheck:all` /
  `lint`（warning 58件・着手前と同数）/ `test`（1321件）/ `client-qsheet` の
  vite build はすべて実施済み（PR本文に明記）。⚠️ **実ブラウザでのクリック確認はしていない**
  （ログインが `client/` 側のモックユーザー選択にリダイレクトする作りで、`client-qsheet`
  単体では認証を通せなかったため。`npm run verify:fresh` で実Postgresにシードし、
  新設 `GET /qsheet/top-items` を実データ＋一時挿入した過去/未来データで代わりに検証した）。
  この点は今後実ブラウザで確認する余地として残る。
  ⚠️ **この PR 自体の棚卸し PR は単独で作りません**（#302→#303 の決めごとと同じ理由。
  次に何かこのブランチ周りで docs を触る機会があれば、そこに相乗りさせます）
- **#308**（`fix(qsheet): 収録設定・配信設定の500エラーを直し、モック・ジャーニーUXに合わせた`）—
  作成 2026-08-22 09:30:43 / CI green 09:32:29（`checks`。`build` は 09:32:02）/
  マージ 09:35:00（terai-takehiro 本人が手動マージ。**CI green から約2分31秒後**）。
  `get_reviews` 0件・`get_comments` 0件・`get_review_comments` `totalCount: 0`。
  表に移す指摘はない（レビュー自体が届いていないため）。4コミット・9ファイル・+416/-53行。
  利用者から「配信収録のページ、まったく機能していない。モックアップとも一致してない。
  サイドバーの内容はジャーニーUXと反している」という指摘を受けて着手。①**実害バグ**:
  `device-settings.service.ts` の `resolveDate()` が Postgres の `DATE` 列を
  `String(row.service_date)` で文字列化しており（`Date.toString()` 表記になる）、
  `?date=` を省いた GET（ハブのミニアプリタイルから開く通常の経路そのもの）で必ず
  `invalid input syntax for type date` の 500 になっていた —
  **一度でも保存すると以後ハブから開くたびに壊れる状態**。`schedule.service.ts` が
  既にコメントで警告していた罠（`to_char(..., 'YYYY-MM-DD')` にすること）を踏んでいた。
  ②ヘッダーのミニアプリ切替・「前回の設定を写す」（API はあったが呼ぶ画面が無く死んでいた）・
  最後に書き出したファイル表示・絞り込みチップをモックアップ突き合わせで追加。
  ③左メニュー「収録・配信設定」の簡易入口（GLS番号/案件ID＋実施日を手入力→日付固定の
  画面へ直行）が正規のジャーニー（案件を選ぶ→ハブ画面→ミニアプリタイル）を経由しない
  導線だったのを、ハブ画面へ遷移する形に作り直した。マルチエージェントで並列実装
  （ヘッダー切替・コピーダイアログ・簡易入口の3並列→統合の1エージェント）。
  検証用DBで実際にバグを再現・修正確認し、Playwright（PC 1440px・スマホ 375px）で
  画面・遷移・コンソールエラー無しを確認済み（棚卸しに残す理由: バグ修正＋UI変更を
  レビュー0件のままマージしたため。特に①は他の凍結解除中アプリでも同型の罠が
  再発しうるので、次に `DATE` 列を素の `SELECT` で読む実装を書くときは注意すること）。
  ⚠️ **この PR 自体の棚卸しは #309 が運んだ**（#302→#303 と同じ形。単独の行は無い）。
- **#310**（`fix(qsheet): 配信設定「配信先を足す」でPC幅の画面全体が暗く覆われ操作不能になる
  不具合を直した`）— #308 のマージ直後、利用者が実際に「配信先を足す」を押して
  スクリーンショット付きで報告した実害バグの修正。作成 2026-08-22 09:51:27 /
  CI green 09:53:24（`checks`/`build` とも）/ マージ 09:54:13（terai-takehiro 本人が
  手動マージ。**CI green から約49秒後**）。`get_review_comments` `totalCount: 0`。
  表に移す指摘はない。1コミット・2ファイル・+30/-3行。**PR #279（このミニアプリの
  実装当初）から存在していたバグ**: `StreamingPage.tsx` の配信先インスペクタ下シートが
  `DialogContent` に `sm:hidden` を付けて「PCでは中身を出さない」つもりだったが、
  `shared/src/client/ui/dialog.tsx` の `DialogOverlay`（画面全体を覆う半透明の板）は
  `DialogContent` とは別要素で `sm:hidden` の対象外のため PC幅でも描かれ続けており、
  配信先を1つでも選ぶ・足すたびに画面全体が暗く覆われ右のインラインインスペクタごと
  一切操作できなくなっていた。CSSで中身を隠すのではなく `Dialog` の `open` 自体を
  `useIsNarrow()`（640px未満）で絞る形に直した。Playwrightで実際に入力欄へ文字を
  打てることを確認（PC）・下シートが引き続き開くことを確認（スマホ）。
  ⚠️ **棚卸しに残す理由**: #308 の実ブラウザ確認では画面遷移とコピーダイアログの
  クリックしか通しておらず、「配信先を実際に足す」という核となる操作を確認しないまま
  マージしていた。**モックアップ・ジャーニーUXの作り直しに気を取られ、既存の基本操作の
  実クリック確認が抜けていた**という教訓として残す。
- **#312**（`feat(qsheet,liveops): 計時LIVEを制作技術支援のミニアプリにするフェーズ1`）—
  作成 2026-08-22 11:28:20 / CI green 11:30:30（`checks`/`build`とも成功）/
  マージ 11:31:49（terai-takehiro 本人が手動マージ。**CI green から約1分20秒後**、
  #251・#272・#305 と同じ「見る前に入った」形）。`get_reviews` 0件・`get_comments` 0件・
  `get_review_comments` 0件。表に移す指摘はない（レビュー自体が届いていないため）。
  2コミット・26ファイル・+1,597/-40行。設計は
  [`docs/design/v4/qsheet-v4-coding/12-live-timer-decision.md`](../design/v4/qsheet-v4-coding/12-live-timer-decision.md)
  （マルチエージェントで作成）。実装もマルチエージェント（実装→実装→レビュー→修正）で行い、
  **セッション内の自己レビューで権限レベル不一致（クライアント側チェックがreader、サーバー側
  がmanager要求で「タイルは見えるが押すと403」になる欠陥）を検出・修正済み**（PR本文に記録）。
  `npm run typecheck:all` / `lint`（warning 58件・着手前と同数）/ `test`（1329件）/
  `check-collab-parity` / `check-links` / `build:changed --base origin/main` は実施済み。
  ⚠️ **実ブラウザでのクリック確認はしていない**（環境制約）。代わりに実Postgres +
  実サーバーへの直接HTTPリクエストで、新設APIの権限403・冪等な取得または作成・404・
  20並列リクエストでも行が1件しかできないことを実測した（PR本文に詳細）。
  実ブラウザでのPC/スマホ確認は今後の課題として残る
- **#314**（`fix(qsheet): 収録設定・配信設定を作り直した（モックが想定した操作が実装に無かった）`）—
  作成 2026-08-22 11:58:46 / CI green 12:00:51（`checks`/`build`とも成功）/
  マージ 12:01:15（terai-takehiro 本人が手動マージ。**CI green から約24秒後**）。
  `get_review_comments` `totalCount: 0`。表に移す指摘はない（レビュー自体が届いていないため）。
  1コミット・28ファイル・**+2,431/-527行**。
  利用者から「配信収録のUIがまるでダメ。モックアップが全然反映されてないし、初期設定の設定も
  できてないし、操作性最悪だし、なにこれ？」との指摘を受け、4エージェントでモックアップ
  （`.dc.html`）とコードを1行ずつ突き合わせて監査し、**46件**（「使えない」14件／「使いにくい」
  24件／「細部」8件）を確認したうえで段1＋段2を実装した。調査結果と作り直し案のモックアップは
  [Artifact](https://claude.ai/code/artifact/c3423c3f-af9d-4901-b0e5-b1a2f8fb0b7a) にまとめてある。

  ⚠️ **この PR は「棚卸しに残す価値がいちばん高い」種類のもの**なので、原因を明記しておく。
  実装がモックと構造レベルでずれた原因は4つで、**どれも次の段（WEB会議・本番3画面の作り直し）で
  そのまま再発しうる**:
  ① **`.dc.html` は下半分の `<script type="text/x-dc">` に状態・操作・判定が書いてあるのに、
  実装は上半分の HTML（見た目）だけを写した。** モックの `state.sel` / `bulkEdit` / `warnOf()`
  に当たるものが実装に1つも無かった。**モックを実装するときは必ず script 側を読むこと。**
  ② **実装設計書（`impl/08` §5）がファイルの割り方しか決めておらず**、「モックのどの操作を作るか」
  の一覧が無かった（全文735行に「一括」「呼び名」「選択」「準備完了」は各0件）。さらに §5-2 の
  「素の `<input>` で構わない」（IME の二重入力を避けるための技術的な但し書き）が
  **「素の `<table>`＋`<select>` で構わない」まで広げて読まれた**。
  ③ **画面の入力とサーバーの保存済みが二重管理**で、点検も書き出しも保存済みしか見なかった。
  ④ **検証が「画面が出るか」で止まり、モックの操作を1つずつなぞっていなかった**（PR #310 の
  オーバーレイ不具合も同じ理由で見落とした）。

  実機で再現できた重いもの: **配信先の名前を変えて保存するとストリームキーが黙って消える**
  （鍵を `(encoderId, name)` で突き合わせていたため。画面は「保存しました」と出る）／
  **12台打ち込んでそのまま書き出すと見出しだけの Excel が「書き出しました」と落ちてくる**／
  **実施日を選べず案件につき事実上1日ぶんしか持てない**（既定値も UTC 基準で JST 0〜9時は前日）。
  検証: 判定ロジックのテスト30件を新規追加（`shared/tests/deviceSettingsJudgement.test.ts`・
  合計1359件）、検証用 PostgreSQL で鍵の保持/削除/保存失敗の理由を実測、
  **実ブラウザ（PC 1440px・スマホ 375px）で16項目を確認**（本線と控えの列の左端が完全一致・
  一括バー・鍵欄に伏せ字が入らないこと・横はみ出し0px・JSエラー0件）。

  ⚠️ **この PR に入れていないもの**（PR 本文にも明記済み。忘れないよう表の外にも残す）:
  **①権限による入力欄の読み取り専用化** — `useCanEditDeviceSettings()` は作ったが画面への配線は
  未実施で、**reader は今も入力できて保存で 403 になる**。**②段3（仕上げ）** — WEB会議の
  一覧＋インスペクタ化、「まとめてコピー」のパスコード有無をボタンの文言で分ける
  （08 §5-5 の明示の決めごとに今も違反）、Excel ダイアログの見出し見本に実データ行を出す。
  **③機器の台数のハードコード**（収録12台・配信10台）— 会場によって台数が違う場合は未決のまま。
- **#316**（`refactor(client): カレンダーの内部識別子を studio から calendar に改名`）—
  作成 2026-08-22 12:46:38 / CI green 12:48:45（`checks`/`build`とも成功）/
  マージ 12:49:06 頃（**CI green から約20秒後**、#251・#272・#305・#312・#314 と同じ
  「見る前に入った」形）。`get_review_comments` 0件。表に移す指摘はない（レビュー自体が
  届いていないため）。2コミット・14ファイル・+110/-62行。AppKey/パス `studio`→`calendar`
  改名（旧 `/studio/*` は全て `RedirectKeepQuery` で後方互換）。マルチエージェント
  （9ファイル並行編集）＋手動レビューで1ファイル漏れ（`home/types.ts` の `AppBadges` 型）
  を検出・修正済み。`npx tsc -b client` / `npx tsc -b server` / `npm run lint`
  （warning 58件・着手前と同数）/ `npm run test`（1329件）/ `npm run build --workspace=client`
  は実施済み。⚠️ 実ブラウザでのPC/スマホ確認・実DB確認・権限403確認は未実施（環境制約）
- **#318**（`feat(qsheet): サイドバーを案件/番組の文脈連動にし、収録配信設定の簡易入口を廃止`）—
  作成 2026-08-22 12:58:34 / CI green 13:00:33頃（`checks`/`build`とも成功）/
  マージ 13:01頃（terai-takehiro 本人が手動マージ。作成から約3分）。
  `get_reviews`・`get_comments`・`get_review_comments` いずれも0件（`GITHUB_TOKEN` 経由の
  `npm run reviews:debt` は今回も401で使えず、GitHub MCP で直接確認した）。
  表に移す指摘はない（レビュー自体が届いていないため）。マージ後の実ブラウザ確認は未実施
  （PR本文に明記済み。検証環境での目視確認を推奨として残した）。
- **#320**（`fix(qsheet): 収録設定・配信設定の WEB会議と Excel 書き出しを作り直した（段3）`）—
  作成 2026-08-22 13:05:40 / `main` とのコンフリクト解消 push 13:10:21 /
  CI green 13:12:29（`checks`/`build` とも成功）/ マージ 13:12:38頃（**CI green から約9秒後**、
  #251・#272・#305・#312・#314・#317 と同じ「見る前に入った」形）。
  `get_reviews`・`get_comments`・`get_review_comments` いずれも0件
  （`npm run reviews:debt` は今回も 401 で使えず、GitHub MCP で直接確認した）。
  表に移す指摘はないが、**この PR で意図して残した判断が6件ある**ので下記に書き出す
  （レビューが付いていれば指摘されうる箇所であり、書かなければ存在ごと消えるため）。

- **#607**（`feat(daily): ウィークリー活動報告に「隔週キープの数字」（BI）と「資料をつくる」（pptx の半自動生成）を足した`・2026-09-07）—
  **PR 全体はレビュー0件ではない**（Codex が 6 巡・23 件を返し、すべてマージ前に解消）が、
  **最後のコミット 2cf31d5 だけがレビューされていない** — 6 巡目の 2 件を直して push した直後に
  Codex が usage limits を返し、そのまま 01:35Z にマージされた。この回で意図して残した判断は
  下の一覧の #607 に書き出した（#600 と同じ「Code Review が届かない」型。ただしこちらは最終コミットのみ）

- **#610**（`docs(reviews): PR #607（隔週キープ）のレビュー棚卸しを記録した`・2026-09-07）— **この記録の PR 自身**。
  作成 01:45:54Z の直後（01:45:59Z）に Codex が「usage limits に達した」を返し、📝 Code Review は走っていない
  （🔒 Security Review のみ）。文書 3 本（この棚卸し・changelog の下書き・pr-watch の落とし穴メモ）だけの
  変更で、コードには触っていない。マージ時点で指摘が付いていなければ「レビュー0件のままマージ」に当たる —
  記録のための PR を連ねないため、この行で先に書いておく（指摘が付いたら別の PR で表に移す）

### #320 で意図して残した判断（レビュー0件のため自分で書き出したもの）

⚠️ **どれも「気づかなかった」ではなく「今回は直さないと決めた」もの**です。
何が変われば直すかを添えます。

| # | どこ | 何が残っているか | 何が変われば直すか |
| --- | --- | --- | --- |
| 1 | `device-settings.routes.ts` の `export-xlsx` | **書き出しに編集権限が要る**まま。閲覧のみの人は Excel を受け取れない（画面側は押せないようにして理由を出した） | 「閲覧の人にも現地用 Excel を渡したい」が業務上必要と決まったら。⚠️ ただし `keyMode:'plain'`（ストリームキー平文）だけは編集権限に残すこと |
| 2 | `journey-marks.routes.ts` | `GET /journey/marks` も **editor** のまま。今回はパスの範囲だけ直し、**権限の高さは1段も動かしていない**（動かすと今回の PR の検証範囲を超えるため） | ピン留めを閲覧の人にも見せると決めたとき。GET だけ `reader` に下げる |
| 3 | `device-excel.service.ts` | **「入力ガイド」シートを外せない**（サーバーが常に付ける。`?sheets=` も recording/streaming しか受けない）。ダイアログは「必ず付きます」と断って見本だけ出している | 現地から「不要」と言われたとき。`?sheets=` に `guide` を足す |
| 4 | `deviceSettingsApi.ts` | `PreflightResult.headerPreview` が**もう使われていないのに型に残っている**（`preview` に置き換わった）。消すとサーバー側の返り値も直す必要があり、今回の PR を広げないために据え置いた | 次に同じファイルを触るとき。サーバーと同時に落とす |
| 5 | `deckOptions.ts` / `destinationHelpers.ts` | **台数が直書き**（HyperDeck 12台・ENC 10台）。機材が増減しても画面が追従しない | 機材台帳（`client-equipment`）から台数を引ける形が決まったとき。**業務側の判断が要る**ので着手していない |
| 6 | `scripts/check-ui-tokens.mjs` | `raw-palette` と `translucent-text` の記録を **client-qsheet で1件ずつ下げた**（旧 `MeetingCard` のインライン `transform` を消したぶん）。数が下がったので検査の指示どおり `--update` した | — （記録のためだけの行。次に増やそうとすると検査が止まる） |

- **#322**（`docs(qsheet): 制作技術支援(qsheet)のディレクトリ/内部識別子リネーム移行計画を起票`）—
  作成 2026-08-22 13:17:16 / CI green 13:19:04（`checks`/`build`とも成功）/
  マージ 13:19:23頃（**CI green から約19秒後**、#251・#272・#305・#312・#314・#317・#320 と同じ
  「見る前に入った」形）。`get_review_comments` 0件。表に移す指摘はない（レビュー自体が
  届いていないため）。コード変更なし・ドキュメントのみ（`docs/reviews/qsheet-techops-migration-plan.md`
  新規作成＋`client-qsheet/CLAUDE.md`の既存メモにリンク追加）。マルチエージェント監査
  （7観点・8エージェント並行）に基づく移行計画で、実施はまだ決定していない
  （同ドキュメント§6参照）。
- **#325**（`fix(shared,qsheet): ポップアップの幅を全体的に広げ、台本作成の不具合と手入力の多さを直した`）—
  作成 2026-08-22 13:48:54 / CI green 13:50:58（`checks`/`build`とも成功）/ マージ 13:54:55頃
  （**CI green から約4分後**、#270・#274 に近い「待つ時間はあったが原因不明の0件」の形）。
  `get_reviews` 0件・`get_comments` 0件・`get_review_comments` 0件。表に移す指摘はない
  （レビュー自体が届いていないため）。⚠️ **56ファイル・+1,815/-84 行・10コミットの大きめのPR**
  （1 PR = 1画面の原則を外れている。理由と分割案はPR本文に記載）。
  ⚠️ **実アプリを起動しての操作確認はしていない**（当該環境ではログインとシードデータが
  用意できなかったため）。代わりにビルド済みCSSと実際のクラス列を実ブラウザで計測して幅を
  実測している（375px で横スクロール0px／1280px で新規台本作成 672px）。
  ⚠️ **共通ライブラリの既定幅を 512px・560px から 640px に引き上げており、全アプリの
  ポップアップに効く**。実画面での見え方は未確認。
  ⚠️ **`GET /lookup/:projectId/context` は `requireAuth` のみで `sales` 権限を要求しない**
  （制作技術支援の利用者が案件情報を引けないことが、直したかった困りごとそのものだったため）。
  返す項目は9つに絞り営業情報は返していないが、**`studio_bookings.location_note`
  （外現場の自由入力）が新たに全社員に見えるようになる**。PR本文で判断を仰いだが、
  レビュー0件のままマージされたため**未回答**。返さない判断に変える余地は残っている。
  実装後に自前で敵対的レビューを1回かけ、見つけた8件（P1 1件・P2 4件・P3 3件）は
  すべてマージ前に直した。**直さずに残した3件は下の一覧表に足した**。
  ⚠️ **この記録は当時の作業ブランチに置き去りになっており、残骸ブランチの棚卸し
  （PR #351）で発見して回収した**。
- **#327**（`feat(qsheet,liveops): 計時LIVEのミニアプリ化フェーズ2（バンドル統合）`）—
  作成 2026-08-22 14:08:12 / CI green 14:20:23（`checks`/`build`とも成功）/
  マージ 14:20:48頃（**CI green から約25秒後**、#251等と同じ「見る前に入った」形）。
  `get_reviews` 0件・`get_comments` 0件。表に移す指摘はない（レビュー自体が届いていない
  ため）。3コミット・72ファイル・+3,553/-720行。設計は
  [`docs/design/v4/qsheet-v4-coding/12-live-timer-decision.md`](../design/v4/qsheet-v4-coding/12-live-timer-decision.md)
  §4（フェーズ2）。マルチエージェント（調査→画面移植→画面移植→URL再設計→
  レジストリ/権限統合→レビュー→修正→再確認）で実装し、**セッション内のワークフロー
  判定バグ**（「指摘なし」の文字列が別小見出しに含まれていたため、レビューが実際に
  致命的指摘2件を出していたのに誤って「指摘なし」と判定され、修正フェーズがスキップ
  されていた）を人間の確認で発見・修正。致命的2件（①qsheetのreader/editor権限では
  運用画面が一切開けない ②旧スタンドアロンセッションが永久に迷子になる案内文）を
  修正した後、**その再確認過程でさらに1件**（`server/src/contexts/liveops/socket.ts`の
  Socket.IO認証が権限区画統合＝migration 232 に追随しておらず、本番相当の
  `AUTH_MODE=password`ではsystem_admin以外の全ユーザーがタイマーのリアルタイム
  同期に接続できなくなる欠陥）を発見し、このセッションで直接修正・実機確認した。
  ⚠️ **push後にmainとのマージコンフリクトが発生**（並行してマージされた#320系の
  サイドバー文脈連動化・#326のレンタル機材スクレイパー修正等）。`client-live/src/pages/
  SessionHomePage.tsx`（modify/delete。削除を維持）・`scripts/check-ui-tokens.mjs`
  （生成物のカウント値。`--update`で実測値を再生成）の2件を解消。**さらにマージ後、
  テキスト上は衝突しなかったが意味的な抜けを発見**: main側で並行して書き直された
  `client-qsheet/src/components/layout/nav.ts`（サイドバーの文脈連動化）が、このPRの
  ミニアプリレジストリ更新（liveopsを`kind:'panel'`に統合）より前に書かれていたため、
  収録設定・配信設定と並ぶはずの「計時・視聴者」が左メニューから漏れていた。追加コミット
  （`24ac647`）で修正し、`client-qsheet/CLAUDE.md`の古い記述（「レジストリは後続の
  ステージがやる」＝実際には完了済みの段階を指す）も実態に合わせ更新した。マージ・
  コンフリクト解消・追加のバグ修正のたびに`npm run typecheck:all`/`lint`/`test`/
  `check-collab-parity`/`check-links`/`build:changed`を独立に再実行し、すべてOKを
  確認してからpushした。表示画面契約（`TimerDisplayPage.tsx`等）は全工程で無変更。
  ⚠️ **実ブラウザでのPC/スマホ確認は未実施**（環境制約。PR本文に明記。代わりに実DB＋
  実サーバーへの直接リクエスト・実Socket.IO接続で権限・移行安全性を確認した）。
- **#329**（`fix(rental-scraper): meta charset宣言が無いページで文字化けが再発する不具合を直した`）—
  作成 2026-08-22 14:34:30 / CI green 14:36:41（`checks`/`build`とも成功）/
  マージ 14:37:55（terai-takehiro 本人が手動マージ。**CI green から約1分14秒後**、
  待つ時間はあったが原因不明の0件・#244・#262 に近い形）。`get_reviews`・`get_review_comments`
  いずれも0件（`npm run reviews:debt` は今回も401で使えず、GitHub MCP で直接確認した）。
  表に移す指摘はない（レビュー自体が届いていないため）。実サイトへの疎通確認はサンドボックスの
  制約で未実施のまま（PR本文に明記済み）——文字化け・価格取得の両方とも、実際に直っているかは
  次のデプロイ・再クロール後のログ／画面確認が必要。
- **#331**（`docs(qsheet): 移行計画をPR #327（liveopsミニアプリ化）の反映で更新`）—
  作成 2026-08-22 14:44:12 / CI green 14:46:13（`checks`/`build`とも成功）/
  マージ 14:47:17頃（**CI green から約1分4秒後**）。`get_review_comments` 0件。
  表に移す指摘はない（レビュー自体が届いていないため）。コード変更なし・ドキュメントのみ
  （`docs/reviews/qsheet-techops-migration-plan.md`を、並行マージされたPR #325/#326/#327/#329の
  内容確認に基づき更新。#327のliveops→qsheet権限統合（migration 232）を検知し、計画書§7の
  「識別子は表示名変更では変えない」前例を条件付きの基準へ修正した）。
- **#333**（`feat(server): 計時・視聴者 表示レイアウト機能のスキーマ＋APIを追加した`・
  表示画面レイアウトエディタ機能の3分割PRの1本目）— 作成 2026-08-22 15:06:59 /
  マージ 15:08:04（terai-takehiro 本人が手動マージ。**作成から約1分5秒後、CI
  〈`checks`/`build`〉はまだ`in_progress`のうちにマージされた** — CI green を待たずに
  マージされた点は #244・#262・#329 の「CI greenから短時間で0件のままマージ」とも
  異なる新しいパターン）。`get_reviews`・`get_review_comments`いずれも0件。表に移す指摘は
  ない（レビュー自体が届いていないため）。実装内容自体は、実装エージェント・独立レビュー
  エージェント（`npm run typecheck`/`lint`/`test`〈`liveDisplayContract.test.ts`5件含む〉・
  `npm run verify:up`の実Postgres・実サーバーへのHTTPリクエストで権限境界を実測）の
  ワークフロー内検証、および私自身によるファイル内容の目視確認（migration DDL・
  ルート実装・既存`GET /:id/display`への無変更）を経てからpushしたため、CI完走前の
  マージであっても実装の妥当性そのものは別経路で確認済み。ただし**CI自体の結果
  （このセッションのローカル実行と、実際のCI環境の差異）は未確認のまま検証環境ブランチに
  入った**ことは記録しておく。⚠️ このPRのマージ直後に別セッションの `client-qsheet` →
  `client-techops` ディレクトリ改名（#334）がmainへ入っており、以後の
  PR2・PR3（表示画面対応・エディタUI）はこの改名後のパスを前提に実装する必要がある。
- **#334**（`refactor(qsheet): Phase 1 — client-qsheet ディレクトリを client-techops に改名`）—
  作成 2026-08-22 15:08:54 / CI green 15:11:01（`checks`/`build`とも成功）/
  マージ 15:12:41頃（**CI green から約1分40秒後**）。`get_review_comments` 0件。
  表に移す指摘はない（レビュー自体が届いていないため）。245ファイル変更（うち219ファイルは
  `git mv` によるディレクトリ移動）。qsheet→techops移行計画のPhase 1を実施 —
  ディレクトリ名・ビルド設定・検査スクリプト・TS識別子（`QSHEET_PC_ONLY`等）の改名のみで、
  ベースパス`/qsheet/`・`AppKey`・`permissionModule`・Socket.IO・DB・MCPツール名は無変更。
  マルチエージェント実装（8タスク並行）後、`shared/tests/*.test.ts`14ファイルの
  ワークスペース境界をまたぐimportパスなど当初のタスク分割から漏れていた参照47ファイルを
  手動監査で追加修正し、`package-lock.json`の孤児エントリも除去した。
  `npx tsc -b`（全ワークスペース）0エラー・`npm run test`（1452件）全通過・`npm run lint`
  0エラー・`npm run build`成功を確認済み。⚠️ **Dockerの実ビルド検証はdockerデーモンが
  この環境に無く未実施**（PR本文に明記済み。`npm run build`成功で代替確認）。
  Phase 2以降（本番URL・Socket.IO切替）は未着手のままユーザーの追加判断待ち。
  併せて `#333`（`docs(design): 計時LIVEディスプレイレイアウトエディタの設計を追加`・
  liveopsコンテキスト内の新規機能設計＋migration 233）を確認し、qsheet移行計画の
  前提事実（`qsheet_`系DBオブジェクト数・`server/src/contexts/qsheet/index.ts`の
  router.use数）に影響が無いことを確認した（migration 233は`liveops_display_layout`
  というliveops固有のテーブルで`qsheet_`プレフィックスではない。新規ルートも
  `server/src/contexts/liveops/`配下でqsheetのマウントには触れていない）。
- **#337**（`refactor(qsheet): Phase 2 — ベースパス・AppKey・APIプレフィックスを techops へ
  二重マウント`）— 作成 2026-08-22 15:41:55 / CI green 15:44:02頃 / マージ 15:44:49頃
  （**CI green から約1分後**）。`get_review_comments` 0件。表に移す指摘はない
  （レビュー自体が届いていないため）。83ファイル変更。qsheet→techops移行計画のPhase 2を
  実施 — ベースパス`/qsheet/`→`/techops/`・`AppKey`・APIプレフィックスを二重マウントで
  改名（旧URLは全て後方互換で維持）。`permissionModule`・Socket.IO・DB・MCPツール名は
  無変更（Phase 1と同じ方針）。マルチエージェント実装（9タスク並行）後、
  `shared/tests/{miniapps,qsheetCollabMetaSync}.test.ts`のソース文字列アサーション9件・
  `client-live/src`の別バンドルからのoutboundリンク10ファイルなど、当初のタスク分割から
  漏れていた参照を手動監査で追加修正した（Phase 1と同型の見落とし）。
  `npx tsc -b`（全ワークスペース）0エラー・`npm run test`（1452件）全通過・`npm run lint`
  0エラー・warning 59件（Phase 1と同数）・`npm run build`（client-techops/server）成功を
  確認済み。Phase 3（Socket.IOカットオーバー）・Phase 4（MCPツール名）は未着手のまま
  ユーザーの追加判断待ち。
- **#336**（`docs+feat(liveops): PR #333棚卸し ＋ 表示レイアウトPR2（表示画面対応）`）—
  作成 2026-08-22 15:15:23 / マージ 15:51:44。`get_reviews`・`get_review_comments`
  いずれも0件。表に移す指摘はない（レビュー自体が届いていないため）。⚠️ **当初は
  「PR #333の棚卸し記録のみ」の予定だったが、PR2（表示レイアウト機能の表示画面
  `TimerDisplayPage.tsx`対応）の実装がマージ前の同じブランチに積み重なり、2つの
  独立した変更が1本のPRに混在した**（force-pushでの巻き戻しはしない方針のため）。
  PR本文を「棚卸し＋PR2実装が混在している」旨と、リスクの高いPR2部分への注意喚起を
  明記する形に更新して対応した。マージ後、並行してマージされた `client-qsheet`→
  `client-techops` Phase 2改名（#337）とのマージコンフリクト（`docs/reviews/
  codex-findings-v4.md`の同時追記による1件）を解消し、`typecheck:all`/`lint`/`test`
  （1452件）を再確認してから再pushした。表示画面契約（`liveDisplayContract.test.ts`
  5項目）はこのPRを通じて無変更のまま。
- **#339**（`refactor(qsheet): Phase 3・4 — Socket.IOブリッジとMCPツール名の二重登録`）—
  作成 2026-08-22 16:20頃 / CI green 16:22:16頃 / マージ 16:24:27頃
  （**CI green から約2分後**）。`get_review_comments` 0件。表に移す指摘はない
  （レビュー自体が届いていないため）。18ファイル変更（うち2ファイルは新規:
  `scripts/dev-verify/socket-bridge-check.mjs`）。qsheet→techops移行計画のPhase 3・4を
  実施 — Phase 3は当初計画（Socket.IOの計画停止枠での一斉切替）を、本番アクセスができない
  この環境からは実施・検証できないため、ユーザーへの`AskUserQuestion`確認を経て
  「ブリッジ」方式（`/qsheet`・`/techops`両ネームスペースの同名ルームを常に相互中継し、
  新旧混在でも同期を止めない設計）に変更して実装した。実Postgres＋実socket.io-client
  2本で6項目を実地検証し、検証中に見つけた既存バグ（不正なyjs:updateバイナリで
  サーバープロセスが丸ごと落ちる。案件の共同編集とも共有する`roomManager.ts`が対象）も
  ついでに直した。Phase 4はMCPツール5本（`get_qsheet`等）を`get_sheet`等へ改名し、
  旧名も`gate.ts`の`WRITE_TOOL_PERMISSIONS`込みで二重登録した（片方だけ登録すると
  旧名が権限ゲート無しで通ってしまう設計上の落とし穴に気づいて対応）。
  `permissionModule`・DBは無変更。`npx tsc -b`（全ワークスペース）0エラー・`npm run test`
  （1452件）全通過・`npm run lint`0エラー・warning 59件（従前と同数）・
  `node scripts/generate-mcp-tools.mjs`（権限ゲート検証OK）・`npm run build`
  （server/client-techops）成功を確認済み。
- **#340**（`feat(techops): 表示レイアウト機能のエディタUI・テンプレートライブラリを
  追加した`・表示画面レイアウトエディタ機能の3分割PRの3本目・最終）— 作成
  2026-08-22 16:25:13 / CI green 16:27:31頃 / マージ 16:27:44（**CI green から
  約13秒後**）。`get_reviews`・`get_review_comments`いずれも0件。表に移す指摘はない
  （レビュー自体が届いていないため）。実装内容自体は、実装エージェント・独立レビュー
  エージェント・独立検証エージェント（3段階のワークフロー内検証）に加え、私自身による
  `npm run typecheck:all`/`lint`/`test`（1452件green）の再実行、`git diff --stat`での
  表示画面ファイル・server配下の無変更確認を経てからpushした。設計書は
  `client-qsheet`（`/qsheet/`）時代に書かれていたため、実装時に`client-techops`
  （`/techops/`。並行してマージされたPhase 1〜4改名・#334/#337/#339）へ読み替えた。
  これで計時・視聴者 表示レイアウト機能（PR1〜3の3分割）はすべて完了。
- **#343**（`release: v4.2.0`）— 作成からCI green確認後すぐにマージ（レビュー到着を
  待たずに自分でマージした・詳細下記）。`get_reviews`・`get_review_comments`いずれも0件。
  表に移す指摘はない（レビュー自体が届いていないため）。⚠️ **コード変更を伴わない
  リリース版上げPR**（`docs/changelog.d/` 下書き167件を`release:notes`で集約・
  `CLAUDE.md`/`README.md`/`package.json`の3か所と`docs/version-history.md`を更新。
  `docs/v4-plan.md`・`scripts/check-frozen-css.mjs`の「v4.1.8」先取り表記2箇所も
  v4.2.0へ修正）で、typecheck/lint/test（1452件）/check:versionすべてgreenを確認済み・
  ユーザーから「R0行きましょう v4.2.0で」の明示指示を受けての実行だったため、
  レビュー到着を待たずマージした。
- **#345**（`fix(client): トップページのタイルから制作技術支援が消えていたのを直した`）—
  作成から28秒でユーザー自身がマージ（PR作成時点でCIはまだ`in_progress`）。
  `get_reviews`いずれも0件。表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **v4.2.0公開直後にユーザーから「制作技術支援アプリ自体が露出していません」と
  報告を受けた本番影響のあるバグ修正**（qsheet→techops移行Phase 2でapps.tsのAppKeyを
  改名した際、`HomePage.tsx`の`DAILY_KEYS`配列が旧キー`qsheet`のまま取り残されていた。
  トップページのタイルからのみ制作技術支援が消え、アプリ切替・左メニューは
  `visibleApps()`経由のため無事だった）。マージ前にローカルで
  typecheck/lint/test（1452件）すべてgreenを確認済み。マージ後のCI
  （push to main・run 32588171778）は直後の PR #346 マージで cancelled
  （同一 `main` への連続 push による GitHub Actions の仕様。#346 の Deploy
  run 32588298575 が success で完了しており、#345 の内容もそこに含まれて
  検証済み）。
- **#347**（`release: v4.2.1`）— 作成から約24秒でユーザー自身がマージ（マージ時点で
  CIは`in_progress`）。`get_reviews`いずれも0件。表に移す指摘はない（レビュー自体が
  届いていないため）。⚠️ **コード変更を伴わないパッチリリースPR**（PR #345のバグ修正1件
  のみを含む・DBマイグレーションなし）で、マージ前にローカルで
  typecheck:all（6アプリ+server）/lint/test（1452件）/check:versionすべてgreenを
  確認済み・ユーザーから「v4.2.1をリリースしましょう」の明示指示を受けての実行
  だったため、レビュー到着を待たずマージした。マージ後のCI（push to main・
  run 32588489962）の完了は別途確認する。
- **#349〜#360（リリース準備 第1・2波の12本 — R1〜R3・R2-e・R6-a を含む・2026-08-22〜23）** — いずれも
  `get_reviews` 0件のまま、PRごとのCI（build/checks）green を確認してマージした
  （レビュー到着を待たなかった）。表に移す指摘はない（レビュー自体が届いていないため）。
  **この12本は R6-c（未レビューPRの掃き出しレビュー）の対象リスト** — 事後レビューの
  結果はこの表へ行追加する。個別の補足:
  - **#349**（`chore(lint): 文書のリンク切れと migration 番号の重複を検査するようにした`）—
    新規検査2本（`check-md-links.mjs` / `check-migration-numbers.mjs`）を `npm run lint` に
    追加。既存文書の実リンクを全数走査して検証した
  - **#350**（`chore(docs): CLAUDE.md の古い記述・矛盾を一掃した`・R2-a）— ルート
    `CLAUDE.md` だけの原子的PR。認証の節は `server/src/config.ts` の実装
    （`AUTH_MODE` の決まり方）と突き合わせて書き直した
  - **#351**（`docs(release-prep): リモートの残骸ブランチの棚卸し表を作った`）— 新規文書のみ。
    「未マージ2件」に見えた `fix/release-tooling-titles` は shallow clone の錯覚で、
    `git fetch --unshallow` 後の `merge-base --is-ancestor` でマージ済みと確認した
  - **#352**（`chore(docs): CONTRIBUTING.md の古い記述を実態に合わせた`・R2-b）— 文書のみ
  - **#353**（`chore(verify): verify:ui にカレンダー等の画面・768px・潰れ検知・
    スクショ証跡を足した`）— マージ前に実行し 15画面×3幅・461チェックが走ることを確認。
    既存のカレンダー系の崩れ6件を発見した（R5 の直し待ち。直したら行追加する）
  - **#354**（`chore(ci): 役目を終えた仕組みを片づけた`・R2-e）— `dev:frozen` /
    `build:render` / `start:render` の削除・`ci.yml` の `release/**` トリガー削除・
    `check-links.mjs` の対象整理（apps.ts の廃止登録行1件は理由コメント付きで ALLOW）
  - **#355**（`chore(deps): 本番依存の既知脆弱性を npm audit fix で一掃した`・R6-a）—
    本番依存の脆弱性 32→10。axios 1.18 の型変更で `OpenItemDialog.tsx` の `mutationFn` を
    async/await 形に直した（レスポンス不使用のため挙動不変）。typecheck:all / lint /
    test（1452件）green。残る High 2件（xlsx → R6-b・nodemailer → 要判断）は別途
  - **#356**（`docs(reviews): 置き去りの棚卸し記録2件を回収し、棚卸しの運用ルールを
    明文化した`・R2-f）— この文書のみ
  - **#357**（`chore(docs): client/CLAUDE.md を現役ルールに絞り、経緯ログを docs/reviews へ
    移した`・R2-d）— エージェント実装。マージ前に新しい `client/CLAUDE.md` 全308行を
    通読し、パス・実測の主張を確認した（経緯ログは `docs/reviews/client-v4-build-log.md`
    へ全文保存）
  - **#358**（`docs: ia.md をロールバック済みの歴史から切り離し v4 の入口の決めごとに
    書き直した`・R3-d+e）— 文書のみ。`check-md-links`（92文書/277リンク）・
    `generate-version-history`（464件・変化なし）で検証
  - **#359**（`chore(docs): shared/CLAUDE.md の凍結前提の記述を現状に合わせた`・R2-c）—
    エージェント実装。マージ前に主要な事実主張（凍結0個＝`check-frozen-css.mjs` の
    `APPS` 空・共通シェルと `base.css` は配信中5アプリ・ストレージキー統一・旧ヘッダーの
    利用者は `client-awards` のみ・`check-ui-tokens` の app-foundation が3アプリのまま等）を
    リポジトリと突き合わせて確認した
  - **#360**（`docs: docs/README.md の目次を新設し、役目を終えた文書を archive/ へ移した`・
    R3-a+b）— エージェント実装。#358 と同じ `docs/reviews/db-drift-audit.md` の行を
    触っていたため衝突し、私が main をマージして両取り（roadmap.md と ia.md の
    張り替えを両方生かす形）で解消した。あわせて並行PRの取り残し1件
    （#357 の経緯ログが、#360 が archive へ移した監査ファイルへ旧パスでリンク）も
    同じマージで直した。マージ前にスコープ（docs/ のみ・禁止ファイル無変更・
    移動は R95〜R100 のリネーム）と新設の `docs/README.md` / `docs/archive/README.md` /
    スタブ7件 / `docs/ops/calendar-dedup-plan.md` の中身を確認した。
    `check-md-links`（103文書/323リンク）・`check-changelog` OK
- **#361〜#365（リリース準備の続き・5本・2026-08-23）** — いずれも `get_reviews` 0件のまま
  CI green でマージ（#361 棚卸し記録 / #362 CI に npm audit・R6-g / #363 README 減量・R3-c /
  #364 PcOnlyPanel 磨き・R5 / #365 カレンダー・設定の崩れ修正・R5）。#364 はマージ前に
  diff 全読＋再利用した CSS 機構（`[data-row-in]`・`--v4-row`・reduced-motion・`.v4-eyebrow`）の
  実在を確認、#365 はマージ前に diff 全読（エージェントは修正前の FAIL 再現→修正→
  対象16ページと client 全60ページ×3幅の再実走で無回帰を確認済み）。#365 の実走が
  報告した**スコープ外の既存崩れ**（機材 貸出設定のバッジ右端ずれ）は下の一覧に行を足した
- **R6-c（未レビューPRの掃き出しレビュー）を実施した（2026-08-23）** — 対象は上の
  #349〜#360 と #361〜#363 の15本。5つの観点（検査スクリプト／依存関係とCI／ルート入口文書／
  アプリ別 CLAUDE.md と ia.md／アーカイブ再編と棚卸し記録）で並列レビューし、出た指摘19件を
  **1件ずつ独立の反証エージェント（P1/P2 は2票・P3 は1票）が潰しにかかり**、全票が実在＋実害を
  認めた12件だけを確定とした（7件は「事実だが実害なし・仕様どおり・誤読」で棄却。棄却理由も
  ワークフローの記録に残る）。確定12件の内訳と処置:
  - **P2 1件**: ルート CLAUDE.md のブロックアプリ一覧が機材管理のポートを 5175 と書くが実体は
    5174 で、しかも制作技術支援と同番衝突していた（dev:all では起動順で非決定に）→
    **vite 側を表どおり 5175 に直して解消**（この修正PR）
  - **P3 10件**（数値・記述の実態ズレと取り残し）: CLAUDE.md「6アプリ配信」→5アプリ／
    CONTRIBUTING「41本・2,179行」→数値をやめ実測コマンド参照に／ci.yml「呼ばれ方は3通り」の
    列挙2件＋workflow_dispatch 漏れ／client/CLAUDE.md「転送 約12本」→36本／棚卸し文書の
    「35回変更」→実測は百回以上／docs/README.md「約300ファイル」→約170／#356 が回収した
    #325 の指摘行の対象ファイルが回収時点で削除済みだった（上の一覧の状態を更新）／
    アーカイブ移動した文書の旧パスがコード内コメント15か所に残存（張り替え）／
    stale-branches.md の2行と「#349 が open」が半日で古くなっていた（更新）／
    #361 の挿入文内の「この11本」と件数の食い違い（修正）— **すべてこの修正PRで対応**
  - **P3 1件（未対応・下の一覧に行あり）**: verify:ui の潰れ検知の判定式が「1文字ずつ縦に
    折り返される崩れ」の一部を原理的に取りこぼす
  - あわせて R6-c とは別に私が発見した1件もこの修正PRで対応: verify:ui の
    `/sales/projects/pj-1` 7ページは **pj-1 がシードに存在せず**「案件が見つからない」の
    エラーパネルを測っていた → シードの GLS-A002 の id を `pj-1` に固定（`gpm-1` と同じ方式）
- **#366〜#368（リリース準備の続き・3本・2026-08-23）** — いずれも `get_reviews` 0件のまま
  CI green でマージ（#366 R6-c の記録と修正11件＋シード pj-1 固定 / #367 xlsx→exceljs 置換・
  R6-b / #368 実機点検チェックリスト・R5 層3）。#367 は旧 xlsx と新 exceljs の両方で組んだ
  フィクスチャの突き合わせ（取込・出力・決算パーサの最終値一致＋2桁年形式は新実装のみ正しく
  読める改善の実証）を経てマージした。R5 層2 のスクショ267枚（89画面×3幅）はこの回に撮影し、
  撮影中に**シードの日付（2026年3〜4月）が既定の期間絞り込みから外れ、案件一覧などが
  0件表示になる**ことを確認した（壊れではないが、シードの日付を相対化する改善は検討に値する —
  下の一覧に行あり）
- **#369〜#372（リリース準備の続き・4本・2026-08-23）** — いずれも `get_reviews` 0件のまま
  CI green でマージ（#369 棚卸し記録と #366 の一覧行の追加漏れ2件の補い / #370 nodemailer v9
  更新＋CI の npm audit ブロッキング化・ユーザー判断「更新する」/ #371 音声サポート旧URL停止・
  段階③・ユーザー判断「消す」/ #372 層3チェックリストの最新化）。#370 は実SMTPスタブで
  送信の動作確認、#371 は実サーバー・実DBで4パターン（有効200・偽404・失効410・旧URL410）を
  確認してからマージした。#370・#371 の解消で**本番依存の High 脆弱性は0件**になり、CI の
  npm audit をブロッキング化した
- **#373〜#378（R4 guide/ 6本・2026-08-23）** — **こちらはレビュー0件マージではない**:
  R4 の決めごと（1本ずつ PR にしてユーザーの読み合わせを挟む）どおり、6本すべてを
  **ユーザー自身が GitHub 上で読んだうえで自らマージした**（コメント・修正指示は0件＝
  ドラフトのまま通過）。内容はマルチエージェントのドラフト（各事実をリポジトリの実ファイルで
  裏取り・数字を書かない・カタカナ語に言い換え併記の規約）を私が精査してから PR にした
  （#377 のドラフトに1件だけあった相対リンクの誤り `../docs/v4-progress.md` を修正済み）。
  マージ方式は Squash ではなく通常のマージコミット（ユーザー操作・リポジトリ所有者の判断）
- **#380（レスターのレンタル機材クロールが0件のまま取得できない不具合・2026-08-23）** —
  `get_reviews` 0件のまま、作成から約2分16秒でCI green・terai-takehiro 本人が手動マージ
  （`get` の `merged_by` で確認）。利用者から「TOCは取得できているがレスターが取得できて
  いない」という報告を受けて調査・修正した。原因は `restar_scraper.py` の商品ID連番スキャンで、
  連続404の打ち切り閾値（`MAX_CONSECUTIVE_MISS=40`）が「1件も見つかっていない」段階から
  効いており、実カタログの先頭ID（docstringの実例 `item277`）に辿り着く前に走査が止まって
  いた可能性が高い。最初の1件が見つかるまでは打ち切らないように修正し、回帰テスト3件を
  追加した。⚠️ **この開発セッションは対象サイト（`restargp.com`）への外部接続ができず、
  実際のHTML構造・実際のID分布を確認できないまま修正しているため、推測に基づく修正である
  ことに注意** — デプロイ後、検証環境の `rental_scraper_dev` ログで実際に件数が取れて
  いるかの確認が必須（README に手順を追記済み）。まだ0件が続く場合は `END_ID` を広げるか
  サイトAPIへの切り替えが必要になる可能性がある
- **#384（shared/ の未使用 export を整理・R6-e・2026-08-23）** — `get_reviews` 0件のまま
  terai-takehiro 本人がマージ（通常のマージコミット。CI の `build` ジョブは Docker Buildx の
  ブートストラップが `moby/buildkit` イメージの pull で 502 Bad Gateway になり失敗していたが、
  diff とは無関係な純粋インフラ障害で、`checks`（型チェック・lint・test 等）は green だった。
  マージが CI 完了を待たずに行われたため、`build` ジョブの再実行は不要になった —
  マージ後に main の CI が再度緑になることを別途確認すること）。マルチエージェントで
  shared/src 全489 export・候補187件を検出→反証検証し、外部参照0の31件を削除、
  同一ファイル内でしか使わない36件を export だけ外した（シンボル自体は残す）。適用前に
  検証結果を実ファイルと突き合わせて矛盾を1件発見・修正した: `ScrollBar` は当初「削除」
  判定だったが、同ファイルの `ScrollArea`（keep 判定）が内部で使うため export だけ外す
  扱いに訂正した。`chartDefaults`/`chartColors` の組は検証バッチの判定が割れて未確定
  だったため見送った（別途判断・下の一覧に行なし＝実害のある指摘ではなく単なる保留）
- **#387（v4.3 実機点検で見つかった不具合をまとめて修正・2026-08-23）** — `get_reviews`
  0件のまま、作成から約3分（CI green・`checks`/`build` とも success、`mergeable_state: clean`
  を確認済み）で terai-takehiro 本人がマージ（通常のマージコミット）。実機点検で見つかった
  15件の不具合のうち11件を修正: トップページの件数バッジ誤表示（`items.length`→
  `counts.total`）／カレンダー一覧の前月データ混入／仮押さえの終了済み案件残存（`from`
  下限追加）／失注分析の月別金額誤集計（`updated_at`→`lost_at`）／請求一覧の半角カナ化け
  （表示側＋保存側の NFKC 正規化）／PDF取込の部分登録が分かりにくい問題（警告表示を追加。
  解析・登録ロジックは不変）／収録設定・配信設定・レンタル機材検索の実施日デフォルト
  （今日→案件の本番実施日）／「計時・視聴者」タブのサブメニュー消失／進行台本一覧の
  「最終更新」誤表示／日常業務の見出し・バッジ表記不整合3件／財務サイドバーの内部向け
  表現。残り3件（取引先マスターの重複登録・権限の仮ロール大量残存・レンタル機材検索の
  Enter必須）は棚卸し済みバックログまたは現ソースで再現せずのため未対応（PR本文に記載）
- **#388（ブラウザタブ・アプリ切替に残っていた旧アプリ名の不具合を修正・2026-08-23）** —
  `get_reviews`／`get_comments` とも0件のまま、terai-takehiro 本人がマージ（通常のマージ
  コミット。CI green を確認済み）。ユーザー指摘（`/techops/top` のブラウザタブが「Qシート」
  のまま）を機に横断調査し、同種の残骸4件を追加で発見・修正した:
  `client-techops`/`client-live` の `&lt;title&gt;` が旧アプリ名のまま／
  `AppShell.tsx` の `appKey="qsheet"` が Phase 2 の改名（→`"techops"`）に追随しておらず
  `APP_BY_KEY` が解決できないため**制作技術支援の全画面でヘッダーのアプリ切替ボタンが
  アイコンなしの「ONAiR」表示に落ち、切替候補にも自分自身が重複して出ていた**
  （今回でいちばん影響が大きいもの）／利用マニュアル・ログイン画面の案内文が「Qシート」の
  まま／設定「ユーザー管理・権限」のマニュアルが権限モデル単純化で廃止済みの旧UI
  （🔑ボタン・「権限修復」ボタン）を説明したままだった。**この開発セッションからは
  実ブラウザでの見え方を確認していない**ため、下の一覧に行を足した
- **#389（本番環境向けレンタル機材スクレイパーを追加・2026-08-23）** — `get_reviews`／
  `get_review_comments` とも0件のまま、作成 10:57:04 / CI（`checks`/`build`）両方 green
  10:59:04 / マージ 11:04:43（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約5分39秒待っており、#251・#256・#272 の「見る前に入った」形ではない**
  （#258・#270 に近い、待つ時間はあったが原因不明の0件）。`npm run reviews:debt` は今回も
  401 で使えなかったため GitHub MCP で直接確認した。利用者から「本番環境で、レンタル機材の
  スクレイピングが全然効かない」という報告を受けて調査したところ、不具合ではなく**本番向けの
  スクレイパー自体（`rental_scraper_prod`）がそもそも存在していなかった**ことが原因と判明。
  検証環境の `rental_scraper_dev` と同構成で本番向けサービスを新設し、実行時刻を
  `RENTAL_CRON_HOUR_PROD`（既定4時）で検証（既定5時）とずらして対象サイトへの
  アクセス頻度が2倍にならないようにした。表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **この開発セッションは対象2社サイト（TOC・レスター）への外部接続ができず、
  `rental_scraper_prod` の実デプロイでの動作は未確認**（デプロイの結線・生存確認の仕組み自体は
  検証環境の `rental_scraper_dev` で実績のある形をそのまま踏襲している）。次の Release 公開後、
  `rental-scraper/README.md`「本番環境での自動実行」記載の4段診断で実際にクロールできて
  いるかの確認が必須
- **#393（カレンダーのスマホ週表示と検収書の備考・分類を修正・2026-08-24）** — `get_reviews`／
  `get_review_comments`／`get_comments` とも0件のまま、作成 05:22:12 / CI（`checks`/`build`）
  両方 green 05:24:24 / マージ 05:25:09（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約45秒でマージ**しており、#251・#256・#272 と同じ「見る前に入った」形。
  `npm run reviews:debt` は今回も 401 で使えなかったため GitHub MCP で直接確認した。
  利用者からの指摘（カレンダーのスマホ表示は幅が狭いと週・月が見られない／見積の備考が
  空でも検収書に「見積 v2 から登録」という内部文言が印字される／検収書のカテゴリが
  見積書と違い英語表記になる）の3件に対応。① スマホの月/週切替（`MobileCalHeader.tsx`）
  と週表（`MobileWeekStrip.tsx`）を新設。② `estimate.service.ts` の `convertToRevenue` が
  備考に自動生成していた `見積 vN から登録` を、見積側の備考をそのまま写す形に直した。
  ③ `estimate_items.category` の内部キー（`studio`/`tech`/`other`）を日本語に翻訳する
  `estimate-pdf.service.ts` の `categoryLabel` を `/revenues/:id/pdf`（請求書・検収書）でも
  使うようにした。表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **この開発セッションは実ブラウザでの PC/スマホ確認・実 DB での保存確認を行っていない**
  （PR本文の検証チェックリストにも未実施のまま記載済み）。typecheck・lint・build（shared/
  client/server）・`npm run test -w shared`（1453件、既存の `estimateCategory.test.ts` を含む）
  は通過している
- **#395（機材管理のMCPツール新設・techopsスケジュール表への書き込み追加・2026-08-24）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 05:27:35 /
  CI（`checks`/`build`）両方 green 05:29:08 / マージ 05:42:13（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約13分待っており、#244・#262・#389 と
  同じ「見る前に入った」形ではない、原因不明のまま0件だったケース**。ユーザーから
  「MCPについて、v4で新たに作成したアプリ関連含めて整っているか確認を」と依頼され、
  機材管理（equipment）・計時視聴者（live）向けの MCP ツールが無いことを調査で発見・
  報告したところ、ユーザーから「機材管理は欲しい」「techops のスケジュール表などは
  どうか」との追加依頼を受けて実装した。機材管理は read 5（台帳検索・詳細・貸出履歴・
  棚卸し状況）＋ write 2（貸出・返却）を新設、techops はスケジュール表の write 3
  （枠の作成・更新・削除）を追加。機材管理の追加は会社方針「AIを使い捨てにしない」に
  基づき `.claude/skills/ai-feedback-loop/` で事前レビューを実施し、貸出/返却は
  AI の自由生成物ではなく操作代行（`lend_security_card` と同型）と判定して5条件の
  フル適用ではなく `mcp_audit_log` 記録で足りると判断した。表に移す指摘はない
  （レビュー自体が届いていないため）。
- **#401（techopsのハブ画面「スケジュール表」タイル件数が常に0件のままだった不具合を修正・
  2026-08-24）** — `get_reviews`／`get_comments` とも0件のまま、作成 06:58:21 /
  CI（`checks`/`build`）両方 green 07:04:13 / マージ 07:04:27（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約14秒でマージ**しており、
  #251・#256・#272・#393 と同じ「見る前に入った」形。`npm run reviews:debt` は今回も 401 で
  使えなかったため GitHub MCP で直接確認した。利用者からの指摘（「スケジュール表入ってるのに
  0件から変わらない（更新しても）」）に対応。サーバー側 `journey.service.ts` の `docs` が
  進行台本（`app: "sheet"`）しか積んでおらず、スケジュール表そのもの（`app: "schedule"`）を
  1件も入れていなかったのが原因。`fetchSchedulesForProject`/`fetchSchedulesForProgram` を
  追加してジャーニーに組み込み、`JourneyDayCard.tsx` の台本一覧側は `app === "sheet"` で
  絞り込むよう修正。あわせて「次に決めること」の `no_schedule` 提案が枠の有無だけで判定して
  いた関連不具合も直した。表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ 実ブラウザ・実DBでの確認はこのセッションから行っていない（PR本文に明記済み）
- **#402（techopsスケジュール表・項目クリックが新規作成の空欄になる不具合を修正・2026-08-24）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 07:06:33 /
  CI（`checks`/`build`）両方 green 07:08:32 / マージ 07:10:13（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約1分41秒でマージ**しており、
  #251・#256・#272・#393 と同じ「見る前に入った」形。`npm run reviews:debt` は今回も 401 で
  使えなかったため GitHub MCP で直接確認した。利用者からの指摘（「項目をクリックしたときに
  なぜか新規作成になる。いろいろと挙動がおかしい」）に対応。`ScheduleItemDialog` が
  `SchedulePage` に常駐し `open` の真偽だけで表示を切り替える作りなのに、フォーム状態
  （`draft`）を `useState` の初期化関数（初回マウント時にしか走らない）で作っていたため、
  2件目以降のクリックで見出しは「項目を編集」なのに中身は新規作成の既定値のまま、という
  不具合だった。`open` が false→true になるたびに `item`/`initial` から作り直すよう修正。
  表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **この開発セッションは実ブラウザでの PC/スマホ確認・実 DB での保存確認を行っておらず、
  この不具合を再現する自動テストも追加していない**（PR本文の検証チェックリストにも
  未実施のまま記載済み）。typecheck:all・lint（warning 58件、着手前と同じ）・
  `npm run build -w client-techops`・`npm run test -w shared`（1454件）は通過している
- **#403（スケジュール表の時間刻みを選択式にし既定を15分にした・2026-08-24）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 07:08:36 /
  CI（`checks`/`build`）両方 green 07:10:25 / マージ 07:11:13（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約48秒でマージされており、
  #159・#239・#241 と同じ「見る前に入った」形**。`npm run reviews:debt` は今回も401で
  使えず、GitHub MCP で直接確認した。ユーザーから「スケジュール表がいま5分刻みで
  非常に見づらい。15分単位くらいにしてほしい（単位を複数選べるように15/30/60分など）」
  と依頼され、DB既定値を5→15分に上げ・CHECK制約に60分を追加するmigrationと、
  スケジュール表詳細画面（PCグリッド表示時のみ）への「表示間隔」セレクタ新設で対応した。
  表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **この開発セッションは実ブラウザでのPC/スマホ確認を行っていない**
  （PR本文の検証チェックリストにも未実施のまま記載済み）。typecheck:all・lint・
  build:changed（client-techops/server）・実DB（`verify:up`）でのmigration適用確認・
  `npm run test -w shared`（1454件）は通過している。
- **#407（制作技術支援のトップ・一覧系画面のページ幅を案件管理と揃えた・2026-08-24）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 08:41:24 /
  CI（`checks`/`build`）両方 green 08:43:24 / マージ 08:44:41（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約1分17秒でマージ**しており、
  #401・#402・#403 と同じ「見る前に入った」形。`npm run reviews:debt` は今回も 401 で
  使えなかったため GitHub MCP で直接確認した。ユーザーからの指摘（「制作技術支援関連の
  ページ幅がほかの案件管理アプリなどと比較して幅が狭い気がしている」）に対応。
  `ProductionTopPage.tsx`・`TopPage.tsx`・`JourneyPage.tsx`・`schedule/ScheduleListPage.tsx`
  のルート要素が揃って `mx-auto max-w-4xl`（896px）で幅を絞っており、案件管理側
  （`ProjectListPage.tsx` 等）は max-width 指定なしでシェル幅いっぱいに広がる設計との
  食い違いが原因。4画面から `mx-auto max-w-4xl` を外して揃えた。本番中に使う4画面
  （`OnAirPage.tsx`・`RundownPage.tsx`・`PrompterPage.tsx`・`AudioSupportPage.tsx`）と、
  同じパターンを持つ計時・視聴者の運用画面（`LiveDashboardPage.tsx`・
  `LiveTimerAdminPage.tsx`）は、ユーザーへの事前確認で対象範囲として明示しなかったため
  対象外とした。表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **この開発セッションは実ブラウザでのPC/スマホ確認を行っていない**
  （PR本文の検証チェックリストにも未実施のまま記載済み）。
  `npx tsc -b client-techops`・`npm run lint`（エラー0・警告は着手前と同数）・
  `npm run test`（shared の Vitest・1454件）は通過している。
  なお本 PR は、並行していた別セッションのリリース PR（#406・`release: v4.4.2`）が
  7秒早くマージされたため、`docs/changelog.d/` 経由の通常のリリースノート収集
  （`npm run release:notes`）には乗らなかった。ユーザーの明示的な指示
  （「v4.4.2 のリリースにマージ」）により、`CLAUDE.md`/`README.md` の v4.4.2 の
  エントリへ直接追記して揃えた（別PR・後述）。
- **#409（techopsスケジュール表グリッドの誤クリックを修正・2026-08-24）** —
  `get_reviews`／`get_comments` とも0件のまま、作成 09:32:17 / CI（`checks`/`build`）
  両方 green 09:34:23 / マージ 09:53:40（terai-takehiro 本人が手動マージ・`merged_by` で
  確認）。**CI green から約19分17秒待っており、#244・#262・#270・#320 と同じ
  「見る前に入った」形ではない、原因不明のまま0件だったケース**。利用者からの指摘
  （「クリック時に別のスケジュールが開くことが度々ある」・PCグリッドの項目クリックで発生）
  に対応。重なる項目を横に割って並べる `assignLanes`（`scheduleLanes.ts`）が、
  コード内のコメントに反して「重なりグループごと」ではなく「表全体を通した」最大同時
  レーン数を全項目へ一律適用していたため、無関係な時間帯の単独項目まで幅1/3等に押し
  縮められ、隣の空白域を誤クリックしやすくなっていた。重なりが途切れたところで
  グループを区切り、レーンの空き状況・最大レーン数をグループごとにリセットするよう
  修正。表に移す指摘はない（レビュー自体が届いていないため）。手元の Node スクリプトで
  アルゴリズムの挙動（無関係な単独項目が1/3幅にならないこと・複数の重なりグループが
  互いに影響しないこと）を検算済みだが、⚠️ **実ブラウザでのクリック再現はこの開発
  セッションから行っていない**（PR本文に明記済み）。
- **#412（レスターの価格・ジャンル取得を修正し、機材管理にレンタル機材検索を新設・
  2026-08-24）** — `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、
  作成 11:27:27 / CI（`checks`/`build`）両方 green 11:29:44 / マージ 11:32:40
  （terai-takehiro 本人が手動マージ・`merged_by` で確認）。**CI green から約2分56秒で
  マージ**しており、#401・#402・#407 と同じ「見る前に入った」形。`npm run reviews:debt`
  は今回も401で使えず、GitHub MCP で直接確認した。ユーザーからの指摘（「レンタル費用が
  かなり取得できていない」「レスターはジャンル分けが効いていない」「レンタル機材検索
  機能だけ機材管理の新規項目に入れたい」）に対応。レスターの価格正規表現が「￥12,000-
  (税込)」の1パターンにしか一致せず取れなかったこと自体もログに残らなかった不具合、
  カテゴリがパンくずの機械的な最終リンク採用のため実質1カテゴリに潰れていた不具合を
  それぞれフォールバック＋診断ログで直し、機材管理に検索専用の新画面
  （`/equipment/rental-search`）を追加した。表に移す指摘はない（レビュー自体が
  届いていないため）。
  ⚠️ **レスター/TOC 側の修正は、開発セッションのサンドボックスが対象2社サイトへ外部接続
  できないため実HTML構造で検証できていない**（PR本文に明記済み・rental-scraper/README.md
  にも既知の制約として追記済み）。デプロイ後のログ（`価格取得不可`／`カテゴリはパンくずの
  フォールバックを使用`）の確認が必要。機材管理の新画面側は `npm run verify:up` の実DB上で
  検索・詳細・取得状況の3エンドポイントと権限ゲート（`equipment` editor→200、`sales`
  editor・無権限→403、未認証→401）を実際に確認済み。実ブラウザでのPC/スマホ確認は
  未実施（PR本文の検証チェックリストにも未実施のまま記載済み）。
- **#415〜#424（多エージェント監査の修正・10本・2026-08-24）** — `get_reviews`／
  `get_review_comments` とも全PRで0件のまま、いずれも作成から数分でCI（`checks`/`build`）
  green・terai-takehiro 本人が手動マージ（`merged_by` で確認。#415〜#421 は16:34〜16:35台に
  連続マージ、#422〜#424 は16:51台に連続マージ）。ユーザー指摘「こういったロジックミスが
  多発しています。アプリ全体をマルチエージェントで総点検してください」を受けて実施した
  監査（[`docs/reviews/2026-08-24-logic-audit.md`](2026-08-24-logic-audit.md)、10領域・
  確定25件）の修正フェーズ。10領域を並行する Workflow（各領域が独立 worktree・ブランチ・
  検証・PR作成まで自走）で一括対応した:
  - #415 財務（Excel取込・入金記録後の一覧が古いまま・4件）
  - #416 プラットフォーム（ダッシュボード「期限が過ぎたやること」の行き先ズレ）
  - #417 カレンダー（仮押さえ一覧のinvalidate漏れ・営業時間外予約の未通知・香盤その他タブ）
  - #418 GPM（週次KPIのリンク先が絞り込みを解釈しない）
  - #419 日常業務（ホームタイルの件数と遷移先の絞り込み不一致）
  - #420 機材管理（書き込み系ルートの権限チェック漏れ・invalidate漏れ）
  - #421 制作技術支援（ハブ画面のアクセス制御漏れ・一覧/ハブの更新漏れ）
  - #422 計時LIVE（**P1**: 計測中の番組を削除すると計測ロックが永久に解放されない／
    タイマー状態APIのキー名の割れ）
  - #423 共通ライブラリ（`AppSwitcher` が権限・hidden・frozenを見ない。唯一の呼び出し元が
    廃止済み`client-awards`のため実害は限定的と明記の上で修正）
  - #424 案件管理（グループ仕入・見積タブ・タスク・営業活動記録の invalidate漏れ×2／
    権限出し分け漏れ×2／件数と表示行数の不一致）
  - ⚠️ **#424（sales-core）だけはWorkflow内で「API Error: Connection lost mid-response」
    により自動化が失敗**（`agents_error: 1`）。ただし該当エージェントの worktree には
    5件すべての修正・changelog下書きが未コミットのまま残っていたため、内容を通しで
    確認（各ファイルの差分が指摘の再現条件と一致することを確認）した上で、検証
    （`npx tsc -b client`／`npm run test`／`npm run lint`）・コミット・push・PR作成を
    セッション本体が引き継いで完了させた
  - 全PRとも実ブラウザ・実DBでの動作確認はこのセッションから未実施（各PR本文に明記済み）。
    表に移す指摘はない（レビュー自体が届いていないため）
- **#429（見積のアーカイブ・請求書Excel出力・財務一覧の案件リンクを直した・2026-08-25）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 18:24:33 JST /
  CI（`checks`/`build`）両方 green 18:26:35 JST / マージ 18:54:32 JST（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約27分57秒でマージ**しており、#412・
  #415〜#424 と同じ「見る前に入った」形。`npm run reviews:debt` は今回も401で使えず、
  GitHub MCP で直接確認した。ユーザー指摘「【ONAiR 反映されていない内容】見積の過去
  バージョンをアーカイブ化したい／請求書のExcelが出力できなくなっている／財務管理の
  一覧に以前あったリンクボタンが無い」の3件にマルチエージェント（サブエージェント3体を
  並行起動）で対応した。①見積アーカイブは `estimates.archived_at`（migration 236・新規
  機能）②請求書Excelはv4でレガシー全機能コンソールを新画面に置き換えた際に4つ目の
  ボタンだけ復元し忘れていた欠落の復元（案件詳細の売上・請求ペイン／見積・請求（全案件）
  ／財務②締め処理の3画面）③財務一覧のリンクボタンは共通行部品 `LedgerRows.tsx` の
  コメントが「行を押すと案件が開く」と言いながら実装は編集権限者には効いていなかった
  欠陥の修正。表に移す指摘はない（レビュー自体が届いていないため）。
  実DB・実サーバー（`npm run verify:up`）でAPI経由の一連の動作（アーカイブの作成→
  次の版→アーカイブ→一覧から消える→表示切替→解除・Excel出力の200＋有効なxlsx・
  権限のない利用者で403）は確認済みだが、⚠️ **実ブラウザでのクリック確認はこの開発
  セッションから行っていない**（PR本文に明記済み・`npm run verify:ui` も未実施）。
- **#432（制作技術支援で独自作成の番組の計時タイマーが表示されない不具合を直した・2026-08-26）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 00:01:25 JST /
  CI（`checks`/`build`）両方 green 00:03:15 JST / マージ 00:04:52 JST（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約1分37秒でマージ**しており、#412・
  #415〜#424・#429 と同じ「見る前に入った」形。`npm run reviews:debt` は今回も401で使えず、
  GitHub MCP で直接確認した。ユーザー指摘「制作技術支援で独自に番組作成をした際に計時
  タイマーのアプリが表示されない」に対応。原因は `liveops_programs.project_id` が
  `projects` テーブルのみを参照するFKで、案件に紐づかない「独自作成の番組」
  （`qsheet_programs`）に対応する列が存在しなかったこと（`12-live-timer-decision.md` §3-5
  に「既知の空白」として明記済みだった）。`liveops_programs.qsheet_program_id`
  （migration 237・`project_id` とは同時に持たないCHECK）を追加し、対になる
  `resolve-by-program` エンドポイント・`useLiveProgram.ts`・`MiniAppTiles.tsx` の
  scope分岐を対応させた。表に移す指摘はない（レビュー自体が届いていないため）。
  実DB（`npm run verify:up`）で番組作成→owner解決→`resolve-by-program`→冪等性・
  CHECK制約・ユニーク制約・権限ゲート（reader可・初回作成はmanager必須）まで確認済み
  だが、⚠️ **実ブラウザでのクリック確認はこの開発セッションから行っていない**
  （PR本文に明記済み）。
- **#433（廃止したリアルタイムCGをURLアクセス可能に戻し、デモ用ダミーデータを投入・
  2026-08-26）** — `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、
  作成 00:01:46 JST / CI（`checks`/`build`）両方 green 00:04:26 JST / マージ 00:05:04 JST
  （terai-takehiro 本人が手動マージ・`merged_by` で確認）。**CI green から約38秒でマージ**
  しており、#429 までと同じ「見る前に入った」形。ユーザー指摘「クローズしたリアルタイム
  CGですがURLを叩けばアクセスできるようにしてもらえますか」を受け、2026-08〜「廃止」
  （サーバー配信・API・Socket.IO・ビルド対象・入口をすべて外した状態）にしていた
  `client-awards` を、`docs/v4-plan.md` の用語でいう一段手前の「凍結」（URLは生かすが
  トップページ・アプリ切替・左メニューには出さない）へ戻した。復活の範囲（URLのみ・
  ホームタイル非表示）は事前に `AskUserQuestion` でユーザーに確認済み。続くユーザー要望
  「PRの前にデモンストレーションが可能な完全なダミーデータをある程度のボリュームで
  格納しておいてほしい」を受け、`server/src/shared/db/seed-awards.ts`（新規）でイベント
  3件・カテゴリ9本・エントリ37件・クイズ2問を投入する仕組みも同PRに追加した。表に移す
  指摘はない（レビュー自体が届いていないため）。
  実サーバー（検証用Postgres）を起動して `seed-awards.ts` の投入・再実行時のスキップ
  （冪等性）、`GET /awards/events`・`/events/:id`・`/events/:id/cg-status`
  （`ranking.live=true`・`oneshot.live=true`を確認）・認証なしの公開エンドポイント
  `/events/:id/output`、`/awards`・`/awards/output/1` の200応答までは確認済みだが、
  ⚠️ **実ブラウザでの操作確認（CGコックピット・出力画面の見た目、PC/スマホとも）は
  この開発セッションから行っていない**（PR本文に明記済み）。
- **#437（内覧会 来場予約の MCP ツールに update_inview_attendee を追加・2026-08-26）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 09:56:39 JST /
  CI（`checks`/`build`）両方 green 09:58:27 JST / マージ 10:01:56 JST（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約3分29秒でマージ**しており、#429・#432・
  #433 と同じ「見る前に入った」形。`npm run reviews:debt` は今回も401で使えず、GitHub MCP
  で直接確認した。ユーザー指摘「ONAiRの内覧会関連ツールは『新規登録
  （register_inview_attendee）』と『一覧参照（list）』のみで、既存レコードを更新・編集する
  ツールを追加」に対応。HTTP 側 (`inview.routes.ts`) が既に使っていた `inviewService.update`
  （部分更新・session_label 変更時の日付/時間帯/対象の再抽出・companions の氏名突き合わせに
  よる受付記録の引き継ぎに対応済み）をそのまま呼ぶ薄いラッパーとして MCP ツールを新設し、
  権限ゲート（`dailyops` editor 以上）にも登録した。DB マイグレーション・画面変更なし。
  表に移す指摘はない（レビュー自体が届いていないため）。
  検証は `npm run typecheck` / `npm run lint` / `npm run build` / `npm run test`（1465件）を
  確認済みだが、⚠️ **実DB・実サーバーでの動作確認（MCP経由での呼び出し）はこの開発セッションから
  行っていない**（PR本文に明記済み）。
- **#439（廃止したリアルタイムCGのデモ用ダミーデータにダミー顔写真を追加・2026-08-26）** —
  `get_reviews`／`get_review_comments`／`get_comments` とも0件のまま、作成 10:26:11 JST /
  CI（`checks`/`build`）両方 green 10:28:17 JST / マージ 10:29:06 JST（terai-takehiro 本人が
  手動マージ・`merged_by` で確認）。**CI green から約49秒でマージ**しており、#429・#432・
  #433・#437 と同じ「見る前に入った」形。GitHub MCP の `pull_request_read` で直接確認した
  （`npm run reviews:debt` は未実行）。ユーザー要望「運用の際に画像部分にダミーの顔写真を
  入れられるようにしたい」に対応。ヒアリングの結果、使う場面はデモ用シードデータのみ・
  画像はイラスト調のアバター（実在の人物写真ではない）でよいと確認したうえで、
  `server/src/shared/db/seed-awards.ts` が投入する37エントリ全件に、コード生成した
  イラスト調アバターSVG（16種類使い回し）を `photo_url` として割り当てた。DB マイグレーション
  なし・画面変更なし。表に移す指摘はない（レビュー自体が届いていないため）。
  検証は `npm run typecheck` / `npm run lint` / `npm run test`（1465件）に加え、実サーバー
  （検証用Postgres、フレッシュな状態から）で `db:seed:awards` の投入・冪等性・画像配信
  （`GET /api/v1/internal/awards/images/dummy-avatar-01.svg` が200）・公開エンドポイントの
  `photo_url` を確認し、生成画像をブラウザで目視確認済み。
- **#442（案件・タスク・AI・カレンダーの根源整理 Phase 1・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 00:11:02 JST / CI（`checks`/`build`）両方 green
  00:13:05 JST / マージ 00:28:24 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約15分19秒でマージ**しており、見る時間はあったが0件だった（#270・#320 に
  近い形）。GitHub MCP の `pull_request_read` で直接確認した（`npm run reviews:debt` は未実行）。
  ユーザー指摘「『お待たせ中』という構造を含めこの辺りのロジック・システムが意味不明。
  案件自動整理も不要判定ができない仕組みで常にゴミが溜まり続ける。案件管理のみならず
  AI連携・タスク管理・ユーザー同士のタスク共有（カレンダー連携含む）という根源的な部分を
  シンプルに整理してほしい」を受け、5体の調査エージェント（案件ステージ/お待たせ中/自動整理の
  ロジック・案件管理UI全画面・タスク管理4系統の実態・AI連携の全接点・他社13ツールのディープ
  リサーチ）→設計文書（`docs/core-redesign-plan.md`）→7体の実装・検証エージェント（Server3・
  Client3・Verify1）という Workflow で対応した。マイグレーション238/239（`snooze_until`・
  失注理由2種・通知ひな形4本）追加。表に移す指摘はない（レビュー自体が届いていないため）。
  検証は `npm run typecheck:all` / `npm run lint` / `npm run test`（1496件）に加え、検証用
  Postgres でマイグレーションの適用・冪等性、実サーバーで健全性導出7ケース・スヌーズの
  設定/解除/過去日拒否・自動見送り/完了繰り上げの履歴・deadlines APIのCOALESCEを実測確認済み。
  ⚠️ **実ブラウザでの操作確認（受信箱の節分け・健全性バッジ・要整理ビュー・スヌーズダイアログ・
  カレンダーのタスク期限レイヤー、375px含む）はこの開発セッションから行っていない**
  （PR本文に明記済み・検証環境での確認を推奨）。
- **#443（PR テンプレートの凍結アプリ欄を実態に合わせた・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 00:24:57 JST / CI（`checks`/`build`）両方 green
  00:27:00 JST / マージ 00:28:37 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約1分37秒でマージ**しており、#433・#437・#439 と同じ「見る前に入った」形。
  GitHub MCP の `pull_request_read` で直接確認した（`npm run reviews:debt` は未実行）。
  ユーザー指摘「client-techops/ は凍結ではありませんし、計時LIVEはアプリマージされて機能は
  もう復活しているので、Readme含めたドキュメント記載やルールについて正しくないと思います」を
  受けて調査した結果、ルート `CLAUDE.md`・`shared/CLAUDE.md`・README.md は既に正しく
  （制作技術支援=凍結解除中・計時LIVE=対象・凍結中はリアルタイムCGのみ）、ずれていたのは
  `.github/pull_request_template.md` の「凍結アプリ (Qシート / 計時LIVE / リアルタイムCG)」
  節だけ（v4.0.0着手当初・3アプリとも凍結だった頃の記載が残っていた）と判明。節見出しを
  リアルタイムCGだけに絞り、制作技術支援・計時・視聴者は「v4.0.0の対象」チェックリストへ
  移した（それぞれの残作業・例外を1行で明記）。#442 とは無関係な独立の文書修正のため別PRに
  分離。DBマイグレーション・画面変更なし。表に移す指摘はない（レビュー自体が届いていない
  ため）。検証は `npm run lint`（warning数58件で着手前と同数）のみ（文書のみの変更のため
  typecheck/build/実DB/実ブラウザは対象外・PR本文に明記済み）。
- **#444（docs(reviews): PR #442・#443 のマージ後の棚卸しを記録・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 00:31:35 JST / CI（`checks`/`build`）両方 green
  00:33:40 JST / マージ 01:45:39 JST 頃（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  CI green から約1時間12分と待つ時間はあったが0件だった。棚卸し記録そのもののPR（コード変更
  なし）。表に移す指摘はない（レビュー自体が届いていないため）。
- **#445（案件・タスク・AI・カレンダーの根源整理 Phase 2・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 01:46:45 JST / CI（`checks`/`build`）両方 green
  01:48:57 JST / マージ 01:57:12 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約8分15秒でマージ**。GitHub MCP の `pull_request_read` で直接確認した
  （`npm run reviews:debt` は未実行）。Phase 1（#442）に続く Phase 2 で、8体の実装・検証
  エージェント（Server3・Client4・Verify1）の Workflow により、①「今日の営業」カード
  （`GET /dashboard/today-sales`・旧「期限が過ぎたやること」パネルを吸収して削除）
  ②AI活動ページ（`/settings/ai-activity`）＋営業側月次AIレビュー（`ai_review_sales`・
  manager通知・確認打刻）③デイリーニュース★評価のAIループ接続（`ops_news_item`）
  ④AIナレッジ承認UI（`/techops/ai-knowledge`）⑤依頼のコメントスレッド（`task_comments`・
  description追記の廃止）⑥標準工程/議事録タスクの担当割当 ⑦タスク期限の個人ICSフィード
  ⑧prompt_version 5ツール展開・GPM一覧health化 を実装（migration 240/241）。
  Phase 2 リストの「保留（d_hold）廃止」はユーザー決定で見送り（実態は仮押さえ＝顧客側の
  事実・経緯は `docs/core-redesign-plan.md`）。表に移す指摘はない（レビュー自体が届いて
  いないため）。検証は typecheck:all / lint / test 1519件 / build:changed に加え、検証用
  Postgres で migration 240/241 の2回適用（冪等）、実サーバーで today-sales・ai-activity
  4口（qsheet系kind 400・権限なし403）・コメントの当事者403と相手方通知・ICS配信
  （text/calendar・不正トークン404）・月次レビューの冪等 upsert を実測済み。
  ⚠️ **実ブラウザでの操作確認（今日の営業カード・AI活動ページ・ナレッジ承認・コメント欄・
  カレンダー購読、375px含む）はこの開発セッションから行っていない**（PR本文に明記済み・
  検証環境での確認を推奨）。
- **#446（docs(reviews): PR #444・#445 のマージ後の棚卸しを記録・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 01:59:10 JST / CI（`checks`/`build`）両方 green
  02:01:28 JST / マージ 04:24:16 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約2時間22分48秒でマージ**しており、見る時間はあったが0件だった。棚卸し
  記録そのもののPR（コード変更なし）。表に移す指摘はない（レビュー自体が届いていない
  ため）。検証は `npm run lint`（warning数58件で着手前と同数）のみ。
- **#447（release: v4.5.0・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 04:43:14 JST / CI（`checks`/`build`）両方 green
  04:45:30 JST / マージ 04:46:26 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約56秒でマージ**しており、#433・#437・#439・#443 と同じ「見る前に入った」形。
  ユーザー指示「リリース準備を」を受けた版上げ（4.4.9→4.5.0）。`npm run release:notes -- 4.5.0`
  で `docs/changelog.d/` の下書き6件（#440・#442〜#446）を集約し、`package.json`・
  `CLAUDE.md`「現在のバージョン」・`README.md` の3か所と `docs/version-history.md` への
  アーカイブ移動を実施。中身は根源整理 Phase 1（#442）・Phase 2（#445）等、本流にマージ済み
  の変更のみで、このPR自体は版の表記のみ（migration・画面変更なし）。表に移す指摘はない
  （レビュー自体が届いていないため）。検証は `npm run check:version`（3か所一致）・
  `RELEASE=1 npm run lint`（warning 58件・着手前と同数）・`npm run test`（1519件全パス）・
  `node scripts/generate-version-history.mjs`（477件、書式崩れなし）を確認済み。
  **本番へは出ていない** — 本番デプロイはユーザーが GitHub Releases でタグ `v4.5.0` を
  Publish したときのみ（このPRのマージでは検証環境にしか出ない）。
- **#448（fix(shared): PageHeader のタイトルが操作ボタンの多い画面で1文字ずつ潰れるのを
  直した・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 05:12:04 JST / CI（`checks`/`build`）両方 green
  05:14:37 JST / マージ 05:19:07 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約4分30秒でマージ**。ユーザー報告「レイアウト崩れてるね」（案件を直す画面の
  見出しが1文字ずつ縦に潰れているスクリーンショット）を受け、Playwright で実ブラウザ・
  DOM 実測して原因を特定: `PageHeader` の見出しラッパーが `flex-1`（`flex-basis: 0%`）
  だったため、flex-wrap の折り返し計算で見出しの幅が0と数えられ、補助操作ボタンが9個ある
  「案件を直す」画面で見出しに残り幅しか渡らなかった。`flex-auto`（basis = 内容の幅）に
  変更し、入り切らないときは補助操作側が下の行へ折り返るようにした。`shared/` の共通部品
  のため配信中5アプリ全部に影響。1440px/1100px/375px で `/sales/projects/:id/edit`
  （補助操作9個・最悪ケース）を実測し、修正後は見出しが1行で正しく表示されることを確認。
  `/sales/projects`（一覧）・`/sales/dashboard` への回帰なしも確認済み。表に移す指摘はない
  （レビュー自体が届いていないため）。検証は `npm run typecheck` / `npm run lint` に加え、
  実ブラウザでの DOM 実測（複数ビューポート）を実施。
- **#449（fix(client): 財務②請求・入金のGLS番号が長いと案件名に重なる不具合を直した・
  2026-08-27）** —
  `get_review_comments` 0件のまま、作成 05:30:34 JST / CI（`checks`/`build`）両方 green
  05:32:41 JST / マージ 05:53:35 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約20分54秒でマージ**。ユーザー報告「GLS番号 レイアウト崩れ」（請求・入金
  画面でGLS番号が案件名に重なるスクリーンショット）を受け、Playwright で実ブラウザ・DOM
  実測して原因を特定: `ClosingRows.tsx` のGLS番号列（`RowSlot w={96}`）がチェックボックス/
  鍵アイコン＋番号を `flex` でまとめているが `min-w-0` が無く、内側の `truncate` 指定が
  効かないまま `GLS-A010-2608` のような長い値が自然幅（実測127px）で描画され、96px の枠を
  31px超えて隣の案件名列に重なっていた。`min-w-0`／`min-w-0 flex-1` を追加して修正
  （修正後は wrapperSpan が正確に96px・numberSpan 70px・隣列と非重複を DOM で実測確認）。
  同型の不具合が姉妹画面 `InvoiceRows.tsx`（見積・請求画面の同種列）にも存在すると分かり、
  同じPRでまとめて修正した。表に移す指摘はない（レビュー自体が届いていないため）。検証は
  `npm run typecheck` / `npm run lint` に加え、実ブラウザでの DOM 実測（修正前後の幅・
  重なりの有無）を実施。
- **#450（fix(finance): 受け取った書類から見積書を除外（台帳に入らないため）・
  2026-08-27）** —
  `get_review_comments` 0件のまま、作成 05:40:58 JST / CI（`checks`/`build`）両方 green
  05:43:11 JST / マージ 05:53:48 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約10分37秒でマージ**。ユーザー指摘「見積書は除外したい 実際に台帳に入れる
  のは請求書になるので」を受け、`finance_docs` の一覧（`inbox.service.ts` の
  `list()`/`pendingCount()`）とホーム受信箱（`dashboard.routes.ts` の `FINANCE_DOC_BASE`）
  から `doc_type='quote'`（見積書）を既定で除外し、台帳への受け渡し境界
  （`doc-handoff.service.ts` の `handoffDoc()`）でも二重に弾くようにした。見積書は
  ワークフローを最後までたどれない（承認しても「台帳に入れる」の先が無い）ため画面から
  外したが、記録自体は消さず `doc_type=quote` を明示すれば読める（MCP の
  `record_finance_doc` 説明文にも注記追加）。`DocumentsPage.tsx` の見出し・空状態の文言も
  「見積書・請求書・注文書」→「請求書・注文書」に更新。表に移す指摘はない（レビュー自体が
  届いていないため）。検証は `npm run typecheck --workspace=server` / `npm run lint` を
  確認済み。
- **#451（docs(reviews): PR #446・#447・#448・#449・#450 のマージ後の棚卸しを記録・
  2026-08-27）** —
  `get_review_comments` 0件のまま、作成 06:02:19 JST / CI（`checks`/`build`）両方 green
  06:04:40 JST / マージ 06:09:42 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約5分2秒でマージ**。棚卸し記録そのもののPR（コード変更なし。#446・#447・
  #448・#449・#450 の5PR分をまとめて記録）。表に移す指摘はない（レビュー自体が届いていない
  ため）。検証は `npm run lint`（warning数58件で着手前と同数）のみ。
- **#452（release: v4.5.1・2026-08-27）** —
  `get_review_comments` 0件のまま、作成 06:04:43 JST / CI（`checks`/`build`）両方 green
  06:06:49 JST / マージ 06:09:52 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。
  **CI green から約3分3秒でマージ**。ユーザー指示「0.0.1バージョン上げでリリース」を受けた
  パッチ版上げ（4.5.0→4.5.1）。`npm run release:notes -- 4.5.1` で `docs/changelog.d/` の
  下書き3件（#448〈PageHeaderの見出し潰れ修正〉・#449〈GLS番号重なり修正〉・#450〈見積書
  除外〉）を集約し、`package.json`・`CLAUDE.md`「現在のバージョン」・`README.md` の3か所と
  `docs/version-history.md` への v4.4.8 アーカイブ移動を実施。中身はいずれも本流にマージ済み
  の不具合修正のみで、新機能・DBマイグレーションを含まない。#451（棚卸し記録PR）はこの
  リリースの時点で未マージだったため今回のリリースには含まれず、次リリースへ繰り越し。
  表に移す指摘はない（レビュー自体が届いていないため）。検証は `npm run check:version`
  （3か所一致）・`RELEASE=1 npm run lint`（warning 58件・着手前と同数）・`npm run test`
  （1519件全パス）・`node scripts/generate-version-history.mjs`（478件、書式崩れなし）を
  確認済み。**本番へは出ていない** — 本番デプロイはユーザーが GitHub Releases でタグ
  `v4.5.1` を Publish したときのみ。
- **#456（fix(calendar): 短い予約の可読性・ドラッグ登録/延長・関連バグを直した・2026-08-27）** —
  `get_reviews` 0件のまま、作成 17:23:02 JST / CI（`checks`/`build`）両方 green 17:25:21 JST /
  マージ 17:26:50 JST（terai-takehiro 本人が手動マージ・`merged_by` で確認）。**CI green から
  約1分29秒でマージ**（作成から約3分48秒）。ユーザー報告「カレンダーですが10分など短い時間の
  予約だと何書いてあるかわからない。UIで直感的にスケジュールの延長や登録ができるように
  してほしい。バグもチェック」を受け、マルチエージェントで週表・データ取得・各ビューを
  並列に読ませてバグを洗い出し、候補は別エージェントが机上トレースで反証検証してから直した。
  ① 週表の札の最低高さ不足（10分予約が1行も入らない）と、底上げが列割りに反映されず短い
  予約どうしが重なるバグを修正。② 空きマスをなぞって新規登録・札の下端を引いて延長できる
  操作を新設（`useGridDrag.ts`/`useCalendarEdit.ts`）。③ 香盤の日またぎ誤描画（時刻部分しか
  見ておらず翌日側で誤った時刻に描かれる）・同室同時間帯の重複予約が完全に重なって押せない
  バグを修正（`koubanLayout.ts` に純関数を切り出し、`placeDay` と同じ列分けを導入）。
  ④ 月表「他N件」が押しても何も起きなかったのを週表へ切り替えるようにし、日マスの
  `<button>` 入れ子（HTML違反・Space キー無反応）も修正。⑤ 3つの予約・予定ダイアログで
  深夜0時〜朝9時(JST)に開くと `toISOString` の影響で既定日が前日になるバグを `localDateStr`
  に統一。⑥ サーバー3系統（スタジオ予約・自分の予定・パートナーの予定）とも「終了が開始
  より前」の検証が無く、新規作成だけでなく部分更新（週表の下端ドラッグ延長のような
  `end_time` だけの更新）でも不正な時刻が保存できてしまう穴を `isReversedTimeRange` で
  塞いだ。表に移す指摘はない（レビュー自体が届いていないため）。検証は
  `shared/tests/`（calendarLayout 12件・koubanLayout 8件・timeRange 4件・format 3件を追加、
  合計1550件）・`npx tsc -b client`/`npx tsc -b`（server）・`npm run lint`（warning 58件で
  着手前と同数）・`npm run build`・`BASE=... node scripts/verify-ui.mjs カレンダー`
  （123/123通過）に加え、実ブラウザ（Playwright + 検証用Postgres + 実サーバー）で
  短い予約の1行表示・香盤の重複列分け・日またぎの正しい時刻表示・月表「他N件」の週表切替・
  キーボード操作・3系統の時刻逆転が作成/部分更新とも400で拒否されること・権限なし利用者
  (`v-none`) で403/非表示になることをDOM実測・API呼び出しで確認済み。
- **#457（docs(reviews): PR #456 のマージ後の棚卸しを記録した・2026-08-27）** —
  `get_reviews` 0件のまま、作成 08:31:14 UTC / CI `build` green 08:31:40 UTC /
  マージ 08:32:58 UTC（terai-takehiro 本人が手動マージ）。**`checks` ジョブの完了（08:33:30
  UTC）を待たずにマージされている**（`build` は間に合っていたが `checks` は完了前）。
  `docs/reviews/codex-findings-v4.md` への追記のみ（コード変更なし）。表に移す指摘はない
  （レビュー自体が届いていないため）。
- **#458（refactor(sales): 案件台帳(projects)の項目整理とバグ修正・2026-08-27）** —
  `get_reviews`/`get_review_comments`/`get_comments` いずれも0件のまま、作成 08:32:32 UTC /
  CI（`checks`/`build`）両方 green 08:34:55 UTC / マージ 08:36:13 UTC（terai-takehiro 本人が
  手動マージ）。**CI green から約1分18秒でマージ**（作成から約3分41秒）。ユーザー指摘
  「会場・スタジオの表記が揺らいでいる。案件台帳のDB項目が拡張し続けてぐちゃぐちゃ」を
  受け、`projects` 全57列を実測で棚卸しし、Phase A（死んだ列7本削除・実バグ5件修正）→
  Phase B（ユーザー判断で死んだ列3本を追加削除・`assigned_to` 方針文書を書き直し）→
  Phase C 一部（受注/失注日時の書き口を `recordStageTransition()` に集約し GPM の
  `lost_at` 欠落バグを修正・`create()` を `createCore` に分割し投入口の `customer_type`
  固定バグを修正）の3段階を1本のPRにまとめた。金額の集計統一・実施日のDATE型化は
  本番データの事前監査が必須のため、このPRには含めず [docs/project-ledger-phase-c-design.md](../project-ledger-phase-c-design.md)
  に未実装として明記した。表に移す指摘はない（レビュー自体が届いていないため）。検証は
  `npm run typecheck:all`・`npm run lint`（warning 58件で着手前と同数）・`npm run test`
  （1522件）・migration 242/243 の適用と冪等性（`check-migration-numbers.mjs` でOK）を確認済み。
  実サーバー（検証用Postgres）で案件の作成/更新/ステージ変更・台帳一括編集のガード・GPMの
  受注/失注（`lost_at` 修正の実地確認込み）・投入口の `customer_type` 修正・ダッシュボード/
  週報への `estimate_amount` 追加を確認済みだが、**実ブラウザでのPC/スマホ確認は行っていない**
  （ほぼ全てサーバー・データモデル側の変更。Phase Bで削除した2〜3個のUI要素
  [ロゴ許諾スイッチ・タグ節・台帳一括編集の対応項目] は目視確認していないことをPR本文に明記済み）。
- **#459（docs(reviews): PR #457・#458 のマージ後の棚卸しを記録した・2026-08-27）** —
  `get_reviews` 0件のまま、作成 08:39:02 UTC / マージ 08:41:45 UTC（terai-takehiro 本人が
  手動マージ、約2分43秒後）。`docs/reviews/codex-findings-v4.md` への追記のみ（コード変更なし）。
  表に移す指摘はない（レビュー自体が届いていないため）。
- **#460→#461（release: v4.5.2・2026-08-27）** — ユーザー依頼「ここまでまとめてリリースしたい」を
  受け、`npm run release:notes -- 4.5.2` で版の3か所（`package.json`/`CLAUDE.md`/`README.md`）を
  更新し `docs/changelog.d/` 7件を集約する版上げ専用PR。最初 `claude/release-prep-lod7ck` 枝で
  #460 として出したところ、`check-changelog.mjs` のリリース判定（`RELEASE=1` か枝名が `release/`
  で始まること）に外れ `checks` が落ちた（CI は `RELEASE=1` を設定しないため、枝名で判定するしか
  ない — `docs/branching.md` の正式な運用どおり）。ユーザーに確認のうえ同一コミットを
  `release/4.5.2` 枝に出し直し、#461 として再オープン（#460 はクローズ、マージなし）。#461 は
  `get_reviews`/`get_review_comments` とも0件のまま、作成 08:44:48 UTC / CI（`checks`/`build`）
  両方 green 08:47:17 UTC / マージ 08:48:29 UTC（terai-takehiro 本人が手動マージ、CI green から
  約1分12秒後）。表に移す指摘はない（レビュー自体が届いていないため）。検証は
  `npm run typecheck:all`・`RELEASE=1 npm run lint`（エラー0・warning は着手前と同数）・
  `npm run test`（1553件）・`npm run check:version`（3か所一致）を確認済み。**本番へは
  出ていない** — 本番デプロイはユーザーが GitHub Releases でタグ `v4.5.2` を Publish
  したときのみ。
- **#463（feat(calendar): 複数経路からの重複予約を表記揺らぎごと検知できるようにした・
  2026-08-27）** — ユーザー報告「複数の経路からの予約により重複した内容のカレンダーが
  貼り付くことが多々あり、表記揺らぎがあったりするので工夫を」に対応。作成 09:22:01 UTC /
  CI（`checks`/`build`）両方 green 09:24:38 UTC（**作成から約2分37秒**）/ マージ 11:28:03 UTC
  （terai-takehiro 本人が手動マージ）。**CI green からマージまで約2時間3分**空いており、
  #460以前に多い「見る前に入った」形ではない — `get_reviews`・`get_review_comments` とも
  終始0件で、この間にレビューが届いた形跡もない（原因不明のまま0件だった #165・#167・#244・
  #258・#270 に近い）。表に移す指摘はない（レビュー自体が届いていないため）。この PR で
  意図して残した判断はコード側に明記済み: ①MCP `create_studio_booking` の説明に「重複していても
  保存は止まらない」旨を追記、②実ブラウザ（PC/スマホ）・検証用 Postgres 保存確認・
  403確認の3項目はこの開発セッションの環境制約（ブラウザ・Postgres起動不可）で未実施と
  PR本文に明記（`npm run typecheck`・`npm run lint`・`npm run build -w client -w server`・
  `npm run test`1497件は実施・緑）。マージ後の実ブラウザでの目視確認を推奨として残した。
- **#464（feat(gpm): プロジェクト詳細にガント・かんばんを追加し、MCP をガント編集に対応・
  2026-08-27）** — `get_reviews`/`get_review_comments`/`get_comments` いずれも0件のまま、
  作成 11:32:09 UTC / CI（`checks`/`build`）両方 green 11:34:23 UTC / マージ 11:41:44 UTC
  （terai-takehiro 本人が手動マージ）。**CI green から約7分21秒でマージ**（作成から約9分35秒）。
  ユーザー指摘「プロジェクト管理ってガントチャートどこにあるんですか？」「むしろガント
  チャート、カンバン管理が必要なのはプロジェクト管理ですよね。実装を。MCPもちゃんと
  整備してください（細部の編集にも対応できるよう）」を受け、GPM（`/gpm/*`）詳細の概要
  タブに見え方の切り替え（リスト／ガント／かんばん・PC のみ）を新設した。ガントは工程
  （`gpm_phases`）とタスクを1本の時間軸に出しドラッグで日程移動・端で期間変更、かんばんは
  工程を列にしてタスクをドラッグで付け替え — 案件側の `GanttView`/`KanbanView` は呼ばず、
  工程が主役の GPM 専用実装にした（列＝工程・時間軸＝工程とタスク）。MCP
  `create_gpm_task`/`update_gpm_task` にガント用フィールド（start_date/progress/
  is_milestone/work_state/sort_order）を追加し、従来「案件タスク側の update_task で」と
  していた迂回を解消。表に移す指摘はない（レビュー自体が届いていないため）。検証は
  `npm run typecheck:all`・`npm run lint`（warning 58件で着手前と同数）・`npm run test`
  （1553件）・`npm run build`（shared/client/server 個別ビルド）に加え、実サーバー
  （検証用 Postgres）+ Playwright（1440/375px）で工程バーのドラッグによる日付更新・
  かんばんのカードドラッグによる工程付け替えを API 実測、権限なし利用者（`v-none`・
  `gpm`/`sales` を持たない `v-keiri`）で 403 になることを確認済み。マイグレーションなし
  （既存の `progress`/`is_milestone`/`start_date` 列を読み書きに使うだけ）。
- **#466（docs(reviews): PR #464 のマージ後の棚卸しを記録した・2026-08-27）** —
  `get_reviews` 0件のまま、作成 11:46:33 UTC / CI（`checks`/`build`）両方 green
  11:48:39 UTC / マージ 13:02:23 UTC（terai-takehiro 本人が手動マージ、CI green から
  約1時間14分後）。`docs/reviews/codex-findings-v4.md` への追記のみ（コード変更なし）。
  表に移す指摘はない（レビュー自体が届いていないため）。
- **#469（fix(client): プロジェクト管理のスマホUIを直しガントを本格化した・2026-08-27）** —
  ユーザー報告「プロジェクト管理のアプリのUIがめちゃめちゃ崩れてる。スマホ版が特に」
  「ガンドチャートを含めプロジェクト管理に必要な機能も結局実装されていないじゃないですか」
  を受け、①GPM詳細のスマホヘッダー（タブとステージ切替が同じ行で重なる）・工程/タスク行の
  スマホ2行カード化、②PMツールのディープリサーチ（小規模工事チームに効くのはFS依存＋
  連鎖リスケ・遅延可視化・ズーム表示、CPM/EVM/フルベースラインは過剰と判断）に基づく
  ガント本格化（今日線が視野に入らず「実装されていない」ように見えていた根本原因を修正・
  ズーム日/週/月・依存関係の矢印・連鎖リスケの確認ダイアログ）とスマホ用縦タイムライン
  「工程表」の新設を行った。作成 22:42:40 UTC / CI（`checks`/`build`）両方 green
  22:44:52 UTC（**作成から約2分12秒**）/ マージ 23:01:37 UTC（terai-takehiro 本人が
  手動マージ、**CI green から約16分45秒後**）。`get_reviews`・`get_review_comments` とも
  終始0件（原因不明のまま0件だった #165・#167・#244・#258・#270・#463・#464 に近い）。
  表に移す指摘はない（レビュー自体が届いていないため）。マイグレーションなし（依存関係は
  既存 `task_dependencies` テーブル・APIをそのまま再利用）。検証は `npm run typecheck`・
  `npm run lint`（warning 58件・着手前と同数）・`npm run test`（1570件）・
  `npm run build` に加え、実サーバー（検証用Postgres）+ Playwrightで実施 —
  ガントのズーム3段・依存矢印の表示、連鎖リスケは実際にバーをドラッグ→確認ダイアログ→
  「ずらす」を押してDBの日付移動まで実測、スマホ375pxで横はみ出し0px を確認。
  ⚠️ **権限のない利用者での403確認は未実施**（表示・編集ロジックの改修のみで権限モデルは
  変更していないためPR本文に明記済み）。

- **#472（fix(ui): プロジェクト管理以外の全アプリのレイアウト崩れを実測で洗い出して直した・
  2026-08-29）** — ユーザー依頼「プロジェクト管理のUIUXは整えましたが、それ以外のアプリの
  UIUXについて先進的な業務管理ツールについてディープリサーチで研究し、反映を。PC、スマホ
  双方で現状レイアウト崩れ起こしているものも多数あり」を受け、全89画面を 375/768/1280px の
  実ブラウザで撮って7エリア並列で監査し、実測の裏付けがある崩れだけを直した（[A]33件・
  [B]4件を検出）。作成 09:56:40 UTC / CI（`checks`/`build`）両方 green 09:58:49 UTC
  （**作成から約2分9秒**）/ マージ 10:15:23 UTC（terai-takehiro 本人が手動マージ、
  **CI green から約16分34秒後**・作成から約18分43秒）。`get_reviews`・`get_review_comments`
  とも終始0件（#165・#167・#244・#258・#270・#463・#464・#469 と同型）。表に移す指摘はない
  （レビュー自体が届いていないため）。`npm run reviews:debt` は GitHub 401 でこの環境から
  実行できず、GitHub MCP で直接0件を確認した。マイグレーションなし（シードのみ変更）。
  検証は `npm run typecheck:all`・`npm run lint`（warning 58件・着手前と同数。作業中に
  `useRail` の依存配列で1件増やしたのを `67492d6` で戻した）・`npm run test`（1572件）・
  全ワークスペースの `build` に加え、`npm run verify:ui` を **2712/2722 → 2724/2724**
  （失敗10件→0件）にした。
  ⚠️ **権限のない利用者での403確認は未実施**（権限モデルを変更していないためPR本文に明記済み）。
  ⚠️ **この PR で意図して残した判断が2つある**（レビュー0件のため自分で書き出す）:
  ① `client-techops` の編集画面の表本体（`CueCardList`/`cells/InsertGap`）に手を入れた —
  `client-techops/CLAUDE.md` の「表本体は作り直しの PR まで触らない」に対し、
  **ユーザーの明示的な指示**（「残る1件も」）を受けて高さを寸法の段（32px）に載せるだけの
  修正を行った。色・字・配置・DOM 構造は変えていない。表本体の作り直し PR とは競合しうる。
  ② `scripts/verify-ui.mjs` に「切ると決めた箱」の印（`data-clip-ok`）を新設し、
  プロンプターの台本欄に付けた — この欄は `scrollTop` を自前で進める作りでスクロールバーを
  出さないため、検査が「中身に届かない」と数えていた。`line-clamp` の既存の除外と同型だが、
  **検査を緩める方向の変更**なので、乱用されていないか（付いている箇所が増えていないか）を
  次の棚卸しで見ること。

- **#473（docs(reviews): PR #472 のマージ後の棚卸しを記録した・2026-08-29）** — #472の直後、
  この棚卸し自体を記録するために出したPR。作成 10:40:51 UTC / CI green（未計測だが両ジョブ
  successを確認）/ マージ 11:01:54 UTC（terai-takehiro 本人が手動マージ）。`get_reviews`・
  `get_review_comments` とも0件。表に移す指摘はない（コード変更なし・ドキュメントのみ）。
- **#474（fix(gpm): ダッシュボードのスマホでカードが中身1行なのに1200px超になるのを直した・
  2026-08-29）** — ユーザー報告（実機iPhoneのスクリーンショット付き）「スマホのこれ中身ないのに
  長すぎない？」を受け、`gpm/pages/dashboard/Panel.tsx` の `h-full` が原因の実バグを特定・修正した。
  PC（`lg:grid-cols-3`）では「止まっている」「動いている」の2枚を`lg:col-span-2`の1つの入れ物に
  まとめ、隣の「未確認事項」と高さを揃えるために`h-full`を使っていたが、スマホ（1列）ではこの2枚が
  同じ入れ物の中に縦に積まれた兄弟になり、`h-full`（height:100%）が「2枚合わせた入れ物の高さ」を
  基準に解決されるため、中身が1行しかない枚まで2枚分の高さに引き伸ばされていた
  （実測: 空状態1200px超→179px、実データ3件/5件で両方1280pxに誤って揃っていたのが473px/793pxの
  コンテンツ量どおりに）。`h-full`を`lg:h-full`にする1行の修正で、PC側の見た目（703px/703pxで
  揃う）は変えていない。作成 10:42:25 UTC / CI green 10:44:32 UTC（約2分7秒）/ マージ
  11:01:59 UTC（terai-takehiro 本人が手動マージ、CI green から約17分27秒後）。`get_reviews`・
  `get_review_comments` とも0件。表に移す指摘はない。案件管理側の同型部品
  （`salesDashboard/Panel.tsx`）は各カードが直接グリッドの子として並ぶ別構造のため、同じ不具合が
  無いことも実測で確認済み（PR本文に明記）。マイグレーションなし。検証は `npm run typecheck`・
  `npm run lint`（warning 58件・着手前と同数）・`npm run build`（client）に加え、実サーバー
  （検証用Postgres）+ Playwrightで実施——空状態・実データ・PC/スマホ双方の高さを実測、
  横はみ出し0・pageerror 0を確認。

- **#475（docs(reviews): PR #473・#474 のマージ後の棚卸しを記録した・2026-08-29）** — #473・
  #474の直後、この棚卸し自体を記録するために出したPR。作成 11:04:59 UTC / マージ 11:42:52 UTC
  （terai-takehiro 本人が手動マージ）。`get_reviews`・`get_review_comments` とも0件。
  表に移す指摘はない（コード変更なし・ドキュメントのみ）。
- **#476（release: v4.5.5・2026-08-29）** — 利用者の明示的な指示「リリースしましょう」を受けて
  出した版上げPR。`npm run release:notes -- 4.5.5` が3件の下書き（#472/#473/#474）を集約し、
  `package.json`/`CLAUDE.md`/`README.md` の3か所と `docs/version-history.md` を更新した
  （v4.5.4 をアーカイブへ移動）。作成 11:12:27 UTC / CI green（`checks`・`build` とも success）/
  マージ 11:42:59 UTC（terai-takehiro 本人が手動マージ）。`get_reviews`・`get_review_comments`
  とも0件。表に移す指摘はない（コード変更なし・版の書き換えのみ）。`check-changelog.mjs` の
  リリース判定（枝名が `release/` で始まる）が正しく通り、`checks` は成功した。マイグレーション
  なし。検証は `npm run typecheck:all`・`npm run test`（1572件）・全ワークスペースの `build`・
  `RELEASE=1 npm run lint`（warning 58件・着手前と同数）・`npm run check:version`（3か所一致）を
  実施。**本番公開（GitHub Release の Publish）はこのPRのマージだけでは走らない** —
  利用者の明示的な指示があるまでClaudeからは実施しない（CLAUDE.md の絶対原則）。
- **#477（docs(reviews): PR #475・#476 のマージ後の棚卸しを記録した・2026-08-29）** —
  #475・#476の直後、この棚卸し自体を記録するために出したPR。作成 11:45:20 UTC / CI
  （`checks`・`build` とも success）11:47:40 UTC（約2分20秒）/ マージ 13:32:14 UTC
  （terai-takehiro 本人が手動マージ、**CI green から約1時間44分後**）。`get_reviews`・
  `get_review_comments` とも0件。表に移す指摘はない（コード変更なし・ドキュメントのみ）。
- **#478（docs(reviews): PR #477 のマージ後の棚卸しを記録した・2026-08-29）** —
  #477の直後、この棚卸し自体を記録するために出したPR。作成 13:34:25 UTC / CI
  （`checks`・`build` とも success）13:36:27 UTC（約2分2秒）/ マージ 13:47:26 UTC
  （terai-takehiro 本人が手動マージ、CI green から約11分後）。`get_reviews`・
  `get_review_comments` とも0件。表に移す指摘はない（コード変更なし・ドキュメントのみ）。
- **#479（docs(reviews): PR #478 のマージ後の棚卸しを記録した・2026-08-29）** —
  #478の直後、この棚卸し自体を記録するために出したPR。作成 13:48:53 UTC / CI
  （`checks`・`build` とも success）13:50:45 UTC（約1分52秒）/ マージ 16:15:53 UTC
  （terai-takehiro 本人が手動マージ、CI green から約2時間25分後）。`get_reviews`・
  `get_review_comments` とも0件。表に移す指摘はない（コード変更なし・ドキュメントのみ）。
- **#480（fix(techops): 配信収録設定の Excel 出力を現地で取り込める形に直し、YouTube プリセットを
  足した・2026-08-30）** — 作成 12:20:51 UTC / CI green（`checks`・`build` とも success）
  12:23:12 UTC（約2分21秒）/ マージ 13:19:50 UTC（terai-takehiro 本人が手動マージ、
  CI green から約56分38秒後）。**`get_reviews`・`get_review_comments` とも終始0件のまま
  マージ**（`npm run reviews:debt` はこの環境からは GitHub 401 で実行できず、GitHub MCP で
  直接確認した）。⚠️ **レビュー0件のため、この PR で意図して残した判断を自分で書き出す**:
  1. **`export-xlsx` の権限を editor 固定 → 閲覧可に緩めた**（`keyMode=plain` のときだけ
     editor を要求する2段構え・`device-settings.routes.ts` の `requireEditorForPlainKeys`）。
     ExportDialog 自身が「サーバー側を緩めるかは別の判断・別の PR」と宿題にしていた線の
     とおりだが、**権限を緩める変更をレビュー0件で通した**ことは事実として残す
     （Excel の中身は閲覧者が画面で見られるものと同じ・平文キーだけが editor 限定、が根拠）。
  2. **preflight の `keyMode=plain` は見本のために復号を実行する**（平文はサーバー内メモリ
     のみ・応答は `****` に伏せる）。見本と実物を同じ `buildSheetSpecs` に通す原則を守る
     ための実装で、復号できない鍵が見本でも（空欄）に見える副作用は仕様として採った
     （その場合は赤 `KEY_UNDECRYPTABLE` が別途出る）。
  3. **収録先の機器呼び名への写像（`SSD 1`→`ssd1` 等）は書き出しの瞬間だけ**。保存値・
     画面表示・preflight の判定（`HDPLUS_SSD` は `SSD` 前方一致）は従来の呼び名のまま。
     写像表に無い自由入力は今までどおり素通し（弾くのは現地の仕事・#279 の方針）。
- **#481（docs(reviews): PR #479・#480 のマージ後の棚卸しを記録した・2026-08-30）** —
  #480の直後、この棚卸し自体を記録するために出したPR。作成 2026-08-30 13:23:21 UTC / CI
  （`checks`・`build` とも success）13:25:26 UTC（約2分5秒）/ マージ 2026-08-31 07:29:19 UTC
  （terai-takehiro 本人が手動マージ、CI green から約18時間4分後）。`get_reviews`・
  `get_review_comments` とも0件。表に移す指摘はない（コード変更なし・ドキュメントのみ）。

- **#482（fix(sales,platform,daily): 失注案件のやること・未入金通知・日常業務の2画面に溜まる
  ゴミを止めた・2026-08-31）** — 作成 06:55:17 UTC / CI green（`checks`・`build` とも success）
  06:57:23 UTC（**約2分6秒**）/ マージ 07:29:31 UTC（terai-takehiro 本人が手動マージ、
  CI green から**約32分8秒後**）。**`get_reviews`・`get_review_comments`・`get_comments` とも
  終始0件のままマージ**（`npm run reviews:debt` はこの環境からは GitHub 401 で実行できず、
  GitHub MCP で直接確認した）。55ファイル・+3877/-621・migration 3本（245/246/247）と
  規模が大きいわりに**誰も読んでいない**ので、⚠️ **この PR で意図して残した判断を自分で書き出す**:
  1. ⚠️ **migration 246 が過去の通知を機械で既読にする**（`inv_late` のうち、いまも
     請求書が未発行の売上あてのぶんだけ `read_at` を打つ。**行は消さない**）。
     そもそも出るべきでなかった督促が未読の山の大半を占めていたため入れたが、
     **人が読んでいない通知を機械が既読にするのは初めて**で、PR 本文でも
     「本番に出す前に方針の確認を」と明記して出した。**この時点で回答は無い。**
     取りやめるならその `UPDATE` 1文を落とすだけで済む。
  2. **督促の宛先を `editor` 全員＋`system_admin` 全員 → `manager` ＋当事者に狭めた**
     （`inv_late` / `inv_send_todo` / `eq_return` / `weekly_unreviewed` の4本）。
     ノイズは確実に減るが、**いままで届いていた人に届かなくなる**変更で、
     「自分は経理ではないが未入金を見ていた」人がいた場合に気づけない。
     `usersWithPermission` に足した `includeAdmins` は**未指定なら従来どおり**なので、
     戻すのはこの4か所の引数を直すだけ。
  3. **督促の頻度を毎朝 → 節目（超過 1・7・30 日目、以後30日ごと）にした。**
     「毎朝来ていたものが来なくなった」を**止まったと誤解される**恐れがあるため、
     通知の本文と設定⑦の一覧の両方に頻度を書いたが、**運用で確かめたわけではない**。
  4. **機材の返却遅れは借用者本人に届かないまま**（`equipment_lendings` に利用者と
     結び付く列が無く `borrower_name` の文字列だけ）。直すには貸出画面ごと変える話なので
     この PR ではやらず、**本文とコードに「届かない」と明記するに留めた**。→ 下の表に ❌ で残す。
  5. **`reminderBucket` を `shared/src/` に置けなかった**（`server/tsconfig.json` の
     `rootDir: "./src"` によりサーバーは `shared/` を import できない）。
     `bookingDuplicate.ts` と同じく `server/src/shared/services/` に置き、テストだけ
     `shared/tests/` に置いた。今回新設した `next-action-state.ts` も同じ形。
  6. **AIフィードバックループの条件3（成果指標）に穴が残る** — 「ストック→見直し→見送り」率を
     数えていない。PR 本文に代替案（`state='dropped' AND stock_review_on IS NOT NULL` を数え、
     `getFeedbackDigest('inquiry_intake')` に `stock_then_dropped_rate` を足す）まで書いた。
     → 下の表に ❌ で残す。
  7. **実装は3エージェントの並行作業**（①営業活動記録 ②通知 ③日常業務の2画面）で、
     統合・検証は1人が行った。統合時に**担当の切れ目で3件の不具合を自分で見つけて直している**
     （受信箱だけ「まだ仕分けていない情報」の条件を写していて件数が食い違う／SQL の
     テンプレートリテラル内にバッククォートを書いた構文エラー／共通化で古くなった既存テスト2件）。
     **レビューが0件だったので、この3件は誰にも確かめられていない。**

- **#484（docs(reviews): PR #482 のマージ後の棚卸しを記録した・2026-08-31）** —
  #482 の直後、この棚卸し自体を記録するために出した PR。作成 07:33:44 UTC / CI green
  （`checks`・`build` とも success）07:35:34 UTC（約1分50秒）/ マージ 07:40:26 UTC
  （terai-takehiro 本人が手動マージ、CI green から約4分52秒後）。`get_reviews`・
  `get_review_comments` とも0件。表に移す指摘はない（コード変更なし・ドキュメントのみ）。
- **#485（release: v4.5.6・2026-08-31）** — `docs/changelog.d/` の下書き7本を集めた版上げ PR。
  作成 07:38:04 UTC / CI green（`checks`・`build` とも success）07:40:24 UTC（約2分20秒）/
  マージ 07:40:40 UTC（terai-takehiro 本人が手動マージ、**CI green から約16秒後**）。
  `get_reviews`・`get_review_comments` とも0件。表に移す指摘はない（版表記と履歴のみ）。
  ⚠️ **枝名を `release/4.5.6` にしてある**（`check-changelog.mjs` は枝名が `release/` で
  始まるか `RELEASE=1` のときだけリリース PR と判定する）。v4.5.2 のときは
  `claude/release-prep-lod7ck` で出して `checks` が落ち、`release/4.5.2` に出し直して
  #460 → #461 になった前例があるので、今回は最初からこの名前で出した。
  ⚠️ **`release:notes` が組み立てた要約は使っていない** — 「収録した見出しを並べただけ」に
  なりスクリプト自身が警告するので、CLAUDE.md と README.md の先頭行は人が書き直した。
  次回から省きたければ `docs/changelog.d/_summary.md` を置いておくとそれが使われる。

- **#487（feat(sales): 失注・見送りの BOX フォルダを片づける・2026-08-31）** —
  作成 10:30:37 UTC / CI green（`checks`・`build` とも success）10:32:48 UTC（約2分11秒）/
  **途中で `processed` が「見た件数」だった実バグを自分で見つけて直し**、
  一括ボタン化のぶんも入れて 10:42:06 UTC に再び green / マージ 11:01:16 UTC
  （terai-takehiro 本人が手動マージ、green から約19分10秒後）。
  `get_reviews`・`get_review_comments` とも0件。⚠️ **本番の BOX を消し得る変更が
  レビュー0件で通った**ので、意図して残した判断を書き出す:
  1. **ご依頼は「削除」だったが「空なら削除・中身があればアーカイブ」に変えた**
     （ご相談のうえ判断をいただいた）。理由は `box-lost-cleanup.service.ts` の頭注 —
     見積書・請求書・検収書の原本が BOX にしか無い／失注は取り消せて機械も付ける／
     `box_url_*` は手で書き換えられる文字列／本番DBのバックアップが社内親フォルダの直下。
  2. ⚠️ **実際の BOX に対しては1度も動かしていない。** → 下の表に ❓ で残す。
  3. **`folders.delete` を型定義に足した**（これまで意図的に外してあった）。
     使ってよいのは安全弁を通ったときだけ、という決めをテストで固定しているが、
     **型が生えた以上どこからでも呼べる**ようになった。次に BOX 周りを触る回に、
     他所から呼ばれていないかを見ること。
  4. **`recursive` を付けない**ことに寄りかかっている（数え間違いがあっても中身が
     あれば BOX 側が 400 で断る、が二重の守り）。BOX の仕様が変わると効かなくなる。
  5. **溜まっているぶんは migration で遡らず、人が押す導線にした**（245 とは別の判断。
     あちらは DB の中だけで完結する可逆な操作だった）。押さなければ減らない。

- **#489（fix(sales): 失注案件の BOX フォルダが「ほとんど片づかない」のを直した・2026-08-31）** —
  作成 12:01:42 UTC / CI green 12:03:54 UTC（約2分12秒）/ **ユーザー指摘を受けて
  名寄せの方式ごと入れ替え**、12:17:25 UTC に再び green / マージ 12:28:26 UTC
  （terai-takehiro 本人が手動マージ、green から約11分1秒後）。`get_reviews`・
  `get_review_comments` とも0件。
  ⚠️ **#487 を「動いているつもりで」出したことの後始末**なので、経緯を残す:
  1. **利用者の報告で初めて分かった** — 「ほとんどのゴミ案件が処理できていない」。
     原因は2つとも**古い案件ほど当たる**もので、検証環境では気づけなかった
     （検証DBの失注案件は新しく、`box_url_*` も入っていたため）。
     ①古い失注案件は `box_url_*` が空でフォルダを見つけられない
     ②安全弁の「名前が `【社内】`/`【社外】` で始まる」が、**頭が付いた 2026-08-25
       より前のフォルダを全部弾いていた**
  2. **1度目の直しも不十分だった。** ユーザー指摘「GLSじゃないやつも当然
     対応できてますよね？」で判明 — フォルダ名を `/^(GLS|OPP)-[A-Za-z0-9-]+_/` で
     読み解く方式では、**プロジェクト管理（GLS-B）は番号を持たない**
     （`createGpmFolderTree(projectName)` は案件名だけ）・**Excel 取込の旧番号は
     `GLS001`（ハイフン無し）**・**Excel 取込のコードは `PRJ-2026-001`** の3種類が漏れる。
     **フォルダ名を読み解くのをやめ、DB の値から候補名を組み立てて突き合わせる**
     形に入れ替えた（`expectedFolderNames`）。形が今後増えても効く。
  3. ⚠️ **PR 本文は1度目の版の説明のまま**（番号を切り出す前提で書いてある）。
     2つ目のコミットで方式を入れ替えたので、**本文と実装が食い違っている**。
     読むときは実装（`box-lost-cleanup.service.ts`）と `shared/tests/boxLostCleanup.test.ts`
     を正とすること。
  4. **名寄せは実際の BOX に対して未検証**（この環境も検証DBも `BOX_CONFIG_JSON` を
     持たない）。→ 下の表の ❓ に含める。

- **#492（fix(sales): 一括片づけが本番で 504 になっていたのを、時間で区切る形に直した・2026-08-31）** —
  作成 12:54:30 UTC / CI green 12:57:02 UTC（約2分32秒）/ マージ 13:17:17 UTC
  （terai-takehiro 本人が手動マージ、green から約20分15秒後）。`get_reviews`・
  `get_review_comments`・`get_comments` とも0件。
  ⚠️ **これも #487・#489 と同じく「本番で押されて初めて分かった」ものなので、経緯を残す**:
  1. **「20件ずつなら大丈夫」という見積もりが根拠を持っていなかった。** フォルダ1件に
     つき BOX を「読む → 中身を数える → 動かす」で十数回叩くため、20件で **200〜300回**の
     呼び出しになり Nginx の 60 秒で切られていた。**BOX の応答時間はこちらでは決められない**
     ので、件数で区切る限り「遅い日は落ちる」が残る。**時間（`BOX_CLEANUP_BUDGET_MS` = 20秒）で
     区切る**形に変えた。
  2. ⚠️ **20秒という値そのものは実測ではない**（60秒の proxy に対して3分の1、という
     取り方をしただけ）。→ 下の表の ❓ に含める。
  3. **504 で切られたのは応答だけで、サーバー側は動き続けていた。** 押した人には
     何件進んだのか分からない状態だった（画面は正直に「0 件まで進みました」と出すが、
     実際には途中まで進んでいた可能性がある）。時間で切れば必ず応答が返るので、
     進んだぶんは必ず画面に出る。
  4. **「1件も進まなかったら止める」だけでは足りなかった。** #487 で入れたこの終わり方は
     安全弁で見送った行を永久に叩き続けないためのものだが、**BOX が遅い日は「時間切れで
     0件」でも止まってしまう**。`timedOut` を返して「進まなかった」と「途中で止めた」を
     区別した。**この2つを混同すると、直したはずの一括ボタンがまた何もしない**。

- **#495（feat(sales): BOX を触らなかった理由を出し、ゴミ案件を台帳から外せるようにした・2026-08-31）** —
  作成 14:12:40 UTC / CI green 14:14:55 UTC（約2分15秒）/ マージ 14:16:05 UTC
  （terai-takehiro 本人が手動マージ、green から約1分10秒後）。`get_reviews`・
  `get_review_comments` とも0件。
  ⚠️ **+765/-11・9ファイル・「案件を台帳から消す」機能が、レビュー0件・
  作成から3分25秒でマージされた。** 意図して残した判断と、あとから見つけた穴を書き出す:
  1. **ご依頼は「DBから削除したい」だったが、論理削除（`deleted_at`）にした**
     （`AskUserQuestion` でご判断をいただいた）。理由は3つ:
     ①請求書・検収書は電子帳簿保存法の保存対象で、案件の行を消すと外部キーで
     ぶら下がる `revenues`/`purchases`/`estimates` まで道連れになる
     ②失注は90日放置の自動見送りで**機械も付ける取り消せる状態**
     ③失注案件の見積は失注分析の唯一の材料。
  2. ⚠️ **「戻せる」と画面に書いたが、戻す導線は画面に無い** — システム管理者が
     `UPDATE projects SET deleted_at = NULL` を直に叩く前提。→ 下の表の ❌。
  3. ⚠️ **台帳から外した案件の BOX フォルダは、以後どの導線からも片づけられない** —
     片づけの対象は `deleted_at IS NULL` で絞っているため。外す直前に片づけを
     試し、片づかなかった件数（`boxLeft`）は画面に出すが、**出したあとに
     片づける手が無い**。→ 下の表の ❌。
  4. ⚠️ **生存証拠（`ALIVE_EVIDENCE_SQL`）の除外漏れという実バグがあった。**
     放置ネタの条件が日数（90日）しか見ておらず、**未来までスヌーズしてある
     ネタが台帳から消える**状態だった（自動見送りは同じ除外を掛けている＝
     「機械が閉じにも来ない案件を台帳からは消す」食い違い）。
     **レビュー0件で通り、マージ後に自分で差分を読み返して見つけた** — #496 で修正。
     本番には出ていない（#495 は検証環境まで）。
  5. **実際の BOX に対しては相変わらず未検証**（既出の ❓ に含まれる）。ただし
     #495 の BOX 側の変更は**すでに DB にある文字列を集めて出すだけ**で、
     BOX を叩く経路は変えていない。

- **#496〜#506（BOX の片づけと案件台帳の整理・2026-08-31 の連続対応）** —
  6本（#496 / #499 / #501 / #503 / #504 / #505）とリリース3本（#498 / #500 /
  #502 / #506）。**すべてレビュー0件でマージ**（`get_reviews`・`get_review_comments`
  とも0）。作成からマージまでは最短で3分台。
  ⚠️ **この日の本当の学びは「実物を見ないまま5回直した」ことです。** 経緯:

  | 版 | 直したつもり | 利用者の報告 |
  | --- | --- | --- |
  | v4.5.7 (#487) | 失注の BOX を片づける | 「ほとんどのゴミ案件が処理できていない」 |
  | v4.5.8 (#489) | URL 未設定を名寄せで拾う | 「Request failed with status code 504」 |
  | v4.5.9 (#492) | 時間で区切る | 「このように出て結局処理されない」 |
  | v4.5.11 (#499) | 空の木を下から消す | 「BOXの削除コマンドが表示されなくなりました」 |
  | v4.5.12 (#501) | 外した案件も対象に | 「まだ大量にゴミファイルがBOXにあります」 |

  1. ⚠️ **毎回「実際の BOX に対しては未検証」と書いて出していた。**
     5回のうち4回は、利用者が本番で押して初めて誤りが分かっている。
     **未検証で出すこと自体は避けられなかった**（この環境も検証DBも
     `BOX_CONFIG_JSON` を持たない）が、**セッションに BOX のコネクタが
     付いていることに5回目まで気づかなかった**のは見落とし。
  2. **実物を読んだ瞬間に、原因は一度で確定した** — 親フォルダ 47 → 40 件
     （6件が実際に削除・1件がアーカイブへ）で **v4.5.11 の直しは効いていた**こと、
     残った 40 件のうち `【社内】OPP-…【問い合わせ】…` が `size 0`・空のサブ
     フォルダ3枚だけであること、`99_失注・見送り` に4件入っていること。
     **推測を1つも挟まずに #504 の設計が決まった。**
  3. **画面に理由を出したことが転機だった**（#495）。`box_cleanup_note` には
     最初から理由が入っていたのに**画面へ出していなかっただけ**で、出した途端に
     「BOX が削除を断った 15 件」と読め、#499 の原因が特定できた。
     ⚠️ **「なぜ何もしなかったか」を出さない機能は、直しようがない。**
  4. **ご判断でもらった線引き**（この日の `AskUserQuestion` 3回ぶん）:
     論理削除にする／失注すべて＋放置ネタ／見積だけなら消す（のち
     「請求前の見込み売上も落とす」に更新）／ネタの空フォルダも消す／
     結び付かないフォルダも空なら消す。
  5. **ご指摘でこちらの前提が直った2点**: 「GLSじゃないやつも対応できてますよね？」
     （番号の形を当てにしない設計へ）と「失注案件で仕入が発生することは
     理論上あり得ない＝処理の誤りの合図」（残す理由ではなく**異常の合図**として
     画面に出す形へ）。

- **#508（fix(sales): 終了日が空の完了案件も「98_終了案件」へ移るようにした・2026-09-01）** —
  **レビュー0件でマージ**（`get_reviews` / `get_review_comments` とも空）。
  利用者の報告（「GLS-A003 はすでに終了しています」「009、010、013 も終了していると
  思います」「プロジェクト案件も終了した案件をアーカイブしてください」）から入り、
  **本番の実データを読んで原因を2つ確定させた**回。

  1. **完了案件の 4/9 が `event_end` を持っていなかった**（A002 / A003 / A009 / A010）。
     日付が無い案件は `98_終了案件` へ**永久に移らない**。プロジェクト管理（GLS-B）は
     さらに外れていて、終了日は `gpm_projects.ends_on` にあり `projects.event_end` は
     ほぼ空。**完了4件のうち自動で移れたのは1件だけ**だった。
  2. **20 秒の予算内に3件しか進まなかった** — 置き場は両親に1つずつしかないのに
     `ensureSubfolder` を案件ごと・側ごとに呼んでいた。

  ⚠️ **実 DB がテストの通り抜けたバグを捕まえた。** `DONE_DATE_SQL` を入れたとき
  **プレースホルダ1つに対して2つ渡して**おり、`bind message supplies 2 parameters,
  but prepared statement "" requires 1` で 500 になった。**`typecheck:all` も
  テスト1781件も緑のまま**で、実 Postgres に当てなければ出せていた。

- **#511（feat(techops): テロップCG（リアルタイムCGの作り直し）を実装した・2026-09-01）** —
  **レビュー0件でマージ**（`get_reviews` / `get_review_comments` とも空）。作成からCI green
  まで約2分20秒、CI green から約5分待ったがCodexのレビューが届かなかったため、その旨をここに
  記録したうえでマージした。凍結中のリアルタイムCG（`client-awards`）を制作技術支援の
  ミニアプリとして作り直した一連の作業（段1〜6・125ファイル・約17,400行）の集大成PR。

  意図して残した判断・未確認事項（PR本文にも記載済み）:
  1. `npm run verify:ui` は未実行 — テロップCGの画面群はまだ同スクリプトの対象範囲に
     登録していない。実測でのフォント・桁揃い・タップ領域検査は今後の課題
  2. バッジ・チップの固定幅は一部（過去データ移行プレビューのパターン表示等）だけ採用し、
     全数監査はしていない
  3. `graphics_cue_state.reveal_phase` のsentinel値変更（`0`→`-1`・migration経由）など、
     実装中に見つけた既存バグの詳細は
     `docs/changelog.d/claude-realtime-cg-v4-optimization-ayc233.md` に記録済み
  4. 過去実績データ（`awards_*`）を新エンジンへ変換移行するツールは実装・検証DBでの検証まで
     完了。本番の `awards_*` データへ実際に実行するかは、当初は今回のタスクの対象外としていたが、
     ユーザーの明示的な追加指示（「PR作成〜本番公開まで一気に進めてよい」）を受けて本PRの
     マージ後に別途実行する運びとなった（実行結果は別途この表に記録する）
  5. ⚠️ **マージ直後、`main` 上で別PR（#512・財務ダッシュボードの索引追加）とのmigration番号
     衝突（`250`）が発覚した**（PR単体のCIはどちらも通っていたが、`main`上で初めて可視化。
     `dev.gmo-onair.jp` へのデプロイが一時スキップされた）。#513 で解消した — 詳細は次の行

- **#513（fix: main上のmigration番号250衝突（PR#511/#512）を解消した・2026-09-01）** —
  **レビュー0件でマージ**（`get_reviews` / `get_review_comments` とも空）。CI green確認後、
  `main` のCI赤・検証環境デプロイのスキップという緊急度から、レビュー到着を待たずにマージした。
  表に移す指摘はない（migrationファイルの改名と参照文書の番号更新のみで、SQLの中身・挙動は無変更）。

- **#521（docs(sales): レギュラー案件（回が蓄積するパターン）のロジックを決めてモックを作り直した・2026-09-01）** —
  **レビュー0件でマージ**（`get_reviews` / `get_review_comments` とも空）。文書とモックだけで
  コードは無変更なので、**表に移すのは「意図して残した判断」と「棚卸し中に実物で見つけた穴」**。
  1. ⚠️ **書いている最中に、書いた当の実害が本番で起きた**（下の表 P1）。
     本番の `GLS-A007「インテリジェンス」` を実測したところ、**04:05:33 に受注済＋レギュラーに
     設定され、その 11 分後の 04:16:46 に `updated_by: system:project_tidy` で `s_completed` に
     落ちていた**。日次ジョブ `completeElapsedWonProjects` が `event_end`（第1回の日付
     2026-06-18）だけを見て繰り上げたもの。**推測ではなく本番のレコードで観測している。**
  2. **出す前に自分の成果物を読み返す工程を入れ、9件の食い違いを見つけて2コミット目で直した**
     （#15・#16 に画面から到達できない／設計文書の単価が旧モックの ¥160,000 のまま／
     2026-06-18 を「水」と書いていた 等）。⚠️ **ただしこれは PR を出したあとだった** —
     出す前に回すべき工程。
  3. **指摘のうち9件は「既存トークンそのもの」だったので直していない**（62px = `FIXED_W`、
     999px = preset の `chip`、スマホの 20/17/13px = `tokens-v4.css` のスマホ用上書き 等）。
     ⚠️ **見直す側が実装を読まずに「確定値から外れている」と判断すると、正しいものを壊す。**
     実装で裏を取ってから直す・直さないを決めた。
  4. `npm run reviews:debt` は**この環境で 401**（GitHub トークンが無い）。棚卸しは MCP の
     `get_reviews` / `get_review_comments` を直に叩いて行った。

- **#538（fix(全体): マルチエージェント全方位レビューで確認した不具合111件を修正・2026-09-02）** —
  **レビュー0件でマージ**（`get_reviews` / `get_review_comments` / `get_comments` いずれも空）。
  作成 00:00:21 / CI（`checks`/`build`）両方成功 00:02:5x 頃 / マージ 00:05:52 頃
  （**CI green から約3分後**、#251・#272・#305 などと同じ「見る前に入った」形）。
  `npm run reviews:debt` は今回も401で使えず、GitHub MCP で直接確認した。
  ⚠️ **この PR 自体が「14観点の並列レビュー→敵対的検証→21グループ修正」というマルチエージェント
  レビューの成果物**であり、201ファイル・+3517/−3908行という規模の割に人間のレビューが
  1件も付いていない。表に移す指摘はない（レビュー自体が届いていないため）が、
  **実装時に意図して残した判断・未検証事項（実ブラウザ/実DB未確認・equipment_settings
  4/6項目が未配線・DBゴミ2種を記録のみに留めた等）は自己申告として
  [docs/reviews/2026-09-01-multiagent-app-review.md](2026-09-01-multiagent-app-review.md)
  の末尾にまとめてある**（この文書に重複転記しない）。

- **#585（fix(equipment): メンテナンスの機材選びに付属品を出し、保管場所と台帳の崩れを直した・2026-09-06）** —
  **レビュー0件でマージ**（`get_reviews` / `get_review_comments` とも空）。
  作成 11:12:00 / CI（`checks` 11:15:41・`build` 11:16:09）両方成功 / マージ 11:19 頃
  （**CI green から約3分後**）。`npm run reviews:debt` は今回も **401** で使えず、
  GitHub MCP で直接確かめた。
  ⚠️ **今回は0件の理由がはっきりしている** — Codex が PR 作成の 5 秒後に
  「**You have reached your Codex usage limits for code reviews.**」を投稿しており
  （[コメント](https://github.com/terai-takehiro/gmo-onair/pull/585#issuecomment-5558828883)）、
  **通常のコードレビューは1行も走っていない**。走ったのは別枠のセキュリティレビューだけで、
  そちらは「Completed・指摘なし」。**つまり「指摘なし」なのはセキュリティ観点だけで、
  ふだんの Codex レビューは読まれていない**。原因不明で0件だった #165・#244・#262 と違い、
  **これは「読まれていない」と確定している 0 件**なので、そう読むこと。
  表に移す指摘はない（レビュー自体が届いていないため）。
  ⚠️ **意図して残した未検証事項**（PR 本文にも明記済み）:
  ① `npm run verify:ui` を走らせていない（実ブラウザで 1440/768/375px の実測に代えた）
  ② 権限のない利用者で開いて 403 / 非表示になることを確認していない
  ③ `shared/src/client/ui/tableBadge.tsx` を触っているので**配信中の5アプリ全部に効く** —
  足したのは `truncate` / `min-w-0` / `overflow-hidden` の3つで、どれも既に使われている
  クラスなので CSS は増えない想定だが、**`npm run check:frozen` でのビルド出力の
  突き合わせはしていない**（凍結アプリは 0 個なので `APPS` が空＝実質何も照合しない）
  ④ 機材台帳の商品名を `line-clamp-2` にしたことで**行の高さが2通りになる**。
  間引き（`useRowWindow`）の送り幅を「先頭2行」から「見えている行の平均」に直してあるが、
  **数千行の実データで長くスクロールしたときのずれは測っていない**

---

## 一覧（PR の新しい順）

- **#607**（`feat(daily): ウィークリー活動報告に「隔週キープの数字」（BI）と「資料をつくる」（pptx の半自動生成）を足した`・2026-09-07）—
  隔週キープ資料の数値部分を「定例報告パック」に集約し、週報の確定で凍結、BI タブ・資料ビルダー（pptx）・
  Slack 下書き・MCP 3 ツールを足した回（148 ファイル・+17,939/−149・25 コミット・migration 291）。
  作成 23:23:23Z（9/6）→ terai-takehiro 本人がマージ 01:35:10Z（9/7）。**Codex の Code Review は
  6 巡走り、23 スレッドの指摘を返した** — bde91b3→e3dce2c／e3dce2c→dc75b02／dc75b02→a3103be／
  a3103be→1b8b584／1b8b584→2aa5781／2aa5781→2cf31d5（左がレビューされたコミット・右が直したコミット）。
  **すべてマージ前に修正・返信・スレッド解決済み**（内容は上の表の #607 の行。各回の対応は PR 本文
  「レビュー対応」にも残した）。🔒 Security Review は 1 巡目（bde91b3）のみで findings なし。
  ⚠️ **最後のコミット 2cf31d5（6 巡目の 2 件への対応）は Codex がレビューしていない** — 01:10Z に
  「You have reached your Codex usage limits for code reviews」が返り、再レビューが走らないまま
  01:35Z にマージされた。レビュー0件は「指摘なし」ではないので、この回で意図して残した判断を自分で書き出す:
  ① `getSummaries` の**按分の合計**は status を見ない台帳（`TOTAL_REVENUE_SQL`）の数え方のままにし、
     行数（`revenue_count`）だけを confirmed に絞った（数え方を 2 つにしない）。概算のグループ請求の
     按分が確定売上の合計に混ざる余地は上の表に ❓ で残した
  ② `freezeMeeting` は 12 通りの凍結を週報の確定の中で**同期に**回す（検証DBで約 0.3 秒・190KB。
     本番の件数では伸びるが、1 つ失敗しても他は続け warn に残す形。遅くなったら非同期化を検討）
  ③ 画面は凍結の一覧を 500 行（≒ 40 回の確定）まで取る。それを超える古い会議日は選択肢から消える
  ④ 5〜6 巡目の画面の直し（取引先マスターの「サムライ関連」チェック・文字の大きさの欄の出し分け・
     「凍結済みだがこの絞り込みは いまの数字」の帯）は**実ブラウザで見ていない**（typecheck・lint・
     Vitest・サンプルのパックからの pptx 出力の XML・シード済みDBでの API の挙動で確かめた）
  `npm run reviews:debt` はこの環境のトークンでは 401 のため、GitHub MCP の `get_reviews` /
  `get_review_comments` で直接確認した（23 スレッドすべて `is_resolved: true`）。
  changelog は [docs/changelog.d/claude-weekly-report-bi-powerpoint-kh02s2.md](../changelog.d/claude-weekly-report-bi-powerpoint-kh02s2.md)。
  **未検証で残したもの**: `npm run build:all` → `check:frozen` は 1 巡目の段で通し、以後は `build:changed` と
  CI の `build` ジョブのみ／実ブラウザでの PC・スマホ確認は 1 巡目の段まで（上の④）／権限別 403 は
  API で確認し画面では見ていない。

- **#605**（`feat(techops): テロップCG 段F（旧リアルタイムCGの畳み込み）を実行した`・2026-09-06）—
  テロップCG再設計の最終段（旧`client-awards`の「移行中」→「廃止」）。サーバーの配信・API・
  Socket.IO・ビルド対象を撤去し、`shared/src/client/apps.ts`のコメント・PRテンプレートの
  記述も揃えた。**Code Review が usage limits 等で実行されなかった #578・#583・#585・
  #590・#596・#598・#600（連続する範囲ではない——同じ間に挟まる #595 は4巡ものCode
  Reviewを受けて19スレッド・P1 11件・P2 8件が付いている）と異なり、#605 はCodexが2巡とも
  Code Reviewを実行して3件の指摘を返した**（🔒Security Reviewは1巡目のみでfindingsなし。
  レビュー指摘・PR #606 で「連続した空白」という誤った表現を訂正）。
  3件とも検証・修正しマージ前に解消:
  ① **P1** `awards_entries.photo_url`が`/api/v1/internal/awards/images/...`という
  絶対パスを値としてそのまま持ち、テロップCGの過去実績移行ツールがこの文字列を書き換えずに
  `RankingEntry.photoUrl`へコピーするため、`createAwardsRoutes()`をまるごと外すと
  既に移行済み・今後移行するランキングの顔写真が全て欠ける指摘。`images.routes.ts`を
  読み取り専用の`imageServingRouter`と書き込み系（顔写真アップロード・ZIP一括）に分離し、
  前者だけ`createAwardsImageRoutes()`経由で残した（`ff47636`）。② **P2**
  `.github/workflows/preview.yml`がプレビューデプロイのたびに廃止済みの`/awards/`を
  リンクとして出し続けていた指摘。行を削除（`ff47636`）。③ **P2** `searchFeatures.ts`の
  APPS走査が`hidden`は見るが`frozen`を見ておらず、⌘K検索に廃止済みアプリが死んだリンクとして
  残っていた指摘。`visibleApps()`の既定と同じく`frozen`も除外するよう1行追加（`875f082`）。
  **検証範囲は指摘ごとに違う**（レビュー指摘・PR #606 で「いずれも実サーバーで実測」という
  過大な記述を訂正）: ①（画像配信）だけ`verify:up`の実サーバーで修正前後の挙動差を実測
  （実在ファイルが実際のバイト列で返ることを確認）。②（preview.yml）はGitHub Actionsの
  step-summary出力そのものなので`verify:up`では検証できず、行の削除を目視確認したのみ。
  ③（検索インデックス）は`npx tsc -b client`・`npm run lint`のみで、⌘K検索の実ブラウザ確認は
  していない。3件とも返信・スレッド解決はマージ前に完了させた。
  changelog は [docs/changelog.d/claude-telop-cg-stage-f-retire-awards.md](../changelog.d/claude-telop-cg-stage-f-retire-awards.md)。
  **未検証で残したもの**: `npm run build`（テロップCG以外の全クライアントのフルビルド）、
  実ブラウザでのPC/スマホ確認、権限別403の画面確認（本PRはUI変更なし・サーバー配線と
  ドキュメントのみのため相対的に優先度は低いと判断）。
  ⚠️ **この棚卸し自身（PR #606）にも、Codexが3件の指摘（この段落を含む3か所の記述の
  不正確さ）を返している**——①ここ（上記表）に3件の行を足していなかった、②「連続した
  空白」という表現が実際には#595という4巡レビュー済みのPRを挟んでいて不正確、③「いずれも
  実サーバーで実測」が過大（②③は指摘ごとに検証範囲が違う）。**#606はCI green後まもなく
  マージされ、この3件を直す前に画面から指摘が消えた**——直しは本追記として同じ段落に
  反映済み（`#206`の決めごとに従い、この記述精度を巡る指摘には新しいPRを割らずまとめて
  扱った。実害のあるコードバグではないため）。

- **#600**（`feat(techops): テロップCG をゼロベースで再設計した（段A〜E）`・2026-09-06）—
  テロップCGの機能とUI/UXをゼロベースで再設計し、段A〜E（設計で定めた実装段の全て）を
  1本のPRにまとめて実装した回（段B〜Eはマルチエージェントのワークフローで実装・検証。
  詳細は [docs/design/v4/graphics-redesign.md](../design/v4/graphics-redesign.md) §11
  「段A 実装メモ」〜「段E 実装メモ」・changelog は
  [docs/changelog.d/claude-telop-cg-redesign-e4bwep.md](../changelog.d/claude-telop-cg-redesign-e4bwep.md)）。
  作成14:42:45Z → CI green 15:04:41〜15:05:04Z（作成から約22分。作成直後に本物の
  マージコンフリクトを検出・解消し、途中でCIが1周走っている）→ terai-takehiro 本人が
  マージ15:06:29Z（CI greenから約1分半）。**`get_reviews` 0件**（API で直接確認。
  `npm run reviews:debt` はこの環境のトークンでは401）。📝 Code Review は
  **usage limits で一度も実行されず**（PRコメントで明示）、🔒 Security Review だけが
  実行され **findings なし**（コメントの状態が"Completed"になっただけで、指摘コメントは
  投稿されなかった）。**#578・#583・#585・#590・#596・#598 に続いて Code Review が
  届かない回が連続している。** 通っているのは Security 観点だけなので「指摘なし」ではない。
  **表に移す Codex指摘はない**（レビュー自体が届いていないため）。
  ⚠️ **このPR自身のマージ作業中に、`main`との合流で実際のコンフリクトとマイグレーション
  番号衝突を両方踏んだ**（#512・#570/#569 と同型の「並行開発でぶつかる」パターン）:
  ① `nav.ts`・`GraphicsHubPage.tsx`・`PartLibraryPage.tsx` の3ファイルで本物の
  マージコンフリクトが発生（`main`側の別PRが左メニューの階層化・用語統一・部品ライブラリ
  削除ファイルの編集を並行して行っていたため）。手動で解消し、tsc・lint・test・build・
  migration適用を再検証してからpush。
  ② `main`が別PRで先に`280`・`281`番を使っており（`280_schedule_item_span_cols.sql`・
  `281_finance_doc_chain.sql`）、テロップCG側の同番号2本と衝突した
  （`check-migration-numbers.mjs`が検出）。新しく足した側（このブランチ）を`282`・`283`へ
  改番し、コード内コメント・設計書・changelogの参照も揃えた。
  **未検証で残したもの**（各段の実装メモに一貫して明記済み）: 実機（物理デバイス）での
  タップ操作、権限のない利用者での403の**画面レベル**確認（段Eの新規APIについては
  curlでのAPIレベル確認は実施済み）、1行に複数のテロップ列がある台本のケース、
  vote・score・ranking種類の行内操作の実ブラウザ確認（seedデータに存在しないためコード
  レビューのみ）。

- **#598**（`fix(skills,server): メール仕分けの枠が GitHub 通知に食い潰される問題と、本番・検証の MCP が同名を名乗る問題を直した`・2026-09-06）—
  ⚠️ **📝 Code Review は Codex の usage limits で一度も実行されず**
  （「You have reached your Codex usage limits for code reviews」と明示的に返答）、
  🔒 Security Review だけが `25b857d` で完了し **findings なし**。
  `get_reviews`・`get_review_comments` とも0件を API で確認（`npm run reviews:debt` は401）。
  **#590・#595・#596 に続いて4本連続で Code Review が届いていない。**
  通っているのは Security 観点だけなので「指摘なし」ではない。
  0件だったことは[PR のコメント](https://github.com/terai-takehiro/gmo-onair/pull/598#issuecomment-5559617825)にも残した。
  **表に移す指摘はない**（レビュー自体が届いていないため）。
  CI green（`checks` 13:39:54Z / `build` 13:41:07Z）から**約9分後**に terai-takehiro 本人がマージ。
  **この PR で見つけたものは自分の実測**（ルーティンの検索式を実メールに当てた）で、
  内訳は下表。全文は
  [docs/reviews/2026-09-06-mail-intake-taxonomy.md](2026-09-06-mail-intake-taxonomy.md) の「6. 試験実行」。
  **未検証で残したもの**: 実ブラウザでの確認・`npm run verify:ui`・
  **検証環境での実際のメール取り込み**（`gmo-onair-dev` のコネクタを繋いでから）。

  | 重み | どこ | 何が起きるか | 状態 |
  | --- | --- | --- | --- |
  | **P1** | `mcp/server.ts` | **本番と検証の MCP が両方とも `gmo-onair` と名乗っていた**。コネクタを2本つなぐと見分けが付かず、**検証のつもりで本番に書ける**（今回この会話のコネクタが本番だと気づけたのは `list_users` が実在の社員を返したからで、名前からは分からなかった） | ⭕️ #598（検証は `gmo-onair-dev`・`instructions` にも環境と行き先を書く。`shared/tests/mcpEnvName.test.ts` が固定） |
  | **P1** | `mail-intake/SKILL.md` | **1回30通の枠が GitHub の通知だけで尽きる**。3日窓201スレッドのうち新しい順40件で `notifications@github.com` が33件・`dmarc-report@` が2件（87.5%）。決定表 #1 は「拾ってから捨てる」なのでラベルでは間に合わない（印を付けても翌日また同じだけ届く）＝**その裏の請求書に永遠に届かない** | ⭕️ #598（検索式に `-from:` を4つ。**201 → 54**） |
  | **P1** | `mail-intake/SKILL.md` | **「古いものから30通」が実行できない** — `search_threads` は新しい順にしか返さない。しかも古い順だと**その日届いた請求書が後回し** | ⭕️ #598（新しいものから30通） |
  | **P1** | `mail-intake/SKILL.md` | **検索結果の抜粋は「いちばん古い5通」**（切れている印も出ない）。6月に始まって今日返信が来たスレッドを**6月の本文で仕分ける**（実測2件） | ⭕️ #598（判断は `get_thread` の最新メッセージで・3か所に明記） |
  | P2 | `mail-intake/references/decision-table.md` | 社内メーリス経由の通知（54件中12件）が #12「残り」に落ち、**`record_inquiry` に大量に入る**（拾いすぎ） | ⭕️ #598（#1b を追加） |


- **#596**（`fix(daily): PR #595 のレビュー棚卸しと、レビューが届かなかったぶんの自己修正4件`・2026-09-06）—
  #595 の棚卸し（下の表19件）を記録し、**Codex が読めなかった2コミット
  （`5482aec` / `05d00b4`）を自分で読み直して4件直した**回。
  ⚠️ **この PR 自体はレビューが1件も付かないままマージされた**
  （`get_reviews`・`get_review_comments` とも0件を API で確認。`npm run reviews:debt` は
  この環境のトークンでは401）。📝 Code Review は **usage limits で一度も実行されず**、
  🔒 Security Review だけが `23e98e8` で完了し **findings なし**。
  **最後のコミット `6fc0292`（4件目の直し）はどちらのレビューも受けていない。**
  #578・#583・#585・#590 と同型——「指摘なし」に見えるのは Security 観点だけ。
  **表に移す指摘はない**（レビュー自体が届いていないため）。自分で見つけた4件は
  この PR の中で直しており、内訳は下表のとおり。
  **未検証で残したもの**: 実ブラウザでの PC・スマホ確認、権限別 403 の画面確認、`npm run verify:ui`。

  | 重み | どこ | 何が起きるか | 状態 |
  | --- | --- | --- | --- |
  | **P1** | `finance-doc-chain.service` | 束は最初の書類（多くは見積書）で作られるが、処理月と支払サイトは**請求書に書いてあることのほうが多い**。後から届いた書類の値で埋めないので、束は最後まで期日を出せず**払う期日があるのに「期日なし」で一番後ろに沈む**（4巡目 P1 の直しが半分だけだった） | ⭕️ #596（空いているところだけ埋める。**入っている値は上書きしない** — 人が直した値かもしれない） |
  | **P1** | `finance-doc-chain.service` | 束を消すときの確認が**取引の外**。数えたあとに台帳へ渡した書類が消され、**仕入・販管費の行だけが宙に浮く** | ⭕️ #596（`withTransaction` の中で `FOR UPDATE`） |
  | **P1** | `finance-doc-chain.service` / `doc-handoff.service` | **上の直しが効いていなかった** — 押さえる対象を「登録済みの書類」に絞ると**0行のときは何も押さえない**。その隙に「仕入・販管費に登録」が通ると擦れ違って両方成功する | ⭕️ #596（束の中の**生きている書類ぜんぶ**を押さえてから状態を見る。`handoffDoc` 側も押さえたあとに `deleted_at` を見る） |
  | P2 | `mail-attachment-box.service` | `decodeStrictBase64` が厳しすぎて**詰め物なしの正しい base64 を落としていた**（URL 用の書き方はそもそも詰め物を付けない）。正しい原本を「読めません」で落とすのは、ゴミを通すのと同じくらい困る | ⭕️ #596（詰め物はサーバーが足し、**余り1文字だけ**弾く。試験の期待のほうが間違っていたので試験を直した） |


- **#595**（`feat(daily,finance): メール自動仕分けを作り直し、受領書類を見積→発注→請求のひとつづりにした`・2026-09-06）—
  メールの仕分けを Claude のルーティンで回す形にし、受領書類を「1通ずつ」から
  「1つの取引」に作り直した回（migration 281）。**Codex が4巡し、19スレッド・
  P1 11件（うちセキュリティ High 1件）／P2 8件。すべて直してからマージ**した
  （レビュー指摘・PR #608 で「P1 12件／P2 7件」という誤った内訳を訂正。下の表を
  実際に数え直すと11／8）。
  下の表に全件を書き出してある。⚠️ **4巡目のあと Codex は usage limits に達し**、
  `5482aec`（3巡目の直し）と `05d00b4`（4巡目の直し）は**レビューを受けていない**。
  そのぶんは自分で読み直して4件直した（#596）。
  マージは terai-takehiro 本人が手動（`05d00b4` の時点）。
  **未検証で残したもの**: 実ブラウザでの PC・スマホ確認、権限別 403 の画面確認、`npm run verify:ui`。

  | 巡 | 重み | どこ | 何が起きるか | 状態 |
  | --- | --- | --- | --- | --- |
  | 1 | **P1** | `281_finance_doc_chain.sql` | 移行が既存の書類を束に入れないので、**当てた瞬間に受領書類が画面から全部消える**（ホームの受信箱は数え続けるので「N件あります」と出て開くと空） | ⭕️ #595（1通=1束で追い込む SQL） |
  | 1 | **P1** | `finance-doc-chain.service` | `expense_kind='sga'` と `project_id` を両方送ると `SET project_id = ?, project_id = ?` になり PostgreSQL が弾く＝**販管費として確定する操作が必ず失敗** | ⭕️ #595（列ごとに1つだけ持つ） |
  | 1 | **P1** | `finance-doc-chain.service` | 束の一覧が書類の列を間引いており、**金額だけ直したつもりでメモが消える**／台帳へ渡すとき**必ず販管費に倒れる**／✨ も原文も出ない | ⭕️ #595（行そのものを返す） |
  | 1 | P2 | `finance-doc-chain.service` | 並びが `updated_at` 順で、**期日を過ぎた請求書より、さっき触った期日2か月先の取引が上**に来る | ⭕️ #595 |
  | 1 | P2 | `finance-doc-chain.service` | 上限300を絞り込みより先に掛けるので、**古い未処理が片づいた履歴に押し出される** | ⭕️ #595（SQL の WHERE へ） |
  | 1 | P2 | `GroupCard.tsx` | 添付が入らなかった理由を捨てて「BOX に入っていません」だけ出す（**Google 連携をやり直せば直ると分からない**） | ⭕️ #595（文言はサーバーが返す） |
  | 2 | **P1 High** | `mcp/gate.ts` | `list_finance_doc_groups` を権限表に足し忘れ、**権限が1つも無い利用者でも**取引先・案件・金額・支払期日・BOX の在り処が読める | ⭕️ #595（表に追加＋全ツールが表か個人スコープ名簿に載っている試験） |
  | 2 | **P1** | `finance-doc-chain.service` | 台帳に渡した書類がある束の**行き先を変えられる**（台帳の行は直らないので食い違う） | ⭕️ #595（409） |
  | 2 | **P1** | `finance-doc-chain.service` | 束で決めたことを書類に降ろさないので、**人が「販管費」に直しても台帳へ渡すダイアログが仕入として開く** | ⭕️ #595 |
  | 2 | **P1** | `doc-handoff.service` | 処理月は書類側の当て推量が勝ち、**人が直した月がいつまでも効かない**（違う月の販管費として計上） | ⭕️ #595（束が先。案件は逆） |
  | 2 | **P1** | `inbox.service` | 取り直しのとき添付を拾い直さないので、**一度失敗した原本が永久に入らない**（Gmail の添付 id は毎回変わる） | ⭕️ #595 |
  | 3 | **P1** | `.claude/skills/mail-intake/SKILL.md` | 捨てたメールに印が無く毎回同じ検索に当たる。**1回30通の枠を捨てるメールが占め、その裏の請求書が3日間見られない** | ⭕️ #595（`mail-intake-skipped`） |
  | 3 | P2 | `mail-attachment-box.service` | `Buffer.from(s,'base64')` は壊れた文字列でもゴミを返すので、**壊れた PDF を BOX に上げて「保存しました」と報告する** | ⭕️ #595（`decodeStrictBase64`） |
  | 3 | P2 | `finance-doc-chain.service` | 1件読みが一覧の上限（300）を通るので、**保存できたのに「見つかりません」と言う** | ⭕️ #595（id で直接引く） |
  | 3 | P2 | `opsreports.tools` | メール取込ログをニュース記事として数え、**回すほどニュースの採用率が下がって見える** | ⭕️ #595 |
  | 4 | **P1** | `finance-doc-chain.service` | 台帳に渡した書類がある束を**消せる**（仕入・販管費の行だけが残り、どの書類から来たか辿れない） | ⭕️ #595（409。**確認は取引の中で `FOR UPDATE`** — 別 PR で移した） |
  | 4 | **P1** | `finance-doc-chain.service` | 期日が書いていない販管費が**一番後ろに沈む**（処理月＋サイトから期日が出るのに使っていない） | ⭕️ #595（`derived_payment_due`。**TS で1回だけ計算**して列に置く） |
  | 4 | P2 | `finance-doc-chain.service` | **束で直した当て先が `ai_corrections` に1件も残らない**（SQL を直接書いていて記録の関数を通らず、`FD_FIELDS` にも当て先が入っていなかった）。この PR の主目的そのものが計測できていなかった | ⭕️ #595（両方直した） |
  | 4 | P2 | `finance-doc-chain.service` | 束で直した取引先が**台帳に1文字も届かない**（ダイアログが `doc.sender` を送り、サーバーがそれを優先） | ⭕️ #595 |


- **#590**（`fix(client,daily,equipment,awards,shared): メニュー名とページ見出しの食い違い10件を修正`・2026-09-06）—
  「メニューバーと実際のページのヘッダーのアプリ名が異なる箇所がある」というご指摘を受け、
  6アプリ（client / client-daily / client-equipment / client-techops / client-live / client-awards）
  をマルチエージェントで全件調査し、見つかった食い違い10件（凍結済み `client-awards` の1件を
  含めると11件）を修正した回。案件管理の利用マニュアルの見出しが入口によらず常に「案件管理」
  固定だった不具合・設定アプリの上辺バーが「設定／設定」と重複表示していた不具合を含む。
  作成から `main` の並行マージ（PR #589）による本物のマージコンフリクト（
  `client/src/contexts/tasks/pages/TaskDashboardPage.tsx`。タイトル修正と main側の
  「未完了」→「未対応」表記統一が同じ行でぶつかった）を検出・解消してpush。
  CI green（`checks` 11:41:47Z / `build` 11:42:22Z）から**約5分後**に terai-takehiro 本人が
  手動マージ。⚠️ **`chatgpt-codex-connector` の Code Review は usage limits で一度も
  実行されなかった**（PRコメントで明示: "You have reached your Codex usage limits for
  code reviews"）。**Security Review は実行され 0 件**（`get_reviews`・`get_review_comments`
  とも0件で確認。`npm run reviews:debt` は今回も401で使えず GitHub MCP で直接確認した）。
  #578・#583・#585 と同型——「指摘なし」に見えるのは Security 観点だけで、
  **コード品質観点のレビューは一度も届いていない**。表に移す指摘はない（レビュー自体が
  届いていないため）。検証: `client`/`client-daily`/`client-equipment`/`client-awards`/`server`
  の型チェック、`npm run lint`（0 errors・warning数は着手前と同数）、`shared` の Vitest は
  PR #590 が `main`（`bd8d605`）を取り込んだ自分の頭で全通過（当時148ファイル/1998件）。
  ⚠️ **この件数は以後の `main` の進み方に依存する**（この棚卸し行自体をレビューした
  Codex の指摘どおり、この行を書いた時点の `main` 頭 `96dcb01` で走らせると
  148ファイル/2017件になる——19件は #590 のマージ後に他PRが足したもの）。
  実ブラウザでのPC/スマホ確認・`npm run build`・権限別403の画面確認・実DB確認は
  いずれも未実施（PR本文に明記済み）。

- **#585**（`fix(equipment): メンテナンスの機材選びに付属品を出し、保管場所と台帳の崩れを直した`・2026-09-06）—
  ①メンテナンスの「記録を追加」で選べる機材が**親機材だけ**で、カメラセットの中のレンズなどの
  付属品（子機材）が候補に1件も出なかったのを `include_children=1` で出るようにし、候補と一覧の
  両方に親の機材名を添えた。②設定＞保管場所で拠点名の札が右隣の保管場所名に重なっていたのを直し
  （列 56px → 160px・1024px 以上でだけ出す。`TableBadge` 自体もあふれたら `…` にした）。
  ③機材台帳のチェック列を 128px → 72px にし、商品名を2行目まで表示するようにした。
  **レビュー0件でマージ（Codex が利用上限で通常レビューを走らせていない）** — 上の
  「レビューが0件のままマージされた PR」を参照。**表に移す指摘なし**

- **#583**（`feat(techops): スケジュール表を第2版として作り直した`・2026-09-06）—
  制作技術支援＞スケジュール表を「MCP くらいしかできる機能がなく、根本的な機能性が皆無」という
  ご指摘から、段A〜C相当（7本のPR分）を1つのブランチにまとめて作り直した回
  （詳細は [docs/design/v4/qsheet-v4-coding/14-schedule-v2-plan.md](../design/v4/qsheet-v4-coding/14-schedule-v2-plan.md)、
  changelog は [docs/changelog.d/claude-schedule-table-features-uiux-kskvms.md](../changelog.d/claude-schedule-table-features-uiux-kskvms.md)）。
  作成から約5分でCI green・その5秒後にユーザー自身がマージ。⚠️ **`chatgpt-codex-connector` の
  Code Review は usage limits で一度も実行されなかった**（PRコメントで明示: "You have reached
  your Codex usage limits for code reviews"）。**Security Review は実行され、マージの約35秒後に
  完了・0件**（`get_reviews` で確認。マージ前は "Running" のままだった）。#578 と同型——
  「指摘なし」に見えるのは Security 観点だけで、**コード品質観点のレビューは一度も届いていない**。
  表に移す指摘はない（レビュー自体が届いていないため）。実装時に意図して残した判断・未検証事項
  （PR本文にも記載済み）:
  1. **項目編集シートを初めて開いたときだけ「項目名」欄が空欄になる**（2回目以降は正しく表示される）。
     Radix Dialog の自動フォーカスと、IME保護のための `useBufferedValue`（全社共通フック）の
     「フォーカス中は外部値を取り込まない」仕様が競合するのが原因。この PR の変更とは無関係な
     既存バグ（base ブランチだけでも再現することを確認済み）だが、`useBufferedValue` は影響範囲が
     広いため直していない。
  2. **`POST /techops/ai/event-plan`（AI下書き生成）が常に500エラーになる**
     （`assertGenerationAllowed` のSQLでPostgresがパラメータ型を決められない）。今回追加した
     拠点・種別の入力欄より前から存在する既存バグで、この PR が原因ではないことを確認済み。
     AI下書きダイアログ自体は正しく表示・入力できるが、実際に生成を押すと現状失敗する。
  3. **権限のない利用者での403/非表示の画面確認は一部のみ**（案件メンバー自動共有の可視条件・
     非メンバーの404は確認済みだが、全画面を全ロールで機械的に確認してはいない）。
  検証: `npm run typecheck:all`（client / client-equipment / client-techops / client-live /
  client-daily / server 全ワークスペース）・`npm run lint`（`0 errors`。warning数は着手前と同数）・
  `shared` の Vitest 1996件 全通過・検証用Postgres＋実ブラウザ（Playwright・PC 1280/375px）で
  列作成・ドラッグ移動・複製・印刷・台本結び直し・担当補完・予約から列コピー・AI下書きダイアログの
  拠点/種別欄まで一通り確認（横はみ出し 0px・JS エラー 0 件）。フル `npm run build` は未実施。

- **#578**（`fix(sales,platform,production): 同種のフォーム初期化レースを他画面でも修正`・2026-09-06）—
  [#575](https://github.com/terai-takehiro/gmo-onair/pull/575)（案件編集画面のSPA遷移で「案件分類」
  保存不可バナーが誤表示される不具合）と同じ型・近縁のバグが他の編集画面にも無いか、
  5アプリ（`client`/`client-daily`/`client-equipment`/`client-techops`/`client-live`）を
  5体の並列サブエージェントで横断調査し、見つかった3件（`LiveDisplayLayoutEditorPage.tsx`・
  `LiveProgramSettingsPage.tsx`・`UserDialog`/`RoleDialog`/`StudioBookingDialog` の対象切り替え）
  を修正した回。⚠️ **`chatgpt-codex-connector` の Code Review が usage limits で一度も
  実行されなかった**（PRコメントで明示: "You have reached your Codex usage limits for
  code reviews"）。**Security Review は実行され 0 件**（`get_review_comments` で確認）。
  つまり「指摘なし」に見えるのは Security 観点だけで、**コード品質観点のレビューは
  一度も届いていない**——silence を「問題なし」と読まないこと。
  表に移す指摘はない（レビュー自体が届いていないため）。**PR #575自体もこの棚卸しに
  記録されていなかった**（見張り時点で `get_reviews`・`get_review_comments` とも0件を
  確認済みだが、この文書への記載を今回まで漏らしていた——両レビューとも実行はされ
  指摘0件だったことは確認済み、コード品質観点も届いていた点が#578と異なる）。
  検証: `npx tsc -b client client-techops`・`npm run typecheck -w server`・`npm run lint`
  （warning数は着手前と同数・0 errors）・`shared` の Vitest 1996件 全通過。⚠️ 実ブラウザでの
  PC/スマホ確認・`npm run build`・権限別403の画面確認・実DB確認はいずれも未実施
  （PR本文に明記済み）。

- **#577**（`fix(equipment): 機材検索プルダウンで型名・Noも検索対象にした`・2026-09-06）—
  機材管理のメンテナンス記録の追加ダイアログ・貸出登録の機材選択で、機材ID・名前に加え
  型名(`model_number`)でも検索できるようにし、同名・同型機材の区別のため No.
  (`unit_number`)・型名も選択肢に表示するようにした回。CI green（作成から約3分半）、
  マージ可否 `clean`。**Codex のセキュリティレビューは実際に完了し、指摘0件**
  （`get_reviews`・`get_review_comments` とも空で、Codexのサマリーコメントが
  `status: completed` を報告——直前に「利用上限に達した」という別コメントが挟まったが、
  レビュー自体はその後正常に完了した）。表に移す指摘なし。コード変更のみで
  マイグレーションなし・AI機能なし。

- **#564**（`feat(calendar): パートナーの予定に希望日トグルと担当者(複数)を追加`・2026-09-05）—
  パートナースケジュール（代休・有給・出張・社外活動・リモート・その他）に「希望日
  （未確定）」のトグルと「担当者（複数・任意・登録ユーザーから選択）」を追加した回。
  **今回は他の回と違い、Codex のレビューが実際に届き、CI green までの間に計8件
  （P1 1件・P2 7件）の指摘が付いた**——マージ前にすべて対応・返信・スレッド解決まで
  終えたため、**表に新しく移す未対応の指摘は無い**。何が指摘され、どう直したかを
  記録として残す:
  1. **P1**: 担当者の候補一覧 (`/users/by-module/partner_schedule`) が旧モジュール名の
     まま（権限モデル単純化・migration 210 で `sales` に統合済み）で、system_admin 以外
     誰も返らず担当者を選べない/外せない → `sales` に修正。react-query の鍵も
     `users-by-module-sales` に統一（別の鍵のままだと同じ内容を別クエリとして持ち、
     `FilterDialogs.tsx` 側と鍵が衝突する）
  2. **P2**: 担当者が上限（20人）を超えると超過分を黙って切り捨てて保存していた
     （選んだはずの担当者が静かに消える）→ 超過時は400で拒否するように変更
  3. **P2**: カレンダー「人で絞る」フィルタが予定の持ち主（`user_id`）しか見ておらず、
     担当者として入っている人を選んでも「空いている」ように見えた →
     `assignees` の id も判定に含めた
  4. **P2**: 担当者の検証が「存在して削除されていない」だけで、`sales` 権限の無い
     ユーザーIDも通ってしまい、画面（担当者チップ）からは選べない/外せない担当者が
     付き得た → `/users/by-module/sales` と同じ条件（`status='active'` かつ sales権限
     保持 or system_admin）に揃えた
  5. **P2**: 4番の修正が新たな詰みを生んでいた——既存の担当者が後から sales 権限を
     失うと、選択肢には出てこないのに保存のたびサーバーに拒否され、その予定を
     二度と保存できなくなる → 選択肢に無い選択中の担当者も「対象外・外すのみ可」
     として表示し、外せるようにした
  6. **P2**: 上記1番と同根の同じバグが `FilterDialogs.tsx` の `UserFilterDialog`
     （「人で絞る」の候補一覧・3番の絞り込みを実際に試すのに必要）にも残っていた →
     同様に `sales` へ修正
  7. **P2**: 週表・日表（`TimeGrid.tsx`）の終日イベント行が `tentative` を見ておらず、
     有給等の既定形である終日の希望日が確定済みと見分けられなかった → 破線表示を追加
  8. **P2**: PUT で担当者を貼り替える delete→insert が行ロック無しに見えると指摘 →
     検証（検証DBで同一予定に30並列PUTを実行・エラー無し/最終状態も一貫）した結果、
     直前の `UPDATE partner_schedules` 文自体が transaction 中ずっと行ロックを保持する
     ため実害は無かったが、将来 UPDATE を条件付きでスキップする書き換えが入っても
     安全なように `SELECT ... FOR UPDATE` を明示的に追加した
  検証: 新規マイグレーション（278番。main に先着した277番との衝突を検知して改番）、
  `server`/`client` の型チェック、`shared` の Vitest 1996件、検証DBでのAPI実地確認
  （作成・部分更新・不正な担当者IDの拒否・21件超過の拒否・sales権限無しの拒否・
  30並列PUTでの競合無し）。⚠️ 実ブラウザでのPC/スマホ確認・`npm run build`・
  `npm run verify:ui` は未実施（PR本文に明記済み）。

- **#563**（`feat(client): 財務ダッシュボードのトグル全体化・仕入担当者・案件一覧の並び替え`）—
  作成 2026-09-05 17:57:45 JST。**今回は他の最近の PR と違い、Codex のレビューが実際に届いた**
  （P1×2・P2×7、計9件・2回に分けて）。**マージ前に全件検証のうえ修正・返信・スレッド解決済み**
  （`npm run reviews:debt` はこの環境のトークンでは401のため使えず、`get_review_comments` で
  直接確認）。マージ 18:40 JST 頃。3機能（財務ダッシュボードのトグル置き場所全体化・仕入の
  担当者登録・案件一覧のヘッダークリック並べ替え）を3体の並列サブエージェント（各自 worktree）に
  実装させ、レビュー対応は自分で直接行った PR。
  | # | 重み | ファイル | 指摘 | 状態 |
  |---|---|---|---|---|
  | 1 | P2 | `FilterBar.tsx` | ヘッダークリックが作る `sort` 値（`name:asc`等）が `SORT_OPTIONS` に無く、プルダウン/スマホの表示が「おすすめ順」に誤読み替えされる | ⭕️ `0e4204a`（`sortLabel`/`sortChoices` で列名＋昇順/降順を動的生成） |
  | 2 | P2 | `purchases.routes.ts` | `assigned_to \|\| req.user!.id` が明示的な `null`（担当者クリア）を未指定と区別できず、作成者にすり替わる | ⭕️ `0e4204a`（`=== undefined` のときだけ作成者に落とす） |
  | 3 | P2 | `PurchaseDialog.tsx` | `BudgetDashboardPage`/`RevenueBillingPane` の閲覧専用・追加ダイアログが `users`/`currentUserId` を渡しておらず、生UUID表示／担当者0件になる | ⭕️ `0e4204a`（サーバーが `assigned_to_name` をJOIN解決・追加ダイアログに配線） |
  | 4 | P2 | `usePurchaseDialogData.ts` | 担当者一覧が `extractPagination` の100件上限で切れ、101人目以降が選べない | ⭕️ `0e4204a`（`fetchAllUsers` で全ページ取得） |
  | 5 | P2 | `ProjectRows.tsx` | `aria-sort` が `columnheader`/`rowheader` 役割を持たない `<div>` に付いており、スクリーンリーダーに効かない | ⭕️ `0e4204a`（ボタンのアクセシブルネームに並び状態を含める） |
  | 6 | P2 | `purchases.routes.ts` | `assigned_to` に外部キー制約が無く、存在しない・削除済みIDをそのまま保存できる | ⭕️ `0e4204a`（POST/PUTとも実在確認） |
  | 7 | P1 | `usePurchaseDialogData.ts` | 新設したフラット配列のキャッシュ鍵 `users-list` が、他画面（`SgaListPage`等）の同名鍵（応答オブジェクト）と衝突し、キャッシュ次第で `users.map` が落ちる | ⭕️ `e518e85`（専用キー `finance-purchase-assignee-users` に分離） |
  | 8 | P2 | `FilterBar.tsx` | ヘッダーで実施日・次のタスクの降順が新たに選べるが、サーバーはこの2列の降順で `NULLS FIRST`（日付なしが先頭）にしており、直近日の案件がページから押し出される | ⭕️ `e518e85`（この2列は降順を生成しない・昇順→既定の2段トグルに変更） |
  | 9 | P2 | `purchases.routes.ts` | PUTで担当者を変えていなくても毎回実在確認しており、検証を足す前から孤立IDを持つ行の他項目更新まで拒否していた | ⭕️ `e518e85`（「既存値から変わった値」のときだけ検証） |

  ⚠️ **意図して残した未検証事項**（型チェック・lint・shared Vitest 1996件・検証用 Postgres への
  実際のAPI呼び出しでの確認に留まる）: ① 実ブラウザでのPC/スマホ確認はしていない
  （`npm run verify:ui` 未実施）。② 権限のない利用者での403/非表示の画面確認はしていない。
  ③ `npm run build` は実行していない。

- **#561**（`feat(daily): フィードバックチケットの可視化ミニアプリを新設`）—
  作成 2026-09-05 08:29:08 JST / CI（`checks`/`build`）とも成功 08:32:43 JST /
  **CI が緑になる前、Codex のレビューが届く前にユーザー自身がマージ**（`pull_request.closed`
  の webhook で 08:35:12 JST 過ぎに確認。Codex のレビューコメント6件はマージの数分後に到着）。
  他の直近の PR（#544〜#558）と違い、**このPRは珍しくレビューが実際に届いた**——0件ではなく
  P2 が6件。マージ後に届いたため直しは別コミット（本ドキュメントと同じコミット）で行った。
  1. `feedback-ticket.service.ts` の一覧に上限が無く、溜まる一方で通信量・DB負荷・描画が
     際限なく伸びる → ⭕️ 直した。上限（既定50・最大200、「入ってきた情報」と同じ考え方）を
     付け、検索も SQL の ILIKE に寄せた（以前は全件取ってから JS で filter していた）
  2. `TicketRows.tsx` の一覧行が `role="button"` を持つのに Enter/Space で開けない
     （`Row` はキーボード操作を合成しない） → ⭕️ 直した。`onKeyDown` を足した
  3. `feedback-ticket.service.ts` の `updateStatus` が `input.response_note ?? existing... ?? null`
     で **明示的な `null`（対応コメントを消す）と省略を区別できず**、消したはずのコメントが
     復活する → ⭕️ 直した。`!== undefined` で明示的に分岐させた
  4. `FeedbackTicketsPage.tsx` の絞り込みチップの件数が、他の軸の絞り込み・検索を反映した
     ローカルの一覧から数えていて実態とずれうる → **⭕️ 別の形で直した**（指摘どおりの
     「他の軸を掛け合わせて数え直す」は実装していない）。件数を `GET .../counts`
     （対象アプリ別の内訳を追加）から読む形に変え、**上限つきの一覧から数える経路自体を無くした**
     （①の上限を付けたことで、ローカル計算のままだと確実に嘘になっていた）。チップの件数は
     `SecurityCardsPage`/`DailyNewsPage` と同じく「その軸だけの総数」（他の軸とは掛け合わせない）
     という、この製品の既存の作りに揃えてある——揃えないと「状態チップは状態だけの数・
     アプリチップは掛け合わせた数」のように**チップごとに意味が変わり**、むしろ読み間違えやすい
  5. `feedback-ticket.service.ts` の `updateStatus` が対応済み/却下にするたびに
     `resolved_at = NOW()` へ**無条件に**上書きしており、対応コメントだけを直す保存でも
     実際に閉じた時刻が書き換わる → ⭕️ 直した。「未対応/対応中 → 対応済み/却下」に
     **初めて遷移した**ときだけ NOW() にし、閉じたまま保存する場合は既存値を保持、
     再オープンで NULL に戻す
  6. `feedback-ticket.service.ts` の `create` が `title`/`description` の型を見ておらず、
     数値・配列等を送ると `.trim()` が例外を投げて意図しない 500 になる → ⭕️ 直した。
     `typeof === 'string'` を先に見るようにした

  検証: 検証用 Postgres で6件それぞれを個別に再現し、直したあと解消したことを確認
  （null クリア・対応コメントのみ保存で `resolved_at` 不変・非文字列で400・limit の丸め・
   ILIKE 検索・`byTargetApp` の内訳）。`server`/`client-daily` の型チェック・
  `shared` の Vitest 1996件・lint 通過。実ブラウザでの確認は未実施。

- **#558**（`feat(client,server): 案件フェーズの確度と財務ダッシュボードの営業見通しを追加`）—
  作成 2026-09-04 13:14:00 JST / `build` 成功 13:16:05 JST / **`checks` が `in_progress` のまま
  （13:18:52 JST に）ユーザー自身がマージ**——他の回のような「CI 緑の直後」ではなく、
  `checks` ジョブの完了そのものを待たずにマージされている（`get_check_runs` で直接確認。
  マージ後に再度叩いても `checks` は `in_progress` のまま止まっていた——PR が閉じたことで
  ジョブが宙に浮いたと見られる）。`get_reviews`・`get_review_comments`・`get_comments` は
  いずれも0件（API で直接確認。この環境では GitHub トークンが無く `npm run reviews:debt` は
  使えない）。**「指摘なし」ではなく「レビューが届いていない」**。
  ⚠️ **事前調査を2体の並列 Explore サブエージェントに委譲**（売上・仕入登録の受注済み制限の
  所在／財務ダッシュボードの集計ロジックとUI構成を独立に調査）、**サーバー側の実装
  （migration・stage-probability service/route・pipeline-forecast service）は自分で直接書き**、
  **設定画面のUI実装（`StageProbabilities.tsx`・`MoneyRulesPage.tsx` への組み込み）は
  1体の並列 general-purpose サブエージェントに委譲**した PR。表に移す指摘はない
  （レビュー自体が届いていないため）が、**意図して残した未検証事項**
  （レビュー0件のため自分で書き出す。PR本文の検証チェックリストにも未チェックのまま明記済み）:
  ① **実ブラウザでの画面確認をしていない**（PC 1440px・スマホ 375px とも未実施。
  型チェック・lint・shared の Vitest 1996件、および検証用 Postgres 上での
  `SELECT` による初期値確認のみ）。
  ② `npm run build` / `npm run verify:ui` は実行していない。
  ③ **権限のない利用者での403/非表示の画面確認をしていない**（`PUT /stage-probabilities/:stage`
  の `requirePermission('sales', 'manager')` はコードレベルでは `money-rules.routes.ts` と
  同じガードだが、reader 権限で実際に画面を開いて「直す」ボタンが消えることは未確認）。
  ④ 財務ダッシュボードの「営業見通し（パイプライン）」カード・設定画面の
  「フェーズごとの受注確度」編集フォームは、型チェック通過の確認に留まり
  実際にクリックして総額/確度加味の切り替え・保存操作を動かしてはいない。
  ⑤ `getPipelineForecast`（`revenues`/`purchases` を stage 別に集計し確度で重みづけ）は
  検証用 Postgres への実データ投入・突き合わせをしておらず、SQL レビューと
  型チェックのみで検証している。
- **#555**（`fix(sales,finance): 見積の売上取消・回の話数重複・財務ダッシュボードの詳細モーダルを直した`）—
  作成 2026-09-04 11:23:10 JST / CI（`build` 11:25:12・`checks` 11:36:08）とも成功 /
  **CI が緑になった約22秒後（11:36:30）にユーザー自身がマージ**、`get_reviews`・
  `get_review_comments` はいずれも0件（API で直接確認）。この環境では GitHub トークンが
  無く `npm run reviews:debt` は使えないため、GitHub MCP で直接確認した。
  **「指摘なし」ではなく「レビューが届いていない」**（22秒では Codex も間に合わない）。
  ⚠️ **調査は3体の並列 Explore サブエージェントに委譲し（財務ダッシュボードの遷移経路・
  見積/売上のデータモデルと差し戻し設計・回の話数重複の根本原因を独立に調査）、
  実装（migration・サーバーの service/route・クライアントのダイアログ配線）は自分で
  直接書いた PR**。表に移す指摘はない（レビュー自体が届いていないため）が、
  **意図して残した未検証事項**（レビュー0件のため自分で書き出す）:
  ① **実ブラウザでの画面確認をしていない**（PC 1440px・スマホ 375px とも未実施。
  型チェック・lint・shared の Vitest 1996件、および検証用 Postgres 上での直接SQL確認のみ）。
  ② **権限のない利用者での403/非表示の画面確認をしていない**（`revert-to-estimate` の
  `requirePermission('sales', 'editor')` はコードレベルでは既存の `convert-to-revenue` と
  同じガードだが、reader 権限で実際に画面を開いてはいない）。
  ③ 財務ダッシュボードの閲覧専用モーダル（`PurchaseDialog`/`SgaDialog` の `readOnly`）は
  実際にクリックして開閉を確認しておらず、型チェック通過の確認に留まる。
- **#552**（`feat(sales): 回の削除・見積のひとまとまり化・回のフェーズ・売上仕入の全ステージ登録`）—
  作成 2026-09-03 11:38:12 / CI（`checks` 11:41:32・`build` 11:42:21）とも成功 /
  **CI が緑になった約6分後（11:48:44）にユーザー自身がマージ**、`get_reviews`・
  `get_review_comments`・`get_comments` はいずれも0件（API で直接確認）。
  `npm run reviews:debt` はこの環境のトークンでは401になるため使えなかった。
  **「指摘なし」ではなく「レビューが届いていない」**。
  ⚠️ **マルチエージェント（DB移行・見積/回のバックエンドは自分で実装、独立した3領域
  ——売上仕入のフェーズ緩和・回一覧UI・見積の複数選択UI——は並行サブエージェントに委譲）
  で実装した PR**。表に移す指摘はない（レビュー自体が届いていないため）が、
  **意図して残した未検証事項**（レビュー0件のため自分で書き出す）:
  ① **実ブラウザでの画面確認をしていない**（PC 1440px・スマホ 375px とも未実施。
  API を直接叩いた検証用 Postgres での動作確認のみ）。
  ② **権限のない利用者での403/非表示の画面確認をしていない**（回の削除がサーバー側で
  `sales`/`manager` 未満に403を返すことはAPIで確認したが、画面のボタン非表示は未確認）。
  ③ `npm run build` / `npm run verify:ui` は実行していない。
  ④ 「複数の回をまとめて見積をつくる」ダイアログ・回のフェーズセレクトの見た目は
  型チェック・lintでの確認に留まり、実際にクリックして動かしてはいない。
- **#544**（`fix(sales,finance): 未対応と報告された11項目を原因から直し、財務の3台帳をスマホ対応にした`）—
  作成 2026-09-02 16:06:35 / CI（`checks` 16:08:28・`build` 16:08:57）とも成功 / mergeable_state
  `clean` / **CI が緑になった36秒後（16:09:33）にマージされ、`get_reviews`・
  `get_review_comments`・`get_comments` はいずれも0件**（API で直接確認）。
  `npm run reviews:debt` はこの環境のトークンでは 401 になるため使えなかった。
  **「指摘なし」ではなく「レビューが届いていない」**（36秒では Codex も間に合わない）。
  ⚠️ **この行は自分自身も含む記録**である（v4.5.17 の決めごと。「レビュー0件を記録する PR」も
  必ずレビュー0件になるため、棚卸しの PR について新しい行を足すのはここで止める）。
  ⚠️ **意図して残した判断・未検証のまま入れたもの**（レビュー0件のため自分で書き出す）:
  ① **実機のスマホで触っていない**。375px は Chromium のエミュレーションでの実測のみで、
  `npm run verify:ui` も回していない。iOS Safari の safe-area・慣性スクロール・
  シートの高さは未確認。
  ② **権限のない利用者での403確認をしていない**（台帳のスマホ表示で「消す」「一括編集」を
  隠したのは画面側の出し分けで、サーバー側のガードが効くことは確認済みだが、
  reader 権限で実際に画面を開いてはいない）。
  ③ **明細が0件のときのスマホカードの見た目（バッジの枠が残らないか）を見ていない**。
  ④ GLS 発番のガード変更（`c_proposal` から発番可・`migrateEstimates` を受注判定に
  付け替え）は、`issue-gls`/`link-gls`/`relink-gls` の3経路を検証用 Postgres で確かめたが、
  **本番の既存データ（すでに発番済みで `c_proposal` に戻された案件など）での挙動は
  未検証**。

- **#541**（`feat(client,server): 財務ダッシュボード・案件管理の9/2仕様変更18項目を実装`）—
  作成 2026-09-02 06:58:34 / CI（`checks`/`build`）とも成功 07:00〜07:01 / mergeable_state
  `clean` / **約20分待ったがレビュー0件のまま** `get_reviews`・`get_review_comments` とも
  0件（API で直接確認）でマージ（07:19頃）。`npm run reviews:debt` はこの環境のトークンでは
  401 になるため使えなかった。**「指摘なし」ではなく「レビューが届いていない」。**
  財務ダッシュボード8項目・案件管理（見積・売上・レギュラー案件）10項目という広い範囲の
  変更で、migration を3本（268/269/270）足している点は特に注意が要る。
  ⚠️ **意図して残した判断・未検証のまま入れたもの**（レビュー0件のため自分で書き出す）:
  ① **実ブラウザでの動作確認をしていない**（`npm run verify:ui` 未実施。PC 1440px・
  スマホ 375px の実測なし）。ダッシュボードの展開UI・行クリック遷移/モーダル・回編集
  ダイアログ等の見た目は型チェックのみで確認している。
  ② **権限のない利用者での403確認をしていない**。
  ③ 見積→売上変換の`convertToRevenue`は**同時に複数人が同じ見積を版上げ→変換する
  競合**まではverify用Postgresで検証していない（`FOR UPDATE`のロックのみ）。
  ④ 検収書PDFの実際の見た目（期間が行ごとに正しく印字されるか）は、生成したPDFの
  バイナリ確認のみで、実際にレンダリングして目視はしていない。
  ⚠️ **検証中に実害のあるバグを1件発見・修正した**（マージ前に直したので棚卸しには
  積んでいない）: 回（episode）編集API（`PUT /projects/:id/episodes/:id`）が渡さなかった
  項目（収録日・放送日・タイトル・状態・備考）を毎回nullで消していた。これまでこのAPIを
  呼ぶフロントが無く実害が出ていなかったが、今回追加した「回ごとの本数・単価」編集
  ダイアログが初めてこのAPIを実戦投入したことで顕在化した。

| PR | 重み | どこ | 何が起きるか | 状態 |
| --- | --- | --- | --- | --- |
| #508 | P2 | `box-lost-cleanup.service.ts` の `DONE_DATE_SQL` | **終了日・開催日・GPM の終了日が3つとも空の完了案件は、いまも `98_終了案件` へ移らない** — 「終わった」と決める材料がこちらに無いので、意図して対象外にした（推測で動かすと、まだ動いている案件のフォルダを片づけてしまう）。⚠️ **その結果、その案件は誰かが日付を入れるまで現役の場所に残り続け、残っている理由は画面のどこにも出ない** | ❌ 未着手（**直さないと決めたのではなく、出し方を決めていない**。最小実装は「日付が無いので判断できない件数」を帯に出すこと。**#495 の学び — 何もしなかった理由を出さない機能は直しようがない — がそのまま当てはまる**） |
| #508 | P2 | `box-lost-cleanup.service.ts` の `archiveDoneProjectFolders` / `cleanupLostProjectFolders` | **社内側だけ引っ越して、社外側が現役の場所に取り残される** — 本番の BOX で**実測3件**（GLS-A006 / OPP-202606-0002 / GLS-A016）。`box_url_external` が空だと社外側は動かないが、**フォルダ自体は実在する**ので人には「片づいていない」としか見えない。⚠️ **原因の確定に必要だったその3件は、ゴミ案件の抹消で台帳から外れた**ため、いまアプリ側からは辿れない | ❌ 未着手（BOX 側は手作業で 98／99 へ揃えたので実害は無い。**次に同じ形が出たときに実物で確かめてから直す** — 推測で直すと「実物を見ないまま直す」の繰り返しになる。案: 片づけの前に名寄せを片側だけでも通す） |
| #508 | P3 | 開発の進め方（`npm run test` / `npm run typecheck` に効く） | **プレースホルダの数え違いは、テストも型検査も素通りする** — SQL を文字列として組み立てているため、`?` の数と渡す配列の長さが合わなくても**実際に DB へ投げるまで誰も気づかない**。今回は 500 になって初めて分かった | ❌ 未着手（**この形の穴は今後も出る**。最小実装は「`?` の数と引数の長さが合うか」を実行前に確かめる小さな関数を1つ置き、BOX の片づけの問い合わせから使うこと） |
| #505 | P2 | `box-lost-cleanup.service.ts` の `archiveDoneProjectFolders` | **`98_終了案件` への引っ越しを実際の BOX で動かしていない** — 判定（`s_completed` かつ `event_end < 今日`）は実 Postgres で4通り確認したが、**引っ越しそのものは未実行**。日次ジョブから毎朝走るので、**間違っていれば人が押さなくても本番のフォルダが動く**（失注の片づけより影響が広い） | ⚠️ 一部確認（2026-08-31・**本番の BOX で実測**）。引っ越しは**実際に動いた** — 条件を満たしていた GLS-A004 / A007 と GLS-B006 が `98_終了案件` に入っていた（**消えたものは無い**）。⚠️ **ただし条件を満たしていたのに残ったものがあり**、そこから #508 の2つの原因（終了日が空・20秒で3件しか進まない）が出た |
| #504 | P2 | `box-lost-cleanup.service.ts` の `cleanupOrphanFolders` | **「このアプリの実行ユーザーが作ったか」だけが、人のフォルダを守る最後の砦** — 案件に結び付かないフォルダには照らす相手がいないため、`created_by` と `users.get('me')` の一致に寄りかかっている。⚠️ **BOX の認証を別のサービスアカウントに切り替えた日、過去に作ったフォルダは全部「人が作った」と判定されて触れなくなる**（安全側だが、片づかない理由が誰にも分からなくなる） | ❓ 未確認（実行ユーザーの ID をログに出しているので、片づかないときはそこを見る。将来アカウントを変えるなら、この判定の代わりを先に決める） |
| #503 | P3 | `project-purge.service.ts` の `PURGE_DROP_REVENUE_SQL` | **費用を分け合うグループに配っている見込み売上は、永久に落ちない** — 落とすと生きている案件からお金が消えるので1件でも配分があれば手を付けない、という判断。⚠️ **その結果、配分先が全部ゴミになっても親の見込み売上は売上台帳に残り続ける**（誰も気づけない） | ❌ 未着手（**直さないと決めたのではなく、まず数を見る**。「配分ありで落とさなかった件数」を画面に出すのが最小実装） |
| #501 | P3 | 開発の進め方（`docs/reviews/` 全体に効く） | **#495 の棚卸しに ❌ で書いた穴が、そのまま本番で起きた** — 「台帳から外した案件の BOX フォルダは以後どの導線からも片づけられない」と書いておきながら塞がずに出し、利用者が実際に踏んだ（「BOXの削除コマンドが表示されなくなりました」）。⚠️ **棚卸しに書くことは、直さない言い訳にはならない** | ❌ 未着手（仕組みとしての対策が要る。案: ❌ のうち **P2 以上は次のリリースまでに塞ぐか、塞がない理由を書く**を決めごとにする） |
| #495 | P2 | `project-purge.service.ts` / `JunkPurgeBand.tsx` | **台帳から外した案件の BOX フォルダを、以後どの導線からも片づけられない** — 片づけの対象は `deleted_at IS NULL` で絞っているため、外した時点でその案件は候補から落ちる。外す直前に片づけを試し、片づかなかった件数（`boxLeft`）は画面に出すが、**出したあとに片づける手が無い**（安全弁で見送ったフォルダは現役の場所に残り続ける） | ❌ 未着手（**直さないと決めたのではなく、まず #495 の理由表示で「なぜ片づかないのか」を実物で見てから決める**。案としては①外す前に BOX が片づいたものだけを対象にする②片づけの帯に「台帳から外した案件のぶん」も含める、の2つ） |
| #495 | P3 | `projects.deleted_at` / `JunkPurgeBand.tsx` | **「戻せる」と画面に書いたが、戻す導線が画面に無い** — 論理削除にした根拠が「間違えても戻せる」ことなのに、実際に戻すにはシステム管理者が `UPDATE projects SET deleted_at = NULL WHERE ...` を直に叩く必要がある。押した人が自分では戻せない | ❌ 未着手（画面には「戻すにはシステム管理者の操作が要ります」と正直に書いてある。**まとめて外した単位で戻せる**と一番よい＝外した時刻で括れるので、`deleted_at` の範囲指定で戻す導線を設定に置くのが最小実装） |
| #492 | P3 | `project.service.ts` の `BOX_CLEANUP_BUDGET_MS` / `BoxCleanupBand.tsx` の `BATCH` | **1回の押下で実際に何件片づくかを本番で測っていない** — 20秒という予算は「Nginx の 60 秒に対して3分の1」という取り方で決めただけで、**BOX 1件あたりの実際の所要時間を知らない**。遅ければ1往復で1〜2件しか進まず、数百件を片づけるのに画面を開いたまま長時間待つことになる（進み具合は出るので何も分からない状態にはならない）。速ければ5件（`BATCH`）で頭打ちになり、時間を余らせる | ✅ 確認済み（2026-08-31・**本番で実測**）。**20 秒で3件**だった（`98_終了案件` への引っ越し）。⚠️ **遅かった原因は BOX が遅いことではなく、こちらが同じ問い合わせを案件ごとに繰り返していたこと** — 置き場の解決を1回にまとめて #508 で直した。予算 20 秒はそのまま |
| #489 | P2 | `box-lost-cleanup.service.ts` の `relinkProjectFolders` | **名寄せ（BOX の親フォルダを一覧して案件に結び付け直す）を実際の BOX に対して1度も動かしていない** — この環境も検証DBも `BOX_CONFIG_JSON` を持たない。どのフォルダをどの案件のものと見なすかの判断は純関数にして 38 件のテストで固定してあるが、**親フォルダに実際どんな名前のフォルダが並んでいるか**は本物でしか分からない。**まだ知らない命名規則があれば、その案件は片づかないまま残る**（消えるより安全側に倒れてはいる） | ✅ 確認済み（2026-08-31・**本番で実測**）。名寄せは動いており「120 件を調べて 94 件を結び付けた」→ 片づけが進んだあと「77 件中 64 件」。⚠️ **結び付かない 13 件が残る**ことも分かり、#504 でそのぶんを片づける導線を足した |
| #487 | P2 | `box-lost-cleanup.service.ts` / `types/box-node-sdk.d.ts` | **実際の BOX に対して1度も動かしていない** — この開発環境も検証DBも `BOX_CONFIG_JSON` を持たないため、`folders.delete` と `folders.update({ parent })` の実呼び出しは未確認（未接続では全経路が no-op になることだけ確認済み）。危険な判断（触ってよいか・空か）は純関数に切り出して 27 件のテストで固定してあるが、**BOX が実際にどう返すか**（削除の 400、引っ越し後の親、権限）は本物でしか分からない | ⚠️ 一部確認（2026-08-31・**本番の BOX を読んで実測**）。**中身ありが `99_失注・見送り` へ移ること**（5件）と**空フォルダが消えること**（親フォルダ 47 → 40 件）は実物で確認できた。**失注から戻すと元に戻る**だけは未確認 |
| #487 | P3 | `server/src/types/box-node-sdk.d.ts` | **`folders.delete` の型を生やした** — これまで「BOX に作ったフォルダはこのアプリからは消せません」という決めごとを、型定義から外すことで機械的に守っていた。安全弁を通ったときだけ呼ぶ決めは `boxLostCleanup.test.ts` が固定しているが（削除は1か所だけ・安全弁と空判定のあと）、**型が生えた以上どこからでも呼べる**。次に BOX 周りを触る回に、他所から呼ばれていないか見る | ❌ 未着手（**直さないと決めたのではなく、見張り続ける項目**。テストは「削除は1か所」を数えているので、増えれば落ちる） |
| #482 | P2 | `scheduler.service.ts` の `equipmentOverdue` | **機材の返却遅れが借用者本人に届かない** — `equipment_lendings` に利用者と結び付く列が無く、借用者は `borrower_name`（氏名の文字列）だけ。`users.id` を持つのは `lent_by` / `returned_by`（社内の担当者）のみ。**返す当人のベルには出せず、受け取った人が声を掛けるしかない**ので、押しても消えない通知になる。直すには貸出に `borrower_user_id` を足し、機材アプリの貸出画面ごと変える必要がある | ❌ 未着手（**直さないと決めたのではなく、範囲が別**。#482 では本文とコードに「届きません」と明記するに留めた。貸出画面に手を入れる回に一緒に直す） |
| #482 | P2 | `ai-output.service.ts` の `getFeedbackDigest('inquiry_intake')` | **「ストック→見直し→見送り」率を数えていない**（AIフィードバックループの条件3の穴）。`inquiry.dropped_rate` は「AI が拾いすぎているか」の唯一の指標だが、#482 以前は**ストックが事実上の見送りだったため実態を過小評価**していた。247 でストックが本当に戻るようになったので指標は本来の意味を取り戻すが、ストック経由で最終的に見送られた率は依然として見えない | ❌ 未着手（**新規テーブル不要**で出せる: `state='dropped' AND stock_review_on IS NOT NULL` を数え、`stock_then_dropped_rate` として digest に足すのが最小実装） |
| #395 | P2 | `equipment.tools.ts` / `production.tools.ts`（新設のMCPツール） | **実際のOAuthフロー・実DBでこの開発セッションから呼び出し確認していない**（typecheck・lint・testのみ）。特に貸出/返却の二重貸出ガード（`ALREADY_LENT`）・スケジュール表write系のエラー変換（`HttpError`→`AppError`。read側と揃えたが実リクエストでは未確認）・`gate.ts`の権限ゲート（reader/editorの実際の拒否挙動）は実機での確認が要る | ❓ 未確認（検証環境での確認待ち） |
| #388 | P2 | `client-techops/src/components/layout/AppShell.tsx` | **`appKey="qsheet"`→`"techops"` の修正・「ユーザー管理・権限」マニュアルの書き直しを実ブラウザで確認していない** — 特にアプリ切替ボタンの表示崩れ（「ONAiR」フォールバック）が直っているかは検証環境での見た目確認が要る | ❓ 未確認（検証環境での確認待ち） |
| #387 | P3 | 実機点検の11件修正・全体 | **実ブラウザ（PC/スマホ）・実DB保存の確認をこの開発セッションからは実施していない**（typecheck・lint・test・build のみ通過確認）。特に⑦（収録設定・配信設定・レンタル機材検索の実施日デフォルト）は複数画面をまたぐ非同期の状態遷移を含み、実機での見え方の確認が要る | ❓ 未確認（検証環境での確認待ち） |
| #380 | P2 | `rental-scraper/restar_scraper.py` | **修正はサンドボックスから実サイト（`restargp.com`）へ接続できないまま行っている** — 連続404の打ち切り閾値が「1件も見つかっていない」段階から効いていた点は直したが、実際のHTML構造・実際の商品ID分布に対して正しく効くかは未検証。デプロイ後、検証環境の `rental_scraper_dev` ログで実際に件数が取れているか確認要 | ❓ 未確認（検証環境ログ待ち） |
| #366 | P3 | `server/src/shared/db/seed.ts` | **シードの日付が固定（2026年3〜4月）のため、時間が経つと既定の期間絞り込みから外れる** — R5 層2 の撮影（2026-08）で案件一覧・財務の一部が0件表示になっていた。壊れではないが、検証環境で「データが入った一覧」を既定表示で見られない。日付を実行日からの相対にする改善を検討 | ❌ 未着手 |
| #365 | P3 | 機材 貸出の決めごと（`/equipment/rental-settings`） | **既存のバッジ右端ずれ**（右端 801/828.5）— #365 の実走が発見したスコープ外の既存崩れ（R5 の6件には含まれない機材アプリの件） | ❌ 未着手 |
| #353 | P3 | `scripts/verify-ui.mjs` の潰れ検知 | **判定式（`clientWidth<24 && scrollWidth>clientWidth`）が「和文が1文字ずつ縦に折り返される崩れ」の一部（幅14〜23px）を原理的に取りこぼす** — 折り返しは scrollWidth を増やさないため。R6-c の反証エージェントが実ブラウザで再現して確定。今は報告のみなので実害は小さいが、**0件必須へ格上げする前に判定の補強が要る**（例: 1文字/行の行数判定を併用） | ❌ 未着手 |
| #325 | P2 | 案件を選ぶ7フォーム | **案件から引けるのに手入力させている** — 機材の貸出登録（使いみち・貸出日・返却予定日）／財務の仕入登録（役務完了日。ここから計上月・支払予定日が連鎖する）／タスク追加（担当・期日）／制作技術支援のスケジュール表作成（題・日付）・収録設定・配信設定（実施日）。#325 で足した `GET /lookup/:projectId/context` を使い回せる | ❌ 未着手（#325 が既に56ファイルのためスコープ外にした） |
| #325 | P2 | ~~`client-live/SessionHomePage.tsx:50`~~（画面ごと削除済み） | 計時・視聴者のセッション作成が `GET /projects` を直叩きしており、この口は `sales` 権限を要求するため **live 権限だけの利用者は 403 になり得る**（#325 の調査中に見つけた別件・実機未確認） | ⭕️ 対象コード消滅 — 記録の回収（#356）時点で当該ファイルは main から削除済みで、後継の計時・視聴者画面は `GET /projects` を呼ばない（R6-c 事後レビューで確認・2026-08-23） |
| #325 | P3 | v4 `Sheet` の PC 判定 | PC 切替が `lg:`(1024px) なので **768〜1023px のタブレット縦が全幅ボトムシートのまま**。横に余裕があるのに1カラムに潰れる。`md:` への引き下げは見た目が大きく変わるため #325 では見送り、コードにコメントを残した | ❌ 見送り（別PRで判断する） |
| #105 | P2 | `doc-box.service` | 昔の綴りのフォルダに入れたのに、表の綴りで「保存しました」と出る | ⭕️ |
| #103 | P2 | `MinutesTab` | 文字起こしを一度も開けない（一覧は本文を積まないのに本文の有無で出し分け） | ⭕️ #104 |
| #103 | P2 | `MinutesTab` | editor に「消す」が出て、押しても何も起きない | ⭕️ #104 |
| #103 | P2 | `gpm.service` | 持ち帰り→未確認事項が同時押しで2件できる | ⭕️ #104 |
| #102 | **P1** | `InvoiceRows` | `sales` だけの人が請求書 PDF を出すと **403**（口が `budget` を要求） | ⭕️ #110 |
| #102 | **P1** | `ClosingRows` | ② 締め処理の「請求書を出す」が**発行の流れを迂回**（`blocked` でも出る・請求書番号が付かない） | ⭕️ 済（締めのまとめ処理は申込書も番号も見ている。#112 で確認） |
| #102 | **P1** | `estimates.routes` | **値引きの承認前**の見積を社外フォルダに保存できる | ⭕️ #112 |
| #101 | P2 | `venue.ts` | 同名の部屋が2つある会場で、予約が1件消える | ⭕️ #123 |
| #101 | P2 | `next-action-short.service` | 失敗の印が**別の本文に付く**（以後この行は二度と処理されない） | ⭕️ #123 |
| #99 | P2 | `RequiredFields` | GLS-B を直すと A 用の分類が付き、分類が壊れる | ⭕️ #134（画面 ＋ **サーバー側でも落とす**） |
| #98 | P2 | `.env.example` | 手順どおり `AI_PRICING_JSON` を差し替えると**過去の費用が消える** | ⭕️ #134（説明 ＋ **落ちたモデルを画面に名前で出す**） |
| #97 | P2 | `OverviewTab` | ぶら下がる作業の件数が概要に出ない（見出しだけ読んで見落とす） | ⭕️ #123 |
| #97 | P2 | `nextAction.ts` | 行の途中の丸数字を拾わない（1件にまとまる） | ⭕️ #123 |
| #97 | P2 | `nextAction.ts` | `-10万円` の頭のマイナスが**印として食われる** | ⭕️ #123 |
| #96 | P2 | `ai-model.ts` | `AI_MODEL_LIGHT=off` が**モデル名として送られる**（必ず失敗してから heavy に落ちる） | ⭕️ #127 |
| #96 | P2 | `ai-model.ts` | 動的な `process.env[name]` は検査から見えない（`.env` から落ちても lint が通る） | ⭕️ #127 |
| #95 | P2 | `check-env-passthrough` | prod/dev の**片方だけ**に渡していても OK と出る | ⭕️ #127 |
| #93 | P2 | `ThreadCard` | 旧データで「もう一度整える」が空振りし、`ai_corrections` を汚す | ⭕️ #135（**空振りどころか「戻しました」と嘘をつく**） |
| #91 | **P1** | `docker-compose` | `ACTIVITY_FORMAT_NIGHTLY=off` が**コンテナに渡っていない**（止めたつもりで課金が続く） | ⭕️ #95 |
| #91 | P2 | `scheduler.service` | 夜間の整形が **03:00 UTC = 正午 JST** に走る | ⭕️ #106 |
| #90 | P2 | `OverviewTab` | 「すべて見る」が `?tab=` でタブを開けない | ⭕️ #99 |
| #90 | P2 | `OverviewTab` | やり取りの件数が**常に 20 件**（`pagination.total` を使っていない） | ⭕️ #123 |
| #90 | P2 | `noteText.ts` | 行の途中の「」で**以降が全部引用に飲まれる** | ⭕️ #123 |
| #90 | P2 | `ThreadDigest` | 日付が読み上げから外れている（`aria-hidden`） | ⭕️ #123 |
| #87 | **P1** | `estimate.service` | 受注→売上の変換が**同時押しで2件**（同じ売上が二重計上） | ⭕️ #111 |
| #87 | **P1** | `estimate.service` | 値引きが明細に写らず、**他の画面が定価で数える** | ⭕️ #111 |
| #87 | **P1** | `RevenueBillingPane` | 按分（グループ）の金額を**満額で足す**（1案件の粗利が他案件の分だけ膨らむ） | ⭕️ #112 |
| #87 | P2 | `RevenueBillingPane` | 100件を超える売上・仕入が合計から落ちる | ⭕️ #112 |
| #87 | P2 | `estimate.service` | 変換後も案件の想定金額が古いまま | ⭕️ #123（**想定金額は上書きしない**。AI の成果を数えるほうを確定売上に直した — 理由は下の 10.） |
| #87 | P2 | `estimates.routes` | 経理だけの人が受注→売上の変換に**到達できない** | ⭕️ #111 |
| #86 | P2 | `useBufferedValue` (qsheet) | フォーカス中に届いた他人の変更を二度と取り込まない | ⭕️ |
| #84 | P2 | `vendor-fonts` | 書体の断片が1つ壊れても `--check` が通る | ⭕️ #129 |
| #83 | **P1** | migration 185 | 旧レポート画面が `highlights` を読んでいて**開くと落ちる** | ⭕️ #113 |
| #83 | **P1** | `keep-report.service` | **未確認の AI 下書き**が確定レポートに載る | ⭕️ #113 |
| #83 | **P1** | `projects.routes` | 写真サムネが**持ち主を確かめない**（案件外のファイルも取れる） | ⭕️ #106 |
| #83 | P2 | `scheduler.service` | 同じ日に終わる案件が20件を超えると、残りに KPT が作られない | ⭕️ #133 |
| #82 | P2 | `AppShell` | パンくずがクエリ違いの項目を取り違える | ⭕️ #125 |
| #82 | P2 | `AppShell` | パンくずが**権限で隠した項目の名前**を出す | ⭕️ #125 |
| #81 | P2 | `projects.tools` (MCP) | `update_project` の分類が**黙って無視される**（`updated: true` は返る） | ⭕️ #117 |
| #80 | P2 | `useNewProjectForm` | `?inquiry=` から開いても**引き合いの中身が写らない** | ⭕️ #117 |
| #80 | P2 | `project.service` | 旧 API 経由で「客入れなし」なのに来場人数が残る | ⭕️ 済（`create` / `update` とも落とす・#117 で実測） |
| #79 | **P1** | `Recorder` | 下読みが**5分まるごと**を毎回送る（費用削減が効いていない） | ⭕️ #114 |
| #79 | **P1** | `useIntake` | 画像の縮小が終わる前に送れて、**添付が黙って消える** | ⭕️ #114 |
| #79 | P2 | `intake-ai.service` | 失敗した light の呼び出しが記録されない（費用が過少に出る） | ⭕️ #114 |
| #79 | P2 | `minutes-ai.service` | 失敗した整形が記録されない（同上） | ⭕️ #114 |
| #78 | **P1** | `Recorder` | 画面を離れた後にマイクが**掴まれたまま**になる | ⭕️ 済（`useEffect` の後始末で解放済み・#114 で確認） |
| #77 | P2 | `tasks.routes` | 録音からの取り込みで、飛ばした行・規則落ちの警告が出ない | ⭕️ #118 |
| #77 | P2 | `task-intake.service` | 一覧が「処理中」のまま永久に残る（詳細だけ復帰処理がある） | ⭕️ #133（**詳細も直っていなかった** — 判定が必ず `Invalid Date` で、一度も効いていない） |
| #76 | P2 | `useIntake` | 複数できたのに1件のときと同じ画面遷移で、**残りに気づけない** | ⭕️ #137 |
| #76 | P2 | `minutes-ai.service` | 再試行込みで nginx の60秒を超えうる | ⭕️ #137（**整形は裏で走るので該当せず**。実際に超えるのは `intake-ai` の 120 秒で、そちらを直した。下読みの文字起こしも 20 秒→40 秒になっていた） |
| #76 | P2 | `integrations.routes` | BOX が**壊れた設定でも「つながっています」**と出る | ⭕️ #129 |
| #75 | **P1** | `tasks.routes` | 長い録音を同期で処理して**60秒で切れる** | ⭕️ 済（migration 180 で 202 ＋ 裏で処理・#114 で確認） |
| #75 | **P1** | `tasks.routes` | `dailyops` だけの人に**案件80件の名前・顧客**が返る | ⭕️ #106 |
| #75 | **P1** | `task-intake.service` | 議事録の修正差分が**取り込みの出力に混ざる**（指標が壊れる） | ⭕️ #114 |
| #75 | P2 | `intake-ai.service` | Anthropic が落とす画像形式を受け付けて、**添付が無視される** | ⭕️ #118 |
| #74 | **P1** | `AppTopbar` | 長いアプリ名で**ログアウトの入口が画面外に切れる** | ⭕️ 済（1024px で実測・本人メニューの右端 1000px／パンくずは縮む・#115 で確認） |
| #74 | P2 | `tokens-v4.css` | 上辺バーの畳みが効かなくなった | ⭕️ #94 で機能ごと削除 |
| #74 | P2 | `tokens-v4.css` | スマホのバッジが均等割り付けに戻った | ⭕️ 後に廃止 |
| #73 | P2 | `pcOnlyScreens` | スマホで電話番号を見に来たのに、電話番号だけ出ない | ⭕️ #140（**探すの行に押せる番号を出す**。お客様の画面は PC 専用のまま） |
| #72 | P2 | `AppTopbar` | `/search` でもロゴが出て、見出しと操作を押し出す | ⭕️ 済（**あとの決定で変わった** — スマホの上辺バーは全画面「ロゴだけ」が正（`v4-live`）。375px で実測: 上辺バー 64px・検索欄 351px・**横はみ出し 0px**。#140 で確認） |
| #71 | P2 | `google-fonts-cache` | 取り置きの書体に CORS が無く、検査が代替書体で測る | ⭕️ #144（**代替書体では測っていなかった** — 実測で Bebas 157.36px / Roboto 208.11px と書体ごとに違う幅が出る。ただし `@font-face` は本来 CORS を要求する経路なので、**版が変わった日に黙って代替書体になる**のを防ぐため1行付けた） |
| #70 | P2 | `SearchPage`(機材) | 子機材が検索で出ない | ⭕️ #119 |
| #70 | P2 | `SearchPage`(機材) | 「置き場所」で探せない（案内文と食い違う） | ⭕️ #119 |
| #70 | P2 | `SearchPage`(機材) | 全角で探せない（案内文と食い違う） | ⭕️ #119 |
| #70 | P2 | `collapseOnScroll` | ゆっくり動かすと畳みが一生効かない | ⭕️ #94 で削除 |
| #70 | P2 | `SearchPage`(日常) | スマホで押すと PC 専用の案内に行き止まる | ⭕️ 済（**スマホでは押せる行にせず、要約まで出して答えにしている**。理由もコードに書いてある。#140 で確認） |
| #69 | P2 | `MobileFilters` | 「ぜんぶ外す」で付属品の表示が外れない | ⭕️ #142 |
| #69 | P2 | `MobileFilterBar` | 用語の説明がシートの下に隠れて見えない | ⭕️ 済（**押すとシートを先に閉じる**形になっていた。375px で実測 — 説明は上から 339px に出て見えている。#142 で確認） |
| #68 | P2 | `SearchPalette` | **前の利用者の**案件名・顧客名が残って出る | ⭕️ #106 |
| #68 | P2 | `SearchPalette` | 古い検索結果が新しい語の下に出る（Enter で違う物が開く） | ⭕️ #122 |
| #68 | P2 | `AppTiles` | 件数バッジを押しても何も起きない | ⭕️ #122 |
| #67 | **P1** | `gpm.service` | GPM の受注が **GLS-B を発番しない**（履歴も残らない） | ⭕️ #115 |
| #67 | **P1** | `BillingTab` | 月次モードにならない | ⭕️ #115 |
| #67 | **P1** | `BillingTab` | `gpm` だけの人は請求タブが**開いた瞬間エラー** | ⭕️ #115 |
| #67 | P2 | `queries.ts` | `gpm` だけの人は顧客を選べず、案件を作れない | ⭕️ #115 |
| #66 | P2 | `tokens-v4.css` | スマホで `<h1>` が消え、読み上げで現在地が分からない | ⭕️ 済（**M7 の `[data-page-title] { display:none }` を戻したときに直っていた**。375px で4画面を実測 — `<h1>` は 1 個・`visible`・20px。#139 で確認） |
| #65 | P2 | `MobileFilterBar` | 既定の半年絞り込みが「絞り込みなし」と表示される | ⭕️ #139（**枠には期間が出ていた**が、件数の文が**「全4件」**と言い切っていた。実データ 46 件） |
| #63 | **P1** | `scheduler.service` | **夜間ジョブが UTC で走る**（09:00 のものが 18:00 JST） | ⭕️ #106 |
| #63 | **P1** | `gpm/index` | `/gpm/estimates/summary` が `/:id` に食われて 404 | ⭕️ |
| #63 | **P1** | `estimate.service` | `can_estimate = false` が**効いていない** | ⭕️ #107 |
| #63 | **P1** | `estimate.service` | 値引きの限度を**送った人**の役割で見る（作った人ではない） | ⭕️ #107 |
| #63 | **P1** | `estimates.routes` | **承認する画面が無い**ので pending の見積は誰も送れない | ⭕️ #109 |
| #63 | **P1** | `money-rules.service` | 休業日シフトが支払期日に効いていない | ⭕️ #116 |
| #63 | P2 | `studio-booking.service` | 予約を直しても時間外の印が更新されない | ⭕️ #138 |
| #63 | P2 | `minutes.service` | 持ち帰り→タスクが同時押しで2件できる | ⭕️ #104 |
| #62 | **P1** | `MobileNewProject` | スマホだと**同じ引き合いから案件が2件**作れる | ⭕️ #116 |
| #62 | P2 | `notice.tsx` | 同じ文言の2回目の帯が動かない（気づけない） | ⭕️ #131 |
| #61 | **P1** | `offlineQueue` | 送信中に付け直した印が**古い値で上書き**される | ⭕️ #113 |
| #61 | **P1** | `MobileScanSession` | **完了した棚卸し**をスマホから書き換えられる | ⭕️ #113 |
| #61 | **P1** | `SearchPage` | 前の利用者の「最近見たもの」が残って出る | ⭕️ #106 |
| #61 | P2 | `ProjectListPage` | スマホで**21件目以降が開けない**（ページ送りが無い） | ⭕️ #122 |
| #61 | P2 | `MobileCollect` | 先月以前の未入金が**画面から消える** | ⭕️ #125 |
| #60 | P2 | `MeetingRecordPage` | 51件目以降の案件を選べない | ⭕️ #122 |
| #60 | P2 | `MeetingRecordPage` | 文字起こしが使えない環境でも録音を始められる | ✅ すでに直っていた（`stt.data === false` でボタンを止めている） |
| #60 | P2 | `MeetingRecordPage` | 「やり取りを開く」が概要に落ちる（`log` タブは無い） | ⭕️ #122 |
| #60 | P2 | `HomePage` | reader に出して 403 に送る／`dailyops` の入口が消える | ⭕️ #110 |
| #59 | **P1** | `RoomAvailabilityPage` | 終日の予約が空きに見える | ⭕️ |
| #59 | P2 | 3件 | （同上・解決済み） | ⭕️ |
| #58 | **P1** | `RoomAvailabilityPage` | 終日予約が帯にならない | ⭕️ #59 |
| #58 | **P1** | `availability.ts` | 日をまたぐ予約が**違う時間帯に描かれる** | ⭕️ #59 |
| #58 | **P1** | `App.tsx` | カレンダー設定の権限が API と食い違う（どちらの人も使えない） | ⭕️ 済（タブを権限で出し分け・#110 で確認） |
| #58 | P2 | `HoldListPage` | editor に「落とす」が出て 403 | ⭕️ 済（`canDrop`・#110 で確認） |
| #58 | P2 | `SearchPage` | 古い検索結果が新しい語を上書き | ✅ すでに直っていた（投げた順の番号を持っている） |
| #58 | P2 | `SearchPage` | 失敗しても「探しています…」のまま | ✅ すでに直っていた（`failed` で断りを出す） |
| #57 | **P1** | `project.service` | 受注で GLS を発番すると**放送種別・媒体が消える** | ⭕️ #113 |
| #57 | **P1** | `ops-report.service` | **公開済みの週報**に後から項目が足せる | ⭕️ #113 |
| #57 | **P1** | `apps.ts` | GPM を出したのに試験が古いまま（`npm test` が落ちる） | ⭕️ |
| #57 | P2 | `ops-report.service` | 週報に**UUID が名前として**出る | ⭕️ #113 |
| #57 | P2 | `equipment.routes` | UUID で読んだ機材が「見つからない」と履歴に残る | ⭕️ #132 |
| #57 | P2 | `invoice-number.service` | 同時押しで**請求番号を1つ無駄に消費**し、嘘の番号を返す | ⭕️ #111 |
| #56 | **P1** | `inbox.routes` | `dailyops` だけの人が**仕入・販管費に直接書ける** | ⭕️ #106 |
| #56 | **P1** | `doc-handoff.service` | 引き渡しが同時に走ると**台帳に二重の行** | ⭕️ #111 |
| #55 | **P1** | `HomePage` | 権限が無くて数えていないのに「期限切れはありません」と言う | ⭕️ #110 |
| #55 | P2 | `TodayCard` | 収録・放送の予定が**名前なし**で出る | ⭕️ #132 |
| #55 | P2 | `HomePage` | 個人のタスクの行き先が `sales` の一覧（403 になる人がいる） | ⭕️ #110 |
| #55 | P2 | `MyTasksCard` | reader にチェックボックスが出て 403 | ⭕️ #110 |
| #53 | **P1** | `billing.routes` | **グループ請求が一覧に1行も出ない**（入金・検収も付けられない） | ⭕️ #111 |
| #53 | P2 | `billing.routes` | 300件で黙って切れる（総額も件数も合わない） | ⭕️ #122 |
| #53 | P2 | `billing.routes` | 見積の行を「入金済み」にできてしまう | ⭕️ #112 |
| #52 | **P1** | `salesOverview.service` | ダッシュボードの「今日」が **UTC**（朝9時前は前日） | ⭕️ #106 |
| #52 | **P1** | `project-ai-feedback` | 直さずに承認した AI 案が**指標に入らない**（受入率が常に0） | ⭕️ #114 |
| #52 | **P1** | `project.service` | **MCP が直した分**を人の修正として記録する（指標が汚れる） | ⭕️ #114 |
| #52 | P2 | `ai-feedback.service` | 同額の案件を1件に潰して合計する | ⭕️ #123 |
| #52 | P2 | `PricingListPage` | 並べ替えが途中で失敗すると順番が壊れる | ⭕️ #138 |
| #51 | P2 | `FilesTab` | フォルダを作った直後も「まだありません」のまま | ⭕️ #122 |
| #51 | P2 | `box.ts` | 100件を超えるファイルが**黙って出ない** | ⭕️ #122 |
| #51 | P2 | `FilesTab` | 通信が失敗しても「まだ何も入っていません」と出る | ⭕️ #122 |
| #50 | **P1** | `EstimateTab` | 旧見積（`revenues`）の作成口が**まだ開いている**（売上に混ざる） | ⭕️ 済（作成口はもう無い。#112 で確認） |
| #50 | **P1** | `estimate.service` | 出したあとの見積を**サーバーが書き換えられる**（画面の鍵だけ） | ⭕️ #112 |
| #50 | **P1** | `estimate.service` | 明細の入れ替えが途中で失敗すると**前の明細が消える** | ⭕️ #112 |
| #50 | **P1** | `EstimateTab` | 出した版・受注した版を**消せてしまう** | ⭕️ #112 |
| #50 | P2 | `TaskDashboardPage` | ガントに絞り込みが効かない | ⭕️ #142（**原因は2つ** — 渡す値が `scoped` だったのと、**道が変わって絞り込みが既定に戻っていた**） |
| #49 | P2 | `apps.ts` | 「システム管理」を押すとホームに戻る（`/admin` のルートが無い） | ✅ すでに直っていた（v4 の改名で `/settings` になり、ルートもある） |
| #48 | P2 | `queryClient` | `mutate(..., { onError })` の画面でも共通の帯が二重に出る | ⭕️ **測って確かめた** — 受け皿は確かに黙れない（per-call は `mutation.options` に入らない）が、**受け皿が先・画面があと**で帯は1つしか持たないので**画面の文言が残る**（利用者に見えるのは正しいほう）。⚠️ **順番のおかげ**なので `queryClient.test.ts` に3項目で固定した |
| #48 | P2 | `client/main.tsx` | 案件管理だけ共通の受け皿を使っておらず、**失敗が黙る** | ⭕️ #131 |
| #46 | P2 | `PricingListPage` | スマホで金額が改行される（`Money` が flex） | ⭕️ 済（**再現せず**。375px で「それでも開く」から開いて実測 — `Money` は 128px・**高さ 24px の1行**・横はみ出し 0px。`justify-between` で数字が右端に寄るのは意図どおり。#141 で確認） |
| #45 | P2 | `base.css` | `tabular-nums` だけの金額に `palt` が掛かり桁がずれる | ⭕️ 済（**前提が違った** — `palt` は数字の幅を1ピクセルも変えない。ただし**`tnum` も効いていない**ことが分かった（下記）。#141 で実測） |
| #43 | P2 | `tailwind.preset` | `text-card` が色と衝突（→ `cardtitle` に改名済み） | ⭕️ |
| #43 | P2 | `check-tokens` | 例外の正規表現が広すぎ、`--border` の欠落を見逃す | ⭕️ #143（`--surface` も巻き込んでいた。**反証: `--border` を落とすと前の版は OK で通る**） |
| #40 | P2 | `client-daily` | iPhone の下端に操作が隠れる（`safe-area` 不足） | ⭕️ #143（**指摘の形では再現せず** — 下タブもシートも逃げを持っている。**下タブが 0 本のときだけ主アクションが重なる**ので、そこを塞いだ） |
| #39 | P2 | `check-shared-wiring` | 二重引用符の import を検査が見落とす | ⭕️ #129（落ちてはいたが**理由が嘘**だった） |

- **#529**（`feat(sales): 見積・売上・仕入の改善4件を実装した`）—
  作成 2026-09-01 10:32:55 / CI（`checks`/`build`）とも成功 / マージ 10:35:19（**約2分半で
  マージ**）。**`get_reviews` 0件・`get_comments` 0件**（API で直接確認。`npm run reviews:debt`
  はこの環境のトークンでは 401 になるため使えなかった）。**指摘なしではなく、レビューが
  届いていない**。見積の受注→売上変換・見積明細の定価表記・「回を足す」の採番という
  お金と一意性に関わる変更を含むため、レビュー0件のまま入った点は特に注意が要る。
  ⚠️ **意図して残した判断・未検証のまま入れたもの**（レビュー0件のため自分で書き出す）:
  ① **実ブラウザでの動作確認をしていない**（`npm run verify:ui` 未実施。PC 1440px・
  スマホ 375px の実測なし）。追加した「定価／値引き」の注記・単位入力・ドラッグハンドルの
  見た目は型チェックとユニットテストのみで確認している。
  ② **実 Postgres への保存→読み直しを確認していない**（`npm run verify:up` 未実施）。
  migration 261（`estimate_items.list_unit_price`）・売上上書きの `UPDATE`/`DELETE`→`INSERT`・
  話数の採番トランザクションは、いずれも SQL 文字列の静的テストで固定しただけで、
  実データベースに対しては未検証。
  ③ **権限のない利用者での403確認をしていない**。
  ④ 売上の上書き（`convertToRevenue` の sibling 検出）は、**同時に複数人が同じ見積を
  版上げ→変換する競合**までは検証していない（`FOR UPDATE` で行ロックはしているが、
  実サーバーでの同時実行テストは行っていない）。
  ⚠️ **この棚卸し自体（#531）については、#528 の決めごとに従い新しい行を足さない**
  （下記「この行で止めます」参照 — 棚卸しの PR がレビュー0件で通ることは自明で、
  そのたびに書くと無限に続くため）。

- **#528（`docs(reviews): #520/#522/#523/#524/#526/#527 の棚卸しを記録し ⑨ を ⭕️ にした`・2026-09-01）** —
  **`get_reviews` 0件・`get_review_comments` 0件**（API で直接確認）。作成 10:32 / CI 緑 10:34 /
  マージ 10:34。⚠️ **棚卸しの PR 自身もレビュー0件で通っている**。
  ⚠️ **この行で止めます。** 「レビュー0件を記録する PR」も必ずレビュー0件になるので、
  そのたびに新しい PR を出すと**無限に続きます**。**この1行が自分自身も含む記録**です
  （棚卸しの PR がレビュー0件で通ることは、以後わざわざ書かなくてよい）。
  ⚠️ **本当の問題はこちら** — 2026-09-01 だけで **#512・#514・#515・#517・#520・#522・
  #523・#524・#526・#527・#528 の11本が、1件のレビューも無くマージされている**。
  うち #526・#527 は **+2,673 / −1,955・28 ファイル**。**書き出す運用は回っているが、
  レビューが届く運用は回っていない**。次に変えるとすれば「記録の書き方」ではなく
  **「マージまで待つ時間」**（Codex は数分で付く）。

- **#520 / #522 / #523 / #524 / #526 / #527（2026-09-01）** — **6本すべて `get_reviews` /
  `get_review_comments` とも 0件**（API で直接確認。`npm run reviews:debt` はこの環境の
  トークンでは 401 になるため使えなかった）。**「指摘なし」ではなく「レビューが届いていない」**。
  #520 は Excel・CSV・MCP の列を増やす変更、#522〜#524 は台帳の検索と一覧 API に触る変更、
  #526・#527 は **2,032 行を 16 段に分ける変更（+2,673 / −1,955・28 ファイル）**で、
  **どれも1件のレビューも無いままマージされている**。
  ⚠️ **#526・#527 で意図して残した判断**（レビュー0件のため自分で書き出す）:
  ① **`RevenueItemRows.tsx` は PC の表とスマホのカードで同じ項目を2度書いたまま**にした
  （`hidden sm:block` と `sm:hidden` の別 DOM）。1枚にまとめると見た目が変わるため触っていないが、
  **片方だけ直すともう片方から欄が消える**穴は残っている（ファイル先頭に警告を書いた）。
  ② **`isEstimateMode` の実質デッドな分岐を消していない**（`BillingTab` が渡していないことを
  テストが見張っており、検証できないため）。
  ③ **`key={idx}` を安定 id に直していない**（入力中のフォーカスと再マウントの挙動が変わるため）。
  ④ **`useCallback` / `useMemo` を1つも足していない** — 依存配列を付けると値引き・
  シミュレーション・インライン編集が古い値を掴む。**分割で props 経由の再描画は増えている**ので、
  遅いと分かったときに**実測してから**入れること。

- **#514 / #515 / #517** — いずれも **`get_review_comments` 0件**（API で直接確認）。
  `npm run reviews:debt` はこの環境のトークンでは 401 になるため使えなかった。
  **「指摘なし」ではなく「レビューが届いていない」**。#514 は**本番で 500 になっていた
  不具合の修正**（仕入の検索）で、レビューが1件も無いままマージされている。

- **#512**（`fix(finance): 財務ダッシュボードの3件（期間解除のエラー・504・役務提供完了日）を直した`）—
  作成 2026-09-01 03:30:27 / CI（`checks`/`build`）とも成功 03:32:46 / マージ 03:43 頃。
  **`get_review_comments` 0件・`get_reviews` 0件**（API で直接確認。`npm run reviews:debt` は
  この環境のトークンでは 401 になるため使えなかった）。**指摘なしではなく、レビューが届いていない**。
  意図して残した判断は下の表に自分で書き出してある。
  ⚠️ **この回のマージで `main` が赤くなった** — #511（テロップCG）が先に 250〜258 を使っており、
  こちらの `250_finance_recognition_date_index.sql` と番号がぶつかった（#516 で 259 へ改名）。
  **番号を「枝を切った時刻」で取っている限り、並行して開発すると必ず起きる**
  （`docs/changelog.d/` が版の番号について解いたのと同じ問題が、マイグレーションに残っている）。

- **#570 / #569（2026-09-05・スタジオ予約の担当者）** — ⚠️ **同じご依頼から並行して2つの実装が走り、
  片方（#567）が先にマージされた。** どちらも `studio_booking_assignees` という同じ表を
  **同じ 279 番**で作っており、後から出したほう（#569）は**全面的に重複**したためクローズした
  （main の #567 の実装のほうが、担当者チップを `AssigneePicker` としてパートナースケジュールと
  共用しており良い形だった）。**マイグレーション番号の衝突（#511/#512 で踏んだもの）と根は同じで、
  こちらは一段上の「機能そのものが二重に作られる」形**。番号だけでなく**着手前に
  「同じことを誰かがやっていないか」を見る手立てが無い**（枝の一覧を見ても、名前が違えば分からない）。
  #569 の重複ぶんは捨て、**#567 に無かった5点だけ**を #570 として出し直した:
  ①ics 本文の `担当:`（画面には出るのに**配ったカレンダーには出ない**状態だった）
  ②MCP `list_studio_bookings` の返り値の `assignees`（作成時は**書けるのに読めない**状態だった）
  ③マニュアル（「人で絞る」の説明が #567 の実装と食い違ったままだった）
  ④検証環境のシード ⑤データビューアの許可テーブル。
  ⚠️ **#570 のレビュー**: `get_reviews` / `get_review_comments` とも 0件だが、
  **今回は「届いていない」ではなく「回せなかった」** — Codex が
  **`You have reached your Codex usage limits for code reviews.`** を返している
  （**利用上限**という新しい落ち方。従来の「速すぎてマージした」とは別の原因）。
  ただし**セキュリティレビューだけは完走して指摘0**（`67a3b52`・13:03 完了）。
  `npm run reviews:debt` はこの環境のトークンでは 401 のため、API で直接確認した。
  ⚠️ **#570 で意図して残した未検証事項**（レビューが回っていないので自分で書き出す）:
  **`npm run build` 未実行**・**権限別 403 の画面確認なし**・**実ブラウザでの確認なし**
  （#570 は画面を変えていないため。ics 本文・MCP の返り値は API で実地確認済み）。

- **#574（2026-09-06・画面の言葉を基本語に統一）** — 全6アプリ・約6,500件の画面文字列を
  再監査して機械置換した回。**Codex のコードレビューが P2 を 10件返し、全件が実在した**
  （`get_review_comments` で直接確認）。**9件を修正して該当スレッドを resolve、1件は
  意図して据え置き**（下の表）。
  ⚠️ **修正の途中で Codex が「コードレビューの利用上限」に達し**（#570 と同じ落ち方）、
  修正コミット（`8b32813` 以降）には**再レビューが一度も走っていない**。稼働したのは
  セキュリティレビューだけ（各コミットで完走・指摘0）。そのため**指摘10件と同じ型
  （置換によるサ変動詞の「する」欠落・語の重複）を自分でスキャンして7件を追加で発見**し、
  同じコミットで直した。**Codex が拾った10件は最初の1回のレビューぶんでしかなく、
  機械置換を伴う PR では自分でも当たりを付けないと取りこぼす**、という教訓が残った。
  ⚠️ **マージ前に `main` が2本の PR（#573・#575）で先行し、14ファイルでコンフリクト**した。
  `main` 側のフィールド並び順修正・400行超過ファイルの分割と、この PR の文言変更が
  同じ行に当たっていたため。`--ours`/`--theirs` は使わず1ファイルずつ手で解決し、
  分割で移動した2ファイル（`client-daily/src/pages/inview/{DayRow,SearchHits}.tsx`）にも
  文言を追随させた。**意図して残した未検証事項**: `npm run build`（CI では通過済み）以外の
  実ブラウザでの PC/スマホ確認・`npm run verify:ui`・権限別403の画面確認はいずれも未実施。

| # | 指摘 | 状態 | 何が変われば直すか |
| --- | --- | --- | --- |
| 1 | Kairos3 経由の内覧会登録を `AI取込 (メール)` から `AI作成 (メール)` に変えたのは、AI が作った登録のように誤解を与える（`client-daily/src/pages/inview/logic.ts:195`） | ❌ 意図して据え置き | `docs/wording.md` ルール1が「AI起票／AI取込／AI登録 → AI作成に統一」と名指しで決めた語。戻すと前回棚卸しが「8通りに割れている」と記録した状態へ逆戻りする。生成と取り込みの区別は語ではなく隣接するバッジの `title` 属性と CSV の括弧書きで持たせている。**「AI が作った」と「AI が取り込んだ」を利用者が区別して操作する場面が出てきたら**、ルール1ごと全8か所を同時に見直す（片方だけ戻すと割れが再発する） |

- **#581**（`fix(equipment): 機材管理のUIルールをページ間で統一した`・2026-09-06）—
  利用者からの「機材管理のUIがページごとにバラバラ」という指摘を受け、まず調査エージェント
  1体で8画面を監査し、ラック図・機材詳細の2画面だけv4化から取り残されていることを特定
  したうえで、3体の並列エージェント（機材詳細／ラック図／周辺ダイアログ）に修正を分担させた回。
  ⚠️ **Codex のコードレビューは「利用上限（usage limits）」で一度も走らず**（#570・#574 と同じ
  落ち方）、**セキュリティレビューだけが完走し指摘0**（`d17de6b`・10:08 完了）。
  `get_review_comments` / `get_comments` とも新規の指摘なし。CI（`checks`/`build`）は緑、
  作成から約14分でマージ。**指摘なしではなく、通常レビューが回っていない**ため、
  ここに書いておく。今回は #570/#574 と異なり `npm run build`・`npm run verify:ui`
  （実ブラウザ・375/768/1280の3幅・522/522通過）・検証DB上でのAPI権限確認
  （`x-user-id: v-none`で403・`v-admin`/`v-keiri`で200・未認証で401）まで実施できている。
  **意図して残した未検証事項**: 実DBへの書き込み→読み直しの往復確認（スタイル統一のみで
  ロジック変更を伴わないため）・権限別403の画面レベルでの目視確認（APIレベルでのみ確認）。

- **#599**（`feat(reorg): 2026年10月の事業再編（計上会社の2社化・案件番号の改番）の基盤を実装`・
  2026-09-06）— P0〜P1残作業（`docs/reorg-2026-10-plan.md`）を1本のPRにまとめて出した回
  （ユーザーからの明示的な依頼で、複数ラウンドをマルチエージェントで並行実装したうえで
  作成・監視・マージまで実施）。⚠️ **Codexのコードレビューは一度も走らず**（`You have reached
  your Codex usage limits for code reviews.`・#570/#574/#581/#592-594と同じ落ち方）、
  **セキュリティレビューだけが完走し指摘0**（`cdba46a`・14:43完了。以後のpush 2回では
  再レビュー自体が走っていない）。`get_review_comments`は0件・`get_comments`は上記の
  bot通知2件のみで新規の指摘なし。`npm run reviews:debt`はこの環境のトークンでは401
  （過去と同じ）のためAPIで直接確認した。**レビューが実質0件のままマージされた**。
  ⚠️ **`main`との統合で自分で見つけて直した実害バグ3件**（Codexではなく衝突解消・検証中に
  発見）:
  ①`doc-handoff.service.ts`——こちらのentity_code配線が`main`側の「束から既定値を引く」
  派生値（`recognitionDate`/`paymentDue`等）を上書きしていた実装当時のバグ
  ②新設した改番対象一覧API（`GET /org-transition/renumber-candidates`）が当初
  `requireAuth`のみで、お客様名・請求状況・主担当まで認証済みなら誰でも読めた権限漏れ
  ③マイグレーション番号の改番で依存順序が壊れ（`entity_code`列を足す前に`NOT NULL`化を
  実行しようとして`column "entity_code" does not exist`）、検証DB再構築で発見。
  さらに`main`側の別PR（テロップCG再設計）と番号が再度衝突し、都合2回改番した
  （最終的に7本が284〜290番。詳細は`docs/reorg-2026-10-plan.md` §13・§12）。
  **意図して残した未検証事項**（PR本文に明記済み）: 実ブラウザでの通し確認はP3
  （GMOコストセンター）のみで他の段はHTTPレベル確認どまり・権限別403確認も主要な
  ものだけで45コミット・141ファイル全数の網羅ではない。

- **#592/#593/#594**（プロジェクト管理（GPM）を案件管理のUIフォーマットに揃える・2026-09-06）—
  「PR #574（提案・モックアップのみ）が反映されていない」というご指摘を受け、
  `docs/design/v4/mockups/gpm-format-alignment.html` の22項目を3体の並列エージェントに
  分担させ実装した回（①言葉と色 #594・②一覧画面 #593・③詳細・ダッシュボード・フォーム #592）。
  ⚠️ **`npm run reviews:debt` はこの環境のトークンでは401**（過去の棚卸しと同じ）。
  `get_review_comments`/`get_comments` を3PRとも直接確認し、届いた指摘は**すべてこの場で
  修正・返信・スレッド解決まで完了**させてから棚卸しに移している（未対応で残った指摘は無し）:
  - **#592**: P2×1（自社構築の編集で `pm_company` が非NULLのまま残ると隠れた入力欄経由で
    直せない）→ 保存時に区分で正規化する形で修正
  - **#593**: P1×1（「すべて解除」でURLの絞り込みが1個しか消えない・3つのsetterが同じ
    古いparamsを元に別々にnavigateしていた）・P2×3（スマホの「用語」ヒントが絞り込みシートの
    下に隠れて見えない／担当名で検索しても`u.name`を見ておらず0件になる／絞り込み後もヘッダーの
    件数が絞り込み前のまま）→ いずれも修正
  - **#594**: P2×2（`gpm_open_items`を「持ち帰り」に統一したのに、共有部品 `MinutesCard.tsx`
    の見出し・空表示・注記と一覧の「用語」ヒントが「未解決事項」「未確認」のまま残っていた）
    → `MinutesTrack` に `itemNoun` を追加して解消
  - ⚠️ **3PRが同じディレクトリを並行して触ったため、マージのたびに後続PRがコンフリクトした**
    （#592→#594・#593→#594で計3回）。都度 `main` を取り込んで手で解決し、**単なる機械的マージに
    留めず「PR592が新設した `tabs.ts` に旧ラベル『未解決事項』が残っている」等、マージだけでは
    気づけない用語の後退も監査して直した**
  - ⚠️ **自己招致のCI赤1件**: #594でCodex指摘への対応コメントを足した際、共有部品
    `MinutesCard.tsx` が400行の上限（`check-file-size.mjs`）を1行超え、CIが落ちた
    （コメントを縮めて解消）。**指摘を直すための追記自体が別の検査に引っかかる**ことがある教訓
  - Codexは#594の6コミット目以降「コードレビューの利用上限」に到達し、以後のコミットは
    セキュリティレビューのみ・完走かつ指摘0（過去の#570/#574/#581と同じ落ち方）
  - **意図して残した未検証事項**（3PR共通、各PR本文にも明記済み）: 実ブラウザでのPC/スマホ
    目視確認・`npm run verify:ui`・権限別403の画面確認はいずれも未実施（#593のみPlaywrightで
    PC/スマホの目視・コンソールエラー0件を確認できている）

### 財務ダッシュボードの3件を直した回（`claude/financial-dashboard-errors-x9721s`）で意図して残した判断

⚠️ **どれも「気づかなかった」ではなく「今回は直さないと決めた」もの**です。
3つの不具合（①案件＋期間解除で 400 ②絞り込み中の 504 ③役務提供完了日が保存されない）を
調べる過程で**同時に見つかったが、この PR の範囲を超えるので触らなかった**ぶんを書き出します。
**書かなければ存在ごと消えるため**（この文書の「なぜここまで書くか」の節）。

| # | どこ | 何が残っているか | 何が変われば直すか |
| --- | --- | --- | --- |
| 1 | `nginx/gmo-onair.conf`（`/api/` の location） | **`proxy_read_timeout` の指定が無く既定 60 秒**。超えると 504（画面は「サーバー側で処理が止まりました」）。今回は「60 秒を延ばす」のではなく**60 秒に収める**側（N+1 の解消・索引・接続プール）を直した | 60 秒に収まらない正当な処理が出たとき。⚠️ **延ばす前に `statement_timeout`（60秒・`connection.ts`）と揃えること** — 片方だけ延ばすと、応答が返る前に DB 側で切られる |
| 2 | `nginx/gmo-onair.conf:8,74` | **レート制限 `30r/s` / `burst=50` の超過は 503** で、`ErrorPanel` では 504 と**同じ文言**になる（`shared/src/client/states/ErrorPanel.tsx:49` は `>= 500` を一括りにしている）。**オフィスの NAT で全社員が1つの IP を共有していると全員でバケットを取り合う** | `access.log` で 503 の比率を見て、実際に当たっていると分かったとき。切り分けには `limit_req_status` を 429 にして文言を分けるのが先 |
| 3 | `client/src/contexts/finance/pages/ledger/LedgerParts.tsx:29`・`shared/src/client/hooks/useCrudPage.ts:125` | **検索欄に debounce が無く、1文字打つごとにリクエストが飛ぶ**（台帳の検索は1回で 4〜5 本の SQL）。今回は財務ダッシュボードだけ `signal` で中断するようにしたが、**台帳側は中断もしていない** | 台帳の 504 が続くとき。`useCrudPage` に入れると4画面まとめて効くが、**その4画面の再検証が要る**ので分けた |
| 4 | `server/src/contexts/finance/routes/{revenues,purchases,sga}.routes.ts` | 一覧 API が**同じ条件・同じ join を 4〜5 回、逐次 `await` で走査**している（COUNT / 本体 / SUM / 按分 / 状態別の件数）。1本の `SELECT COUNT(*), SUM(...), COUNT(*) FILTER (...)` にまとめられる。**ページ送りのたびに 2 ページ目以降でも COUNT/SUM を数え直している** | 索引（migration 259）を入れても遅いと分かったとき。**まず実測してから**（`pg_stat_statements` の `total_exec_time` 上位） |
| 5 | `server/src/contexts/finance/list-query.ts:22,90-93` | **検索が 5 列の `%…%`**（`billing_key`/`notes`/案件名/GLS/顧客名）で、`pg_trgm` も GIN 索引も無い＝必ず全走査。しかも④のとおり 4〜5 回繰り返される | ⚠️ **調べた結果「索引だけ足しても1本も使われない」と分かったので、当面やらない（❌ のまま残す）。** 検索の OR が `revenues`/`projects`/`companies` の**3つの表にまたがる**のでプランナが BitmapOr を組めず、結合してから Filter するしかない。さらに**日本語の検索語は1〜2文字が多く、`%ab%` からは完全なトリグラムが取れない**ので索引に効かない。効かせるには検索節を UNION 形に書き換える必要があり、Excel・MCP・月次集計の**全経路に波及**する。**#524 で④の集計を1本にまとめたので、まず実測してから**判断する |
| 6 | `client/src/contexts/finance/pages/BudgetDashboardPage.tsx` の `ErrorPanel` | **内訳3列（売上・仕入・販管費）が失敗しても画面に何も出ない**（`summaryQuery` だけが `ErrorPanel` に渡っている）。内訳が 504 でも「内訳が空」に見えるだけ | 「合計は出ているのに内訳が空」という報告が出たとき。⚠️ **今回は足さなかった** — 失敗しない側を直すのが先で、先にパネルを増やすと「エラーが増えた」ように見える |
| 7 | `server/src/shared/db/connection.ts` の型パーサー | **`DATE`(oid 1082) のパーサーを設定していない**ので、`node-pg` が JS の `Date` にして返す（`purchases.service_completed_date` ほか **DB 全体で 25 列**）。**コンテナが UTC で動いているから今は正しく見えているだけ**で、`TZ=Asia/Tokyo` を入れた日に**全部 1 日ずれる** | `TZ` を設定する必要が出たとき、または DATE 列を新しく足すとき。`types.setTypeParser(1082, v => v)` で 25 列すべて文字列になるが、**それらを `Date` として扱っている箇所の洗い出しが要る**ので今回は入れていない（この PR では `toServiceDateInput()` が 1 列ぶんを吸収している） |
| 8 | `excel.routes.ts:230,245,332` / `purchases.routes.ts` の CSV 列 / `mcp/tools/finance.tools.ts:146` | **役務提供完了日が Excel・CSV・MCP のどこにも出ない**（列リストに入っていない）。**Excel から仕入を取り込むと必ず NULL になる** | 「Excel で役務提供完了日も扱いたい」と決まったとき。⚠️ 取り込み側だけ足すと**空欄の取り込みで既存の日付を消す**ので、書き出しと取り込みを同時に直すこと |
| 10 | （追記）`list-query.ts` の別名 | **⭕️ #514 で修正**。上の9件を調べている最中に見つけた**本番の不具合** — `v.name` が未定義の別名で、仕入の検索・仕入先での並べ替え・Excel 書き出し・MCP が**検索語を入れると必ず 500** だった | — |
| 11 | （追記）マイグレーションの番号 | **⭕️ #516 で修正**。#511 と #512 が同じ 250 番を使い `main` の CI が赤くなった。番号を「枝を切った時刻」で取る限り再発する | 番号をマージ順で決める形（`changelog.d` と同じ考え方）にしたとき。**適用済みの記録がファイル名に紐づく**ので移行の設計が要る |
| 9 | `client/src/contexts/production/components/episodes/BusinessProjectView.tsx` | **⭕️ #526（前半8段）＋ #527（後半7段）で修正**。**2,032 → 227 行**（上限 400 行）。中身は `episodes/businessProject/` の hook 4本・部品6枚・型／帳票2本に分けた。⚠️ **v4 で作り直すのを待たずにやった** — 「作り直すときに分割する」と書いたまま、その間ずっと1か所直すのに 2,000 行を読む状態が続くため。**振る舞いは変えず、移した JSX は1文字も変えていない**（各段で実ブラウザの全文・ボタン・ダイアログの入力欄・`datalist`・Esc の挙動・JS エラー0件を基準と突き合わせた） | — |

## この文書の使い方

**これは決めごとです**（[docs/branching.md](../branching.md#マージしたらその-pr-のレビューを棚卸しに移す必須)）。

1. **マージしたら `npm run reviews:debt` を走らせる**（自分の PR だけでなく、その回のぶん）
2. 返していない指摘が出たら、**まずこの表に足す**。
   ⚠️ **直すのが先ではありません** — 書いてあれば、直すのは別の日でもよい
3. 潰したら状態を ⭕️ に変え、**直した PR 番号を書く**

### やってはいけないこと

- ⚠️ **「直さない」と決めたものを表から消すこと。** ❌ のまま残し、
  **理由と、何が変われば直すか**を書いてください。消すと**次に読んだ人には
  無かったことになり**、同じところをもう一度掘るか、知らないまま間違ったことを書きます
  （実際に #141 で指摘されました）
- ⚠️ **件数を文章に書いて、表と食い違わせること。** 数えるのは上の「数え方」で。
  文章の数字は古くなりますが、表は古くなりません（実際に1度食い違いました）
- ⚠️ **束に分けたあと、合計を数え直さないこと**（1件どの束にも入っていないことがありました）
- ⚠️ **並行開発中にこの文書の並べ替え・再構成をしないこと（行追加のみ）。** この文書は
  全セッションがマージのたびに追記する最大の衝突源です（2026-08 だけで百回以上変更）。
  表全体の整理・節の並べ替えは、他セッションが止まっている静穏時に単独PRで行ってください

### なぜここまで書くか

**マージすると指摘は GitHub の画面から消えます。** 直っていなくても消えるので、
**書かなければ存在ごと消えます**。実測: **143 件が埋もれ、それを潰す作業にも
26 件付き、24 件が記録されていません**でした（うち1件は**リリースが出せなくなる P1**）。
