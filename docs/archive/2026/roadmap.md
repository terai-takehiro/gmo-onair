# 実装ロードマップ (UI/UX 刷新の残り)

デザイン一式 (6部36章・約60画面) と実装を1画面ずつ突き合わせて引いた順番。
**決まった理由も一緒に書く** — 同じ議論を繰り返さないため。

最終更新: 2026-07-29 / v3.1.0 全体監査 (サイトツリー再編) — 残りは末尾の一覧

---

## 突き合わせで見つかった「デザインにあったのに計画表に無かったもの」

| 章 | 内容 | 扱い |
| --- | --- | --- |
| **14章 案件の入力を減らす** | 22項目 → 起票は3つ。残りは進んだ段で聞く | **順番2に入れた** (毎日いちばん触る入力) |
| 25章 お試し (サンドボックス) | 案件を作らずに道具を試す。実績に混ざらない | 順番7 |
| 36章 検索結果の画面 | ⌘K の続き。見えないものは件数にも出さない | 順番6 |
| Qシート「似た台本から引き継ぐ」 | マイク香盤を前回の台本から写す | 順番6 と一緒に |

これ以外の未実装は計画どおりの残り。**進め方の管理自体は概ね機能していた。**

---

## 順番

| # | 章 | 何ができるようになるか | 大きさ |
| --- | --- | --- | --- |
| — | 30 | **見積をつくる** (料金表・AIの下書き・粗利がその場で出る・PDFはBOX) | 済 v2.9.276 |
| — | 31 | **請求と入金** (締めの日にまとめて出す・入金の確認・検収書) | 済 v2.9.277 |
| 1 | 32 | **合同案件** — 1回のイベントを9社に請求。デザイン到着済 (40a / 40b) | 大 |
| 2 | 14・13 | **案件の入力を減らす** (起票15秒) ＋ **案件のお金・予定タブ** | 大 |
| 3 | 29 | **隔週キープをつくる** — 18ページのうち16ページをAIが埋める (6.5時間 → 45分) | 大 |
| 4 | 21・22 | **香盤表・運営マニュアル** — 当日の動きを1枚に (3〜4時間 → 15分) | 大 |
| 5 | 20 | **リアルタイムCG** — 4つに分かれた送出を1画面に。アワードだけ実装 | 大 |
| 6 | 15・36 | **案件の成果物・書類・カード・検索結果** — 何が揃っていないかが期日順に並ぶ | 中 |
| 7 | 25 | **お試し** — 練習が実績に混ざらない | 小〜中 |
| 8 | 6 | **部品と数字のサイズの共通化** — 画面ごとにバラバラに書けなくする | 中 |

### README の順番から変えた2点

1. **14章 (入力を減らす) を2番目に繰り上げ**
   案件の画面 (2,494行) を **13章のタブ化と同じ工事で触る**ため。別々にやると同じ場所を2回開ける
2. **8番 (部品サイズ) は最後のままだが、新しく作る画面は先に新ルールで作る**
   後から作る画面が増えるほど、まとめて直す量が増えるため。30章で `Money` を新設済み

---

## 決めたこと (2026-07-26)

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 14章の「やめる4項目」 | **グループ区分は やめない**。タグ / 教訓・学び / 案件種類(その他) は削除 | グループ区分 = **費用を分け合う請求**。グループ9社合同の株主総会などで現役 |
| 請求 (31章) と 入力を減らす (14章) の順 | **請求が先** | 締めには期限があり「間に合わなかった」が起きる。入力の重さは重いだけで止まらない |
| 隔週キープの出力 | **PDF のみ** (PowerPoint は作らない) | 投影も配布もPDFで足りる。その分を他の章に回す |
| リハーサルと本番の分離 (24c) | **不要**。デザインから削除依頼 | 分ける概念を持たない |

---

## デザインに出した依頼 (2026-07-26)

`docs/design-requests/2026-07-26-design-change-request.md`

1. **【新規】費用を分け合う請求 (グループ合同案件) の画面** ← 本体
   現状は「**1社が払って費用を9案件で分け合う**」形で、業務の「**9社がそれぞれ払う**」を表現できない。
   請求書を参加社数ぶん出す経路が無い
2. 【修正】14章 27a の「やめる」から グループ区分を外す (+ 27c の中身を埋めてほしい / 件数9 と 中身7 の不一致)
3. 【修正】29章 出力は PDF のみ
4. 【削除】24c リハーサルと本番を分ける
5. 【小】「按分」を画面に出さない (⌘K のラベルは実装側で直す)

**依頼1 は 31章と同じ場所の話**なので、デザインが届き次第31章と合わせて作るのが手戻りが少ない。
遅れる場合は31章を先に作って後から足す (請求の画面を2回触ることになる)。

---

## デザイン修正版の受領と検証 (2026-07-26)

依頼5件はすべて反映された。**ただし依頼していない削除が1件**あった。

| 依頼 | 反映 |
| --- | --- |
| 1 合同案件の画面 | **32章として新設** (40a 2つの「分ける」を区別する / 40b 合同案件をつくる) |
| 2 「やめる」からグループ区分を外す | 済 (やめるは3項目・進んだら聞くは7項目に訂正・**27c の1対1の表も埋まった**) |
| 3 隔週キープは PDF のみ | 済 (PowerPoint / PPTX の記述が 0 件) |
| 4 24c リハーサルと本番を分ける | 削除済 |
| 5 「按分」を画面に出さない | 済 (残る1件は「按分という言葉は画面に出しません」という説明文) |

### 巻き添え削除 (デザインに再依頼が必要)

20章から **24e「テンプレートは『アワード』だけ実装する」** と **24f「データを入れる（アワード）」** が
24c と一緒に消えている (画面IDが 24a/24b/24c/24e/24f/24d → 24a/24b/24d、分量が 51,032 → 28,324 文字)。

- **仕様書 §7.7 には要件が残っている**ので、消えたのは画面だけ
- README の順番表にも「リアルタイムCG のテンプレート化（アワード1枚に畳む）とデータ入力」が残っている
- 20章は順番5なので**当面の作業は止まらない**が、着手前に復元を依頼する

### 32章 合同案件で決まったこと (デザイン側の回答)

| 論点 | 決定 |
| --- | --- |
| 入口 | **お金 ＞ 合同案件**。既存の費用分けは 案件 ＞ お金 ＞「他の案件と分け合う」 |
| 2つの見分け方 | **「請求書が何枚出るか」**。1枚なら費用分け、参加社数ぶんなら合同案件。作成時に最初にこの質問を出す |
| 分け方 | 均等 (既定) / 割合 / 金額を直接。**端数の行き先を画面に出す** (既定は幹事・変更可) |
| 1社だけ直したとき | 合計と総額の一致を画面で示し、**合わないうちは出せない**。差額は「残りの社で割り直す」か「幹事に寄せる」 |
| 出したあとに1社抜けたとき | **取り消しの請求書を1枚出して**残りに配り直す。「取り消し済み」と出して履歴に残す |
| 呼び名 | 「合同案件」。「按分」は出さない |

**31章の請求のしごとは `revenues` を読んでいる**ので、合同案件の請求書もそのまま一覧に並ぶ
(= 32章を作るときに31章を作り直さない)。

### 巻き添え削除の復元 (2026-07-26 第2版で確認済み)

デザイン側が **24e / 24f を復元**した。3回目の受領版で確認:

| 確認項目 | 結果 |
| --- | --- |
| 20章の画面ID | `24a / 24b / 24d / 24e / 24f` (24c は削除のまま = 依頼どおり) |
| 24e | 「テンプレートは『アワード』だけ実装する」バッジ表記は `20e` に振り直し |
| 24f | 「データを入れる（アワード）」バッジ表記は `20f` |
| 依頼1〜5 の反映 | 全部そのまま維持 (32章 40a/40b、27a/27c、PDF のみ、24c 削除、按分の非表示) |
| 意図しない差分 | なし |

**20章は着手できる状態**になった (順番は5のまま)。

---

## コーディングが遅い原因と、直したこと (2026-07-26)

実測して 3 つに切り分けた。

| # | 原因 | 実測 | 直したこと | 効果 |
| --- | --- | --- | --- | --- |
| 1 | **CLAUDE.md が毎ターン全文読み込まれるのに、その 96.7% が過去の履歴だった** | 828,591 文字中 801,027 文字が履歴 | 最新5件だけ残し `docs/version-history.md` に切り出し | **828KB → 58KB (93%減)** |
| 2 | ルートの `npm run build` が 8 ワークスペースを**直列**で回していた | 1分50秒 | `shared` の後を `concurrently -m 4` で並列化 (旧挙動は `build:serial` に残置) | **1分50秒 → 47秒** |
| 3 | 「実 Postgres で N 項目」の**検証の土台を毎回ゼロから書き起こしていた** | 毎回 15〜30 分 + 同じ落とし穴の踏み直し | `scripts/dev-verify/` に up/down を用意し、落とし穴も README に固定 | **冷えた状態から 4 秒** |

### 1 が一番効く理由

CLAUDE.md はコーディング中、**ツールを 1 回叩くたびに全文が読み込まれる**。
履歴を全部ここに置くと、1 行直すだけの作業でも毎回 828KB を運ぶことになる。
さらに読み込み量が増えるほど会話の途中で要約が挟まり、**作業の途中経過が失われてやり直しになる**。
遅さの体感の多くはこれだった。

**今後のルール**: 新しい版を足したら、6件目に押し出された版を `docs/version-history.md` へ移す。
CLAUDE.md に残すのは常に最新5件 (このルールは CLAUDE.md 本文にも書いた)。

### 3 で固定した「毎回踏んでいた落とし穴」

`DATABASE_URL` の 1 本だけを読む / `user_permissions.access_level` (`level` ではない) /
`users.role` は 2 値のみ / `JWT_SECRET` は 32 文字以上 / 開発の認証は `x-user-id` ヘッダー /
Playwright の外部フォント失敗は JS エラーに数えない。

### 手をつけなかったもの (意図的)

- **検証の項目数そのものは減らさない**。v2.9.277 では実際に本番影響のバグを 2 件見つけている
  (経理が財務を開けない / ベルが出ない)。ここを削ると速くはなるが、壊れたまま出る。
- **1章 = 1PR の粒度も変えない**。まとめると差し戻しが効かなくなる。

---

## 32章 合同案件 の実装 (v2.9.279)

順番1が完了。デザイン 40a/40b と仕様書 §7.14 のとおり。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 器 | **新しいテーブル2つ** (`joint_events` / `joint_event_companies`) | 既存の `project_groups` は「1社が払い費用を案件で配る」形。向きが逆なので同じ器に入れると行を見て意味が判別できない |
| `project_groups` の行 | **作らない** | 作ると「費用を分け合う案件」の一覧に合同案件が並び、避けたかった「2つの意味が混ざる」を裏側で作る |
| 見分け方 | 作成時に**「請求書が何枚出るか」**を聞く | 1枚 = 既存の費用分け / 参加社数ぶん = 合同案件 |
| 案件の作成 | 会社を選んだ時点で**社数ぶん自動作成** | 手で9個作らせない (デザインの要件) |
| 初期ステージ | `b_verbal` 口頭決定 → 請求書を出したら `a_won` + GLS 発番 | 最初から受注にすると未確定の金額が数字に乗る。GLS を9本先取りしない |
| 割合の持ち方 | **万分率の整数** | 小数だと9等分で誤差が溜まり合計が総額に1円合わない |
| あまりの1円 | **行き先を画面に出す**。既定は幹事、変更可 | 既存の費用分けは黙って先頭に寄せていて誰に付いたか追えなかった |
| 合計が合わないとき | **サーバーでも止めて1枚も出さない** | 画面だけの制限は必ず抜ける |
| 1社抜けたとき | **取り消しの請求書を1枚出して**残りに配り直す。行は消さない | 出した請求書は消せない |
| 権限 | `budget` (お金) | 入口が「お金 ＞ 合同案件」。31章と同じ担当者 |

### 31章との関係

出した請求書は `revenues` なので、**31章の請求のしごとの入金待ちにそのまま並ぶ**
(実測で9枚並ぶことを確認)。32章のために31章を作り直していない。

### ブラウザ検証で見つかった実バグ3件

1. **経理 (budget だけ) は会社を1社も選べなかった** — 顧客の一覧 `/customers` は `sales` 権限。
   合同案件を作るのは経理なので画面が成立していなかった。選ぶのに要る id と名前だけを返す
   `/joint-events/companies` を足した (連絡先や住所は返さない)。
2. スマホで「合同案件へ戻る」のタップ領域が 36px。
3. **375px で 277px 横にはみ出していた** — `min-w-[720px]` の表は `overflow-x-auto` に
   入れても画面ごと横に伸びる (実測)。スマホでは表を出さず**1社 = 1カードの縦積み**にした。

### まだ無いもの

- **合同案件の見積** (30章の見積画面を「全体で1本」として使う導線)。
  いまは総額を数値で入れる形。デザインは「30章の見積画面をそのまま使う」としているので、
  30章側に「この見積は合同案件のもの」という紐づけを足せば繋がる。

### 次

順番2 = **14章・13章「案件の入力を減らす (22項目 → 起票は3つ) ＋ 案件のお金・予定タブ」**。
27c「進んだ段で聞く」の中身はデザイン側で埋まっている。

---

## 14章 案件の入力を減らす の実装 (v2.9.280)

順番2の前半が完了。デザイン 27a/27b/27c と仕様書 §7.1。
（13章のお金・予定タブは README でも別項目 = 順番4なので分けた）

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 起票で聞く数 | **3つ**（案件名 / お客様 / スタジオを使うか） | 電話を切った直後に分かっているのはこれだけ |
| 起票の入口 | `/sales/projects/new` を差し替え | 既存の入口4箇所を触らずに新画面へ着く |
| 22項目のフォーム | **編集のときだけ**使う | 消すと「進んだ段で聞く」以外の直し方が無くなる |
| 案件分類 | **人に聞かない**（スタジオを使うかから決める） | 2度聞くと片方だけ直された案件ができる |
| GLS番号 | 受注後に採る | 先に採ると消えた案件の番号が空く |
| 聞く項目の定義 | **サーバー1か所**（`stage-ask.service`） | 2か所だとずれ、緩いほうを通って空のまま進む |
| 必須の判定 | 画面とサーバーの**両方** | 画面だけの制限は必ず抜ける |
| すでに入っている項目 | **聞かない** | 同じことを2度打たせない |
| ボードで動かしたとき | 足りないときだけ聞く | 押すだけの段で1手増やさない |

### デザインと違う判断をした1点

**教訓・学びは残した。** 27a では「やめる」だが、ふりかえりの失注分析
（`getLostReasonAnalysis`）が実際に `lessons_learned` を集計しているので、
入力を消すとその画面が空になる。「誰も検索・集計に使っていない」という
27a の前提がこの項目には当てはまらなかった。
かわりに**失注にする瞬間に任意で聞く**形にした（27c の趣旨どおり）。

タグと案件種類(その他) は削除した（サーバー側の絞り込みと MCP の一括更新は残置）。

### ブラウザ検証で見つかった既存バグ1件

**営業が案件フォームの部屋と予約を読めなかった。**
`/studios/locations` と `/studios/bookings` は `studio` 権限で、営業は普通これを持たない。
画面はエラーを出さないので「この案件には部屋が登録されていない」と読めてしまう状態だった。
v2.9.277 のベルと同じ形なので同じ直し方（既定は `studio` 必須のまま、この2本の GET だけ通す）。

### まだ無いもの

- 27a の仕分け（いま必要／進んだら聞く／自動で入る／やめる）を**画面に出すこと**。
  定義は `GET /projects/stage-asks/all` で読めるようにしてあるので、出すだけならすぐできる。
- 13章のお金・予定タブ（順番4）。

### 次

順番3 = **29章 隔週キープをつくる（PDFのみ）**。

---

## 29章 隔週キープをつくる の実装 (v2.9.281)

順番3が完了。デザイン 35a/35b/35c と仕様書 §7.3。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 型 (Ver.2.5) | **変えない**。変えるのは「誰が埋めるか」だけ | 会社のフォーマットなので実装側で変えない |
| ページの構成 | **コードの定数**（`KEEP_PAGES` 16枚） | DB に置くと「型が変わったのか、その回だけ違うのか」が区別できない |
| DB に持つもの | 人が書いた分 と その回に足した議題 だけ | それ以外は既にある |
| 数字 | **写し取らない**。出すときに読む | 写すと元を直しても資料が古いまま |
| ④以降 | **議題があるときだけ足す**。無い回はページを作らない | 毎回の型に混ぜると空のページが残る |
| 「そのほか（自由）」 | **名前を人が書くのを必須にした** | 候補の名前がそのまま議題名になると何の話か分からない |
| ③の6テーマ | テーマと担当は**固定**。人は1行だけ | 毎回作り直さない |
| ①の3行 | **空でも進める**（前の値を消さない） | 相談が無い回もある。書かないという判断も記録 |
| チェックリスト | 落ちている項目**だけ**出す | 全部出すと出したことの意味が消える |
| 確定 | 落ちている項目があっても**止めない** | 止めると会議が始められない |
| 出力 | **PDFだけ** | 依頼3で合意。投影は画面のまま |
| 過去分 | 消さない（削除の経路を作らない） | Ver.2.5 の決まり |

### 既存の資産をそのまま使った

`keep-report.service`（月次PL・イベント報告・議事録）は 29章の「材料」なので作り直していない。
ふりかえり ＞ 隔週キープの既存画面は「材料を確定する場所」として残し、そこから資料づくりへ送る。

### まだ無いもの

- 29c「できあがりのページ」を**画面でそのまま投影**する形（いまは PDF に出す）
- ②案件実施報告への写真の差し込み（BOX の写真は既存のイベント報告側で紐づけ済み）

### 次

順番4 = **13章 案件ワークスペースの「お金」「予定」タブ化**。

---

## 13章 お金・予定タブ の実装 (v2.9.282)

順番4が完了。デザイン 13章 7a と仕様書 §7.12。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| どれをタブにするか | **お金と予定だけ** | §7.12 の判断。案件の中で往復がいちばん多い |
| やり取り・タスク・書類 | あとで（今回は触らない） | §7.12 でそう決めている |
| 現場の道具 | リンクのまま | 同上 |
| 数字の集めかた | **1本の口** (`/projects/:id/money`) | 画面から集めると3〜4往復。数字の出しかたも画面ごとに分かれる |
| 受注前の粗利 | **想定金額を元に見込む**（ラベルは「粗利（見込み）」） | 旧3値は想定金額が無く、受注前は粗利が常にマイナスに見えた |
| 仮の仕入 | 件数を**別に出す** | 分からないと未確定の粗利を信じてしまう |
| 操作の可否 | サーバーが返す | 画面側で条件を書くと条件が2か所になる |
| 予定の出どころ | **案件側の口** | 予約一覧は `studio` 権限で、案件を見るのは営業 |
| 仮押さえの期限 | サーバーが計算（本番日の45日前） | 画面で計算すると案件一覧と違う期限が出る |

### 既存バグ1件（影響範囲が広い）

**`getSummary` が見積を売上に数えていた。** `status` の絞り込みが無く、`status='estimate'`
の見積が「売上（確定）」に混ざっていた。見積はまだ提案で、GLS 発番のとき
`migrateEstimates` が `confirmed` に変える。数えていた間は想定金額としても別に出しているので
**二重計上**になり、案件の粗利・粗利率が過大に出ていた（粗利率が100%を超えることもあり得た）。

同じ数字を読んでいるのは：案件詳細 / 案件一覧 / MCP `get_project` / 隔週キープのイベント報告。
按分ぶん（`revenue_allocations`）にも同じ絞り込みを入れた。
実測で 9,820,000 → 5,000,000、確定に変えれば数え、戻せば元に戻ることを確認。

### まだ無いもの

- やり取り・タスク・書類のタブ化（§7.12 で「あとで」と決めている）

### 次

順番5 = **21・22章 香盤表・運営マニュアル（AIが下書き、人が直す）**。

---

## 21章 香盤表 の実装 (v2.9.283)

順番5の前半が完了。デザイン 21章 28a と仕様書 §7.8。
（22章 運営マニュアルは分量が大きいので次の版に分けた）

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 最初の1枚 | **自動で組む**（白紙を見せない） | Excel を画面に移すだけでは入力が二度手間 |
| 組み直し | 自動の枠だけ差し替え、**手直しは残す** | 区別が無いと「組み直したら消えた」が起きる |
| 枠の持ち方 | **分で持つ**（px は画面が掛ける） | px を持つと目盛りを変えて全部ずれる |
| 人のレーン | 案件メンバーを指す（名前は写さない） | 写すと片方だけ直された表ができる |
| 重なりの判定 | **サーバー** | 画面で判定すると印刷とPDFで違う警告が出る |
| 部屋の重なり | **警告しない** | 部屋では同時に複数の作業が走る |
| レーンの出し入れ | **消さずに隠す** | 消すと置いた枠も消える／2枚作ると食い違う |
| 計時LIVE | 枠の長さ = 尺。冪等 | 当日タイマーを作り直さない |
| 終わりの無い枠 | タイマーにしない（理由を言う） | 尺が決まらない |
| AI | **使っていない** | 21章は既存データからの組み立て |

### 会社方針「AIを使い捨てにしない」について

21章は AI を使わないので、5条件は**この版には適用されない**（`ai-feedback-loop` の
適用範囲どおり）。AI が入るのは 22章の「会場図をAIが下書き」なので、
そこで `ai_outputs` / `ai_corrections` / `ai_outcomes` と `getFeedbackDigest` に載せる。

**22章での5条件の当て方（先に決めておく）**

| 条件 | どう満たすか |
| --- | --- |
| 1 出力の記録 | `ai_outputs`（kind=`venue_layout_draft`・target_table=`manual_parts`・図の要素の全文） |
| 2 修正差分 | 保存のたびにサーバーが `diffByKey` で突合（鍵は要素の**記号**。index だと1つ足すだけで以降全部が「変更」になる） |
| 3 成果 | `ai_outcomes`（PDFに出した / 出さなかった・版数） |
| 4 還流 | 生成前に `getFeedbackDigest('venue_layout_draft')` を読んでプロンプトに載せる |
| 5 レビュー | MCP `get_ai_feedback_digest` と `/review`（既存の経路にそのまま乗る） |

### 検証中に直した実装の穴

1. `studio_bookings.start_time` は TEXT なので `to_char` が使えず日で絞れなかった
2. 部分ユニーク索引を `ON CONFLICT` の的にするには同じ条件を書く必要がある（42P10）

### 次

**22章 運営マニュアル**（部品12種を束ねて1冊にする / 会場図をAIが下書き / 渡す相手ごとの版）。

---

## 22章 運営マニュアル の実装 (v2.9.284)

順番5が完了（21章と合わせて2版に分けた）。デザイン 22章 29a/29b/29c と仕様書 §7.8。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 何を作るか | **PDFを作る機能ではなく、部品を作って束ねる機能** | 部品はそれぞれ別のタイミングで揃う |
| 部品12種の定義 | **コードに置く** | DBだと「種類が変わったのか案件ごとか」が区別できない |
| 足りない部品 | **名前で指摘**。白紙のページは作らない | 何が足りないか分からないと動けない |
| 自動で入る部品 | **元データがあるかで判定** | 人に押させると押し忘れで足りないことになる |
| 渡す相手 | 社内12種／お客様10種／アルバイト3種。**社外版から自動で外す** | 人が毎回消すと必ず1回は消し忘れる |
| 出した版 | **出した瞬間の中身を残す** | 現場で古い紙と混ざったときにどちらが何版か分かる |
| 記号と凡例 | 記号は人・物を指し、**凡例は勝手にできる** | 名前を図に写すとスタッフ表を直しても図が古いまま |
| 位置 | 0〜1000 の相対 | 図の大きさが変わってもずれない |
| 会場図 | **AIが下書き、人が実測と動線を直す** | 下書きが無いと3〜4時間かかる |

### 「AIを使い捨てにしない」5条件の充足

| 条件 | どう満たしたか | 判定 |
| --- | --- | --- |
| 1 出力の記録 | `ai_outputs`（kind=`venue_layout_draft`・target=`manual_layouts`・置いた要素の**全文**） | ○ |
| 2 修正の差分 | 保存のたびにサーバーが `diffByKey`。**鍵は「記号:名前」**。落とされた=`reject`／動かされた=`fix`。二重計上しない | ○ |
| 3 成果 | `ai_outcomes` に `issued`（版数つき）と `dropped`（使わなかった） | ○ |
| 4 還流 | 生成前に `getFeedbackDigest` を読んでプロンプトに載せ、**載せた回は版を分ける**（`v1+digest`） | ○ |
| 5 レビュー | MCP `get_ai_feedback_digest` と `/review` の既存経路に乗る | ○ |

**鍵を index にしない理由**（このスキルが挙げる典型的な失敗）: 記号を1つ足すだけで
以降すべてが「変更された」ことになり、修正率が実態とかけ離れる。

**AI が増やした記号は通さない**（置ける6種だけに絞る）。AIキーが無ければ 503 で
「記号を選んで自分で置くこともできます」と言う。

### まだ無いもの

- PDF のファイル出力（いまは「出した版」として中身を残す。29章と同じ pdfkit の経路に載せられる）
- 会場図の平面図を Box から敷くこと（`floor_plan_box_file_id` は用意済み）
- 部品の中身の編集画面（いまは「できた」の印と自由な中身だけ）

---

## 20章 リアルタイムCG の実装 (v2.9.285)

順番6が完了。デザイン 20章 24a / 24b / 24d / 20e / 20f と仕様書 §7.7。

**24a（送出を1画面にする）はユーザーの明示的な指示で範囲に含めた** — 本番中に押すボタンが
変わるので、着手前に「24a を含めるか」を相談し、「2でいきましょう（含めて全部やる）」の判断を得た。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 送出の画面 | **1つにする**。以前の4つは「準備」に降ろす | どれを開いて本番に臨むのかが人によって違っていた |
| ステップの順番 | **サーバーが正**（画面の定数をやめた） | 2か所に書くと「次はWINNER BAR」と出て別のものが出る |
| 送出の経路 | **既存の `/cue` `/oneshot/cue` をそのまま使う** | 出る絵を1バイトも変えない（本番の絵を作り替えない） |
| 次に出るもの | **必ず先に見せる**（そのあとの流れも） | 分からないまま TAKE を押させない |
| 出したまま | **5分で知らせ、その場で消せる** | 字幕スーパーの出したままは本番中に気づけない事故の型 |
| キー操作 | **一覧を画面に出すが既定では効かない** | v2.9.96 で全廃したのは誤操作で本番に出るため。判断を変えない |
| スマホ | **TAKE を置かない**（24d） | 見張ることと消すことだけ |
| 出力URL | **選ぶだけ**にして URL は組み立てさせない（24b） | 1文字違いは「何も出ない」形で失敗し原因が分からない |
| テンプレート | **一覧には出すが作れないものは言う**（20e） | 隠すと「うちの演出は作れないのか」が分からず毎回聞かれる |
| データの入れ方 | **貼る / 落とす / AIに整えさせる の3つ**（20f） | 届き方が毎回ちがう。メール本文の表は受け口が無く手打ちだった |
| 取り込みの処理 | **1本のまま**（貼った文字はサーバーで xlsx にして既存の取り込みへ） | 2本あると「ファイルなら入るのに貼ると入らない」食い違いが出る |
| 列の当て方の画面 | **作り直さない**（既存のダイアログに貼る口を足した） | 貼るとファイルで当て方が違うと本番中に原因が分からない |
| AI の役割 | **表の形に直すところまで**。取り込むかは人が決める | 中身を作らせない（書かれていない人を足させない） |

### 「AIを使い捨てにしない」5条件の充足

| 条件 | どう満たしたか | 判定 |
| --- | --- | --- |
| 1 出力の記録 | `ai_outputs`（kind=`awards_intake_tidy`・target=`awards_events`・整えた表の**全文**） | ○ |
| 2 修正の差分 | 取り込みのたびにサーバーが `diffByKey`。**鍵は「部門::名前」**。落とされた=`reject`／足された=`enrich`／直された=`fix`。二重計上しない | ○ |
| 3 成果 | `ai_outcomes` に `imported`（入った件数つき）と `dropped`（使わなかった） | ○ |
| 4 還流 | 生成前に `getFeedbackDigest('awards_intake_tidy')` を読んでプロンプトに載せ、**載せた回は版を分ける**（`v1+digest`） | ○ |
| 5 レビュー | MCP `get_ai_feedback_digest` と `/review` の既存経路に乗る | ○ |

**鍵を index にしない理由**: 1行足すだけで以降すべてが「変更された」ことになり、
修正率が実態とかけ離れる。

AIキーが無ければ 503 で「列を自分で当てて取り込むこともできます」と言う（技術用語を出さない）。

### まだ無いもの

- 送出画面からの部門の切り替え（いまは準備の画面で選ぶ。本番中に部門を飛ばす操作は
  誤操作の的が増えるので、次の版で「次の部門へ」1つだけにするか検討する）
- アワード以外のテンプレート（20e は「準備中です」と言うところまで）
- 閲覧だけの人の書き込み制限（awards の権限は今も presence だけを見ている。
  ここを変えると awards の全ルートの意味が変わるので、UI の章では触らなかった）

---

## 15章 案件の書類 / 36章 検索結果 の実装 (v2.9.286)

順番7が完了。デザイン 15章（案件の成果物・書類・カード）と 36章（検索結果の画面）。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 12種の定義 | **コードに置く** | DBだと「種類が変わったのか案件ごとか」が区別できない（22章と同じ） |
| そろったかの判定 | **元データを見る**。人に押させない | 押し忘れが「足りない」として残り、押しただけで実物が無い状態も作れる |
| 並び | **期日順**。過ぎたものは上 | 種類順に並べても次に何をすればよいか分からない |
| まだ要らないもの | **「足りない」に数えず「あとで」に分ける** | ヨミで請求書が無いのは当たり前。数えると印の意味が消える |
| 期日 | **サーバーが計算** | 画面で計算すると案件一覧と食い違う（13章と同じ） |
| 期日の決め方 | 申込書=本番日14日前 / 現場のもの=7日前 / 仮押さえ=45日前 / 入金=支払期日 / カード=返却予定日 | 逆算の根拠を画面にも出す |
| カードの案件紐づけ | **列を1つ足す**（migration 152・任意） | 貸出は「誰に」だけで、どの案件かが分からなかった |
| 検索の権限 | **見えない種類は件数にも出さない** | 「案件 0件」は「無い」と読まれるが実際は「見せてもらえない」 |
| 権限で外した種類 | **名前だけ言う**（件数は出さない） | 黙ると「壊れているのか権限が無いのか」が分からない |
| 検索のSQL | **1か所**。⌘K と検索結果で違うのは件数の上限だけ | 2か所に書くと「⌘K には出るのに検索結果に出ない」が起きる |

### 見つけた既存の穴（36章の本体）

`GET /search` が **ログインしているかだけを見て権限を見ていなかった**。
案件を見る権限が無い人の ⌘K にも

- 案件名と GLS 番号
- お客様の名前
- 仕入先の名前

が出ていた。開くと 403 で止まるが、**名前はもう見えている**。
顧客名・案件名は「誰と何をしているか」なので、それ自体が見せてよい情報ではない。
`meetsPermissionLevel`（HTTP の `requirePermission` と同じ関数）で絞る形にした。

### 検証で見つけて直した実装の穴

`security_card_lendings.due_on` は **DATE 型なので pg が Date オブジェクトで返す**。
`String(v).slice(0,10)` では `Wed Sep 02` になり、画面に
**「Wed Sep 02（9164日 過ぎています）」**と出ていた。
`dateStr()` で Date と TEXT の両方を `YYYY-MM-DD` にそろえた。

### まだ無いもの

- 案件をまたいだ「そろっていないもの」の一覧（いまは案件ごと。ふりかえり・今日の画面に足せる）
- 成果物のファイルそのもの（BOX のリンク。いまは「あるか」だけを見ている）
- 検索の並び替え・期間の絞り込み（いまは種類で絞るところまで）

---

## 25章 お試し の実装 (v2.9.287)

順番8が完了。デザイン 25章（お試し・サンドボックス）。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 練習用の画面 | **作らない**。道具は本物と同じものに入る | 別に作ると練習で通った道と本番で通る道が違い、練習の意味が無くなる |
| 印の向き | `is_sandbox` を**練習だけが持つ**（既定 FALSE） | 逆にすると印を付け忘れた本物が数字から消える。この向きなら「多い方に間違う」ので気づける |
| 数字 | **数える側で外す**（列を1つ足すだけ） | 練習用のDB・テーブルにすると道具を2本書くことになる |
| 外に出るもの | **書く側で止める**（予約・タイマー・GLS・BOX・請求書） | 読む側を1か所忘れると黙って混ざるが、書く側なら「出てしまった」とその場で見える |
| 本物への昇格 | **できない**。画面にもそう書く | 練習の金額・日程が実績に入る道を作らない |
| 片づけ | **人が押す**。自動で消さない | 「作りかけの練習が朝には消えていた」が起きる |
| 予約 | お試しでは**作らせない** | カレンダーに出ると他の人には本物と区別が付かない |

### 「混ざらない」をどう確かめたか

**数字を返す口を20個ぜんぶ叩いて記録 → お試しに 9,999,999 円を入れる → もう一度ぜんぶ叩く
→ 1バイトでも違えば漏れ**、という形で機械的に確かめた。
目で40か所を追うより確実で、あとで数える口が増えても同じ手で確かめられる。

外した場所（約20か所 / 10ファイル）:
案件一覧・件数・タブ・CSV / ダッシュボード（KPI・気づき・最近の案件・売上ボード・月次グラフ・
パイプライン・今週の予定・受信箱・ベル）/ 売上・仕入の一覧と CSV と Excel と MCP
（`list-query.ts` の2つのビルダー1か所で全部に効く）/ 月次サマリー（＝月次PL・予実・MCP）/
ヨミのファネル・失注理由・営業成績 / 週次の数字 / 隔週キープの事実 / 朝の1通 /
さがす（⌘K・検索結果）/ お客様一覧と顧客360 / 請求のしごと / タスクの案件選び /
GLSの選択肢 / 見積の「似た案件」/ AIの成果。

**わざと外していない場所**: 請求書番号の連番カウンタ（全行を数えないと番号が衝突する）、
決算取込の重複判定（全行を見ないと重複を見つけられない）、データビューア（生の表を見る道具）。

### 検証で見つけて直した既存の不具合

**営業成績の画面が 500 で開けなかった**。`SELECT p.assigned_to, u.name … GROUP BY p.assigned_to`
は Postgres では通らない（`column "u.name" must appear in the GROUP BY clause`）。
`u.name` を GROUP BY に足した（2か所）。お試しの検証で全部の口を叩いたときに見つかった。

**ルートの並びの罠**: `/projects/sandbox` は1セグメントなので `/:id` より後ろに置くと
id='sandbox' として食われて 404 になる（`/docs/format` のような2セグメントは食われない）。

### まだ無いもの

- Qシート・リアルタイムCG の練習（別アプリ側にも印を渡す必要がある）
- お試しの中で予約を試すこと（カレンダーに出るので今は作らせない）
- 古いお試しの自動片づけ（いまは日数を出して人が押す）

---

## 6章 部品と数字のサイズの共通化 の実装 (v2.9.288)

順番9（最後）が完了。デザイン 6章。**これでデザイン一式の主要な章はすべて実装済み。**

### 実装を数えたら、こうなっていた

| 見つかったもの | 数 |
| --- | --- |
| `¥{n.toLocaleString()}` を手で書いていた箇所 | 14 |
| 万円に丸める関数（別の名前・別の丸め方） | 4本 |
| 大きい数字に `text-*` を直書き | 2 |
| ページ見出しの重複 | 38か所（書き方が3種類） |

**丸め方が2種類あったのが実害**: `Math.round(v/10000)` と `(v/10000).toFixed(0)` は
**負の数で結果が違う**（`Math.round(-0.5) = -0` / `(-0.5).toFixed(0) = "-1"`）。
取り消しの請求書などマイナスが出る画面があるので、画面によって数字が変わり得た。

**見出しの書き方が3種類あった**: `text-xl lg:text-2xl font-bold` / `text-xl font-bold sm:text-2xl` /
`heading-page text-xl lg:text-2xl`。しかも **`/finance/import` は枠の見出しが `sm:`、
中身の見出しが `lg:`** だったので、**同じページの中で2つの段が混ざっていた**
（幅 640〜1024px では枠と中身で見出しの大きさが違って見える）。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 部品の置き場所 | `shared/src/client/ui/numbers.tsx` に集約 | 7アプリから同じものを使う |
| 数字のサイズ | **4段だけ**（lg / md / sm / inline）。`KpiCard` と同じ値 | 片方だけ変えると同じ数字が画面で違う大きさになる |
| 万円の丸め | `manYen()` 1本 | 負の数で結果が変わらないよう「絶対値で丸めて符号を戻す」形にそろえた |
| ページ見出し | `<PageTitle>` | 38か所・3種類の書き方を1つにする |
| 守り方 | **`npm run lint` の先頭で検査を走らせて止める** | 部品を用意しても手で書けるままだと必ず増える |
| 逃げ道 | 行に `ui-tokens-ok` と書けば見逃す | 例外を作れないと、例外が必要な場面で検査そのものを外される |

### 検査で止めること（理由も一緒に出す）

1. `¥` を数字と一緒に手で書く → 桁が揃わない・丸め方が揃わない
2. 万円に丸める式をその場に書く → 負の数で結果が変わる
3. 大きい数字に `text-*` を直書き → 同じ数字が画面で違う大きさになる
4. ページ見出しの指定を直書き → 見出しの大きさがばらつく

**対象から外したもの**: 部品の実装そのもの（`shared/src/client/ui/`）、各アプリの
再エクスポート層（`components/ui/`）、放送に出るCG（`/cg/`・出力ページ。画面設計の対象外）。

### 検証

- `check:ui-tokens` は **437ファイルで違反0**（着手前は29件、網を広げたあと38件）
- Playwright 1440px で16ページ・375px で6ページ、あわせて **72項目**。
  **見出しの大きさが実測で1つにそろっている**（PC 24px 太字 / スマホ 20px 太字）
- 初回の実行で2ページが FAIL になったが**どちらも検証スクリプト側の誤り**
  （存在しないパスを見ていた / vite の HMR の切断を JS エラーに数えていた）。
  直して再確認し、画面側の不具合ではないことを確かめた

### まだ無いもの

- 色と間隔の共通化（今回は数字と見出しだけ。色は DADS トークンに乗っているので次の段）
- 表の列幅の段（`MoneyCell` の `width` は7段あるが、選ばせているだけで検査していない）

### 全体

デザイン一式（6部36章）の実装は **順番1〜9すべて完了**。
残りは各章の「まだ無いもの」に書いた積み残し（PDFのファイル出力・Qシートの練習など）。

---

## v2.9.289 — 全体コードレビュー 1回目（本番中に出る不具合 + セキュリティ + デプロイ）

デザイン一式の実装が一段落したので、依頼された6点（①全体レビュー ②肥大化の整理
③セキュリティ ④デプロイが遅い ⑤リアルタイムCGとQシートの挙動・バグ ⑥新しいUIUXに
追いついていない箇所）に着手した。**1回で全部やらず、効果が大きく「直った」を
実測で示せるものから**進めている。

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| どこから直すか | **本番中に出る不具合 → セキュリティ → デプロイ** の順 | ①は放送が止まる形で出る。②③は「見えないまま損をしている」形 |
| 接続の持ち方 | `shared/src/client/socketPool.ts` **1本に集約**（鍵ごとに1本・使用数を数え0で切る・**つながっているかは見ない**） | 同じ作りが4か所にあり**4つとも同じ2つの誤り**を持っていた。4か所に書くとまた1か所だけ直し忘れる |
| 受け口の張り方 | `id` につき**1組だけ**張り、中身は ref で最新の関数を呼ぶ | 依存に状態を入れると再生・一時停止のたびに張り直し、外さないので受け口が積み上がる |
| xlsx | **ライブラリは差し替えず、境界で止める** | npm に修正版が無い。差し替えは取り込み3系統の実データ突き合わせが必要で、別の版でやる |
| xlsx の守り方 | `safeReadWorkbook()` 1本に集約し、**`XLSX.read` の直接使用を lint で禁止** | 1か所でも直接読むと境界を通らない |
| 型チェック | **イメージのビルドから出して CI で並走**。`deploy-*` は `needs: [build, typecheck]` | `noEmit` なので成果物は変わらないのに約50秒が待ち時間に乗っていた。**型エラーでデプロイは止める** |
| 残る脆弱性 | **README「既知の課題」に隠さず書く**（開発用 / 実行環境で発現しない / 大版更新が必要 に分類） | 「14件残っている」だけだと判断できない |

### 見つけた不具合（すべて実測で確認）

| # | 不具合 | どう見えるか | 直し方 |
| --- | --- | --- | --- |
| 1 | 接続を「つながっていなければ捨てて作り直す」形。**送信関数が毎回それを呼ぶ** | 回線が一瞬揺れているときに TAKE / 「次へ」を押すと**受け口ごと接続を捨てる**。出ている絵は動いているのに**画面の表示だけ止まる** | `socketPool` に集約。つながっているかは見ない |
| 2 | 片付けで**無条件に切る**（共有している接続なのに） | **片方の画面を閉じるともう片方の同期が黙って止まる** | 使用数を数え、0 のときだけ切る |
| 3 | Qシート OnAir の socket effect の依存に `running`/`next`/`prev` 等。**受け口を `off` していない** | **ランダウンで「次へ」を1回押すとキューが複数進む**／放送中に「切断されました」が出る | 受け口は1組・ref で最新を呼ぶ・**全部 `off` する** |
| 4 | `cue:update` の依存に経過秒（100ms ごとに変わる） | **毎秒10回**送っていた（受ける側も毎秒10回描き直す） | 変化時は即座に、経過だけは**毎秒1回** |
| 5 | `cues` を毎レンダー組み直し | **毎秒10回キュー全体を組み直して effect を走らせていた** | `useMemo` |
| 6 | `setCue` が常に新しい object を作る | 再接続時の**同内容の同期**で出ている絵が描き直され、**演出が跳ねる** | 同値なら同じものを返す |
| 7 | `useAwardsStore()` を引数なしで購読（＝ストア全体） | 票数（3秒ごと）が変わるたびに関係ない画面まで描き直す | 選択購読に |
| 8 | `off('cue:sync')` （ハンドラ指定なし） | **他の画面が張った受け口まで消す** | ハンドラを指定して外す |
| 9 | Qシートの vite proxy が `3000`（他は `3001`）＋ **`/socket.io` の中継が無い** | **ローカルでリアルタイム同期が一度も動かない**。本番の nginx では通るので**手元で再現できないまま本番だけ壊れる** | ポートを揃え、中継を足した |
| 10 | 機材管理と Qシートが**同じ 5174** | `npm run dev` は7本同時なので必ず片方が落ちる（ドキュメントの表は 5175） | 5175 に |
| 11 | `client-daily` の 5180 が許可オリジンに無い | Socket.IO のハンドシェイクが **400 で落ちる** | ポート一覧から生成する形に |
| 12 | ZIP 一括アップロードに展開の上限が無い | 10MB の ZIP から数GB を作れる = **1リクエストでメモリが尽きる** | 1000件 / 展開後300MB |
| 13 | OnAir の操作ボタンに名前が無い（「停止」以外） | **本番中に押すボタンが読み上げでも検証でも識別できない**。±1分は 36px | 名前を付け、44px に |

### 検証

- ビルド 9 ワークスペース通過 / `eslint` 0 errors・**77 warnings**（着手前 78）/ 禁止パターン違反 0（677ファイル）
- **xlsx の境界で 17項目** — 中身での判定・パーサに届く前に日本語で弾く・**prototype 汚染を検知して消して拒否**・正常なファイルは今まで通り
- **実サーバー + 実 Postgres + ブラウザ2ページで Qシート 17項目** —
  接続1本 / 5回操作しても増えない / **cue:update が5秒で5回**（修正前 **50回**）/
  **「次へ」1回でキューが1つだけ進む** / 名前で押せる6つ / 44px 4つ / JSエラー0件
- **同 リアルタイムCG 14項目** — `/awards` 1本 / **`/quiz` も1本**（修正前 **2本**）/
  TAKE 4回で増えない / **出力ページを閉じても送出側の同期が止まらない** / JSエラー0件
- **修正前の実測も取った**（同じ手順を修正前のコードで走らせた）: 接続 **4本** / cue:update **50回**

### 次（このレビューの残り）

- `alert()` / `confirm()` **129か所**を新しい通知・確認の部品へ
- 共通の状態表示部品の適用（**118ページ中46ページ**）
- **1000行超のファイル13本**の分割（最大 `ProjectFormPage.tsx` 2,513行）。
  **バグ修正と同じ版に混ぜない**（差分が大きくレビュー不能になる）
- `react-router` v7 / `box-node-sdk` 4 / `eslint` 10 への大版更新（**それぞれ独立した版で**）

---

## v2.9.290 — ロードマップ1つ目: 通知と確認を部品に寄せる

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 確認の書き方 | `await confirmAction({...})` が **true / false を返す**（`window.confirm()` と同じ形） | 呼ぶ側は1行の置き換えで済む。**分岐の形を書き換えないので条件を反転させる事故が出ない** |
| 取り消せない操作 | `tone: 'danger'` で赤 + **最初から「やめる」にフォーカス** + 外側を押しても閉じない | 開いた瞬間に Enter を押しても実行されない（勢いで通すのを防ぐ） |
| メッセージ | **title と description に割る** | 「削除しますか？」だけでは一緒に消えるものを伝えられない |
| `<ConfirmHost />` が無い画面 | **`confirmAction` は false を返す**（実行しない） | 逆向き（置き忘れたら実行する）にすると、置き忘れが一番危ない事故になる |
| 置き場所 | **アプリのルート直下**（AppShell の中ではない） | OnAir・ランダウン・プロンプターは**AppShell を通らない全画面ページ** |
| 二重に置いたとき | **1つしか出ない**（部品側で防ぐ） | 置き場所を足しても事故らないようにする |
| `alert()` の振り分け | 中身で `notifyError` / `notifySuccess` / `notifyInfo`。**変数の4か所は目で見る** | `catch` の中で「失敗しました」を組んでいたので機械判定では info になっていた |
| 守り方 | `browser-dialog` ルールを `npm run lint` に追加 | 部品を作るだけでは必ず戻る |
| 進め方 | 置き換えは機械的に。**`await` の付け忘れは型チェックに列挙させる** | 85か所を目で追うより確実（`await` を非 async に置くと TS が必ず落ちる） |

### つまずいたところ（記録として残す）

1. **置き場所を間違えていた。** 最初は共通の AppShell に1組置いた。だが Qシートの
   OnAir・ランダウン・プロンプターは全画面ページで AppShell を通らないので、
   **本番中に「停止してリセット」を押しても確認が出せず、黙って何も起きない**状態だった
   （`confirmAction` が false を返す）。ブラウザ検証で気づいた。**型チェックでは絶対に見つからない**種類の不具合。
2. **自分の置換スクリプトに誤りが3つあった。** どれも型チェックとブラウザ検証で捕まえた:
   メッセージ分割で ASCII の `?` も見ていたため **`cat?.name` のオプショナルチェーンで
   テンプレートリテラルを割って構文を壊した** / import の追加位置が**複数行 import の途中**に入った /
   `import` の有無を見る正規表現が **`importance` という変数に当たった**。
3. **v2.9.289 で自分が入れた不具合を1件見つけた。** ルートの `typecheck` が `concurrently` の
   **ラベル順とコマンド順で食い違っていた**ので、ログの見出しが別アプリのファイルを指していた
   （`[equipment]` の枠で Qシートを走らせていた）。**この食い違いのせいで、最初の自動修正が
   別アプリのファイルを書き換えて壊した** — CI のログを読む人も同じように誤解する。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0（683ファイル）
- **残った `alert(` / `confirm(` は 0 か所**（置き換え 129 = 確認 89 + 通知 40）
- **ブラウザで 61項目** — 確認ダイアログ17／お知らせ帯18（PC・スマホ375px）／OnAir 全画面ページ9／
  リアルタイムCG・Qシートの同期17（v2.9.289 と同じ結果 = 壊していない）
- 通した中身: **ブラウザ標準ダイアログが1件も出ない** / 「やめる」で**本当に消えない** /
  「削除する」で**本当に消える** / Esc で閉じても消えない / **4秒たっても帯が消えない（トーストではない）** /
  44px / 横はみ出し 0px / JSエラー0件

### 次（ロードマップ2つ目）

読み込み中・空・エラーの表示を部品に寄せる。**117ページ中44ページ**しか共通部品
（`SkeletonCard` / `EmptyState` / `ErrorPanel` / `NoPermissionPanel`）を使っておらず、
「読み込み中」の直書きが29か所、「〜がありません」の直書きが85か所ある。

---

## v2.9.291 — ロードマップ2つ目: 読み込み中・空・エラー・権限なしを部品に寄せる

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| `EmptyState` が2つあった | **`states/` に一本化**し、`dashboard/` の互換ラッパーを削除 | dashboard 側の既定タイトルが**「データがありません」**（§2.4 が禁じる言い方）。**全24ファイルが実際には title を渡していた**ことを機械的に確かめてから消したので表示は変わらない |
| 読み込み中 | ログイン判定中は `<Delayed>` で包むだけ／画面の中身は `<Delayed><SkeletonRows /></Delayed>` | ログイン前は**骨格が分からない**ので骨組みを出せない。画面の中身は骨格を出したまま中身だけ骨組みにする |
| 空 | 一覧の代わりに**縦の場所を取るブロック**だけ `EmptyState` に | **全部を置き換えない。** 「待たせているものはありません」は良い知らせで空の状態ではないし、検索欄の注記や高さ40の選択リストに枠付きパネルを入れると読みにくくなる |
| 権限なし | `NoPermissionPanel`（**必要な権限を名前で出す**） | 「アクセス権限がありません」だけでは誰に何を頼めばよいか分からない |
| エラー | `ErrorPanel`（原因1文 + 次の一手1文） | **HTTPコード・debug の JSON を画面に出さない**（console に留める・§2.5） |
| 検査の広さ | `empty-by-hand` は **`text-center` を持つブロックに絞る** | 最初に書いたルールは広すぎて、**インラインで正しい12か所まで拾った**。逃げ道を12個作るのは「ルールが間違っている」合図 |

### 数えたこと

| | 着手前 | 着手後 |
| --- | --- | --- |
| 共通部品を使うページ | 117中 **44** | 117中 **63** |
| `EmptyState` の実装 | **2つ**（既定タイトルが違う） | **1つ** |
| ページ全体の読み込み分岐 | 39か所（うち27か所がその場書きのスピナー） | `Delayed` 36か所 / `SkeletonRows` 22 / `SkeletonCard` 17 |
| 権限なしの自作表示 | 5か所 | 0（`NoPermissionPanel` 14か所） |
| 画面に出る debug の JSON | 1か所（機材一覧） | 0（console へ） |

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0（683ファイル）
- **ブラウザ16項目** — 空（何が無いか / 次にやること / **「データがありません」で終わっていない**）、
  権限なし（**白紙でなく権限名「機材管理」が出る** / **403・Forbidden を画面に出さない**）、
  読み込み（**300ms で返る取得では骨組みを出さない** / **3秒かかる取得では出る**）、
  エラー（何をしようとして失敗したか / 原因が日本語 / 次の一手 / **HTTPコードを出さない** / やり直せる）
- **v2.9.289・290 の検証 75項目も再実行して全通過**（壊していないことの確認）
- **ルールが本当に止めることを確かめた**（わざと違反を書いて2つとも落ちる）

### つまずいたところ（検証スクリプト側。実装の不具合ではない）

1. ページの URL と API のパスが同じ形（`/equipment/cables`）だったため、
   **モックがページ自体に当たって画面が JSON になった**
2. Playwright のグロブ (`**/…**`) が当たらず、正規表現に変えた
3. **5xx は最大2回リトライする設定**（1秒・2秒）なので、待ち時間が足りずエラー表示を見逃していた

### 次（ロードマップ3つ目）

1000行を超えるファイル13本の分割。最大 `ProjectFormPage.tsx` 2,513行 /
`BusinessProjectView.tsx` 2,045行 / `EquipmentListPage.tsx` 2,018行。
**1版1〜2ファイル**に分ける（挙動を変えない作業なので、差分が大きいとレビューできなくなる）。

---

## v2.9.292 — ロードマップ3つ目: 1000行超のファイル分割 (1本目・2本目)

**挙動を1つも変えない作業**なので、1版で1〜2ファイルに絞る。差分が大きいとレビューできない。

### やったこと

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `ProjectFormPage.tsx` | 2,513 | **1,925** | 型と定数 (`types.ts` 78) / ジャーニー (`JourneyPanel.tsx` 135) / やり取りの記録 (`ActivityQuickAdd.tsx` 93) / **ダイアログ7種** (`dialogs.tsx` 520) |
| `EquipmentListPage.tsx` | 2,026 | **1,880** | 列の定義 (`columns.ts` 24) / **印刷まわり一式** (`printing.tsx` 181) |

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 何のために分けるか | **探せるようにするため**（行数を減らすためではない） | 2,513行の1ファイルだと「この定数はどこで使われているか」を追うのにファイル内検索しか手が無く、**変更の影響範囲が読めない** |
| ダイアログ7種 | **1ファイルにまとめる** | どれも「開く・閉じる + 1操作」の小さな部品で、単独では開かない（必ず案件フォームから開く）。7ファイルに割ると import が7行増えるだけ |
| どこで切るか | **state を共有していないところ** | 印刷は列を選ぶ・題を付ける・紙に出すだけで、一覧の絞り込みや編集と state を共有していない = 切れ目がきれい |
| state の置き場所 | **親に残す** | 案件フォームのスタジオ日程は約30個の state を**保存処理が読んでいる**。子に移すと挙動が変わる |
| 関数の扱い | **そのまま親に残して props で渡す** | `handlePrint` は `print-landscape` クラスを付けて 150ms 待つ。書き直すと印刷の向きが変わる |

### 検証で分かったこと（直していない）

「やり取りを記録」というボタンが **2つある**。案件の中で書ける方と、警告バナーから
営業活動ページへ飛ぶ方。後者は `ActivityQuickAdd` のコメント「営業活動ページに飛ばさない —
飛ばすと戻ってこないので」と食い違う。**既存の挙動なので今回は直していない**
（挙動を変えない版なので、直すなら別の版で）。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ21項目** — 案件フォーム（ジャーニーの6段 / 次の一手が1つ /
  **ステージ変更の確認に「見積提案」から「口頭決定」と出る** / 失注ダイアログ /
  **やり取りをその場で書ける = 画面を離れない**）、機材台帳（**印刷設定が開く** / 列 / 用紙方向 / 件数 / 印刷実行）
- **v2.9.290・291 の検証51項目も再実行して全通過**

### 残り (11本)

`BusinessProjectView.tsx` 2,047 / `SchedulePage.tsx` 1,515 / `RackLayoutPage.tsx` 1,505 /
`manual/content.tsx` 1,455 / `RevenueListPage.tsx` 1,438 / `EventEditorPage.tsx` 1,294 /
`ProjectGroupListPage.tsx` 1,177 / `project.service.ts` 1,130 / `TodayPage.tsx` 1,117 /
`EditorPage.tsx` (Qシート) 1,094 / `EquipmentDetailPage.tsx` 1,093 / `CueTable.tsx` 1,078。

今回の2本も **1,880〜1,925行**あるので続きがある
（案件フォームのスタジオ日程、機材台帳の登録・一括編集ダイアログ）。

---

## v2.9.293 — ロードマップ3つ目の続き: 分割 3本目 (2,000行超がゼロに)

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `BusinessProjectView.tsx` | 2,047 | **1,520** | 型 (`types.ts` 59) / 月次請求 (`MonthlyBilling.tsx` 181) / 売上一覧 (`RevenueList.tsx` 347) / 仕入一覧 (`PurchaseList.tsx` 102) |

### 決めたこと

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| どこを切るか | **「読むだけの並び」を選ぶ** | この画面の最大の塊はダイアログ2つ（売上明細400行・仕入198行）だが、**中で使う state が 25〜40 個**ある。props に開くと「渡せるが意味が違う」事故が起きやすい。一覧の並びは表示 + 数個のハンドラなので props 7〜18 個で収まる |
| props の型 | **親の宣言をそのまま写す** | 推測で書いたら **5件が型エラーで止まった**（後述）。各ファイルの冒頭にこの注意を書いた |
| 条件分岐 | **位置だけ動かす** | 月次請求は元が `{monthlyMode && (…)}`。中身を部品にして `{monthlyMode && <MonthlyBilling … />}` にした（意味は同じ） |
| 未使用の import | **型チェック (TS6133) に列挙させる** | 目で探すより確実。7件を機械的に消し、残り1件だけ手で消した |

### 型を推測して間違えたところ（型チェックが止めてくれた）

- `monthEpisodes` は `billing_key` ではなく **`episode_code`** を持つ
- `handleDownloadPdf` は**引数が2つ**で、種類（見積書 / 請求書 / **検収書**）を取る
- `handleDeleteMonth` は **Promise を返す**（`async`）
- `openNewForMonth` は `(epId, monthTitle, recMonth?)` の**3引数**

**「渡せてしまうが中身が違う」**形なので、型が無ければ気づけない。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ10項目** — 案件名 / **月次管理と「GLS-B900-YYMM の請求単位」の説明** /
  売上の見出し / **登録した明細の 1,200,000 円が出る** / 仕入の見出しと追加ボタン /
  **仕入ダイアログが開く（親に残した部分）** / 横はみ出し 0px / JSエラー0件
- **v2.9.291・292 の検証54項目も再実行して全通過**

### 残り (14本・2,000行超はゼロ)

`ProjectFormPage.tsx` 1,925 / `EquipmentListPage.tsx` 1,880 / `BusinessProjectView.tsx` 1,520 /
`SchedulePage.tsx` 1,515 / `RackLayoutPage.tsx` 1,505 / `manual/content.tsx` 1,455 /
`RevenueListPage.tsx` 1,438 / `EventEditorPage.tsx` 1,294 / `ProjectGroupListPage.tsx` 1,177 /
`project.service.ts` 1,130 / `TodayPage.tsx` 1,117 / `EditorPage.tsx` (Qシート) 1,094 /
`EquipmentDetailPage.tsx` 1,093 / `CueTable.tsx` 1,078。

`manual/content.tsx` は**文章のかたまり**（画面の使い方の本文）なので、分割の意味が薄い。
次は `SchedulePage.tsx` / `RackLayoutPage.tsx` のような**画面**から進める。

---

## v2.9.294 — 分割 4本目・5本目 (初めて1000行を切った)

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `RackLayoutPage.tsx` | 1,505 | **714** | 定数と型 (`config.ts` 49) / ダイアログ2つ (`dialogs.tsx` 156) / ラック図の絵 (`RackDisplay.tsx` 412) / 印刷用 (`print.tsx` 205) |
| `SchedulePage.tsx` | 1,515 | **1,234** | 型・定数・道具 (`types.ts` 145) / カレンダー連携と種別ピッカー (`dialogs.tsx` 148) |

### 分かったこと: 「切りやすいファイル」と「切りにくいファイル」がある

`RackLayoutPage` は **もともと関数が9つに分かれて並んでいた**
(`RackDisplay` / `DefaultCellContent` / `ConfiguredCellContent` / `UnitBadge` /
`ItemTooltip` / `PrintRackArea` / `PrintRackDisplay` / ダイアログ2つ)。
props はすでに決まっているので**そのまま移すだけ**で済み、−791行と最大の削減になった。

つまり **「1つの巨大な関数」なのか「小さい関数が1ファイルに積んである」のか**で
分割の難易度がまったく違う。後者はほぼ機械作業。前者 (ProjectFormPage / EquipmentListPage /
BusinessProjectView) は props を1つずつ決める必要があり、型を間違えると
「渡せるが意味が違う」事故になる。

### 祝日の一覧がページ本文に埋まっていた

`SchedulePage.tsx` には **2027年までの祝日が手で書いてあった**。
「調べて直すもの」がページの本文にあると、直すたびに 1,500 行のファイルを開くことになる。
`schedule/types.ts` に移した。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ11項目** — 予定表（**「部屋」パネルに切り出した部屋とスタジオ名が出る** /
  **カレンダー連携ダイアログが開く** / **種別ピッカーが開く**）、
  ラック図（**前面・背面の切替** / **印刷ボタン**）、横はみ出し 0px、JSエラー0件
- **v2.9.292・293 の検証47項目も再実行して全通過**
- 検証で「予定表は部屋名を既定では出さない（「部屋」パネルの中）」と分かった。
  最初これを知らずに FAIL と判定したが、**既存の作りであって分割の影響ではない**

### 残り (13本)

`ProjectFormPage.tsx` 1,925 / `EquipmentListPage.tsx` 1,880 / `BusinessProjectView.tsx` 1,520 /
`manual/content.tsx` 1,455 / `RevenueListPage.tsx` 1,438 / `EventEditorPage.tsx` 1,294 /
`SchedulePage.tsx` 1,234 / `ProjectGroupListPage.tsx` 1,177 / `project.service.ts` 1,130 /
`TodayPage.tsx` 1,117 / `EditorPage.tsx` (Qシート) 1,094 / `EquipmentDetailPage.tsx` 1,093 /
`CueTable.tsx` 1,078。

**`manual/content.tsx` は対象から外す** — 画面の使い方の本文そのもの (文章のかたまり) なので、
分割しても読みやすくならない。

---

## v2.9.295 — 分割 6本目・7本目

| ファイル | 前 | 後 | 切り出したもの |
| --- | --- | --- | --- |
| `EventEditorPage.tsx` | 1,294 | **827** | 型と計算 (`types.ts` 80) / 1行ぶんの編集部品 (`rows.tsx` 190) / 部門・賞のカード (`sections.tsx` 222) |
| `RevenueListPage.tsx` | 1,438 | **1,046** | 型・定数・並べ替えアイコン (`types.tsx` 82) / 売上明細の編集まわり (`RevenueItemsEditor.tsx` 379) |

### 決めたこと: 「大きいから切る」ではなく「境目が細いところで切る」

売上ダイアログは **571行**あってこのファイル最大の塊だが、**中で使う変数が 56 個**ある。
props に開くと、親のコードを読むより props の一覧を読むほうが大変になり、**分割で読みにくくなる**。

一方、その中の「明細を編集する部分」だけなら **14 個**で収まる。そこで切った。

| 切る候補 | 行数 | 中で使う変数 | 判断 |
| --- | --- | --- | --- |
| 売上ダイアログ全体 | 571 | **56** | 切らない |
| 明細の編集だけ | 341 | **14** | **切る** |
| 一覧 (カード + 表) | 156 | 16 | 今回は見送り (行数が小さい) |

### 機械的にやるなら、機械が壊したことも機械が見つけられる形にする

未使用 import の自動削除で `type X, type Y` から 1 つ消すときに **`type type Y`** という
壊れた形を作った。**型チェックが即座に構文エラーで止めた**ので実害はゼロ。
`type ` 付きも見るようにパターンを直した。

### 検証

- ビルド9ワークスペース通過 / `eslint` 0 errors・**77 warnings（着手前と同数）** / 禁止パターン違反0
- **ブラウザ15項目** — 売上一覧（**登録した 880,000 円が出る** / 売上ダイアログ /
  **明細の見出し** / **「行追加」で行が増える**）、リアルタイムCG のイベント編集
  （**部門が出る** / **受賞者の行と所属が出る**）
- **v2.9.292〜294 の検証75項目も再実行して全通過**

### 検証スクリプト側のつまずき (実装の不具合ではない)

1. 売上一覧のパスは `/finance/revenues` ではなく **`/finance?tab=revenue`**
2. 明細を足すボタンの名前は「項目を追加」ではなく **「行追加」**
3. 売上一覧は**今月で絞り込んでいる**ので、12月の日付で入れた検証データは出てこなかった

### 残り (12本)

`ProjectFormPage.tsx` 1,925 / `EquipmentListPage.tsx` 1,880 / `BusinessProjectView.tsx` 1,520 /
`SchedulePage.tsx` 1,234 / `ProjectGroupListPage.tsx` 1,177 / `project.service.ts` 1,130 /
`TodayPage.tsx` 1,117 / `EditorPage.tsx` (Qシート) 1,094 / `EquipmentDetailPage.tsx` 1,093 /
`CueTable.tsx` 1,078 / `RevenueListPage.tsx` 1,046。
(`manual/content.tsx` 1,455 は文章のかたまりなので対象外)

---

## v2.9.296 — CI が落ちていたのを直した (検証用の一時ファイルの混入)

### 何が起きたか

v2.9.294 で、実 Postgres の列を調べるために作った使い捨てスクリプト `server/vs7.mjs` を
**消し忘れてコミットしていた**。`eslint` がこれを見て 3 件のエラーを出し
(`console` が未定義 / 未使用の import)、**typecheck ジョブが落ちた**。

### なぜ消し忘れたか

そのコマンドは「スクリプトを作る → 実行する → `rm` で消す」を 1 行につないでいたが、
**実行に時間がかかってバックグラウンドに回され、時間切れで打ち切られた**ため
末尾の `rm` まで届かなかった。次のコミットで `git add -A` が拾った。

### 影響

v2.9.289 で入れたゲート (`deploy-*` は `needs: [build, typecheck]`) が働き、
**v2.9.294 と v2.9.295 は dev に配られていなかった** (dev は v2.9.293 のまま動いていた)。

**壊れたものが配られる前に止まった**ので、ゲートは意図どおりに機能した。
「型エラー・lint エラーでデプロイを止める」を入れておいてよかった事例。

中身は無害だった (検証用の使い捨て DB への接続だけ。秘密情報・本番の接続情報は含まれていない)。

### 再発防止: 「気をつける」ではなく「置き場所を決める」

検証スクリプトは `pg` を解決するためワークスペースの中に置く必要がある。
そこで **`**/verify-scratch/` を `.gitignore` に足した**。ここに置けば消し忘れても混入しない。

### 検証

- `eslint` **0 errors / 77 warnings (基準どおり)** / 型エラー0 / 禁止パターン違反0 / ビルド9ワークスペース通過
- `server/verify-scratch/` にファイルを置いて `git status` に出ないことを確認

---

## v3.1.0 の全体監査で残した課題 (2026-07-29)

サイトツリー・デザイン適用・機能欠陥・AI 導線を6観点で全アプリ横断監査し、
**検証済み 180 件** (high 70 / medium 90 / low 20) を洗い出した。
v3.1.0 で **85 件**を直し、**残り 95 件** (うち high 27 件)。

検証の方法: 各観点の findings を別のエージェントが**実コードで1件ずつ裏取り**し、
成立しないものを落とした (棄却 3 件)。下の一覧は裏取りを通ったものだけ。

**進め方**: 版を小さく切る。見た目が大きく変わるもの (書体の重み・文字サイズの段) は単独の版で出す。

### サイトツリー — 残り 14 件

| 重み | 何が起きるか | どこ | 直し方 |
| --- | --- | --- | --- |
| high | **機材ホームのケーブル等がフルリロードする** — RedirectOnce の 55行 `pathname.endsWith(to)` はクエリ付きの to では常に不一致になるが、200ms タイマーは 63行のクリーンアップでアンマウント時に消えるため、実際にフルリロードは起きない (このフォールバックはクエリ付きリダイレクトで事実上死んでいる… | `shared/src/client/RedirectOnce.tsx:55` | 判定を `const [p] = to.split('?'); if (!window.location.pathname.endsWith(p))` にする (クエリを外して比べる)。あわせて EquipmentDailyPage の5か所を最終URL /e… |
| medium | **「現場の道具」が3か所で中身が違う** — 「現場の道具」という同じ見出しが3か所にあり中身が違う。今日の画面 (TodayPage.tsx:223) は BLOCK_APPS 13枚 (案件管理・財務管理・カレンダーまで含む)、/tools (ToolsPage.tsx:233) は3つ (翻訳・インタラクティブ・CG)、⌘K の『現場の道… | `client/src/contexts/platform/pages/TodayPage.tsx:223` | 名前を分ける。今日のグリッドは廃止 (別項)、/tools は「案件に紐づけていない成果物」に改名、⌘K のグループ名を実体に合わせる。docs/wording.md に3語を登録して check-ui-tokens 相当の検査を足す。 |
| medium | **案件の親子URLが /projects と /sales/projects/:id で不一致** — 案件一覧は /projects なのに、そこから案件を1件開くと URL が /sales/projects/<id> に変わる。ユーザーが URL を短くして /projects/<id> を開くと (メールで送られたリンクを削ってしまった等)、その案件ではなく一覧が黙って出る。どの案件を見ようと… | `client/src/App.tsx:140` | /projects/:id を追加して ProjectFormPage を出し、/sales/projects/:id は RedirectTo で新URLへ寄せる (§3.3 の旧URL保持と同じ形)。243行目の /projects/* は :id より後… |
| medium | **別アプリにいるのに「今日」が点灯する** — 今日の行列で請求書を開き「直してから承認」を押すと、日常業務アプリの /daily/finance が開く。このときレールは『今日』が点灯したままで、左の二次ナビには『ホーム/ウィークリー活動報告/デイリーニュース報告/内覧会 来場予約/見積 / 請求書/その他問い合わせ/セキュリティカード』という… | `shared/src/client/shell/railItems.ts:57` | 日常業務側に「今日に戻る」を置く (client-daily の該当ページの上部)。または請求書・問い合わせの編集を /today のドロワー内で完結させ、別アプリへ飛ばさない。 |
| medium | **貸出カテゴリ画面がどこからも開けない** — /equipment/rental-categories (RentalCategoryPage) はどこからもリンクされていない死んだルート。貸出カテゴリの管理そのものは『モノ（台帳）』→ タブ『貸出機材』の中の『カテゴリ管理』ダイアログ (ModelGroupPage.tsx:359) で同じこ… | `client-equipment/src/App.tsx:61` | 二次ナビの『設定』グループと設定ハブの『機材のマスター』に追加する。あるいは貸出機材設定 (RentalSettingsPage) の中のタブに畳んでルートを消す。 |
| medium | **Qシートの本番画面が押す場所で変わる** — Qシートの本番画面へのURLが2系統ある。一覧の「OnAir」は /qsheet/onair/:id (DashboardPage.tsx:656)、エディタの「OnAir」は /qsheet/live/:id?role=onair (EditorPage.tsx:818)。描かれる画面は Live… | `client-qsheet/src/pages/DashboardPage.tsx:656` | DashboardPage:656 を /qsheet/live/:id?role=onair に変える。App.tsx の 47-49 行を LivePage への ?role= 付きリダイレクトにする (旧URLのブックマークは残す)。 |
| medium | **送出予約(NEXT)の出力URLが配れない** — リアルタイムCG の新しい「出力URLの配り方」画面 (OutputsPage / OUTPUT_USES) は OA の3種 (ランキングCG・字幕スーパー・クイズ) だけで、NEXT (送出予約) の出力3種が入っていない。NEXT のURL自体はイベント編集画面 (EventEditorPag… | `server/src/contexts/awards/services/onair.service.ts:94` | OUTPUT_USES に next 用の3件を追加し (buildOutputUrl は path.replace だけなのでそのまま通る)、OutputsPage の一覧に『OA / NEXT』の2列で並べる。 |
| medium | **日常業務の6機能に入口が3本ずつある** — 日常業務アプリでは、ウィークリー活動報告・デイリーニュース報告・内覧会 来場予約・見積/請求書・その他問い合わせ・セキュリティカードのそれぞれに (a) 左の二次ナビ (b) ホーム画面のカード (c) ⌘K の項目 の3つの入口がある。見積/請求書と問い合わせはさらに『今日』の行列からも開くので4… | `client-daily/src/pages/HomePage.tsx:91` | ホーム (HomePage) を廃止して二次ナビ1本にする (index を /weekly にリダイレクト)。見積/請求書と問い合わせは『今日』側のドロワーで編集まで完結させ、日常業務から外す。 |
| medium | **案件の3タブがURLに載らない** — 案件ワークスペースの『お金 / 日程 / 書類』タブは URL に出ないので、リロードすると必ず『お金』に戻る。他の人に「この案件の書類の状態を見て」とリンクを送っても相手には『お金』が開く。通知や ⌘K からその案件の書類を直接開くこともできない。 | `client/src/contexts/sales/pages/ProjectFormPage.tsx:655` | CustomerDetailPage と同じ ?tab= 方式に揃える (既定はクエリ無し = money)。 |
| medium | **通知の「名前を呼ばれた」がコメントに着かない** — 通知ベルの『案件で名前を呼ばれた』を押すと案件のページが開くが、画面はページ先頭のまま。呼ばれたコメントは下のほうにあるので、自分でスクロールして探すことになる。長い案件では見つけられない。 | `server/src/contexts/platform/routes/dashboard.routes.ts:550` | TasksPage.tsx:66-81 のハッシュ処理を shared のフック (useScrollToHash) に切り出し、ProjectFormPage でも使う。 |
| low | **設定ハブに設定でないものが混ざる** — 設定の画面に『スタジオカレンダー』(予約状況そのもの)、『AI がやったこと』(履歴)、『現場の道具』(成果物の紐づけ) が並ぶ。どれも毎日の業務画面で、設定 (決めごと) ではない。設定を探しに来た人には無関係な行が混ざり、逆にカレンダーを探す人は設定を見ない。 | `client/src/contexts/platform/pages/SettingsHubPage.tsx:90` | 90/144/150 行を削除する (どれもレールか ⌘K から到達できる)。127-137行の『Qシートのマスター』も、説明文自身が「全社共通のマスターにはしていない」と書いているので外す。 |
| low | **絞り込みの「解除」が旧URL経由で2ホップ** — 売上一覧で案件の絞り込みを「解除」すると、/revenues → /finance?tab=revenue と2回URLが変わる。履歴に余分な1件が残るため、ブラウザの戻るを押しても絞り込み状態には戻れず、さらに前の画面まで飛ぶ。仕入一覧の解除 (PurchaseListPage.tsx:297 の… | `client/src/contexts/finance/pages/RevenueListPage.tsx:540` | navigate("/finance?tab=revenue") を直接呼ぶ (または setSearchParams で project_id/project_name を delete する)。 |
| low | **/qsheet/editor が「編集」なのに一覧を出す** — /qsheet/editor を開くとエディタではなくドキュメント一覧が出る。URLだけを見て編集画面だと思って共有すると、相手には一覧が出て「開けない」と言われる。 | `client-qsheet/src/App.tsx:40` | 40行を削除し、旧URL互換が必要なら <RedirectOnce to="/qsheet" /> にする。AppShell.tsx:31 の読み替えも消す。 |
| low | **権限ゲートが6アプリのうち2つにしか無い** — 権限なしの受け皿がアプリごとにばらばら。計時LIVE と日常業務はシェルで <NoPermissionPanel> を出す (client-live AppShell.tsx:96 / client-daily:40-42、usePermissions.ts を持つのもこの2つだけ)。機材管理とリア… | `client-equipment/src/components/layout/AppShell.tsx:47` | client-live の usePermissions.ts を shared に移して4アプリのシェルで使う (module は equipment / qsheet / techsheet / awards)。 |

### デザインの適用漏れ — 残り 20 件

| 重み | 何が起きるか | どこ | 直し方 |
| --- | --- | --- | --- |
| high | **font-medium/semiboldが893箇所で無効** — font-medium (500) 601箇所・font-semibold (600) 292箇所の計893箇所が、先頭書体 LINE Seed JP に 500/600 の face が無いため 400/700 に丸められ、font-normal / font-bold と同じ太さで出る（toke… | `client/index.html:14` | どちらかに決める: (a) index.html の LINE Seed JP に 500/600 を足す (Google Fonts が配信していなければ Noto Sans JP を先に置く)、または (b) `font-medium`/`font-sem… |
| high | **リアルタイムCGが共通部品を使っていない** — リアルタイムCG (/awards/) だけボタン・入力・カード・ダイアログが他の6アプリと全部違う見た目・違う高さ・違う角丸になる。ボタンの高さは `h-9` (36px) が既定なのでスマホのタップ領域 46px を満たさない。ダイアログは Radix を使っていないため Esc で閉じられず、… | `client-awards/src/pages/DashboardPage.tsx:145` | client-awards に `src/components/ui/` を作り、他6アプリと同じ1行の再エクスポート (`export * from '@gmo-onair/shared/src/client/ui'` 相当) を置く。まず操作画面 (Das… |
| high | **音声サポートの待機バッジが読めない** — Qシートの編集画面・OnAir・ランダウン（CueRow 経由）でマイク状態セルの standby が `bg-warning text-warning-strong`（MicAssignmentCell.tsx:46）になり、山吹の面に茶色の文字で実測約1.8:1 = AA 不成立。tokens.… | `client-qsheet/src/components/editor/MicAssignmentCell.tsx:46` | `standby: "bg-warning text-warning-foreground"`、`on: "bg-destructive text-destructive-foreground"` に直す。check-ui-tokens.mjs に「`bg-w… |
| high | **見積請求の日付3列が375pxで潰れる** — iPhone SE 幅 (375px) で日常業務の見積請求 (/daily/finance-docs) を開き「登録」を押すと、締月・支払期日・受信日の3つの日付入力が横1列に押し込まれる。1列あたり約100pxしかないので、`type="month"` / `type="date"` の日付表示… | `client-daily/src/pages/FinanceDocsPage.tsx:282` | `grid grid-cols-1 gap-3 sm:grid-cols-3` に変える。StudioRoomsManagerDialog の `grid-cols-12` は 375px では縦積みにする (`grid-cols-1 sm:grid-cols… |
| high | **アプリ別に見出しの大きさが4段バラバラ** — アプリを渡り歩くと、同じ「ページの名前」が場所によって 14px・18px・20px・24px で出る。計時LIVE のページ見出しは 14px (本文と同じ大きさ) なので、どこにいるのか一目で分からない。リアルタイムCG は 18px、案件管理と機材管理は 20/24px。 | `client-live/src/pages/DashboardPage.tsx:104` | 7アプリの pages を `<PageTitle>` に寄せる。計時LIVE とリアルタイムCG は全画面レイアウトの上部バーに見出しを置いているので、PageTitle に `className` で余白だけ渡すか、shared に上部バー用の小さめの段 … |
| medium | **全面スピナーが49箇所で自作** — 取引先一覧 (/companies) を開くと、一覧の場所が空白になり中央でスピナーが回る。取得が速いときは一瞬だけ点滅して消え、画面がちらつく。検索条件を変えるたびに表全体が消えてまた出るので、どの列を見ていたか分からなくなる。CLAUDE.md が決めた「1秒未満はスピナーを出さない / 骨格は… | `client/src/contexts/sales/pages/CompanyListPage.tsx:221` | 49箇所を `<Delayed><SkeletonRows rows={…} /></Delayed>` に置換 (カードは SkeletonCard、KPI は SkeletonKpi)。規則を「同一ファイル内で `isLoading`/`isPending… |
| medium | **エラー画面が8つの別実装** — アプリが落ちたときの画面がアプリごとに全部違う。技術資料 (/techsheet/) は色を直に書いた白背景の文字で、補助文が #9ca3af (白地で AA 不成立の薄さ) なので何が起きたか読みにくい。日常業務と計時LIVE は素の `<h2>` と赤い `<pre>` にオブジェクトを文字列化… | `client-techsheet/src/main.tsx:29` | shared に `<AppErrorBoundary>` を1つ作り、中身は `<ErrorPanel title error onRetry />` を使う。スタックは `console.error` のみ。7アプリの main.tsx と AppShel… |
| medium | **Qシートと技術資料に権限の壁が無い** — qsheet 権限を持たない人が /qsheet/ を開くと、レールもサイドナビも編集ボタンも普通に出る。中身の取得が 403 で失敗するので、一覧は「読み込み中」のあと空になり、新規作成を押しても黙って失敗する。何の権限が足りないのか画面に出ない。技術資料 (/techsheet/) も同じ。 | `client-qsheet/src/components/layout/AppShell.tsx:24` | `usePermissions` を shared に上げ (モジュール名を引数にする)、client-qsheet / client-techsheet の AppShell に client-live と同じ形の `noAccess ? <NoPermis… |
| medium | **金額の自前ヘルパーが3画面で復活** — 二重計上スクリーニング (/admin/dedup) と決算インポート (/admin/kessan-import) の金額が、ほかの画面 (Money / formatCurrency) と桁区切りの規則が違う。ブラウザの言語設定が日本語以外だと `1.234.567` のような区切りになり、隣の… | `client/src/contexts/platform/pages/DedupScreeningPage.tsx:38` | 3ファイルの自前ヘルパーを消して `formatCurrency` / `<Money>` に置換。規則を「同じ行に `toLocaleString` と (`¥` または `¥` または `'円'`) があれば止める」+「`\\u00a5` の直書きを止める… |
| medium | **表用の金額部品が全体で0回使用** — CLAUDE.md の表は「金額 (表・並べて比べる) → `<Money value={n} />` / `<MoneyCell />`」と決めているが、MoneyCell は誰も使っていない。結果として一覧の金額列の幅がページごとに違い、右揃えの位置も揃わないので、複数ページを行き来して金額を比… | `shared/src/client/ui/money.tsx:1` | 金額列を持つ一覧 (RevenueListPage / PurchaseListPage / SgaListPage / BillingWorkPage / JointEventsPage / client-daily FinanceDocsPage / cl… |
| medium | **香盤表の枠色に状態色を流用** — 香盤表 (/projects/:id/callsheet) の枠が「リハーサル=山吹」「本番=赤」「客入れ=緑」で色分けされている。同じ画面には警告 (warnings) も出るので、赤い枠が「本番の枠」なのか「問題がある枠」なのか区別が付かない。しかも本番・客入れの枠は `positive`/`n… | `client/src/contexts/production/pages/CallSheetPage.tsx:53` | CAT_STYLE と DOT を cat-1〜cat-8 に置換する (setup=cat-8, rehearsal=cat-4, performance=cat-1, audience=cat-3, move=cat-6 など)。状態色は「日付未定」「部屋… |
| medium | **機材の種別記号が状態色で3種重複** — 機材一覧 (/equipment/list) の「種別」バッジで、C (カメラ)・IC (インカム)・NW (ネットワーク) の3つがまったく同じ水色、A (音声) と L (照明) が同じ山吹になる。色で種別を絞り込むことができない。さらに山吹は同じ画面の「修理中」「レンタル」バッジ (Model… | `client-equipment/src/pages/EquipmentListPage.tsx:1866` | 8種別を cat-1〜cat-8 に1対1で割り当てる。状態色 (success/warning/destructive) は ASSET_BADGE (EquipmentListPage.tsx:1851-1856) の「消耗品/リース」判定からも外し、こち… |
| medium | **文字サイズの段が無く1058箇所が任意値** — 同じ意味の補助テキストがページによって 10px・11px・12px・12.5px・13px・13.5px で出る。並べて見ると行の高さが揃わず、表を横に読むと目が上下に振れる。0.5px 刻みの値まであるので、どれが正しいのか実装時に判断できない。 | `shared/tailwind.preset.ts:20` | preset に fontSize の段を定義する (例: `'xxs': 10px, 'xs': 11px, 'sm': 13px, 'base': 15px` を ONAiR の実測値に合わせて7段以内で確定)。そのうえで `text-[Npx]` を禁止… |
| medium | **影の2種決めが守られず104箇所違反** — 同じ意味のカードが画面ごとに違う浮き方をする。リアルタイムCG のモーダルは `shadow-xl`、Qシートのポップアップは `shadow-lg`、案件管理の選択バーは `shadow-sm` で、並べると奥行きの順序が意味と合わない。 | `shared/tailwind.preset.ts:167` | 104箇所を `shadow-card` / `shadow-float` に置換 (面=card、浮くもの=float)。preset の boxShadow を `extend` から `theme.boxShadow` に移して既定の影を消すと、以後は書… |
| medium | **生のselect/textarea/inputが251箇所** — AI 提案の修正欄（client/src/contexts/tasks/components/AiActionBox.tsx:678,703,724,805,823）で案件・お客様・担当者などの選択が OS 標準の生 `<select>` になり、検索できず全件が平坦な option 一覧で並ぶ。高… | `client/src/contexts/tasks/components/AiActionBox.tsx:678` | AiActionBox の5つの select を `SearchableSelect` (案件・お客様・担当者) と shared の `<Select>` (ステージ・種別) に置換し、SearchableSelect を shared に上げる。他は入力… |
| medium | **検索付き選択が機材内に二重実装** — 検索付き選択が client/src/components/ui/searchable-select.tsx（141行）と client-equipment/src/pages/EquipmentDetailPage.tsx:1018（ページ内定義）に二重実装されている。高さ (h-ctl-3) と… | `client-equipment/src/pages/EquipmentDetailPage.tsx:1018` | shared/src/client/ui/searchable-select.tsx に1本化し、そこで矢印キー・Enter・Escape・`role="listbox"`/`option` と `aria-activedescendant` を実装する。cl… |
| medium | **横スクロール囲いの無い表が5箇所** — CLAUDE.md が定めた「横スクロールが発生しうる表は overflow-x-auto で囲む」を満たしていない `<table>` が画面系で13箇所（印刷2箇所を除く）。ProjectGroupListPage.tsx:610 の売上明細表は `w-full text-xs` に w-16/… | `client/src/contexts/sales/pages/ProjectGroupListPage.tsx:610` | 各 `<table>` を `<div className="overflow-x-auto">` で囲む。列幅は `w-col-1`〜`w-col-7` に寄せる。check-ui-tokens に「`<table` の直前6行に overflow-x が無… |
| medium | **7部品が案件管理だけのローカル実装** — 一覧の表・ドロップダウンメニュー・スクロール領域・検索付き選択・金額入力・数値アニメーションが案件管理にしか無い。ほかの6アプリは同じ物を作れないので、機材管理・日常業務・Qシートの表は生の `<table>` を毎回書いており、行の区切り線・ヘッダーの色・hover の当たり方がアプリごとに違う。 | `client/src/components/ui/table.tsx:1` | table / dropdown-menu / scroll-area / searchable-select / currency-input を shared/src/client/ui/ に上げ、client の7本を再エクスポートに変える。そのうえで他… |
| medium | **ダッシュボード部品が現場4アプリで未使用** — 計時LIVE・リアルタイムCG のダッシュボードは KPI の大きい数字も枠付きのセクションも自作で、案件管理や機材管理のダッシュボードと段組み・数字の大きさ・カード見出しが揃わない。ロードマップに「9 ダッシュボード刷新 (Qシート / 機材 / 技術資料 / ライブ Session+Dashbo… | `client-live/src/pages/DashboardPage.tsx:102` | client-live / client-awards / client-daily のダッシュボードを `DashboardHeader` + `KpiCard` + `SectionCard` に載せ替える。載せられない形 (全画面の操作卓など) がある場… |
| low | **共通見出しクラスが出ないウェイト指定** — shared/src/client/tokens.css:313 `.heading-section` と :318 `.heading-card` が font-weight: 600 を指定しているが、先頭書体 LINE Seed JP に 600 の face が無く 700 に丸められない側… | `shared/src/client/tokens.css:315` | `.heading-section` / `.heading-card` の font-weight を 700 に直す。合わせて5箇所を `<PageTitle>` またはセクション見出し用の部品に寄せて、ユーティリティクラス自体を消す (使用が5箇所しかな… |

### 案件管理アプリの欠陥 — 残り 12 件

| 重み | 何が起きるか | どこ | 直し方 |
| --- | --- | --- | --- |
| high | **案件新規登録でスタジオを選んでも予約が黙って作られないことがある** — 案件を新規に作るとき「使用する部屋」を選んで保存すると、案件は保存され「保存しました」と出る。しかし予約 API が失敗した場合(studio 権限が編集未満・お試し案件・タイトル/日付が空)、部屋は1つも押さえられていないのに画面には一切出ない。ユーザーは押さえたつもりで別部署に取られる。 | `client/src/contexts/sales/pages/ProjectFormPage.tsx:599` | catch で `notifyWarning('案件は保存しましたが、スタジオの仮押さえは登録できませんでした（理由）。予定の画面から登録してください')` を出す。予約が本質的に必要なら、保存前に studio 権限を確認して部屋選択欄自体を出さない。 |
| high | **今日の依頼「受ける/相談/辞退」が閲覧権限の人には無反応** — dailyops を「閲覧」だけで持つ人の今日の画面に「あなたへの依頼」カードが出て、受ける/相談/辞退のボタンが押せる。押すと 403 になるが何も出ず、依頼はそのまま残る。依頼した側からは「見たのに答えない人」に見える。 | `client/src/contexts/tasks/components/MyTasksSummarySection.tsx:71` | MyTasksSummarySection でも `hasPermission('dailyops','editor')` を取り、返答ボタンをその条件で描く。respondMutation に notifyApiError を足す。 |
| medium | **案件フォームの「+ 新規顧客」が編集権限では必ず失敗する** — 案件を新規に作る途中で「+ 新規顧客」を押して会社名を入れ「登録」を押すと、sales editor では何も起きない(ダイアログが開いたまま、顧客も作られない)。案件の作成自体は editor でできるので、権限が足りないと気づけない。 | `client/src/contexts/sales/components/CustomerDialog.tsx:39` | mutation に `onError: (e) => notifyApiError('顧客の登録', e)` を足す。さらに ProjectFormPage.tsx:1226-1232 のボタンを `hasPermission('sales','manage… |
| medium | **GLS分類のA↔B切替が管理権限以外では無反応（ダイアログも閉じない）** — 発番済み案件で「ビジネス案件(B)に変更…」を押すと確認ダイアログが開き、「採番し直して変更」を押しても sales editor では何も起きない。ダイアログが開いたまま、GLS番号も変わらず、理由も出ない。 | `client/src/contexts/sales/pages/ProjectFormPage.tsx:637` | `onError: (e) => notifyApiError('案件分類の変更', e)` を足す。ボタンを `hasPermission('sales','manager')` で出し分ける。同じファイルの aiReviewMutation(273) / … |
| medium | **旧GLS一括編集からのヨミ変更は必須チェックと自動処理を飛ばす** — /sales/gls-import で案件を複数選んで「ヨミ」を仮押さえ(d_hold)や受注(a_won)に一括変更すると、本番日も部屋も見積も無いままステージだけが変わる。その案件は「仮押さえ」なのにスタジオの仮押さえ予約が1件も作られず、予定画面の「期限が近い仮押さえ」にも出ない。案件詳細から… | `client/src/contexts/sales/pages/GlsImportProjectsPage.tsx:135` | bulkUpdate で stage を扱うときは1件ずつ `assertStageRequirements` → `applyStageAnswers` を通し、満たさない案件は結果に「できなかった理由」として返す(v3.0.11 の AI 実行器が1件ずつ止… |
| medium | **予約を消しても「期限が近い仮押さえ」に残り続ける** — 予定画面のカレンダーから仮押さえの予約を削除すると、カレンダーからは消えるのに下の「期限が近い仮押さえ」一覧には残る。そこに出ている「本予約にする」を押すと 404 になり、しかも何も表示されないので壊れているように見える。 | `client/src/contexts/production/pages/SchedulePage.tsx:266` | deleteMutation と StudioBookingDialog の createMutation の onSuccess に `qc.invalidateQueries({ queryKey: ["studio-holds"] })` を足し、両方に… |
| medium | **旧URLから取り込み画面を開くと本文が真っ白になる** — ブックマークやSlackに残っている `/budget/kessan-import` `/admin/kessan-import` `/budget/dedup-screening` を budget 権限の人(system_admin ではない)が開くと、`/finance/import?tool… | `client/src/components/layout/routeAdapters.tsx:256` | tool が指定されているのに描けるものが無い場合に `<NoPermissionPanel modules={['admin']} level='manager' />`(権限不足) または `<EmptyState>`(未知の tool) を出す。SOUR… |
| medium | **料金シミュレーションの結果が案件に保存されないまま反映される** — 料金シミュレーションで項目を選び「適用」を押すと、金額と明細は見積フォームに入りダイアログが閉じる。しかし案件へのシミュレーション保存が失敗していた場合(sales 権限が編集未満など)何も表示されない。次にダイアログを開くと選択が全部消えていて、なぜ消えたのか分からない。 | `client/src/contexts/sales/components/SimulationDialog.tsx:161` | `saveMutation.mutate(undefined, { onSuccess: () => { onApply(...); onOpenChange(false); }, onError: (e) => notifyApiError('シミュレーショ… |
| medium | **取得失敗を「登録されていません」と表示して隠す** — 料金表の取得が失敗(500/403/通信断)すると、読み込みスピナーが消えて「カテゴリが登録されていません」と出る。マスターが空だと誤解して登録し直そうとする。再試行の手段も画面に無い。 | `client/src/contexts/sales/pages/PricingListPage.tsx:324` | `if (q.isError) return <ErrorPanel title="料金表を読み込めませんでした" error={q.error} onRetry={() => q.refetch()} />` を入れる。26 画面分は共通ラッパー(query… |
| low | **AIに投げる箱が閲覧権限の人にも出て、投げた時点で権限エラーになる** — dailyops を「閲覧」だけで持つ人の今日の画面の最上部に「AIに投げる」が出る。議事録を貼って読ませると「このモジュールへのアクセス権限がありません」だけが返る。何が足りないのか(誰に頼めばいいのか)分からず、最も目立つ場所が使えないまま置かれる。 | `client/src/contexts/platform/pages/TodayPage.tsx:199` | カタログの応答に「この人が投げられるか」(can_plan)を足し、false なら AiActionBox を出さずに `<NoPermissionPanel modules={['dailyops']} level='editor' />` か1行の説明を… |
| low | **AIの提案「破棄する」が失敗しても破棄したことになる** — AIの提案パネルで「破棄する」を押すと、サーバー側の破棄が失敗しても画面は閉じて「提案を破棄しました」と出る。実際には下書きが残ったままなので、投入ログには宙に浮いた提案が残り続ける。 | `client/src/contexts/tasks/components/AiActionBox.tsx:253` | discardMutation に `onError: (e) => setError(e?.response?.data?.error?.message ?? '破棄できませんでした')` を足す(planMutation と同じ書き方)。 |
| low | **取得エラーを「案件が見つかりません（削除された可能性）」と言い切る** — 案件の回一覧を開いたとき、通信断や 500 でも「この案件は見つかりませんでした / 削除された可能性があります。案件一覧から選び直してください」と出る。実際には案件は存在するので、ユーザーは消えたと誤解して作り直そうとする。再試行の手段も無い。 | `client/src/contexts/production/pages/EpisodeListPage.tsx:23` | `isError` を受け取り `<ErrorPanel title="この案件を読み込めませんでした" error={error} onRetry={refetch} />` を出す。EmptyState は本当に 404 のときだけにする。 |

### 現場アプリの欠陥 — 残り 9 件

| 重み | 何が起きるか | どこ | 直し方 |
| --- | --- | --- | --- |
| high | **リアルタイムCGは切断を検知していない** — 送出用CG (OBS のブラウザソース) とオペレーターの操作画面のどちらも、Socket.IO が切れたことを画面に出さない。切れている間に TAKE すると、`sendCue` はローカルの cue を先に書き換えるのでオペレーターの画面では絵が進んだように見えるが、出力側は最後に届いた絵のまま… | `client-awards/src/hooks/useAwardsCue.ts:41` | useAwardsCue / useQuizStackSocket / useOneShotCue に `connect` / `disconnect` を張って `connected` を返し、オペレーター側の全画面に client-qsheet と同じ `… |
| high | **計時LIVEの接続が再描画ごとに作り直される** — 計時LIVEのタイマー表示機・管理画面で、接続が確立していない一瞬（初回接続中・再接続中）に再描画が起きると、そのたびに新しい Socket.IO 接続が作られる。古い接続は disconnect されずに残って再接続を続けるので接続が積み上がり、`socketRef.current` は最初の1本… | `client-live/src/lib/socket.ts:6` | client-live/src/lib/socket.ts を `createSocketPool<string>({ namespace: '/liveops', options: (timerId) => ({...}) })` に置き換え、useTime… |
| high | **タイマー表示機が切れても最後の秒数を出し続ける** — 会場の表示機 (/live/display/:timerId) で liveops の接続が切れると、残り時間は最後に届いた値のまま止まって表示され続ける。壇上の出演者からは「05:00 のまま動かない」だけが見え、止まっているのか切れているのか分からない。オペレーター側の start/stop も… | `client-live/src/hooks/useTimer.ts:18` | useTimer が `connected` も返すようにし、TimerDisplayPage で切断中は残り時間を薄くして「同期が切れています」を重ねる（放送に出る絵なので色トークンは NOT_A_SCREEN 扱いのまま、文字で出す）。cleanup は … |
| high | **権限が無いと「イベントは0件」と嘘をつく** — awards 権限を持たない人がリアルタイムCGを開くと、「イベントはまだ1件も作られていません」という空状態が出る。実際には 403 で弾かれているだけで、イベントは存在する。権限を足せば見えることが分からず、他の人には見えているので「自分の環境だけ壊れている」と受け取る。500 のときも同じ表示… | `client-awards/src/pages/DashboardPage.tsx:55` | `error` を受け、403 なら `<NoPermissionPanel modules={['awards']} target="リアルタイムCG" />`、それ以外なら `<ErrorPanel title="イベントの一覧を読み込めませんでした" e… |
| high | **見積請求と問い合わせが今日と日常業務の両方にある** — 同じ見積/請求書の行を、/today（案件管理アプリの「今日」の行列）と /daily/finance（日常業務アプリ）の2つの画面から承認・却下・処理完了にできる。どちらが正なのか分からず、片方で承認したものがもう片方のバッジに反映されるタイミングも違う。/daily/finance を開いている… | `client-daily/src/components/layout/AppShell.tsx:24` | /daily/finance と /daily/inquiries も `/daily/tasks` と同じ ForwardTo（それぞれ /today?queue=finance / /today?queue=inquiry）に置き換え、AppShell の… |
| medium | **タイマー一覧が取れないと「タイマーがありません」と出る** — 本番中に計時LIVEのダッシュボードを開き直したとき、タイマー一覧の取得が 403/500/切断で失敗すると「タイマーがありません」「本番進行用のタイマーを登録しましょう」と出る。登録済みのタイマーが消えたように見え、オペレーターがその場で作り直そうとする。 | `client-live/src/pages/DashboardPage.tsx:70` | `error` を受け、失敗時は空状態ではなく `<ErrorPanel>` を出す。`data: timers = []` の既定値で失敗を成功に化かさない。 |
| medium | **貸出機材カテゴリのページに入口が無い** — /equipment/rental-categories を直接打てば貸出機材カテゴリのマスター画面が開いて編集もできるが、アプリのどこからも辿れない（二次ナビにも無い、どのページからも navigate/Link されていない）。しかも同じカテゴリの追加・改名・削除は貸出機材のタブ (ModelG… | `client-equipment/src/App.tsx:61` | RentalCategoryPage を削除して ModelGroupPage 側 1 本に寄せる（コピーを残さない = Phase 5 で TasksPage を移動したときと同じ方針）。ルートは `<RedirectOnce to="/equipment/… |
| medium | **機材のマスターが2か所のメニューに出ている** — 保管場所・メーカー・機材色・貸出機材設定の4つが、プラットフォームの /settings（設定ハブの「機材のマスター」）と、機材管理アプリの二次ナビ「設定」グループの両方に出る。同じ画面への入口が2つあり、どちらから入ったかで戻り先（レールの現在地）が変わる。 | `client/src/contexts/platform/pages/SettingsHubPage.tsx:101` | §4.17 の予定どおり /settings 側を正として、機材管理の二次ナビから「設定」グループ4項目を外す（マスターを触るのは頻度が低く担当者も違う）。 |
| medium | **data?.x.y の形が音声サポート画面に残っている** — `data?.blocks[0]` / `data?.masters.micChannels` / `data?.quizzes.find(...)` という「?. が1段目で止まり2段目は素のアクセス」の書き方が3か所（AudioSupportPage.tsx:351,352 と QuizStac… | `client-qsheet/src/pages/AudioSupportPage.tsx:351` | `data?.blocks?.[0]` / `data?.masters?.micChannels` に直す。あわせて `data?.\w+\.` を止める lint ルールを1本足す（v2.9.267 で人手で洗ったが再発している）。 |

### AI の導線 — 残り 24 件

| 重み | 何が起きるか | どこ | 直し方 |
| --- | --- | --- | --- |
| high | **AI の入口が7種8箇所に散り、集約画面が無い** — 「AI に何を頼めるか」を知る場所が無い。ユーザーは (1) /today の投入欄、(2) /tasks?scope=me の同じ投入欄、(3) 見積画面の「下書きを作る」、(4) /today の行列を開いた先の「返信の下書きをAIに作らせる」、(5) 同じ場所の「作り直す」、(6) 開催マニュ… | `client/src/contexts/platform/pages/TodayPage.tsx:199` | `/settings` 配下に「AI にできること」の1画面を作り、action-catalog.ts の11操作と上記7つの生成入口を1つの表にする (カタログを正にする設計はサーバー側に既にあるので、`GET /dailyops/ai/actions/ca… |
| high | **「投げたものの行き先」に AI の提案が1件も出ない** — /tasks?scope=me では AiActionBox のすぐ下に「投げたものの行き先」という折りたたみがある。AI に投げた直後にここを開いても**必ず空**（または昔の投入だけ）。ユーザーは「さっき投げたのに記録されていない」と読む。 | `client/src/contexts/tasks/pages/TasksPage.tsx:250` | IntakeLogTab を2本のデータ源 (task_intake + ai_action_plans) をマージした「投げたものの行き先」にする。pending 行は該当の確認画面を再開するボタンを持たせ、再開できないなら pending 文言の案内先を直… |
| high | **投入欄で AI が作ったものに AI の印が1つも付かない** — 「AIに投げる」で案件・お客様・タスク・活動記録を作ると、その後どの画面を見ても「AI作成」バッジが付かない。案件一覧の「AI作成の未確認」で絞っても出てこない。/today の「待たせている行列」の AI 起票インボックスにも入らない。「AI がやったこと」にも1行も出ない。つまり v3.0.11 … | `server/src/contexts/tasks/services/action-executor.service.ts:175` | executor に `audit('ui:ai-action:'+kind, …)` 相当の記録を追加する (mcp_audit_log を正の「AI がやったこと」台帳として使い続けるなら最小変更)。あるいは `is_ai_created` の判定を「mc… |
| high | **「AI が作った」の判定が6通りあり、同じタスクが画面によって AI に見えない** — 同じ1件のタスクが、/tasks?scope=me の一覧では「AI作成」バッジ付き、同じ /tasks のリスト表示・ボード表示ではバッジ無し、という状態になる。逆に MCP がメールから作ったタスクはリスト/ボードでは AI作成、/tasks?scope=me では無印。どちらが本当か画面から判… | `client/src/contexts/tasks/pages/MyTasksPanels.tsx:303` | 「AI が作った」の判定を `shared/src/client/aiAttribution.ts` に1本化する (例 `isAiCreated(row)`)。サーバー側も列を1つ増やして (`origin` 等) 明示的に持たせ、6つの推測式を捨てる。 |
| high | **AI が推測した人名が /daily/news と /daily/weekly にまだ出ている** — デイリーニュースとウィークリー活動報告の行に、AI が本文から推測して書いた人名がそのまま表示される。v3.0.6 で「実在しない人名 (寺井武大) を見せていた」として15画面から消したはずの表示が、この2画面では残っている。 | `server/src/contexts/mcp/tools/opsreports.tools.ts:110` | opsreports.tools.ts:110 で `requested_by` を `recorded_by` に流すのを止め、AI 行は `recorded_by = null` + `source='ai'` だけにする。画面側は `source ===… |
| high | **「AI がやったこと」に画面から動かした AI の結果が1件も出ない** — AI の結果を後から確認する唯一の集約画面 /sales/ai-activity を開いても、見積の AI 下書き・返信の AI 下書き・配置図の AI 下書き・awards の AI 整形・投入欄の行動提案の実行は**1件も出てこない**。「今日 AI が何をしたか」を1画面で追えると期待して開い… | `server/src/contexts/platform/routes/dashboard.routes.ts:644` | feed を `mcp_audit_log ∪ ai_outputs` の UNION にする (ai_outputs には kind / target_table / model / prompt_version が既に入っているので画面のラベル付けは可能)… |
| high | **AI が登録した直後の「ひらく」で真っ白な画面かホームに戻る** — 投入欄で AI に問い合わせを記録させて実行し、結果の「ひらく」を押すと**真っ白な画面**になる (/daily/inbox)。活動記録を案件に紐づけずに登録した場合に「ひらく」を押すと**ホームに戻される** (/sales/activities)。 | `server/src/contexts/tasks/services/action-executor.service.ts:323` | `/daily/inbox` → `/daily/inquiries`、`/sales/activities` → `/sales/activity-logs` に直す。あわせて executor の link 一覧 (155,170,180,202,223,… |
| high | **AI がどれだけ直されているかを人が見る画面が無い** — 「AI を使い捨てにしない」の条件5 (レビュー頻度と担当を決める) を実行しようとしても、無修正採用率・よく直される項目・モデル別/プロンプト版別の成績を**ONAiR の画面から見る方法が無い**。人は MCP クライアント (AI エージェント) を通してしか自分たちの AI の成績を読めない… | `server/src/shared/services/ai-feedback.service.ts:111` | `GET /settings/ai/feedback?kind=&days=` を追加し、/settings 配下に「AI の成績」画面を作る (kind 別に reviewed_outputs / as_is_rate / top_corrected_fie… |
| high | **AI 起票案件を直して「確認した」を押しても直した内容が残らない** — AI がメールから作った案件を開き、お客様や日付を直してから「確認した」を押す。次に AI が同じようなメールを読んでも同じ間違いを繰り返す。人が直した内容はどこにも教師データとして残っていない。 | `server/src/contexts/sales/routes/projects.routes.ts:154` | `create_project` / `create_customer` / `create_activity_log` の MCP ツールで `recordAiOutput({ kind: 'project_intake', … })` を残し、`ai-re… |
| high | **AI を使うかどうか・何を任せるかを決める画面が無い** — 「うちは AI に案件を作らせたくない」「GLS 発番だけは AI に触らせたくない」と思っても、画面のどこにもその設定が無い。AI の ON/OFF は環境変数にキーを入れるか外すか (= サーバー再起動) しかなく、社内の誰にも自分で切れない。ユーザー単位・操作単位の同意も無い。 | `client/src/contexts/platform/pages/SettingsHubPage.tsx:141` | `/settings/ai` を新設し、(a) 現在のプロバイダ/モデル/接続状態の表示、(b) action-catalog の11操作それぞれの「AI に提案させる / させない」トグル (DB に保存し catalog API が反映する)、(c) 前述… |
| high | **AI が落ちたとき「サーバー内部エラー」と出て、タスクの登録手段も失われる** — AI プロバイダがタイムアウト/429/障害のとき、投入欄に「サーバー内部エラーが発生しました」だけが赤字で出る。何が起きたのか・次に何をすればよいのかが書かれていない。しかも AI が設定済みの環境では規則ベースのタスク投入欄に切り替わらないので、**そのとき依頼をシステムに残す手段が無くなる**… | `server/src/contexts/dailyops/routes/ai-actions.routes.ts:156` | ai-actions.routes.ts:156 を try/catch し、失敗時は `AppError(503, 'AI_FAILED', 'いま AI が応答しません。タスク・依頼だけなら下の投入欄から登録できます')` にする。クライアントはこのコード… |
| medium | **AI 活動の絞り込みが40種のうち17種しか選べず、残りは英語の関数名で並ぶ** — /sales/ai-activity の「すべての操作」プルダウンには17項目しか無い。議事録の登録・案件メモの追記・セキュリティカードの貸出・月次予算の更新などを AI がやった行は、一覧に `upsert_meeting_minutes` `lend_security_card` のような**英… | `client/src/lib/aiFeed.ts:27` | ラベル表を MCP ツール登録側 (server/src/contexts/mcp/tools/) から生成し、API が日本語名を返す形にする。少なくとも23件を aiFeed.ts に追記し、ラベル漏れを検出するテストを足す。 |
| medium | **配置図の修正差分は PDF を出さないと1件も回収されない** — 開催マニュアルの配置図を AI に下書きさせ、位置を人が全部直しても、PDF を出さずに画面を閉じると**その修正は教師データとして1件も残らない**。「AI の下書きを使わなかった」も画面から記録できない。ManualPage には「直した分は次の下書きに返ります」と書いてあるので、返っていると誤… | `server/src/contexts/production/routes/manual.routes.ts:81` | 配置図を編集するミューテーション (putItem/delItem) の後、または画面離脱時に `/corrections` を叩く。AI 下書きを消して自分で置き直したときは `/dropped` を呼ぶボタン (「この下書きは使わない」) を足す。 |
| medium | **AI が集めたニュースを消しても直しても差分が残らない** — /daily/news で AI が集めたニュースの行を人が書き直したり削除したりしても、「AI がどんな記事を拾い過ぎたか」は1件も蓄積されない。毎日同じ種類の要らない記事を消し続けることになる。 | `server/src/contexts/dailyops/services/ops-report.service.ts:209` | AI が行を足すときに `recordAiOutput({ kind: 'daily_news_items', targetTable: 'ops_reports', payload: { items } })` を残し、`reviewed_at` を打つとき… |
| medium | **返信を直してコピーして送っても、保存を押さなければ直した内容が消える** — AI の返信下書きを画面で書き直し、「本文をコピー」→ 自分のメールで送信 →「送った」を押す、という一番自然な流れをたどると、**書き直した本文が DB に入らず、差分も残らない**。次の下書きも同じ癖のまま出てくる。 | `client/src/contexts/platform/components/todayQueue/InquiryReply.tsx:164` | 「本文をコピー」と「送った」を押したときに、`text` が `reply.final_text` と違えば先に `save` を実行する (または save 未実施なら「先に保存します」と1操作で通す)。 |
| medium | **AI 機能ごとに使うモデルが違い、.env の説明も実装と食い違う** — OpenAI と Anthropic のキーを両方入れると、投入欄・見積・返信の下書きは OpenAI、配置図と awards の整形は Anthropic で動く。運用者は `.env.example` を読んで「OpenAI が優先される」と理解しているので、なぜ2つの請求が立つのか・どのモデル… | `server/src/contexts/tasks/services/intake-ai.service.ts:91` | `intake-ai.service.ts` の `resolveProvider` / `intakeAiModel` を全 AI 機能から使う共通関数にし、manual と awards の独自実装を消す。機能ごとにモデルを分けたいなら `AI_MODEL… |
| medium | **見積の AI 下書きが2種類あり、成績と助言が混ざる** — 「AI が作った見積の下書き」が2箇所に別実装で存在する。案件フォームには橙色の「AI が作った見積の下書きがあります → 確定する」(simulations)、見積画面には紫の「AIが作った下書き → 下書きを作る」(estimates)。どちらを直せばよいか画面から分からず、しかも一方を直した傾… | `server/src/contexts/sales/services/estimate.service.ts:42` | kind を `estimate_draft` (見積) と `pricing_simulation_draft` (料金シミュレーション) に分ける。画面側は「見積の下書き」を1つの入口 (/sales/projects/:id/estimates) に寄せ… |
| medium | **同じ紫が「AI が作った」と「返事待ちの依頼」の両方を指している** — /today と /tasks?scope=me の同じ画面の中で、紫のバッジ/枠が「AI が作った」を意味する場所と「まだ返事が来ていない依頼」を意味する場所が並ぶ。ユーザーは紫を見て「AI が絡んでいる」と読み、実際は人からの依頼で返事待ちだと気づけない。 | `client/src/contexts/tasks/components/MyTasksSummarySection.tsx:111` | AI の紫は `text-ai` / `bg-ai-surface` / `border-ai-border` に統一し、依頼の返事待ちは `warning` か `cat-*` に移す。`raw-palette` ルールの除外から `client/` を外す… |
| medium | **「誰の指示だったかも残る」という説明が2画面に残り、実際には出ない** — 設定ハブの「AI がやったこと」の説明に「誰の指示だったかも残る」と書いてあり、/sales/ai-activity のページ説明にも「誰の指示だったかも残ります」と書いてある。期待して開くと、どの行にも指示者は出ない (v3.0.6 で意図的に消した)。「壊れている」と読まれる。 | `client/src/contexts/platform/pages/SettingsHubPage.tsx:145` | 両方の文を「AI が自動で登録した記録です。誰の指示かは画面には出しません (監査ログで追えます)」に直す。あわせて SettingsHubPage の「AI と連携」から「現場の道具」を外し、`現場の道具` は独立した節にする。 |
| medium | **AI が入れたスタジオ仮押さえがカレンダーで見分けられない** — 投入欄で AI にスタジオを仮押さえさせる (または MCP の create_studio_booking) と、/schedule のカレンダーに他の予約とまったく同じ見た目で並ぶ。「これは AI がメールから読んだ日程なので裏取りが要る」ことが画面から分からず、そのまま本番日として扱われる。 | `client/src/contexts/production/pages/SchedulePage.tsx:1` | 予約行に `is_ai_created` を返すようにし (studio_bookings も mcp_audit_log 逆引き or `source` 列)、カレンダーのイベントに `ai` トークンの印を付ける。少なくとも AI 経路で作った予約は `b… |
| low | **AI の印の色がトークンと生パレットで2種類あり、画面ごとに紫が違う** — AI のバッジ/アイコンの紫が画面ごとに違う実装で出る。案件一覧・お客様一覧・EstimatePage・ProjectMoneyTab は `ai` トークン、/sales/ai-activity(AiActivityPage.tsx:65-66,87,133-134)・/today の AI フィ… | `client/src/contexts/sales/pages/AiActivityPage.tsx:66` | 上記を `ai` / `ai-surface` / `ai-border` に置換し、案件フォームの見積下書きバナーの橙も `ai` に揃える (橙は warning の意味と衝突している)。 |
| low | **AI 活動一覧が画面全体をスピナーに差し替える** — /sales/ai-activity を開くと一覧の場所が大きなスピナーになり、期間チップや絞り込みを押し直すたびに画面が点滅する。他の画面 (TodayQueue 等) は骨組みを出したまま中身だけ差し替わるので、同じ操作の見え方が揃わない。 | `client/src/contexts/sales/pages/AiActivityPage.tsx:116` | `<Delayed><SkeletonRows rows={8} /></Delayed>` に置き換える。`loading-by-hand` ルールの正規表現を三項演算子形 (`isLoading ? … animate-spin`) にも当たるよう拡張する… |
| low | **「AI作成の未確認」で絞れるのに、そこからまとめて確認できない** — 案件一覧で「AI作成の未確認」を押して30件出ても、1件ずつ開いて「確認した」を押すしかない。まとめて確認できるのは /today の「待たせている行列」に出ている分だけで、そちらは50件上限。 | `client/src/contexts/sales/pages/ProjectsPage.tsx:347` | ProjectsPage の行に選択チェックを足し、選択中は `/projects/ai-review-bulk` を呼ぶバーを出す (TodayQueue.tsx:220-240 の実装をそのまま流用できる)。 |
| low | **AI の印だけデイリーニュースが別のアイコンで、名前も付いていない** — /daily/news の行では AI 由来の印が「Bot」の小さいアイコン1つで、文字もツールチップも無い。同じ `ops_report_items` を出す /daily/weekly/:id では「✨ AI」のバッジになっている。同じデータの同じ状態が2画面で別の記号で出る。 | `client-daily/src/pages/DailyNewsPage.tsx:293` | DailyNewsPage の Bot を `<Sparkles />` + 「AI」バッジ + `title={aiOriginTitle('収集')}` に揃える。FinanceDocsPage / InquiriesPage の2件にも `title={… |

### 業務をまたぐ導線 — 残り 16 件

| 重み | 何が起きるか | どこ | 直し方 |
| --- | --- | --- | --- |
| high | **AI実行結果のリンク2種が死んでいる** — 今日の「AIに投げる」で提案を実行した直後に出る結果一覧の「開く」リンクのうち、案件が決まっていないやり取りの記録は押すと「今日」に戻り、問い合わせの記録は押すと真っ白な画面が出る。AI が作ったものを確認しに行けない。 | `server/src/contexts/tasks/services/action-executor.service.ts:303` | 303行を /sales/activity-logs、323行を /daily/inquiries に直す。あわせて executor が返す link を実ルート表 (client/src/App.tsx と client-daily/src/App.tsx… |
| high | **やり取りの記録で案件を選び直す** — 案件詳細の「今すべきこと」で次アクション未設定の警告から「やり取りを記録」を押すと、営業活動記録の一覧に移るだけでダイアログは開かず、案件・顧客も空のまま全件セレクトから選び直すことになる（ProjectFormPage.tsx:872 / ActivityLogPage.tsx:140,501,5… | `client/src/contexts/sales/pages/ProjectFormPage.tsx:872` | ActivityLogPage に ?project=<id>&customer=<id>&new=1 を読ませ、new=1 のときはダイアログを開いて2つのセレクトを埋める。あるいは CustomerDetailPage.tsx:366 が既に持っているイン… |
| high | **現場から写真を上げられない** — 本番終了後にスマホからイベントの実施報告に写真を付けようとすると、写真を選ぶ欄が無く「Box ファイル ID」という文字入力欄しかない。現場のスマホでBoxのファイルIDを調べる手段が無いので、誰かがPCでBoxに上げたあとでないと付けられず、報告がその日に完結しない。 | `client/src/contexts/sales/pages/KeepReportPage.tsx:263` | <input type="file" accept="image/*" capture="environment" multiple> を置き、サーバー側で受けた画像を Box の案件フォルダ (box_url_internal から folder_id を引… |
| medium | **押さえるボタンがダイアログを開かない** — 案件の予定タブで「まだ日程が押さえられていません」の下の「カレンダーで押さえる」(または「足す」) を押すと、予約ダイアログは開かず、その案件で絞り込まれた=1件も予定が無い空のカレンダーが出る。自分で日付枠をクリックし、ダイアログで案件をもう一度選ぶ。 | `client/src/contexts/sales/components/ProjectScheduleTab.tsx:107` | ProjectScheduleTab の2箇所を ProjectFormPage.tsx:1166 と同じ state 経路に統一する。より望ましいのは SchedulePage の435行の useEffect にクエリ経路 (?project_id=X&n… |
| medium | **お客様から案件をつくると顧客が空** — お客様詳細を開いて「案件をつくると、ここに並びます」の「案件をつくる」を押すと、起票画面のお客様欄が空で開く。いま開いていた会社名を自分で打ち直して候補から選ぶ。3項目しか聞かない起票画面のうち1項目を毎回二度入力することになる。 | `client/src/contexts/sales/pages/CustomerDetailPage.tsx:656` | ProjectIntakePage で ?customer=<id>&customer_name=<name> を読み、あれば customerId と表示名を初期値にする (「変える」で外せる現在のUIのまま差し込める)。CustomerDetailPage… |
| medium | **リアルタイムCGが案件と結びつかない** — 本番当日、案件からリアルタイムCGへ行く導線が無いだけでなく、CG側にも案件・GLS番号の欄が無い。イベント一覧からイベント名を目で探して開くしかなく、同じ名前の回が複数あると取り違える。案件詳細から「この案件のCG」を確認することもできない。 | `client-awards/src/App.tsx:60` | awards_events に project_id (nullable) を足す移行を1本入れ、イベント作成・編集で案件を選べるようにする。そのうえで案件詳細に計時LIVEと合わせて「本番の道具」2行を出し、CG側の breadcrumb に案件へ戻るリンク… |
| medium | **香盤表から本番の道具へ行けない** — 本番当日に香盤表を開いても、そこからQシートのOnAir・計時LIVE・リアルタイムCGへ行くリンクが無い。当日の動きを1枚にまとめた画面なのに、当日実際に押す3画面へは毎回⌘Kでアプリを開いて名前で探し直す。しかもOnAir・ランダウン・プロンプターは共通シェルの外なので、その先ではレールも⌘Kも… | `client/src/contexts/production/pages/CallSheetPage.tsx:246` | 香盤表のヘッダーに「当日開くもの」の行を足し、この案件に紐づくQシート (qsheet_documents.project_id) と計時LIVEの番組 (liveops_programs.project_id) を引いて素の <a> で並べる。存在しないもの… |
| medium | **損益の期間が売上一覧に効かない** — 月末にお金 (/finance) で期間を先月にして損益を見たあと、損益の流れの「売上」を押すと、売上一覧は今月で絞られて開く。さっき見ていた金額の行が1件も出ないので「データが消えた」ように見える。仕入・販管費も同じ。 | `client/src/contexts/finance/pages/RevenueListPage.tsx:77` | FinancePage の期間をクエリ (?month=2026-06 等) に持たせ、flowSteps の to にそのまま付ける。RevenueListPage / PurchaseListPage / SgaListPage は monthFilter… |
| medium | **タスクを押すと権限なしが出る** — 日常業務の権限を持たず営業の権限だけを持つ人がレールの「タスク」を押すと、案件のタスクは全部見られるのに「日常業務の閲覧権限が必要です」というパネルが最初に出る。上のスコープ切替を押せば見られるが、初回は権限が無いページに来たように見える。 | `client/src/contexts/tasks/pages/TasksPage.tsx:166` | TasksPage の scope 既定値を権限から決める (canDailyops ? "me" : "all")。レール側の href も dailyops を持たない人には /tasks?scope=all を渡すか、TasksPage の既定値だけで済… |
| medium | **「現場の道具」が3箇所で別物を指す** — 同じ「現場の道具」という見出しが2箇所（今日の SectionCard = 13アプリ / ToolsPage = 翻訳・インタラクティブ・CGの3つ）で別の集合を指し、⌘K でもこの語で括られた10項目が更に別の集合だった。⌘K 側は現在の作業ツリーで section 体系に書き換えられ「現場で使… | `client/src/contexts/shared/pages/ToolsPage.tsx:42` | 集合ごとに名前を分ける。/tools は実体が「案件に紐づいていない成果物と外部ツール」なので⌘Kのグループ名と揃えて改名し、今日のカード群は上の finding のとおり縮小する。docs/wording.md の言い換え表に3語を分けた記録を残す。 |
| medium | **機材と仕入先の検索が一覧に飛ぶ** — ⌘Kや検索結果で機材名を打って当たった1件を選ぶと、その機材の詳細ではなく機材アプリのトップ画面が開く。探していた機材をそこでもう一度検索する。仕入先も同じで、押すと仕入先一覧の先頭が出る。 | `client/src/contexts/platform/pages/SearchResultsPage.tsx:69` | equipment を /equipment/items/${r.id} にする。仕入先は詳細画面が無いので /budget/vendors?highlight=<id> のように一覧で該当行を出す形にする (一覧のままなら選んでも何も起きないことを画面に書く… |
| medium | **案件の詳細だけ旧URLに残っている** — 案件の一覧は /projects なのに、案件1件の詳細は /sales/projects/:id のまま。URLを見て /projects/<案件ID> を開くと、エラーも出ずに案件一覧に飛ばされる (開こうとした案件は分からない)。通知・AIの実行結果・検索・書類タブ・お客様一覧はすべて /sa… | `client/src/App.tsx:243` | /projects/:id を正のパスとして追加し (ProjectFormPage をそこにも割り当て)、/sales/projects/:id を RedirectTo で新パスへ寄せる。243行の catch-all は :id より後に評価されるので残… |
| medium | **問い合わせ画面から案件にできない** — ⌘Kで「その他問い合わせ」を選んで /daily/inquiries を開くと、AIが分類した問い合わせが並ぶだけで「ネタ案件にする」ボタンが無い。案件にするには今日 (/today) に戻り、同じ問い合わせを行列の中から探し直す。同じデータに2つの画面があり、片方だけが仕事を終わらせられる。 | `client-daily/src/pages/InquiriesPage.tsx:39` | どちらかに寄せる。今日の行列を正とするなら ⌘K の open-daily-inquiries を /today に向け、/daily/inquiries は今日へリダイレクトする。/daily/inquiries を残すなら、そこにも promote ボタン… |
| medium | **案件からイベント報告に行けない** — 本番が終わった案件の実施報告を書こうとしても、案件詳細のどこにもその入口が無い。ふりかえり (/review?tab=keep) を開き、実施済み案件の一覧から自分の案件を探して初めてダイアログが出る。⌘Kで「イベント報告」「写真」と打っても何も当たらない。 | `client/src/contexts/sales/pages/ReviewPage.tsx:216` | 案件詳細に「イベント報告」の行を出す (実施日を過ぎていて report_status が confirmed でない案件だけ)。押したら /review?tab=keep&project=<id> に飛ばし、ReviewPage 側でその案件のダイアログを開… |
| low | **AI投入欄が2画面にあり案内が片方だけ** — 「AIに投げる」の入力欄が今日 (/today) とタスク (/tasks?scope=me) の2画面に出るのに、投入したものの一覧 (IntakeLogPanel) はタスク側にしか無く、⌘K の「AIに投げる」は今日にしか案内しない。今日から投入の確認へ寄せるリンク（MyTasksSummar… | `client/src/contexts/tasks/pages/TasksPage.tsx:176` | 入力欄は今日に1つだけ置き、タスク側は投入の記録だけを残して「投げるのは今日から」と1行書く。今日の AiActionBox の下に「投げたものを見る」→ /tasks?scope=me のリンクを出し、投げてから確認するまでを1本にする。 |
| low | **通知のコメントが本文まで飛ばない** — 通知ベルで「コメントで呼ばれています」を押すと案件が開くが、画面はページ先頭のまま。コメント欄は案件詳細のかなり下にあるので、呼ばれた本人が長いページをスクロールして探すことになる。 | `server/src/contexts/platform/routes/dashboard.routes.ts:550` | TasksPage.tsx:66-80 のハッシュ寄せを shared のフック (useScrollToHash) に切り出し、ProjectFormPage でも呼ぶ。ハッシュ付きの通知は今後も増えるので、リンクを出す側ではなく共通シェル (AppShel… |


## カレンダーの二重登録 (v3.1.1 で止血、以降の段取り)

この節は**現役の計画**として [docs/ops/calendar-dedup-plan.md](../../ops/calendar-dedup-plan.md) へ切り出した（2026-08-22・段4以降が未着手のため）。
