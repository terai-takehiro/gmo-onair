# モックに合わせる作業一覧（v4）

> **状態**: 現役の計画（進行中）— ①「実害4件」は4件とも判断済み・実装済み。②のうち大5件は完了。小・中は「表に取り消し線が無い行」が残り
> **最終確認**: 2026-09-08（v4.6.10）
> **位置づけ**: モック（`docs/design/v4/mockups/`＝v4 の正）と実装の差の作業一覧。`docs/README.md` の「実装との乖離」から参照される。⚠️ 要確認: 取り消し線が無いまま本文に「済」「改名済み」「→ ①（要判断）」と書かれた行が混在しており、未着手の実数は行ごとの棚卸しが要る（この改訂では本文の表を書き換えていない）。

**前提**: モック（`docs/design/v4/mockups/`）が v4 の正です（`docs/v4-plan.md` の「大前提」）。
実装がモックと違うところは**すべてモックに合わせます**。この文書は
①モックどおりにすると実害が出る4件（止めて相談する分）と
②モックに合わせるためにやること（作業一覧）の2つだけです。

「そう決めたから」「そのほうが親切だと思ったから」で残していたものは、
**この改訂で全部「モックに合わせる」側へ移しました。**

---

## ① モックどおりにすると実害が出る4件（ここだけ止めます）

判断をいただくまで実装しません。**それ以外はモックに合わせて進めます。**

### 1. セキュリティカードのレベルを3分類に畳む — **違う部屋のカードを渡すことになります**

モックは「全域 No.1-4 ／ 技術 No.5-14 ／ 共用 No.15-24」の3分類ですが、
**この帯は本番の実データと一致していません**（実測）。

| モックの帯 | 実データ |
| --- | --- |
| 全域 = No.1-4 | `master` は **No.1-9**・入れる部屋 **10** |
| 共用 = No.15-24 | `meeting` は **No.19-21**・入れる部屋 **1** |

6レベルを3つに畳むと、**ROOM A のカードと ROOM B のカードが画面上で同じに見えます**。
セキュリティカードは実物を人に手渡す運用なので、**入れない部屋のカードを渡す**か、
**入れてはいけない部屋に入れるカードを渡す**ことになります。

- **推奨**: レベルは DB の6段のまま。**モックの3分類は「絞り込みの束ね方」として採用**し、
  カード1枚の表示は実際のレベル名と部屋数を出す（見た目はモックどおりにできます）
- モックの帯どおりに畳むご判断なら、**先に台帳側の番号とレベルを実運用に合わせて振り直す**必要があります

### 2. 請求書番号の自動採番 — **会計側の番号と二重になります**

`revenues` に請求書番号の列がありません。採番を始める前に3つ決める必要があります。

- **連番の単位**（年度ごと／月ごと／通し／拠点ごと）
- **取り消したときの扱い**（欠番にする／詰める）
- **いま経理が使っている番号との突き合わせ**（既存の請求書に付いている番号をどう扱うか）

決めずに採番すると、**同じ請求書に ONAiR の番号と会計システムの番号が2つ付きます**。
入金消込は番号で突き合わせるので、ここがずれると**入金が消し込めなくなります**。

- **推奨**: ①年度ごとの通し ②取り消しは欠番（詰めると過去の請求書と食い違う）
  ③既存分は空のままにして、新規発行分から採る。この3点でよければ実装します

### 3. GLS 番号を「受注が決まったら自動で採る」 — **押し間違いで番号が焼けます**

いまは `POST /projects/:id/issue-gls` を人が明示的に叩きます。モックどおり
ステージが `a_won` になった瞬間に採ると、**ステージを押し間違えただけで番号が消費されます**。
GLS 番号は BOX のフォルダ名・Qシート・請求書に載るので、**取り消しても番号は戻せません**。

- **推奨**: 自動採番にするが、**`a_won` に上げるときの確認ダイアログに「GLS番号 GLS◯◯◯ を採ります」と出す**。
  モックの「自動」を満たしつつ、押し間違いは止まります
- 「発番済みの案件を `a_won` から戻した場合」の扱いも決めが要ります（番号は保持する想定）

### 4. プロジェクト管理の BOX サブフォルダ — **本番の BOX に実物のフォルダができます**

モックは 個別見積／原価・発注／議事メモ／図面／仕様書／工程表 の6フォルダですが、
既存の `box-folder.service.ts` は案件用の別構成（01_見積・提案 …）を作ります。
**BOX に作ったフォルダは人が手で消すしかありません**（アプリからは消せない）。

- **推奨**: プロジェクト用のフォルダ構成として**別に定義**し（案件用と混ぜない）、
  **検証環境の BOX で1件作って中身を確認してから**本番に出す
- 命名（プロジェクト番号の形）も先に決めが要ります

---

## ② モックに合わせるためにやること

「データが無い」「動く先が無い」は**作らない理由になりません**（列を足す・API を作る）。
規模の目安を付けてあります。

### 共通・トップページ

| モック | いまの実装 | やること | 規模 |
| --- | --- | --- | --- |
| 受付カード3タイル（メール取込・**録音**・手で登録） | 動く2つだけ | 録音は**案件詳細のやり取りタブに実装済み**（Whisper→議事録）。タイルからそこへ送る | 小 |
| プロジェクト管理のタイルを出す | 出していない | 出す | 小 |
| ホームのタブ・挨拶チップ「**お待たせ中**（お待たせ N件）」 | 「**受信箱**」に改名済み（**意図した逸脱・戻さない**） | 済。根源整理 Phase 1 のご指示（[docs/core-redesign-plan.md](core-redesign-plan.md) §3-3・§6）— 4種の寄せ集めを「お待たせ中」という1つの状態の言葉で束ねない | — |

### 案件管理

| モック | いまの実装 | やること | 規模 |
| --- | --- | --- | --- |
| ダッシュボードに「今月の**受注**」 | 「今月の売上（確定）」 | `project_stage_changes`（案件id・前/後ステージ・変えた人・日時）を足し、`change_project_stage` と `update` から書く。停滞理由の具体化も同じ表で出せる | 中 |
| 「見積を送ったまま連絡がありません」等の具体的な停滞理由 | ステージから導いた一般文言 | 上と同じ表で実現 | 小（上に含む） |
| ネタ一覧に「入口（メール/電話）」「確信（高/中/低）」 | 出していない | `projects.intake_channel` / `intake_confidence` を足し、**MCP の `create_project` にも引数を足す**（スキル側の追随も要る） | 中 |
| 受付は引き合い（メール・電話）だけ | 4種類 | 引き合いだけにする。**期限超過アクションはダッシュボードの「止まっている案件」に寄せる**（受け皿を作ってから外す） | 中 |
| ネタ →「**E 問合せ**」 | 「ネタ」のまま | 改名。`stages.ts` とバッジ、`docs/wording.md` も直す | 小 |
| 「D 仮押さえ」は**灰色** | 淡い青 | モックの灰にする | 小 |
| タブ名「**エピソード**」 | 「回」 | 改名。**`docs/wording.md` の禁止語と `check-ui-tokens` の `forbidden-wording` から「エピソード」を外す**（ルールのほうを直す） | 小 |
| 案件詳細に**請求タブは無い**（見積のみ） | 「見積・請求」にしている | 見積タブに戻す。請求は ⑤ 見積・請求（全案件）で見る | 小 |
| PC サイドバーに**受付は無い** | メニューに出した | 外す。入口はダッシュボードのカード | 小 |
| ~~案件をつくるのは**全画面モーダル1枚（16項目）**~~ | ~~フォーム画面~~ | **済（大①・migration 170）**。全画面1枚・必須は4つ。無かった6項目（ご担当・継続区分・規模・やりたいこと・返事の期限・求められているもの）の列を足した | 大 |
| タグ／案件種類（その他）／教訓・学び を**消す** | 残した | **画面から外す。列は残す**（既存データを消さない）。移行が要るデータがあれば別途 | 小 |
| ~~料金表を**拠点ごと**に持つ~~ | ~~1つの表~~ | **済（大③・migration 172）**。`pricing_categories.location_id`（分類ごと場所に属する）。既存78品目は全部用賀の料金なので `loc-yoga` へ。空の場所は「料金は未定」＋写す/空から作る。**写せるのは空の場所だけ**。見積側は案件のスタジオ予約から場所を決め、**複数拠点にまたがる案件では推測しない**。MCP `list_pricing` に `location_id` / `project_id` | 大 |
| 料金表の「変更の記録」パネル | 載せていない | `pricing_history` を足し、`created_by`/`updated_by`（migration 139 で追加済み）と合わせて出す | 中 |
| ⑦ 標準工程テンプレート | 未着手 | 作る。**スタジオ案件用とプロジェクト用の2系統**（GPM 側は `gpm_templates` として実装済みなので、案件側を同じ形で作る） | 中 |

### 財務管理

| モック | いまの実装 | やること | 規模 |
| --- | --- | --- | --- |
| 取り込みの行ごとに **AI 読み取り信頼度 %**（80未満は橙） | 出していない | パーサに**実際に数えられるスコア**を持たせる（必須項目のうち何項目を読めたか・金額の桁が合うか・日付が解釈できたか）。**ベタ書きの数字は置かない** | 中 |
| 「BOXに置く必要はありません」 | 書いていない | **文言ではなくパイプラインを直す**。直接アップロードを Box を経由しない解析に通せば、モックの文言が事実になる | 中 |
| 販管費を**勘定科目**で絞る | 固定費/都度・社員/経理 | `sga_expenses.account_title` を足す（マスターも要る）。既存行の移行方針が要る | 中 |
| 取引先ごとの**取引額** | 出していない | 期間は**今年度**に決めて出す（切替は後から足せる） | 小 |
| 請求書番号 | 採番していない | **→ ①-2（要判断）** | — |

### 設定

| モック | いまの実装 | やること | 規模 |
| --- | --- | --- | --- |
| **権限は役割に付ける**（役割5種 × 権限8項目 × 3レベル・値引き上限も役割ごと） | **人ごと**にモジュール権限を持つ | `roles` と `role_permissions` を作り、`user_permissions` を役割経由に読み替える。**サーバーの権限判定を全部通る**ので単独の回にする。既存の人ごとの権限をどう役割に寄せるか（＝いま誰と誰が同じ権限か）を先に実測する | 大 |
| ⑤ **お金のルール**（締め日・支払サイト・消費税の扱い） | 表が無い（取引先ごとに手入力） | **どの単位で決めるか**（会社ぜんぶ／取引先ごと／案件ごと）を決めてから表を作る。決めずに作ると、あとから単位を変えるときに入った値が全部使えなくなる | 大 |
| ⑥ **休日・営業時間** | 表が無い | 入れると予約が「その時間は取れません」と言えるようになる。ただし**いま入っている予約に営業時間外のものがある**はずなので、既存分の扱い（そのまま通す／警告だけ出す）を先に決める | 中 |
| ⑦ **通知とテンプレート**（メール文面と送り先） | 表が無い（送るたびに書く） | **どの通知を誰に送るか**の一覧を先に作る。文面だけ先に作ると、使われない文面が溜まる | 中 |

> ⑤⑥⑦ は**設定トップに「これから作ります」として並べてあります**（押せない形）。
> 消すと「設定にそんな項目は無い」と読まれるため。

### 日常業務

| モック | いまの実装 | やること | 規模 |
| --- | --- | --- | --- |
| ニュースの「採用した行を週報に送る」 | 出していない | `POST /dailyops/news/:id/to-weekly` を作り、`ops_report_items.source_news_id` を足す | 中 |
| 週報トピックの「ニュース由来」バッジ | 出していない | 上の列で出せる | 小（上に含む） |
| ~~入ってきた情報に **出どころ・4段階の状態・タグ・チケット連携**~~ | ~~重要度と対応済みだけ~~ | **済（大②・migration 171）**。行き先は**5つ**にした（「案件にした」を足した — 無いと送ったものが未仕分けに残り、**同じ引き合いから案件が2件できる**）。チケットは `project_tasks` を1本作る。案件は登録モーダルへ送って書き戻す。MCP の `record_inquiry` に `source` / `tags` を足した。**本番のメール取込スキルは Git 管理外なので直せない** — 何を直すかは `docs/mcp-server.md` に書いた | 大 |
| セキュリティカードのレベル3分類 | 6レベル | **→ ①-1（要判断）** | — |

### 機材管理

| モック | いまの実装 | やること | 規模 |
| --- | --- | --- | --- |
| 機材台帳は**3タブ**（ケーブル・コネクタで1つ） | 4タブ | 1つに畳む。ケーブルにしか無い2列（長さ・色）は**コネクタ行では列ごと出さない**（行内で出し分ける） | 中 |
| 設定「貸出の決めごと」**6スイッチ** | 列挙して「保存先がありません」 | `equipment_settings`（キー・値）を足し、6つとも実際に効かせる。**「返却予定日の初期値」と「修理中は貸出不可」は貸出処理側の分岐も要る** | 中 |
| ダッシュボードに「本日・明日の**入出庫**」 | 「返してもらう」だけ | `equipment_lendings` に**貸出予定日**を足す（いまは持ち出した瞬間の記録しかない） | 中 |
| QRスキャンの**読み取り履歴**とその場で貸出返却 | 出していない | `equipment_scans` を足す。貸出・返却の実行は既存 API を呼ぶ | 中 |
| 台帳の列に「貸出可」チェック | 設定タブに置いた | 台帳に出す。**owner 権限が無い人には押せない形で出す**（消すと理由が分からない） | 小 |
| ~~ラック図も v4 に~~ | ~~据え置き~~ | **済（大④）**。12本の横並び → 左に一覧・右に1本＋実装一覧の表。印刷の寸法には手を入れず、位置だけ動かした。**実ブラウザで A4 の PDF を出して確認**（11項目）— 画面の中身が印刷で場所を取って**白紙が1枚増えていた**のを `print:hidden` で直した（2ページ → 3ページ → 2ページ） | 大 |
| （モックに無い）ケーブル/コネクタの列の出し入れ・表編集 | v4 で外した | **モックに無いので外したままにする** | — |

### プロジェクト管理

| モック | いまの実装 | やること | 規模 |
| --- | --- | --- | --- |
| ~~⑥ 見積・請求（提出先＝自社／依頼元／PM会社）~~ | ~~作っていない~~ | **済（大⑤・migration 173）**。`project_id` を NULL 可にし `gpm_project_id` / `submit_to` を追加（両方入らない CHECK 付き）。**先に `estimates` を読む場所を実測: 3か所** — うち `salesOverview` の「見積の返事待ち」だけが案件の結合を持たず、**GPM の見積が案件管理に足される穴**だった（塞いだ）。明細の入力はまだ（案件の見積と同じ部品を使う。写しを作ると合計の計算が2か所になる） | 大 |
| ~~ダッシュボードの KPI **5枚**~~ | ~~3枚~~ | **済**。個別見積 未提出 ／ 検収待ち を追加。**検収待ちは「受注にしたが検収が済んでいないもの」**で数える — プロジェクトの見積は売上に変換できない（`revenues.project_id` が NOT NULL）ので請求前のものも入る。**その但し書きをカードに出している** | 小（上に含む） |
| ⑤ 全プロジェクトの**タスク一覧** | 「次にやること」 | GPM 専用のタスク一覧を作る（`project_tasks` は `project_id` NULL なので既存の一覧は使えない） | 中 |
| ③ 詳細に**体制図**（発注者／PM会社／自社のボックス図） | 一覧（読むだけ） | ボックス図にする。**`gpm_members` の書き込み API を作る**（いま入れる口が無い） | 中 |
| ④ 新規作成は**5段** | 3段 | 5段にする（4段目＝体制、5段目＝メンバー・書類）。上の2つが前提 | 中 |
| BOX のサブフォルダ構成 | 作らない | **→ ①-4（要判断）** | — |
| モックの生の色コード（`#a6ceeb` 等） | トークンに写した | **モックに無い色はトークンに足す**（`_tokens.md` と突き合わせる）。生の色コードを画面に直書きはしない（色を切り替えたときここだけ取り残される） | 小 |

---

## 2026-08-13 追記 — `v4-live-sales.dc.html`（案件管理の最新モック）との突き合わせ

**きっかけ**: `docs/design/v4/mockups/README.md` は「案件管理（受付〜詳細〜見積）は
`v4-mockup-main.dc.html` ではなく `v4-live-sales.dc.html` を見ること」と明示しているが、
**`docs/design/v4/projects.md` を作る `scripts/extract-v4-design.mjs` は今日まで
`v4-mockup-main.dc.html`（古い）しか読んでいなかった**。つまりこれまでの案件管理の
v4 化は、すでに古くなった仕様書を見て進めていたことになる。

`v4-live-sales.dc.html` は `dash`（ダッシュボード）／`new`（案件作成）／`list`（案件一覧）／
`detail`（案件詳細）の4画面だけを高忠実度で持つ（ファイル冒頭の `const BUILT` がそう
宣言している）。**タスク一覧・見積・請求（全案件）・料金表・標準工程テンプレートは
このモックに中身が無い**（サイドバーのラベルと件数だけの飾り）ので、旧モック
（`v4-mockup-main.dc.html`）どおりのままでよい。

### 見つけて直したもの（小・すべて済み）

| モック | 直す前 | 直したこと |
| --- | --- | --- |
| 「自動で届いたもの」レール・「いま必要な5つ」枠・会場の注記（3か所）の色 | `info`（藍・`#c7d2fe` 系） | `primary`（GMOブルー・モック実測 `#cfe4f4`）に統一（`IntakeRail.tsx` / `NewProjectDialog.tsx` / `MoreFields.tsx`） |
| お客様の選択欄のプレースホルダー「会社を選ぶ」 | 「会社を探す」 | 文言をモックに合わせた（`RequiredFields.tsx`） |
| ダッシュボード「止まっている案件」の見出し帯 | 枠線だけ赤 | モック実測どおり見出し行にピンクの帯（`destructive-surface` 背景 + `destructive-border` の下罫線）を追加（`Panel.tsx`） |
| 案件作成（スマホ）最終段のボタン | 「案件にする（与件化）」 | 「つくる」1語に（モック。PC の見出しボタンは長い文言のまま） |
| `tabs.ts` の「ふりかえりは絵が無い」という説明 | 古いモック時点の記述 | **もう事実と違う**（最新モックに KPT・写真・お金のふりかえり一式があり、実装は既に近い）。コメントを訂正した |

### 要判断 → ご判断いただき実装済み（2026-08-13）

1. **「エピソード（回）」タブが最新モックの見えるタブバーに無い問題 → タブごと外した。**
   `PROJECT_TABS` から `episode` を削除し（`tabs.ts`）、`DetailHeader.tsx` /
   `ProjectDetailPage.tsx` から `isSeries` によるタブ出し分けも削除した。
   ただし**回を新しく作る手段がこのタブにしかなかった**（`EpisodeScopeToggle` も
   スタジオ予約ダイアログも「既存の回から選ぶ」だけ）ため、モックが持っていた
   簡易表（回・名前・実施日・タスク進捗・状態）と「回を足す」ボタンを
   **タスクタブの回の絞り込みのすぐ隣**に移設した
   （`client/src/contexts/tasks/components/EpisodesPanel.tsx` 新設）。
   状態（未着手／進行中／完了）はタスクの完了件数から自動で導く
   （`GET /projects/:id/episodes` に `task_count` / `task_done_count` を追加）。
   古い `/sales/projects/:id/episode` URL は `ProjectTabKey` に無い値として
   自然に概要タブへフォールバックする（追加の転送コードは書いていない）
2. **見積タブの「売上・請求」ペインの設計差 → モックの3カード設計に置き換えた。**
   `BusinessProjectView.tsx`（2,042行のフル機能会計コンソール）呼び出しをやめ、
   `RevenueBillingPane.tsx`（新設）に差し替えた。売上／請求（`invoice_issued`）／
   仕入（原価）の3枚のカード＋確定売上・仕入・粗利の合計行、**読むだけ**（モックどおり
   行を押しても編集は開かない）。月次ユニット作成・PDF/Excel出力・明細のインライン
   編集は**畳んだ**——既存の財務台帳（`/budget/revenues` `/budget/purchases`）と
   ⑤ 見積・請求（全案件）にすでにある機能なので、同じ表を2か所で編集できるようにする
   デメリット（片方だけ直された行ができる）を避けた。呼び手が無くなった
   `LegacyViewTab.tsx` は削除。**`BusinessProjectView.tsx` 自体は残す**——
   プロジェクト管理（GPM）の見積タブ（`contexts/gpm/pages/projectDetail/BillingTab.tsx`）
   がいまも直接呼んでいる
3. モック未満に見えるが**意図的な拡張だった**もの（ここでは変えていない）:
   - ダッシュボードの各パネルの補足文（「受注に近い順」等）— `StagePanel.tsx` 自身が
     「モックは補足そのものが無い」と明記したうえで加えている
   - 案件作成の各項目のヒント文・スマホの「ネタのまま残す／見送りにする」ボタン —
     後者は「消すと受付でできていたことができなくなる」と明記済み
   - 案件一覧の「AI・未確認」トグル／用語ヒントの追加行／ネタの状態表記（4種）／
     モバイルのリスト・ボード・ネタ切替 — 実務上の価値がありそうな拡張で、
     はぎ取る前に確認が要る
   - 案件一覧ボードの列順（受注に近い順に統一）と記号つきラベル — `ProjectBoard.tsx`
     に既存の理由コメントあり（意図した差）

---

## 2026-08-14 追記 — 概要タブの「事実の帯」を4列から 3列＋1行にした（意図した差）

**きっかけ**: 利用者からのご指摘「次にやることが、文字切れしてますね」。

モックの帯は**4列で、値は1行に収めて省略記号で切る**
（`white-space:nowrap; text-overflow:ellipsis`）。この形が成り立っていたのは
**モックの値が「田中：見積を送る」の8文字**だったからで、本番の
`activity_logs.next_action` の言い切り1文は
「★8/14(金)までに 8/28分の備品レンタル発注可否を確定し発注する(発注期日 8/19 は…)」の
ように **40〜60字**ある。実測すると 1440px でも1枠 200px（12〜13字）しかなく、
**2行に折り返しても半分以上が読めない**。

→ **実施日／会場・スタジオ／見積金額の3列＋「次にやること」だけ1行ぶん**にした。
798px 取れるので**本番データが1行で入り、切れがゼロになる**（390px では3行）。
文を縮める・隠す・要約するより幅をやるほうが素直で、**1文字も落とさない**
（`OverviewTab.tsx` にも理由を書いた）。帯の高さは PC で 84 → 約 135px。

**同じ回で直した、モックとは無関係の不具合2件**（どちらも実 Postgres ＋
実サーバー ＋ 実ブラウザで再現を確認した）:

1. **会場・スタジオが必ず「部屋 未設定」になっていた。** 画面は
   `booking.room_name ?? booking.location_name` を読んでいたが、
   **`GET /studios/bookings` はその2つを1つも返さない** — 部屋は `rooms[]`
   （`studio_booking_rooms` → `studio_rooms`）、外現場は `location_note` にある。
   `types.ts` の `StudioBooking` が**存在しない項目を宣言していた**ので、
   型チェックにも lint にも出なかった。組み立ては `projectDetail/venue.ts` に集約し、
   型を実際に返ってくる形に合わせた（`shared/tests/venue.test.ts` で固定）
2. **右の欄「押さえている部屋」の日付が空行だった。** 同じく存在しない
   `booking_date` を描いていた（正しくは `start_time`）

**モックに合わせたもの**: 見積金額の `¥`。モックの帯は
`display:flex; align-items:baseline; gap:4px` で**¥ を数字のすぐ左**に置くが、
実装は `<Money>` の既定（列で桁をそろえるため枠いっぱいに ¥ と数字を引き離す形）を
使っていたため、**円記号だけが遠くに離れて見えていた**（ご指摘）。
`<Money inline>` を足して 4px にそろえた（実測 4px）。

---

## 進め方

**小（1日以内）→ 中 → 大** の順に片づけます。小はモックとの見た目の差なので、
先に潰すと**検証環境で「モックとどこが違うか」が読める状態**になります。

- **小 14件** — 用語・色・タブ・メニュー・タイルの出し入れ。DB 変更なし
- **中 13件** — 列を1〜4本足す。マイグレーション番号は **163 から**
  （137〜157 は v3.2.0 のロールバックで二重に使われているので再利用しない）
- ~~**大 5件**~~ — **5件とも完了** 案件作成モーダル（大①）・入ってきた情報（大②）・
  料金表の拠点別（大③）・ラック図（大④）・GPM 見積（大⑤）

**要判断の4件**（①）は4件ともご判断をいただいて実装済みです。

---

## 残っているもの

この文書の作業一覧のうち、**表に取り消し線が付いていない行**が残りです。
着手するときは、その行の「やること」をそのまま読んでください。
