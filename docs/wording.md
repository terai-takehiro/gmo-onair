# 画面に出す言葉のルール

UI/UX 刷新 (v2.9.251〜) で決めた文言ルール。**PR レビューのチェック対象**。
画面に出るテキスト (ボタン・見出し・説明・エラー・トースト・メール本文) すべてに適用する。
コード内のコメントと変数名は対象外。

---

## 5つのルール

### 1. 1文に1つのことだけ書く

同じ情報を対象ごとに書き分けない。「AI が作った」という1つの事実に4通りの表記を作ると、
読む人には**表記の違いに意味があるように見えてしまう**。

| ✕ | ○ |
| --- | --- |
| AI起票 / AI取込 / AI登録 / AI (対象ごとに4通り) | **AI作成** (すべて統一) |

### 2. 次にやることを先に書く

状態の説明より、その人が次に押すもの・確かめるものを先に置く。

| ✕ | ○ |
| --- | --- |
| この案件は AI（問い合わせメール取込等）により起票されました。 | AI がメールから作った案件です。内容が合っているか見てください。 |
| 確定するまで想定金額には反映されません。 | 「確定する」を押すと想定金額に入ります。 |

### 3. 期限は「何月何日何時何分まで」

「今週中」「なるべく早く」「金曜まで」は**期限として成立していない**。
朝か夜かが決まらないものは期限ではない。日付だけ選ばれたときは 18:00 を補い、**補ったことを画面で示す**。

- 聞き方でも示す: 「いつまで？」ではなく **「何月何日何時何分まで？」**
- AI が曖昧な期限を読んだときは、黙って補完しない。補完した印を出して人に確認させる。
- 期限が読めない依頼も**捨てない**。残して期限を聞く。

### 4. 数字で書き、感想を書かない

「たくさん」「かなり」「だいぶ改善」は書かない。件数・金額・率・日時で書く。

| ✕ | ○ |
| --- | --- |
| 未対応の問い合わせが多数あります | 未対応の問い合わせ 7件 |
| だいぶ待たせています | 最も古い待ち 2日 3時間 |

### 5. 技術用語を画面に出さない

**禁止語** (画面テキストに出したら PR で落とす):

`MCP` / `監査ログ` / `actor` / `共用キー` / `トークン` / `500` / `タイムアウト` /
`Internal Server Error` / `Network Error` / `null` / `undefined` / `JSONB` / `SQL` /
`エンドポイント` / `ペイロード` / `スナップショット`

内部の詳細は**消すのではなくツールチップ (title 属性) に退避**する。ただし退避先も画面テキストなので、
用語そのものは平易な言い方に置き換える。

| ✕ | ○ |
| --- | --- |
| 実行: 共用キー ／ 指示: 寺井武大 | AI が自動実行 |
| AI (MCP 経由) が実行した書き込みの監査履歴です | AI が作ったり直したりした記録です。 |
| 500 Internal Server Error | サーバー側で処理が止まりました。少し待ってから、もう一度お試しください。 |
| 429 Too Many Requests | 短い時間に何度も読み込んだため、少し待つ必要があります。10 秒ほど待ってから、もう一度お試しください。 |
| 504 Gateway Timeout | 時間内に読み終わりませんでした。期間や案件で絞ってから、もう一度お試しください。 |

エラー文の組み立ては `shared/src/client/states/ErrorPanel.tsx` の `humanizeError()` に集約してある。
**例外オブジェクトのメッセージを画面にそのまま出さない**こと。

---

## 開くか、残すか (2026-09-05 追加)

**ルール1〜5 は「どう書くか」だけを決めていて、「その語を開くかどうか」を決めていなかった。**
そのため現場では「とにかく開く」に倒れ、開いた語が別の語とぶつかったり
（按分グループ ⇔ 費用を分け合うグループ）、新しい造語になったり（レイヤー・投入口・月次ユニット）していた。

判断の材料にした調査は [docs/reviews/2026-09-05-terminology-review.md](reviews/2026-09-05-terminology-review.md)
（画面文字列 約6,500件の棚卸し＋Salesforce・Slack・Outlook・Google カレンダーほか8サービスの日本語UI調査）。

### ルール6: 開くかどうかは「画面の外で通用するか」で決める

**難しさでは決めない。** Salesforce は「商談」「取引先責任者」と開きながら
「フェーズ」「確度」「パイプライン」は残している。線は難易度ではなく**通用範囲**で引かれている。

| 条件 | 判定 | 例 |
| --- | --- | --- |
| 社外・帳簿・請求・契約・法令に出てくる | **残す** | 按分・検収・販管費・限界利益・売上総利益・税区分・適格事業者・役務提供完了日・GLS番号 |
| 業界標準語で、他社ツール・書籍・転職先でも同じ語 | **残す** | ステージ・パイプライン・確度・ガント・カンバン・ランダウン・プロンプター・香盤・尺・稿・押し／巻き・TAKE／PVW・ラック・U位置 |
| ONAiR の中でしか意味を持たない機能名・状態・操作 | **開く** | 未仕分け・今日さばくもの・見るだけ／書ける・仮押さえ |
| 単独では何のことか分からない | **語を足す**（開くのではない） | 「ルール」→「貸出のルール」、「レイヤー」→「出すもの」 |

**開いた結果が新しい造語になっていたら失敗**。造語は検索されず、引き継ぎで通じない。

### ルール7: ラベルは12文字まで。超えたら副題に落とす

**名前の場所に説明文を置かない。** 短く呼べない名前は、口頭でも検索でも指せない。

| ✕ | ○ |
| --- | --- |
| 費用を分け合うグループ（左メニュー・11文字） | **按分グループ** ＋副題「複数案件にまたがる費用・売上を案件ごとに分ける」 |
| アーカイブする（一覧から隠す。消えません） | **一覧から隠す**（対は「一覧に戻す」） |
| 売上・請求への登録を取り消して見積もりに戻す | **売上登録を取り消す** |
| 先行タスク（これが終わってから着手する） | **先行タスク**（定義は直下の説明文に） |
| あとから足された機材を取り込む | **機材を取り込む** |

### ルール8: 動詞は1組に固定する (標準語系)

**追加 / 編集 / 削除 / キャンセル / 保存** に統一する。
和語系（足す・直す・消す・やめる）とは**混ぜない**。

以前は和語系と標準語系が並走していた（実測: やめる 63 / キャンセル 27、追加 31 / 足す 13、
編集 11 / 直す 16、削除 11 / 消す 21）。分岐線は「v4 で作り直した画面かどうか」とほぼ一致しており、
**作業の切れ目がそのまま語彙の切れ目**になっていた（利用者から見ると理由なく言葉が変わる）。

- **業務上の意味がある動詞は残す**: 取り消す・複製する・提出済にする・稿を上げる・配布をやめる
- **進行中は「動詞＋中…」**（三点リーダは `…`。`...` は使わない）
- **失敗は `〈対象〉を〈動詞〉できませんでした。` ＋ 次の一手1文**。「〜に失敗しました」は使わない

### 文型は3つだけ

```
エラー   : 〈対象〉を〈動詞〉できませんでした。〈次の一手〉。
空状態A  : まだ〈対象〉がありません。〈〜すると、ここに出ます〉。[ 〈対象〉を追加 ]
空状態B  : 条件に合う〈対象〉はありません。〈条件を変えてお試しください〉。
権限     : 〈アプリ名〉の「〈見るだけ／書ける／管理〉」が必要です。
```

正は `shared/src/client/states/ErrorPanel.tsx` の `humanizeError()` と
`shared/src/client/states/NoPermissionPanel.tsx`。**新しい型を作らず、ここに寄せる。**
`EmptyState` の `title` は**必須**（既定値「データがありません」は 2026-09-05 に廃止した。
渡し忘れると、このルールが名指しで禁止した文が出てしまうため）。

---

## 言い換え表

| いまの言い方 | これから | なぜ |
| --- | --- | --- |
| 実行: 共用キー | AI が自動実行 | 認証方式は業務に関係ない。「特定の人ではない」という意味だけ伝わればよい |
| 指示: 寺井武大 | （出さない。「AI が自動実行」だけ） | `requested_by` は **AI が名簿と突き合わせず自由記述で書く値**で、実際に「寺井 赳博」さんが「寺井武大」として記録されていた。人名として出すと**実在しない人を断定的に見せてしまう**（`shared/src/client/aiAttribution.ts`／検査 `ai-person-name`） |
| この案件は AI により起票されました | AI がメールから作った案件です。内容が合っているか見てください。 | 次にやることを先に書く |
| 確定するまで想定金額には反映されません | 「確定する」を押すと想定金額に入ります。 | 二重否定をやめる |
| 内容を確認済みにする | 確認した | 短く |
| ~~エピソード~~ | ~~回（第1回・7月分）~~ | **2026-08-07 に取り消し。** v4 のモックがタブ名として「エピソード」を使っており、モックが v4 の正だと決まったため（`docs/v4-plan.md` の「大前提」）。**ルールのほうをモックに合わせた** |
| ビジネス案件（GLS-B） | プロジェクト | **GLS-B はプロジェクト管理へ移った**（migration 179・`docs/design/gpm-merge.md` 決め⑪）。同じものを指す言葉が2つ残ると、移ったことが伝わらない。**内部の記号（`gls_category` の `A`/`B`）は変えていない** — 画面に出ないので、変えても利用者には何も起きず触る箇所が増えるだけ |
| スタジオ案件（GLS-A） | 案件 | 上と対。GLS-B が案件管理から出たので、案件管理の中で言い分ける必要がなくなった |
| 確定案件（ビジネス） | プロジェクト一覧 | 置き場所（プロジェクト管理）と一致させる |
| ~~按分グループ~~ | ~~費用を分け合う案件のまとまり~~ | **2026-09-05 に取り消し。** 按分は帳簿・請求で使う会計の標準語で、開くと freee の総勘定元帳や社外とのやり取りと突き合わせられなくなる（ルール6）。開いた結果「按分グループ」と「費用を分け合うグループ」が同時に生き、**同じものに名前が2つ**できていた。`scripts/check-ui-tokens.mjs` の禁止語からも外した |
| 二重計上スクリーニング | 同じ支払いが2回入っていないか調べる | 何をする機能なのかを名前にする |
| AI 活動フィード / AI 活動履歴 | AI がやったこと | 表記を1つに |
| データがありません | (対象ごとの文 + 次の一手) | 何が無いのか・次に何をするのかが分からない |
| アクセス権限がありません | (必要な権限名) の「(レベル)」が必要です | 何を依頼すればよいか分かる |
| 該当なし | 当てはまるものはありませんでした | 体言止めをやめる |
| 音声サポート画面 共有 URL | 本番のURLを配る | 何をする操作なのかを名前にする |
| 公開URL · リアルタイム同期中 (常時緑) | 同期中 / 放送同期が切断されました | **状態と関係なく出す表示は嘘になる**。切れているときは切れていると出す |
| ゴミ箱 | ゴミ箱と直前の変更 | 保存前に効くのはどちらかを名前で示す |
| 列 (サイドバーのタブ) | 出す列 | 「追加・削除する」のか「いま見る列を選ぶ」のかが分かれる |
| ユーザー管理 | 人と権限 | 「管理」だけでは何をする画面か分からない |
| 権限設定 | この人にできること | 設定の対象 (人) と結果 (できること) を名前に出す。※実装は「人と権限」の中の1画面に畳んだ |
| reader / editor / manager | 見るだけ / 書ける / **管理** | DBの値は変えず、画面の言い方だけ日本語にする。**2026-09-05 に `manager` を「任せる」から「管理」へ**——「任せる」は強さの順序が読めず、AI の「AI に任せる」とも衝突していた。正は `shared/src/client/states/NoPermissionPanel.tsx` の `LEVEL_LABELS` の1か所だけ（以前は画面上に**4系統**あり、`manager` を「管理者」と呼ぶ画面が「システム管理者に依頼」と誤誘導していた） |
| 保存に失敗しました (409) | この案件は○○さんが先に保存しています。最新を読み込んでから直してください。 | 誰と競合したか・次に何をするかを書く。「失敗」だけでは打ち直せない |
| 重複の可能性があります | 同じ金額（¥330,000）の仕入が GLS-A004 にすでに 1 件あります。 | 金額と行き先を出す。**断定はしない** (同額の定額費用は普通にある) |
| 共有を解除 (音声サポート) | 配布をやめる | 止めるのは URL で、台本は消えない |
| 404 Not Found (無効にした配布URL) | この配布URLは無効になりました | 410 で返し「あったが今は無い」と伝える。404 だと URL を間違えたと思って探し続ける |
| AI起票の未確認案件 (Slack本文) | AIが作って、まだ人が見ていないもの | 社内語を Slack に持ち出さない。全社が読むので一番平易に書く |
| 次回アクション期限超過 (Slack本文) | 返事の期限が過ぎたもの | 誰が待っているかで書く |
| 通知設定 | 知らせと文面 | 何が届くのかを名前にする。**2026-09-05 に「朝の1通（Slack）」から変更**——社外に出す文面のコピー元も同じ画面が持っており、朝の1通だけではない |
| 添付ファイル / 添付書類 | 原本 | 何のためのファイルかを名前にする (承認のために原本を見る) |
| ファイルがありません | 原本がまだありません + その場の置き場所 | 無いことを言うだけで終わらせない |
| 返信を送信 | 保存する + 本文をコピー + 送った | **ONAiR はメールを送らない**。送信だと誤解されると「送ったつもりで届いていない」が起きる |
| 修正内容を入力してください | 直した理由（任意・AIの改善に使います） | 差分はサーバーが自動で取る。人に入力させる設計は続かない |
| 共同編集 / リアルタイム同期 | みんなで書くメモ + 保存ボタンはありません | 仕組みの名前ではなく、何ができるかで書く |
| 接続エラー (同時編集) | 同時編集に繋がりませんでした。いまは読むだけにしています | 黙って編集させると「書いたのに保存されていない」になる |
| 他のユーザーが編集中です | いま編集中: 案件名（寺井 岳宏） | 誰がどの欄かまで書く。保存は全項目まとめてなので、別の欄でも相手の変更を消す |
| メンション | 知らせる人（この人のベルに出ます） | 何が起きるかを書く。「メンション」だけでは相手に何が届くのか分からない |
| 既読にする (コメントの知らせ) | 対応した（ベルから消す） | 読んだだけでは何も終わっていない。ベルから消えるのは本人が対応したときだけ |
| 書く (コメントの送信) | コメントする | すぐ上に「やり取りを記録」があるので、どちらに書くのか分かる言葉にする |
| 更新履歴 / 監査ログ (案件) | 変更の記録 | 誰が・いつ・何を変えたかを見る場所だと分かる名前にする |
| (印なしで AI が足した行) | AI の印 + 「要らなければ消してください」 | 見分けが付かないと「勝手に増えている」になり、消すか残すかの判断ができない |
| 計時LIVE | 計時・視聴者 | タイマーだけでなく視聴者数カウンターも扱うアプリだと分かる名前にする（`docs/design/v4/qsheet-v4-coding/12-live-timer-decision.md` フェーズ1・PR-B。ディレクトリ名 `client-live/`・URL `/live/`・`permissionModule: 'liveops'` 等の内部識別子は変えていない） |
| AI が自動更新しました | ── AIが追記（日時）指示: ○○ ── | いつ・誰の指示で足したのかまで書く。既存の文には触っていないことも伝わる |
| アクセスなし | 見えない | 「メニューにも出ない」ことが伝わる |
| 報告資料 / 営業レビュー / 週次 | ふりかえり（タブで切り替え） | どれも「終わったことを見る」画面なので入口を1つに |
| 足す / 直す / 消す / やめる | **追加 / 編集 / 削除 / キャンセル** | ルール8。和語系と標準語系が並走し、v4 で作り直した画面かどうかで言葉が変わっていた |
| 〜に失敗しました | 〈対象〉を〈動詞〉できませんでした。＋次の一手 | 「失敗」だけでは打ち直せない。`shared/src/client/queryClient.ts` の共通文もこの形にした |
| 決める人 / 進める人 / 手を動かす人 | 決裁層 / 推進層 / 実務層 | 発注者・PM会社と共有する体制図に出る。「手を動かす人 3名」は相手に対して失礼にも読める |
| レイヤー（カレンダーの重ね表示） | 出すもの | 「レイヤー」はデザイン用語。Google・Outlook は「カレンダー」と呼ぶ |
| 投入口 / 投入ログ | 書き留めたもの | 「投入」は造語。実物のカードは「依頼・タスクを書き留める」と名乗っている |
| 月次ユニット / 月ユニット | 月ぶんの請求 | データモデルの名前がそのまま画面に出ていた |
| スコア / 9マス | 最優先 / 重要度 × 緊急度 | 内部の点数と表示形式の形が名前になっていた |
| 部屋を押さえる（新規予約） | スタジオを予約する | 「仮押さえ」と語幹が同じで、確定予約なのか仮なのか区別できない |
| 同期（機材の取り込み） | 機材を取り込む | 同じ操作が画面ごとに「同期」「あとから足された機材を取り込む」の2名だった |
| 刷る / 刷る列 | 印刷 / 印刷する列 | 同じアプリの他4箇所は「印刷」。開いた1画面だけが浮いていた |
| MCP コネクタ | AI につなぐ | ルール5の禁止語が**全アプリの本人メニュー**に出ていた |
| セッション（計時・視聴者） | 番組 | 現行 UI は「番組」。マニュアルと旧一覧だけが古い語のまま残っていた |
| Qシート / ドキュメント / 台本データ / 資料 | 進行台本 | `shared/src/production/miniapps.ts` が「Qシートは内部識別子のみ」と決めているのに、1画面に4語が同居していた |
| 保管場所 / 設置場所 / 置き場所 / 場所名 | 保管場所 | 同じ列に4つの名前が付いていた |
| シリアル / シリアルNo / S/N | 製造番号 | 同上 |
| 型名 / 型番 | 型名 | 同上。しかも `機材名 (型番)` が別項目（`model_number`）に付いていた |

---

## 残す言葉 (社内語)

次は**社内の共通語なので残す**。ただし**初めて出る画面に1文の説明を必ず置く**。

| 語 | 説明文の例 |
| --- | --- |
| ネタ | まだ提案していない、見込みの段階。 |
| ヨミ | 受注の見込み。ネタから受注までの途中の段階。 |
| GLS番号 | 経理・請求で使う案件の番号。 |
| 押し / 巻き | 本番の進行が予定より遅れている / 早いこと。 |
| 稿 (第3稿) | 配った版の番号。「稿を上げる」を押したときだけ増える。 |
| ロール | 台本のかたまり (オープニング・受賞発表 など)。 |
| キープ | 隔週の報告会。ここに出す資料を「報告資料」と呼ぶ。 |

案件一覧の「用語」ボタン (`ProjectListPage`) がこの役目を担っている。

---

## 文字の大きさと色

- 補助テキストの**下限は 12px**。これより小さい文字は使わない。
- 補助テキストの色は **`#5d6470` (`text-secondary-foreground` / `text-muted-foreground`) 以上の濃さ**。
  `#8b929c` は白背景でコントラスト 3.08:1 しかなく AA を満たさないため**使用禁止** (装飾アイコンのみ許可)。

---

## チェックの仕方

**正は `npm run lint` の `scripts/check-ui-tokens.mjs`**（`forbidden-wording` の検査）。
下は手元で当たりを付けるための grep で、**門はスクリプトのほう**です。

```bash
# 禁止語が画面テキストに出ていないか (コメント行を除く)
grep -rn "共用キー\|MCP\|監査ログ\|論理削除\|Internal Server Error\|Network Error\|データがありません" \
  --include=*.tsx client*/src shared/src | grep -v "^\s*//" | grep -v "\* "

# 英語のロール名が画面に漏れていないか (reader/editor/manager/owner)
grep -rn "\b\(reader\|editor\|manager\|owner\)\b.*権限" --include=*.tsx client*/src shared/src

# 動詞が2系統になっていないか (和語系が残っていたら標準語系へ・ルール8)
grep -rho ">\(足す\|直す\|消す\|やめる\)<" --include=*.tsx client*/src shared/src | sort | uniq -c

# 「〜に失敗しました」が残っていないか (ルール8)
grep -rn "に失敗しました" --include=*.tsx --include=*.ts client*/src shared/src | grep -v "^\s*//"

# 三点リーダが `...` になっていないか
grep -rn '\.\.\.<' --include=*.tsx client*/src shared/src

# 使用禁止の色
grep -rn "8b929c" --include=*.tsx --include=*.css client*/src shared/src
```

### baseline を増やさない

`scripts/check-ui-tokens.mjs` は**既存の違反件数を baseline として持っています**。
`forbidden-wording` は 2026-09-05 の棚卸し時点で 40件超を容認しており、**事実上ザルでした**。
新しく足すときは baseline を**増やさず**、直してから通してください。
